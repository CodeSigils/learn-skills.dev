多媒体音频/视频/图像处理工具（FFmpeg/ImageMagick）。当用户需要视频格式转换（MP4/WebM/MOV/AVI/MKV等）、视频压缩优化（CRF编码/网页预设/存档预设）、视频修剪/剪切（trim/cut/slice）、音频提取（MP3/WAV/FLAC等）、视频缩放和分辨率调整（resize/scale）、视频水印（水印/徽标/文字叠加）、图像格式转换时立即使用(JPEG/PNG/WebP/GIF/TIFF/BMP)、图像压缩优化(质量压缩/文件瘦身)、图像缩放和裁剪(调整大小/裁剪/缩略图)、多媒体文件批量处理(批处理)、生成视频缩略图(缩略图/屏幕截图)、处理GIF动画(转换/压缩/优化)、提取视频帧(帧提取)、调整视频码率/帧率(bitrate/fps)、旋转视频/图像（旋转/翻转）、音频格式转换以及相关任务。跨平台支持（Windows/macOS/Linux），JSON输出格式，方便程序集成。
