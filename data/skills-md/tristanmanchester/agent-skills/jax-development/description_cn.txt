当用户编写、调试、分析、重构、审查、基准测试、并行化、导出或解释 JAX 代码时，或者当他们提到 JAX、jax.numpy、jit、grad、value_and_grad、vmap、scan、lax、随机键、pytrees、jax.Array、sharding、Mesh、PartitionSpec、NamedSharding、pmap、shard_map、Pallas、XLA、 StableHLO、checkify、profiler 或 JAX 存储库。它有助于将 NumPy 或 PyTorch 风格的代码转换为纯功能性 JAX，修复跟踪器/控制流/形状/PRNG 错误，删除重新编译和主机设备同步，选择转换和分片策略，检查 jaxpr/lowering/IR，并正确对编译代码进行基准测试。
