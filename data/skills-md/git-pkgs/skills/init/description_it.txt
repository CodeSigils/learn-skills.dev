Inizializza, reindicizza o gestisci il database delle dipendenze git-pkgs. Da utilizzare quando si configura git-pkgs in un repository per la prima volta, si aggiorna il database dopo aver eseguito nuovi commit o si controlla lo stato del database.
