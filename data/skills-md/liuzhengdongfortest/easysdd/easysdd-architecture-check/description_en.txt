Do an architecture health check, choose one of three options: check if a single design document is internally consistent (terminology, contracts, and execution steps don't conflict); check if design and code match (promised items are actually implemented in code); check if multiple architecture documents under `easysdd/architecture/` are consistent with each other (terminology, module boundaries, and cross-document references don't conflict). This skill only produces issue lists and fix recommendations, does not make changes. Lock onto one target at a time, no "might as well check the other one while we're at it" allowed. Trigger scenarios: user says "do architecture check", "is design internally consistent", "do plan and code match", "are documents in architecture folder conflicting with each other", or wants to do a health check before proceeding during implement / acceptance phase.
