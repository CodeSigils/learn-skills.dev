Feature process phase 3 — complete the acceptance loop. Two tasks: first, layer-by-layer verification against {slug}-design.md to check if implementation deviated, fixing any discrepancies on the spot rather than "noting them down" in a report; second, merge this feature into the project's overall architecture documentation. Finally produce a {slug}-acceptance.md as the closing credential for the entire process. Prerequisite: easysdd-feature-implement completed. Trigger scenarios: user says "feature is done, acceptance check", "final check", "ready to merge", or "generate acceptance report".
