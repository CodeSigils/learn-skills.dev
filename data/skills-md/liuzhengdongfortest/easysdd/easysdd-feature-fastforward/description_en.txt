A super lightweight channel for the feature process—no design doc, no checklist, no staged reviews. Just let AI write code directly as usual, but before it starts, tell it where the project's easysdd knowledge base is and how to search it, so the code it writes avoids more pitfalls and stays more consistent with project conventions. Trigger scenarios: user says "quick mode", "fastforward", "skip the steps", "just do it", "help me build an xxx" and the requirement is too small to warrant the design process.
