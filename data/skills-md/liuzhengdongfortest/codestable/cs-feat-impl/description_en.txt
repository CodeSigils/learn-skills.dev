Feature process stage 2—write code in the order specified in {slug}-design.md, and after finishing, report completion in a unified format for user review. Prerequisite: {slug}-design.md has been approved (standard design includes test design, or fast-forward design includes acceptance criteria), and {slug}-checklist.yaml exists in the same directory. Trigger: user says "方案确认了开始实现", "按方案写代码", or "开工". If during implementation you encounter anything not covered by the design (new concepts, out-of-scope files, need to patch branches), stop proactively and return to the design for discussion—do not force ahead.
