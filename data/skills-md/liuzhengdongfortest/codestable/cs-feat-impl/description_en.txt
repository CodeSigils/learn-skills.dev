Feature process phase 2 — write code following the sequence in {slug}-design.md, then report completion to user for review using a unified format. Prerequisites: {slug}-design.md is already approved (standard design includes test design, or fastforward design includes acceptance criteria), and {slug}-checklist.yaml exists in the same directory. Trigger scenarios: user says "plan confirmed, start implementation", "write code according to plan", or "let's begin". If encountering situations not covered by the plan during implementation (new concepts, out-of-scope files, need for patch branches), proactively pause and return to plan discussion — do not push forward blindly.
