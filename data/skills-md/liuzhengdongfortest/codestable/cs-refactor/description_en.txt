做代码优化时进入这套子流程——处理"行为不变、结构变"的工作（结构 / 性能 / 可读性），把单模块内部的优化从"AI 胡乱重构"改成"先扫出清单、和用户逐条确认、按方法库分步执行、每步人工放行"。触发场景：用户说"优化一下 / 重构 / 重写 / 拆一下 / 性能不行 / 代码太长"这类，且不夹带行为改动。不处理新需求（走 feature）、不处理 bug（走 issue）、不处理跨模块架构重划（走 architecture + decisions）。
