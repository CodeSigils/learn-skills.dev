When optimizing code, enter this sub-process—handle “behavior unchanged, structure changed” tasks (structure / performance / readability). Replace the “AI refactors blindly” approach with: first scan out a checklist, confirm each item with the user, execute step-by-step via the method library, and get human approval at every step. Trigger: user says “optimize / refactor / rewrite / split / performance is poor / code is too long” without behavior changes. No new features (use feature), no bugs (use issue), no cross-module architecture reshaping (use architecture + decisions).
