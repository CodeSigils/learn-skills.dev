When optimizing code, enter this sub-process—handling work where "behavior stays the same, structure changes" (structure / performance / readability). Replace the "AI randomly refactors" approach with "first scan to generate a checklist, confirm each item with the user, execute step-by-step using the method library, human approval at every step." Trigger scenarios: user says "optimize / refactor / rewrite / split / poor performance / code too long" without any behavior changes. Does not handle new requirements (use feature), does not handle bugs (use issue), does not handle cross-module architectural reshuffling (use architecture + decisions).
