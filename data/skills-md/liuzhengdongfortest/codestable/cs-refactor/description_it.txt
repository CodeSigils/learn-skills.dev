Quando si ottimizza il codice, inserire questo sottoprocesso: gestire il lavoro in cui "il comportamento rimane lo stesso, la struttura cambia" (struttura/prestazioni/leggibilità). Sostituisci l'approccio "Refactoring casuale dell'intelligenza artificiale" con "prima scansione per generare una lista di controllo, confermare ogni elemento con l'utente, eseguire passo dopo passo utilizzando la libreria di metodi, approvazione umana ad ogni passaggio". Scenari di attivazione: l'utente dice "ottimizza/refactoring/riscrivi/dividi/prestazioni scarse/codice troppo lungo" senza alcuna modifica del comportamento. Non gestisce i nuovi requisiti (utilizza funzionalità), non gestisce i bug (utilizza il problema), non gestisce il rimpasto architetturale tra moduli (utilizza architettura + decisioni).
