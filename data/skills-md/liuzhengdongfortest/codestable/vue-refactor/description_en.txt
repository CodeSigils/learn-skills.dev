Vue Component/Composable Refactoring Sparring Partner—user pastes a snippet or points to an SFC; the skill first gives a diagnosis (“fat trunk” / “UI & IO tangled” / “reactivity & business logic tangled”), then picks one of three prescriptions and lists the exact extract-step sequence (which variable to move first, what compiler error will appear, how to eliminate them one by one, when you can rollback). The whole process relies on compiler green lights + single-step rollback to guarantee behavioral equivalence, no test safety net required. Trigger phrases: user says “this Vue component is too fat / want to extract logic / split this SFC / this composable is messy / extract a composable / split into humble / make it pure-functional”, or points to an obviously oversized .vue / composable file and asks for “refactor / optimize / split”. Only handles Vue (Vue 2 Options, Vue 2/3 `<script setup>`, composable, Pinia store). Won’t do: new features (use feature flow), bug fixes (use issue flow), cross-module architectural reshuffle, backend code.
