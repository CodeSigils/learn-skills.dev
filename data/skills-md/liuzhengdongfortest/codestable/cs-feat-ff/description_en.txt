An ultra-lightweight lane for the feature process—no design doc, no checklist, no phased reviews; just let the AI start coding as usual, but first tell it where the project’s CodeStable knowledge base is and how to search it, so the code it writes steps on fewer landmines and sticks closer to project conventions. Trigger: the user says “quick mode,” “fastforward,” “skip the steps,” “just do it,” or “build me an xxx” for a demand too small to justify the design flow.
