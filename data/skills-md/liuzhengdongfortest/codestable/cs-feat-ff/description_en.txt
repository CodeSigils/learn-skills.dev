An ultra-lightweight lane for the feature process—skip writing design docs, checklists, or phased reviews; just let the AI start coding as usual, but first tell it where the project’s CodeStable knowledge base is and how to search it, so the code it writes steps on fewer landmines and sticks closer to project conventions.  
Trigger: the user says “quick mode”, “fastforward”, “skip the steps”, “just do it”, or “build me an xxx” and the request is too small to justify the full design flow.
