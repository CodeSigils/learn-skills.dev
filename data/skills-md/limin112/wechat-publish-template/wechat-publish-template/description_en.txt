Convert a Markdown article into a "personal tech style" WeChat public account HTML (orange‑black cyberpunk style). When the user provides a .md file path or pastes a block of Markdown and says “convert to public account,” “format for public account,” or “apply public account template,” trigger the conversion. The output is a single HTML file with all inline styles, which can be opened directly in a browser, then selected, copied, and pasted into the WeChat public account editor. Do not trigger this when the user is only asking questions like “how to write a public account article” or “public account topics,” which are non‑formatting issues.
