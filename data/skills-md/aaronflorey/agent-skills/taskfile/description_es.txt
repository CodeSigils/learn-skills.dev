Cree, depure y refactorice la automatización `Taskfile.yml` o `Taskfile.yaml` utilizando go-task y la CLI `task`. Utilice esta habilidad siempre que el usuario mencione Taskfile, go-task, `task`, dependencias de tareas, inclusiones, variables, plantillas, almacenamiento en caché de `sources` o `generates`, archivos de tareas divididos en directorios o migración de Makefiles o scripts npm a Task.
