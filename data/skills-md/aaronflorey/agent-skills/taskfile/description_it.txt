Crea, esegui il debug e il refactoring dell'automazione "Taskfile.yml" o "Taskfile.yaml" utilizzando go-task e la CLI "task". Utilizza questa abilità ogni volta che l'utente menziona Taskfile, go-task, `task`, dipendenze delle attività, include, variabili, modelli, `sources` o `generates` caching, taskfile suddivisi in directory o migrazione di Makefile o script npm su Task.
