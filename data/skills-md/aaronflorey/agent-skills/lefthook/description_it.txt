Scrivi, esegui il debug e spiega la configurazione di Lefthook per gli hook Git. Utilizza questa abilità quando l'utente menziona `lefthook`, `lefthook.yml`, `lefthook install`, `pre-commit`, `pre-push`, filtraggio di file in stage o push, `jobs`, `commands`, `scripts`, sostituzioni locali o risoluzione dei problemi di esecuzione dell'hook e comportamento ricorsivo dell'hook.
