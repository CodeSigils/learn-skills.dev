일반 셸 패턴 대신 가장 좁은 CLI로 터미널 작업을 라우팅합니다. 사용자가 `rg` 또는 `fd`로 검색, `sd`, `comby` 또는 `ast-grep`으로 텍스트 다시 작성, `head` 또는 `less`로 출력 검사, `wget`으로 파일 가져오기, `jq` 또는 `yq`로 JSON 또는 YAML 변환, `fzf`로 항목 선택, `difftastic`으로 코드 비교, `shellcheck`로 lint 셸 또는 저장소 측정을 요청할 때 이 기술을 사용하세요. 'scc'.
