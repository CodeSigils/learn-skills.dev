ターミナル作業を汎用シェル パターンではなく最も狭い CLI にルーティングします。このスキルは、ユーザーが「rg」または「fd」で検索、「sd」、「comby」または「ast-grep」でテキストを書き換え、「head」または「less」で出力を検査、「wget」でファイルを取得、「jq」または「yq」でJSONまたはYAMLを変換、「fzf」で項目を選択、「difftastic」でコードを比較、「lintシェル」でシェルを実行するように要求したときに使用します。 `shellcheck` を使用するか、`scc` を使用してリポジトリを測定します。
