`vendor/bin/monorepo-builder` 및 `monorepo-builder.php`로 PHP 모노레포를 관리하려면 `simplify/monorepo-builder`를 사용하세요. 사용자가 monorepo-builder를 언급하고, 패키지 `composer.json` 파일을 루트 `composer.json`으로 병합하고, 패키지 간 버전을 검증하고, 패키지 디렉터리를 구성하고, 종속성을 전파하거나, monorepo 릴리스 및 릴리스 작업자를 자동화하는 경우 이 기술을 트리거합니다.
