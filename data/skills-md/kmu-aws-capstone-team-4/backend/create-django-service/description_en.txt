Create a Django service layer class. Use it for requests such as writing business logic, creating services, and implementing CRUD logic. Apply the BaseService (write) or BaseQueryService (read) pattern.
