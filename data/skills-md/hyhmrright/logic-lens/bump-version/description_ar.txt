قم بتوزيع إصدار Logic-Lens عبر جميع مواقع البيانات التعريفية الستة مرة واحدة (package.json، وبيانات المكونات الإضافية الأربعة، وشارة README)، ثم قم بالتحقق من صحتها. يُستخدم عند قص إصدار أو عندما يُبلغ `npm run validate` عن عدم تطابق الإصدار.
