전용 Herdr "백그라운드" 작업 공간 내에서 사용자가 제어할 수 있는 실시간 포그라운드 프로세스로 장기 실행 백그라운드 프로세스(웹 서버, LLM 프록시, 교육 작업, 평가 실행, 데몬, 감시자)를 실행합니다. 작업 영역 및 {목적}_N 탭을 자동으로 생성하고, 각 프로세스를 창에 배치하고(2x2 그리드에서 탭당 최대 4개, 새 탭), 수신 포트로 창 이름을 지정하고, 모든 출력을 로그/{목적}/에 지정하고, 프로젝트별 레지스트리(logs/launches.jsonl)에 모든 시작을 기록합니다. 사용자가 보고 제어할 수 있는 곳에서 계속 실행되어야 하는 서버, 서비스, 교육 또는 평가 프로세스를 시작, 실행, 시작 또는 배포하도록 요청할 때마다 사용합니다(启动服务, 后台跑训练, PART署代理). 일회성 명령이 아닌 일반 Bash를 사용하세요.
