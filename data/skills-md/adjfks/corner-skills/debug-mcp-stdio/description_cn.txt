用于在 VS Code 中断点调试 **Node.js stdio MCP Server**（附加到 Node Inspector）。每当用户提到：MCP stdio、@modelcontextprotocol/inspector、node --inspect/--inspect-brk、VS Code 附加调试、未命中断点、sourcemap、调试 build/dist 工件、端口 9229 冲突/占用、launch.json 配置等时，必须使用此技能。输出应直接提供复制粘贴就绪的 launch.json 片段、可运行的检查器启动命令（包括端口更改的版本）以及按优先级排序的故障排除清单。
