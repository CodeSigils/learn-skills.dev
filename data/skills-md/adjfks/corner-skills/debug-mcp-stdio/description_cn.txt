用于在 VS Code 中对 Node.js stdio MCP 服务器进行断点调试（附加到 Node Inspector）。每当用户提到：MCP stdio、@modelcontextprotocol/inspector、node--inspect/--inspect-brk、VS Code 附加调试、断点未命中、sourcemap、构建 build/dist 工件、port9229 冲突/占用、launch.json 配置等时，都应该使用此技能。输出应直接提供可复制粘贴的 launch.json 片段、可运行的检查器启动命令（包括具有备用端口的版本）以及按优先顺序进行故障排除清单。
