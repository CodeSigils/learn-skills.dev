When the code contains calls such as LocalDate.now() / LocalDateTime.now() / Instant.now() / new Date() / Date.now() / time.Now() that retrieve the “current time,” or involves week/month/day start points, cross‑timezone comparisons, or database time fields, it should be triggered. This prevents statistical errors caused by time‑zone drift in the production environment.
