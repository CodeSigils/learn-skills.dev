Triggered when the code involves queries inside loops, batch ID queries, IN clauses, BFS/recursive traversals, or nested service calls. It aims to prevent performance pitfalls such as N+1 queries, excessively long IN clauses, and recursive memory blowouts.
