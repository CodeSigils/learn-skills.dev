When designing or modifying REST API response structures, handling API return values, or generating downstream artifacts such as Excel/CSV/PDF/reconciliation files, it is triggered. This prevents problems caused by API design defects, such as field misalignment, type ambiguity, and the issue of key fields missing in generated artifacts while the process silently succeeds.
