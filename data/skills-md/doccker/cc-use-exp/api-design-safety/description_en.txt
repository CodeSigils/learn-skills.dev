Triggered when designing or modifying REST API response structures, handling API return values, or generating downstream products such as Excel/CSV/PDF/reconciliation files. It prevents issues such as field misalignment, type ambiguity caused by API design defects, and silent success when key fields are missing in generated outputs.
