Triggered when designing or modifying REST API response structures, handling API return values, or generating downstream artifacts such as Excel, CSV, PDF, or reconciliation files. It prevents issues like field misalignment and type ambiguity caused by API design flaws, as well as the problem of silently succeeding when key fields are missing during artifact generation.
