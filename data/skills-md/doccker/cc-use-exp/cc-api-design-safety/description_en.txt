Triggered when designing or modifying REST API response structures, handling API return values, or generating downstream artifacts such as Excel/CSV/PDF/reconciliation files. It prevents issues such as field misalignment and type ambiguities caused by API design flaws, as well as silently successful generation of artifacts that lack critical fields.
