Batch data operation workflow: orchestrates large-scale read, write, update, and import/export operations for Feishu multidimensional tables and spreadsheets, with built-in breakpoint resume and error recovery. Use it when users need to process hundreds or thousands of records, perform bulk imports/exports, migrate data across tables, analyze large datasets, or when Agent operations on Feishu docs are truncated early due to high data volume.
