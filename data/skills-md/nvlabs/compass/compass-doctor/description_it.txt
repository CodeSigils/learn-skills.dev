Diagnosticare il motivo per cui COMPASS non funziona: contenitore, GPU, shell attivata, risorse, init Isaac Sim, validità del checkpoint. Da utilizzare ogni volta che l'utente segnala vaghi errori COMPASS: "l'allenamento non si avvia", "c'è qualcosa che non va", "perché non funziona" - anche senza la parola "debug". Assicurati di usarlo quando l'utente menziona COMPASS non si sta comportando bene e non ha un intento più specifico.
