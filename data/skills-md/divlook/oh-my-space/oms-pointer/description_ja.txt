`oms commit` または `oms pull` がサブモジュールの作業コミットを移動した後に使用して、移動されたルート gitlink ポインタを `oms Record` で記録します。クロスコマンドの commit-or-pull-then-record ループをカバーするため、サブモジュールのソース変更が記録されたルート ポインターなしで残されることはなく、ルート ポインターが誤ってコミットされることもありません。
