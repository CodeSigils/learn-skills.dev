Da utilizzare quando si scopre, si avvia, si cambia o si elimina un ramo all'interno di un sottomodulo `oms/<alias>/` — `oms branch list` per controllare le scelte, `oms branch switch` per i rami locali, `oms branch checkout` per i rami remoti e `oms branch delete` per la cancellazione locale sicura.
