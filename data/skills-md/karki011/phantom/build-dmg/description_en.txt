Build, verify, and install the PhantomOS macOS DMG. Use when the user asks to "create a DMG", "build the app", "package PhantomOS", "install the build", "make a release", or troubleshoots the packaged `.app` (terminal won't spawn, routes 404, `file:///api/...` errors).
