Crea, verifica e installa PhantomOS macOS DMG. Da utilizzare quando l'utente chiede di "creare un DMG", "creare l'app", "pacchetto PhantomOS", "installare la build", "creare una versione" o risolve i problemi del pacchetto `.app` (il terminale non viene generato, percorsi 404, errori `file:///api/...`).
