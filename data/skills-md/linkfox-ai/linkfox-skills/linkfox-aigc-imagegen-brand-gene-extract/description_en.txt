Brand DNA style extraction atomic skill. Based on product images and user brand DNA parameters (primary color, font, platform, region, language), extract a unified brand visual language (Brand DNA) and output a structured brandGeneJson for downstream atomic skill consumption. Brand DNA extraction, brand gene extract, brand DNA, brand visual definition, brand tone extraction, brand style extraction, visual identity extraction. The image set composition layer (linkfox-aigc-imagegen-cloth / product image set composition path) is called in step three; it is triggered when the user says "extract brand gene", "define brand style", "brand gene", or "brand visual".
