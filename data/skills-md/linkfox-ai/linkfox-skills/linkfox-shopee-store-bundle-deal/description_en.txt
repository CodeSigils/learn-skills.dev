Shopee (Xiaopi) bundle discount Bundle Deal (same series as linkfox-shopee-store-auth), forwarded via /shopee/developerProxy to the Shopee Open API Bundle Deal module encompassing all 10 interfaces: add_bundle_deal, get_bundle_deal_list, add_bundle_deal_item, update_bundle_deal, end_bundle_deal, etc. When a user mentions Shopee bundle discount, Bundle Deal, combo promotion, add_bundle_deal, bundle_deal_id, or full‑item discount, it triggers. Even if “bundle” is not explicitly referenced, any discussion involving the management of Bundle Deal activities for an authorized Shopee store should also trigger.
