Verwenden Sie diese Option, wenn Sie Java-Protokollierung und -Beobachtbarkeit implementieren oder verbessern müssen – einschließlich der Auswahl von SLF4J mit Logback/Log4j2, der Anwendung geeigneter Protokollebenen (ERROR, WARN, INFO, DEBUG, TRACE), parametrisierter Protokollierung, sicherer Protokollierung ohne Offenlegung vertraulicher Daten, umgebungsspezifischer Konfiguration, Protokollaggregation und -überwachung oder der Validierung der Protokollierung durch Tests. Teil des Skills-for-Java-Projekts
