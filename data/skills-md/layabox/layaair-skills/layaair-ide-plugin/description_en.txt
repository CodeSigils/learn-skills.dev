Create LayaAir IDE editor plugins (extensions). TRIGGER when: user asks to create/build/write a plugin, panel, editor panel, menu, inspector field, build plugin, asset processor, custom editor, scene hook, gizmo, or any editor extension. Also trigger when user mentions LayaAir/Laya/laya, @IEditor, @IEditorEnv, EditorPanel, panel/面板, or wants to extend the IDE. Chinese triggers: 编辑器插件, 编辑器面板, 插件面板, 面板插件, 写一个面板, 写个插件. This project IS the LayaAir IDE — any request to write an editor plugin or panel in this repo should use this skill.
