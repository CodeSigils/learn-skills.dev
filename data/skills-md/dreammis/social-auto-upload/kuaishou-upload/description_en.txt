Use this skill when an agent needs to complete Kuaishou login, cookie verification, video upload, or image-and-text publishing via the installed `sau` CLI. This skill applies to environments where `social-auto-upload` is already installed and the `sau` command can be invoked. Prefer using this skill for a stable, command‑based Kuaishou workflow rather than starting by reading the uploader source code.
