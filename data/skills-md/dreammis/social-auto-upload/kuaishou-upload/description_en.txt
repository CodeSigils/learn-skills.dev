When an agent needs to complete Kuaishou login, cookie verification, video upload, or graphic‑post publishing through the installed `sau` CLI, use this skill. This skill applies to environments where `social-auto-upload` is already installed and the `sau` command can be invoked. Prefer using this skill for a stable, command‑based Kuaishou workflow rather than reading the uploader source code from the outset.
