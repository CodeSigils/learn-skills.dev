Use this skill when the agent needs to complete Douyin login, cookie verification, video upload, or image-and-text publishing via the installed `sau` CLI. This skill applies to environments where `social-auto-upload` is already installed and the `sau` command can be invoked. Prioritize using this skill for a stable, command‑based Douyin workflow rather than starting by reading the uploader source code.
