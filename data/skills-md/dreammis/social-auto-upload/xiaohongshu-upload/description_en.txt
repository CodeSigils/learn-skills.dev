Use this skill when the agent needs to perform Xiaohongshu login, cookie verification, video upload, or article posting via the installed `sau` CLI. This skill applies to environments where `social-auto-upload` is already installed and the `sau` command can be invoked. Prefer using this skill for a stable, command‑based Xiaohongshu workflow rather than starting by reading the uploader source code.
