Use this skill when the agent needs to complete Xiaohongshu login, cookie verification, video upload, or post publishing via the installed `sau` CLI. This skill is applicable in environments where `social-auto-upload` is installed and the `sau` command can be invoked. Prefer using this skill for a stable, command‑driven Xiaohongshu workflow rather than starting by reading the uploader source code.
