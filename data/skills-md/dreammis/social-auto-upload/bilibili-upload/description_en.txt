Use this skill when the agent needs to complete Bilibili login, account verification, or video upload via the installed `sau` CLI. Prefer using `sau bilibili ...`; the program will automatically set up `biliup`, without requiring the user to install it manually.
