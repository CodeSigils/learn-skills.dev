Use this skill when the agent needs to perform Bilibili login, account verification, or video upload through the installed `sau` CLI. Prefer using `sau bilibili ...`; the program will automatically set up `biliup` and does not require the user to install it manually.
