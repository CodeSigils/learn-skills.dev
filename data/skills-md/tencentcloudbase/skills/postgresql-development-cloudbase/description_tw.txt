在構建、除錯或評估 CloudBase PostgreSQL / CloudBase PG / PG 模式應用程式時使用，包括 Postgres 架構設定、queryPgDatabase / managePgDatabase、JS SDK v3 app.rdb() 的 CRUD / RPC、PG HTTP API 後備、RLS 風格的權限、使用者名稱‑密碼驗證，以及以 CloudBase PG 為後端的 Web CMS / 管理員 CRUD 流程。
