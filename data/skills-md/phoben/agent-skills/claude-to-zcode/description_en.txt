When you need to migrate the engineering configuration of `.claude`, existing `.zcode`, or `.claude‑plugin/.codex‑plugin/.zcode‑plugin` into workspace resources or plugin packages that conform to the ZCode specification, use this skill. It is suitable for automatically inventorying sources, comparing differences in existing `.zcode`, migrating skills/commands/hooks/mcp/agents/templates/scripts, and handling compatibility issues such as ZCode plugin manifests, hook JSON protocols, command overrides, and MCP schemas.
