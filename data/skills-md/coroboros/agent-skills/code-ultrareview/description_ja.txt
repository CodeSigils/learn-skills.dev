現在の差分に対する 8 軸の判断コード レビュー — 正確性、簡素化、テスト、ドキュメント、スタイル、意図、デザイン/API、パフォーマンス (+ メタデータ変更の一貫性)。 5 フェーズのパイプライン スコープ → 決定論的ツール バッテリー (npx/uvx 推奨、JS + Python の大多数はゼロインストール) → 8 人の並列 LLM 軸レビューア → サブ 80 ​​の結果に対する Haiku バリデータ (逐語的なルーブリック、≧80 のしきい値) → サイレントドロップなしの合成 + 従来のコメント JSONL。すべてのレポートは「チェックしなかったこと」で終わります (セキュリティ → /security-review、ランタイム パフォーマンス、不安定な検出)。オプトイン フラグ `--verify-build`、`--mutation-test`、`--reconcile`、`--apply-safe`。パブリックスキルの姿勢 — 自動インストールはなく、不足しているネイティブ ツールは適切にスキップされます。コミットまたは PR の前に呼び出します。「変更をレビューする」、「詳細なレビュー」、「何か見逃していないか」、「コミットする前にチェックする」、「この PR を監査する」、「ドリフト / ギャップ / 盲点」。
