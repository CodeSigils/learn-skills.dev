Utilizza l'endpoint Alpha POST /responses di Venice, un'API di risposta compatibile con OpenAI con blocchi di output tipizzati (ragionamento, messaggio, function_call, web_search_call). Riguarda la forma della richiesta, lo streaming, le differenze rispetto a /chat/completions, il sottoinsieme venice_parameters supportato e il comportamento E2EE.
