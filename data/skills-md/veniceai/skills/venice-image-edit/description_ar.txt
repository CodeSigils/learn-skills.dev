تحويل الصور الموجودة مع البندقية. يغطي POST /image/edit (تحرير صورة واحدة موجهة بسرعة)، و/image/multi-edit (إنشاء 1-3 صور)، و/image/upscale (2-4x upscale + Enhance)، و/image/background-remove. يقبل base64 أو تحميل الملف أو عنوان URL HTTPS.
