使用 Venice 生成图像。涵盖 POST /image/generate (Venice-native)、POST /images/ Generations (OpenAI 兼容)、GET /image/styles (样式预设)、请求字段 (prompt、dimensions、cfg_scale、seed、variants、style_preset、aspect_ratio、resolution、safe_mode、watermark) 和响应格式。
