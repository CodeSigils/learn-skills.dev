Générez des images avec Venise. Couvre POST /image/generate (natif de Venise), POST /images/generations (compatible OpenAI), GET /image/styles (styles prédéfinis), les champs de requête (invite, dimensions, cfg_scale, seed, variantes, style_preset, aspect_ratio, résolution, safe_mode, filigrane) et les formats de réponse.
