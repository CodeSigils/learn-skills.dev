اكتشف واستخدم الشخصيات العامة في البندقية (يطالب النظام القائم على الشخصية بنموذج مقيد). يغطي GET /characters (search/filter/sort)، /characters/{slug}، /characters/{slug}/reviews، ومخطط الأحرف، وكيفية تطبيق حرف عبر venice_parameters.character_slug في عمليات إكمال الدردشة.
