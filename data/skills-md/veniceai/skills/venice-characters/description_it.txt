Scopri e utilizza i personaggi pubblici di Venezia (sistemi guidati dai personaggi con un modello associato). Copre GET /characters (search/filter/sort), /characters/{slug}, /characters/{slug}/reviews, lo schema dei caratteri e come applicare un carattere tramite venice_parameters.character_slug nei completamenti della chat.
