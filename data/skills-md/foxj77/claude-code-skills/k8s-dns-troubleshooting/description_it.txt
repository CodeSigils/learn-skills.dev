Da utilizzare quando i pod non sono in grado di risolvere i nomi dei servizi, CoreDNS non è integro, le ricerche DNS sono lente o scadono, il rilevamento dei servizi è interrotto o quando si esegue un controllo di integrità DNS su un cluster Kubernetes
