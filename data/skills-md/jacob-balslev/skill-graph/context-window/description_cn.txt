在跨系统/技能注入/工作区域分配上下文窗口预算、监视上下文运行状况（正常/紧凑/耗尽）、决定何时压缩会话、通过预压缩挂钩保留状态以便工作不会丢失、压缩后恢复或为 1M / 200K / 128K 上下文窗口选择模型级策略时使用。涵盖三区预算模型和每个模型的实际预算表； 80% 压缩规则和 20% 安全裕度（用于完成当前操作、写入检查点、运行结束协议、发出继续信号）；压缩前和压缩后协议，包括钩子职责和跨会话持久性层次结构（git > 磁盘上的文件 > 持久内存 > 实时上下文）；每个操作的代币消耗范围；五种令牌减少技术（MCP/重型工具上的确定性 CLI、具有偏移+限制的目标读取、读取前搜索、渐进式技能披露、探索计数模式）；以及在任务中耗尽窗口的反模式。不要用于决定在工作集中加载或删除“什么”上下文（使用“context-management”）、设计提示本身（使用“prompt-craft”）、多图上下文架构（使用“context-graph”）或跨会话持久内存管理（内存管理技能）。
