Create precise hunk-level semantic commits from staged or unstaged diffs, then execute the full plan in one pass with a single final apply confirmation (minimal ping-pong) via `scripts/*.sh` wrappers.
