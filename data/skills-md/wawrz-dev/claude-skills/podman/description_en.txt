Container engine reference for Podman and Podman Compose. INVOKE THIS SKILL whenever writing Dockerfiles, Containerfiles, docker-compose.yml, compose.yaml, container commands, or Makefile targets that build/run containers. Also invoke when the user mentions podman, containers, rootless containers, or is migrating from Docker. This skill ensures commands target Podman (not Docker) and handles rootless gotchas, networking, and compose differences.
