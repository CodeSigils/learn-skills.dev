Integration of single-dimension execution and multi-source search in deep research; when plan.json has defined dimensions, it is necessary to invoke the appropriate search capability according to dimension.search_strategy for multiple rounds of retrieval, evidence filtering, cross-validation, and write to sub_reports/{dimension_id}.md.
