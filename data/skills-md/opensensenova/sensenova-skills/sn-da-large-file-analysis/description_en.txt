A high‑performance analysis engine for Excel datasets with more than ten thousand rows. It offers openpyxl read_only streaming reads (iter_rows supports over 100,000 rows), accelerated Parquet conversion, memory optimization, chunked processing, and a large‑file write mode. **Proactively use this skill whenever any of the following conditions are met**: ① The dataset contains ≥ 10 k rows (triggered by the row‑count evaluation step of sn‑da‑excel‑workflow); ② The user mentions trigger terms such as large file / big data volume / performance optimization / insufficient memory / OOM / million rows / hundred‑thousand rows / streaming read / Parquet / chunked processing / large file / big data / streaming read / chunked processing; ③ Direct use of pd.read_excel() results in timeout or memory overflow; ④ The user explicitly requests high‑performance handling of a massive dataset. Not applicable for: routine Excel analysis of fewer than 10 k rows (use sn‑da‑excel‑workflow instead).
