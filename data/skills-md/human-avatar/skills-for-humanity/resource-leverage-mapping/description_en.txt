Finds the highest-leverage use of available resources — where the same input produces the most output. Triggers: 'resource leverage', 'highest-leverage use', 'where should we put our energy', 'maximize impact', 'leverage mapping'.
