Supervise y controle un servidor Unraid a través del servidor MCP de Unraid Management Agent (125 herramientas, 5 recursos, 6 indicaciones de diagnóstico) y la API REST. Úselo cuando: conecte un cliente AI (Claude, ChatGPT, Cursor, Copilot, Gemini CLI) a Unraid; comprobar el estado del sistema, matriz, disco, Docker, VM, GPU, UPS, ZFS o red; iniciar/detener contenedores, máquinas virtuales, la matriz o comprobaciones de paridad; diagnosticar el estado del disco, el rendimiento o el estado de la matriz; gestionar alertas, comprobaciones de estado, ventilador/CPU/ajuste o actualizaciones; o elegir entre MCP, REST y WebSocket. Corrige: inventar nombres de herramientas Unraid o rutas REST; ejecutar acciones destructivas (detener la matriz, reiniciar, eliminar instantánea) sin confirmar = verdadero; sondear REST en un bucle en lugar de usar WebSocket/MCP; volcar listas completas en lugar de herramientas search_/get_; y asumiendo que el agente carece de autenticación de forma predeterminada.
