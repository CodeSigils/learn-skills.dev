Soke CLI shared foundation: application configuration initialization, authentication login (auth login), permission management, error handling, security rules. Triggered when the user needs to perform the first configuration (soke-cli config init), use login authorization (soke-cli auth login), encounter insufficient permissions, or use soke-cli for the first time.
