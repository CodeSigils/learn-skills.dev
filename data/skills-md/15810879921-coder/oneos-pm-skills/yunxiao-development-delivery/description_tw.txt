雲霄開發交付及程式碼實現。即使用户不写$yunxiao-development-delivery或标准命令，只要自然语言明确表达查看、声明、分配、启动、实施、提交、完成开发任务、修复自己或指定的Bug、处理发布回流或清理发布分支，都会被主动识别为开发意图；首先通过阿里云官方DevOps CLI唯一定位[交付]、[开发]或bug，然后将其规范化为内部操作。当没有唯一的正式项但开发意图明确时，在第一次业务代码编写之前创建可追溯的TEMPDEV分支，不妨碍编码和测试验证；在正式投产前，必须完成认领和冻结。当存在多个候选者或证据冲突时，只屏蔽受影响的存储库，不要根据标题猜测数字，也不要默默给云霄写信。一般本地代码测试、正式的 QA 测试任务、产品需求和生产版本不应触发此技能。所有平台写入继续使用快照、哈希预检查、漂移检查、单次写入、定向读回和幂等收据；禁止浏览器、DOM、cookie 和内部 Web 界面回滚。
