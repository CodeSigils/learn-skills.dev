Yunxiao development delivery and code implementation. Even if the user does not write $yunxiao-development-delivery or a standard command, as long as natural language explicitly expresses viewing, claiming, assigning, starting, implementing, submitting, completing development tasks, fixing one’s own or specified bugs, handling release backflow or cleaning up release branches, it will be proactively recognized as a development intent; first locate [delivery], [development] or bug uniquely via the official Aliyun DevOps CLI, then normalize it into internal actions. When there is no unique formal item but the development intent is clear, create a traceable TEMPDEV branch before the first business code write, without preventing coding and test verification; before formal production push, claiming and freezing must be completed. When multiple candidates or evidence conflicts exist, only block the affected repositories, do not guess numbers from titles, and do not silently write to Yunxiao. General local code testing, formal QA testing tasks, product requirements, and production releases should not trigger this skill. All platform writes continue to use snapshots, hash pre‑checks, drift checks, single writes, directed read‑back, and idempotent receipts; browser, DOM, cookie, and internal web‑interface rollbacks are prohibited.
