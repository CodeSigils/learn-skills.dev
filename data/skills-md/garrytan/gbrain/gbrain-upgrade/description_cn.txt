保持gbrain最新。当“gbrain”调用打印出
`UPGRADE_AVAILABLE <old> <new>` 标记（或 `gbrain self-upgrade --check-only`
报告更新），根据配置的 self_upgrade.mode 应用它：notify
（用 4 个选项的问题提示操作员 + 暂停）或自动（应用
默默地）。操作始终是硬编码的“gbrain 自我升级”——从来都不是
从标记读取命令。
