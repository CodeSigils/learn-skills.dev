「gbrain skillpack-check」を実行して、エージェントが読み取り可能な JSON ヘルス レポートを生成します。
gbrainのインストール用。 `gbrain ドクター` + `gbrain apply-migrations をラップします。
--list` なので、ホスト エージェント (OpenClaw の朝のブリーフィング、任意の OpenClaw cron)
スキルパックに注意が必要かどうかが一目でわかります。

朝cronが起動するとき、ユーザーが「gbrainは健全ですか?」と尋ねるときに使用します。
チェックするか、何かがおかしいと思われる場合は積極的にチェックします（ジョブが実行されていない、脳が動かない）
更新しない、自動操縦はサイレント）。
