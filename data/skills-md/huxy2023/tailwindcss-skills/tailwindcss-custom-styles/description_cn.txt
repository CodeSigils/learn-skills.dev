在 Tailwind CSS v4 中添加除内置实用程序之外的自定义样式时，请使用此技能。涵盖任意值 (`top-[117px]`)、任意属性 (`[mask-type:luminance]`)、任意变体、空格和类型提示、自定义 CSS、`@layer base/components`、`@utility`、带有 `--value()`、`--modifier()`、`--default()` 的功能实用程序，以及何时优先使用组件而不是 `@apply`。触发条件：任意值、任意属性、任意变体、自定义 CSS、@layer、@utility、@apply、--value、--modifier、--default、组件类。
