當 Tailwind CSS v4 未產生預期的類別或配置來源掃描時，請使用此技能。涵蓋純文字類別偵測、完整類別名稱、動態類別陷阱、將 props 對應到靜態類別字串、忽略檔案、`@source`、`source()` 匯入基本路徑、`source(none)`、`@source not`、`@source inline()` 安全清單、大號擴充和排除產生的類別。觸發條件：缺少類別、未產生類別、動態類別、bg-${color}、@source、安全性清單、來源（無）、來源內聯、來源不、掃描檔案、內容配置。
