Use this skill when implementing or debugging dark mode in Tailwind CSS v4. Covers the `dark:` variant, default `prefers-color-scheme` behavior, selector-driven dark mode with `@custom-variant dark`, `.dark` and `data-theme` strategies, three-way system/light/dark toggles, localStorage, server-rendered theme classes, and avoiding FOUC. Triggers on: dark mode, dark:, prefers-color-scheme, .dark, data-theme, theme toggle, system theme, localStorage theme, FOUC.
