استخدم هذه المهارة عند كتابة توجيهات ووظائف Tailwind CSS v4 في CSS. يغطي `@import`، `@theme`، `@source`، `@utility`، `@variant`، `@custom-variant`، `@apply`، `@reference`، `@config` و`@plugin`، استيراد المسار الفرعي، `theme()`، `--spacing()`، `--alpha()`، `--value()`، `--modifier()`، و`--default()`. المشغلات على: @import، @theme، @source، @utility، @variant، @custom-variant، @apply، @reference، @config، @plugin، theme()، --spacing()، --alpha()، --value()، --modifier()، --default().
