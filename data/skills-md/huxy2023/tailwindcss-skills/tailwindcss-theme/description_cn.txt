使用“@theme”自定义 Tailwind CSS v4 设计令牌时，请使用此技能。涵盖主题变量、标记命名空间（`--color-*`、`--font-*`、`--text-*`、`--spacing-*`、`--breakpoint-*`、`--container-*`、阴影、半径、缓动、动画）、生成的实用程序、默认主题扩展/删除、CSS 变量用法以及何时使用 `:root` 而不是 `@theme`。触发：@theme、主题变量、设计标记、CSS 变量、自定义颜色、自定义字体、自定义断点、间距比例、默认主题、标记命名空间。
