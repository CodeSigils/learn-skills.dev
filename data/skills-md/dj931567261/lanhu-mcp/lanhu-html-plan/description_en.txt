Enter the Lanhu design document detail URL (including image_id), use the lanhu MCP image_id to precisely link and output a native HTML page layout and code draft; annotations serve as the main path, units and border‑radius normalization are completed within skill, the schema is only a fallback in case of failure, and resources are uniformly stored under src/assets/lanhu/<screen>/ or assets/lanhu/<screen>/.
