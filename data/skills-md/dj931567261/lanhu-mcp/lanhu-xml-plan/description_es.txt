Ingrese la URL de detalle del diagrama de diseño de Lanhu (incluido image_id) y use el MCP de Lanhu image_id para generar un enlace preciso que genere XML tradicional de Android (las anotaciones generan dp directamente, el esquema solo como respaldo en caso de falla, el elemento dibujable-nodpi se guarda uniformemente en el disco; si falta una línea base dp_xx, el conjunto completo se complementa automáticamente a partir de recursos de habilidades con siete conjuntos de dimensiones).
