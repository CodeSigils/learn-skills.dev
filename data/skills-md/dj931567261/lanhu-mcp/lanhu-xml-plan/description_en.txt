Enter the Lanhu design file detail URL (including image_id) and use the lanhu MCP image_id to precisely generate traditional Android XML (annotations directly output in dp, schema only provides a fallback on failure, drawable‑nodpi is uniformly written to disk; if the dp_xx baseline is missing, the full set of seven dimen resources is automatically supplemented from the skill assets).
