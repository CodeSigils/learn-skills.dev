Geben Sie die Detail-URL des Lanhu-Designdiagramms (einschließlich image_id) ein und verwenden Sie die image_id des Lanhu MCP, um einen genauen Link zu generieren, der einen iOS SwiftUI-Seitenplan und einen Codeentwurf ausgibt; Anmerkungen dienen als Hauptpfad, wobei Einheiten und Eckradien innerhalb des Skills standardisiert sind, das Schema nur als Fallback bei Fehlern fungiert und alle Ressourcen einheitlich in Assets.xcassets/Lanhu/<screen>/ gespeichert werden.
