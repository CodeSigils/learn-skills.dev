Enter the Lanhu design diagram detail URL (including image_id), and use the Lanhu MCP image_id to generate an exact link that outputs an iOS SwiftUI page plan and code draft; annotations serve as the main path, with units and corner radii standardized within the skill, the schema acting only as a fallback on failure, and all resources are uniformly saved to Assets.xcassets/Lanhu/<screen>/.
