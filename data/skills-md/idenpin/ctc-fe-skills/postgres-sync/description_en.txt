Synchronize the remote PostgreSQL database (testing/staging server) to a local PostgreSQL instance. Use pg_dump in custom format + pg_restore, together with FORCE to drop the database and rebuild it with template0/UTF8. Use when the user asks to sync/copy/refresh/mirror/pull a PostgreSQL database from a remote/test/staging server to local, or the user says “synchronize/update/pull the test server database to local” “pg_dump/pg_restore copy data between two PostgreSQL instances”.
