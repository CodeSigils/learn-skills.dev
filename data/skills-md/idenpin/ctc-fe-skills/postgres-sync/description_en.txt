Synchronize a remote PostgreSQL database (test/pre‑release server) to a local PostgreSQL instance. Use pg_dump in custom format together with pg_restore, employing FORCE to drop the database and rebuild it with template0/UTF8. Use when the user asks to sync/copy/refresh/mirror/pull a PostgreSQL database from a remote/test/staging server to local, or when the user says “sync/update/pull test‑server database to local” or “pg_dump/pg_restore copy data between two PostgreSQL instances”.
