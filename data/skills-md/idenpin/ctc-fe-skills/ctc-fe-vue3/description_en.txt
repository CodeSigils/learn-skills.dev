Modern Vue 3, TypeScript, and CSS writing styles and code conventions. It standardizes the <script setup> declaration order, TypeScript strong‑type boundaries, scoped styles, prevention of circular dependencies, and static toolchain (ESLint/Prettier) validation rules. It is suitable for everyday development, architecture reviews, and style constraints when generating code with AI.
