OpenClaw session cleanup assistant. Used when users mention cleaning up OpenClaw sessions, deleting old cron sessions, compressing or rebuilding sessions.json, or troubleshooting session file bloat. After triggering, it first checks the number of session files and the size of sessions.json under ~/.openclaw/agents/main/sessions/, then performs cleanup according to commands.
