Byte‑by‑byte alignment verification before reverse delivery — Before claiming “signature restoration succeeded / ready for delivery”, enforce a byte‑for‑byte comparison between real browser samples and local output, and systematically check dynamic fields (timestamp / random IV / nonce). Trigger phrases: “signature matches”, “ready for delivery”, “runs locally”, “restoration complete”, “verify parity”, “cross‑check verification”, “are local and browser consistent?”, “why is it correct locally but wrong online”. Use this when the user is about to claim that encryption parameters / signature restoration is complete, or before integrating the signer into the main script. Scope: only performs pre‑delivery alignment verification discipline, not responsible for algorithm implementation (param‑encryptor).
