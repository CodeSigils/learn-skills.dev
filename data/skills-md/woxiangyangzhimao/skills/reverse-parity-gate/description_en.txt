逆向交付前的逐字节对齐验收 — 在宣称「签名还原成功 / 可以交付」之前,强制用真实浏览器样本和本地输出做逐字节对照,并系统性排查动态字段(时间戳 / 随机 IV / nonce)。触发词:「签名对上了」「可以交付了」「本地能跑了」「还原完成」「verify parity」「对照验证」「本地和浏览器一致吗」「为什么本地对了线上还是错」。当用户即将宣称加密参数 / 签名还原完成、或要把 signer 集成进主脚本前使用。边界:只做交付前的对齐验收纪律,不负责算法实现(param-encryptor)。
