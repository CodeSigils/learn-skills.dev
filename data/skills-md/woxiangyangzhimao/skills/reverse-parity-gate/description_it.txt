Verifica dell'allineamento byte per byte prima della consegna inversa: prima di dichiarare "ripristino della firma riuscito/pronto per la consegna", applica un confronto byte per byte tra campioni di browser reali e output locale e controlla sistematicamente i campi dinamici (timestamp/IV casuale/nonce). Frasi trigger: "corrispondenza della firma", "pronto per la consegna", "eseguibile localmente", "ripristino completato", "verifica parità", "verifica controllo incrociato", "sono coerenti con il browser locale?", "perché è corretto localmente ma sbagliato online". Utilizzalo quando l'utente sta per dichiarare che i parametri di crittografia/il ripristino della firma sono stati completati o prima di integrare il firmatario nello script principale. Ambito: esegue solo la disciplina della verifica dell'allineamento pre-consegna, non è responsabile dell'implementazione dell'algoritmo (param‑encryptor).
