Create CLI tools optimized for coding agents. Use this skill when the user asks to "create a CLI", "build a command-line tool", "wrap an API as a CLI", "make a CLI for coding agents", "build an agent-friendly CLI", or discusses building terminal tools, CLI wrappers for APIs/MCP servers, structured CLI output, or agent-readable command-line interfaces. Trigger this skill whenever CLIs, terminal tools, or command-line utilities are being built -- even if the user doesn't say "agent-friendly" -- because agent-first design makes better CLIs for everyone.
