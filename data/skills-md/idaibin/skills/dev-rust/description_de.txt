Wird verwendet, wenn eine Rust-Quellenänderung über APIs, Crates, Dienste, CLIs, Async, Persistenz, unsichere oder FFI-Grenzen, Tests oder Dokumente hinweg implementiert, portiert oder umgestaltet werden muss; ist für die Quellbearbeitung und -validierung zuständig, nicht für reine Audit-, Review- oder Git-Delivery-Arbeiten.
