Based on a technical solution, application requirements, or Android code snippets, search for and list the required HarmonyOS ArkTS public APIs. This should be triggered when a user describes a functionality they want to implement or needs to know which APIs/Kits/interfaces to use, for example: “What API do I need to implement…”, “What interfaces are required for a photo‑capture feature”, “Which Kit should be used for this requirement”, “What HarmonyOS capabilities are involved in the technical solution”, “Look up camera‑related APIs”, “Which interfaces can I use to integrate maps”. It should also be triggered when a user describes an application need without explicitly requesting code, such as: “I want to build an IM app that needs audio‑video calls” — in this case, identify all relevant APIs. When a user provides Android (Java/Kotlin) code snippets and asks for the corresponding HarmonyOS APIs, it should be triggered as well, e.g., “What API would implement this Android code on HarmonyOS”, “Help me migrate this Android feature to HarmonyOS, which interfaces are needed” — parse the Android code and map it to the appropriate HarmonyOS APIs. Trigger keywords include: which APIs are needed, what interfaces to use, lookup API, involved capabilities, components, technical solution, API documentation, documentation path, HarmonyOS migration, Android migration, Android code, especially when combined with ArkTS/HarmonyOS/Harmony/ability/UIAbility/ArkUI or any Kit name. If the user explicitly requests writing or generating a .ets code file, this skill should not be triggered. 

Please always respond in the user’s language.
