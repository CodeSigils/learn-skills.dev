(fslzrr) Erkennt Projekt-Lint-Tools und führt sie bei bereitgestellten Änderungen aus – zuerst nur prüfen, bei Fehler automatisch beheben, reparierte Dateien erneut bereitstellen, Hard-Stopp, wenn Lint nicht aufgelöst werden kann. Benachrichtigt den Benutzer und überspringt, wenn kein Werkzeug gefunden wird. Wird von /implement zwischen /tdd und /review aufgerufen. TRIGGER, wenn: von /implement nach jeder SUBTASK aufgerufen wird oder der Benutzer „Run lint“, „lint the code“, „check lint“ sagt.
