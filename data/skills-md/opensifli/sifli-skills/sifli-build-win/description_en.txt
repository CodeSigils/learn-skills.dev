Build SiFli SDK projects on Windows using PowerShell. Use this skill when the user wants to compile/build SiFli SDK firmware, specify a board+example combo, or asks "编译", "build", "编译xxx例程", "用板子xxx编译". Handles environment setup via export.ps1, SCons build with --board option, and locating build artifacts.
