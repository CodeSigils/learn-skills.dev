Klonen Sie wichtigen Quellcode für Projektabhängigkeiten in einen ignorierten lokalen Arbeitsbereich, damit OpenCode die Interna der Bibliothek überprüfen kann. Verwenden Sie diese Option, wenn der Benutzer Abhängigkeiten klonen, Abhängigkeits-/Quellinterna überprüfen, das SDK-/Framework-Verhalten aus der Quelle verstehen, Bibliotheksimplementierungsdetails debuggen oder Kernabhängigkeits-Repos lokal lesbar machen möchte. Nicht für normale API-/Dokumentenfragen verwenden, bei denen @librarian ausreicht.
