CLI + Backend-Integrationstest-Workflow. Wird zur Überprüfung von Backend-API-Änderungen (TRPC-Router, Dienste, Modelle) über die LobeHub-CLI gegenüber einem lokalen Entwicklungsserver verwendet. Wird ausgelöst bei „CLI-Test“, „Test mit CLI“, „Überprüfung mit CLI“, „Lokaler CLI-Test“, „Backend-Test mit CLI“ oder wenn serverseitige Änderungen durchgängig validiert werden müssen.
