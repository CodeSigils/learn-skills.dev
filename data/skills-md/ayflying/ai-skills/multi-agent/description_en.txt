OpenCode multi-agent parallel collaboration configuration. Supports multiple agents working simultaneously, enabling a pipeline development model.
Use when: (1) Multiple agents need to work in parallel (2) A master is needed to coordinate the agents
(3) A standardized process of design→development→acceptance→testing is required (4) OpenCode's multi-agent collaboration capability needs to be configured
