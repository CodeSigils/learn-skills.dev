無狀態線束循環路由器 — eng-harness 技能係列的單一前門，以及「the-flow」的線束循環類似物（引導 SDD 管道）。每次呼叫時，它都會從確定性回購訊號加上可選的呼叫者提示重新匯出工作在循環中的位置，然後路由到單一正確的利用技能 - 切勿直接呼叫子級。它在工程區域（啟動→反壓→觀察→復古→改進）之前強制採用採用門（安裝→偵察→治理→注入→啟動最後），遵循`at=`/`--event`/`--plan-dir`/`--spec`/`--phase`/`--prompt-Optional`/`--json`--spec`/`--phase`/`--prompt-Optional`/`--json`，並透過路由/重定向/n/衝突無狀態－任何呼叫者都可以隨時安全地呼叫。切勿設定門、得分或障礙；從不發明健康判斷（那是「駕馭醫生」的工作）。
