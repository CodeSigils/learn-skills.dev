Routeur harnais-boucle sans état — la porte d'entrée unique à la famille de compétences eng-harnais et l'analogue harnais-boucle de « le flux » (qui guide le pipeline SDD). À chaque appel, il redérivait l'endroit où le travail se trouve dans la boucle à partir de signaux de dépôt déterministes plus un indice facultatif de l'appelant, puis achemine vers la compétence d'exploitation correcte UNIQUE - n'appelez jamais les enfants directement. Il applique une porte d'adoption (installer → scout → gouvernance → injecter → démarrer LAST) avant la zone d'ingénierie (démarrage → contre-pression → observer → rétro → améliorer), honore `at=` / `--event` / `--plan-dir` / `--spec` / `--phase` / `--prompt-optional` / `--json`, et résout les conflits entre indices et signaux via un route/redirection/noop/matrice ambiguë. Apatride : vous pouvez appeler en toute sécurité à tout moment, depuis n'importe quel appelant. Jamais de portes, de scores ou de blocages ; n'invente jamais un verdict de santé (c'est le travail du « médecin du harnais »).
