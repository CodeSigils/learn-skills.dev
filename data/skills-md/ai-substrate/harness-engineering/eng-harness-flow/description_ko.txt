상태 비저장 하네스 루프 라우터 - eng-harness 기술 제품군에 대한 단일 정문이자 SDD 파이프라인을 안내하는 'the-flow'의 하네스 루프 유사체입니다. 모든 호출에서 결정론적 repo 신호와 선택적 호출자 힌트로부터 작업이 루프에 있는 위치를 다시 파생한 다음 단일 올바른 하네스 기술로 라우팅합니다. 하위 항목을 직접 호출하지 마세요. 엔지니어링 영역(부팅 → 백프레셔 → 관찰 → 레트로 → 개선) 이전에 채택 게이트(설치 → 스카우트 → 거버넌스 → 주입 → LAST 부팅)를 시행하고 `at=` / `--event` / `--plan-dir` / `--spec` / `--phase` / `--prompt-ional` / `--json`을 존중하며 경로/리디렉션/noop/모호한 매트릭스를 통해 힌트 대 신호 충돌을 해결합니다. 무상태(Stateless) — 언제 어떤 발신자로부터든 안전하게 전화할 수 있습니다. 게이트, 점수 또는 블록을 사용하지 마십시오. 결코 건강 판정을 내리지 않습니다(이것이 바로 '마구 의사'의 일입니다).
