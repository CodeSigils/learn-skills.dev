ステートレス ハーネス ループ ルーター — eng-harness スキル ファミリへの唯一の玄関口であり、(SDD パイプラインをガイドする) 「the-flow」 のハーネス ループの類似物です。呼び出しごとに、確定的なリポジトリ信号とオプションの呼び出し元ヒントからループ上の作業が配置される場所を再導出し、単一の正しいハーネス スキルにルーティングします。決して子を直接呼び出さないでください。エンジニアリング ゾーン (ブート → バックプレッシャー → 観察 → レトロ → 改善) の前に導入ゲート (インストール → スカウト → ガバナンス → 注入 → ブート LAST) を強制し、`at=` / `--event` / `--plan-dir` / `--spec` / `--phase` / `--prompt-optional` / `--json` を尊重し、ヒントとシグナルの競合を解決します。ルート/リダイレクト/noop/曖昧な行列。ステートレス — いつでも、どの発信者からでも安全に通話できます。決してゲート、スコア、ブロックをしないでください。決して健康診断をでっち上げることはありません（それが「ハーネスドクター」の仕事です）。
