Enrutador de bucle de arnés sin estado: la única puerta de entrada a la familia de habilidades de arnés de ingeniería y el análogo de bucle de arnés de "the-flow" (que guía el canal SDD). En cada llamada, vuelve a derivar dónde se encuentra el trabajo en el bucle a partir de señales de repositorio deterministas más una pista de llamada opcional, luego dirige a la ÚNICA habilidad de arnés correcta; nunca llame a los niños directamente. Aplica una puerta de adopción (instalar → explorar → gobernanza → inyectar → arrancar ÚLTIMO) antes de la zona de ingeniería (arrancar → contrapresión → observar → retro → mejorar), respeta `at=` / `--event` / `--plan-dir` / `--spec` / `--phase` / `--prompt-optional` / `--json` y resuelve conflictos de sugerencia versus señal a través de una matriz de ruta/redireccionamiento/noop/ambigua. Sin estado: es seguro llamar en cualquier momento y desde cualquier persona que llame. Nunca puertas, puntajes o bloqueos; nunca inventa un veredicto de salud (ese es el trabajo del "médico de arnés").
