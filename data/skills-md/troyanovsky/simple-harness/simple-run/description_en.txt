Orchestrate the full implementation of a feature by looping through its issues.json task list, invoking simple-implement for each task sequentially until all tasks are done. Use this skill whenever the user wants to auto-complete a feature, run through all tasks, automate implementation, or execute the full task queue without manual intervention. Trigger on phrases like "run all tasks", "auto-implement", "complete the feature", "run through the issues", "implement everything", "execute the task list", or any request that implies looping through tasks automatically.
