Prevent unnecessary helpers, wrappers, abstractions, and duplicate implementations. Enforce reuse of platform primitives, framework capabilities, shared utilities, and proven libraries. Use when implementing features to ensure code discipline and reduce technical debt.
