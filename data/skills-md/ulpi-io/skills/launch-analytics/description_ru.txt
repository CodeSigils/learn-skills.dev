Инструментируйте запуск так, чтобы всплеск трафика был измеримым и объяснимым — определите единый последовательный UTM.
схема для каждой ссылки запуска, подключите ключевые события конверсии (посещение → регистрация → активация) в GA4.
(или существующую аналитику проекта) и настройте быструю проверку + считывание. Параметризуется
источник запуска (например, producthunt, hackernews), чтобы каждая ссылка и отчет были правильно атрибутированы. Общий
строительный блок для семейства навыков запуска: вызывается с помощью `launch-product-hunt`, `launch-hacker-news` и
другие, чтобы замкнуть цикл измерения, или запустить автономно, чтобы добавить UTM + отслеживание событий в любую кампанию.
Записывает карту UTM и план мероприятия в `.ulpi/launch/<channel>/analytics.md`. Измеряет долгосрочные результаты
(регистрации, активация), а не только голоса/посещения; никогда не собирает персональные данные в ссылках или событиях.
