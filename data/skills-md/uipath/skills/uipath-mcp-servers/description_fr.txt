Enregistrement du serveur UiPath AgentHub MCP + création d'outils via `uip agenthub mcp` (six types de serveur : uipath / coded / command / remote / platform / swagger) et `uip agenthub mcp-tools` (trois types d'outils : is-activity / resource / raw sur les serveurs de type `uipath`). Pour la création d'activités Integration Service → chargez `references/is-activity-workflow.md`. Pour les serveurs Python MCP / intégration d'agents codés → agents uipath. Pour la plate-forme IS CLI → uipath brute.
