Crea prototipi di interfaccia utente interattivi con uno stack solo CDN (z-proto + Tailwind v4 + componenti in stile shadcn + Preact). Da utilizzare quando viene richiesto di "prototipare", "creare un prototipo", "schermate di mockup", "prototipo interattivo" o quando si esplorano i flussi dell'interfaccia utente prima dell'implementazione.
