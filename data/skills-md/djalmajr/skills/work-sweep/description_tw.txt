從多個獨立角度（按結構/文件、按內容/語義、按 git 歷史記錄/時間、按實體/域、按錯誤路徑、替代存儲路徑、配置/領域維度等）進行並行盲搜索，然後在合併步驟中使用 BARRIER，對持久 SEEN 集進行重複數據刪除並列出未覆蓋的角度。每個搜索者都不知道其他人在同一輪中發現了什麼。當已知一種搜尋策略不完整時使用：「尋找所有 X」、安全審查、「這會在哪裡中斷？」、資料流追蹤。透過 /work-sweep 調用。
