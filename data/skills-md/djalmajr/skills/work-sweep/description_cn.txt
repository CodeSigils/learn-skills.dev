从多个独立角度（按结构/文件、按内容/语义、按 git 历史记录/时间、按实体/域、按错误路径、替代存储路径、配置/领域维度等）进行并行盲搜索，然后在合并步骤中使用 BARRIER，对持久 SEEN 集进行重复数据删除并列出未覆盖的角度。每个搜索者都不知道其他人在同一轮中发现了什么。当已知一种搜索策略不完整时使用：“查找所有 X”、安全审查、“这会在哪里中断？”、数据流跟踪。通过 /work-sweep 调用。
