Feishu OAuth authentication and User Access Token management. Two-step non-interactive login (AI Agent exclusive), token status checking, scope configuration, automatic refresh mechanism, and token dependency for search functionality. Use when the user requests “login to Feishu,” “get token,” “OAuth authorization,” “auth login,” “authentication,” “what permissions are needed for search,” “token has expired,” or “refresh token.” It should also be used when encountering permission errors (e.g., 99991679 Unauthorized), token expiration, state mismatches, and similar issues. Applicable to scenarios such as search commands reporting permission errors, token‑related troubleshooting, and needing to determine the current authorization status. When other Feishu skills (e.g., toolkit/msg/read) encounter User Access Token issues, this skill should be consulted.
