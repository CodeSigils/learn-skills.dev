Scrape the main content from a single article URL or an article index/archive page and save it locally as Markdown. When users mention “collect article,” “save webpage,” “batch scrape articles from a page,” “pull the first N items from this archive page,” or “save to Obsidian / Clippings / locally,” this approach should be prioritized. It is especially suitable for public web pages, Substack, blog article pages, and article archive pages. If encountering lazy‑loaded lists, dynamic pages, or incomplete static scraping, first use a browser to gather the article URLs, then run a script to batch download, prioritizing success rate.
