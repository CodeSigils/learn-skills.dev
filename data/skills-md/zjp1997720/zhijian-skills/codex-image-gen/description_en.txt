Provide any Agent that is logged into the Codex CLI with local image-generation capability, requiring no private dependencies. Whenever a user mentions generating images, drawing, image generation, text‑to‑image, illustration, cover, poster, creating an image based on a prompt, or wants to reuse the Codex OAuth login state from the local `~/.codex/auth.json` as an executable script, this skill should be used preferentially. It applies to Claude Code, Codex, OpenCode, and any other Agent that can run bash or python3 scripts.
