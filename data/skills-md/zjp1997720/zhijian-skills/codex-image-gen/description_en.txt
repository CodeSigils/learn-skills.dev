Provide any Agent that is already logged into the Codex CLI with local image generation capability, with zero private dependencies. Whenever a user mentions generating images, drawing, image generation, text-to-image, illustration, cover, poster, creating images from a prompt, or wants to reuse the Codex OAuth login state from the local `~/.codex/auth.json` as an executable script, this skill should be used first. It applies to Claude Code, Codex, OpenCode, and any other Agent that can run bash/python3 scripts.
