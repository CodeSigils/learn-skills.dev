Convert Markdown articles into inline-style HTML usable in WeChat public accounts, supporting multiple theme switching, structured components, and SVG opening animations. It is used for public account layout, article HTML generation, and as a replacement for unstable external layout services. The default is a clean layout; add --components to enable Agent intelligent rewriting (components + opening animation).
