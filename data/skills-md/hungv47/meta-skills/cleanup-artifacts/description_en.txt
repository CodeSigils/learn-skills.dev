Audits and grooms the .agents/ artifact tree — classifies every file (KEEP/STALE/ORPHAN/LEGACY/EPHEMERAL), surfaces references and risk, and (in --apply mode) MOVES candidates to .agents/skill-artifacts/.archive/[date]/ behind explicit per-category confirmation. Never deletes. Use when .agents/ has gone junk-drawer, before a release, or when artifacts feel stale. Not for cleaning code (use code-cleanup) or machine state (use machine-cleanup).
