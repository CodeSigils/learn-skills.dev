短編ビデオのフィードバック ループを閉じます。既存のマーケティング評価ループ内で、公開された投稿を元の概要およびプラットフォーム インテリジェンスの参照に対してスコア付けします。 `<loop-slug>` + `<post-url>` + `<brief-path>` + 一致する短い形式の研究カタログを読み取り、4 次元暫定ルーブリック (v0.1、サイクル 2 ～ 3 後の必須改訂) を実行し、サイクル レポートを `skills-resources/loops/[slug]/evals/[date]-cycle-N.md` に書き込み、追加します。 `results.tsv`。出版前の要約オーサリングには適していません (マーケティング スキルでは短い形式の要約を使用してください)。カタログ発見用ではありません (短い形式のリサーチを使用してください)。サイクル 1 では、単一のキャリブレーション ペアでのオーバーフィッティングを避けるために、観察値 70 / スコアリング 30 に重み付けします。
