关闭短视频的反馈循环——根据现有营销评估循环内的原始摘要和平台情报参考对已发布的帖子进行评分。读取 `<loop-slug>` + `<post-url>` + `<brief-path>` + 匹配的短格式研究目录，运行 4 维临时标题（v0.1，第 2-3 周期后强制修订），将周期报告写入 `skills-resources/loops/[slug]/evals/[date]-cycle-N.md`，并附加`结果.tsv`。不适用于发布前的摘要创作（在营销技巧中使用简短的摘要）。不适用于目录发现（使用简短形式的研究）。周期 1 对观测值进行权重 70/评分 30，以避免单个校准对的过度拟合。
