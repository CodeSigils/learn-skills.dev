[Espressione regolare] Genera una regex da una descrizione in linguaggio naturale + spiegazione passo passo + casi di test. Condizione di attivazione: quando l'utente dice "scrivi una regex", "corrispondenza regex", "regex" o "spiega regex".
