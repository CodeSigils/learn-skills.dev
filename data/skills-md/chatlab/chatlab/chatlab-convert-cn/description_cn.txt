将 ChatLab 尚不支持的本地聊天导出转换为经过验证的 ChatLab JSONL 或 JSON：分析源文件结构，编写并运行本地 Node.js 或 Python 转换脚本，并在导入之前验证记录计数。当 ChatLab 无法识别聊天导出（例如 CSV、HTML、TXT、XML、SQLite/数据库、供应商特定的自定义 JSON/JSONL、压缩包等）时，或者当用户请求代理适应未知的聊天格式时，请使用此选项。
