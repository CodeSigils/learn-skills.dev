Convert local chat exports that ChatLab does not yet support into validated ChatLab JSONL or JSON: analyze the source file structure, write and run a local Node.js or Python conversion script, and verify the record count before importing. Use this when ChatLab cannot recognize chat exports such as CSV, HTML, TXT, XML, SQLite/database, vendor‑specific custom JSON/JSONL, compressed packages, etc., or when a user requests the Agent to adapt to an unknown chat format.
