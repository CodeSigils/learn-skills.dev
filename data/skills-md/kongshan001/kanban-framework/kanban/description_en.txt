Use the /kanban command to manage the full lifecycle of kanban tasks: initialize the board, create TASK‑NNN tasks, execute phases (plan/execute/evaluate), archive completed tasks, view status/progress, restore interrupted tasks, and handle multi‑iteration workflows. When a user mentions kanban, board, TASK‑NNN, the /kanban command, task creation/archive/restoration, or any kanban task‑management intent, apply this skill.
