現在インストールされている Media.io CLI を通じて画像とビデオを生成します。
テキストから画像へ、画像から画像へ、テキストからビデオへ、画像からビデオへ、
ライブに表示されるビデオと公開されたワークフローへの参照
「mediaio モデル リスト」または「mediaio ワークフロー リスト」。エフェクトのみ使用可能
現在のパラメータが独立して検証されている場合、
CLI は「エフェクト リスト」を公開しますが、「エフェクトの取得」は公開しません。
送信する前に、必ず正確なジョブ タイプとスキーマを確認してください。毎
送信にはユーザーのクレジットが消費されるため、常に「見積もりの生成」を実行してください。
ユーザーにコストを表示し、ユーザーが明示的に承認するまで停止します。
人間が判読できる検出出力を使用し、事前にローカル ファイルをアップロードしてください。
生成、「generate create」で送信、別のコマンドで待機
`generate wait` コマンドを実行し、`generate download` で結果ファイルを取得します。
署名された結果 URL を複製する代わりに。
ローカル コマンド ネットワークをサンドボックス化するホストでは、最初にネットワーク化された
「mediaio」または「curl」シェル/Bash ツール呼び出しは、処理前にホストの承認を要求する必要があります
打ち上げ。デフォルトのサンドボックスで実行してネットワークの可用性を調査しないでください。
