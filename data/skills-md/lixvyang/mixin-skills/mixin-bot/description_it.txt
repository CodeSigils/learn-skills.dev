Questa competenza deve essere utilizzata durante la creazione di bot Mixin: caricamento di archivi di chiavi e costruzione di SafeUser (Go) / MixinApi (Node), autenticazione JWT, separazione delle chiavi di sessione/spesa, invio di messaggi (diretto, di gruppo, di testo, post, scheda app, pulsanti), loop Blaze WebSocket su wss://blaze.mixin.one (Mixin-Blaze-1, gzip), conversazioni e gestione di gruppi, ricerca utente, registrazione PIN TIP utente semplice, OAuth di terze parti login, idempotenza, message_id dedup e wrapper mixin-kit-go (solo Go).
