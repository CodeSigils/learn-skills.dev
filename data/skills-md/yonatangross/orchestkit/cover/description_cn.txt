生成并运行全面的测试套件 - 单元测试、与实际服务的集成测试（testcontainers/docker-compose）以及 Playwright E2E 测试。分析覆盖范围差距，在每层生成并行测试生成器代理，运行测试并修复故障（最多 3 次迭代）。在为现有代码生成测试、提高实施后的覆盖率或从头开始创建完整的测试套件时使用。在 /ork:implement 之后自然链接。不要用于验证/分级现有测试（使用 /ork:verify）或运行测试而不生成（直接使用 npm test）。
