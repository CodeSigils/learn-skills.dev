產生並執行全面的測試套件 - 單元測試、與實際服務的整合測試（testcontainers/docker-compose）以及 Playwright E2E 測試。分析覆蓋範圍差距，在每層產生並行測試生成器代理，執行測試並修復故障（最多 3 次迭代）。在為現有程式碼產生測試、提高實施後的覆蓋率或從頭開始建立完整的測試套件時使用。在 /ork:implement 之後自然連結。不要用於驗證/分級現有測試（使用 /ork:verify）或執行測試而不產生（直接使用 npm test）。
