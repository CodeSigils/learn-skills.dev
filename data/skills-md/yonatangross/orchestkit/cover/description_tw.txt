產生並執行全面的測試套件 - 單元測試、與實際服務的整合測試（testcontainers/docker-compose）以及 Playwright E2E 測試。分析覆盖范围差距，在每层生成并行测试生成器代理，运行测试并修复故障（最多 3 次迭代）。在为现有代码生成测试、提高实施后的覆盖率或从头开始创建完整的测试套件时使用。在 /ork:implement 之後自然連結。不要用于验证/分级现有测试（使用 /ork:verify）或运行测试而不生成（直接使用 npm test）。
