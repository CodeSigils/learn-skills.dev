将 Figma COMPONENT_SET 的变体组织成一个干净的、带标签的网格 - 从变体属性派生的行和列，行标签位于左侧，列标题位于顶部，以及一个带标题的白色容器。当变体集是一堆杂乱的东西并且您想要一个美观的、可用于文档的布局时使用 - 触发器：“在网格中排列我的变体”，“组织此组件集”，“用标签布置变体”，“制作变体矩阵”，“清理我的组件集布局”，“向我的变体添加行/列标题”。默认情况下，选择最后一个属性（通常是“状态”）作为列，其余属性作为行。这不是本机 MCP 的只读工具所做的事情 - 这会将新的有组织的布局写入画布。
