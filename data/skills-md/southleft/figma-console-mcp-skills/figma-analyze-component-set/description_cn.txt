分析 Figma COMPONENT_SET 作为代码生成的状态机 — 提取变体轴（状态/大小等），将状态变体映射到 CSS 伪类（hover→:hover、focus→:focus-visible、disabled→:disabled、error→[aria-invalid]），并计算每个变体的视觉差异（仅计算每个状态的变化）。从Figma变体集生成交互式组件时使用 - 触发器：“分析此组件集”，“将这些变体转换为CSS状态”，“从Figma变体生成按钮/输入/复选框”，“悬停和默认状态之间的变化”，“将Figma变体映射到组件道具”，“提取此组件的状态机”。将绑定变量解析为标记名称。原生 MCP 的 get_design_context/get_metadata 未涵盖，它们不会为您提供变体轴→CSS 状态机。
