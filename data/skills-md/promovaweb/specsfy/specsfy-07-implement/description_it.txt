Utilizzare quando l'utente chiede di implementare, eseguire o concludere le operazioni della sezione "14". Tariffe` di `specs/{id}-{slug}/spec.md`, crea il codice definito come fonte unica, continua la prossima tarafa pronta o finalizza una funzionalità. Utilizzare anche quando si avvia una transizione automatica o si ritorna all'invio. Utilizzare per la produzione; per apenas criar testis utilizzare specsfy-06-tdd-bdd, e per apenas decompor utilizzare specsfy-05-tasks.
