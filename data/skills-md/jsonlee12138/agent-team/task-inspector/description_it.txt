Capacità di ispezione delle attività di sola lettura per sessioni controller, lavoratore e utente. Da utilizzare quando l'utente desidera visualizzare lo stato dell'attività, ispezionare un'attività o elencare le attività senza modificare lo stato del ciclo di vita.
