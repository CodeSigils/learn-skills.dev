Da utilizzare quando un repository contiene .sops.yaml e secrets/<env>.env o uno script di segreti pnpm. Copre la lettura e la scrittura dei segreti di sviluppo, l'elevazione della produzione, l'esecuzione con i segreti e la configurazione della chiave umana.
