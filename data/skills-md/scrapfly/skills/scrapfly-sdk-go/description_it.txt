Web scraping da Go con Scrapfly SDK (`github.com/scrapfly/go-scrapfly`). Da utilizzare quando l'utente chiede di acquisire un URL, ignorare la protezione anti-bot, eseguire il rendering di JavaScript, acquisire screenshot o estrarre dati strutturati da un programma Go.
