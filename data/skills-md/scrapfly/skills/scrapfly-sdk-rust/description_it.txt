Web scraping da Rust con Scrapfly SDK (crate `scrapfly-sdk`). Da utilizzare quando l'utente chiede di acquisire un URL, ignorare la protezione anti-bot, eseguire il rendering di JavaScript, acquisire screenshot o estrarre dati strutturati da un programma Rust asincrono.
