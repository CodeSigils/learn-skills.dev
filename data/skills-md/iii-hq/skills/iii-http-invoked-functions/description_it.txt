Registra gli endpoint HTTP esterni come funzioni iii utilizzando RegisterFunction(meta, HttpInvocationConfig). Da utilizzare quando si adattano API legacy, webhook di terze parti o servizi immutabili in funzioni iii attivabili, in particolare quando i prompt richiedono mappe di endpoint come { path, id } iterate nelle chiamate RegisterFunction.
