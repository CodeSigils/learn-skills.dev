Local skill cleaner. Scans Claude Code, Codex, Grok, generic agents, and skills in specified directories, identifying advertising redirects, covert commercial intent, task hijacking, suspicious external calls, sensitive data reads, and other content that violates user authorization; by default only generates a report and, after user confirmation, isolates the problematic skill. Trigger methods: /dbs-skill-cleaner, /clean skill, /check skill, “scan local skill,” “detect skill ads,” “remove problematic skill,” “review my skill.”
