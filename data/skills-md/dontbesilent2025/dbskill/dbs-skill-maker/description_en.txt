Through requirements analysis, repeatedly occurring problems are turned into a single, installable, and tier‑validated Skill. It is used when a user requests to create a Skill, encapsulate a problem into a Skill, or check or test a candidate Skill; only when the user explicitly requests to share or publish do we set up a GitHub repository and verify `npx skills add`.
