Führen Sie /document „pr“ | aus `Änderungsprotokoll` | `Versionshinweis` | „postmortem“ (oder lassen Sie es fragen), um Prosa über eine Veränderung zu schreiben, die dem Menschen zugewandt ist. Entwürfe aus den echten Commits und Diff, die an die richtige Stelle geschrieben werden. Schreibt keinen Code, keine Tests oder Spezifikationen.
