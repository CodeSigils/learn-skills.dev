Führen Sie /document „pr“ | aus `Änderungsprotokoll` | `Versionshinweis` | „post mortem“ (oder lassen Sie es fragen), um die menschliche Prosa über eine Veränderung zu schreiben. Entwürfe aus den echten Commits und Diff, die an die richtige Stelle geschrieben werden. Schreibt keinen Code, keine Tests oder Spezifikationen.
