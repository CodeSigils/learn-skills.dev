View the proxy route of a specified process. Query the active connections via the Mihomo API to display the rules matched by the process and the proxy chain. Use it to verify which subscription route a process (e.g., claude, chrome) is taking.
