View the proxy route of a specified process. Query the current active connections via the Mihomo API to display the rules matched by the process and the proxy chain. Used to confirm which subscription line a certain process (e.g., claude, chrome) is taking.
