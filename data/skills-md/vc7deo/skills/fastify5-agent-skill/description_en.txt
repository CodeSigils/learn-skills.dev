Production Fastify 5 (TypeScript) patterns: schema validation, plugins, typed routes, error handling, security hardening, logging, testing with inject, and graceful shutdown. Use this skill whenever the user is building a Node.js/TypeScript API with Fastify, setting up route validation with Zod or TypeBox, creating Fastify plugins, adding security middleware (Helmet, CORS, rate limiting), writing tests using Fastify's inject method, or configuring graceful shutdown. Also trigger when the user asks about Fastify vs Express, Zod vs TypeBox type providers, or any Fastify plugin architecture question.
