When you need to organize legal PDFs: detect the text layer, generate a page index, organize drafts and downstream hand‑off documents, split, merge, or directly rename OCR double‑layer scans according to content and standardize the filenames; you can also rotate and correct skew, but do not perform OCR or compression.
