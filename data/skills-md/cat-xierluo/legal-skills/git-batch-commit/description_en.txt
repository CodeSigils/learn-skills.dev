Smart Git batch commit shortcut button. Trigger words: “git commit,” “batch commit,” “split commit,” “organize commit,” or used when the user explicitly wants to split staged changes into multiple focused commits. It only handles commit splitting and commit message generation; branch, PR, push, merge, and issue‑closing semantics follow the git workflow.
