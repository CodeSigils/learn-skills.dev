Only use when the user explicitly invokes /dependabot-sweep (or $dependabot-sweep in Codex). Never auto-invoke. Analyze the dependency update PRs created by Dependabot in bulk, and create a single PR that consolidates all updates into one branch. Instead of merging each Dependabot PR individually, combine them into a consolidated PR, and close the original PRs with a comment left.
