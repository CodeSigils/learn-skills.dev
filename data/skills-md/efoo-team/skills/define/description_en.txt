Only use when the user explicitly invokes /define (or $define in Codex). Never auto‑invoke. Skill for performing detailed requirements definition before implementation. For new features or modifications in a project, establish a shared understanding of demands and requirements with the user, define technical constraints, design policies, and implementation specifications, and clarify the direction of implementation. Do not implement source code; output only a requirements definition document (Markdown) as the deliverable.
