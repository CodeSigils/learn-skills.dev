Only use when the user explicitly invokes /define (or $define in Codex). Never auto‑invoke. Skill for performing detailed requirements definition before implementation. For new features or revisions of a project, establish a common understanding of requests and requirements with the user, define technical constraints, design policies, and implementation specifications, and clarify the direction of implementation. Do not perform source code implementation; output only a requirements definition document (in Markdown) as the deliverable.
