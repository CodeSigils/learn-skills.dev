Only use when the user explicitly invokes /pr-stage (or $pr-stage in Codex). Never auto-invoke. Commit only changes that have already been staged (git add completed), and if necessary, also create a feature branch, push, and create a PR. The definitive procedures and safety guards reside in the PR skill, and this skill is a thin wrapper that defines only the difference—handling only staged changes.
