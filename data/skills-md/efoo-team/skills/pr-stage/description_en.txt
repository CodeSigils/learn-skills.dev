Only use when the user explicitly invokes /pr‑stage (or $pr‑stage in Codex). Never auto‑invoke. Commit only the changes that are already staged (git add completed), and if needed also create a feature branch, push, and create a PR. The original procedure and safety guard are in the pr skill, and this skill is a thin wrapper that defines only the difference (handling only staged changes).
