Only use when the user explicitly invokes /pre-define (or $pre-define in Codex). Never auto-invoke. A skill for refining a user's ambiguous request into a concrete input that can be passed to a detailed requirement definition (/define). It does not decide specifications or perform the requirement definition itself; instead, after researching relevant existing specifications, it asks simple questions repeatedly to eliminate communication mismatches.
