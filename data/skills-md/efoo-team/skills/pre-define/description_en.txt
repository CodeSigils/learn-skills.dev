Only use when the user explicitly invokes /pre-define (or $pre-define in Codex). Never auto-invoke. A skill to refine the user's vague requests into concrete inputs that can be passed to detailed requirement definition (/define). It does not perform specification decisions or requirement definition itself; instead, after investigating related existing specifications, it repeatedly asks simple questions to eliminate miscommunication. Please always respond in user's language.
