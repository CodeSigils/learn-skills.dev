Only use when the user explicitly invokes /search-history (or $search-history in Codex). Never auto-invoke. Claude Code / Codex CLI's local chat history (JSONL) is cross‑searched by keyword, and the matching sessions and conversation fragments are presented. The history file is treated as read‑only.
