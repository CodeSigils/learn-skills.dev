Only use when the user explicitly invokes /search-history (or $search-history in Codex). Never auto‑invoke. Search the local chat history (JSONL) of Claude Code / Codex CLI across keywords, and present the relevant sessions and conversation fragments. The history file is treated as read‑only.
