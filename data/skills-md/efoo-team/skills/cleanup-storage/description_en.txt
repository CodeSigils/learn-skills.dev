Only use when the user explicitly invokes /cleanup‑storage (or $cleanup‑storage in Codex). Never auto‑invoke.  
A skill to relieve storage pressure on the local machine. It examines disk usage, organizes deletion candidates in order of safety categories and presents them, obtains user approval for each category before deleting, and reports the change in free space.
