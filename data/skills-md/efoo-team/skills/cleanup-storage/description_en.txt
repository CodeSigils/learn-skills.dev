Only use when the user explicitly invokes /cleanup-storage (or $cleanup-storage in Codex). Never auto-invoke. ローカルマシンのストレージ逼迫を解消するスキル。ディスク使用量を調査し、削除候補を安全度カテゴリ順に整理して提示し、カテゴリごとにユーザー承認を得てから削除し、空き容量の変化を報告する。
