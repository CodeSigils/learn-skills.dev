Utilícelo únicamente cuando el usuario invoque explícitamente/agents‑md‑sync(o$agents‑md‑syncin Codex). Nunca invoque automáticamente. Una habilidad que analiza todo el repositorio y genera/actualiza una base de conocimientos AGENTS.md para cada jerarquía (la raíz y los subdirectorios seleccionados mediante puntuación). Cada vez que se ejecuta, detecta divergencias entre AGENTS.md existente y el código base, lo actualiza y también mantiene el puente CLAUDE.md para Claude Code. Úselo cuando necesite organizar, inicializar o actualizar AGENTS.mdor para crear una base de conocimientos equivalente a init-deep. Para conocer los principios de diseño sobre qué escribir en el archivo de instrucciones, consulteagent-native-project-design.  

Por favor responda siempre en el idioma del usuario.
