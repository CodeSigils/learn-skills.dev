仅当用户显式调用 /pr-body（或 Codex 中的 $pr-body）时使用。切勿自动调用。使用分层抽象结构分析 AI 实现的更改，并将其输出为结构化 GitHub PR 主体（仅生成主体；不执行任何 Git 操作。原名：github-pull-request）。即使是大规模的变更，也按照从抽象到具体的顺序组织：系统架构图→数据结构→API→处理流程→实现细节。还介绍了如何撰写包含图像证据的公关正文。
