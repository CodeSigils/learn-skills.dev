ユーザーが明示的に /pr-body (または Codex の $pr-body) を呼び出す場合にのみ使用します。決して自動呼び出ししないでください。 AI によって実装された変更を階層抽象構造を使用して分析し、構造化された GitHub PR ボディとして出力します (ボディのみを生成し、Git 操作は実行しません。旧名: github-pull-request)。大規模な変更の場合でも、システム構成図→データ構造→API→処理フロー→実装詳細の順に抽象から具体に整理してください。また、画像証拠を含む PR 本文の作成方法についても説明します。
