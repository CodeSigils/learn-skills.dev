Nur verwenden, wenn der Benutzer explizit /pr-body (oder $pr-body im Codex) aufruft. Niemals automatisch aufrufen. Analysieren Sie die von der KI implementierten Änderungen mithilfe einer hierarchischen Abstraktionsstruktur und geben Sie sie als strukturierten GitHub-PR-Body aus (generieren Sie nur den Body; führen Sie keine Git-Operationen aus. Früherer Name: github-pull-request). Organisieren Sie selbst bei umfangreichen Änderungen vom Abstrakten zum Konkreten in der folgenden Reihenfolge: Systemarchitekturdiagramm → Datenstruktur → API → Verarbeitungsfluss → Implementierungsdetails. Behandeln Sie auch, wie man ein PR-Körper zusammenstellt, das Bildbeweise enthält.
