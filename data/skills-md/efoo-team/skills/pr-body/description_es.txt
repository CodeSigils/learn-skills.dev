Úselo únicamente cuando el usuario invoque explícitamente /pr-body (o $pr-body en Codex). Nunca invoque automáticamente. Analice los cambios implementados por la IA utilizando una estructura de abstracción jerárquica y generelos como un cuerpo estructurado de GitHub PR (genere solo el cuerpo; no realice ninguna operación de Git. Nombre anterior: github-pull-request). Incluso para cambios a gran escala, organice de lo abstracto a lo concreto en el orden: diagrama de arquitectura del sistema → estructura de datos → API → flujo de procesamiento → detalles de implementación. Cubra también cómo redactar un cuerpo de relaciones públicas que incluya evidencia de imagen.
