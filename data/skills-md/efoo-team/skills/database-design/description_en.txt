A skill for naming database tables and columns based on the persisted concepts, entities, and relationships rather than processing purposes. It is used for DB naming, schema naming, naming associated with normalization, and when avoiding names that are overly tailored to specific uses.
