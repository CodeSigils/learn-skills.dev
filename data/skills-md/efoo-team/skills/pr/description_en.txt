Only use when the user explicitly invokes /pr (or $pr in Codex). Never auto‑invoke. Safely perform the series of Git operations from creating a feature branch through staging, committing, pushing, and creating a Pull Request, while adhering to branch management rules (prioritizing develop and prohibiting direct commits to protected branches).
