Only use when the user explicitly invokes /pr (or $pr in Codex). Never auto-invoke. Safely perform the series of Git operations from creating a feature branch, staging, committing, pushing, to creating a Pull Request, while complying with branch management rules (prioritizing develop and prohibiting direct commits to protected branches).
