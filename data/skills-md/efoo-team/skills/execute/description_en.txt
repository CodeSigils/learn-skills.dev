Only use when the user explicitly invokes /execute (or $execute in Codex). Never auto-invoke. An orchestration skill that organizes and breaks down complex tasks, oversees available delegation mechanisms, and carries out projects. It focuses on overall organization and coordination rather than implementation details, arranges task dependencies, and delegates independent tasks in parallel. It is used at explicit activation moments such as “execute the task,” “carry out together,” or “orchestrate.”
