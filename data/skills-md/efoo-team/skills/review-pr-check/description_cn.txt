仅当用户显式调用 /review-pr-check （或 Codex 中的 $review-pr-check）时使用。切勿自动调用。将 PR 审查处理分为收集工作人员、分类子代理和执行工作人员并协调它们的技能。收集由运行“gh pr-review-check”的收集工作人员执行；分类、分组和工作包生成由分类子代理处理（默认示例是 Oracle）；实现、回复和解析等实际工作被委托给执行工作线程，而父进程则管理完整性门、回退激活和轮询循环。在没有子代理机制的环境中，父代理本身执行降级执行。
