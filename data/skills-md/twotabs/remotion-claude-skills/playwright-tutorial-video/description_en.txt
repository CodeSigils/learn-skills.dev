Record a real user flow with Playwright, then generate a Remotion tutorial video. Three entry points: capture (discrete screenshots + coordinates per step), recording (continuous video with timestamp-synced overlays), or from-manifest (re-generate scenes from existing manifest). No new UI code created.
