Generate UI tutorial videos and interactive components from the same source. Describe a task flow, a UI mockup idea, or a component with states — the skill produces a Remotion animation AND deployable React/Tailwind components in one pass. Four modes: Tutorial, Mockup, State Demo, Live Components (reuses existing app components).
