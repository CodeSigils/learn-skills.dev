Create, edit, and update devcontainer environments (`.devcontainer/devcontainer.json`). Trigger this skill when asked to create a standardized Docker development environment, add development features (like Python, Go, Node.js), or initialize a VS Code/Codespaces compatible workspace.
