Implement the MongoDB persistence layer for a hexagonal architecture TypeScript project. Use this skill whenever the user needs to create or modify a repository interface, repository implementation, MongoDB document type, or entity mapper. Trigger when the user says things like "create a repo for Foo", "add a mapper for Bar", "I need to persist Baz to MongoDB", "add the persistence layer for X", "write the document type for Foo", "create repository and mapper", "wire up Foo to the database", or whenever a domain entity exists and the storage layer is missing or incomplete. Also trigger when the user modifies an entity and says the persistence files need to stay in sync, or when they reference repo/mapper/document files by name.
