Enrich the existing references/<id> §8 Component Patterns and frontmatter `tokens.components` by crawling multi‑surface (multiple routes plus menu/modal interactions) and public design systems (Storybook/Primer/Polaris/Cedar/Geist/*.design). Expand the “button‑level” of a single landing snapshot to include modals, tabs, tables, toasts, and form states, but if the source is weak (companies whose apps are landing‑page only), do not force it and cap it honestly. When completed, add the `tokens.components_harvested: true` marker. Trigger on terms such as “component enrichment,” “component harvest,” “X component reinforcement,” and “component harvest.” If the token itself does not exist, first run omd-token-backfill/omd-add-reference.
