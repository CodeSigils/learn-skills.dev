Enrich the existing references/<id> §8 Component Patterns and frontmatter `tokens.components` by crawling multi‑surface (multiple routes + menu/modal interactions) and public design systems (Storybook/Primer/Polaris/Cedar/Geist/*.design). Expand the “button‑level” of a single landing snapshot to include modals, tabs, tables, toasts, and form states, but if the source is weak (e.g., companies that only have landing‑page apps), do not force it and honestly cap. When finished, set the marker `tokens.components_harvested: true`. Trigger tags such as “component enrichment,” “component harvest,” “X component reinforcement,” and “component harvest.” If the token itself is missing, first run omd-token-backfill/omd-add-reference.
