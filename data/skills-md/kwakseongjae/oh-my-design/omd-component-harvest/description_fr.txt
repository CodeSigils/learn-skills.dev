Enrichissez les références existantes/<id> §8 modèles de composants et le frontmatter `tokens.components` en explorant plusieurs surfaces (plusieurs itinéraires plus menu/interactions modales) et des systèmes de conception publics (Storybook/Primer/Polaris/Cedar/Geist/*.design). Étendez le « niveau bouton » d'un seul instantané de destination pour inclure les modaux, les onglets, les tableaux, les toasts et les états de formulaire, mais si la source est faible (entreprises dont les applications sont uniquement des pages de destination), ne la forcez pas et limitez-la honnêtement. Une fois terminé, ajoutez le marqueur « tokens.components_harvested : true ». Déclenchez sur des termes tels que « enrichissement de composants », « récolte de composants », « renforcement de composants X » et « récolte de composants ». Si le jeton lui-même n'existe pas, exécutez d'abord omd-token-backfill/omd-add-reference.
