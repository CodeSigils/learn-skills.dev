From the existing references/<id>/DESIGN.md (strictly written prose), reverse‑engineer the machine‑readable `tokens:` block (DTCG‑lite: colors/typography/rounded/spacing/shadow/components) derived from the prose and backfill it into the frontmatter. Validate with a token↔prose consistency gate and update the token‑status checklist. Execute in batches (default 10) spread over several days. Trigger actions such as “token backfill,” “fill tokens block,” “add token to X,” “run token batch,” and “tokenize remaining references.” Tokens for new references are handled by omd‑add‑reference Phase 4.5 (this is for existing refs only).
