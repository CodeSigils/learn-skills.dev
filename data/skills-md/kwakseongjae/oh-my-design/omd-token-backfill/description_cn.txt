从现有的引用/<id>/DESIGN.md（严格编写的散文）中，通过逆向工程（散文派生）将机器可读的 `tokens:` 块（DTCG-lite：颜色/版式/圆角/间距/阴影/组件）回填到 frontmatter 中。使用 token↔prose 一致性门进行验证并更新 token 状态检查表。分几天分批执行（默认 10 个）。触发诸如“代币回填”、“填充代币块”、“将代币添加到 X”、“运行代币批次”和“对剩余引用进行代币化”等操作。新引用的标记由 omd-add-reference Phase4.5 处理（这仅适用于现有引用）。
