À partir des références existantes/<id>/DESIGN.md (prose strictement écrite), remplissez le bloc `tokens:` lisible par machine (DTCG-lite : couleurs/typographie/arrondi/espacement/ombre/composants) dans le frontmatter par ingénierie inverse (dérivé de prose). Validez avec une porte de cohérence jeton↔prose et mettez à jour la liste de contrôle du statut du jeton. Exécuter par lots (10 par défaut) répartis sur plusieurs jours. Déclenchez des actions telles que « remplissage de jetons », « remplir le bloc de jetons », « ajouter un jeton à X », « exécuter un lot de jetons » et « tokeniser les références restantes ». Les jetons pour les nouvelles références sont gérés par omd‑add‑reference Phase4.5 (cela concerne uniquement les références existantes).
