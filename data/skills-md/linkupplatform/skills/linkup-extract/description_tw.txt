用於從一個已知清單頁面中提取許多結構化記錄 - 團隊目錄、產品/定價目錄、工作清單、會議發言人、分頁清單。在編寫整合 /v1/extract 端點（架構、非同步輪詢、NDJSON 結果）的程式碼時也請閱讀此內容。使用 Linkup 的非同步 /v1/extract REST 端點並傳回 NDJSON 行。需要 LINKUP_API_KEY。對於單頁的散文，請使用 linkup-fetch；對於網路發現，請使用連結搜尋。
