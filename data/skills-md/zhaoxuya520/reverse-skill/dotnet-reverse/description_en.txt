.NET / C# binary reverse engineering. Use when the target is a .NET assembly (PE header includes CLR, .exe/.dll managed program), a C# compiled product (including NativeAOT), red‑team Sharp* tools (Rubeus / SharpHound / etc.), .NET obfuscation programs (ConfuserEx / SmartAssembly / Babel / Eazfuscator), or a .NET loader / info‑stealer / packed malware. Prefer dnSpyEx + de4dot; when AI needs to act directly, coordinate with dnSpy MCP. Not for pure native binaries (use reverse‑engineering / ida‑reverse).
