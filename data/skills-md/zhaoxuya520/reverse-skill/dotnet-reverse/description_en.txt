.NET / C# binary reverse engineering. Used when the target is a .NET assembly (PE header includes CLR, .exe/.dll managed program), C# compiled artifacts (including NativeAOT), red‑team Sharp* tools (Rubeus / SharpHound / etc.), .NET obfuscation programs (ConfuserEx / SmartAssembly / Babel / Eazfuscator), or .NET loaders / info‑stealers / packed malware. Prefer dnSpyEx + de4dot, and when AI needs to operate directly, integrate with dnSpy MCP. Not used for pure native binaries (use reverse‑engineering / ida‑reverse).
