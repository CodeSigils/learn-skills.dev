跨版本符号迁移和二进制差异。当您有旧版本的符号或逆向工程结果并且需要快速迁移到新版本时，请使用此功能。  
适用场景：内核缺失PDB，使用旧符号推导；程序更新后批量迁移函数名；应用程序更新后快速找到新的偏移量。  
核心方法：使用LLM进行结构化差异比较，通过编程输入和输出；成本极低（200个功能1元左右）。  
触发关键字：符号迁移、bindiff、跨版本、缺失PDB、函数偏移迁移、符号迁移、二进制diff、版本比较。
