Build with Story Confidential Data Rails (CDR) — threshold-encrypted vaults on Story L1 with on-chain access control. Use when code touches @piplabs/cdr-sdk, allocates CDR vaults, uploads encrypted files to IPFS through CDR, or gates content behind Story license tokens or IP assets.
