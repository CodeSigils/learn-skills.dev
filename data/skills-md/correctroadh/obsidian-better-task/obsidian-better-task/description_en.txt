Read and write tasks in an Obsidian vault through the Better Task plugin's CLI. Use when the user wants to list, schedule, complete, abandon, or add tasks — or when they want estimate-accuracy / tag-distribution stats. Obsidian must be running with the `obsidian-better-task` plugin enabled; all verbs are namespaced `obsidian better-task:<verb>`.
