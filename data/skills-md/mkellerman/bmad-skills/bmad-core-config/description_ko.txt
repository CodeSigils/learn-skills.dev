이 내부 스킬을 사용하여 모든 BMAD 프로젝트 구성 값(핵심 설정 및 모듈별 재정의 모두)을 로드하여 호출 스킬에서 사용할 수 있도록 합니다. 구성이 필요한 모든 BMAD 기술 시작 시 자동으로 호출됩니다. 사용자가 직접 호출하지 않습니다. 기술은 기술 이름 패턴(bmad-[module]-...)에서 호출 모듈을 파생시키고, _bmad/core/config.yaml에서 핵심 구성을 읽은 다음(없는 경우 user_name, communications_언어, document_output_언어 및 출력_폴더에 대한 기본값으로 대체) 모듈별 값을 계층화하기 위해 적절한 모듈 구성 기술(예: bmad-bmm-config)에 위임합니다. 사용자에게 표시되는 출력을 생성하지 않습니다. 모든 값은 자동으로 컨텍스트로 전달됩니다. 모듈 구성 기술과의 차이점은 이 기술이 핵심 수준 값을 처리하고 검색 및 라우팅 계층 역할을 한다는 것입니다. 모듈 구성 기술은 도메인별 재정의를 처리합니다. 사용자 대상 요청에서는 이 기술을 호출하지 마세요. 내부 전용 구성 로더입니다.
