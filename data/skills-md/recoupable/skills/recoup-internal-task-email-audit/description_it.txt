INTERNO: recuperare gli strumenti del personale. Non utilizzare mai per richieste rivolte ai clienti o agli artisti. Controlla lo stato delle e-mail con attività pianificate in un intervallo di tempo: cosa viene effettivamente consegnato dall'attività di richiesta del cliente, quanti invii vuoti/in formato errato sono stati bloccati dalla protezione API, suddivisione per account/per destinatario e se gli agenti hanno utilizzato l'assistente affidabile o l'arricciatura manuale. Da utilizzare quando viene chiesto "come sono state eseguite le attività ieri sera", "quali e-mail sono uscite dalle attività", "stiamo ancora inviando e-mail vuote", "stato dell'e-mail delle attività", "la correzione delle e-mail vuote è rimasta valida". Produce una tabella per esecuzione (una riga per esecuzione di Trigger.dev `customer-prompt-task`: run_id, destinatario, prompt, oggetto dell'email, corpo kb) più un titolo, una **traccia di chiamata allo strumento** per esecuzione e un controllo del contenuto **`has_hallucinated_data`** / `expected_data_source`. Legge l'elenco di esecuzione di Trigger.dev + `email_send_log` e `chats`/`chat_messages` (Supabase).
