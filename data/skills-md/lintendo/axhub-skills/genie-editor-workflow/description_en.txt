Achieve a full closed-loop workflow for webpage-editor todos via the Axhub AI Extension (Chrome extension) together with the @axhub/genie CLI. Guide users to install the extension and verify connection status, discover the online-editor client, read user annotations and todo nodes, export context screenshots and images, set editing states (four-state lifecycle: editing/idle/completed/error), finish code changes and write back the final state. Includes communication stability guarantees such as CLI auto-retry, exponential-backoff reconnection, tab-visibility awareness, and task-state persistence. Used when a user annotates a webpage modification todo in the Genie Editor and wants the AI to handle it.
