Generate standard ID photos (1-inch, 2-inch, passport, visa, etc.). Natural beautification + AI redraw (replace with formal attire + solid‑color background + specification cropping). Triggered when the user explicitly requests ID photo specifications, background color, or dimensions. Execution reads Meitu credentials, calls the local `meitu` CLI, sends the portrait photo and ID‑photo parameters to the Meitu OpenAPI, and writes the result to the local output directory.
