画像の前景の被写体は背景から正確に分離され、透明な背景の PNG (標準の 4 つのカテゴリ: 人物、ペット、製品、グラフィック スタンプ) または白背景の画像 (非標準の被写体、またはユーザーが明示的に白い背景を必要とする場合) が出力されます。これは、ユーザーが切り抜き、背景の削除、透明な背景/ベース、被写体の抽出を明示的に要求した場合、または既存の画像を白い背景に変換することを明示的に要求した場合にのみトリガーされます。実行にはローカルの Meitu CLI、Meitu OpenAPI 認証情報が使用され、ローカルの出力ディレクトリに書き込まれます。
