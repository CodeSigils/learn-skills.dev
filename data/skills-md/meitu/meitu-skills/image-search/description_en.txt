Keyword image search / search by image, joint retrieval of the platform image library and Pinterest. Triggered only when the user explicitly requests an image search, a search by image, or a lookup of reference/material images. During execution, it reads Meitu credentials and search configuration, invokes the local `meitu` CLI, and writes the search results together with optionally downloaded files to the local output directory.
