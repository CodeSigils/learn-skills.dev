Replace the entire image background / change the scene while keeping the foreground subject unchanged. Triggered when the user says “change background,” “replace background,” “change to xx background,” “product scene change,” “portrait background change,” “ID photo background color change,” or “change background.” This tool cannot output a transparent background. Execution will use the local Meitu CLI, Meitu OpenAPI credentials, and write to a local output directory.
