Replace existing text in an image (A → B), supporting both simple precise character changes and complex alterations (decorative text, handwriting, multiple locations, changes in character count). Triggered when the user mentions changing characters, replacing text, changing A to B, modifying text, swapping a character, text replacement, changing the title, price, name, or slogan.
