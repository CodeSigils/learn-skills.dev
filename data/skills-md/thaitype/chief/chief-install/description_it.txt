Installa il Capo nel progetto corrente. Utilizza setup.sh come metodo principale, quindi verifica e corregge manualmente se necessario. Da utilizzare quando l'utente desidera configurare il framework (ad esempio "/chief-install" o "/chief-install canary").
