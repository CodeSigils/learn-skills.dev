SSH/Server Operation Assistant. Used for tasks such as remote servers, user@host, SSH configuration, upload/download, deployment, jump host, tunneling, port forwarding, server command execution, etc.; uses the Host aliases in ~/.ssh/config as the unique server inventory, prefers key authentication, and wraps OpenSSH operations via this skill’s Python scripts.
