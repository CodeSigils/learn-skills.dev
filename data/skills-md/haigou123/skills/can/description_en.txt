Embedded CAN/CAN‑FD debugging tool for scanning interfaces, monitoring messages, sending test frames, logging, decoding database files, and bus statistics. It automatically activates when the user mentions CAN, CAN‑FD, DBC decoding, bus sniffing, USB‑CAN co‑debugging, message transmission, bus statistics, PCAN, Vector, slcan, CAN interface scanning, CAN‑ID filtering, ASC logs, BLF files, and it also supports explicit /can calls. Even if the user only says “let’s look at CAN messages,” “send a test frame,” or “decode the DBC,” as long as the context clearly refers to CAN‑bus communication operations or issues, this skill should be triggered.
