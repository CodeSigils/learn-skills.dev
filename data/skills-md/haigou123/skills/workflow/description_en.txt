The thin orchestration layer of embeddedskills is used to discover projects in the current workspace, select build/flash/debug/observe backends, chain .embeddedskills/state.json, and aggregate the results of underlying skills. It is triggered when the user explicitly inputs any of the following commands: “One‑click build and flash”, “Automatic diagnosis”, “Chain build → flash → debug → observe”, or explicitly invokes /workflow.
