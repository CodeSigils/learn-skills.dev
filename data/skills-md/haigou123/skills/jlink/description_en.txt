J-Link download and online debugging tool, used for probing devices, flashing firmware, reading/writing memory, viewing registers, resetting the target, reading RTT/SWO logs, and online debugging (pause/resume/single-step/breakpoint execution/call stack/variable inspection). When the user mentions J‑Link, JLink, RTT, flashing firmware, writing memory, reading memory, register viewing, target reset, probe connectivity check, online debugging, single‑step, breakpoint, or call stack, it automatically triggers, and it also supports explicit /jlink invocation. Even if the user only says “flash it,” “check RTT output,” or “debug a bit,” as long as the context involves a J‑Link probe, this skill should be triggered.
