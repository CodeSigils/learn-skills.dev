OpenOCD download and debugging tool, used for probe detection, firmware flashing, Flash erasing, GDB Server startup, target reset control, Telnet online debugging (halt/resume/step/register/memory/breakpoint), GDB source‑level debugging, as well as Semihosting/ITM output capture and low‑level queries. When the user mentions OpenOCD, ST‑Link, CMSIS‑DAP, DAPLink, FTDI, flashing firmware, erasing Flash, GDB Server, reset, interface/target/board configuration, openocd.cfg, online debugging, single‑stepping, breakpoints, register viewing, memory read/write, or semihosting, it should be automatically triggered, and it also supports explicit /openocd calls. Even if the user only says “flash it”, “start GDB Server”, “erase the chip”, “check the registers”, “single‑step debug”, or “capture semihosting”, as long as the context involves an OpenOCD‑supported open‑source debugger, this skill should be invoked.
