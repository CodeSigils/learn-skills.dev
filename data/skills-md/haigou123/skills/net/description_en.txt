Embedded network debugging tool, used for discovering interfaces, capturing packets, analyzing pcap/pcapng, performing connectivity tests, port scanning, and traffic statistics. It automatically triggers when the user mentions Wireshark, tshark, Npcap, packet capture, network integration debugging, port scanning, connectivity troubleshooting, pcap analysis, network interfaces, ping testing, traceroute, traffic statistics, Modbus TCP, EtherNet/IP, and other network protocol debugging, and it is also compatible with explicit /net calls. Even if the user simply says “capture a packet to take a look,” “scan a port,” “is the network reachable,” or “analyze this pcap,” as long as the context includes specific tool names (tshark, Wireshark, Npcap), protocol names (Modbus TCP, EtherNet/IP, ICMP, etc.), debugging actions (packet capture, port scanning, connectivity testing, ping, traceroute, traffic statistics, pcap analysis), or network interface operations, this skill should be triggered.
