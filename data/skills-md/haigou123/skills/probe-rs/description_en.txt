probe-rs download and debugging tool, used for probe discovery, firmware flashing, reset, memory read/write, GDB Server debugging, and RTT log reading. When the user mentions probe-rs, cargo-embed, DAP, RTT, CMSIS-DAP, ST-Link, J-Link, flashing, chip information, connection under reset, probe selector, probe-rs gdb, probe-rs attach, it should be automatically triggered, and also compatible with explicit /probe-rs calls. Even if the user only says "use probe-rs to flash it", "check RTT", or "pull a backtrace", as long as the context clearly mentions probe-rs's functionality, CLI commands, or related terms (such as cargo-embed, RTT, probe-rs flash), this skill should be triggered. Please always respond in the user's language.
