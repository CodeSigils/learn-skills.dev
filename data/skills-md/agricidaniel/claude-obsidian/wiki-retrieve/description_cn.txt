复合库的混合检索基元。用上下文前缀 + BM25 + 余弦重新排序替换 v1.6 静态热→索引→钻取读取顺序，以 Anthropic 2024 年 9 月的上下文检索研究为模型（检索失败减少 35-49-67%）。通过 `bash bin/setup-retrieve.sh` 选择加入；通过维基查询和自动研究检测到的功能。触发：检索、混合检索、BM25、重新排序、上下文检索、搜索块、块搜索、库搜索、语义搜索、块匹配的内容、查找相关段落。
