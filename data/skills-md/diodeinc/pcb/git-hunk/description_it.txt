Staging di hunk e line-range non interattivi con la CLI `git-hunk`. Da utilizzare quando un utente desidera commit atomici, staging selettivo, staging parziale o una sostituzione sicura per l'agente per "git add -p" o "git commit -p", soprattutto quando "git-hunk" è disponibile nel repository corrente o su "PATH".
