Connect to a real browser via chrome-devtools-mcp, trace the generation chain of sign/enc/token, identify the runtime entry function and dependencies, then generate JSRPC injection code, Flask proxy code, and Burp autoDecoder integration documentation with validation output; this skill only produces the corresponding code and will not test unauthorized targets.
