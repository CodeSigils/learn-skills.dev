Use chrome-devtools-mcp to connect to a real browser, trace the generation chain of sign/enc/token, identify the runtime entry function and its dependencies, then generate JSRPC injection code, Flask proxy code, and a Burp autoDecoder integration document with validation output; this skill only produces the corresponding code and will not test unauthorized targets.
