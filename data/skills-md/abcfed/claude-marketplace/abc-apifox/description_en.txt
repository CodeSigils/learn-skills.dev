ABC Medical Cloud API Documentation Query Tool. Reads and queries the ABC API’s OpenAPI specification (5,000+ endpoints), supporting searches by module, path, and method, with automatic $ref resolution. Uses a module-split caching structure for fast queries. Use cases: (1) query API endpoint definitions (2) search for specific-function endpoints (3) query Schema definitions (4) view endpoint statistics
