Entry-point dispatcher for CometChat skills v2. Detects the user's framework via @cometchat/skills-cli and routes to the correct framework skill. Replaces the v1 prose dispatcher once the CLI is published to npm.
