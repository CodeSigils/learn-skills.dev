Shared SCSS, BEM mixins/functions, animation styles, and theme runtime documentation skills for `@veltra/styles`. Used to configure `pkg:@veltra/styles/...` Sass references, connect to `NodePackageImporter`, modify `normalize`/`anime`, track `UITheme`/`loadTheme`/preset theme logic in `@veltra/styles/theme`, or reuse the Ultra UI theme system in other projects.
