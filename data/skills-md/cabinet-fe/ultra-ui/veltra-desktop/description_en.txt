Documentation skill for the desktop component library of `@veltra/desktop`. Used to add, modify, troubleshoot, or import desktop components on demand, track the pairing between `src/components` and `src/types`, maintain the `style.ts` side-effect entry, form component modes, `di.ts` context, built-in `vLoading` directive, and quickly understand real-world usage via the `playgrounds/desktop` examples.
