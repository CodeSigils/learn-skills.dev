Documentation skill for the desktop component library aimed at `@veltra/desktop`. Used when adding, modifying, debugging or on-demand importing desktop components, tracking the correspondence between `src/components` and `src/types`, maintaining the `style.ts` side-effect entry, form component modes, `di.ts` context, built-in `vLoading` directive, and leveraging the `playgrounds/desktop` examples to quickly grasp real-world usage.
