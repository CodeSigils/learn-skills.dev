審計淺層模組的程式碼庫－在這些地方，介面幾乎與實作一樣複雜，理解一個概念需要在許多小檔案之間切換，提取純函數只是為了可測試性，但真正的錯誤隱藏在它們的呼叫方式中，或者緊密耦合的模組在其接縫處洩漏。將它們作為深化的機會（將淺層模組轉變為深層模組的重構），應用刪除測試，並與用戶一起遍歷設計樹。支援並行子代理程式扇出，以進行「Design It Twice」式的介面探索。每當用戶想要改進架構、尋找重構機會、整合緊密耦合的模組、識別淺層抽象、使程式碼庫更易於測試，或者用「我們在哪裡重構」、「找到架構摩擦」、「這段程式碼感覺過於抽象」、「幫我深化模組」或「審核我的程式碼庫」等短語時，就可以使用它。
