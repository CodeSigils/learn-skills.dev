Ricevi e verifica i webhook dell'API Events di Slack. Usalo quando configuri i gestori di webhook Slack, esegui il debug della verifica della firma di Slack, gestisci la sfida `url_verification` o elabori eventi come `app_mention`, `message`, `reaction_added`, `team_join` o `app_home_opened`.
