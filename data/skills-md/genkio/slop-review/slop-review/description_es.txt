Flujos de trabajo de hilo de revisión de revisión pendiente en una rama de funciones de git. TRIGGER solo cuando el usuario nombra explícitamente "slop-review" / "slop review" o hace referencia a sus artefactos (`<repo>/.reviews/`, `thread_*.json`). OMITIR las indicaciones genéricas de revisión de código que no mencionan la revisión de slop.
