Use this skill when the user wants to complete the daily check-in on WorkBuddy (to earn check-in points). If WorkBuddy is not running, it will be launched automatically. Use Playwright to locate DOM elements via CSS selectors and click: first click the user menu in the lower-left corner (class="user-menu"), wait 2 seconds, then click the "Claim Now →" button (class="daily-checkin-banner-action") to complete the check-in. Trigger keywords include "check-in", "WorkBuddy check-in", "daily check-in", "punch-in points", etc.
