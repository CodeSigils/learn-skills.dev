Ciclo SDD con avanzamento rapido: include l'esplorazione obbligatoria come passaggio 0, quindi esegue automaticamente proposte → specifiche+progettazione (parallelo) → attività, quindi chiede prima di applicare. Trigger: /sdd-ff <descrizione>, ciclo SDD rapido, avanzamento veloce, avanzamento veloce SDD.
