Écrivez du JavaScript ou du Python pour l'outil de code personnalisé n8n (@n8n/n8n-nodes-langchain.toolCode) — l'outil appelable par l'agent IA, PAS le nœud de code de workflow. À utiliser lors de la création d'un outil de code attaché à un agent AI, de l'écriture du code qu'un LLM invoquera, de l'analyse de l'entrée « requête », du renvoi d'un résultat de chaîne, de la définition d'un schéma d'entrée pour les arguments structurés (specifyInputSchema, jsonSchemaExample, DynamicStructuredTool) ou du dépannage d'erreurs telles que « Mauvais type de sortie renvoyé », « Aucune donnée d'exécution disponible », « La propriété de réponse doit être une chaîne, mais il s'agit d'un objet », « Impossible d'attribuer une propriété en lecture seule ». 'nom'", ou un agent IA qui refuse d'appeler l'outil. Couvre les différences critiques entre le nœud Code et Code Tool : format de retour (chaîne vs `[{json:{...}}]`), indisponibilité de `$fromAI`/`$input`/`$helpers` dans le bac à sable Code Tool, règles de dénomination pour l'invocation de l'IA et quand utiliser `toolWorkflow`/HTTP Request Tool à la place.
