Escriba JavaScript o Python para la herramienta de código personalizado n8n (@n8n/n8n-nodes-langchain.toolCode): la herramienta invocable por el agente de IA, NO el nodo de código de flujo de trabajo. Úselo al crear una herramienta de código adjunta a un agente de IA, escribir código que invocará un LLM, analizar la entrada de "consulta", devolver un resultado de cadena, definir un esquema de entrada para argumentos estructurados (especificarInputSchema, jsonSchemaExample, DynamicStructuredTool) o solucionar errores como "Se devolvió un tipo de salida incorrecto", "No hay datos de ejecución disponibles", "La propiedad de respuesta debe ser una cadena, pero es un objeto", "No se puede asignar a propiedad de solo lectura". 'nombre'", o un agente de IA que se niega a llamar a la herramienta. Cubre las diferencias críticas entre el nodo de código y la herramienta de código: formato de retorno (cadena frente a `[{json:{...}}]`), falta de disponibilidad de `$fromAI`/`$input`/`$helpers` en el entorno limitado de la herramienta de código, reglas de nomenclatura para la invocación de IA y cuándo usar `toolWorkflow`/herramienta de solicitud HTTP en su lugar.
