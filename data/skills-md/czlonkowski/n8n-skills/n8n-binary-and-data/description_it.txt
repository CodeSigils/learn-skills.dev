Gestisci correttamente file e dati binari in n8n. Da utilizzare quando si lavora con file, immagini, PDF, allegati, caricamenti o download, base64, input vision/multimodale o quando un agente AI ha bisogno di un file come input o output dello strumento e ogni volta che l'utente menziona $binary, BinaryPropertyName, "leggi il PDF", "allega il file", "invia l'immagine", Unisci file binario perdente o un CDN per le immagini della chat. Copre la suddivisione $binary vs $json, la lettura/scrittura del codice binario, il mantenimento in vita del codice binario attraverso le trasformazioni con Merge, il limite del codice binario dello strumento agente e i requisiti CDN/URL per le superfici di chat.
