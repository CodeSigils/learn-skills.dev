When the user requests to create a prototype, start a prototype, generate a page prototype, build a mobile prototype, or build a PC backend prototype, this skill follows a two-stage process: first generate a solution document and wait for the user’s confirmation, then create the prototype.
