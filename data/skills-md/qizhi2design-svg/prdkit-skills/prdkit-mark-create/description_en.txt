Used when the user copies element information from the viewer. This skill first analyzes the element context and existing annotations, confirms the user's intent, then clarifies the annotation content through structured questioning, and finally calls the prdkit mark create command to create the annotation.
