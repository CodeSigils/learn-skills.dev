Used when a user provides page element information (selectors, HTML snippets) and requests modifications to a prototype page. This skill follows a two‑stage process: the first stage parses the element information and locates the file; the second stage clarifies the modification intent (data, interaction, or functionality) through structured questioning, then directly applies the changes, creates a checkpoint version, and guides a preview.
