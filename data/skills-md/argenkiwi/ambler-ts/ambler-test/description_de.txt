Erstellt eine Testdatei für einen Ambler-Knoten im Verzeichnis nodes/. Verwenden Sie dies immer dann, wenn der Benutzer Tests für einen Knoten wünscht – einschließlich „Diesen Knoten testen“, „Tests hinzufügen“, „Tests für X schreiben“ oder immer dann, wenn ein Knoten ohne entsprechende Testdatei erstellt wird.
