Use when you need to drive a Motrix download manager from the command line — list/inspect downloads, add downloads (URL, magnet, or torrent), pause/resume/remove tasks, watch live progress, or read aggregate stats. Works against a local desktop Motrix (auto-discovered) or a remote/headless server (via `--endpoint` + a paired token).
