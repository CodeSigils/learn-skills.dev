An industry → an ecological terrain map card (PNG). Using the “Qian Nao Intelligent” reference‑frame theory as the foundation: unfold the industry into an overhead “ecological terrain”—value flows over the landscape like a river, and then mark two spots on the terrain—“bottlenecks” (narrow passes/dams where flow or capacity narrows) and “value‑capture points” (treasure piles where profit settles). The terrain makes the power structure instantly visible: places where flow is throttled are often not where money accumulates. Include a base rate (scale) of three key indicators plus three “big questions” (frontiers). Deep research is truly networked; the image is AI‑generated (default ‑a Yoshida Seiji‑style picture‑book aesthetic, sharing origins with ljg‑library; optional ‑c pixel+cyber), then the freshly minted surveyor stands on the terrain looking down. Use when the user says “行业地图”, “产业地图”, “生态地形图”, “画一下这个行业”, “industry map”, “map this industry”, “行业版图”, “产业链地图”, “/ljg‑map”, or provides an industry/domain name wanting its terrain mapped. Style: default ‑a picture‑book (same as ljg‑library), add ‑c for cyber. NOT FOR a field rank‑reduction generator (use ljg‑rank), breaking down a book (use ljg‑book), single‑project investment analysis (use ljg‑invest), or deep‑diving into a concept (use ljg‑think).
