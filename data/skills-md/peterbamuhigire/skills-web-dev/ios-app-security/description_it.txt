Da utilizzare per proteggere un'app iOS: best practice per il portachiavi con Secure Enclave, classi di protezione dei dati per i file, sicurezza del trasporto delle app, blocco dei certificati tramite delegato URLSession, rilevamento del jailbreak, rilevamento delle manomissioni in fase di runtime, protezione binaria (anti-debug, anti-hooking), firma del codice e igiene dei diritti e manifest della privacy di iOS 17+. Integra lo sviluppo ios (standard generali), le soluzioni di stabilità ios (modelli di crash) e l'accesso biometrico ios (LocalAuthentication).
