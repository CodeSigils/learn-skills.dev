قام Spring Security 7 بتغيير الإعدادات الافتراضية للأمان ومعالجة CSRF - قد تحتاج الاختبارات باستخدام hasRole() أو csrf() إلى تحديثات. اقرأ قبل اختبار المصادقة أو الترخيص أو نقاط النهاية الآمنة. المشغلات: @WithMockUser، csrf()، jwt()، oauth2Login()، SecurityFilterChain، @PreAuthorize، AccessDeniedException، @WithUserDetails، @WithSecurityContext، hasRole، hasAuthority، SCOPE_، JwtAuthenticationToken.
