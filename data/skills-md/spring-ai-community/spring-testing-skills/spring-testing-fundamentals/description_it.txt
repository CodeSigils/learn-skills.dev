Boot 4 ha rinominato @MockBean in @MockitoBean, ha spostato le annotazioni delle sezioni di test in nuovi pacchetti e ha modificato il comportamento della memorizzazione nella cache del contesto. Leggere per gli idiomi AssertJ/BDDMockito corretti e la migrazione Boot 4. Fallback predefinito quando non è presente alcuna annotazione di sezione specifica. Trigger: assertThat, BDDMockito, ArgumentCaptor, @MockitoBean, @MockitoSpyBean, @DirtiesContext, UnnecessaryStubbingException, piramide di test, cache di contesto.
