Les tests WebFlux nécessitent StepVerifier pour les flux réactifs : les assertions standard transmettent silencieusement le Flux vide. L'API WebTestClient diffère de MockMvc. À lire avant de tester les points de terminaison réactifs. Déclencheurs : @WebFluxTest, WebTestClient, StepVerifier, Mono, Flux, mockUser(), mockJwt(), mutateWith, verifyComplete, expectBody, expectStatus, @AutoConfigureWebTestClient.
