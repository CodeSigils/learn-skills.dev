运行有界的自检轮询循环（睡眠 -> 检查 -> 决定，重复）而不结束回合，因此回合仅在所监视的事情完成、失败或真正需要用户注意时结束 - 这就是使 Amplifier 的回合结束通知正确且诚实地触发的原因。主要通过“/monitor <thing to watch>”显式调用，例如`/监视 PR 412 的 CI 运行，每 2m 检查一次，1h 后停止`。也可以自调用（不是通过斜杠命令），在极少数情况下，您将告诉用户“我会继续工作并让您知道”，以获得真正可检查的有界条件 - 在执行此操作之前请参阅下面的自调用防护。
