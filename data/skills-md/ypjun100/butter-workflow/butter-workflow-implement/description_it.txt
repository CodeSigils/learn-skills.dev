Trigger: docs/specs/{TASK-ID}/00-META.md esiste con lo stato pianificato e l'utente approva tale specifica o chiede che venga creata ("implementa questo", "crea il piano approvato", "continua l'implementazione"). Sono necessarie entrambe le condizioni. Legge lo stato del flusso di lavoro, applica l'ambito della traccia approvata, esegue la verifica, applica modifiche delle dimensioni delle attività, invia e crea o aggiorna un PR.
Salta: non esiste docs/specs/{TASK-ID}/00-META.md — l'utente non ha mai avviato un flusso di lavoro Butter, quindi esegui direttamente il lavoro richiesto anziché inserirlo in questa fase. Salta anche le modifiche ai file di specifiche e le singole correzioni di follow-up all'interno di un flusso di lavoro attivo: applicale direttamente. Salta anche quando un PR esiste già ed è in attesa di revisione: usa invece `butter-workflow-code-review`.
