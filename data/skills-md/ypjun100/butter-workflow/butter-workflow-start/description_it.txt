Trigger: l'utente nomina questa competenza o Butter Workflow stesso: `/butter-workflow-start`, `$butter-workflow-start`, "usa la competenza butter-workflow-start" o una richiesta che menziona Butter Workflow per nome. Classifica la traccia A/B/C, pianifica il ramo di lavoro, esegue il bootstrap dei dati sulle preferenze condivise e scrive documenti/specifiche/file delle specifiche {TASK-ID} per ogni traccia prima di fermarsi per l'approvazione.
Salta: la richiesta descrive solo il lavoro da svolgere — "avvia questa attività", "pianifichiamolo", un collegamento al problema incollato — senza nominare questa competenza o Butter Workflow; fallo invece direttamente. Salta anche quando esiste già un docs/specs/{TASK-ID}/00-META.md approvato per l'attività corrente (lo stato è pianificato o successivo) — usa `butter-workflow-implement` per continuare quel lavoro.
