Рабочий процесс проверки связей с общественностью GitHub через интерфейс командной строки `gh` из локального кода Claude Code. Выбор пользователя
PR из `gh pr list`, затем стиль обзора (общий/безопасность/многосторонний).
/ второе мнение / упрощение / проверка SHA подмодуля). Извлекает и
анализирует различия, возвращает структурированные результаты и, при необходимости, публикует комментарии.
вернемся к ПР. Никаких затрат на Anthropic API — остается в рамках подписки Claude Code.

Используйте при: просмотре существующего PR («просмотрите этот PR», «посмотрите PR #N»),
листинг/проверка последних PR, публикация результатов обратно на GitHub через
`gh pr comment`/`gh api`, проверка PR родительского репозитория, который поднимает подмодуль
SHA, или когда репо/PR неоднозначны и кандидаты должны быть указаны первыми.
