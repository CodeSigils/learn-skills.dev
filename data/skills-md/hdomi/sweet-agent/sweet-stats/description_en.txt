It shows the actual token usage for this session. It reads the Claude Code session log directly — no AI estimation. Turn it on with /sweet-stats. The output is injected by a mode-tracker hook, and the model does not calculate the numbers.
