Compress the natural language memory files (CLAUDE.md, to‑do, settings memo) to reduce input tokens. Preserve all technical content, code, URLs, and structure. The compressed version overwrites the original, and a human‑readable backup is saved as FILE.original.md. Process both Korean and English files. Trigger: /sweet-compress file_path, or “compress the memory file for me”.
