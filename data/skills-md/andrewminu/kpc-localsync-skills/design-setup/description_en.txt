At the bootcamp 5c stage, before creating a prototype, the team design (mood, color, typography) is finalized as CSS tokens. This skill is used only when the user calls it directly. It does not trigger automatically in regular conversation.
