Guida all'ottimizzazione condivisa più overlay DSL CuTe Python. Da utilizzare quando: (1) si selezionano le ottimizzazioni per un kernel DSL CuTe Python, (2) si decide se un risultato è condiviso o specifico per cute-dsl, (3) si registrano le note di implementazione di CuTe Python DSL, (4) si esamina il layout della conoscenza per il lavoro cute-dsl, (5) si mappano modelli condivisi su una superficie di implementazione DSL CuTe Python.
