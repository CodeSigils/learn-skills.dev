Guida all'ottimizzazione condivisa più overlay specifici di cuTile Python DSL. Da utilizzare quando: (1) si selezionano le ottimizzazioni per un kernel DSL cuTile Python, (2) si controllano le trappole di implementazione specifiche di cuTile, (3) si decide se un risultato di profilazione appartiene alla conoscenza condivisa o a un overlay cuTile, (4) si aggiorna la documentazione di ottimizzazione DSL cuTile Python, (5) si esamina il modo in cui un modello condiviso viene mappato a cuTile.
