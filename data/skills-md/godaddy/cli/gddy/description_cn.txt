使用 GoDaddy 的测试版 CLI (`gddy`) 搜索、注册和管理域名和 DNS 记录。每当任务涉及运行“gddy”命令、解析其 JSON 输出、查找或购买域名或编辑 GoDaddy 托管的域名的 DNS 记录（A、CNAME、MX、TXT 等）时，请加载此技能。 `gddy` 是一个独立于 GoDaddy 旧版 `godaddy` CLI（应用程序、部署、webhook）的工具 — 不要将此技能用于该工具，并且不会触发其他注册商或 DNS 提供商（Cloudflare、Route 53、Namecheap 等），除非 GoDaddy 明确参与。
