Use the KWC CLI (kd) to translate user requirements into deliverable KWC projects, components, page metadata, environment configuration, deployment, and debugging results. Employ it when the Agent needs to scaffold or expand a KWC project, split features into KWC components, create or update *.page-meta.kwp files, configure the kd environment, deploy metadata to a target environment, or guide the full workflow from requirements to KWC page rendering.
