Ricostruisci la suite di test unitari di un pacchetto attorno a una specifica di comportamento (SPEC.md) derivata dall'implementazione, con ogni test che cita un ID di regola numerato. Da utilizzare quando viene richiesto di eseguire una ricostruzione di test basata sulle specifiche, scrivere un SPEC.md per un pacchetto, ripetere lo stato o memorizzare il processo delle specifiche (PR)
