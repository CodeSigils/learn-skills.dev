CodeArts CLI shared foundation: AK/SK configuration initialization, credential management, common flag (--dry-run), endpoint resolution, error handling. Triggered on first use of codearts-cli or when encountering authentication/configuration issues.
