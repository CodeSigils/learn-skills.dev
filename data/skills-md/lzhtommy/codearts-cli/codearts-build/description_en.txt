CodeArts Build (CodeCI): query build job list (ListProjectJobs), execute build (ExecuteJob), stop build (StopTheJob), query build job status (ShowJobStepStatus). Used when the user needs to view build jobs, trigger a build, stop a running build, or query the build status.
