CodeArts Pipeline: elenca le pipeline (ListPipelines), avvia la pipeline (RunPipeline), arresta la pipeline (StopPipelineRun), interroga i dettagli di esecuzione della pipeline (ShowPipelineRunDetail). Utilizzato quando gli utenti devono visualizzare, attivare, arrestare o eseguire query sullo stato della pipeline CI/CD.
