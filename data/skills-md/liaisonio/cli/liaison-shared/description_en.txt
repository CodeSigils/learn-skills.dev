Liaison Cloud CLI shared foundation: installation, authentication with Personal Access Tokens, token precedence (flag > env > config file), error handling, and output format. Auto-loaded by every other liaison-* skill — always consult first when you are helping a user operate liaison-cli.
