Routes DocuAgent work by user intent — product UI (NestJS /execution/* + MSAL) vs Agents OpenAPI (/agents/v1). Use when choosing which skill applies, auth (X-API-Key for ConfigAgent only), polling async jobs, or staying endpoint-first. For Results tabs, filters, and NestJS vs SDU paths, use docagent-results.
