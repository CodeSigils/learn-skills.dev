Safety controls for any software project. Subcommands: careful on (enable confirmation gates before risky actions), freeze (lock file or scope boundaries), guard (validate preconditions before destructive ops), off (disable active guards). Trigger before bulk edits, migrations, destructive operations, or any change where accidental scope creep would be costly.
