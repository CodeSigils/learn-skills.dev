MCPLab 配置创作和执行工作流程的操作员指南。当用户需要帮助编写或调试 MCPLab eval YAML、运行场景（首选 MCP 工具“mcplab_run_eval”（如果可用）；CLI 回退“mcplab run/app/report/results”）、排除运行故障（身份验证、配置、场景选择、数字标志）、解释“mcplab/results/evaluation-runs/*”（“results.json”、“summary.md”）中的输出时使用`trace.jsonl`、`report.html`），或将代理性能与 `--agents` 进行比较。
