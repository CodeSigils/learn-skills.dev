Wires pallets into Substrate runtimes with construct_runtime!, parameter_types!, impl Config, feature flags, genesis config, and type aliases. Use when adding pallets to a runtime or configuring existing ones.
