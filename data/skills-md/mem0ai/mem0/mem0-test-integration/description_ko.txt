/mem0-integrate에 의해 생성된 Mem0 통합을 확인합니다. 동일한 분기의 동일한 작업 공간에서 실행됩니다(느슨한 결합) — 종속성을 설치하고 저장소의 기본 테스트 모음을 실행한 다음 사용자의 API 키에 대해 실제 종단 간 연기 흐름을 실행합니다. 스코어카드를 생성합니다. TRIGGER 언제: 사용자가 방금 /mem0-integrate를 실행하고 "확인", "통합 테스트", "/mem0-test-integration 실행"이라고 말하거나 .mem0-integration/ 디렉터리가 존재하고 현재 분기에서 테스트가 아직 실행되지 않은 경우. 다음과 같은 경우에는 트리거하지 마십시오: 사용자가 일반 프로젝트 테스트를 실행하려고 하거나(저장소의 기본 테스트 명령에 따름), 현재 분기에 이전 /mem0-integrate 실행이 없는 경우(먼저 /mem0-integrate를 실행하도록 요청). 이 기술은 설계상 컴파일 및 런타임 버그만 포착합니다. 잘못된 데이터 저장, 잘못된 시간 검색, 잘못된 사용자 범위 지정 등 논리적 통합 오류는 검토자에게 있습니다.
