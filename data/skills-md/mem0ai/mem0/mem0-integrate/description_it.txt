Integra Mem0 in un repository esistente utilizzando una pipeline TDD orientata agli obiettivi. Rileva automaticamente la lingua del repository e chiede all'utente di scegliere tra la piattaforma Mem0 (gestita) e Mem0 Open Source (auto-ospitato). Scrive test falliti prima di qualsiasi implementazione. Produce un ramo di funzionalità locale più gli artefatti `.mem0-integration/` consumati dalla capacità di verifica abbinata. TRIGGER quando: l'utente dice "integra mem0", "aggiungi mem0 a questo repository", "collega mem0 a <repo>" o chiede come aggiungere memoria a un progetto esistente. NON SI ATTIVA quando: l'utente desidera l'utilizzo generale dell'SDK (usa skill:mem0), l'utilizzo della CLI (usa skill:mem0-cli) o Vercel AI SDK (usa skill:mem0-vercel-ai-sdk). Dopo il successo, richiama skill:mem0-test-integration per verificare nello stesso spazio di lavoro (accoppiamento lento).
