Mem0 CLI: l'interfaccia della riga di comando per le operazioni sulla memoria mem0. TRIGGER quando: l'utente menziona "mem0 cli", "mem0 command line", "@mem0/cli", "mem0-cli", "pip install mem0-cli", "npm install -g @mem0/cli" o sta eseguendo comandi mem0 in un terminale/shell (mem0 add, mem0 search, mem0 list, mem0 get, mem0 init, mem0 config, mem0 import). Si attiva anche quando la query include flag della CLI come --user-id, --output, --json, --agent o descrive l'utilizzo di bash/zsh/terminal/shell. NON SI ATTIVA quando: l'utente chiede informazioni sull'integrazione dell'SDK programmatico nel codice Python/TS (usa la competenza mem0) o il provider Vercel AI SDK (usa la competenza mem0-vercel-ai-sdk).
