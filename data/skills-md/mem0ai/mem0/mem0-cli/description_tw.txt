Mem0 CLI——mem0 記憶體操作的命令列介面。觸發條件：使用者提及「mem0 cli」、「mem0 命令列」、「@mem0/cli」、「mem0-cli」、「pip install mem0-cli」、「npm install -g @mem0/cli”，或是在終端機/shell 中執行 mem0 命令（mem0 addmem0/cli”，或是在終端機/shell 中執行 mem0 命令（mem0 inmemem. config、mem0進口）。當查詢包含 CLI 標誌（例如 --user-id、--output、--json、--agent）或描述 bash/zsh/terminal/shell 用法時也會觸發。請勿在以下情況下觸發：使用者詢問 Python/TS 程式碼中的程式設計 SDK 整合（使用 mem0 技能）或 Vercel AI SDK 提供者（使用 mem0-vercel-ai-sdk 技能）。
