mem0 오픈 소스/자체 호스팅 SDK(로컬 `Memory` 클래스)에서 mem0 플랫폼/호스팅/관리형 SDK(`MemoryClient` 클래스)로의 프로젝트 마이그레이션을 계획하고 실행합니다. 개발자가 mem0 사용을 OSS/자체 호스팅에서 호스팅 API로 이동, 전환 또는 마이그레이션하려고 할 때마다 이 기능을 사용하세요. "mem0 설정을 플랫폼으로 마이그레이션", "자체 호스팅 mem0에서 MemoryClient로 전환", "로컬 Qdrant 대신 내 mem0 API 키 사용", "mem0을 클라우드/호스팅/관리 서비스로 이동" 또는 "로컬 mem0 벡터 저장소 + 임베더 구성을 플랫폼으로 교체". Python(`from mem0 import Memory` → `from mem0 import MemoryClient`) 및 TypeScript/JavaScript(`import { Memory } from "mem0ai/oss"` → `import MemoryClient from "mem0ai"`)에 적용됩니다. 사용자가 "마이그레이션"이라는 단어를 말하지 않지만 기존 mem0 통합이 호스팅된 플랫폼에 대해 실행되기를 분명히 원하는 경우에도 트리거됩니다. 먼저 검토 가능한 마이그레이션 계획을 수립한 다음 개발자가 승인한 후 실행합니다. 엄격하게 mem0 통합으로 범위가 지정됩니다. 관련 없는 코드를 리팩터링, 재구성 또는 "개선"하지 않습니다.
