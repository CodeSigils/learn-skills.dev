単にマージするだけでなく、実際にレビューできるように変更を形作ります。コードを書く前に、その作業が 1 つのプル リクエストであるかそれらのスタックであるかを決定し、最初の計画ではなく、現在存在する差分を説明するタイトルと本文を保持し、依存関係の順序で懸念ごとにコミットをグループ化し、すべてのレビュー コメントに明示的な処理を与え、プッシュまたは応答が機能したことを信頼するのではなく、チェックとスレッドの実際の状態を再確認した後にのみ変更準備完了を呼び出します。これは、作業に必要な PR の数の決定、依存ブランチのスタックの計画または同期、PR 説明の書き込みまたは更新、レビューをリクエストする前のコミット履歴の形成、レビュー フィードバックの優先順位付け、PR が実際にマージ可能かどうかの判断など、変更がオープン、分割、記述、更新、またはプル リクエストやマージ リクエストとして準備されるときに常に使用します。仕事がブランチから離れて、他の誰かが読まなければならないものになりそうなときはいつでも、それに手を伸ばしてください。
