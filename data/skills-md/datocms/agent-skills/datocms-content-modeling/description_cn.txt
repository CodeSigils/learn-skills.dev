DatoCMS 内容建模的决策框架 — 模式形状、字段选择、内容重用、分类法、内容与表示、管理 UI 组织。用于建模*决策*，而不是实现：模型与块； single_block vs 模块化内容 vs 结构化文本；引用与嵌入块；分类形状（平面/树形/多面形）；将页面形状的模式重构为可重用的内容；符合 300 KB / 500 块 / 5 级记录限制；模型行为（单例、草稿模式、all_locales_required、sortable/tree/ordering_field、presentation_title_field、collection_appearance、inverse_relationships_enabled）；字段配置（验证器 + 外观 - 枚举 + string_select、slug 自动填充、required_alt_title、结构化文本允许列表、有框 vs 无框 single_block）。还有模式审查（重用、编辑器人体工程学、全渠道）。 *创建*架构 → `datocms-cli` 或 `datocms-cma`。查询/渲染 → `datocms-cda` + `datocms-frontend-integrations`。验证器+级联：`datocms-cma/references/schema.md`。
