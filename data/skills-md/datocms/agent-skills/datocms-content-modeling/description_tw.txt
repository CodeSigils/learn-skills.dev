DatoCMS 內容建模的決策架構 — 模式形狀、欄位選擇、內容重複使用、分類法、內容與表示、管理 UI 組織。用於建模*決策*，而不是實現：模型與區塊； single_block vs 模組化內容 vs 結構化文字；引用與嵌入區塊；分類形狀（平面/樹形/多面形）；將頁面形狀的模式重構為可重用的內容；符合 300 KB / 500 區塊 / 5層級記錄限制；模型行為（單例、草稿模式、all_locales_required、sortable/tree/ordering_field、presentation_title_field、collection_appearance、inverse_relationships_enabled）；欄位配置（驗證器 + 外觀 - 枚舉 + string_selectslects_enabled）；欄位配置（驗證器 + 外觀 - 枚舉 + string_select、slug 自動填入錶格_還有模式審查（重複使用、編輯器人體工學、全通路）。 *建立*架構 → `datocms-cli` 或 `datocms-cma`。查詢/渲染 → `datocms-cda` + `datocms-frontend-integrations`。驗證器+級聯：`datocms-cma/references/schema.md`。
