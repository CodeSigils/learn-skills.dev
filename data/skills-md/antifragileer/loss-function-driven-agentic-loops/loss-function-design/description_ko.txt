손실 함수를 설계, 구현, 검증 및 반복하는 방법
for agentic development loops. 손실함수는
루프가 최적화하는 목표 — 좋은 목표 없이
the loop spirals or mode-collapses. 언제든지 이 스킬을 로드하세요.
새로운 손실 작성, 후보 손실 검토 또는 진단
a loop that isn't converging. The companion harness-engineering
skill is the half the agent sees; this is the half the driver
본다.
