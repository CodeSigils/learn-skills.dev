如何驱动 **fake-agent** 存根作为内部编码
损失函数驱动循环内的代理。假代理
是一个确定性存根：它回显循环提示
作为“候选文本”，将固定文件写入
迭代目录，并发出确定性 JSON
结果。没有模型，没有网络，没有脆弱性。
该适配器用于 **dogfood 测试** - 验证
LFD 系统本身可以端到端地工作，无需
花费代币或依赖于真实的编码代理。
在构建确定性验证器时加载此技能
（LFD系统自我验证），或者测试时
CI 中的循环/脚手架/解析器。请勿将其用于
实际开发周期。
