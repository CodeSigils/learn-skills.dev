Plugin nativi Yakit (tipo yak/mitm) e sistema di parametri CLI. Chiarire la differenza tra plugin nativi e caricamento a caldo, come cli.* diventa i moduli dei parametri della pagina di esecuzione Yakit (String/Int/Bool/Urls/Ports/StringSlice dropdown/File, ecc. + setRequired / setDefault / setVerboseName / setCliGroup) e presentare tre casi esemplari: un plugin nativo yak che utilizza la CLI per raccogliere l'input dell'utente; un plugin nativo mitm che esegue la scansione passiva di contenuti complessi e genera rischi; e un plugin interattivo mitm+CLI che carica parametri esterni (dizionario/ambito delle parole chiave) per guidare la scansione. Usalo quando un utente chiede "come scrivere plugin yak/mitm con moduli parametrici, come usare la CLI, come eseguire la scansione passiva con un plugin mitm".
