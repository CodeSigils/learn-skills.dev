Yakit native plugins (yak/mitm type) and the CLI parameter system. Clarify the difference between native plugins and hot loading, how cli.* becomes Yakit execution page parameter forms (String/Int/Bool/Urls/Ports/StringSlice dropdown/File, etc. + setRequired / setDefault / setVerboseName / setCliGroup), and present three exemplary cases: a yak native plugin using CLI to collect user input; a mitm native plugin performing passive scanning of complex content and generating risks; and a mitm + CLI interactive plugin loading external parameters (keyword dictionary/scope) to drive scanning. Use this when a user asks “how to write yak/mitm plugins with parameter forms, how to use CLI, how to perform passive scanning with a mitm plugin.”
