Yaklang DSL syntax examples. Use this when a user needs to write or understand Yaklang (.yak) code: variables and types, slices/maps, control flow, functions (func/arrow/closures/variadic parameters/multiple return values), string template f-strings, and the three essential error handling techniques (~ tilde / if err / defer recover). Includes directly runnable syntax walkthroughs and error handling examples.
