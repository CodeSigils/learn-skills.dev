Test di inquinamento del prototipo per stack JavaScript. Da utilizzare quando l'input dell'utente viene unito in oggetti (parser di query, corpi JSON, assegnazione profonda), quando si configurano librerie tramite chiavi non attendibili o quando si cercano gadget RCE tramite Object.prototype inquinato in Node o nel browser.
