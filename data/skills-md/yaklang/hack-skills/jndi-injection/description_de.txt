JNDI-Injection-Playbook. Wird verwendet, wenn Java-Anwendungen JNDI-Suchen mit vom Angreifer kontrollierten Namen durchführen, insbesondere über Log4j2, Spring oder einen beliebigen Codepfad, der InitialContext.lookup() erreicht.
