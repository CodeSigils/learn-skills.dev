Úselo cuando el usuario invoca explícitamente /tidy-project. Ejecuta una auditoría de tres fases de solo lectura de la estructura del proyecto para facilitar el mantenimiento del desarrollador en solitario. Identifica código inactivo, duplicación, abstracción prematura, sobreanidamiento, sobreacoplamiento, andamiaje de marco vestigial, marcadores obsoletos y deriva de documentos/código. Produce un único plan de ejecución consolidado en `docs/AAAA-MM-DD-tidy-project-plan.md` (o raíz del repositorio si no hay `docs/`): no modifica los archivos fuente. Salta todos los directorios que comienzan con `.` (por ejemplo, `.git`, `.venv`, `.rdf`, `.agents`, `.claude`, `.github`, IDE/CI/agent scaffolding) más los habituales `node_modules`, `vendor`, `dist`, `build`, `coverage`, `target`.
