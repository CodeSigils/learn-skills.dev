사용자가 명시적으로 /tidy-project를 호출할 때 사용합니다. 보다 쉬운 단독 개발 유지 관리를 위해 프로젝트 구조에 대한 읽기 전용 3단계 감사를 실행합니다. 데드 코드, 중복, 조기 추상화, 과도한 중첩, 과도한 결합, 흔적 프레임워크 스캐폴딩, 오래된 마커 및 문서/코드 드리프트를 식별합니다. `docs/YYYY-MM-DD-tidy-project-plan.md`(또는 `docs/`가 없는 경우 repo 루트)에서 단일 통합 실행 계획을 생성하며 소스 파일을 수정하지 않습니다. `.`(예: `.git`, `.venv`, `.rdf`, `.agents`, `.claude`, `.github`, IDE/CI/agent 스캐폴딩)로 시작하는 모든 디렉터리와 일반적인 `node_modules`, `vendor`, `dist`, `build`, `coverage`, `target`을 건너뜁니다.
