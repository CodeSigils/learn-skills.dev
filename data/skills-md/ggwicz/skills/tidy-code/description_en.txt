Invoked by /tidy-code. Reviews source code for structural quality violations (hidden dependencies, god functions, silent failures, deep nesting) across any language. Produces a read-only findings report with concrete refactoring suggestions. Skips every directory beginning with `.` (e.g. `.git`, `.venv`, `.rdf`, `.agents`, `.claude`, IDE/agent scaffolding) plus the usual `node_modules`, `vendor`, `dist`, `build`, `coverage`, `target`.
