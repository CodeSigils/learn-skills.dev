/tidy-code에 의해 호출됩니다. 모든 언어에서 구조적 품질 위반(숨겨진 종속성, God 함수, 자동 실패, 깊은 중첩)에 대한 소스 코드를 검토합니다. 구체적인 리팩토링 제안이 포함된 읽기 전용 조사 결과 보고서를 생성합니다. `.`(예: `.git`, `.venv`, `.rdf`, `.agents`, `.claude`, IDE/agent 스캐폴딩)로 시작하는 모든 디렉터리와 일반적인 `node_modules`, `vendor`, `dist`, `build`, `coverage`, `target`을 건너뜁니다.
