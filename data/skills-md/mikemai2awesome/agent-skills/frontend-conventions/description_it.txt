Definisci e applica standard di codifica coerenti tra HTML, CSS e JavaScript. Usa sempre questa abilità quando dai un nome a una nuova classe, variabile, componente o file; definizione delle convenzioni di un nuovo progetto; scegliere un prefisso di classe per una nuova categoria CSS; decidere i nomi delle API dei modificatori (dimensioni, sfumature, gerarchia, punti di interruzione); o rivedere il codice per la formattazione e la coerenza dei nomi. Se stai per inventare un prefisso, un'abbreviazione o un modificatore di nome senza prima controllare le convenzioni, usa questa abilità.
