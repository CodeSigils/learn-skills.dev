Use Browser-Use for complex web automation (multi-step login, form filling, posting, data scraping). When the built-in browser tool (snapshot→act) can't handle it, use this—it’s a dedicated browser AI agent that autonomously completes the entire workflow when you give it a task. Trigger words: browser-use, web automation, auto login, auto form filling, auto posting, web manipulation, complex web operations.
