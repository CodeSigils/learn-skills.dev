Use Browser-Use for complex web automation (multi-step login, form filling, posting, data scraping). Use it when the built-in browser tool (snapshot→act) can't handle the job—it's a dedicated browser AI agent that autonomously completes the entire workflow from a single task. Trigger words: browser-use, browser automation, auto-login, auto-form-fill, auto-posting, web control, complex web operations.
