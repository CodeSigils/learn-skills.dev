针对表示格式 YAML 文件树运行元数据库语义检查器，以验证所有引用是否解析 — 跨实体引用（collection_id、dashboard_id、parent_id、参数源卡、代码段引用、转换标记等）以及对 MBQL 和本机查询内的列的引用。慢（每次运行≥1 分钟）。仅当用户明确要求验证 MBQL/SQL 查询中的实体引用或列引用时才使用；在大多数情况下，这作为 CI 步骤运行，而不是本地运行。需要磁盘上的数据库元数据（默认为“.metadata/table_metadata.json”）。
