針對表示格式 YAML 檔案樹執行元資料庫語意檢查器，以驗證所有參考是否解析 — 跨實體參考（collection_id、dashboard_id、parent_id、參數來源卡、程式碼段參考、轉換標記等）以及對 MBQL 和本機查詢內的列的參考。慢（每次運轉≥1 分鐘）。僅當使用者明確要求驗證 MBQL/SQL 查詢中的實體引用或列引用時才使用；在大多數情況下，這會作為 CI 步驟運行，而不是本地運行。需要磁碟上的資料庫元資料（預設為“.metadata/table_metadata.json”）。
