Controlla e progetta modelli di dati di entità in un report di ribasso campo per campo: un confronto tra target e corrente
tabella di controllo per entità, una classificazione di ogni campo (colonna della tabella/blob di configurazione/
calcolato in fase di esecuzione/rimozione), un elenco raggruppato delle modifiche di migrazione e le giustificazioni della decisione di progettazione.

Utilizzalo ogni volta che l'utente ragiona su quali campi dovrebbe avere un'entità o dove dovrebbe avere
dal vivo, anche se non dicono mai "audit": progettare un modello di dati, controllare i campi PRD rispetto a un DB
schema, decidere store-vs-compute o column-vs-jsonb, individuare uno schema gonfio o sovramodellato
entità o pianificare una migrazione dello schema. Innesco
anche quando incollano semplicemente un modello PRD, schema o Prisma/SQLAlchemy e chiedono "sono i campi
giusto", "cosa manca", "dovrebbe essere una colonna o un jsonb", "questo campo è addirittura necessario". Inoltre
su 实体建模, 数据模型审核, 字段审查, 字段太多了, 这个表设计得对吗, PRD 对齐, 模型设计,
该存还是该算, 列还是配置, 帮我看下这个模型.
