Создает готовый к использованию шаблон ast-grep или правило YAML, которое выполняет структурный поиск, анализирует или автоматически перезаписывает код посредством сопоставления AST с использованием дерева, проверенного на соответствие положительным и отрицательным значениям. Запуск по ast-grep, sg scan, sgconfig, Tree-sitter, сопоставлению AST, структурному поиску и замене, codemod — а ТАКЖЕ по этим безымянным задачам: (1) структурный поиск — «перечислить все места вызова X перед рефакторингом», «найти асинхронные функции, которые никогда не ждут», или всякий раз, когда grep/regex превышает/не соответствует (попадает на комментарии/строки, пропускает варианты форматирования); (2) пакетное перезапись — «заменить весь console.log на logger», «переименовать эту функцию везде», «изменить эту подпись на каждом сайте вызова», «перенести этот API по всему репозиторию»; (3) Guard — «написать для этого правило lint», «запретить это использование в CI». Китайские триггеры: 写 ast-grep 规则, 结构化搜索, 结构化搜索替换, 语法树匹配, 按语法找代码, 找出所有调用X 的地方, 哪些地方用了这个 API, 重构前先找全调用点, grep 误报太多, 正则匹配不准, lint 规则,禁止这种写法, 自动改写代码.
