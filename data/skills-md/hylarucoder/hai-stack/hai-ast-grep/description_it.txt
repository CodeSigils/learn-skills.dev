Produce un modello ast-grep pronto per l'esecuzione o una regola YAML che ricerca strutturalmente, lint o riscrive automaticamente il codice tramite corrispondenza AST tree-sitter, convalidato rispetto a dispositivi positivi + negativi. Attivazione su ast-grep, sg scan, sgconfig, tree-sitter, corrispondenza AST, ricerca e sostituzione strutturale, codemod — e ANCHE su queste attività senza nome: (1) ricerca strutturale — "elenca tutti i siti di chiamata di X prima del refactoring", "trova funzioni asincrone che non attendono mai" o ogni volta che grep/regex supera/sotto corrisponde (ricorre a commenti/stringhe, manca varianti di formattazione); (2) riscrittura batch: "sostituisci tutto console.log con logger", "rinomina questa funzione ovunque", "cambia questa firma in ogni sito di chiamata", "migra questa API a livello di repository"; (3) guard — "scrivi una regola per questo", "vieta questo utilizzo in CI". Trigger cinesi: 写 ast-grep 规则, 结构化搜索, 结构化搜索替换, 语法树匹配, 按语法找代码, 找出所有调用X 的地方, 哪些地方用了这个 API, 重构前先找全调用点, grep 误报太多, 正则匹配不准,批量改写代码, 批量重命名, 全局改函数签名, 改参数顺序, 升级旧 API, codemod, 写个 lint 规则,禁止这种写法, 自动改写代码.
