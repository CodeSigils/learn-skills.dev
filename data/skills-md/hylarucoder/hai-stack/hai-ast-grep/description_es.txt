Produce un patrón ast-grep listo para ejecutar o una regla YAML que busca estructuralmente, elimina o reescribe automáticamente código a través de coincidencias AST de árbol, validadas contra accesorios positivos + negativos. Activar en ast-grep, sg scan, sgconfig, tree-sitter, coincidencia de AST, búsqueda y reemplazo estructural, codemod, y TAMBIÉN en estas tareas sin nombre: (1) búsqueda estructural: "enumerar todos los sitios de llamadas de X antes de refactorizar", "encontrar funciones asíncronas que nunca esperan", o cuando grep/regex coincida por encima o por debajo (accede a comentarios/cadenas, pierde variantes de formato); (2) reescritura por lotes: "reemplazar todos los console.log con logger", "cambiar el nombre de esta función en todas partes", "cambiar esta firma en cada sitio de llamada", "migrar este repositorio de API en todo el repositorio"; (3) guardia: "escribir una regla de pelusa para esto", "prohibir este uso en CI". Desencadenantes chinos: 写 ast-grep 规则, 结构化搜索, 结构化搜索替换, 语法树匹配, 按语法找代码, 找出所有调用X 的地方, 哪些地方用了这个 API, 重构前先找全调用点, grep 误报太多, 正则匹配不准,批量改写代码, 批量重命名, 全局改函数签名, 改参数顺序, 升级旧 API, codemod, 写个 lint 规则,禁止这种写法, 自动改写代码.
