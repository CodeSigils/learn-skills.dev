生成可立即运行的 ast-grep 模式或 YAML 规则，通过树托管 AST 匹配进行结构搜索、lint 或自动重写代码，并针对正 + 负固定装置进行验证。触发 ast-grep、sg scan、sgconfig、tree-sitter、AST 匹配、结构搜索和替换、codemod — 以及这些未命名的任务：(1) 结构搜索 — “在重构之前列出 X 的每个调用站点”、“查找从不等待的异步函数”，或者每当 grep/regex 过度/不足匹配时（命中注释/字符串、错过格式变体）； (2) 批量重写 — “用 logger 替换所有 console.log”、“在各处重命名此函数”、“在每个调用站点更改此签名”、“在整个 API 存储库范围内迁移此 API”； (3)guard——“为此编写一个 lint 规则”，“在 CI 中禁止这种用法”。中文触发器：写ast-grep规则，格式化搜索，格式化搜索替换，找语法树匹配，按语法代码，查找所有调用X的位置，哪些位置用了这个API，重构前先找全调用点，grep误报太多，正则匹配不准，批量改写代码，批量重命名，全局改函数签名，改参数顺序，升级旧API，codemod，写个lint规则，禁止这种写法，自动改写代码。
