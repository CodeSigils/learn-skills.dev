Produit un modèle ast-grep ou une règle YAML prêt à l'emploi qui recherche, lint ou réécrit automatiquement le code via une correspondance AST arborescente, validée par rapport aux appareils positifs + négatifs. Déclenchez sur ast-grep, sg scan, sgconfig, tree-sitter, AST matching, structural search-and-replace, codemod — et AUSSI sur ces tâches sans nom : (1) recherche structurelle — « lister chaque site d'appel de X avant de refactoriser », « trouver des fonctions asynchrones qui n'attendent jamais », ou chaque fois que grep/regex sur/sous-correspond (atteint des commentaires/chaînes, manque des variantes de formatage) ; (2) réécriture par lots — "remplacer tous les console.log par logger", "renommer cette fonction partout", "changer cette signature à chaque site d'appel", "migrer ce dépôt d'API à l'échelle du dépôt" ; (3) guard — "écrire une règle de charpie pour cela", "interdire cette utilisation dans CI". Déclencheurs chinois : 写 ast-grep 规则, 结构化搜索, 结构化搜索替换, 语法树匹配, 按语法找代码, 找出所有调用X 的地方, 哪些地方用了这个 API, 重构前先找全调用点, grep 误报太多, 正则匹配不准,批量改写代码, 批量重命名, 全局改函数签名, 改参数顺序, 升级旧 API, codemod, 写个 lint 规则, C'est vrai, c'est vrai.
