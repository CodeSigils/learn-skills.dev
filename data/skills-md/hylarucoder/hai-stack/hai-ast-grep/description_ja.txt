ツリーシッター AST マッチングを介してコードを構造的に検索、リント、または自動書き換えする、すぐに実行できる ast-grep パターンまたは YAML ルールを生成し、ポジティブ + ネガティブ フィクスチャに対して検証されます。 ast-grep、sg scan、sgconfig、tree-sitter、AST マッチング、構造検索と置換、codemod でトリガーします。また、名前のない次のタスクでもトリガーします: (1) 構造検索 - 「リファクタリングする前に X のすべての呼び出しサイトをリストする」、「待機しない非同期関数を見つける」、または grep/regex がオーバー/アンダーマッチするとき (コメント/文字列にヒットする、書式設定バリアントをミスする)。 (2) バッチ書き換え — 「すべての console.log をロガーに置き換える」、「どこでもこの関数の名前を変更する」、「すべての呼び出しサイトでこの署名を変更する」、「この API リポジトリ全体に移行する」。 (3) ガード — 「これに対する lint ルールを作成する」、「CI でのこの使用を禁止する」。中国語トリガー: 書き込み ast-grep 规则、構造化搜索、構造化搜索代替、语法树適合、按语法找代码、找出全部调用 X 的地方、哪これら地方用了这个 API、重构前先找全调用点、grep误报太多、正则適合不規格、批量改写代、批量重命名、全局改関数数签名、改パラメータ顺序、升级旧API、codemod、写个lint规则、禁止这种写法、自動改写コード。
