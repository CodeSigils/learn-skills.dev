產生可立即運行的 ast-grep 模式或 YAML 規則，透過樹狀託管 AST 匹配進行結構搜尋、lint 或自動重寫程式碼，並針對正 + 負固定裝置進行驗證。觸發 ast-grep、sg scan、sgconfig、tree-sitter、AST 匹配、結構搜尋和替換、codemod — 以及這些未命名的任務：(1) 結構搜尋 — “在重構之前列出 X 的每個調用站點”、“查找從不等待的異步函數”，或者每當 grep/regex 過度/不足匹配時（命中註記/不足）（錯位） console.log”、“在各處重命名此函數”、“在每個調用站點更改此簽名”、“在整個 API 存儲庫範圍內遷移此 API”； (3)guard——“為此編寫一個 lint 規則”，“在 CI 中禁止這種用法”。中文觸發器：寫ast-grep規則，格式化搜索，格式化搜索替換，找語法樹匹配，按語法代碼，查找所有調用X的位置，哪些位置用了這個API，重構前先找全調用點，grep誤報太多，正則匹配不准，批量改寫代碼，批量重寫，全域規則修改
