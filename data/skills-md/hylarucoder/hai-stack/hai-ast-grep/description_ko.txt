포지티브 + 네거티브 픽스처에 대해 검증된 트리 시터 AST 매칭을 통해 코드를 구조적으로 검색, 린트 또는 자동 재작성하는 즉시 실행 가능한 ast-grep 패턴 또는 YAML 규칙을 생성합니다. ast-grep, sg scan, sgconfig, tree-sitter, AST 일치, 구조적 검색 및 교체, codemod에 대해 트리거합니다. 그리고 이름이 지정되지 않은 작업에도 적용됩니다. (1) 구조적 검색 — "리팩터링하기 전에 X의 모든 호출 사이트 나열", "기다리지 않는 비동기 함수 찾기" 또는 grep/regex가 초과/미만 일치할 때마다(주석/문자열에 도달하고 형식 변형이 누락됨); (2) 일괄 재작성 — "모든 console.log를 로거로 교체", "이 함수의 이름을 모든 곳에서 변경", "모든 호출 사이트에서 이 서명 변경", "이 API 저장소 전체 마이그레이션"; (3) 가드 - "이것에 대한 린트 규칙을 작성합니다", "CI에서 이 사용을 금지합니다". 중국어 트리거: 写 ast-grep 规则, 结构化搜索, 结构化搜索替换, 语法树匹配, 按语法找代码, 找take所有调用 X 的地方,哪些地方用了这个 API, 重构前先找全调用点, grep 误报多多, 正则匹配不准, 批改写代码, 批weightloading命名, 전체局改函数签名,改参数顺序, 升级旧 API, codemod, 写个 lint 规则, 禁止这种写법, 自动改写代码.
