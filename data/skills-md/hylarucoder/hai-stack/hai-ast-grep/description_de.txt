Erzeugt ein betriebsbereites ast-grep-Muster oder eine YAML-Regel, die den Code über Tree-Sitter-AST-Matching strukturell durchsucht, lintet oder automatisch umschreibt und anhand positiver und negativer Fixtures validiert. Trigger auf ast-grep, sg scan, sgconfig, tree-sitter, AST-Matching, strukturelles Suchen und Ersetzen, Codemod – und AUCH auf diese unbenannten Aufgaben: (1) strukturelle Suche – „Jede Aufrufstelle von (2) Batch-Umschreibung – „alles console.log durch Logger ersetzen“, „diese Funktion überall umbenennen“, „diese Signatur an jeder Aufrufstelle ändern“, „dieses API-Repo-weit migrieren“; (3) Guard – „schreibe eine Lint-Regel dafür“, „verbiete diese Verwendung in CI“. Chinesische Auslöser: 写 ast-grep 规则, 结构化搜索, 结构化搜索替换, 语法树匹配, 按语法找代码, 找出所有调用 X 的地方, 哪些地方用了这个 API, 重构前先找全调用点, grep 误报太多, 正则匹配不准,批量改写代码, 批量重命名, 全局改函数签名, 改参数顺序, 升级旧 API, codemod, 写个 lint 规则,禁止这种写法, 自动改写代码.
