Quando si risponde "Come dovrebbe essere testata questa funzionalità", utilizzare una valutazione del rischio con prove allegate (mappa dei rischi). Translate this into the functional domain and type domain, covering both scope and depth: for the type domain, all ten axes must be addressed (script scanning signals + pre‑filled revisions), include attached signals, exclude attached reasons, and set a budget cap for full coverage. This is not used for: existing strategies that directly write test cases (test‑case‑writing), requirement modeling (requirement‑analysis), or end‑to‑end pipelines (QA).
