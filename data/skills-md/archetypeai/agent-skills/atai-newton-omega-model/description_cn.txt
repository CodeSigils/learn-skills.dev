通过 prod `/query` 端点从 Archetype AI 的 Omega 编码器 (`OmegaEncoder::omega_embeddings_1_4`) 获取时间序列嵌入 — 发送传感器读数窗口，返回每个通道固定大小的向量，没有批处理作业，也没有会话。当用户想要嵌入多变量传感器窗口（振动、流量、压力、网络等）以进行轻量级下游 ML（KNN 分类、异常评分、相似性搜索或 2D 投影）时，请使用此技能，这些在客户端通过嵌入完成。涵盖请求形状（“data.numeric_array”，通道优先窗口）、每通道 768 维输出、支持的 16-1024 窗口长度范围、“normalize_input”以及“联合多通道状态”+ KNN 模式。要首先清理/分割/窗口化原始传感器 CSV，请参阅“atai-newton-omega-model-data-prep”。请勿用于文本/图像/视频推理（即“/query”上的牛顿融合模型）。
