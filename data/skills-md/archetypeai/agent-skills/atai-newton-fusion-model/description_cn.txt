在 prod `/query` 端点上调用 Newton 的 C 2.6 融合模型 (`Newton::c2_6_8b_fp8_260424d7a55d5e`)，在单个无状态请求中使用文本、图像和视频输入 — 无会话、无批处理作业、无 SSE 管道。当用户想要每次调用多模态推理（描述图像、总结剪辑、对传感器 + 视觉上下文的状态进行分类）而不管理会话生命周期时，或者当他们需要通过“/query”对视频帧进行推理的 Newton C 检查点时，请使用此技能。涵盖每个模态的请求形状、两个图像附件路径（file_ids 与 base64）、多图像模式、两个视频路径（“.mp4”+“max_frames”和客户端采样帧+“query_metadata”）、生成参数、JSON 输出提示、延迟预算和 C 2.6 标识符陷阱。请勿用于流/基于会话的活动监控、大型多文件批处理作业或时间序列嵌入（KNN/异常）分类。
