Richiama il modello di fusione C 2.6 di Newton (`Newton::c2_6_8b_fp8_260424d7a55d5e`) sull'endpoint del prodotto "/query" con input di testo, immagine e video in un'unica richiesta stateless: nessuna sessione, nessun lavoro batch, nessun impianto idraulico SSE. Utilizza questa abilità quando l'utente desidera un ragionamento multimodale per chiamata (descrivere un'immagine, riassumere una clip, classificare uno stato dal sensore + contesto visivo) senza gestire il ciclo di vita della sessione, o quando ha bisogno del checkpoint Newton C che ragiona sui fotogrammi video tramite "/query". Copre la forma della richiesta per modalità, i due percorsi di allegato immagine (file_ids vs base64), la modalità multi-immagine, entrambi i percorsi video (`.mp4` + `max_frames` e frame campionati dal client + `query_metadata`), parametri di generazione, prompt di output JSON, budget di latenza e identificatore C 2.6 gotcha. NON utilizzare per il monitoraggio delle attività in streaming/basato su sessioni, lavori batch multifile di grandi dimensioni o classificazione di incorporamento di serie temporali (KNN/anomalie).
