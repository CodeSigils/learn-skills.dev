A read‑only skill that extracts the `## Acceptance Criteria` section from a GitHub issue body, automatically checks each item, and reports the results as ✓ / ✗ / ?. It is invoked after implementation and commit but before cross‑review within the `issue-implement` cycle to standardize acceptance‑criteria verification.
