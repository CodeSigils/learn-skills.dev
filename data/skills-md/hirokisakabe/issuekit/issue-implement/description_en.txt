Starting from the GitHub issue number, proceed in a single flow through status check → worktree creation → implementation/commit → acceptance‑criteria check → cross‑review → PR creation → CI verification. From the user “issue”.
