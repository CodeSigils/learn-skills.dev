创建和管理 Neo4j 向量索引，运行向量相似性搜索 (ANN/kNN)，在节点或关系上存储嵌入，使用 SEARCH 子句（Neo4j 2026.01+，首选）或 db.index.vector.queryNodes() 过程（2026.04 已弃用，仍适用于 2025.x），配置 HNSW 和量化选项，选择相似性函数和嵌入提供程序维度，以及批量更新嵌入。当任务涉及 CREATE VECTOR INDEX、vector.dimensions、余弦/欧几里得搜索、嵌入摄取管道或语义最近邻查找时使用。不处理 GraphRAGretrieval_query 图形遍历 - 使用 neo4j-graphrag-skill。不处理全文/关键字索引（FULLTEXT INDEX、db.index.fulltext）——使用 neo4j-cypher-skill。不处理 GDS 图嵌入（FastRP、Node2Vec）——使用 neo4j-gds-skill。
