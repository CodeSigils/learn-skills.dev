يغطي برنامج Neo4j Go Driver v6 - دورة حياة برنامج التشغيل، وExecuteQuery، والمعاملات المُدارة والصريحة، وتكوين الجلسة، ومعالجة الأخطاء، وتعيين نوع البيانات، وضبط الاتصال. استخدمه عند كتابة كود Go الذي يتصل بـ Neo4j، أو إعداد NewDriver أو ExecuteQuery، أو تصحيح أخطاء الجلسات/المعاملات/معالجة النتائج، أو العمل مع ترحيل neo4j-go-driver v5 → v6. المشغلات على NewDriver، ExecuteQuery، SessionConfig، ManagedTransaction، neo4j-go-driver. لا يتعامل مع تأليف استعلام Cypher — استخدم neo4j-cypher-skill. لا يغطي خطوات ترحيل إصدار برنامج التشغيل — استخدم neo4j-migration-skill.
