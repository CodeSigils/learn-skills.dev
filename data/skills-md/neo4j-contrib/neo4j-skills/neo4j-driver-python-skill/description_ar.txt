Neo4j Python Driver v6 - دورة حياة برنامج التشغيل، وتنفيذ الاستعلام، والمعاملات المُدارة والصريحة، والمزامنة (AsyncGraphDatabase)، ومعالجة النتائج، وتعيين نوع البيانات، ومعالجة الأخطاء، وتجميع UNWIND، وضبط تجمع الاتصال، والاتساق السببي. يُستخدم عند كتابة كود Python الذي يتصل بـ Neo4j عبر GraphDatabase.driver، أو Execute_query، أو Execute_read، أو Execute_write، أو AsyncGraphDatabase، أو neo4j.Result، أو RoutingControl. اسم الحزمة هو `neo4j` (وليس برنامج تشغيل neo4j) منذ الإصدار 6. بايثون> = 3.10 مطلوب. لا يتعامل مع تأليف استعلام Cypher — استخدم neo4j-cypher-skill. لا يغطي ترقيات برنامج التشغيل أو التغييرات العاجلة — استخدم neo4j-migration-skill. لا يغطي خطوط أنابيب GraphRAG (حزمة neo4j-graphrag) - استخدم مهارة neo4j-graphrag.
