Neo4j JavaScript/TypeScript Driver v6 — cycle de vie du pilote, executeQuery, transactions gérées (executeRead/executeWrite), session.run, gestion des entiers, sérialisation JSON, accès aux enregistrements, modèles asynchrones/attente, types TypeScript, gestion des erreurs et configuration de la connexion pour Node.js et le navigateur. À utiliser lors de l'écriture de code JS/TS qui se connecte à Neo4j — neo4j.driver(),executeQuery,executeRead,executeWrite, session.run, neo4j.int, record.get, Integer, BookmarkManager, bolt://, neo4j+s://. Ne gère PAS la création de requêtes Cypher – utilisez neo4j-cypher-skill. Ne couvre PAS la migration de la version du pilote – utilisez neo4j-migration-skill.
