Configure y opere el conector Neo4j para Kafka (sumidero + fuente) y la API CDC nativa de Neo4j. Cubre estrategias de receptor Cypher/Pattern/CUD, fuente basada en CDC y basada en consultas, semántica exactamente una vez, manejo de errores DLQ, conector administrado por Confluent Cloud, registro de esquemas (Avro/JSON) y patrones nativos de bucle de cursor db.cdc.query (Neo4j 5.13+ Enterprise/Aura BC/VDC). Úselo cuando transmita eventos de Kafka a Neo4j, transmita cambios de Neo4j a Kafka o consulte eventos de cambios de Neo4j sin Kafka. NO maneja la creación de consultas Cypher; use neo4j-cypher-skill. NO maneja la importación masiva de archivos/CSV: use neo4j-import-skill. NO maneja algoritmos GDS; use neo4j-gds-skill.
