Verwenden Sie die ai.text.*-Funktionen und -Prozeduren des Neo4j GenAI Plugins für die Generierung von In-Cypher-Einbettungen, die Textvervollständigung, die strukturierte Ausgabe, den Chat, die Tokenisierung und die Stapelaufnahme. Behandelt ai.text.embed(), ai.text.embedBatch(), ai.text.completion(), ai.text.structuredCompletion(), ai.text.aggregateCompletion(), ai.text.chat(), ai.text.tokenCount(), ai.text.chunkByTokenLimit() und die Anbieterkonfiguration für OpenAI, Azure OpenAI, VertexAI und Amazon Bedrock. Erfordert CYPHER 25. Ersetzt veraltete genai.vector.encode(). Verwenden Sie es, wenn Sie reines Cypher-GraphRAG schreiben, Knoten in ein Diagramm einbetten, strukturierte Karten aus Eingabeaufforderungen generieren oder LLMs innerhalb von Cypher-Abfragen aufrufen. Verarbeitet NICHT neo4j-graphrag-Python-Bibliothekspipelines – verwenden Sie neo4j-graphrag-skill. Übernimmt NICHT die Erstellung/Suche von Vektorindizes – verwenden Sie neo4j-vector-index-skill.
