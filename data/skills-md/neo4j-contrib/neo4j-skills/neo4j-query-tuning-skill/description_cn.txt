通过读取执行计划、识别不良运算符（AllNodesScan、CartesianProduct、Eager、NodeByLabelScan）以及规定修复（索引、提示、查询重写、运行时选择）来诊断和修复缓慢的 Neo4j Cypher 查询。当查询速度较慢时、当 EXPLAIN 或 PROFILE 输出需要解释时、当 dbHits 或 pageCacheHitRatio 很差时、当基数估计与实际值不同时，或者在分槽/管道/并行运行时之间做出决定时使用。涵盖 USING INDEX / USING SCAN / USING JOIN 提示、db.stats.retrieve、SHOW QUERIES、SHOW TRANSACTIONS、TERMINATE TRANSACTION。不从头开始编写新的 Cypher - 使用 neo4j-cypher-skill。不涵盖 GDS 算法调整 - 使用 neo4j-gds-skill。不涵盖索引/约束创建语法细节 - 使用 neo4j-cypher-skillreferences/indexes.md。
