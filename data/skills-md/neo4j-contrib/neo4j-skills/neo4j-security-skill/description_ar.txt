إدارة الأمان البرمجية في Neo4j - RBAC/ABAC، دورة حياة المستخدم (إنشاء/تغيير/إسقاط مستخدم)، دورة حياة الدور (إنشاء/منح دور/إسقاط دور)، منح الامتيازات ورفضها (منح/رفض/إلغاء على الرسم البياني، قاعدة البيانات، نظام إدارة قواعد البيانات)، التحكم في الوصول على مستوى الخاصية، التحكم في الوصول إلى الرسم البياني الفرعي، فحص إظهار الامتيازات، ومرجع تكوين موفر المصادقة (LDAP، OIDC/SSO). يُستخدم عندما يحتاج الوكيل إلى إدارة المستخدمين أو الأدوار أو الامتيازات برمجيًا عبر Cypher في قاعدة بيانات النظام. لا يتعامل مع كتابة استعلام Cypher — استخدم neo4j-cypher-skill. لا يتعامل مع العمليات العنقودية أو النسخ الاحتياطية - استخدم neo4j-cli-tools-skill. يتطلب الأمان على مستوى الخاصية وABAC إصدار Enterprise.
