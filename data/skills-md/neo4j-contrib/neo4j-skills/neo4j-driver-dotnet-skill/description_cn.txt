Neo4j .NET 驱动程序 v6 — IDriver 生命周期、DI 注册（单例）、ExecutableQuery 流畅 API、ExecuteReadAsync/ExecuteWriteAsync 托管事务、IResultCursor (FetchAsync/ ToListAsync)、记录值访问 (.Get<T>/As<T>)、空安全、UNWIND 批处理、临时类型、等待使用、EagerResult、对象映射、CancellationToken、错误处理和常见陷阱。在编写连接到 Neo4j 的 C# 或 .NET 代码时使用。还会在 Neo4j.Driver、IDriver、ExecutableQuery、ExecuteReadAsync、ExecuteWriteAsync、IResultCursor、IAsyncSession 或 .NET/C# 中的任何 Bolt/Aura 工作上触发。不处理 Cypher 创作 — 使用 neo4j-cypher-skill。不包括驱动程序版本升级 - 使用 neo4j-migration-skill。
