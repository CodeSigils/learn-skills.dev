Neo4j .NET 驅動程式 v6 — IDriver 生命週期、DI 註冊（單例）、ExecutableQuery 流暢 API、ExecuteReadAsync/ExecuteWriteAsync 託管事務、IResultCursor (FetchAsync/ ToListAsync)、記錄值存取權（A.A.A.A./A.SK.批次、臨時類型、等待使用、EagerResult、物件映射、CancellationToken、錯誤處理和常見陷阱。在編寫連接到 Neo4j 的 C# 或 .NET 程式碼時使用。也會在 Neo4j.Driver、IDriver、ExecutableQuery、ExecuteReadAsync、ExecuteWriteAsync、IResultCursor、IAsyncSession 或 .NET/C# 中的任何 Bolt/Aura 工作上觸發。不處理 Cypher 創作 — 使用 neo4j-cypher-skill。不包括驅動程式版本升級 - 使用 neo4j-migration-skill。
