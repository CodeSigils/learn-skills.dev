Neo4j .NET Driver v6 — IDriver ライフサイクル、DI 登録 (シングルトン)、ExecutableQuery Fluent API、ExecuteReadAsync/ExecuteWriteAsync マネージド トランザクション、IResultCursor (FetchAsync/ ToListAsync)、レコード値アクセス (.Get<T>/As<T>)、null 安全性、UNWIND バッチ処理、テンポラル型、待機の使用、EagerResult、オブジェクト マッピング、CancelToken、エラー処理、および共通罠。 Neo4j に接続する C# または .NET コードを作成するときに使用します。また、Neo4j.Driver、IDriver、ExecutableQuery、ExecuteReadAsync、ExecuteWriteAsync、IResultCursor、IAsyncSession、または .NET/C# でのボルト/Aura 作業でもトリガーされます。 Cypher オーサリングは処理しません。neo4j-cypher-skill を使用します。ドライバーのバージョンのアップグレードは対象外です。neo4j-migration-skill を使用してください。
