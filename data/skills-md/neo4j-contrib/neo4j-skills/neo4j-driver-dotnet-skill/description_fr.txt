Neo4j .NET Driver v6 — Cycle de vie IDriver, enregistrement DI (singleton), API fluide ExecutableQuery, transactions gérées ExecuteReadAsync/ExecuteWriteAsync, IResultCursor (FetchAsync/ ToListAsync), accès à la valeur d'enregistrement (.Get<T>/As<T>), sécurité nulle, traitement par lots UNWIND, types temporels, attente d'utilisation, EagerResult, mappage d'objets, CancellationToken, gestion des erreurs, et les pièges courants. À utiliser lors de l'écriture de code C# ou .NET pour la connexion à Neo4j. Se déclenche également sur Neo4j.Driver, IDriver, ExecutableQuery, ExecuteReadAsync, ExecuteWriteAsync, IResultCursor, IAsyncSession ou tout travail Bolt/Aura dans .NET/C#. Ne gère PAS la création de Cypher – utilisez neo4j-cypher-skill. Ne couvre PAS les mises à niveau de la version du pilote – utilisez neo4j-migration-skill.
