Neo4j .NET 드라이버 v6 — IDriver 라이프사이클, DI 등록(싱글톤), ExecutableQuery 흐름 API, ExecuteReadAsync/ExecuteWriteAsync 관리형 트랜잭션, IResultCursor(FetchAsync/ToListAsync), 레코드 값 액세스(.Get<T>/As<T>), null 안전, UNWIND 일괄 처리, 임시 유형, 대기 사용, EagerResult, 개체 매핑, CancellationToken, 오류 처리 및 일반 함정. Neo4j에 연결하는 C# 또는 .NET 코드를 작성할 때 사용합니다. 또한 Neo4j.Driver, IDriver, ExecutableQuery, ExecuteReadAsync, ExecuteWriteAsync, IResultCursor, IAsyncSession 또는 .NET/C#의 Bolt/Aura 작업에서도 트리거됩니다. Cypher 저작을 처리하지 않습니다. neo4j-cypher-skill을 사용하세요. 드라이버 버전 업그레이드는 다루지 않습니다. neo4j-migration-skill을 사용하십시오.
