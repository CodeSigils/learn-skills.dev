Orquesta el viaje completo desde cero hasta una aplicación Neo4j en ejecución. Ejecuta 8 etapas nombradas en orden: requisitos previos → contexto → provisión → modelo → carga → exploración → consulta → compilación. Cada etapa tiene su propio archivo de referencia en references/ que el agente lee y sigue al ingresar a esa etapa. Admite tanto HITL como funcionamiento totalmente autónomo. Presupuesto de tiempo: ≤15 min después de ejecutar DB (autónomo), ≤90 min en total (HITL).
