ゼロから Neo4j アプリケーションを実行するまでのプロセス全体を調整します。 8 つの名前付きステージを順番に実行します: 前提条件 → コンテキスト → プロビジョニング → モデル → ロード → 探索 → クエリ → ビルド。各ステージには、references/ 内に独自の参照ファイルがあり、エージェントはそのステージに入るときにこのファイルを読み取ってフォローします。 HITL と完全自律動作の両方をサポートします。時間予算: DB 実行後 15 分以内 (自律)、合計 90 分以内 (HITL)。
