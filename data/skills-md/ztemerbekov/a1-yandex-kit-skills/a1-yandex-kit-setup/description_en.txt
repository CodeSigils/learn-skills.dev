Connect a Yandex KIT store to the current AI client, or reconnect it by replacing its token. Use for natural‑language setup requests, including Russian text or voice transcripts such as “Connection with the store”, “Connect the store”, “Reconnect the store”, “Reinstall the connection with the store” and “Change the token”. Treat the short “Connection with the store” as an orientation request.
