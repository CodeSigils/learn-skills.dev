Connect a Yandex KIT store to the current AI client, or reconnect it by replacing its token. Use for natural‑language setup requests, including Russian text or voice transcripts such as “Store connection”, “Connect the store”, “Reconnect the store”, “Reinstall the store connection”, and “Change the token”. Treat the short “Store connection” as an orientation request.

Please always respond in the user's language.
