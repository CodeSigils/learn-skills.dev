Architettura neutrale rispetto al framework e standard di progettazione per l'ingegneria del frontend in TypeScript (React, Angular, Vue o qualsiasi framework di componenti): SOLID, codice pulito, logica espressiva, sicurezza dei tipi come progettazione, architettura pulita e limiti dei livelli, struttura basata sulle funzionalità, modello adattatore/repository, stato asincrono discriminato, livelli di stato e recupero dei dati, sicurezza, strategia di test, modelli tattici DDD, micro-frontend e moduli. Usalo quando generi un componente o un modulo, esegui il refactoring del codice frontend, rivedi una richiesta pull o prendi una decisione sull'architettura, o quando l'utente dice "revisione del codice", "architettura pulita", "SOLID", "modello di progettazione", "DDD", "contesto delimitato", "micro-frontend", "recupero dei dati", "convalida del modulo" o "come dovrei strutturarlo".
