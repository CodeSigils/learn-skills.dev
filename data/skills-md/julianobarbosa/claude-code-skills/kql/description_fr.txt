Création, débogage, optimisation, traduction et outils Kusto Query Language pour Azure Monitor, Sentinel, ADX et Application Insights. À UTILISER LORSQUE l'utilisateur mentionne « KQL », « Kusto », « Requête Log Analytics », « Requête Sentinel », « Requête de chasse », « Requête ADX », « Requête Application Insights », « Traduire SQL en KQL », « Splunk en KQL », « Optimiser la requête », « Performances KQL », « Fichier .kql », « Règle de détection », « Analyses » règle », « chasse aux menaces », « requête de moniteur Azure », « requête de journal », « opérateur de résumé », « où TimeGenerated » ou toute requête impliquant l'interrogation de données de journal/télémétrie Azure. Même si l'utilisateur ne dit pas explicitement « KQL » – s'il pose des questions sur l'interrogation des journaux Azure, des événements de sécurité ou des données de télémétrie, cette compétence s'applique.
