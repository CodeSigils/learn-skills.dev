Generate a Markdown file from a Google Docs URL and extract embedded images as local files. Use for requests like "read this Google Doc" or "load this document." Runs a Bash script to generate the Markdown and image files, then reads them with the Read tool.
