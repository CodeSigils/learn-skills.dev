Convenciones de la API de Truto: URL base (https://api.truto.one), autenticación de portador, patrones de URL unificados/proxy/personalizados, paginación del cursor, idempotencia, sintaxis de filtro de administrador y enrutamiento de habilidades entre `truto` (código de aplicación) y `truto-cli` (terminal). Cargue cada vez que llame, configure o razone sobre cualquier punto final de api.truto.one.
