After creating a reproducible feedback loop, we minimize bugs and verify hypotheses. It is used for failures of unknown cause, intermittent errors, or performance regressions, and not for simple changes where the cause and fix are already determined.
