After creating a reproducible feedback loop, we minimize bugs and validate hypotheses. It is used for failures of unknown cause, intermittent errors, or performance regressions, but not for simple changes where the cause and fix have been determined.
