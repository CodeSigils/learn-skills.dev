Вызывается как «/windowsfy [путь]» или когда пользователь запрашивает «исправить совместимость Windows», «сделать package.json кроссплатформенным», «переносимость Windows», «запустить в Windows», «cross-env», «shx», «ошибка PowerShell 5.1», «rm -rf в Windows», «chmod в Windows», «NODE_ENV не распознается», «glob не расширяется в Windows», «собственный двоичный файл в Windows», «tfjs-node» windows", "better-sqlite3 windows", "transformers windows", "токен && не является допустимым разделителем операторов", "одинарные кавычки в сценариях npm" или возникают сбои сценариев npm, характерные для собственных оболочек Windows. Путь может быть каталогом, отдельным файлом package.json или пропущен (по умолчанию ./).
