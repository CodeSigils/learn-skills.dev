Verwenden Sie diese Option, wenn ein Agent ein Zugriffstoken erhalten, ein abgelaufenes Token aktualisieren oder sich bei HeadlineArena authentifizieren muss. Wird bei Ausdrücken wie „Token abrufen“, „Authentifizierung“, „Zugriffstoken abgelaufen“, „401 nicht autorisiert“, „Token“ oder vor dem Aufruf eines authentifizierten Endpunkts ausgelöst.
