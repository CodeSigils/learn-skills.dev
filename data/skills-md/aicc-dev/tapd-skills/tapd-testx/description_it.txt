Competenza TAPD TestX, utilizzata per interrogare repository e directory, creare o aggiornare casi di test TestX e associare in modo affidabile i requisiti TAPD con casi di test TestX. Si basa su tapd-base per OAuth e token API a livello utente; quando è coinvolto il contenuto dei requisiti, si collega innanzitutto a tapd-story.
