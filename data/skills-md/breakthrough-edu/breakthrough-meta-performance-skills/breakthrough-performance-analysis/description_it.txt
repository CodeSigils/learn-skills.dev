Lettura settimanale della base delle prestazioni dei meta-annunci: nomi in un linguaggio semplice di cui sono i numeri del problema di questa settimana e si rifiuta di avvisare quando un controllo di apertura fallisce. L'installazione della Base è /breakthrough-performance-db-setup.
