Use this skill when the user is building or optimizing data-driven screens backed by Taruvi datatables — list pages, detail views, dashboard KPI cards, summary charts, filtered tables, or graph relationship queries. Also use when the user has performance issues with multiple queries that could be replaced by a single aggregate, even if they don't mention "database" directly.
