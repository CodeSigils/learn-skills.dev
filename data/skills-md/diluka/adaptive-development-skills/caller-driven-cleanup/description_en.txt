Use when removing dead code, adapters, exported items, feature toggles, dependencies, tests, or other paths that do not change behavior, and safety depends on the real caller and runtime loading proof.
