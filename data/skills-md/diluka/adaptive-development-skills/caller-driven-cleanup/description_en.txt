Use when deleting dead code, adapters, exported items, feature flags, dependencies, tests, or other paths that do not change behavior, and safety depends on the actual caller and runtime loading verification.
