Use when the task needs to create or check out a development branch for project changes, the root path is already occupied by such a development branch, performing read‑only review of other branches or versions, executing high‑risk Git operations, working in parallel, or the target files overlap with existing changes.
