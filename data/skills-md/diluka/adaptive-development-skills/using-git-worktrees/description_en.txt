Use when a task requires creating or checking out a development branch for project changes, the root path is occupied by such a development branch, read‑only reviewing other branches or versions, performing high‑risk Git operations, working in parallel, or the target files overlap with existing changes.
