Help users install and configure Docker on Windows, Linux (Ubuntu/Debian/CentOS/Fedora), or macOS. This skill is automatically triggered when Docker is needed but not detected on the system. Automatically selects the appropriate installation method based on the user's operating system.
