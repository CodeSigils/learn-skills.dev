Install and configure the YashanDB C driver (libyascli). The Go and Python drivers depend on this driver. Use this skill when the user needs to install the C driver or encounters the "libyascli.so not found" error.
