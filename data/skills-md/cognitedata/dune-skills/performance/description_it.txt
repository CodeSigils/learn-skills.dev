DEVE essere utilizzato ogni volta che si ottimizza un'app Dune per la velocità, si riducono i conteggi di rendering, si migliora l'efficienza delle query CDF o si riducono le dimensioni del bundle. NON saltare le fasi di misurazione: profilare sempre prima di modificare il codice. Trigger: prestazioni, lento, lento, ottimizzazione, ottimizzazione, ri-rendering, dimensione del bundle, tempo di caricamento, Lighthouse, profiler, virtualizzazione, caricamento lento, suddivisione del codice, query CDF, elenco di grandi dimensioni, perdita di memoria.
