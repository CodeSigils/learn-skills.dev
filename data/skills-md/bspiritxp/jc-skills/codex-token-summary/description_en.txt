Count the tokens consumed by the local Codex in recent periods, categorized by task purpose, and output a Chinese table that includes the model and category proportions; output the Faster x2 status only when an explicit session-level field is present.
