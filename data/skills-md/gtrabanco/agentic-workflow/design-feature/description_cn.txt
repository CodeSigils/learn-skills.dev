将想法或功能请求转化为详尽的、可检查的产品定义——这是工程规划之前的阶段。核心机制是**功能关闭**：一个清单，强制功能引入的每个实体、功能和角色都走遍其全部表面（CRUD + 状态转换 + UI 入口点 + API + 测试，或显式设计时“n/a”），因此非前沿执行器模型停止默默地省略隐式工作（“使用仪表板管理和 ACL 进行身份验证”不得折叠为用户表 + 列表视图）。闭包具有三个固定清单：**实体闭包**（每个实体到整个表面）、**集成闭包**（针对项目能力清单中的每个子系统进行协调的功能，`docs/CAPABILITIES.md` — auth、ACL、导航、通知等）和 **角色矩阵**（每个能力清单角色明确允许/拒绝） — 加上一个 **期望扫描**，强制有能力的人隐式假设（“博客有草稿”）纳入范围内/范围外/推迟，从未未说明。写入 SPEC 的 **产品一半** 并在计划功能的工程一半之前标记“## 设计状态：已设计”标记“计划功能”。裸露的“design-feature <slug>”审查并询问要更改的内容； `design-feature <slug> <instruction>` 直接应用更改。在 Claude Code 上并且想要手动调整每技能模型/努力等级？安装 `#claude` 分支（`npx Skills add gtrabanco/agentic-workflow#claude`） - 请参阅自述文件。该分支与模型无关：该技能继承您的代理会话已经使用的任何模型和工作。触发器：“添加功能”、“添加功能”、“新功能”、“设计功能”、“设计 NN 的产品一半”、“NN 的能力收敛”、“在规划功能之前定义功能”。
