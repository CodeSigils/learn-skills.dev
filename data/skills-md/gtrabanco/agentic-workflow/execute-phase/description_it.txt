Implementa una fase di una funzionalità (impostazione predefinita), una piccola funzionalità end-to-end in un unico passaggio (solo SPEC, senza artefatti di pianificazione) o una correzione (--fix). Applica la sicurezza delle filiali, la policy sui problemi, il cancello di verifica del progetto e la disciplina dei documenti per fase. Sei su Claude Code e desideri livelli di modello/impegno ottimizzati manualmente in base alle competenze? Installa invece il ramo `#claude` (`npx skills add gtrabanco/agentic-workflow#claude`) — consulta il file README. Questo ramo è indipendente dal modello: la competenza eredita qualunque modello e sforzo stia già utilizzando la sessione dell'agente. Trigger: "esegui la fase P1 di NN", "implementa la funzione NN", "crea NN dalle sue specifiche", "esegui-fase NN P2", "esegui-fase --fix".
