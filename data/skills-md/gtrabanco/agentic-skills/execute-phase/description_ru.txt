Реализуйте одну фазу функции (по умолчанию) или небольшую функцию/исправление (--fix) с помощью реестра ## Phases SPEC — одна фаза на каждый вызов, последняя фаза всегда — усиление и PR (закрытие); устаревшие спецификации без ## фаз выполняются сквозным образом за один проход. Обеспечивает безопасность филиалов, политику проблем, шлюз проверки проекта и дисциплину документации на каждом этапе. Вы используете Claude Code и хотите вручную настроить модель/уровень усилий для каждого навыка? Вместо этого установите ветку #claude (навыки npx добавляют gtrabanco/agentic-workflow#claude) — см. README. Эта ветвь не зависит от модели: навык наследует любую модель и усилие, которые уже использует сеанс вашего агента. Триггеры: «выполнение фазы P1 NN», «реализовать функцию NN», «построить NN на основе ее спецификации», «фаза выполнения NN P2», «фаза выполнения --fix».
