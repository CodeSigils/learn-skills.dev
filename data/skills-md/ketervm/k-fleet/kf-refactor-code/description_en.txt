Improve internal code structure while preserving externally observable behavior. Use for cleanup with a concrete structural goal such as reducing duplication or complexity. Do not use when the primary request adds behavior, fixes a defect, investigates a cause, or only validates changes.
