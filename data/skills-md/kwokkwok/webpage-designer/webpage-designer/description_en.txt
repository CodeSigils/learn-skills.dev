Convert the content provided by the user into a web page. First understand the content, then choose a style based on it (or use the style specified by the user), load the corresponding prompt, and finally generate the web page code. When to use: when the user needs to generate a web page, landing page, portfolio, or any scenario that requires a web interface.
