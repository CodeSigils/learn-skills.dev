공급자 어댑터 선택 및 구성: openaiText, anthropicText, geminiText, ollamaText, grokText, groqText, openRouterText, bedrockText, byteplusText, openaiCompatible. modelOptions, 추론/사고 구성, 런타임 어댑터 전환, 사용자 정의 모델을 위한 확장Adapter(), createModel()을 통한 모델별 유형 안전성. @tanstack/ai-openai/ Compatible의 openaiCompatible({ baseURL, apiKey, models })을 통한 일반 OpenAI 호환 제공자(DeepSeek, Together, Fireworks 등). API 키 환경 변수: OPENAI_API_KEY, ANTHROPIC_API_KEY, GOOGLE_API_KEY/GEMINI_API_KEY, XAI_API_KEY, GROQ_API_KEY, OPENROUTER_API_KEY, OLLAMA_HOST, BEDROCK_API_KEY(또는 AWS_BEARER_TOKEN_BEDROCK). BytePlus에는 ARK_API_KEY(ModelArk — 채팅/비디오/이미지) 및 BYTEPLUS_VOICE_API_KEY(시드 음성 — TTS/녹화)라는 두 개의 키가 필요합니다. 둘 다 다른 것에 대한 대체가 아닙니다.
