提供程序适配器选择和配置：openaiText、anthropicText、geminiText、ollamaText、grokText、groqText、openRouterText、bedrockText、byteplusText、openaiCompatible。每个模型的类型安全性，包括 modelOptions、推理/思考配置、运行时适配器切换、自定义模型的extendAdapter()、createModel()。通过来自 @tanstack/ai-openai/complete 的 openaiCompatible({ baseURL, apiKey, models }) 实现通用 OpenAI 兼容提供程序（DeepSeek、Together、Fireworks 等）。 API 密钥环境变量：OPENAI_API_KEY、ANTHROPIC_API_KEY、GOOGLE_API_KEY/GEMINI_API_KEY、XAI_API_KEY、GROQ_API_KEY、OPENROUTER_API_KEY、OLLAMA_HOST、BEDROCK_API_KEY（或 AWS_BEARER_TOKEN_BEDROCK）。 BytePlus 需要两个密钥：ARK_API_KEY（ModelArk — 聊天/视频/图像）和 BYTEPLUS_VOICE_API_KEY（种子语音 — TTS/转录）；两者都不是对方的后备力量。
