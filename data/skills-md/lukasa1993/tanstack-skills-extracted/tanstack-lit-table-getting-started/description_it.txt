Crea una tabella TanStack Lit Table v9 con un campo host TableController stabile, tableFeatures esplicite, controller.table(opzioni, selettore) durante il rendering e modelli Lit headless. Carica per l'impostazione della prima tabella, il ciclo di vita di TableController, FlexRender o l'adattamento di un esempio di React a Lit.
