collection.insert, collection.update (proxy bozza in stile Immer), collection.delete. createOptimisticAction (onMutate + mutazioneFn). createPacedMutations con debounceStrategy, ThrottleStrategy, QueueStrategy. createTransaction, getActiveTransaction, contesto della transazione ambientale. Ciclo di vita della transazione (in sospeso/persistente/completata/non riuscita). Fusione di mutazioni. gestori onInsert/onUpdate/onDelete. Tipo di mutazione in sospeso. Transaction.isPersisted.
