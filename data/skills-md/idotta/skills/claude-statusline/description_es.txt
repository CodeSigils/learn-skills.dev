Instale o personalice la línea de estado de Claude Code: la barra de una línea debajo del mensaje que muestra el nombre del modelo, el esfuerzo de razonamiento, el directorio actual, la rama git y la bandera sucia, y el contexto utilizado. Incluye un script bash coloreado listo para usar más un instalador de un solo comando que conecta settings.json y documenta el contrato JSON estándar para que los segmentos y colores se puedan cambiar con confianza. Use esto siempre que el usuario mencione su línea de estado/línea de estado/configuración de línea de estado, solicite mostrar el modelo, nivel de esfuerzo, directorio de trabajo, rama de git o porcentaje de contexto en su barra de mensajes, quiera cambiar el estilo o reordenar lo que ya está allí, quiera configurar la misma línea de estado en otra máquina o compartirla con un repositorio, o informe que su línea de estado está en blanco, rota o muestra información obsoleta o incorrecta, incluso cuando nunca diga la palabra "línea de estado".
