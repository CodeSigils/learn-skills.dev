Snapshot dell'architettura dichiarata per un agente Agentforce: pianificatore, argomenti, azioni, flussi, Apex, modelli di prompt e plug-in NGA. Rende un documento di architettura leggibile dall'uomo e un grafico di chiamata della sirena dai metadati in fase di progettazione (non righe di controllo di runtime). ATTIVA quando l'utente chiede di descrivere, creare un diagramma, un inventario, un controllo, un documento o una differenza (ad esempio v3 vs v5) l'architettura/albero delle azioni/struttura degli argomenti/inventario degli strumenti di un agente specifico in base al nome dell'API dell'agente in un'organizzazione specifica. NON ATTIVARE per tracce di sessioni di runtime, trascrizioni di conversazioni, tempi di generazione o catene di controllo del gateway: questa abilità legge solo i metadati in fase di progettazione (utilizza investigating-agentforce-d360 per le tracce di sessione).
