當專案包含 uiBundles/*/src/ 目錄或 sfdx-project.json 並且提示顯示從頭開始建立、建置、建構或產生新的應用程式、網站或頁面時，必須啟動 - 即使提示也描述了視覺樣式。当任务跨越多个 ui-bundle 技能时也必须激活。在端到端构建完整的应用程序时使用此技能。这是协调器，以正确的依赖顺序协调脚手架、功能、数据访问、前端 UI、集成和部署。如果没有它，阶段执行就会乱序，并且应用程序会崩溃。请勿用于具有自定义对象的 Lightning Experience 应用程序（使用生成闪电应用程序）。不要用于对现有页面的单关注编辑（使用building-ui-bundle-frontend）。
