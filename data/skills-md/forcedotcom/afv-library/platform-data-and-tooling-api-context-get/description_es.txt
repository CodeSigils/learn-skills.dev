Referencia autorizada de campo/esquema para objetos ESTÁNDAR 2130 de Salesforce: utilícela para buscar nombres, tipos, propiedades (filtrables/clasificables/agrupables/actualizables) de API de campos de herramientas y objetos estándar (filtrables/clasificables/agrupables/actualizables) y nombres de relaciones para cuenta, contacto, oportunidad, cliente potencial, caso, ApexClass, ApexCodeCoverage, TraceFlag y más. Cargue esto junto con una habilidad SOQL/consulta/Apex cuando los nombres de campo, tipos o capacidades de filtro/clasificación/grupo no estén verificados, no cuando los campos ya se conocen y solo se necesita sintaxis de consulta u optimización. TRIGGER al verificar, validar o depurar campos en una consulta SOQL/SOSL o DML contra un objeto estándar (capacidades, rutas de relación/subconsulta o qué campos tiene un objeto) para que la consulta se ejecute en lugar de adivinar. Los objetos __c personalizados y los campos __c NO están en estos activos; describe la organización en vivo para aquellos (sf sobject describe). NO DISPARE para crear/implementar *-meta.xml o fuente sfdx (use la habilidad API de metadatos).
