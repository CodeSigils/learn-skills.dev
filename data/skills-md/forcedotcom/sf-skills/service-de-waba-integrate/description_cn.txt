确认客户的 WhatsApp Business 帐户与 Meta 端的 Salesforce 共享。纯指令 + 一个用户确认提示 — 没有 API 调用，没有令牌处理。实际共享发生在 Meta Business Suite 中（仅 UI；Meta 不提供图形端点来自动执行此步骤）。呈现分步说明，并替换为 Salesforce 的 Business Manager ID，然后要求用户确认“Salesforce”是否出现在 WABA 的合作伙伴列表中。
