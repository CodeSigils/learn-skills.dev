在用户明确确认后，通过 Connect API 同步端点重新同步管道上已配置的 DevOps Center 测试提供程序以获取新的测试套件。获取 pipelineId 和一个或多个 testProviderId 并触发异步重新同步，以便新添加的套件可供分配​​。当测试提供程序（例如 Apex Unit Tests、代码分析器、Flow Tests、Provar）自上次配置以来添加了套件并且用户希望这些套件显示在 DevOps Center 中时，请使用此技能。在以下情况下触发：用户想要重新同步测试提供程序、从提供程序拉入新套件、刷新提供程序的套件列表，或者说已配置提供程序的指定套件丢失/过期。不要在以下情况下触发：首次配置提供程序（创建新的 DevopsPipelineTestProvider 配置），或将现有套件分配/映射到阶段（使用管理套件分配）。
