Da utilizzare durante la revisione o l'implementazione delle migliori pratiche di Lightning Data Service in un LWC (.js, .html, .js-meta.xml): UIAPI vs Apex, refreshApex/notifyRecordUpdateAvailable, importazioni @salesforce/schema, modelli di dati di moduli record LDS. TRIGGER su "applica le migliori pratiche LDS a questo LWC", "rivedi questo LWC per problemi relativi alle migliori pratiche LDS", "rivedi questo componente per problemi con Lightning Data Service", "UIAPI o Apex per questi dati?", "correggi i dati non aggiornati dopo il salvataggio del record", "sincronizza la cache LDS", "usa @salesforce/schema per i nomi dei campi", "scegli tra getRecord e Apex". NON SI ATTIVA quando si crea un nuovo LWC (usa experience-lwc-generate), applica token di progettazione SLDS (usa design-systems-slds-apply), seleziona o cabla genericamente oggetti di scena/eventi/slot di un componente base `lightning-*` (usa experience-lwc-base-components-integrate — questa abilità copre solo la logica del livello dati LDS, anche quando la correzione implica un modulo di record di base), o per revisioni di sicurezza/RTL/accessibilità (pass separati).
