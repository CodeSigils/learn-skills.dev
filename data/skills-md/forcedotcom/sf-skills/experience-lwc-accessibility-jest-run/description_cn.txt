在为 Lightning Web 组件运行 Sa11y 可访问性 Jest 测试时，请始终使用 — 在推送之前在本地进行、生成精确的命令、运行一个文件与整个套件、通过命名约定选择测试、保存 Sa11y 渲染的 HTML 或重现 Sa11y / A11yBug 故障。涵盖 Salesforce 核心（Bazel；`WORKSPACE`、`./core` root、`bazel test //<module>:<target>`）和独立的 `npx jest`（`.accessibility.test.js`、`@sa11y/jest`）。当提示要求提供 `run-plan.md` 进行可访问性测试时，这是必需的，退出代码意味着可访问性测试失败，或者引用 `SA11Y_*` 环境变量、`A11yBug` 或 core-Bazel `modules/<mod>/<cmp>/__tests__/*.test.js` 路径。不要依赖常识——该技能具有正确的单文件 Bazel 目标和 EPERM 解决方法。不要触发功能（非可访问性）Jest 测试、不运行测试的 a11y 审查（使用 experience-lwc-accessibility-validate）或基于 Selenium 的 a11y 功能测试。
