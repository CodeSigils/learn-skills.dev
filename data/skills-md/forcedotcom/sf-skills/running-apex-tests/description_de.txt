Apex-Testausführung, Abdeckungsanalyse und Test-Fix-Schleifen mit 120-Punkte-Bewertung. Verwenden Sie diese Option, wenn der Benutzer Apex-Tests ausführen, die Codeabdeckung überprüfen, fehlgeschlagene Tests beheben oder mit *Test.cls-/*_Test.cls-Dateien arbeiten muss. TRIGGER, wenn: der Benutzer Apex-Tests ausführt, die Codeabdeckung überprüft, fehlgeschlagene Tests behebt oder *Test.cls-/*_Test.cls-Dateien berührt. NICHT AUSLÖSEN, wenn: Apex-Produktionscode geschrieben (generierende-apex verwenden), Agentforce-Agententests (testing-agentforce verwenden) oder Jest/LWC-Tests (generierende-lwc-Komponenten verwenden) getestet werden.
