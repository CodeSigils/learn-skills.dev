標準の REST オブジェクト エンドポイントを介して `MessagingChannelUsage.DeploymentStatus` を `Disabled` から `Provisioning` にパッチして、拡張された `MessagingChannel` (WhatsApp / Apple / Facebook / SMS / RCS) をアクティブにします。 UDD 保存フックは REST 書き込み時に起動し、外部コールアウト (`WHATS_APP` → Meta `/register`、`FACEBOOK` → `subscribeFacebookPage`、`TEXT` → `registerCsotSms`。Apple、LINE などは外部呼び出しを必要としません) に対して `MessageType` によってディスパッチし、ターミナルに `Active` または `Error` を書き込み、反転します。 「MessagingChannel.IsActive」。チェーンは HTTP 応答で同期します。WhatsApp は、メタが登録を確認した後 (約 15 ～ 21 秒) にのみ 204 を返します。外部コールアウトのないチャネルはほぼ即時 (Apple では約 1 秒) です。 Aura RPC はありません。 CSRF Coo​​kie はありません。このスキルは、ユーザーがすでに挿入されている拡張メッセージング チャネルを REST 経由でヘッドレスでアクティブ化する必要がある場合に使用します。
