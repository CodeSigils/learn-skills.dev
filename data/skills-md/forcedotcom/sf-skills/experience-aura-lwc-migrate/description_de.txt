Verwenden Sie diese Fähigkeit, um ein Salesforce Aura-Komponentenpaket (.cmp, .app, .evt, .intf, Controller.js, Helper.js, Renderer.js) zu analysieren und einen Framework-unabhängigen Migrationsentwurf (PRD.yaml / PRD.md / PRD.json) zu erstellen, der öffentliche API, Datenanforderungen, Slots, Ereignisse, Zustände, Zugänglichkeit, Stil, Lokalisierung und Sicherheitslage erfasst. Dies ist nur Phase 1 (Analyse + PRD) – es werden KEINE Komponentencodes, Wire-Adapter oder Jest-Tests erstellt oder bearbeitet und es werden keine .html/.js/.css/.js-meta.xml geschrieben. TRIGGER, wenn der Benutzer „Diese Aura-Komponente analysieren“, „Aura migrieren“, „.cmp konvertieren“, „Migrationsblaupause erstellen“, „PRD für diese Aura-Komponente generieren“ sagt oder aura:attribute, aura:handler, force:recordData, $Label, $Resource, Component.get, cmp.find oder ein Eingabeverzeichnis mit .cmp-Dateien erwähnt. NICHT AUSLÖSEN, wenn keine Aura-Quelle vorhanden ist (nachgelagerte LWC-Erstellung an experience-lwc-generate delegieren), für die Bewertung nach der Migration oder für die Umgestaltung einer vorhandenen modernen Komponente.
