Scrivi, esegui e analizza suite di test strutturati per gli agenti Agentforce. TRIGGER quando: l'utente scrive o modifica la specifica di test YAML (AiEvaluationDefinition); esegue sf agent test create, run, run-eval o i comandi dei risultati; chiede informazioni sulla strategia di copertura dei test, sulla selezione delle metriche o sulle valutazioni personalizzate; interpreta i risultati dei test o diagnostica i fallimenti dei test; chiede informazioni su test batch, suite di regressione o integrazione di test CI/CD. NON SI ATTIVA quando: l'utente crea, modifica, visualizza l'anteprima o esegue il debug dei file .agent (usa development-agentforce); distribuisce o pubblica agenti; scrive il codice dello script dell'agente; utilizza l'anteprima dell'agente sf per l'iterazione dello sviluppo; analizza le tracce della sessione di produzione (usa osservatore-agentforce).
