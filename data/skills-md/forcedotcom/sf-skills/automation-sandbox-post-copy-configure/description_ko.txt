대상 조직에 Salesforce sandbox 복사 후 자동화 JSON 구성을 적용합니다. 각 항목에 대해 기술은 항목의 `ConfigurationName`에서 올바른 도구 API 개체를 파생시키고, 설명 프로브를 통해 파생을 확인하고, SOQL-over-REST를 통해 레코드 ID를 확인한 다음, `sf api request Rest`를 사용하여 복합 `메타데이터` 필드를 통해 레코드를 패치합니다. 사용자가 샌드박스에 대해 복사 후 또는 새로 고침 후 구성 파일(예: `post-copy-config.json`)을 적용, 실행, 실행, 테스트 실행 또는 미리보기하도록 요청할 때 사용합니다. 트리거 문구: "복사 후 구성 적용", "복사 후 자동화 실행", "샌드박스 새로 고침 후 JSON 실행", "샌드박스 새로 고침 구성 적용", "새로 고침 후 샌드박스 구성". SOP에서 구성 JSON을 생성하거나(`automation-sandbox-post-copy-config-generate`에 위임) 메타데이터 XML을 배포하기 위해 트리거하지 마세요.
