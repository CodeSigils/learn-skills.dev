当用户想要构建、创建或生成 React 应用程序、React 应用程序、Web 应用程序、单页应用程序 (SPA) 或前端应用程序时，必须激活 - 即使尚不存在项目文件。当项目包含 uiBundles/*/src/ 目录或 sfdx-project.json 并且提示显示从头开始创建、构建、构建或生成新的应用程序、站点或页面时，也必须激活 - 即使提示还描述了视觉样式。当任务跨越多个 ui-bundle 技能时也必须激活。在端到端构建完整的应用程序时使用此技能。这是协调器，以正确的依赖顺序协调脚手架、功能、数据访问、前端 UI、集成和部署。如果没有它，阶段执行就会乱序，应用程序也会崩溃。请勿用于具有自定义对象的 Lightning Experience 应用程序（使用生成闪电应用程序）。不要用于对现有页面的单关注编辑（使用building-ui-bundle-frontend）。
