DEVE essere attivato quando l'utente desidera creare, creare o generare un'applicazione React, un'app React, un'applicazione web, un'applicazione a pagina singola (SPA) o un'applicazione frontend, anche se non esistono ancora file di progetto. DEVE essere attivato anche quando il progetto contiene una directory uiBundles/*/src/ o sfdx-project.json e il prompt dice crea, costruisci, costruisci o genera una nuova app, sito o pagina da zero, anche se il prompt descrive anche lo stile visivo. DEVE essere attivato anche quando l'attività comprende più di una competenza del pacchetto ui. Utilizza questa competenza quando crei un'app completa end-to-end. Questo è l'orchestratore che coordina impalcature, funzionalità, accesso ai dati, interfaccia utente frontend, integrazioni e distribuzione nell'ordine di dipendenza corretto. Senza di esso, le fasi vengono eseguite in modo non corretto e l'app si interrompe. NON utilizzare per le app Lightning Experience con oggetti personalizzati (utilizza generation-lightning-app). NON utilizzare per modifiche singole a una pagina esistente (utilizzare building-ui-bundle-frontend).
