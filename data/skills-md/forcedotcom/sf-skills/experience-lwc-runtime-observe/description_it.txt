Da utilizzare durante l'esecuzione di Salesforce Lightning Preview per un'app o un singolo componente LWC per estrarre il DOM di runtime per l'ispezione. TRIGGER quando l'utente dice "anteprima di un LWC localmente", "esegui sf lightning dev", "avvia Local Dev", "ispeziona il DOM shadow di un componente in fase di runtime", "prendi HTML renderizzato da un'anteprima live", "risolvi i problemi di ciò che effettivamente viene visualizzato rispetto al modello", "imposta il plug-in Salesforce Live Preview", "anteprima Lightning", "server di sviluppo locale" o "estrai DOM runtime". NON SI ATTIVA quando l'utente sta creando o modificando il codice sorgente LWC (usa experience-lwc-generate) o rivedendo staticamente il codice del componente senza eseguirlo.
