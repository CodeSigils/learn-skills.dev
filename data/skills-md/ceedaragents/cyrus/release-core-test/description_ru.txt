Вызывайте при тестировании разработки изменений Cyrus, которые охватывают CYPACK (edgeworker + CLI) и CYHOST (графический интерфейс, размещенный на Vercel), и размещенный графический интерфейс должен указывать на невыпущенное ядро ​​Cyrus из этого репозитория. Публикует `cyrus-core` (и `claude-runner`, если необходимо) как предварительную версию `-test.N` под dist-тегом `test` npm, чтобы CYHOST мог установить его через `cyrus-core@test`, не затрагивая `latest` dist-тег и не отправляя реальный выпуск.
