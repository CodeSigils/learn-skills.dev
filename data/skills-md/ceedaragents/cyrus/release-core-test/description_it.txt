Richiamare durante il test di sviluppo una modifica Cyrus che si estende su CYPACK (edgeworker + CLI) e CYHOST (GUI ospitata da Vercel) e la GUI ospitata deve puntare a un "cyrus-core" non rilasciato da questo repository. Pubblica `cyrus-core` (e `claude-runner` se necessario) come prerelease `-test.N` sotto il tag dist npm `test` in modo che CYHOST possa installarlo tramite `cyrus-core@test` senza influenzare il tag dist `latest` o spedire una versione reale.
