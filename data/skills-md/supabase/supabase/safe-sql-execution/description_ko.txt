코드가 사용자의 실제 Postgres 데이터베이스에 대해 실행되는 SQL을 빌드, 반환, 가져오기 또는 실행할 때마다 사용합니다. 요청이 일반 기능이나 버그 수정처럼 읽히고 "보안", "주입" 또는 "SafeSqlFragment"라고 말하지 않는 경우에도 사용합니다. 여기에는 데이터베이스 개체(테이블, 뷰, 함수, DB 트리거, 인덱스, RLS 정책)에 대한 SQL을 구축/반환하는 pg-meta 함수, 쿼리 빌더 또는 엔드포인트를 작성하거나 편집하는 작업이 포함됩니다. 스키마/테이블/열/검색/경로 매개변수 값을 SQL 텍스트에 삽입합니다. 데이터베이스에서 왕복하는 SQL을 저장, 가져오기 또는 재실행합니다(정책 정의, 함수/뷰 정의, 조각의 저장된 콘텐츠). 그리고 프로젝트의 데이터베이스에 SQL을 보내는 "실행"/"적용"/"실행" 작업(SQL 편집기 실행 선택, 정책 편집기 적용, 조각 실행기). 완성된 diff를 검토할 때뿐만 아니라 이러한 코드를 작성하기 전에 이를 로드하십시오. SQL 텍스트나 실행에 영향을 주지 않는 변경 사항(스타일 지정, 관련 없는 데이터 후크, 비SQL 양식 유효성 검사 또는 UI 레이아웃 작업)만 건너뜁니다.
