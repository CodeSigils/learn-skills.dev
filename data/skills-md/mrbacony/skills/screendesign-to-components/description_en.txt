Extracts Angular components from a screen design image. Analyzes the design pixel-perfectly, decomposes it into a component tree, identifies reusable shared UI, and implements each component with Tailwind CSS / DaisyUI. Use when the user provides a screenshot, mockup, or screen design and wants it turned into Angular components.
