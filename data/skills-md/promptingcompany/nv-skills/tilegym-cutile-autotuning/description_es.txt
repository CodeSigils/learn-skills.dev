Úselo al agregar, modificar, optimizar o depurar código de autoajuste de CuTile. Señales de activación: `exhaustive_search` / `replace_hints` / `hints_fn` / `cuda.tile.tune` en el código, `autotune` en los nombres de archivos o problemas de corrección/rendimiento en los núcleos CuTile autoajustados. Cubre: ajuste único/caché/patrón de lanzamiento, configuraciones por arquitectura (sm80–sm120), diseño del espacio de parámetros (tamaños de mosaicos, ocupación, num_ctas) y 7 errores comunes con soluciones.
