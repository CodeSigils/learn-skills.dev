Exécute le workflow d'intégration puis d'extraction DEFT pour les itérations VCN AOI : intègre le parquet cible d'analyse des écarts, intègre un pool de sources et exploite les images sources des voisins les plus proches pour une augmentation en aval. Use as the immediate next step after `tao-route-visual-changenet-samples` when expanding a real-image augmentation queue from the mining subset.
