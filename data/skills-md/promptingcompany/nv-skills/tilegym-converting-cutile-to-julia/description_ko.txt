cuTile Python GPU 커널(@ct.kernel)을 cuTile.jl Julia에 상응하는 커널로 변환합니다. 커널 구문 변환, 0-인덱스에서 1-인덱스로의 변환, 브로드캐스팅 차이점, 메모리 레이아웃(행 주요에서 열 주요로), 유형 시스템 매핑 및 실행 API 차이점을 처리합니다. cuTile Python 커널을 Julia cuTile.jl로 변환, 이식 또는 번역하거나 기존 Julia cuTile 번역을 디버깅/최적화할 때 사용합니다.
