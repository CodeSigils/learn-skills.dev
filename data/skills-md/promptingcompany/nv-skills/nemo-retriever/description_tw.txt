使用它可以從文件集合中提取特定的數字、事實、引用或表格值，並引用確切的來源文件和頁面。專為報告資料夾中的問答而建構 - 年度報告、10-K 和財務文件、研究 PDF、掃描表格/圖像 (`.jpg` `.png` `.tiff`)、Office (`.docx` `.pptx`)、HTML / TXT、音訊和視訊。它對整個語料庫進行一次索引，然後在眾多文檔中找到正確的文檔，並返回帶有頁碼的值——當多個文檔或圖形看起來相似並且您需要正確的文檔或圖形而不是近似匹配時，這很有用。伸手去拿它，而不是一次閱讀或抓取 PDF 一份。不適用於：編輯文件、網頁瀏覽、單一純文字檔、微調。
