Revise la diferencia de la rama actual con su rama base: la rama base del PR abierto cuando exista; de lo contrario, la rama predeterminada del repositorio. Marca errores recién introducidos (errores lógicos, derefs nulos/indefinidos, condicionales invertidos, asíncrono no controlado, off-by-one), regresiones de funcionalidad, ganchos poco comunes innecesarios (useRef/useEffect/useMemo/useCallback), funcionalidad que duplica utilidades/ayudantes/componentes compartidos existentes (duplicados con nombre y reimplementaciones en línea), código que se desvía de las convenciones/arquitectura/sintaxis establecidas en el base de código y código sobrante obsoleto de la iteración. Úselo cuando el usuario ejecuta /self-code-review o solicita una revisión de su rama actual, una verificación previa al PR o "revisar mis cambios".
