Umfassende PR-Überprüfung von TypeScript/React. Gibt die strukturierte Datei „review.json“ aus, die von einem Posting-Skript verwendet wird, das eine GitHub-Rezension mit Inline-Kommentaren erstellt. Agent wird schreibgeschützt ausgeführt; Das Workflow-Skript verfügt über Schreibberechtigungen.
