Translate and localize the Avo admin — resource, action, and field labels, field help/placeholder/include_blank text, save-button labels, the interface language, and right-to-left layout — via `config/locales/avo.<locale>.yml`, `self.translation_key`, and `config.locale`. Use when the user wants to translate the admin to another language (French/Spanish/German/Arabic/…), localize or rename resource/field/action labels, make the admin multilingual, support RTL languages, change or force the admin's interface language, or show a different label per language.
