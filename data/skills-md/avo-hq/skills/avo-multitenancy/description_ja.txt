Avo 管理者の範囲をテナント (アカウント、チーム、組織) ごとに設定し、各顧客が自分のデータのみを表示できるようにします。ルート ベースのテナント (/:tenant_id + config.default_url_options + set_tenant before_action の下に Avo をマウント) と、アカウント スイッチャーを使用したセッション ベースのテナントをカバーし、すべて自分で設定した Avo::Current.tenant / tenant_id 属性に基づいて構築されます。ユーザーがアカウント/チーム/組織ごとに管理者をスコープする場合、各顧客が管理者で自分のデータのみを表示するようにする場合、テナント スコープを追加する場合、マルチテナント管理者を構築する場合、アカウント/テナント スイッチャーを追加する場合、/:account_id で管理者をマウントする場合、または現在のアカウントに基づいて管理者にデータを切り替える場合に使用します。これには、「管理者は現在のアカウントに基づいてデータを切り替える必要がある」または「各クライアントは自分のデータのみを表示する必要がある」など、Avo について言及していない Rails 形式のリクエストが含まれます。記録。」
