Сантехника движка Rails, с которой вы работаете при написании пользовательского Ruby, который работает внутри Avo — помощники пути движка (`avo.` против `main_app.`), `Avo::Current`, `Avo::ExecutionContext`, зарезервированные имена моделей / конфликты маршрутов, `Avo::Services::EncryptionService` и вызов помощников вашего приложения изнутри Avo. Используйте, когда пользователь хочет подключиться от администратора обратно к своему основному приложению, исправить `неопределенный метод `posts_path'` (или маршрут, ведущий к неправильной странице) внутри Avo, получить доступ к текущему пользователю/запросу/параметрам/контексту/тенанту внутри блока Avo, узнать, какие переменные (`record`, `resource`, `view`, `current_user`) доступны в лямбде или почему `record` равен нулю, используйте один из помощников представления своего приложения внутри поле Avo, исправьте модель с именем `resource` / `chart` / `search` / `home` / `attachment`, которая нарушает работу администратора или вызывает конфликты маршрутизации, сохраните модель с зарезервированным именем при переименовании ее ресурса Avo (`--model-class`), исправьте конфликт вспомогательных маршрутов `resources :resources` или зашифруйте/расшифровайте значение для безопасной передачи через параметры Avo. Установите арендатора для текущего запроса администратора.
