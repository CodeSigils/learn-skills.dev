Anule los enlaces del controlador CRUD por recurso que genera Avo (rutas de redireccionamiento, mensajes flash, respuestas personalizadas, comportamiento de guardar/destruir) y extienda de forma segura el ApplicationController privado de Avo. Úselo cuando el usuario quiera redirigir a otro lugar después de crear/actualizar/eliminar en el administrador, cambiar el mensaje flash "creado/actualizado con éxito", eliminar temporalmente o archivar en lugar de destruir, intercambiar `@record.save!` por un objeto de servicio, ejecutar código antes de cada solicitud de administrador, establecer un atributo o inquilino `Current` por solicitud de administrador, anular `fill_record` o corregir `ActionDispatch::MissingController`. Cubre `Avo::CoursesController < Avo::ResourcesController`, el generador `avo:controller`, `after_create_path`/`after_update_path`/`after_destroy_path`, `*_success_message`/`*_fail_message`, `create_/update_/destroy_success_action`/`*_fail_action`, `save_record_action`/`destroy_record_action` y extendiendo `Avo::ApplicationController` con una preocupación en `to_prepare`.
