Remplacez les hooks du contrôleur CRUD par ressource générés par Avo (chemins de redirection, messages flash, réponses personnalisées, comportement de sauvegarde/destruction) et étendez en toute sécurité l'ApplicationController privé d'Avo. À utiliser lorsque l'utilisateur souhaite rediriger ailleurs après la création/mise à jour/suppression dans l'administrateur, modifier le message flash « créé/mis à jour avec succès », effectuer une suppression logicielle ou archiver au lieu de détruire, échanger « @record.save ! » contre un objet de service, exécuter du code avant chaque demande d'administrateur, définir un attribut ou un locataire « actuel » par demande d'administrateur, remplacer « fill_record » ou corriger `ActionDispatch :: MissingController`. Couvre `Avo::CoursesController < Avo::ResourcesController`, le générateur `avo:controller`, `after_create_path`/`after_update_path`/`after_destroy_path`, `*_success_message`/`*_fail_message`, `create_/update_/destroy_success_action`/`*_fail_action`, `save_record_action`/`destroy_record_action`, et étendant `Avo::ApplicationController` avec un problème dans `to_prepare`.
