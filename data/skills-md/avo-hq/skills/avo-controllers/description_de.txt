Überschreiben Sie die von Avo generierten CRUD-Controller-Hooks pro Ressource (Umleitungspfade, Flash-Nachrichten, benutzerdefinierte Antworten, Speicher-/Zerstörungsverhalten) und erweitern Sie Avos privaten ApplicationController sicher. Verwenden Sie diese Option, wenn der Benutzer nach dem Erstellen/Aktualisieren/Löschen im Admin an eine andere Stelle umleiten, die Flash-Nachricht „erfolgreich erstellt/aktualisiert“ ändern, vorläufig löschen oder archivieren statt zerstören möchte, „@record.save!“ gegen ein Dienstobjekt austauschen, Code vor jeder Admin-Anfrage ausführen, ein „Aktuelles“ Attribut oder einen Mandanten pro Admin-Anfrage festlegen, „fill_record“ überschreiben oder reparieren möchte „ActionDispatch::MissingController“. Behandelt `Avo::CoursesController < Avo::ResourcesController`, den `avo:controller`-Generator, `after_create_path`/`after_update_path`/`after_destroy_path`, `*_success_message`/`*_fail_message`, `create_/update_/destroy_success_action`/`*_fail_action`, `save_record_action`/`destroy_record_action` und Erweitern von `Avo::ApplicationController` mit einem Anliegen in `to_prepare`.
