覆盖 Avo 生成的每个资源 CRUD 控制器挂钩（重定向路径、闪存消息、自定义响应、保存/销毁行为）并安全地扩展 Avo 的私有 ApplicationController。当用户在管理中创建/更新/删除后想要重定向到其他地方、更改“成功创建/更新”闪存消息、软删除或存档而不是销毁、将“@record.save!”交换为服务对象、在每个管理请求之前运行代码、为每个管理请求设置“Current”属性或租户、覆盖“fill_record”或修复“ActionDispatch::MissingController”时使用。涵盖 `Avo::CoursesController < Avo::ResourcesController`、`avo:controller` 生成器、`after_create_path`/`after_update_path`/`after_destroy_path`、`*_success_message`/`*_fail_message`、`create_/update_/destroy_success_action`/`*_fail_action`、 `save_record_action`/`destroy_record_action`，并扩展 `Avo::ApplicationController` 并关注 `to_prepare`。
