Generate concise technical diagrams (architecture diagram / flowchart / sequence diagram / state machine / data flow / ER / concept diagram, etc.) from natural language descriptions. **Do not let the LLM draw SVG**—the LLM only outputs JSON describing nodes and edges; a deterministic Python renderer computes Manhattan routing, collision avoidance, label placement, ensuring the layout is 100% clean. Default Ledger style (warm white background + teal accent + dark‑gray stroke). Use `rsvg-convert` to also produce PNG.

TRIGGER when user asks to: draw a diagram, draw one, help me draw, draw a, generate a diagram, produce a diagram, make a diagram, architecture diagram, flowchart, sequence diagram, state‑machine diagram, ER diagram, visualize, draw, diagram, visualize, chart it.
