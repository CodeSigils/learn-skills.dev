从自然语言描述生成简洁的技术图（架构图/流程图/序列图/状态机/数据流/ER/概念图等）。 **不要让LLM绘制SVG**——LLM只输出描述节点和边的JSON；确定性 Python 渲染器计算曼哈顿路由、避免碰撞、标签放置，确保布局 100% 干净。默认 Ledger 样式（暖白色背景 + 青色色调 + 深灰色描边）。使用“rsvg-convert”也可以生成 PNG。

当用户要求：画一张图、画一张、帮我画、画一张、生成一张图、生成一张图、制作一张图、架构图、流程图、序列图、状态机图、ER图、可视化、绘制、图表、可视化、图表化时触发。
