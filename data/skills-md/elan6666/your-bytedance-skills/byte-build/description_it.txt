Esegui i file del piano del sistema operativo ByteDance/Byte tramite ondate pronte per la dipendenza. Da utilizzare quando l'utente chiede di creare, implementare, eseguire piani, eseguire la wave successiva, eseguire `byte-build --wave N`, eseguire `byte-build --plan ID` o eseguire `byte-build --all`.
