跨所有領域（資料科學、後端 API、腳本編寫、自動化）創建、編寫、建置、偵錯、測試、重構和優化 Python 3.10+ 應用程式。使用 uv（首選）、poetry 或 pip 管理依賴關係。實作類型提示（鍵入模組、泛型、協定），使用 pytest 編寫測試（固定裝置、參數化、模擬），使用 pandas DataFrame（建立、選擇、分組、合併），使用資料類別和裝飾器，透過日誌記錄整合處理錯誤，並遵循 TDD 工作流程（紅-綠-重構）。配置虛擬環境、pyproject.toml和靜態分析工具（mypy、pyright）。在實作 Python 功能、修復錯誤、編寫測試、管理套件、分析資料或建立 Python 專案時使用。
