مستندات التصميم على مودا — صفحة واحدة أو خمسين: صفحة واحدة، تقرير، ملخص، ورقة بيضاء، اقتراح، ورقة بيانات، نشرة، إنفوجرافيك؛ صفحات بحجم US‑Letter/A4 على لوحة حية، تُصدَّر كملف PDF حقيقي بنص قابل للتحديد. يُستخدم لـ: PDF (أي عدد صفحات)، النشرة الإخبارية، "اجعل هذا markdown/README يبدو مصممًا". ليس لـ: مواد مطبوعة (ملصق، منشور، قائمة طعام، سيرة ذاتية، بطاقات) → moda-document-print؛ شرائح → moda-deck؛ محتوى متحرك/حركة → moda-video.

الرجاء دائمًا الرد بلغة المستخدم.
