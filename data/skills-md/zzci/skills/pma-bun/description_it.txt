Guida all'implementazione del bun per backend gestito da PMA e monorepos full-stack. Copre gli spazi di lavoro Bun con `apps/*` e `packages/*`, moduli API in `src/modules`, linting rigoroso con ESLint + @antfu/eslint-config, Drizzle su SQLite-first storage, OpenAPIHono su `Bun.serve()`, configurazione env convalidata, integrazione Vite dev, compilazione binaria autonoma con risorse e migrazioni incorporate e gate di qualità CI.
