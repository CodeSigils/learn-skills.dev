Guida pratica per ridurre il consumo di token e abbassare i costi dell'intelligenza artificiale sia per gli utenti di Claude Code che per gli sviluppatori che chiamano direttamente l'API Anthropic. Copre l'organizzazione dei file, la gestione del contesto, la selezione strategica del modello, la memorizzazione nella cache dei prompt, l'API batch, l'ottimizzazione delle attività e l'architettura dei prompt. Supportato da dati sperimentali reali. Da utilizzare quando l'utente menziona "ottimizza token", "riduci i costi", "Claude è lento", "troppi token", "budget token", "finestra di contesto piena", "organizza codebase per AI", "riduci consumo token", "richiedi memorizzazione nella cache", "cache_control", "API batch", "cache danneggiata", "costi API" o "budget ponderato". NON utilizzare per domande generali sulla codifica, debug o ottimizzazione delle prestazioni non correlate all'utilizzo del token AI.
