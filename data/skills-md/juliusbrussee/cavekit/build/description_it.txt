Pianificare ed eseguire l'implementazione rispetto a SPEC.md. Thread singolo nativo
loop, nessun sub-agente. In caso di test o errore di compilazione, richiama automaticamente il backprop
abilità prima di riprovare: una verifica fallita considera sempre se
una nuova invariante §V impedirebbe il ripetersi. Si attiva quando l'utente lo chiede
per costruire, implementare, eseguire le specifiche o affrontare un compito §T specifico
(`build §T.3`, `build --next`, `implementa l'attività successiva`, `esegui la build`).
Si aspetta che SPEC.md esista; in caso contrario, rimanda all'abilità specifica.
