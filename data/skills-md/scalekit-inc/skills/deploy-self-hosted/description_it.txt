Distribuisce Scalekit self-hosted in modo che un agente possa installare Helm o on-prem. Da utilizzare quando l'utente desidera Scalekit self-hosted o Helm. Non scrive il login di SaaSKit (questo è `implement-saaskit`).
