エージェントが API を保護できるように、API キーまたはクライアント認証情報による認証を追加します。ユーザーが API キーまたはクライアント認証情報を必要とする場合に使用してください。OAuth 2.1 を MCP サーバーに導入するものではありません（それは `add-mcp-oauth`）し、SaaSKit のログインを書き込むものでもありません（それは `implement-saaskit`）。
