在 Next.js App Router 中實作 SaaSKit 登入，使代理能加入登入、回呼、會話 Cookie 與登出。當使用者需要 SaaSKit 的 Next.js 或 App Router 認證時使用。它不會撰寫通用的 Node 登入（那是 `implement-saaskit`）或撰寫 Django、FastAPI、或 Flask 認證（那是 `implement-saaskit-python`）。
