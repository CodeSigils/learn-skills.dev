エージェントがすべての項目が PASS もしくは理由付きで WAIVE の場合にのみサインオフできるように、AgentKit の本番稼働をチェックします。ユーザーが AgentKit の本番環境を希望する場合や、AgentKit を本番稼働させる場合に使用します。接続されたアカウントやトークンは書き込みません（それは `integrate-agentkit` が行います）し、SaaSKit の本番稼働チェックも行いません（それは `check-saaskit-prod` が行います）。
