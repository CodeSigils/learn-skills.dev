Verifica el lanzamiento en vivo de AgentKit para que un agente pueda firmar solo cuando cada elemento sea PASS o WAIVE con una razón. Úsalo cuando el usuario quiera producción de AgentKit o poner en vivo AgentKit. No escribe una cuenta conectada ni un token (eso es `integrate-agentkit`) ni verifica el lanzamiento en vivo de SaaSKit (eso es `check-saaskit-prod`).
