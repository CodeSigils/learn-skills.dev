التحقق من تشغيل AgentKit حتى يتمكن الوكيل من تسجيل الخروج فقط عندما يتم تمرير كل عنصر أو التنازل عنه لسبب ما. يُستخدم عندما يريد المستخدم إنتاج AgentKit، أو تشغيل AgentKit. ولا يكتب حسابًا أو رمزًا متصلاً (هذا هو "integrate-agentkit") أو التحقق من بدء تشغيل SaaSKit (وهذا هو "check-saaskit-prod").
