Überprüft die SaaSKit‑Produktionsumgebung, sodass ein Agent die Go‑Live‑Checkliste abschließen kann. Verwenden Sie dies, wenn der Benutzer SaaSKit‑Produktion wünscht oder SaaSKit live schalten möchte. Es schreibt keinen Login (das ist `implement-saaskit`) und führt keinen Trockenlauf aus (das ist `run-dryrun`).
