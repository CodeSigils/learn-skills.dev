Controlla la produzione di SaaSKit affinché un agente possa completare la checklist di go-live. Da usare quando l'utente desidera la produzione di SaaSKit o avviare SaaSKit. Non scrive il login (questo è `implement-saaskit`) né esegue il dry‑run (questo è `run-dryrun`).
