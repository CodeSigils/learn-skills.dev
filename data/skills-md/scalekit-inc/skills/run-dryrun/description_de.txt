Führt einen Trockenlauf aus, damit ein Agent das SaaSKit‑Auth‑Setup vor dem Go‑Live testen kann. Verwenden, wenn der Benutzer die Authentifizierung testen oder einen Trockenlauf durchführen möchte. Es schreibt keinen Login (das ist `implement-saaskit`) und schließt keine Go‑Live‑Checkliste ab (das ist `check-saaskit-prod`).
