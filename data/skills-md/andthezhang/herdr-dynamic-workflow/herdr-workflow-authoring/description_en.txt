Write and run a dynamic workflow on Herdr — a JS script whose agent() calls run real coding-agent CLIs (claude, codex, cursor, …) as subagents in Herdr panes, locally or on an ssh host. Use when asked to run a herdr workflow, fan a task out across subagents, pick which CLI or which computer an agent call runs on, or resume an earlier run.
