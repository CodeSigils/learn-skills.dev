Use when implementing any non-trivial feature, behaviour, or bug fix — write the failing test first, in vertical tracer-bullet slices, one test → one implementation at a time. Tests verify behaviour through public interfaces, never implementation details. Trigger on "build X", "implement X", "fix this bug", "add a feature", "red-green-refactor", "write tests", and any behaviour change that isn't config/docs/rename.
