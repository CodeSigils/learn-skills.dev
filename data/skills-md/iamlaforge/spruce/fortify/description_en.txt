Spruce design reasoning — Apply production readiness to an existing interface. Builds missing states (empty, loading, error, success, disabled), addresses accessibility comprehensively (focus, keyboard navigation, ARIA, contrast), and surfaces responsive behavior issues for separate work. Builds states with reasonable context-calibrated copy; user can run /voice to refine. Handles structural and interactive work autonomously; surfaces architectural decisions for approval. Accepts optional scope arguments.
