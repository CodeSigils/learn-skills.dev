Spruce design reasoning — Apply component discipline to an existing interface. Addresses component coherence, state completeness, shape and elevation treatments, system-level decisions, and minor component consolidation. Handles small state gaps and coherence fixes autonomously; surfaces larger character shifts (shape system changes, major state additions) for user approval. Flags component architecture restructuring for separate work. Accepts optional scope arguments to focus on specific component concerns.
