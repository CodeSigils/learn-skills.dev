Use when planning or executing tag creation, releases, pushing RC tags, generating the next patch/minor/major version number, or when you need to write back the `tag` field in `package.json` and push it to the remote; it must be used whenever the writing‑plans stage involves release tasks.
