Optional local comprehension interview after AI review. Dev checks out the work branch, tests and previews the PR, then runs this skill in a local Claude Code session — no routine, no GitHub label changes. Skip by merging the PR and closing the issue. Use when the user runs /workflow-comprehension or wants to verify understanding of an issue's changes before merge.
