Da utilizzare quando si pianifica l'esecuzione di Gradle tramite `gradle`, `./gradlew` o uno script wrapper `gradlew*` personalizzato oppure si diagnostica una build, un controllo, un test, un lint, un avviso o un errore di Gradle.
