Multi-perspective adversarial review. In full/lean mode, it spawns in parallel when reviewer agents are deployed; if agents are missing, abnormal, or spawning fails, it automatically degrades to solo, and uses a built-in rubric fallback when the reference file is unreadable. Trigger commands: /story-review, /review, “review it”, “help me review”.
