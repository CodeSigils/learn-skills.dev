Delega attività di codifica, debug e implementazione alla CLI del Codex. L'agente funge da responsabile tecnico: ricerca i requisiti, crea suggerimenti, supervisiona l'esecuzione e rivede l'output. Da utilizzare quando viene richiesto di "utilizzare il codex", "delegare al codex" o quando l'implementazione deve essere gestita dalla CLI del Codex anziché dall'agente stesso.
