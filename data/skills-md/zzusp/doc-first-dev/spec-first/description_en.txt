Manage the full development cycle of project modules: requirements analysis → technical solution update → spec confirmation → development → acceptance → closure. Trigger condition: a spec document exists under the project, and the user's request involves code changes (feature addition, bug fix, requirement change, interface adjustment, refactoring, field addition/removal), without requiring the user to explicitly specify the process.
