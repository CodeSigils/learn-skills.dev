Konfigurieren Sie automatisch heimische Spiegelquellen und wechseln Sie zu diesen, um die Installation des Paketmanagers zu beschleunigen. Verwenden Sie diese Fähigkeit, wenn ein Benutzer Pakete/Abhängigkeiten installiert (z. B. npm install, pip install, Yarn install, brew install, apt install, gem install, cargo install, go get usw.), sich über langsame Downloads, Installations-Timeouts, Netzwerkprobleme beschwert oder explizit nach inländischen Spiegelquellen fragt.
