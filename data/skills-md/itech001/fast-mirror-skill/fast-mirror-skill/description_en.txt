Automatically configure and switch to domestic mirror sources to accelerate package manager installations. Use this skill when a user is installing packages/dependencies (e.g., npm install, pip install, yarn install, brew install, apt install, gem install, cargo install, go get, etc.), complains about slow downloads, installation timeouts, network issues, or explicitly asks for domestic mirror sources.
