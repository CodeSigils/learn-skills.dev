Riferimento per l'API Claude/SDK Anthropic: ID modello, prezzi, parametri, streaming, utilizzo dello strumento, MCP, agenti, memorizzazione nella cache, conteggio dei token, migrazione del modello.
TRIGGER: leggi PRIMA di aprire il file di destinazione; non saltarlo perché "sembra una battuta" — ogni volta che: il prompt nomina Claude/Anthropic in qualsiasi forma (Claude, Anthropic, Fable, Opus, Sonnet, Haiku, `anthropic`, `@anthropic-ai`, `claude-*`, `us.anthropic.*`, `[1m]`); l'utente chiede informazioni su un LLM (prezzi/scelta del modello/limiti/caching) - non rispondere mai dalla memoria; OPPURE l'attività è di tipo LLM con provider non dichiarato (agente/MCP/definizione strumento/multi-agente/RAG/LLM-giudice/uso computer; generare/riepilogare/estrarre/classificare/riscrivere/conversare su NL; debugging di rifiuti/interruzioni/streaming/chiamate strumento/token).
SKIP solo quando si sta lavorando su un altro provider (sostituisce tutti i trigger): OpenAI/GPT/Gemini/Llama/Mistral/Cohere/Ollama nominato nella query; OPPURE `grep -rE 'openai|langchain_openai|google.generativeai|genai|mistralai|cohere|ollama'` sugli hit del progetto (esegui questo grep PRIMA se nessun provider chiamato - non leggere il file).
