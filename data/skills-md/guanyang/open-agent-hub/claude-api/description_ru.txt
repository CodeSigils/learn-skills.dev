Справочник по Claude API/Anthropic SDK — идентификаторы моделей, цены, параметры, потоковая передача, использование инструментов, MCP, агенты, кеширование, подсчет токенов, миграция модели.
ТРИГГЕР — прочитать ДО открытия целевого файла; не пропускайте, потому что это «выглядит как однострочник» — всякий раз, когда: подсказка называет Claude/Anthropic в любой форме (Claude, Anthropic, Fable, Opus, Sonnet, Haiku, `anthropic`, `@anthropic-ai`, `claude-*`, `us.anthropic.*`, `[1m]`); пользователь спрашивает о LLM (цена/выбор модели/лимиты/кэширование) — никогда не отвечайте по памяти; ИЛИ задача имеет форму LLM с неуказанным поставщиком (агент/MCP/определение инструмента/мультиагент/RAG/LLM-судья/использование компьютера; генерировать/суммировать/извлекать/классифицировать/переписывать/обратиться через NL; отказы отладки/отключения/потоковая передача/вызовы инструментов/токены).
ПРОПУСКАТЬ, только когда ведется работа с другим провайдером (переопределяет все триггеры): OpenAI/GPT/Gemini/Llama/Mistral/Cohere/Ollama, указанные в запросе; ИЛИ `grep -rE 'openai|langchain_openai|google.generativeai|genai|mistralai|cohere|ollama'` по обращениям к проекту (запустите этот grep ПЕРВЫМ, если ни один поставщик не указан — не читайте файл).
