Distribute tasks minimally and sufficiently between Grok, Claude, and Codex based on task value, with the current session acting as the main controller to complete acceptance. It is used when the user explicitly requests “let Grok go first,” “multimodel collaboration,” “two parties provide independent solutions,” “model competition,” “seek a second opinion,” “use Grok to check X/Reddit/real‑time web pages,” or when the task itself warrants cross‑model execution, review, competition, or real‑time research. Supports five entry modes: auto, fast, review, race, scout; scout uses an isolated Grok 4.5 search process to discover and retain evidence from X, Reddit, and web pages, while the other modes select Grok, Claude, or Codex according to the task. By default it is not used for simple Q&A with no apparent benefit, and it does not allow multiple models to write to the same workspace simultaneously, silently switch models, or use a model’s self‑report in place of actual verification. Trigger: /ray-multimodel.
