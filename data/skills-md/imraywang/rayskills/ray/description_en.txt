The main entry point and router of the rayskills toolbox. When a user is unsure which ray‑* skill to use, simply provides a real task or situation, or explicitly wants to take a piece of content from idea, research, and writing all the way to a public‑account draft box, X Articles draft box, or a spoken‑video script, this skill reads the context, determines the most appropriate next step, or executes an already confirmed content‑production chain. It is also used to decide the next step after a round of work is completed. Trigger: /ray or /ray followed by any real‑task description. Users need not remember specific skill names; “push to platform,” “enter the next stage,” or “run the full process” do not constitute public‑publish authorization. Before the platform writes, a unified authorization rule requires creating the local artifact first, then confirming it.
