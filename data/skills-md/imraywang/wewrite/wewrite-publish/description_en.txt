WeWrite typesetting and publishing module: convert Markdown into a WeChat preview, or, with explicit user authorization, push it into the public account’s draft box; it also supports themed galleries and image posts. It only handles WeChat public accounts.
