Générez un aperçu HTML/CSS à partir de l'arborescence des couches JSON et des images découpées exportées depuis PSD. Par défaut, la structure imbriquée groupée du PSD est conservée ; utilisez le paramètre --flatten pour passer à une disposition en mosaïque.
