Generate an HTML/CSS preview from the JSON layer tree and sliced images exported from PSD. By default, the PSD's grouped nested structure is preserved; use the --flatten parameter to switch to a tiled layout.
