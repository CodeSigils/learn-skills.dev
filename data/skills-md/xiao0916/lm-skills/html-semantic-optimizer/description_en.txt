HTML semantic optimization tool that automatically converts generic HTML tags (div, span) into semantic HTML5 tags (header, nav, main, article, button, etc.). Intelligently identifies element semantic roles based on CSS class names and supports custom rule configuration.
