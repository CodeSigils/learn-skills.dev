Strumento di ottimizzazione semantica HTML che converte automaticamente tag HTML generici (div, span) in tag HTML5 semantici (intestazione, nav, main, articolo, pulsante, ecc.). Identifica in modo intelligente i ruoli semantici degli elementi in base ai nomi delle classi CSS e supporta la configurazione di regole personalizzate.
