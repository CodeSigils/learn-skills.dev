When encountering toolchain issues during development, testing, documentation generation, skill creation or modification, file editing, browser operations, screenshots, building, validation, script execution, or PowerShell/Python/Node command execution, this skill must be used. It applies to scenarios such as sandbox helper exceptions, permission or path problems, apply_patch failures, partial initialization script failures, missing dependencies, validation tools that cannot run, Windows PowerShell Chinese garbled text, encoding misdetections, incompatible command parameters, network or installation failures, alternative verification, and situations where users request “record the recent problem” or “document the solution”. If this skill does not cover the current issue, after resolving it you must write the reusable experience back into this skill or its eval/usage instructions.
