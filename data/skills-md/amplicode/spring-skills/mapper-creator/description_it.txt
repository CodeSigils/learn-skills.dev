Crea un mappatore tra un'entità e un DTO (MapStruct o convertitore personalizzato). Utilizzare questa competenza quando è necessario creare un mappatore/convertitore tra entità e DTO, in modo autonomo o come parte di un'attività più ampia (ad esempio dopo la creazione di DTO, durante la configurazione CRUD).
