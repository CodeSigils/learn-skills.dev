Erstellt einen Mapper zwischen einer Entität und einem DTO (MapStruct oder benutzerdefinierter Konverter). Verwenden Sie diese Fähigkeit, wenn ein Mapper/Konverter zwischen Entität und DTO erstellt werden muss, entweder eigenständig oder als Teil einer größeren Aufgabe (z. B. nach der DTO-Erstellung, während der CRUD-Einrichtung).
