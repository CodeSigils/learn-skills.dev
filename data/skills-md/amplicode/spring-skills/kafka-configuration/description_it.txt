Configura lo starter Kafka di Spring Boot tramite `application.properties`/`application.yml`. Utilizza questa competenza quando è necessario creare la configurazione di Kafka, in modo autonomo o come parte di un'attività più ampia (ad esempio prima di aggiungere un produttore `KafkaTemplate` o un consumatore `@KafkaListener`).
