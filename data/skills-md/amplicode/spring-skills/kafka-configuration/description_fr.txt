Configure le démarreur Kafka de Spring Boot via `application.properties` / `application.yml`. Utilisez cette compétence lorsque la configuration Kafka doit être créée, soit de manière autonome, soit dans le cadre d'une tâche plus vaste (par exemple avant d'ajouter un producteur `KafkaTemplate` ou un consommateur `@KafkaListener`).
