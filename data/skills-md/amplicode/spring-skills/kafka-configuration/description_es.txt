Configura el iniciador Kafka de Spring Boot a través de `application.properties`/`application.yml`. Utilice esta habilidad cuando sea necesario crear una configuración de Kafka, ya sea de forma independiente o como parte de una tarea más grande (por ejemplo, antes de agregar un productor `KafkaTemplate` o un consumidor `@KafkaListener`).
