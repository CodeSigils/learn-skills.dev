Flutter project's routing pattern — route declarations based on `go_router`, management of `RoutePaths` constants, implementation of bottom navigation with `StatefulShellRoute`, handling of route parameters (`:recipeId`), and use of `context.go`/`context.push`. Triggers on terms such as "add route", "go_router", "screen navigation", "navigation", "GoRoute", "StatefulShellRoute", "pathParameters", "deep link".
