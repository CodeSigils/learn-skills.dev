Modèles de test pour un projet Flutter — tests unitaires et tests de widgets basés sur `flutter_test`, vérification d'un ViewModel `ChangeNotifier`, Fake Repository, `pumpWidget`+`ListenableBuilder` et vérification de flux. Déclencheurs sur des termes tels que « écriture de test », « test ViewModel », « test de widget », « pumpWidget », « Fake Repository », « Mock » et « test unitaire ».
