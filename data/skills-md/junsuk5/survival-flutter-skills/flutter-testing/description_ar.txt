أنماط الاختبار في مشروع Flutter - اختبارات الوحدة واختبارات عناصر واجهة المستخدم بناءً على `flutter_test`، والتحقق من `ChangeNotifier` ViewModels، والمستودعات المزيفة، و`pumpWidget`+`ListenableBuilder`، والتحقق من التدفق. يتم تشغيل مصطلحات مثل "اختبار الكتابة"، و"اختبار ViewModel"، و"اختبار القطعة"، و"pumpWidget"، و"Fake Repository"، و"Mock"، و"unit test".
