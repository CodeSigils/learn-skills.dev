A skill that organizes the layer structure, directory rules, and dependency direction of a single‑package Flutter project. Use it whenever questions arise such as where to place a new feature, which directory to create a new screen, repository, or use case in, “how should we set up the folder structure,” “where should the feature go,” “whether to put it in core or not.” It triggers on expressions like “project structure,” “directory structure,” “layer separation,” “clean architecture,” “where does X live,” “adding a new feature.”
