PUA Shot — v2 Original Concentrate (449-line full injection), the complete single-file version before splitting, strongest flavor. Zero dependencies, zero references, all context injected at once. Ideal for sub-agent injection, when maximum PUA effect is needed, or when progressive loading is undesired. Triggers on: '/pua:shot', '/pua shot', 'PUA浓缩', 'shot mode', '最强PUA', '全量注入'. Also great for injecting into sub-agents via Read tool since it's self-contained.
