Applicare durante la progettazione o l'implementazione della struttura di runtime di un'app backend VTEX IO sotto node/. Copre il punto di ingresso del servizio, il contesto e lo stato digitati, la configurazione del runtime di service.json e il modo in cui percorsi, eventi e gestori GraphQL vengono registrati ed eseguiti. Da utilizzare per strutturare app backend, definire limiti di runtime o risolvere problemi relativi al modello di esecuzione nei servizi IO VTEX.
