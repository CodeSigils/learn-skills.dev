Used when writing research results to `.artifacts/research/[topic].md`. It structures the conclusion, evidence, and reference sources according to a template and stores them as a knowledge base. It is used by the researcher.
