It is used when writing the technical design specifications to `.artifacts/features/<feature>/specification.md`. It structures the design solidified in design-spec according to a template, and the main agent uses it directly.
