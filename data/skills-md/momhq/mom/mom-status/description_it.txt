Mostra lo stato attuale di MOM. Da utilizzare quando l'utente chiede se MOM funziona, cosa sa MOM, per verificare la configurazione, dopo il ripristino del contesto o quando viene richiesto lo stato di MOM.
