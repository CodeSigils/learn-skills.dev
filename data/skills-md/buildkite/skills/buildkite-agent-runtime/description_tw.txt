當使用者要求「新增註解」、「從步驟上傳工件」、「在步驟之間共用資料」、「動態上傳管道」、「在步驟內要求 OIDC 令牌」、「取得分散式鎖定」、「取得或更新步驟屬性」、「從日誌中編輯機密」、「在執行時間檢索機密」或「偵錯物件中的環境變數」時，應使用此技能。當使用者提及 buildkite-agent 註解、buildkite-agent 工件上傳/下載、buildkite-agent 元資料集/取得、buildkite-agent 管道上傳、buildkite-agent oidc 請求令牌、buildkite-agent 步驟、buildkite-agent oidc 請求令牌、buildkite-agent 步驟、buildkite-agent oidc 請求令牌、buildkite-agent 步驟、buildkite-agent oidc秘密取得、buildkite-agent 編輯器新增、buildkite-agent 工具簽署/驗證或任何 buildkite-agent 時也可使用在正在執行的作業步驟中使用的子命令。
