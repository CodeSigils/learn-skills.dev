Questa abilità deve essere utilizzata quando l'utente chiede di "aggiungere un'annotazione", "caricare elementi da un passaggio", "condividere dati tra passaggi", "caricare una pipeline in modo dinamico", "richiedere un token OIDC all'interno di un passaggio", "acquisire un blocco distribuito", "ottieni o aggiorna un attributo del passaggio", "cancella un segreto dai log", "recupera un segreto del cluster in fase di esecuzione" o "debug delle variabili di ambiente negli hook". Utilizzare anche quando l'utente menziona buildkite-agent annotate, buildkite-agent upload/download di artefatti, buildkite-agent meta-data set/get, buildkite-agent pipeline upload, buildkite-agent oidc request-token, buildkite-agent step, buildkite-agent lock, buildkite-agent env, buildkite-agent secret get, buildkite-agent redactor add, buildkite-agent tool sign/verify o qualsiasi sottocomando buildkite-agent utilizzato all'interno di una fase di lavoro in esecuzione.
