Esta habilidad debe usarse cuando el usuario solicita "agregar una anotación", "cargar artefactos de un paso", "compartir datos entre pasos", "cargar canalización dinámicamente", "solicitar un token OIDC dentro de un paso", "adquirir un bloqueo distribuido", "obtener o actualizar un atributo de paso", "redactar un secreto de los registros", "recuperar un secreto del clúster en tiempo de ejecución" o "depurar variables de entorno en enlaces". También se utiliza cuando el usuario menciona la anotación del agente buildkite, la carga/descarga de artefactos del agente buildkite, el conjunto/obtención de metadatos del agente buildkite, la carga de canalización del agente buildkite, el token de solicitud oidc del agente buildkite, el paso del agente buildkite, el bloqueo del agente buildkite, el entorno del agente buildkite, la obtención secreta del agente buildkite, el agregado del redactor del agente buildkite, la herramienta del agente buildkite. firmar/verificar, o cualquier subcomando buildkite-agent utilizado dentro de un paso de trabajo en ejecución.
