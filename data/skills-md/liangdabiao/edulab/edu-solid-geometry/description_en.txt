Convert a solid geometry problem into a self‑contained interactive teaching web page: step‑by‑step analysis on the left with MathJax, and an interactive 3D model on the right using Three.js (stepwise highlighting + camera switching). Supports three entry modes—given text problem, random problem generation, and uploading a problem image for recognition and solving. Covers problem types on cubes/rectangular prisms, pyramids/prisms, cylinders/cones such as line‑plane angles, dihedral angles, angles between skew lines, point‑to‑plane distances, volumes, etc., using a unified “coordinate construction + vector method” driven by exact calculations from SymPy (answers, 3D coordinates, and step values are consistently derived from the same source). Other agents can also invoke this capability to generate such pages. Trigger words: solid geometry, line‑plane angle, dihedral angle, skew lines, distance to plane, regular quadrilateral pyramid, cube angle calculation, solve this geometry problem, generate a random solid geometry problem, the solid geometry problem in this picture; solid geometry, line‑plane angle, dihedral angle, angle between skew lines, distance to plane, interactive geometry solution page.
