SvelteKit configuration, build, deployment, and performance skills. Used when a user configures an adapter (node, static, Cloudflare, Netlify, Vercel), employs advanced routing/layouts, optimizes performance (code splitting, assets, hydration), handles images (@sveltejs/enhanced-img), implements accessibility/SEO, debugs SvelteKit applications, and migrates from SvelteKit v1 or Sapper.
