Svelte 5 テンプレート構文スキル。ユーザーが、{#if}、{#each}、{#await}、{#snippet} などのブロックレベルの構文、{@render} や @{html} などのテンプレート タグ、bind:、use:、transition:、in:、out:、animate:、style:、class:、{@attach} などのディレクティブを使用する必要がある場合、またはプロパティ、イベント、テキスト式などの基本的なテンプレート構文を理解する必要がある場合に使用します。
