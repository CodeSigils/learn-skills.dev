Docker 映像注册表指南 — 将映像发布到 Docker Hub、拉取映像、标签管理策略、私有注册表设置（Docker 注册表容器和 Harbor）、用于加速的注册表镜像、映像签名和内容信任以及注册表 API 使用。当用户询问 docker hub、镜像注册表、docker 推/拉、私有注册表、docker 登录、标签管理、Harbor 或需要管理注册表中的容器镜像时使用。 使用场景：docker hub、镜像仓库、docker push、docker pull、镜像仓库、registry、Harbor、镜像标签管理。
