Guida esperta per la creazione di Dockerfile: il riferimento completo per la scrittura di Dockerfile di livello produttivo. Copre ogni istruzione (FROM/RUN/COPY/ADD/ENV/ARG/WORKDIR/EXPOSE/CMD/ENTRYPOINT/USER/HEALTHCHECK/SHELL) con sintassi, best practice ed errori comuni. Include modelli di build a più fasi (build+scratch, build+alpine, builder pattern), strategie di ottimizzazione della memorizzazione nella cache dei livelli, regole .dockerignore e modelli di produzione specifici del linguaggio (Go/Rust/Java Spring Boot/Python FastAPI/Node.js Express/TypeScript). Da utilizzare quando l'utente chiede informazioni su Dockerfile, come scrivere un Dockerfile, build in più fasi, ottimizzazione dei livelli o ha bisogno di esempi di Dockerfile per un linguaggio specifico. 使用场景：Dockerfile 编写、怎么写 Dockerfile、Dockerfile 指令、多阶段构建、multi-stage、层优化、layer cache、Dockerfile 模板.
