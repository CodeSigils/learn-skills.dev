Dockerfile 創作專家指南 — 撰寫生產級 Dockerfile 的完整參考。涵蓋每個指令（FROM/RUN/COPY/ADD/ENV/ARG/WORKDIR/EXPOSE/CMD/ENTRYPOINT/USER/HEALTHCHECK/SHELL）以及文法、最佳實務和常見錯誤。包括多階段建置模式（建置+scratch、建置+alpine、建置器模式）、圖層快取最佳化策略、.dockerignore 規則和特定於語言的生產範本（Go/Rust/Java Spring Boot/Python FastAPI/Node.js Express/TypeScript）。當使用者詢問 Dockerfile、如何編寫 Dockerfile、多階段建置、層優化或需要特定語言的 Dockerfile 範例時使用。 使用場景：Dockerfile編寫、怎麼寫Dockerfile、Dockerfile指令、多階段建置、多階段、分層優化、層快取、Dockerfile模板。
