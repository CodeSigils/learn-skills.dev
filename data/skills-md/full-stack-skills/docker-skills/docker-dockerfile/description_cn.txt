Dockerfile 创作专家指南 — 编写生产级 Dockerfile 的完整参考。涵盖每条指令（FROM/RUN/COPY/ADD/ENV/ARG/WORKDIR/EXPOSE/CMD/ENTRYPOINT/USER/HEALTHCHECK/SHELL）以及语法、最佳实践和常见错误。包括多阶段构建模式（构建+scratch、构建+alpine、构建器模式）、层缓存优化策略、.dockerignore 规则和特定于语言的生产模板（Go/Rust/Java Spring Boot/Python FastAPI/Node.js Express/TypeScript）。当用户询问 Dockerfile、如何编写 Dockerfile、多阶段构建、层优化或需要特定语言的 Dockerfile 示例时使用。 使用场景：Dockerfile编写、怎么写Dockerfile、Dockerfile指令、多阶段构建、多阶段、分层优化、层缓存、Dockerfile模板。
