Docker Compose 專家指南 — 為多容器應用程式編寫生產級 compose.yml 檔案。涵蓋完整的 Compose 檔案語法（服務、網路、磁碟區、機密、設定、設定檔、擴充功能、包含）、服務設定（映像/建置、連接埠、環境、env_file、depends_on 與運行狀況檢查、部署資源、重新啟動策略）、Compose 中的網路（預設網路、自訂網路、內部網路）、磁碟區管理、環境變數時使用程式碼和環境變數的工具Kubernetes。當使用者詢問 docker compose、docker-compose.yml、compose 檔案、多容器、服務編排、compose 配置或需要編寫/修改 compose 檔案時使用。 使用場景：docker compose、docker-compose.yml、compose檔案、多容器、服務編排、compose配置、depends_on、profiles。
