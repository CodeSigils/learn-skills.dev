إرشادات لتشغيل أحمال عمل AI/ML على Docker. يغطي Docker Model Runner (سحب/تشغيل نموذج docker لمهام LLM المحلية)، وDocker Agent (تنسيق متعدد الوكلاء لمهام الترميز)، وDocker Sandboxes لتنفيذ وكيل AI المعزول، وتسريع GPU (nvidia-container-toolkit، --gpus flag)، ونموذج AI الذي يعمل مع Docker، وتشغيل LLMs مفتوحة المصدر (Llama، Gemma، Qwen) محليًا. يُستخدم عندما يسأل المستخدم عن docker ai، أو docker model runner، أو LLM المحلي، أو وكيل docker، أو GPU docker، أو sandbox، أو تقديم النماذج، أو يحتاج إلى نشر نماذج AI مع Docker. الميزات: docker ai، وmodel runner، وLLM، وعامل الإرساء، وGPU docker، وsandbox، وAI، وAI.
