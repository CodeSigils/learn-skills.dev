إرشادات الخبراء لبناء صور Docker — docker build CLI، و buildx للإنشاءات متعددة المنصات، وميزات BuildKit (التثبيتات السرية، وتركيبات ذاكرة التخزين المؤقت، وإعادة توجيه SSH، وذاكرة التخزين المؤقت المضمنة)، وDocker Build Cloud للإنشاءات السحابية المتسارعة، واستراتيجيات التخزين المؤقت للطبقات (--cache-from، --cache-to، وذاكرة التخزين المؤقت GHA، وذاكرة التخزين المؤقت للتسجيل)، وتحسين بناء CI/CD. يُستخدم عندما يسأل المستخدم عن docker build، أو buildx، أو multi-platform، أو multi-arch، أو linux/arm64، أو BuildKit، أو Build Cloud، أو تسريع بناء Docker، أو تحسين البناء. الأنظمة الأساسية: docker build، وbuildx، وأنظمة التشغيل المتعددة، وlinux/arm64، وBuildKit، وDocker Build Cloud، وتحسين البناء.
