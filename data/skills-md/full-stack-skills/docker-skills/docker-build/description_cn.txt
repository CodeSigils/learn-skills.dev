“docker build”命令的专家指南 — 构建容器镜像、标记策略、构建参数、镜像管理和检查。涵盖 `docker build` 完整语法、`--build-arg` 用法、镜像标记 (`docker tag`)、镜像检查 (`dockerspect`、`dockerhistory`、`dockerimagels`)、推送到注册表 (`dockerpush`)、镜像修剪 (`dockerimageprune`、`dockersystemprune`) 和 Docker BuildKit 基础知识。与 docker-dockerfile（专注于 Dockerfile 创作）和 docker-buildx（专注于多平台构建和高级 BuildKit）不同。当用户询问 docker build、docker tag、docker push、docker images、docker image management 或如何构建和管理 Docker 镜像时使用。 使用场景：docker build 构建镜像、docker tag 打标签、docker push 自动镜像、docker images 镜像管理、docker image 命令、构建参数 --build-arg、镜像标签、docker build 命令。
