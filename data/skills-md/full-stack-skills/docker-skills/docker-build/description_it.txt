Guida esperta per il comando "docker build": creazione di immagini di contenitori, strategie di tagging, argomenti di creazione, gestione e ispezione delle immagini. Copre la sintassi completa di `docker build`, l'utilizzo di `--build-arg`, il tagging delle immagini (`docker tag`), l'ispezione delle immagini (`docker inspect`, `docker History`, `docker image ls`), il push ai registri (`docker push`), l'eliminazione delle immagini (`docker image prune`, `docker system prune`) e le nozioni di base di Docker BuildKit. Distinto da docker-dockerfile (si concentra sulla creazione di Dockerfile) e docker-buildx (si concentra su build multipiattaforma e BuildKit avanzato). Da utilizzare quando l'utente chiede informazioni su build docker, tag docker, push docker, immagini docker, gestione delle immagini docker o come creare e gestire immagini Docker. 使用场景：docker build 构建镜像、docker tag 打标签、docker push 推送镜像、docker images 镜像管理、docker image 命令、构建参数--build-arg, docker build disponibile.
