إرشادات لتخزين Docker واستمرارية البيانات. يغطي أنواع التخزين (وحدة التخزين/ربط الربط/Tmpfs) مع المقارنة واختيار حالة الاستخدام، وإدارة دورة حياة وحدة التخزين (إنشاء/ls/فحص/تقليم)، وأنماط تحميل الربط، وtmpfs للبيانات المؤقتة، واستراتيجيات النسخ الاحتياطي والاستعادة، واختيار برنامج تشغيل التخزين (تراكب 2)، وأفضل ممارسات استمرارية قاعدة البيانات (PostgreSQL/MySQL/Redis)، وتكامل NFS/التخزين السحابي. يُستخدم عندما يسأل المستخدم عن حجم وحدة الإرساء، أو استمرارية البيانات، أو تركيب الربط، أو tmpfs، أو برنامج تشغيل التخزين، أو النسخ الاحتياطي، أو حجم البيانات، أو يحتاج إلى تخزين مستمر للحاويات. الحجم: وحدة تخزين عامل الإرساء، وحدة تخزين الربط، وحدة الربط، tmpfs، وحدة التخزين، وحدة التخزين، وحدة التخزين، وحدة التخزين.
