إرشادات لتصحيح الأخطاء واستكشاف مشكلات Docker وإصلاحها. يغطي حالات فشل بدء تشغيل الحاويات (رموز الخروج، وسجلات عامل الإرساء، والفحص)، واستنفاد مساحة القرص (نظام df/Prune، وتدوير السجل)، وضغط OOM والموارد (حدود الذاكرة، والإحصائيات)، ومشاكل الاتصال بالشبكة (فحص الشبكة، وiptables، وحل DNS)، ومشكلات التخزين (رفض الإذن، وفحص الحجم)، ومشاكل البرنامج الخفي (بدء تشغيل dockerd، وأذونات المقبس)، وتشخيص الأخطاء الشائعة. يُستخدم عندما يسأل المستخدم عن استكشاف أخطاء عامل الإرساء وإصلاحها، أو تصحيح أخطاء عامل الإرساء، أو OOM، أو امتلاء القرص، أو مشكلات الشبكة، أو عدم بدء تشغيل عامل الإرساء، أو تعطل الحاوية، أو الحاجة إلى التحقيق في مشكلات عامل الإرساء. اسم المستخدم: عامل الإرساء وتصحيح أخطاء عامل الإرساء وتصحيح الأخطاء وOOM و磁盘满 و网络不通 و启动失败 و配置问题.
