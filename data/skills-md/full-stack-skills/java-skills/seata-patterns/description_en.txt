Seata distributed transaction capabilities. Covers the decision tree for selecting among the four modes AT, TCC, SAGA, and XA; the AT mode requires an undo_log table; the TCC mode uses useTCCFence to resolve idempotency, hanging, and empty rollbacks; the SAGA mode handles long‑running state‑machine processes; global transaction timeout configuration (@GlobalTransactional timeoutMills); read‑isolation enhancements (@GlobalLock + FOR UPDATE).  

Correction for LLM: does not know which of AT/TCC/SAGA to choose, does not create the undo_log table, TCC Confirm is not idempotent, and does not handle the watchdog.
