Seata分布式事务能力。涵盖AT、TCC、SAGA、XA四种模式选择的决策树； AT模式需要undo_log表； TCC模式使用useTCCFence解决幂等性、挂起、空回滚； SAGA 模式处理长时间运行的状态机进程；全局事务超时配置（@GlobalTransactional timeoutMills）；读取隔离增强（@GlobalLock + FOR UPDATE）。  

LLM的更正：不知道选择AT/TCC/SAGA中的哪一个，不创建undo_log表，TCCConfirm不是幂等的，并且不处理看门狗。
