Competenze ORM principali di MyBatis. Copre la selezione XML e SQL di annotazione, il riutilizzo di ResultMap (esteso), le query di associazione (associazione/join di raccolta vs selezione nidificata/decisione N+1), configurazione del plug-in di impaginazione, protezione # e $ SQL injection, best practice SQL dinamico (if/where/set/foreach/choose) e utilizzo di columnPrefix per risolvere più join sulla stessa tabella. Utilizzato quando gli utenti scrivono MyBatis Mapper XML, gestiscono query di associazione, configurano l'impaginazione e impediscono l'iniezione SQL.
