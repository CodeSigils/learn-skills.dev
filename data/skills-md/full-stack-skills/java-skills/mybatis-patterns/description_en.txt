MyBatis core ORM skills. Covers XML vs annotation SQL selection, ResultMap reuse (extends), association queries (association/collection join vs nested select/N+1 decision), pagination plugin configuration, # and $ SQL injection protection, dynamic SQL (if/where/set/foreach/choose) best practices, and using columnPrefix to solve multiple joins on the same table. Used when users write MyBatis Mapper XML, handle association queries, configure pagination, and prevent SQL injection.
