MyBatis core ORM skills. Covers XML vs annotation SQL selection, ResultMap reuse (extends), association queries (association/collection join vs nested select/N+1 decision), pagination plugin configuration, SQL injection protection for # and $, best practices for dynamic SQL (if/where/set/foreach/choose), and using columnPrefix to resolve multiple joins on the same table. Used when users write MyBatis Mapper XML, handle association queries, configure pagination, and prevent SQL injection.
