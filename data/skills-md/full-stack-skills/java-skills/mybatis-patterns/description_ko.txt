MyBatis 핵심 ORM 기술. XML 대 주석 SQL 선택, ResultMap 재사용(확장), 연결 쿼리(연결/컬렉션 조인 대 중첩된 선택/N+1 결정), 페이지 매김 플러그인 구성, # 및 $ SQL 주입 보호, 동적 SQL(if/where/set/foreach/choose) 모범 사례, 동일한 테이블에서 여러 조인을 해결하기 위한 columnPrefix 사용 등을 다룹니다. 사용자가 MyBatis Mapper XML을 작성하고, 연관 쿼리를 처리하고, 페이지 매김을 구성하고, SQL 삽입을 방지할 때 사용됩니다.
