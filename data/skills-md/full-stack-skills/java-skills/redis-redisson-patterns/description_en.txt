Redis + Redisson cache and distributed-lock expertise. Covers cache key naming conventions, expiration‑time setting principles, RedisTemplate serialization choices (JSON vs JDK vs String), the three main Redisson distributed‑lock modes (watchdog / tryLock with leaseTime / tryLock without leaseTime decisions), protection against cache penetration (Bloom filter / null‑value caching), cache breakdown (mutex lock), and cache avalanche (random TTL), and the decision between Caffeine and Redis.  

Corrections for LLM: do not omit expiration times; avoid mixing StringRedisTemplate and RedisTemplate; always unlock Redisson locks in a try‑finally block; the watchdog does not work when a leaseTime is explicitly set.
