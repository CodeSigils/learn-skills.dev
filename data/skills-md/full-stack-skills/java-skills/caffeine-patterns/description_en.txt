Caffeine JVM local caching skills. Decision tree for choosing between local cache vs remote cache (Redis), comparison of the three expiration strategies expireAfterWrite, expireAfterAccess, and refreshAfterWrite and their combination rules (refresh < expire), CacheLoader loading and asynchronous reload mode, Spring Cache @Cacheable integration, maximumSize eviction strategy, recordStats cache statistics.  

Correction for LLM: use ConcurrentHashMap + manual expiration, route all caches through Redis, refreshAfterWrite > expireAfterWrite causes never refreshing, CacheLoader returning null leads to cache penetration.
