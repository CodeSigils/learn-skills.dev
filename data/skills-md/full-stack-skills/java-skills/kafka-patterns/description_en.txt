Apache Kafka message queue skills. Covers producer acks=all/1/0 selection decisions, idempotence (enable.idempotence + acks=all) configuration, consumer manual offset commit (enable.auto.commit=false), DLT dead‑letter handling (@RetryableTopic/@DltHandler), @KafkaListener + exception handler (DefaultErrorHandler), ordered messages (same key to the same partition), transactional messages.  
Correction for LLM: acks=0 may lose messages, auto.commit may miss messages, does not handle duplicate consumption, and does not configure DLT.
