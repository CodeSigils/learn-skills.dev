Google Guava best‑practice patterns. Refined from the official wiki, covering the three Immutable‑collection modes (of, copyOf, builder) and defensive‑copy rules, Cache selection decisions (LoadingCache vs. Cache vs. Caffeine), immutable pipelines with Joiner/Splitter, CharMatcher as a regex alternative, Preconditions formatted‑message validation, and CaseFormat naming conversion. Correct LLM misuse: hand‑written immutable collections, using ConcurrentHashMap instead of Cache, and being unaware of CharMatcher.
