DDD4J Jackson serialization rule skill. Overrides date format (non‑ISO‑8601 yyyy‑MM‑dd HH:mm:ss), null value serialization rules (array → [], string → "", number/boolean → keep null), @Sensitive sensitive data masking (5 strategies), dual instances of JacksonKit and WebObjectMapper (JacksonKit for Redis / ObjectMapper for Web), and ToLongDeserializer (handles precision loss in front‑end JavaScript). Used when a user in a DDD4J project uses Jackson, needs to configure ObjectMapper, handle sensitive data masking, and resolve front‑end/back‑end JSON format issues. Works together with the ddd4j‑core skill.
