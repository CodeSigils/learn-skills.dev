规划在Linux主机上安装LinuxMirrors Docker Engine，替换Docker CE软件源，并配置Docker注册表镜像，涵盖版本选择覆盖、现有安装、仅注册表替换、多个镜像地址、daemon.json合并、服务验证和回滚。用户提到使用 docker.sh、DockerInstallation.sh、Docker pull 加速或 `--only-registry`；常规系统软件源更改不使用此功能。
