设计、实施、审核和报告 Java 到 Rust 迁移的有价值的测试，而不会将绿色测试推广到虚假的完成声明中。将 JUnit 测试移植到 Rust、检查每个 Java 测试配置、验证对象迁移分类帐、证明精确的依赖项重用、决定 Rust 特定的义务、比较覆盖范围（无需游戏）或构建差异、黄金、属性、模糊、突变、并发、取消、生命周期、适配器一致性、真实依赖项、主机、负载、安全性和回滚证据时使用。即使 Cargo 测试通过，仍使 MISSING、MISPLACED、STUB、PARTIAL 和 UNVERIFIED 对象保持不完整。
