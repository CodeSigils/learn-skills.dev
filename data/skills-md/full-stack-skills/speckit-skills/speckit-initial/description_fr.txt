Exécutez `specify init` dans le répertoire actuel ou cible pour amorcer un projet Spec Kit (commandes pull .specify/ et slash) ; prend en charge plusieurs agents IA et --script sh/ps. À utiliser lorsque l'utilisateur dit « initialiser le projet Spec Kit », « spécifier init » ou « configurer Spec Kit dans ce dépôt ».
