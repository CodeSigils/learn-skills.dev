Esta habilidad debe usarse cuando el usuario solicita “fork”, “race claude and codex”, “dual implement”, “run both models”, “compare implementations”, “implement with both”, “fork it”, o quiere que la misma tarea sea implementada tanto por Claude Code como por OpenAI Codex en paralelo para elegir el mejor resultado. También se activa con el comando /fork.

Por favor, siempre responde en el idioma del usuario.
