Questa funzione dovrebbe essere utilizzata quando l'utente chiede di “autofix pr”, “fix pr locally”, “fix ci failures”, “fix review comments”, “iterate on pr”, “fix failing checks”, “fix pr comments”, “make ci green”, “fix the build”, “address reviewer feedback”, oppure desidera correggere iterativamente i fallimenti CI e i commenti di revisione su una PR di GitHub dal CLI locale.

Per favore, rispondi sempre nella lingua dell'utente.
