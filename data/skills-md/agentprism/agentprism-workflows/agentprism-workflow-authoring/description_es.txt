Escriba y ejecute scripts de flujo de trabajo de AgentPrism: `export const meta` + agent()/parallel()/pipeline() JavaScript DSL ejecutado por @automatalabs/workflows y por la herramienta @automatalabs/mcp-server `workflow`. Úselo al escribir o editar un script de flujo de trabajo, o cuando ejecute uno a través de la herramienta "flujo de trabajo" de MCP. Cubre la API de secuencia de comandos, el enrutamiento por llamada a los backends de ACP (Claude Code, Codex, OpenCode, pi, agentes personalizados), salidas estructuradas a través del esquema JSON, puntos de control humanos, aislamiento del árbol de trabajo, reglas de reanudación y operaciones de ejecución (ejecutar, esperar, inspeccionar, detener, registros de ejecución).
