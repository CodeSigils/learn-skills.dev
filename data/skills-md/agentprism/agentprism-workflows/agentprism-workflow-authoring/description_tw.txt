編寫並執行 AgentPrism 工作流程腳本 - 由 @automatalabs/workflows 和 @automatalabs/mcp-server `workflow` 工具執行的`export const meta` + agent()/parallel()/pipeline() JavaScript DSL。在編寫或編輯工作流程腳本時或透過 MCP「工作流程」工具執行腳本時使用。涵蓋腳本 API、每次呼叫路由到 ACP 後端（Claude Code、Codex、OpenCode、pi、自訂代理程式）、透過 JSON 架構的結構化輸出、手動檢查點、工作樹隔離、復原規則和執行操作（執行、等待、檢查、停止、執行日誌）。
