Run a temporary BuildKit daemon in the active Kubernetes sandbox namespace, then use sandbox-local buildctl to send the local build context and push a GHCR image. Use after brain-github-deploy writes .sealos/build-request.json, or when creating, debugging, or inspecting sandbox BuildKit builds based on moby/buildkit buildkitd and buildctl.
