PHP Web archive extraction (Zip Slip/path traversal) auditing tool. It identifies how extracted entry names are concatenated with the target directory, whether a base-dir constraint is missing, and outputs exploitability classification, observable PoC, and remediation suggestions (omissions forbidden).
