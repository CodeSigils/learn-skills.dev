PHP component version vulnerability detection tool. Scans composer.json/composer.lock or JAR/package metadata, matches known vulnerability rules, and generates a report (including a potential trigger point analysis framework).
