When a user requests to generate, modify, or review Vue3 + TypeScript + Arco Design subproject code, this trigger applies. It overrides the usage specifications for kb‑pro‑table / useTable / useForm and the Arco Design + UnoCSS styling rules. It covers module organization and naming conventions, enumeration and type constraints, ref‑first reactive specifications, and so on. Regardless of whether the user explicitly mentions “specifications” or “rules,” any code writing involving the Vue3 + TypeScript + Arco Design subproject should use this skill.
