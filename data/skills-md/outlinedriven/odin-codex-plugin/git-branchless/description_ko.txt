작업을 계획하고 실행하는 동안 관용적인 git-branchless를 적용합니다. 분리된 HEAD 우선 작업, `git move`를 통한 메모리 내 리베이스, `git undo`를 통한 이벤트 로그 복구, 지연된 브랜치 생성, 기본 업데이트를 위한 추측 병합 `git sync`. 다중 커밋 작업, 기록 재작성, 스택 편집, 리베이스/재주문, 스택 중간 수정 삽입, 스택 PR 게시 또는 잘못된 git 작업 복구를 계획하거나 실행할 때 사용합니다. 또는 사용자가 Branchless, smartlog, `git move` 또는 `git undo`를 언급하는 경우. 현재 저장소에 대해 브랜치리스가 초기화되지 않은 경우 자동으로 비활성화됩니다.
