当用户要求“生成图像”，“创建徽标”，“绘制图标”，“编辑这张照片”，“将背景更改为透明”，“删除背景”，“使用GPT图像”，“使用Codex绘制”，“用GPT图像生成图片”，“用Codex画图”，“帮我生成一张图”，“改成透明”，“把这张图编辑一下”，或任何提示图像或参考图像编辑任务受益于返回 JSON 结果和 JSONL 进度事件的结构化 CLI。在一个命令界面下支持 OpenAI `gpt-image-2`（通过 `OPENAI_API_KEY` 或 OpenAI 兼容的基本 URL）和 Codex `image_ Generation`（通过 `~/.codex/auth.json`），具有遮罩、高达 4K 的自定义尺寸、透明背景和原始请求转义舱口。
