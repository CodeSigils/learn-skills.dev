當使用者要求“生成圖像”，“創建徽標”，“繪製圖標”，“編輯這張照片”，“將背景更改為透明”，“刪除背景”，“使用GPT圖像”，“使用Codex繪製”，“用GPT圖像生成圖片”，“用Codex畫圖”，“幫我生成一張圖”，“使用GPT圖像生成圖片”，“用Codex畫圖”，“幫我生成一張圖”，“將這張透明圖”，“把這張圖片編輯一下圖像”，“請”參考圖像或參考進度事件的結構化 CLI。在一個命令介面下支援 OpenAI `gpt-image-2`（透過 `OPENAI_API_KEY` 或 OpenAI 相容的基本 URL）和 Codex `image_ Generation`（透過 `~/.codex/auth.json`），具有義艙、高達 4K 的自訂尺寸、透明背景和原始轉接尺寸、透明背景。
