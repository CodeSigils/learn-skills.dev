Critica il colore di uno schermo renderizzato: rapporti di contrasto, coerenza della tavolozza e significato semantico. Da utilizzare durante la revisione di una schermata. Per un audit WCAG a livello di prodotto utilizzare "audit di accessibilità" (sistemi di progettazione); per costruire la tavolozza usa `color-system` (ui-design).
