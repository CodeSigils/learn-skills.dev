Extract abstracted creative style rules from a reference novel and generate a knowledge base file. Triggers: user requests style extraction, referencing a novel to build a style, guided invocation during cold start, need to supplement fragments. Core capability: convert implicit style into explicit rules, and they must be abstract techniques rather than the original book's settings. Works together with novel-setup (knowledge base maintenance), novel-lite (style usage), and novel-review (style review).
