このスキルは、Stackbone ワークスペース (「stackbone dev」 でローカルに実行されているディープ エージェントや耐久性のあるワークフロー、またはクラウド/セルフホストにデプロイされたインストール) でエラー、障害、または予期しない動作を優先順位付けするときに使用します。 SDK エラー (アンビエント `stackbone` クライアントによって返されたカタログからの { data, error } エンベロープ コード)、コントロール プレーンからの HTTP 4xx/5xx、永続的な実行の失敗とタイムアウト、`workflow_input_invalid` (入力が宣言されたスキーマに失敗したワークフローの開始)、いずれかのレベルでスタックした HITL (ワークフローが `requestApproval()` ゲートに保留されている、またはエージェント ターン) をカバーします。 「interruptOn」でゲートされたツールでの「中断」）、永続セッションの問題（「x-stackbone-session」が送信されなかったか、チェックポイント パッケージが見つからないためにメモリが存続しない）、データベースの遅いクエリ / インデックスの欠落 / pgvector の不一致、シークレット復号化エラー、「stackbone.connection(id)」によるコネクタ アクションの失敗（あいまいな / 不正な接続）、パブリッシュ/ビルドの失敗(SDK インライン化、ネイティブ デプロイ)、階層クォータ超過応答、および OpenRouter レート制限/請求一時停止の状況。次のようなリクエストでトリガーします: エージェントが機能していない、なぜこれは 500 なのか、この実行はハングしている、HITL 受信箱は空ですが実行は一時停止中、エージェントは会話を忘れた、ワークフローが入力を拒否した、公開に失敗した、なぜ 402 が返されるのか、なぜコネクタ呼び出しがあいまいなのか、モデル呼び出しが応答を返さない。スキルは診断コマンドの実行をガイドします。修正を提案するものではありません。原因を特定したら、stackbone または stackbone-cli スキルに戻って修正を実装します。
