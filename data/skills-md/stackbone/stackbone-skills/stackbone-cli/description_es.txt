Utilice esta habilidad al manejar la CLI `stackbone` para desarrollar, publicar u operar un agente en Stackbone: autenticarse con el flujo del dispositivo (iniciar sesión/whoami), crear un entorno de trabajo y vincularlo al plano de control (init, opcionalmente con una primera pieza de agente/flujo de trabajo/agente de flujo de trabajo), agregar más piezas sin conexión a un espacio de trabajo existente (agregar agente profundo/agregar flujo de trabajo/agregar agente de flujo de trabajo), vincular un directorio existente a un agente (enlace), ejecutar el desarrollo local apilar con Studio (dev), publicar el espacio de trabajo (publicar), cambiar la organización activa como la CLI actúa (uso de la organización), administrar migraciones de bases de datos con Drizzle (db migrar hacia arriba / crear / estado), leer metadatos del espacio de trabajo en JSON (metadatos), enumerar los conectores disponibles y las conexiones de la cuenta con identificadores y nombres (conectores), imprimir documentos en línea (docs) y operar una instalación de agente en ejecución desde el shell: descubrir destinos de instalación (agentes), inspeccionar y activar flujos de trabajo duraderos expone (lista de flujos de trabajo/esquema/inicio), inspecciona y controla ejecuciones (ejecuciones), sigue registros (registros), consulta/explora la base de datos del agente (consulta de base de datos/esquemas/tabla), mueve objetos en la tienda (almacenamiento), opera el índice RAG administrado (rag), establece secretos (secretos), versiona el documento de configuración y regenera sus tipos (config), inspecciona el contrato de protocolo (contrato), decide aprobaciones humanas en el ciclo. (hitl), administrar el catálogo de mensajes (prompts) y leer el cableado de OpenRouter (openrouter). Activar solicitudes como: desarrollar un espacio de trabajo, iniciar un proyecto vacío, agregar un agente profundo a mi espacio de trabajo, agregar un flujo de trabajo, agregar un flujo de trabajo que llame a un agente (agente de flujo de trabajo), iniciar la pila de desarrollo, publicar mi espacio de trabajo, generar una migración, enumerar mis organizaciones, cambiar de organización, enumerar conectores o conexiones, ver qué está vinculado, iniciar sesión en Stackbone, enumerar mis agentes, enumerar los flujos de trabajo que expone una instalación, iniciar un flujo de trabajo por nombre, reintentar una ejecución fallida, seguir el registros, ejecutar un SELECT contra el agente, establecer un secreto, revertir la configuración, aprobar una solicitud HITL. Para escribir código dentro del agente con @stackbone/sdk, utilice la habilidad stackbone en su lugar. Para clasificar errores y ejecuciones atascadas, utilice la habilidad stackbone-debug.
