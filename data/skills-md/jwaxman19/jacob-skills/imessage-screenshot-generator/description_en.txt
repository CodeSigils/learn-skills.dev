Generates realistic iPhone Messages (iMessage) screenshots as retina-quality PNG at 3× resolution from a JSON conversation config. Use for mockups, storytelling, presentations, or any context where you need an iMessage screenshot without a real phone. Trigger phrases: iMessage screenshot, mock Messages, fake text thread, iPhone Messages PNG generator.
