Enforce staged-only behavior for git commits. Always use this skill whenever the user asks to commit, wants a commit created, asks to run git commit, wants a commit message based on repo changes, or uses short prompts like "commit", "commit plz"; combine it with kf-g-git-commit-japanese-commit-message when creating the message.
