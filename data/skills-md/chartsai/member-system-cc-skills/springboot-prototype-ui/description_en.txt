Trigger when the user says "prototype the UI", "mock up the pages", "design the interface first", "create a static HTML mockup", "show me what the app will look like", "run springboot-prototype-ui", or wants to see the UI before setting up any backend code. Also trigger when the user asks to "validate the design", "get a visual preview", "generate fake data pages", or "start with the design". This skill runs as a short interactive design session and produces self-contained HTML prototypes that open in any browser with no server and no build step.
