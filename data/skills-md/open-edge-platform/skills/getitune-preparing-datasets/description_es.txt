Prepare y apunte conjuntos de datos a la biblioteca getitune (la biblioteca de entrenamiento de Geti) para entrenamiento, pruebas y predicción. Úselo cuando un usuario pregunta qué formatos de conjunto de datos son compatibles, cómo funciona el argumento `data=` de `create_engine(...)` / `--data_root`, por qué falla la detección automática de formato, cómo diseñar datos nativos de COCO/YOLO/Pascal VOC/Datumaro, cómo usar un archivo zip o cómo pasar un `data.yaml` de Ultralytics YOLO. Cubre las expectativas de datos por tarea y la detección automática basada en Datumaro.
