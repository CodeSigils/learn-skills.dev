在 getitune 库（Geti 训练库）中准备和指向数据集以进行训练、测试和预测。当用户询问支持哪些数据集格式、`create_engine(...)` / `--data_root` 的 `data=` 参数如何工作、为什么格式自动检测失败、如何布局 COCO/YOLO/Pascal VOC/Datumaro 原生数据、如何使用 zip 存档或如何传递 Ultralytics YOLO `data.yaml` 时使用。涵盖基于 Datumaro 的自动检测和每任务数据期望。
