Préparez et pointez des ensembles de données dans la bibliothèque getitune (la bibliothèque de formation Geti) pour la formation, les tests et la prédiction. À utiliser lorsqu'un utilisateur demande quels formats d'ensemble de données sont pris en charge, comment fonctionne l'argument `data=` de `create_engine(...)` / `--data_root`, pourquoi la détection automatique du format échoue, comment présenter les données natives COCO/YOLO/Pascal VOC/Datumaro, comment utiliser une archive zip ou comment transmettre un `data.yaml` Ultralytics YOLO. Couvre la détection automatique basée sur Datumaro et les attentes en matière de données par tâche.
