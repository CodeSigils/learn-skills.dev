在 getitune 庫（Geti 訓練庫）中準備和指向資料集以進行訓練、測試和預測。当用户询问支持哪些数据集格式、`create_engine(...)` / `--data_root` 的 `data=` 参数如何工作、为什么格式自动检测失败、如何布局 COCO/YOLO/Pascal VOC/Datumaro 原生数据、如何使用 zip 存档或如何传递 Ultralytics YOLO `data.yaml` 时使用。涵蓋基於 Datumaro 的自動偵測和每任務資料期望。
