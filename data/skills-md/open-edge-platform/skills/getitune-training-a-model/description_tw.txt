使用 getitune 庫（Geti 訓練庫）的 Python API 或 CLI 來訓練電腦視覺模型。當使用者想要使用「create_engine(...)」和「engine.train()/engine.test()」來訓練、微調或評估模型、執行「getitune train」/「getitune test」、選擇或覆寫「getitune.recipe.<task>」下的配方、選擇裝置 (cpu/gpu/cpu/cpu)。涵蓋分類、檢測、實例/語意分割和關鍵點檢測。
