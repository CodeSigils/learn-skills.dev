Convert the bid evaluation report from Markdown format to a structured JSON format. Read the entire `投标评估报告.md` and directly extract it as a front‑end readable `投标评估报告.json`. Trigger this when the user requests to convert the evaluation report, generate the evaluation JSON, or format the evaluation data. Prerequisite: the bid‑evaluation Markdown report must already be completed.
