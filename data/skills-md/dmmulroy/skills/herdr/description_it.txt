Controlla il gregge dall'interno. Gestisci aree di lavoro e schede, riquadri divisi, genera agenti o subagenti, leggi l'output, avvia server di sviluppo e attendi cambiamenti di stato, il tutto tramite comandi CLI che comunicano con l'istanza herdr in esecuzione su un socket unix locale. Da utilizzare quando si corre all'interno di herdr (HERDR_ENV=1).
