Punto de entrada principal del análisis del modelo de IA (enrutador): reconocer el tipo de entrada: los archivos del modelo se someten a ingeniería inversa a nivel de archivo (re-ai-modelo), las entradas solo de API se someten a una evaluación a nivel de comportamiento (re-ai-ataque), el comportamiento malicioso se somete a un análisis malicioso. Palabras desencadenantes: análisis del modelo de IA, seguridad del modelo de IA, archivo de modelo, fuga del modelo, ingeniería inversa del modelo, punto de entrada del análisis del modelo.
