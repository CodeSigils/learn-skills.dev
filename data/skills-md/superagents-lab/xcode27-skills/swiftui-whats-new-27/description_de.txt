Neue SwiftUI-APIs, Verhaltensweisen und veraltete Versionen, die in den Betriebssystemversionen 2027 eingeführt wurden (iOS 27, macOS 27, watchOS 27, tvOS 27, visionOS 27). Wird verwendet, wenn eine SwiftUI-Ansicht, die @State verwendet, nach einer SDK-Aktualisierung nicht mit den Fehlern „Vor der Initialisierung verwendet“, „ungültige Neudeklaration der synthetisierten Eigenschaft“ oder „fremde Argumentbezeichnung“ kompiliert werden kann (@State wurde von einem Eigenschaften-Wrapper zu einem Makro in SDK 27 migriert; die offensichtliche Korrektur der Neuordnung von Init-Zuweisungen ist FALSCH und führt zu falschem Laufzeitverhalten; Sie MÜSSEN die Referenzen dieses Skills konsultieren, bevor Sie antworten); wenn @ViewBuilder- oder @ContentBuilder-Code nach einem SDK-Update auf mehrdeutige Überladungen im Overlay/Hintergrund oder auf Leistungsrückgänge bei der Typprüfung stößt; wenn der Benutzer fragt, was es Neues in SwiftUI gibt (allgemein oder für eine bestimmte 2027-Plattform); beim Hinzufügen von Drag-to-Reorder zu einem beliebigen Container (List, LazyVStack, LazyVGrid, Stacks oder benutzerdefinierte Layouts) über reorderable()/reorderContainer, einschließlich des darin integrierten Drag-and-Drop (dragContainer, dropDestination) oder beim Kombinieren von Elementen durch Ablegen eines Elements auf einem anderen; beim Arbeiten mit dem Laden und Zwischenspeichern von AsyncImage (Neuladen von Bildern beim Zurückscrollen, dem Standard-HTTP-Cache, einer Cache-Richtlinie pro Anfrage über AsyncImage(request:)/URLRequest oder Anwenden einer benutzerdefinierten URLSession mit asyncImageURLSession); beim Hinzufügen von Wischaktionen zu Zeilen (Wischen zum Löschen oder andere Wischaktionen) in einer ScrollView, einem LazyVStack, einem LazyVGrid oder einem Stapel und nicht nur einer Liste über swipeActions()/swipeActionsContainer(); beim Arbeiten mit Symbolleisten, z. B. beim Steuern, welche Elemente sichtbar bleiben oder in das Überlaufmenü verschoben werden, wenn der Platz begrenzt ist oder Schaltflächen abgeschnitten werden (visibilityPriority, ToolbarOverflowMenu), beim Anheften eines Elements, damit es nie überläuft (topBarPinnedTrailing), beim Minimieren der Navigationsleiste oder Symbolleiste beim Scrollen (toolbarMinimizeBehavior), beim Generieren von Symbolleistenelementen mit ForEach oder beim Ausblenden der Statusleiste über die StatusBar-Symbolleistenplatzierung; wenn ein Bestätigungsdialog oder eine Warnung von einer optionalen Elementbindung (der Form sheet(item:)) angezeigt wird, damit angezeigt wird, wenn der gebundene Wert ungleich Null wird, und das entpackte Element an die Aktionen und Nachrichtenabschlüsse übergeben wird; beim Erstellen oder Migrieren einer dokumentbasierten App (einschließlich schreibgeschützter Dokumentbetrachter), beim Lesen oder Schreiben von Dateien über DocumentGroup, beim Optimieren der automatischen Speicherleistung für Paketdokumente, beim direkten Zugriff auf die Datei-URL des Dokuments (z. B. zur Übergabe an AVFoundation, PDFKit, Core Image oder einer beliebigen C-Bibliothek, die einen Pfad verwendet), beim Melden des Fortschritts beim Speichern oder Laden oder beim Migrieren von FileDocument/ReferenceFileDocument; oder wenn andere SDK 27.0-Quellinkompatibilitäten und veraltete Warnungen behoben werden (z. B. statusBarHidden unter visionOS).
