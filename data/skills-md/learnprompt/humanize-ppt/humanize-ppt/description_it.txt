Direttore degli schemi con sede in AST e decisore dei media per pagina. Produce brief di produzione per competenze PPT downstream native (guizang / frontend-slides / beautiful-html-templates) ed esegue un ciclo di QA post-render sull'HTML renderizzato. Da utilizzare prima di generare diapositive PPT/HTML dalla materia prima.
