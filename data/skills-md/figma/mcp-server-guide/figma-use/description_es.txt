**Requisito previo OBLIGATORIO**: DEBES invocar esta habilidad ANTES de cada llamada a la herramienta `use_figma`. NUNCA llames a `use_figma` directamente sin cargar esta habilidad primero. Omitirlo provoca fallas comunes y difíciles de depurar. Se activa cada vez que el usuario desea realizar una acción de escritura o una acción de lectura única que requiere la ejecución de JavaScript en el contexto del archivo Figma, p. cree/edite/elimine nodos, configure variables o tokens, cree componentes y variantes, modifique el diseño automático o los rellenos, vincule variables a propiedades o inspeccione la estructura de archivos mediante programación.
