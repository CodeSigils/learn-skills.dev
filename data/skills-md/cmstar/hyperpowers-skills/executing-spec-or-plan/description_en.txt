Execute the complete plan or an approved spec, and before implementation centrally confirm Git, commits, TDD, sub‑agent strategy, and the sub‑agent model when the platform is supported. Use it only when the user explicitly requests “executing‑spec‑or‑plan” or explicitly selects an option labeled with that name; do not treat negatives, questions, documentation references, or task matches as authorization to invoke.
