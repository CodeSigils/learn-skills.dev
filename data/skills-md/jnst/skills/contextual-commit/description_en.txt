Along with code changes, write contextual commits that capture intent, decision‑making, and constraints. Use them when committing, upon task completion, or when a user requests a commit. By extending Conventional Commits and adding a structured action line to the commit body, you preserve not only “what” changed but also “why” the code was written.
