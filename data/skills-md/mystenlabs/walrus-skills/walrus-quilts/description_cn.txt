海象被子用于将许多小斑点批量放入单个存储单元。当用户需要将多个文件存储为被子、通过 QuiltPatchId 或标识符检索单个 blob、列出被子中的补丁或使用被子 HTTP 端点时使用。也可在优化许多小 blob 的存储成本时使用。涵盖 CLI（store-quilt、read-quilt、list-patches-in-quilt）和 HTTP API quilt 操作。对于常规的单 blob 存储，请参阅“walrus-cli”或“walrus-http-api”。
