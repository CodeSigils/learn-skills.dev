By examining the git commits and code changes between the current latest version and the previous version, a version change log is generated and used when the user asks “create/generate change log” or “create/gener generate changelog”.
