Système de sauvegarde/chargement robuste utilisant la liaison de données, des DTO sérialisables et des backends de service de données échangeables (fichier/JSON par défaut). Prend en charge l'identité GUID par entité pour enregistrer plusieurs acteurs (joueur, inventaire, ennemis).
