Prendi un problema GitHub, un PR aperto o un file di piano da plans/ e indirizzalo a un PR pronto per l'unione: riproduci prima con un test fallito, implementa la correzione (bug) o la funzionalità (piani), verifica rispetto ai cancelli del repository e apri o aggiorna il PR. Utilizzare quando viene richiesto di risolvere un problema, implementare o eseguire un piano, completare o ottenere un PR aperto o elaborare piani/problemi.
