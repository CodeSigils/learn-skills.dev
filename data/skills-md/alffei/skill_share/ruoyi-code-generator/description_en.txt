Generate CRUD code according to the RuoYi framework specifications. Use this skill when a user needs to generate backend Java code (entity classes, Mapper, Service, Controller), frontend Vue code (list pages, API wrappers), MyBatis XML mapping files, or menu initialization SQL for a database table. Suitable for scenarios such as creating new modules, adding business functions, and generating code based on table structure.
