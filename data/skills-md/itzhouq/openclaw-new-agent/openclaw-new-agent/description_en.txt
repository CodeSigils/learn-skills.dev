Create a new standalone Feishu bot on OpenClaw (multi-account mode). Use when the user requests to create a new Feishu bot, add a new Agent, or deploy a second bot. The process includes: collect configuration → backup → create workspace → modify configuration → verify.
