Create a new standalone Feishu bot (multi-account mode) on OpenClaw. Use this when users request to create a new Feishu bot, add a new Agent, or deploy a second bot. The process includes: gather configuration info → backup → create workspace → modify configuration → verify.
