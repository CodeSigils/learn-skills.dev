使用 npm-check-updates (ncu) 將 package.json 依賴項升級到最新版本。當使用者想要檢查過時的依賴項、升級套件版本、過濾或拒絕特定套件、定位特定版本範圍（最新、最大、次要、修補程式、semver、@tag）、掃描單一或多個套件檔案（--deep、--workspaces、--packageFile）、執行互動式選擇（--interactive）、透過在每次碰撞後執行測試來檢測重大升級（--doctor）、使用私人註冊表、使用或使用 .ncurc.js 配置或 Node 模組中的程式設計 API。
