Define mandatory standards for Python environment and dependency management. Agents must apply this skill whenever they need to use Python, create virtual environments, install dependencies, or manage Python versions. The use of the uv tool is mandatory; pip and conda are prohibited, ensuring project-level environment isolation.
