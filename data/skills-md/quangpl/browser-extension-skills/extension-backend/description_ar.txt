إنشاء واجهات برمجة التطبيقات الخلفية لإضافات Chrome. أوصى NestJS + MongoDB (Mongoose) بالمكدس. المصادقة، خطافات الويب، التحقق من الترخيص، CORS. يُستخدم عندما: الواجهة الخلفية، واجهة برمجة التطبيقات (API)، الخادم، قاعدة البيانات، الترخيص، خطاف الويب.
