Dockerize a repository and produce production-grade deployment scripts, a docker-compose setup with properly configured volumes, and a pre-flight validation shell script. Da utilizzare quando l'utente desidera inserire in un contenitore un'app, aggiungere il supporto Docker, scrivere una pipeline di distribuzione, impostare la composizione docker, configurare volumi o eseguire la distribuzione in produzione con Docker.
