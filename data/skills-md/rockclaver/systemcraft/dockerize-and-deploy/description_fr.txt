Dockerisez un référentiel et produisez des scripts de déploiement de niveau production, une configuration Docker-compose avec des volumes correctement configurés et un script shell de validation avant vol. À utiliser lorsque l'utilisateur souhaite conteneuriser une application, ajouter la prise en charge de Docker, écrire un pipeline de déploiement, configurer Docker-compose, configurer des volumes ou déployer en production avec Docker.
