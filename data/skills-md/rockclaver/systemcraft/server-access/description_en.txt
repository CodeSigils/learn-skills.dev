Access named servers over SSH using a local credentials file, then run safe inspection commands or delegated checks. Use when the user asks to check a server by name, inspect Docker/container state on a remote machine, verify health over SSH, or use credentials from ~/.systemcraft/ssh/creds.yml.
