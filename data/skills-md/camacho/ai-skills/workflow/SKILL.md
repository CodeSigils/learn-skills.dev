---
name: workflow
description: Alias for /task — the universal task dispatcher. Use /task or /workflow interchangeably.
---

Invoke `/task` with the same args. This is a thin alias — all logic lives in `/task`.
