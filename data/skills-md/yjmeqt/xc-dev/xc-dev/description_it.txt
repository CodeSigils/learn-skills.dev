Utilizzare per i flussi di lavoro di creazione, esecuzione, test e simulatore iOS/Xcode in progetti che dispongono di una directory `.xc-dev/`. Le rotte funzionano tramite `xc-dev <task>` invece di `xcodebuild` grezzo.
