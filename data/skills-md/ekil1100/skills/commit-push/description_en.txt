When the user requests a commit, push, submit, push, generate a commit message, or says “commit msg semantic” or “help me commit and push,” use this skill. It will examine the diff, generate a Conventional Commits message, and commit or push as needed.
