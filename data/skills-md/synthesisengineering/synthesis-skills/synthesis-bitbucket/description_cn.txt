通过开源 bkt CLI 使用 Bitbucket Cloud 存储库 — PR 生命周期（列表、查看、比较、评论、批准、合并）、存储库/分支读取和 bkt api escape hatch。对身份验证设置、上下文主机陷阱、gh-to-bkt 命令映射和写入安全规则进行编码，以便代理使用一个一致的命令界面，而不是重新发明 REST 调用。当要求时使用：bitbucket、bkt、bitbucket pr、打开 bitbucket pr、查看 bitbucket pr、bitbucket cli、bitbucket api。
