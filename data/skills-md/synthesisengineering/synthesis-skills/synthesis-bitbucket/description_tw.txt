透過開源 bkt CLI 使用 Bitbucket Cloud 儲存庫 — PR 生命週期（清單、檢視、比較、評論、核准、合併）、儲存庫/分支讀取和 bkt api escape hatch。對身份驗證設定、上下文主機陷阱、gh-to-bkt 命令映射和寫入安全規則進行編碼，以便代理使用一個一致的命令介面，而不是重新發明 REST 呼叫。要求時使用：bitbucket、bkt、bitbucket pr、開啟 bitbucket pr、查看 bitbucket pr、bitbucket cli、bitbucket api。
