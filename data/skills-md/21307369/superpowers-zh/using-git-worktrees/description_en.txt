When you need to start feature development isolated from the current workspace, or use it before executing the implementation plan—ensure the isolated workspace exists through native tools or the git worktree fallback mechanism.
