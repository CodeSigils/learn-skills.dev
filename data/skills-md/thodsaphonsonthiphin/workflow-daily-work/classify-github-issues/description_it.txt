Mappa i risultati sottoposti a valutazione su GitHub Issues con le etichette, l'elemento cardine e il corpo corretti ed emetti github_backlog_input.json pronto per la creazione. Utilizza questa valutazione DOPO la valutazione e PRIMA di creare problemi: quando l'utente chiede "quali etichette dovrebbero ottenere", "trasforma questi risultati in problemi GitHub", "classificali per GitHub" o "crea l'input del backlog github". Associa la gravità alle etichette di priorità (P0-P3), il tipo alle etichette di tipo (bug/miglioramento/attività) e le ore alle etichette di dimensione (taglia: XS-XL). Crea un'attività cardine per il batch. Giù le mani per i problemi di github-create.
