Da utilizzare quando si aggiunge un componente dell'interfaccia utente animato, un effetto di testo, un effetto cursore/al passaggio del mouse o uno sfondo WebGL/tela a un progetto React tramite React Bits (reactbits.dev) — o quando viene richiesto di richiamare/scrivere il codice, gli oggetti di scena di un componente React Bits o installare passaggi dalla memoria invece di recuperarlo.
