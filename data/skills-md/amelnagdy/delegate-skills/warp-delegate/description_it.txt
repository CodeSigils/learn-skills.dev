Delega un'attività di codifica alla CLI di Warp Agent (`oz`) come implementatore in background, quindi rivedi le sue differenze e eseguilo tu stesso. Usalo ogni volta che l'utente desidera affidare il lavoro di implementazione a Warp - frasi come "fai in modo che Warp implementi X", "delega questo alla CLI di Warp", "eseguilo tramite Warp", "usa oz per implementare/correggere/refactoring" - o desidera eseguire una coda di attività di codifica tramite Warp rimanendo il revisore. NON UTILIZZARE per attività sufficientemente piccole da essere eseguite in linea, quando l'utente desidera che il codice venga scritto direttamente senza delega, o per la TUI interattiva "warp" (questa abilità guida l'esecuzione headless di "oz agent", non l'app terminale).
