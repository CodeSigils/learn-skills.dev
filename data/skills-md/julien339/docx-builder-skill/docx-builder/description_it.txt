Utilizza questa funzione per creare documenti Word (.docx) professionali e completamente modificabili — rapporti, lettere, contratti, memo, manuali. Progetta il documento come HTML/CSS fluido (visualizzandolo come Artefatto per iterare visivamente con l'utente), quindi esegui la pipeline inclusa Playwright + python‑docx per convertire l'HTML in contenuto Word nativo e modificabile (intestazioni reali, paragrafi con formattazione per run, elenchi, tabelle, immagini) — non un dump di screenshot. Attiva con “crea un documento Word”, “genera un docx”, “scrivi un rapporto”, “redigi una lettera/contratto”, “crea documentazione”.

Rispondi sempre nella lingua dell'utente.
