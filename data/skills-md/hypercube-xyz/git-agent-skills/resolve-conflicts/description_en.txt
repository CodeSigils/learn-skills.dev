Resolve Git index conflicts semantically for an active merge, rebase, cherry-pick, or revert. Use to inspect stages, edit exact files, stage resolved paths, and return control to the workflow that owns continuation.
