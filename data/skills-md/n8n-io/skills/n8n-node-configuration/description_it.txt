Da utilizzare durante la configurazione di qualsiasi nodo n8n: HTTP, webhook, database, comunicazioni (Slack/Gmail/Discord), AI, trigger, Unisci, qualsiasi cosa. Si attiva su qualsiasi chiamata del generatore di nodi (`node(...)`, `trigger(...)`, `tool(...)`, `memory(...)`, `lingualModel(...)`, `ifElse(...)`, `merge(...)`, ecc.), configurando un parametro, `useDataOfInput`, `numberOfInputs`, convergenza fan-in o qualsiasi debug specifico del nodo.
