يُستخدم عند كتابة أو مراجعة كود n8n SDK الذي يربط IF أو Switch أو Merge أو مخرجات الخطأ أو أي اتصال متعدد الإدخال/متعدد الإخراج. المشغلات على .add()، .to()، .input(n)، .output(n)، .onTrue، .onFalse، .onCase، .onError، useDataOfInput، الدمج، التبديل، عقد IF، فروع الخطأ، fan-out، fan-in، أو أي مراجعة لكائن اتصالات سير العمل.
