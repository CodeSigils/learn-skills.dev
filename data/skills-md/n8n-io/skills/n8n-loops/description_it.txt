Da utilizzare quando si lavora con dati multielemento, batch, API impaginate, API con velocità limitata, tutto ciò che deve "fare questo per ciascuno" o ogni volta che l'utente menziona il looping, l'iterazione, il batching, il paging o il "loop over items". Si attiva su "loop", "iterate", "for ciascuno", "batch", "page through", "paginate", "rate limit", "process all" o qualsiasi nodo che dovrebbe essere eseguito una volta invece che una volta per articolo.
