Da utilizzare quando si lavora con dati multielemento, batch, API impaginate, API con velocità limitata, fan-out su più rami, tutto ciò che deve "fare questo per ciascuno" o ogni volta che l'utente menziona loop, iterazione, batch, paginazione, parallelismo o "loop su elementi". Trigger su "loop", "iterate", "for ciascuno", "batch", "page through", "paginate", "rate limit", "process all", "fan-out", "parallel branch", "concurrency" o qualsiasi nodo che dovrebbe essere eseguito una volta anziché una volta per articolo.
