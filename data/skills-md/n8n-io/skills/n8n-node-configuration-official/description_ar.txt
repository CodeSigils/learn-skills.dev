يُستخدم عند تكوين أي عقدة n8n: HTTP، وخطافات الويب، وقاعدة البيانات، والاتصالات (Slack/Gmail/Discord)، والذكاء الاصطناعي، والمشغلات، والدمج، وأي شيء. تشغيل أي استدعاء لمنشئ العقدة (`node(...)`، `trigger(...)`، `tool(...)`، `memory(...)`، `languageModel(...)`، `ifElse(...)`، `merge(...)`، وما إلى ذلك)، وتكوين معلمة، `useDataOfInput`، `numberOfInputs`، fan-in التقارب، أو أي تصحيح الأخطاء الخاصة بالعقدة.
