Zur Verwendung bei der Konfiguration eines beliebigen n8n-Knotens: HTTP, Webhooks, Datenbank, Kommunikation (Slack/Gmail/Discord), KI, Trigger, Merge, alles. Wird bei jedem Node-Builder-Aufruf („node(...)“, „trigger(...)“, „tool(...)“, „memory(...)“, „ languageModel(...)“, „ifElse(...)“, „merge(...)“ usw.) ausgelöst, bei der Konfiguration eines Parameters, „useDataOfInput“, „numberOfInputs“, Fan-In-Konvergenz oder bei jedem knotenspezifischen Debugging.
