Deploy the “high‑IQ leadership + cheap execution” hierarchical sub‑agents on a new machine or new project: Codex Sol as leader + Luna as worker, with optional Claude Code project‑level agents and Pi/pi‑flow cross‑tool orchestration. Used for:
1. Install and configure Codex / Claude Code / Pi from scratch
2. Write project‑level .codex/agents, AGENTS.md, .claude/agents
3. Fix the issue where Sol cannot spawn Luna in the multi‑agent catalog
4. Run Sol/Luna/multi‑agent smoke tests
Triggers: new machine setup, Sol‑Luna, hierarchical sub‑agents, multi‑agent configuration, codex agents initialization.
