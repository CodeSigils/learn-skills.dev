RL 환경의 검증자(PrimeIntellect) 변형을 구축합니다. 누군가가 검증자 환경의 스캐폴드, 검증자로 포팅, 프로세스 내 툴킷 구축, 루브릭으로 `vf.ToolEnv` 설정, TRL `GRPOTrainer` 롤아웃 연결을 요청할 때마다 사용하세요. 검증자는 사용자가 처리 중인 도구(HTTP 서버 없음), 일반 Python 함수로 구동되는 구조화된 도구 호출, 여러 등급 기능이 포함된 구성 가능한 보상 루브릭, Docker가 없는 빠른 반복 또는 프로토타입에서 TRL 훈련까지의 가장 깔끔한 경로를 원할 때 적합한 프레임워크입니다. 출력은 `env.py`(툴킷 + 독립 실행형 도구 기능 + `create_verifiers_env`), `rollout.py` 및 `pyproject.toml`이 포함된 실행 가능한 `<env_dir>/verifiers/` 폴더입니다. "X용 검증기 환경 만들기", "검증기에서 내 게임 래핑" 또는 "vf.ToolEnv ​​설정"과 같은 프롬프트에 사용하세요.
