`models.ts`、`index.ts`、package/tsconfig スキャフォールディングを生成し、`createSync()`、`createAction()`、または `createOnEvent()` でレガシー スクリプトをラップすることにより、レガシー `nango.yaml` Nango プロジェクトを Zero YAML TypeScript に手動で移行します。 Nango リポジトリにまだ「nango.yaml」があり、手動によるゼロ YAML 移行が必要な場合に使用します。
