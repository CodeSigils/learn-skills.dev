Java 구성 요소 버전 취약점 감지 도구입니다. pom.xml, build.gradle 또는 jar 파일에서 타사 종속성을 검사하고 알려진 취약성 규칙(CVE)을 일치시키며 취약성 탐지 보고서를 생성합니다. 다음에 적합합니다: (1) Java 프로젝트 종속성에 대한 보안 감사, (2) Log4j, Fastjson, Shiro, Spring과 같은 고위험 구성 요소 취약점 식별, (3) jar 패키지 디컴파일 후 종속성 추출. 디렉터리별 계층적 그룹화를 지원하고 java-decompile-mcp를 통해 .class/.jar 파일에서 종속성 정보 추출을 지원합니다.
