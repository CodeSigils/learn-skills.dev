When the user requests auditing Java, .NET, or PHP source code, deployment artifacts, decompiled artifacts, or security findings, and needs the default script output directory, report output directory, Java/.NET decompilation and deobfuscation references, Java component YAML regex‑matching scan, confirmation of vulnerability determination criteria, security payloads, and BurpSuite raw HTTP request packet evidence. It is used solely for authorized code auditing and defensive security verification.
