构建后将 CodeSpring 与真实代码重新同步。独立检查 CodeSpring 中的功能、注释和 PRD 是否仍然与代码现在实际执行的操作相匹配（功能一旦构建就偏离了计划），然后更新 CodeSpring 以匹配。在代码库上是只读的——它只读取 code/git 并写入 CodeSpring。在功能完成后使用，或定期使用，以防止地图变得陈旧。触发器，“重新同步我的代码库”，“CodeSpring 是否是最新的”，“检查过时的 PRD”，“从代码更新 CodeSpring”，“我的计划偏离了代码”。
