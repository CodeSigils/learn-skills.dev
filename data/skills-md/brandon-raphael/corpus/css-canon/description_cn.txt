在编写或编辑 CSS、Tailwind 类或 React/Web UI 样式时应用此技能 - 构建布局、组件、页面、仪表板、表单或为项目设置主题。触发：flexbox、网格、间距、定位、响应式设计、shadcn/ui 主题、设计标记、深色模式，或任何 UI 需要结构或视觉 CSS 的时候，即使是单个元素。目标是保持简单且可维护的 CSS：自上而下工作（主题 → 布局 → 组件 → 元素），依靠令牌比例而不是幻数，使用相对单位以便 UI 缩放并保持可访问性，并编写产生正确结果的最少属性。使用它可以阻止代理产生过度设计、黑客攻击、难以调试的样式。
