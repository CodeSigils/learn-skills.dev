Execution is a Git workflow that persists repository changes. During the prepare phase, it reads docs/user/git‑preferences.md and establishes the Git context for the current run; checkpoint and finalize inherit this context, managing branches/worktrees, synchronization, commit, push, merge, and cleanup. If a configuration is missing or behavior is ambiguous, it initializes or prompts for each item.
