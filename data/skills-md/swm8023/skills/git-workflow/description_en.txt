The execution runs a Git workflow that persists repository modifications. During the prepare phase, it reads `docs/user/git-preferences.md` and creates the Git context for this run; checkpoint and finalize inherit this context, handling branch/worktree management, synchronization, commit, push, merge, and cleanup. If configuration is missing or behavior is ambiguous, it initializes or prompts for each item.
