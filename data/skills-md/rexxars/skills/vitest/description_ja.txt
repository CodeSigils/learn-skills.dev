vitest (`vitest`、`npx vitest`、`pnpm vitest`、`npm test`/`pnpm test`/`yarn test`、または vitest バイナリを呼び出すコマンド) を実行する前、および環境変数、グローバル、またはキャプチャされたエラーのアサートに触れる vitest テストを作成または編集するときに使用します。監視モードでのハング、出力を再フィルタリングするためだけにスイートを再実行すること、並列ワーカー全体で `process.env` を変更すること、実際にテストを中断したエラーを隠すアサーションを防止します。
