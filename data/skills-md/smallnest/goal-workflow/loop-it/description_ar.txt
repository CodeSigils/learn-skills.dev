حلقة مشكلة تلقائية مع نقطة تفتيش/استئناف: جلب مشكلات GitHub المفتوحة ← فرز طوبولوجي مدرك للتبعية ← تنفيذ كل مشكلة من البداية إلى النهاية ← المراجعة باستخدام /review-it ← المستند مع /note-it ← الشحن مع /ship-it ← التكرار. استمرار الحالة إلى .loop-state.json لاستعادة الأعطال. المشغلات على: التكرار، مشكلات الحلقة، التنفيذ التلقائي، التكرار، التكرار، التكرار، المشكلة، التكرار، حلقة الاستئناف.
