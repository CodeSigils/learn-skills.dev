Ciclo automatico dei problemi con checkpoint/riprendi: recupera i problemi aperti di GitHub → ordinamento topologico in base alle dipendenze → implementa ogni problema end-to-end → rivedi con /review-it → documenta con /note-it → spedisci con /ship-it → ripeti. Lo stato persiste su .loop-state.json per il ripristino da arresto anomalo. Si attiva su: loop-it, problemi del loop, implementazione automatica, 批量实现, 循环实现, 实现所有issue, 恢复循环, curriculum loop.
