À utiliser pour transformer une exigence, une spécification ou une description de fonctionnalité en un seul document de conception HTML autonome dans un style maison fixe : une page HTML stylisée avec une table des matières, des diagrammes SVG d'architecture/séquence, des blocs code/SQL/YAML, des zones de légende, un panneau de décisions alignées (已对齐结论), des cas de test et un index de code. Réutiliser pour tout nouveau 需求/设计文档 qui devrait être identique à chaque fois.
