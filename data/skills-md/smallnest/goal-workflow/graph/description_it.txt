Ingegneria dei grafici per l'esecuzione di attività parallele: converti un'attività, un PRD, uno SPEC o un set di problemi in un grafico delle dipendenze (DAG), sovrapponilo in superstep, quindi implementa ciascun nodo indipendente contemporaneamente ai subagenti: ciascun nodo esegue /goal → /review-it → /ship-it in un albero di lavoro git isolato, con una barriera fan-in tra le onde. Trigger su: grafico, ingegneria dei grafici, creazione di un grafico, grafico delle attività, grafico delle dipendenze, DAG, implementazione parallela, 并发实现, 并行实现, 任务图, 把任务变成图, fan-out fan-in, superstep, flusso di lavoro dinamico.
