用於平行任務執行的圖形工程：將任務、PRD、SPEC 或問題集轉換為依賴圖 (DAG)，將其分層為超級步驟，然後與子代理同時實現每個獨立節點 — 每個節點在隔離的 git 工作樹中運行 /goal → /review-it → /ship-it，波之間有扇入屏障。觸發：圖、圖工程、建置圖、任務圖、依賴圖、DAG、平行實作、並發實作、零件實作、任務圖、把任務變成圖、扇出扇入、超級步驟、動態工作流程。
