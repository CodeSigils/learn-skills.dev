Escribir, revisar y adaptar SQL para bases de datos en modo AUTO de edición distribuida PolarDB-X 2.0 (edición empresarial), manejar correctamente las diferencias entre PolarDB-X y MySQL (tablas particionadas, índices secundarios globales GSI, índices en columnas CCI, secuencia, transacciones distribuidas, grupos de tablas, tablas TTL, etc.). Adecuado para escenarios que requieren generación de SQL para ejecutar en PolarDB-X, migrar MySQL SQL a PolarDB-X o depurar problemas de compatibilidad de PolarDB-X SQL.
