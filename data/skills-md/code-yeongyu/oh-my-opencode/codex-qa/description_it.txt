Effettua il QA dell'edizione omo Codex Light (lazycodex / packages/omo-codex) stessa, in stretto isolamento in modo che venga utilizzato SOLO il nostro plugin, mai il vero ~/.codex dell'utente. Il metodo proprietario guida il vero `codex app-server` contro un CODEX_HOME isolato più un modello fittizio LOCALE (nessuna vera chiamata API) e dimostra un hook del plugin attivato affermando notifiche hook/iniziato + hook/completato. Inoltre: verifica dell'installazione isolata, sonde di hook per componente, fumo TUI tmux e osservazione del registro di runtime (RUST_LOG / logs SQLite / /debug-config). Le navi hanno testato gli script di supporto, ciascuno con un --self-test. Da utilizzare ogni volta che qualcuno modifica qualcosa in packages/omo-codex o desidera eseguire il QA, effettuare smoke-test, verificare o eseguire il debug del plugin Codex, dei suoi hook/componenti, del programma di installazione/config.toml, del flusso del server dell'app o della TUI Codex. Trigger: codex qa, qa codex, codex-qa, test plugin codex, verifica hook codex, server app codex, lazycodex qa, CODEX_HOME isolato, prova hook codex attivato, test codex tui.
