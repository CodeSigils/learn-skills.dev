QA opencode stesso, per caso: verificare la CLI/terminale (esecuzione opencode, db, servire, esportare), dimostrare uno specifico hook/azione/evento del plugin attivato tramite il flusso di eventi SSE, effettuare uno smoke-test della TUI sotto tmux ed esaminare le sessioni nel database SQLite di opencode in base a ID, titolo/nome o testo del messaggio. Vengono forniti script di supporto testati (ciascuno con un --self-test) più riferimenti per dominio. Da utilizzare ogni volta che qualcuno desidera eseguire il QA, effettuare smoke-test, verificare o eseguire il debug della CLI di opencode, del server HTTP, degli hook/eventi del plugin o della TUI oppure per trovare/ispezionare le sessioni di opencode nel database. Trigger: opencode qa, qa opencode, test opencode, verifica hook opencode, db sessione opencode, trova sessione opencode per ID/nome/testo, test tui opencode, integrità del server opencode, flusso di eventi opencode.
