當階段涉及 UI（元件、頁面、佈局、樣式、互動）或將 UI 與非 UI 工作混合時，在「<cwd>/.plans/<feature>/plan.md」處執行分階段規劃文件的一個階段。對於純粹的非 UI 階段（後端、庫、CLI、基礎設施、腳本），不要觸發。觸發器包括「實施階段 X」、「執行階段 N」、「開始實施」、「建置 <feature> 的階段 2」。編寫滿足每個驗收標準的生產程式碼，並為每個 AC 建立一個原子 git 提交。 FP 優先，OOP 後備；當使用者選擇加入時，支援可選的 TDD 模式（紅色 → 綠色 → 重構）。
