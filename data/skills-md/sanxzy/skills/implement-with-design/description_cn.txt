当阶段涉及 UI（组件、页面、布局、样式、交互）或将 UI 与非 UI 工作混合时，在“<cwd>/.plans/<feature>/plan.md”处执行分阶段计划文件的一个阶段。对于纯粹的非 UI 阶段（后端、库、CLI、基础设施、脚本），不要触发。触发器包括“实施阶段 X”、“执行阶段 N”、“开始实施”、“构建 <feature> 的阶段 2”。编写满足每个验收标准的生产代码，并为每个 AC 创建一个原子 git 提交。 FP 优先，OOP 后备；当用户选择加入时，支持可选的 TDD 模式（红色 → 绿色 → 重构）。
