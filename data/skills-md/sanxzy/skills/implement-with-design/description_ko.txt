단계가 UI(구성 요소, 페이지, 레이아웃, 스타일 지정, 상호 작용)에 닿거나 UI와 UI가 아닌 작업을 혼합할 때 `<cwd>/.plans/<feature>/plan.md`에서 단계적 계획 파일의 한 단계를 실행합니다. UI가 아닌 단계(백엔드, 라이브러리, CLI, 인프라, 스크립트)의 경우 실행하지 마세요. 트리거에는 "X단계 구현", "N단계 수행", "구현 시작", "<기능>의 2단계 빌드"가 포함됩니다. 모든 허용 기준을 충족하는 프로덕션 코드를 작성하고 AC당 하나의 원자 git 커밋을 생성합니다. OOP 폴백을 포함한 FP 우선; 사용자가 선택하면 선택적 TDD 모드(빨간색 → 녹색 → 리팩터링)를 지원합니다.
