根据 `.plans/<feature>/plan.md` 的计划实现 **非 UI** 阶段 - 后端、库、CLI、基础设施、数据管道；该阶段必须没有 UI 工作（组件、页面、样式、交互）。在“实施 X 阶段”、“执行 N 阶段”、“构建 <功能> 的第 2 阶段”时触发。更喜欢 FP 惯用语（纯函数、不变性、组合）；对于基于类的语言或 OOP 重的代码库（“FP.md”），回退到 OOP。 “tdd”/“测试优先”上的可选 **TDD 模式**（红色→绿色→重构）——请参阅“TDD.md”。
