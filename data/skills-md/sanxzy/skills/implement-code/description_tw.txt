根據 `.plans/<feature>/plan.md` 的計畫實作 **非 UI** 階段 - 後端、函式庫、CLI、基礎架構、資料管道；這個階段必須沒有 UI 運作（元件、頁面、樣式、互動）。在「實施 X 階段」、「執行 N 階段」、「建置 <功能> 的第 2 階段」時觸發。偏好 FP 慣用語（純函數、不變性、組合）；對於基於類別的語言或 OOP 重的程式碼庫（“FP.md”），回退到 OOP。 「tdd」/「測試優先」上的選用 **TDD 模式**（紅色→綠色→重構）－請參閱「TDD.md」。
