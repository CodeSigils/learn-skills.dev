Planifiez - n'exécutez pas - les portes de qualité du projet TypeScript : hooks Husky, lint-staged, commitlint, linter/formatter (ESLint+Prettier ou Biome), Vitest et détection de code mort Knip. Écrit un plan pouvant être repris dans `<cwd>/.plans/setup-ts-project-config/plan.md` ; les réexécutions se réconcilient avec l'état actif. Déclenchez sur « planifier la configuration de pré-validation », « planifier la configuration de Husky et Lint », « planifier la configuration eslint/prettier/biome », « ajouter Knip », « configurer les portes de qualité du code », « configurer les hooks de pré-commit » ou /setup-ts-project-config.
