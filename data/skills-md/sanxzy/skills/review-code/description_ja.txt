実装レポートをその計画と実際のコミットに対して検証します。各 AC の差分を読み取り、コードが約束を満たしているかどうかを確認します。トリガー: 「実装のレビュー」、「フェーズ X のレビュー」、「レポートの検証」、「実装は計画と一致したか」。欠落した AC、幻覚的な SHA、スコープ クリープ、FP 規律違反、複雑さの不一致、および (UI) 設計決定のギャップを検出します。判定を `.plans/<feature>/reviews/{with-design,code}/<phase>.md` に書き込みます。 1 回の呼び出し = 1 フェーズ。
