Orchestra il QA multidimensionale (funzionale, rete, prestazioni, reattivo, console, differenza visiva/DOM) con `chrome-devtools-axi` come driver (loop di snapshot, Lighthouse, CWV, heap, emulazione) e `agent-browser` per ciò che manca ad AXI (`diff` visiva, HAR, auth vault, multisessione, cloud, iOS). Attiva su "QA questa funzione", "regressione su X", "controlla Lighthouse su Y" o `/qa-test`. Fratello di `agent-browser`: sceglilo per la navigazione una tantum; scegli questo per un piano strutturato in `./.plans/qa-test/<run-id>/`. Riprendibile; legge prima lo stato precedente; non genera mai codice di test.
