Implementa le istruzioni utente in formato libero come criteri di accettazione (AC), un commit atomico per AC: non sono richiesti file di piano o regole di progettazione. Trigger: "implementa <cosa>", "crea <funzionalità> end-to-end", "implementa semplicemente <descrizione>", "spedisci <X> end-to-end". Per le fasi di un piano esistente in `<cwd>/.plans/<feature>/plan.md`, non attivare. Scrive un rapporto su `<cwd>/.plans/<slug>/implementations/with-instructions/{feat,fix,refactor}/run.md` con follow-up aperti. FP-prima; modalità TDD opzionale.
