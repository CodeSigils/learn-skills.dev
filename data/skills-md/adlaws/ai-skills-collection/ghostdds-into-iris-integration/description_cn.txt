擅长将 GhostDDS 集成到类似 IRIS 的 Web 可视化平台，无需任何 RTI 运行时或许可证即可回放 RTI Connext DDS 记录数据库，同时保留现有的实时 DDS 模式。当询问有关集成 GhostDDS、向 IRIS 添加录制回放、删除 RTI 重播依赖项、创建 DataBridge 抽象、添加回放端点、将重播前端迁移到统一 API 或对 GhostDDS 集成问题进行故障排除时使用。
