Une compétence pour installer ou supprimer les hooks de sécurité git de ce plugin dans ~/.claude/settings.json. Déclenché par "hooks install", "guard install", "setup hooks", "install hooks", "hook setup", "hooks Remove", "guard Remove", "uninstall hooks", "hook delete", etc.
