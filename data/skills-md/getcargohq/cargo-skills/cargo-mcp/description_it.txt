Guida Cargo dal suo server MCP ospitato su https://mcp.getcargo.io/mcp — collega un client, scopri e valuta un'azione, eseguila su un singolo record o su un batch, interroga lo stato e leggi i modelli dello spazio di lavoro, senza installare la CLI. Indica anche quando chiamare uno strumento MCP invece di avviare `cargo-ai`. Trigger: "collega Cargo a Claude Desktop", "aggiungi Cargo a ChatGPT", "server MCP di Cargo", "mcp.getcargo.io", "usa Cargo senza installare nulla", "quale strumento Cargo devo chiamare", "search_actions", "execute_action_batch", "Il server MCP mostra lo spazio di lavoro sbagliato". Strumenti: whoami, search_actions, get_action_schema, execute_action, execute_action_batch, get_run, query_models. Salta quando: hai una shell e il compito è un workflow, un deploy CDK, SQL di magazzino o una casella di posta — usa le competenze CLI; quando pubblichi un server MCP dal tuo spazio di lavoro o ne colleghi uno a un agente Cargo — usa cargo‑ai.

Rispondi sempre nella lingua dell'utente.
