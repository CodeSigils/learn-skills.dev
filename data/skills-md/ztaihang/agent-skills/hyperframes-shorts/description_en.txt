HyperFrames Chinese short‑video visual and layout preprocessing specification (vertical‑screen readable on Douyin/Xiaohongshu). Applicable to Chinese narration videos with any number of shots. Must first read this Skill, then read hyperframes, hyperframes‑cli, gsap; before writing HTML you must read design‑taste‑frontend (taste‑skill), templates/anti‑slop‑motion‑scheme.md, templates/hyperframes‑zh‑checklist.md, css‑animations, design‑motion‑principles, web‑typography and templates/visual‑style‑guide.md. For each project you must produce a design.md (including a Motion Plan) and rotate visual styles; reusing the same template is prohibited.

Hard layout rules: bottom subtitles must always be a single line, page text must not overlap, Chinese must use local fonts/ + @font‑face (PingFang, Microsoft YaHei, Google Fonts are prohibited), AI‑style emoji or meme‑style illustrations are forbidden; animations: L0 `#root` static background + L1 entrance + L2 semantic alignment. TTS is mandatory (CSS infinite ambient per shot is prohibited).

Before delivery you must run verify‑delivery‑checklist.py + verify‑index‑encoding.py + npm run check (zero errors). Chinese TTS defaults to Edge TTS (generate‑tts.py); after TTS you must run align‑subtitles.py (faster‑whisper word‑level timestamps) then apply‑audio‑schedule.mjs; hyperframes‑media is not required.

The Agent must automatically complete the entire environment and build process (pre‑check, scaffolding, TTS, alignment, timeline, validation); the user only needs to manually run npm run dev to preview. Automatic render/export to MP4 is prohibited—export must wait for user confirmation.

Voice (voice) and subtitles (subtitle) are separated: TTS is generated per spoken sentence, and TTS must not be split to meet subtitle single‑line limits. Split the user's spoken script semantically into multiple scenes, generate on‑screen copy, plan layout, allocate duration, specify transitions and animations, and coordinate voice‑over/BGM/sound‑effect arrangement.
