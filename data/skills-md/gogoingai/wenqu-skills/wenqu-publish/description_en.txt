Clean up the draft article written with wenqu-write into a final version ready for external publication: remove diagram prompt code blocks and project generation identifiers, generate eye‑catching title candidates, an about‑100‑word summary, and a cover image, and output them to a separate publishing directory. This is suitable for the scenario of preparing an article for external release after finalization. Trigger keywords: publish this article, generate publishing version, prepare for publishing, export publishing version. It also serves as the point of integration for future automatic publishing capabilities (e.g., WeChat public accounts, Juejin, etc.).
