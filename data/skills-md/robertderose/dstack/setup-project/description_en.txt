Create and initialize a new Copier-managed dstack project from the latest stable template release or the explicitly selected unstable channel. Use only for a new project, with an explicit project name or basename($PWD) by default.
