يُستخدم عند كتابة أو مراجعة JavaScript/TypeScript في هذا الريبو الذي ينشئ وكيلًا صوتيًا تفاعليًا عبر "agent.deepgram.com/v1/agent/converse". يغطي `client.agent.v1.createConnection()` / `connect()` و`sendSettings` و`sendMedia` وتحديثات وقت التشغيل ومعالجة الأحداث واستجابات استدعاء الوظائف. استخدم "deepgram-js-text-to-speech" للتوليف أحادي الاتجاه، و"deepgram-js-speech-to-text" أو "deepgram-js-conversational-stt" للنسخ فقط، و"deepgram-js-management-api" لإدارة المشروع/النموذج بدلاً من وقت تشغيل الوكيل المباشر. تتضمن المشغلات "وكيل الصوت"، و"وكيل المحادثة"، و"الازدواج الكامل"، و"الدخول إلى البارجة"، و"استدعاء الوظائف"، و"agent.v1".
