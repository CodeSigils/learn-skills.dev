Pixel‑level 750 px image‑to‑code and slicing workflow. Used when Codex needs to convert the currently selected UI image, screenshot, app/web design mockup, Figma export, or design long image into code and output independent transparent PNG slice assets. Suitable for tasks that require strict 1:1 proportional restoration, an artboard width exactly 750 px, disabling automatic layout optimization, editable text, simple graphics as native vectors/code, independent transparent PNG extraction of icons and bitmaps, and independent validation of three module types: vector layers, text layers, and bitmap/icon slices.
