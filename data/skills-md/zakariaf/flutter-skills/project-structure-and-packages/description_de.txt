Erzwingt Flutter/Dart-Projektgerüste, bei denen das Layout Fehler beseitigbar macht – standardmäßig eine Einzelpaket-App (feature-first unter lib/features/, gemeinsame Grundlage aufgeteilt nach Rolle in core/data/services/routing/theme/l10n/, ein dünnes main.dart, das bootstrap() aufruft, kein lib/src in der App), das ein reines Dart-Paket mit generischem Namen nur dann extrahiert, wenn eine Logik dies verdient (öffentliches Fass über privates). lib/src/ nur für PAKETE, nur Meta-Deps als Kompilierungs-Firewall, Resolution:Workspace-Mitglied, nur nach unten gerichtete Abhängigkeits-DAG), wobei Pubspec als Audit-Artefakt behandelt wird und keine utils/helpers/common/misc/grab-bag-shared Junk-Drawer-Ordner vorhanden sind. Verwenden Sie diese Option, wenn Sie eine .dart-Datei oder ein Verzeichnis erstellen, entscheiden, wo ein Repository/Notifier/MethodChannel/Werttyp/Token hingehört, eine pubspec.yaml erstellen oder reparieren, ein Arbeitsbereichsmitglied hinzufügen, eine Importanweisung schreiben, einen zu testenden Test spiegeln oder einen Diff auf Organisation überprüfen.
