透過 MaterialApp.router 在 lib/routing/ 中強制執行一個應用程式範圍內的 GoRouter，路徑參數中的深層可連結身分從不 state.extra、context.go-vs-context.push 規則、將守衛重新導向為由 RiverpodSavienable 驅動的純函式、用於保留分支狀態的 BottomNavigationk. CustomTransitionPage 轉換、PopScope (canPop/onPopInvokedWithResult) 用於未儲存變更攔截，以及 errorBuilder 404 路由。在新增路由、GoRoute、重定向、驗證/入門門、深層連結、ShellRoute 或巢狀導航、底部導覽/軌道標籤外殼、頁面轉換、後退按鈕/未儲存變更處理、通知有效負載到位置對應、類型化路由、go_router_builder 或將 go_router 連接到 app.dart 時使用。
