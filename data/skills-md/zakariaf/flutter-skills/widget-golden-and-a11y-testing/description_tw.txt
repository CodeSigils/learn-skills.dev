強制執行嚴格的 widget/layout/golden/a11y 測試表面 - 一個 PumpApp 工具，用於固定 tester.view.physicalSize x devicePixelRatio 並在 MaterialApp 上一層 MediaQuery (textScaler/boldText/accessibleNavigation)，一個永遠不會抑制設備的粗元組 x RenderObject 報告一次溢出），一個計算的 getSize/getRect 擬合幾何門而不是 Goldens，兩個 Golden車道（Ahem幾何+一種固定操作系統真實字體），帶有loadAppFonts和阻塞--update-goldens，方向性下的RTL金色，純Dart WCAG / APCA顏色值對比，以及滿足指南的誠實限制。在編寫 test/support/harness.dart、呼叫 PumpWidget/pumpApp、追蹤「溢位 N 像素」故障、取得 takeException/ignoreOverflowErrors/FittedBox/withClampedTextScaling、新增 matchesGoldenFile 或使用 isSemantics/simulatedAccess.ibility
