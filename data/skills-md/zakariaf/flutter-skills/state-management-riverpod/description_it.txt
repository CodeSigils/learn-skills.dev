Applica lo stato della funzionalità come un Notifier/AsyncNotifier/StreamNotifier ViewModel su un valore di stato immutabile con uguaglianza di valori, stato modificabile privato modificato solo tramite metodi di intento, deriva-non-archiviare, un singolo percorso di scrittura attraverso un repository e flusso di dati unidirezionale; Riverpod 3.x è DI (provider-as-collaborators, ProviderScope esegue l'override per sapore, lanciando giunzioni), legge ref.watch/.select diviso per display vs ref.read(p.notifier) in callback vs ref.listen per effetti collaterali, modello asincrono come AsyncValue, stato per entità con chiave familiare + autoDispose e acquisizioni/legacy di chiusura obsoleta StateProvider-StateNotifierProvider-ChangeNotifierProvider / get_it / package:provider sono vietati. Da utilizzare quando si aggiunge uno stato a una schermata, si scrive un controller di funzionalità/ViewModel, provider di cablaggio o DI, si deriva un modello di lettura da un flusso o si esaminano problemi di ricostruzione/perdita di stato/smaltimento/percorso di scrittura.
