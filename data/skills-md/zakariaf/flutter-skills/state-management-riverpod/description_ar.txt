يفرض حالة الميزة كنموذج عرض Notifier/AsyncNotifier/StreamNotifier ViewModel واحد على قيمة حالة غير قابلة للتغيير مع تساوي القيمة، وحالة خاصة قابلة للتغيير يتم تغييرها فقط من خلال أساليب الهدف، واشتقاق عدم التخزين، ومسار كتابة واحد من خلال مستودع، وتدفق بيانات أحادي الاتجاه؛ Riverpod 3.x هو DI (المزودون كمتعاونين، تجاوزات ProviderScope حسب النكهة، طبقات الرمي)، يقرأ المرجع المقسم.watch/.select للعرض مقابل ref.read(p.notifier) في عمليات الاسترجاعات مقابل ref.listen للتأثيرات الجانبية، والمزامنة على غرار AsyncValue، ومفتاح العائلة لكل كيان + autoDispose، والتقاط/التقاط الإغلاق القديم/الإرث StateProvider-StateNotifierProvider-ChangeNotifierProvider / get_it / package:provider محظورة. يُستخدم عند إضافة حالة إلى شاشة، أو كتابة وحدة تحكم الميزات/ViewModel، أو موفري الأسلاك أو DI، أو اشتقاق نموذج قراءة من دفق، أو مراجعة مشكلات إعادة البناء/تسرب الحالة/التخلص/مسار الكتابة.
