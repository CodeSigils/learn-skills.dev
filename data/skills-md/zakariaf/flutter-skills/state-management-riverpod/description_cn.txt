将功能状态强制为一个 Notifier/AsyncNotifier/StreamNotifier ViewModel，覆盖具有值相等的不可变状态值、仅通过意图方法、派生不存储、通过存储库的单个写入路径和单向数据流改变的私有可变状态； Riverpod 3.x 是 DI（providers-as-collaborators，ProviderScope 覆盖每个风格，抛出接缝），读取 split ref.watch/.select 进行显示，回调中的 ref.read(p.notifier) 与 ref.listen 进行副作用，异步建模为 AsyncValue，每个实体状态系列键控 + autoDispose，以及过时闭包捕获 / 遗留 StateProvider-StateNotifierProvider-ChangeNotifierProvider / get_it / package:provider 被禁止。在向屏幕添加状态、编写功能控制器/ViewModel、连接提供程序或 DI、从流中派生读取模型或检查重建/状态泄漏/处置/写入路径问题时使用。
