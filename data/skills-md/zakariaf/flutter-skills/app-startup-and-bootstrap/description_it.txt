Impone un ordine di avvio a freddo main() fisso: crash-log sink + FlutterError.onError + PlatformDispatcher.onError installato PRIMA di qualsiasi codice che possa generare, impostazioni/tema letti prima di runApp in modo che il primo frame venga visualizzato correttamente, vera infra costruita in un bootstrap() di composizione-root e iniettata tramite ProviderScope overrideWithValue lanciando provider segnaposto, riscaldamento non bloccante rinviato a addPostFrameCallback, esattamente due gestori di errori senza runZonedGuarded, ProviderException scartato prima della registrazione e un WidgetsBindingObserver che scarica lo stato durevole in background/riprendi. Da utilizzare durante la modifica di lib/main.dart, main_<flavor>.dart, bootstrap.dart o app.dart, il riordino di qualsiasi cosa in main(), l'aggiunta di un gate splash/onboarding/permission, il ripristino del tema prima della prima verniciatura, il collegamento di un DB o un servizio in ProviderScope, la gestione degli scaricamenti in background/riprendi del ciclo di vita dell'app o la ricerca di latenza di avvio a freddo, ANR o jank del primo frame.
