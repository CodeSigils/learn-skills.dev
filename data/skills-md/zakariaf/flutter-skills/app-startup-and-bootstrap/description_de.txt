Erzwingt eine feste main()-Kaltstartreihenfolge – Absturzprotokollsenke + FlutterError.onError + PlatformDispatcher.onError wird VOR jedem Code installiert, der auslösen kann, Einstellungen/Design vor runApp lesen, damit der erste Frame korrekt dargestellt wird, echte Infrastruktur in einem Kompositionsstamm-Bootstrap() erstellt und über ProviderScope overrideWithValue über werfende Platzhalteranbieter injiziert, nicht blockierendes Aufwärmen auf addPostFrameCallback verschoben, genau zwei Fehlerhandler ohne runZonedGuarded, ProviderException vor der Protokollierung entpackt und ein WidgetsBindingObserver, der den dauerhaften Status im Hintergrund/bei der Wiederaufnahme löscht. Verwenden Sie diese Option, wenn Sie lib/main.dart, main_<flavor>.dart, bootstrap.dart oder app.dart bearbeiten, alles in main() neu anordnen, ein Splash-/Onboarding-/Berechtigungs-Gate hinzufügen, ein Theme vor dem ersten Malen wiederherstellen, eine Datenbank oder einen Dienst mit ProviderScope verbinden, App-Lifecycle-Hintergrund-/Resume-Flushes verarbeiten oder Kaltstartlatenz, ANRs oder Fehler im ersten Frame verfolgen.
