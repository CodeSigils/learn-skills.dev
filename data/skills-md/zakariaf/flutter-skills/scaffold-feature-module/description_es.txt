Presenta una función navegable de la misma manera cada vez: una carpeta lib/features/<feature>/presentation/ fija (vista tonta + un notificador 1:1/AsyncNotifier ViewModel + widgets/ + <feature>_providers.dart con alcance), dependencias solo hacia abajo sin importaciones de funciones cruzadas, lecturas reactivas como StreamProviders con alcance sobre un repositorio .watch(), cada mutación a través de un repositorio método de ruta de escritura única, resultado/fallo escrito apareció como AsyncValue, una ruta go_router escrita que transporta identidad en los parámetros de ruta, paridad ARB entre configuraciones regionales, clave familiar+disposición automática y geometría de solo EdgeInsetsDirectional. Úselo al agregar una pantalla, pestaña o carpeta de funciones; conectar una función Notificador/ViewModel o un proveedor de flujo con alcance; registrar una ruta; agregar claves ARB para una nueva pantalla; dividir una característica demasiado grande; o agregar un registro persistente que lee su Vista.
