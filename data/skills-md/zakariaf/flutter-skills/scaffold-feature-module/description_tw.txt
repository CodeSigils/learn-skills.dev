每次都以相同的方式建立一個可導航功能 - 一個固定的 lib/features/<feature>/presentation/ 資料夾（啞視圖 + 一個 1:1 Notifier/AsyncNotifier ViewModel + widgets/ + 作用域 <feature>_providers.dart），僅向下依賴，沒有跨域存儲庫通過 .進行反應性讀取，透過一個儲存庫單一寫入路徑方法進行的每個突變，類型為結果/失敗AsyncValue，一個類型化的 go_router 路由，在路徑參數中攜帶身份、跨區域設定的 ARB 奇偶校驗、family+autoDispose 鍵控和僅限 EdgeInsetsDirectional 的幾何圖形。新增螢幕、標籤或功能資料夾時使用；連線功能通知程式/檢視模型或範圍流提供者；註冊路線；為新螢幕新增 ARB 鍵；分割過度成長的特性；或新增其檢視讀取的持久記錄。
