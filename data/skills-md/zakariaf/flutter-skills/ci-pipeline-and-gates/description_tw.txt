實作精實的 GitHub Actions Flutter CI，其中每個閘都對應到一個命名的發布阻止合約 - 固定運行程式 + subosito/flutter-action@v2 工具鏈、dart 格式 --set-exit-if-changed、flutteranalysis --fatal-infos、build_runner 和漂移模式random、靜態導入/禁止字串 grep 捕獲運行時不能做到的，覆蓋作為報告從不-a-gate，向上謊言固定，驗證從不祝福黃金（CI 中沒有 --update-goldens），以及 CI 無法證明的誠實陳述（音頻、真實字體、設備上行為）。在編輯 .github/workflows/*.yml、新增或刪除作業或步驟、連接 codegen/schema/format/analyze/coverage 門、在 test/policy 或 tool/ 下編寫基於 grep 的策略測試或門腳本、固定操作版本或聲稱 CI 證明了它不能證明的東西時使用。
