يفرض نظامًا حتميًا لإنشاء كود build_runner: قم بتشغيل تمريرة واحدة مثبتة `dart run build_runner build --delete-conflicting-outputs` قبل ``تحليل الرفرفة`` (ليس بعد ذلك أبدًا)، قم بتسييج كل منشئ باستخدام `generate_for:` globs في كل حزمة `build.yaml`، واتخذ قرارًا متعمدًا بالالتزام مقابل gitignore لـ `*.g.dart`/`*.freezed.dart`/`*.drift.dart` وإعادته باستخدام بوابة CI المطابقة (فرق الحداثة إذا تم الالتزام به، الكودجين أولاً إذا تم gitignored)، قم بعكس كتل الملفات التي تم إنشاؤها في المحلل واستبعاد التغطية، وقم بتثبيت SDK، ولا تقم أبدًا بتحرير المخرجات التي تم إنشاؤها يدويًا. يُستخدم عند تحرير مسارات عمل build.yaml أو Analysis_options.yaml أو pubspec.yaml أو .gitignore أو .gitattributes أو CI؛ الأسلاكdrift_dev/freezed/json_serializable/riverpod_generator/ gen-l10n/mockito codegen؛ إصلاح أخطاء "ملف الجزء المفقود" أو "الفئة غير المحددة _$Foo" أو "المخرجات المتعارضة"؛ تحديد ما إذا كان سيتم الالتزام بالتعليمات البرمجية التي تم إنشاؤها؛ أو تحديد نطاق المنشئين بحيث لا يؤدي تعديل واحد إلى إعادة إنشاء كل شيء.
