결정론적 build_runner 코드 생성 규율을 시행합니다. `flutter analyze` 전에 고정된 `dart run build_runner build --delete-con conflicting-outputs` 패스를 실행하고(이후에는 절대), 패키지별 `build.yaml`에서 `generate_for:` glob으로 모든 빌더를 펜싱하고, `*.g.dart`/`*.freezed.dart`/`*.drift.dart`를 일치하는 CI 게이트(커밋된 경우 최신 상태 차이, gitignored인 경우 코드 생성 우선)로 백업하고, 생성된 파일 glob을 분석기에 미러링하고 적용 범위를 제외하고, SDK를 고정하고, 생성된 출력을 직접 편집하지 마세요. build.yaml, analyze_options.yaml, pubspec.yaml, .gitignore, .gitattributes 또는 CI 워크플로를 편집할 때 사용합니다. Drift_dev/freezed/json_serialized/riverpod_generator/gen-l10n/mockito codegen 연결; "누락된 부품 파일", "정의되지 않은 클래스 _$Foo" 또는 "출력 충돌" 오류 수정 생성된 코드를 커밋할지 여부를 결정합니다. 또는 한 번의 편집으로 모든 것이 재생성되지 않도록 빌더 범위 지정.
