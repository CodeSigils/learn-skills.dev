Обеспечивает детерминированную дисциплину генерации кода build_runner: запустите один закрепленный проход `dart run build_runner build --delete-conflicting-outputs` ПЕРЕД `flutter Analysis` (никогда после), изолируйте каждый сборщик с помощью `generate_for:` glob в `build.yaml` для каждого пакета, примите одно обдуманное решение о коммите против-gitignore для `*.g.dart`/`*.freezed.dart`/`*.drift.dart` и подкрепите его соответствующим шлюзом CI (разница актуальности, если зафиксировано, кодирование в первую очередь, если gitignored), отразите glob сгенерированных файлов в анализаторе И исключите покрытие, закрепите SDK и никогда не редактируйте сгенерированные выходные данные вручную. Используйте при редактировании файлов build.yaml, Analysis_options.yaml, pubspec.yaml, .gitignore, .gitattributes или рабочих процессов CI; проводкаdrift_dev/freezed/json_serializable/riverpod_generator/gen-l10n/mockito codegen; исправление ошибок «отсутствует файл детали», «неопределенный класс _$Foo» или «конфликтующие выходные данные»; принятие решения о фиксации сгенерированного кода; или строители области видимости, чтобы одно редактирование не восстанавливало все.
