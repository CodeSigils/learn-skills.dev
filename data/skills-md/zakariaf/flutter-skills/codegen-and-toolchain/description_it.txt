Impone una disciplina deterministica di codegen build_runner: esegui un passaggio `dart run build_runner build --delete-conflitto-outputs` bloccato PRIMA di `flutter analyze` (mai dopo), delimita ogni builder con `generate_for:` glob in un `build.yaml` per pacchetto, prendi una decisione deliberata commit-vs-gitignore per `*.g.dart`/`*.freezed.dart`/`*.drift.dart` e supportalo con il gate CI corrispondente (diff di freschezza se impegnato, codegen-first se gitignored), rispecchia i glob del file generato nell'analizzatore E la copertura esclude, blocca l'SDK e non modificare mai manualmente l'output generato. Da utilizzare durante la modifica di build.yaml, analysis_options.yaml, pubspec.yaml, .gitignore, .gitattributes o flussi di lavoro CI; cablaggio drift_dev/freezed/json_serializable/riverpod_generator/ gen-l10n/mockito codegen; correzione degli errori "file di parti mancanti", "classe non definita _$Foo" o "output in conflitto"; decidere se impegnare il codice generato; o l'ambito dei builder in modo che una modifica non rigeneri tutto.
