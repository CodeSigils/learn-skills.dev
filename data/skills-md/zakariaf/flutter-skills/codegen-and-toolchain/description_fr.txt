Applique une discipline déterministe build_runner codegen : exécutez une passe épinglée `dart run build_runner build --delete-conflicting-outputs` AVANT `flutter analyse` (jamais après), clôturez chaque constructeur avec des globs `generate_for:` dans un package `build.yaml`, prenez une décision délibérée de validation contre gitignore pour `*.g.dart`/`*.freezed.dart`/`*.drift.dart` et sauvegardez-le avec la porte CI correspondante (diff de fraîcheur si validé, codegen-first si gitignoré), mettez en miroir les globs de fichiers générés dans l'analyseur ET la couverture exclut, épinglez le SDK et ne modifiez jamais manuellement la sortie générée. À utiliser lors de la modification des workflows build.yaml, Analysis_options.yaml, pubspec.yaml, .gitignore, .gitattributes ou CI ; câblage drift_dev/freezed/json_serializing/riverpod_generator/gen-l10n/mockito codegen ; correction des erreurs de « fichier de pièce manquant », de « classe non définie _$Foo » ou de « sorties en conflit » ; décider s'il faut valider le code généré ; ou des générateurs de portée afin qu'une seule modification ne régénère pas tout.
