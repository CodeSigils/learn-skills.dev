Erzwingt eine deterministische Build_Runner-Codegen-Disziplin: Führen Sie einen angehefteten „Dart Run Build_Runner Build --delete-conflicting-outputs“-Pass aus, BEVOR Sie „Flutter Analysis“ ausführen (nie danach), grenzen Sie jeden Builder mit „Generate_for:“-Globs in einer „build.yaml“ pro Paket ein und treffen Sie eine bewusste Commit- vs. Gitignore-Entscheidung für `*.g.dart`/`*.freezed.dart`/`*.drift.dart` und sichern Sie es mit dem passenden CI-Gate (Freshness Diff, wenn es festgeschrieben wird, Codegen-First, wenn Gitignoriert wird), spiegeln Sie die Globs der generierten Datei in den Analysator UND schließen Sie die Abdeckung aus, heften Sie das SDK an und bearbeiten Sie die generierte Ausgabe niemals manuell. Zum Bearbeiten von Build.yaml-, Analysis_options.yaml-, pubspec.yaml-, .gitignore-, .gitattributes- oder CI-Workflows verwenden; Verkabelung drift_dev/freezed/json_serializable/riverpod_generator/gen-l10n/mockito codegen; Beheben von Fehlern wie „fehlende Teiledatei“, „undefinierte Klasse _$Foo“ oder „widersprüchliche Ausgaben“; Entscheiden, ob generierter Code festgeschrieben werden soll; oder Scoping-Builder, damit bei einer Bearbeitung nicht alles neu generiert wird.
