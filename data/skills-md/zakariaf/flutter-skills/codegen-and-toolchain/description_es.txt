Aplica una disciplina determinista de codegen build_runner: ejecute un `dart run build_runner build --delete-conflicting-outputs` anclado ANTES de `flutter analyse` (nunca después), cerca cada constructor con `generate_for:` globs en un `build.yaml` por paquete, tome una decisión deliberada de commit-vs-gitignore para `*.g.dart`/`*.freezed.dart`/`*.drift.dart` y respaldelo con la puerta CI correspondiente (diferencia de frescura si se confirma, codegen-first si se ignora git), refleje los globs del archivo generado en el analizador Y la cobertura excluya, fije el SDK y nunca edite manualmente la salida generada. Úselo al editar build.yaml, analisy_options.yaml, pubspec.yaml, .gitignore, .gitattributes o flujos de trabajo de CI; cableado drift_dev/freezed/json_serializable/riverpod_generator/ gen-l10n/mockito codegen; corregir errores de "archivo de pieza faltante", "clase indefinida _$Foo" o "salidas conflictivas"; decidir si enviar el código generado; o constructores de alcance para que una edición no regenere todo.
