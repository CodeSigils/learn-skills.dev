強制執行確定性的 build_runner codegen 規則：在 `flutteranalyze` 之前運行一個固定的 `dart run build_runner build --delete-conflicting-outputs` 傳遞（永遠不會之後），在每個包 `build.yalic 中使用 gene gene commit-vs-gitignore 決定`*.g.dart`/`*.freezed.dart`/`*.drift.dart` 並使用匹配的 CI 閘支援它（如果提交了新鮮度差異，如果 gitignored 則程式碼產生優先），將生成的檔案 glob 鏡像到分析器中，永遠不會被排除，固定在 SDK。在編輯 build.yaml、analysis_options.yaml、pubspec.yaml、.gitignore、.gitattributes 或 CI 工作流程時使用；接線drift_dev/freezed/json_serialized/riverpod_generator/gen-l10n/mockito codegen；「缺少類別的檔案」 _$Foo」或「輸出衝突」錯誤；決定是否提交產生的程式碼；或確定建構器的範圍，以便一次編輯不會重新產生所有內容。
