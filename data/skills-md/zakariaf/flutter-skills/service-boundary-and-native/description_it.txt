Collega ogni effetto collaterale e canale nativo come un'interfaccia iniettabile dietro un Provider che genera UnimplementedError finché la radice della composizione non lo sovrascrive, con un impl live per sapore, firme tipizzate con valore che restituiscono risultati digitati, falsi scritti a mano che onorano il contratto su mock, MethodChannel messo in quarantena in una directory lib/native/ e contratti multilingue con versioni modificati su entrambi i lati in un unico commit. Da utilizzare quando si aggiunge o si modifica un ShareService/AnalyticsService/RemoteConfigService o qualsiasi porta con effetto collaterale, si inserisce un Clock (pacchetto:clock) tramite clockProvider, un canale MethodChannel/piattaforma o un widget bridge nativo, un punto di ingresso di sapore (main_*.dart) o un provider per sapore, un contratto JSON/file condiviso con mirroring in Kotlin/Swift, sostituendo un DateTime.now() vagante o una chiamata SDK diretta all'interno di un widget/notificatore/repository o collegare un falso tramite ProviderScope(override:) nei test.
