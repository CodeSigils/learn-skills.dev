將每個副作用和本機通道連接為 Provider 後面的可注入接口，拋出 UnimplementedError 直到組合根覆蓋它，每種風格有一個實時 impl，返回類型化結果的值類型簽名，通過模擬手寫的合同兌現偽造，隔離到一個 lib/native/ 目錄的 MethodChannel 以及在一次提交中在雙方編輯版本的化跨語言。新增或變更 ShareService/AnalyticsService/RemoteConfigService 或任何副作用連接埠、透過 ClockProvider 注入時脈（包：時鐘）、MethodChannel/平台通道或本機小工具橋接器、風格入口點 (main_*.dart) 或每個風格提供者覆蓋、Kotlin/Swift 程式中 Date的共用檔案/JSON調用，或透過連接偽造時使用測試中的 ProviderScope(overrides:) 。
