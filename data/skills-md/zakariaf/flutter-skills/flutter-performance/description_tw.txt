強制 Flutter 運行時效能 - const 子樹、透過 ref.watch(select) 最小化重建範圍、惰性 ListView/GridView 建構器和條子、大小影像解碼 (cacheWidth/ResizeImage)、透過計算/隔離進行 UI 隔離的繁重工作、外科手術式 RepaintBoundary、處理所有內容以及透過計算/隔離進行 UI 隔離的繁重工作、外科手術式 RepaintBoundary、處理所有內容以及在地面測量設備上進行裝置測量。在優化 UI、診斷卡頓或丟幀、調整長列表或圖像、檢查重建/重繪範圍或任務提到 const、select、ListView.builder、cacheWidth、compute、RepaintBoundary、AnimatedBuilder、DevTools、光柵線程或 60/120fps 時使用。
