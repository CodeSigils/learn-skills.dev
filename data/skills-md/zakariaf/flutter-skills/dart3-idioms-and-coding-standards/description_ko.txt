각 선언이 획득하는 Dart 3 구성을 적용합니다. 봉인된 클래스 + `기본값:`/`case _:`가 없는 철저한 스위치, 세 가지 클래스 수정자(`sealed`/`final`/`추상 인터페이스 클래스`) 및 나머지는 건너뛰고, 레이어 내 튜플로만 기록하고, 사소할 때는 변경할 수 없는 값 유형(`final` 필드, `const` ctor, 값 동일성)을 수동으로 롤링하고 상용구가 지배할 때 `freezed`, 명시적 안정적인 ID, 전체 비-던지기 도메인 기능, 불법 상태 표시 불가, 확고한 방법/빌드/파일/중첩 복잡성 제한(다른 기술이 인용하는 단일 진실 테이블) — '늦음'/'!'/'동적' 정직 회피를 금지합니다. Dart 유형 또는 선언을 작성하거나 검토할 때 사용합니다. 클래스 vs 열거형 vs 레코드 vs typedef, 수동 롤링 vs `freezed`, `switch`/`if-case` 추가, `copyWith` 또는 `==`/`hashCode` 작성, ID 결정, 도메인 함수 전체 유지 또는 길이/중첩 제한 도달.
