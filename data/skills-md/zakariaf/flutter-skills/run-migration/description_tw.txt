執行只進的 Drift/SQLite 模式遷移儀式－離線優先應用程式中最危險的確定性操作：錯誤的遷移會默默地破壞裝置上不存在於其他地方的行。強制執行精確的排序順序：在打開資料庫之前拍攝預遷移檔案快照，將 schemaVersion 精確提高 1，編寫僅追加的逐步前進步驟（從不編輯已發布的步驟，從不編寫向下遷移），提交drift_dev make-migrations 架構快照，重新生成，然後通過覆蓋每個 from→to 路徑（包括多版本跳轉）的測試來證明它，wnwat-readn-v>內容測試、PRAGMAintegrity_check +foreign_key_check，以及恢復快照的強制遷移中期拋出。手動的、有副作用的工作流程。新增或變更 Drift 表、列、索引或 CHECK 時使用；碰撞 schemaVersion；編輯 onUpgrade/stepByStep；或撰寫遷移測試。
