強制執行類型化錯誤脊柱 - 一個手工滾動的密封 Result<T,F> 加上一個每個邊界密封的 Failure，攜帶穩定代碼和類型化參數（絕不是本地化字符串），返回而不是拋出；可恢復的故障是值，只有錯誤才會拋出。 Convert-at-boundary 使用 on 子句捕獲並在返回類型化失敗之前記錄原始錯誤+堆疊；調用站點完全切換，沒有預設值：；全域錯誤網（FlutterError.onError + PlatformDispatcher.onError，由 app-startup-and-bootstrap 安裝）路由到的分類，加上 Isolaterun.重新包裝；機制選擇（錯誤、斷言、異常、密封結果）和@useResult；以及永不丟失資料層（每個突變一個事務、去抖自動保存草稿、樂觀軟刪除/撤消）。在編寫 result.dart/failures.dart、try/catch 或密封開關預設值時使用：連接引導處理程序、儲存庫/服務/DAO 邊界或交易、草稿或軟體刪除/撤銷。
