强制执行 CustomPainter/Canvas 规则 - 视图/画家/场景与哑画家馈送一个不可变的场景值类型，shouldRepaint 作为单个值比较与动画控制器作为重画动画路径严格分开，由画家和命中测试器读取的一个共享仿射变换（toCanvas/toLogical 精确反转，从不重新导出比例），几何命中测试（整数晶格或光栅化区域 ID 缓冲区，从不） Path.contains）、零分配 Paint()、纯粹手势翻译器向通知程序发出键入的命令（永远不会在处理程序中发生变化）、ExcludeSemantics + 同级语义语言显示具有冗余非颜色编码的值、测量的 TextPainter 拟合、第一方 RoundedSuperellipseBorder、物理像素细线和仅方向几何图形。在编写或检查 CustomPainter/CustomPaint、画布上的手势、点击/拖动命中测试、画布动画、测量的文本拟合或自定义绘制像素的语义时使用。
