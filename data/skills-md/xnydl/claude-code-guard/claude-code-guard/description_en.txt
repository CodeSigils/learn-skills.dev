Configure Claude Code anti‑ban for the client’s machine: first detect the actual proxy (ladder) and operating system (macOS/Windows) running on the user’s computer, then after the client selects a fixed node, apply the timezone, local proxy binding, Hook, fingerprint browser, and clear any residual credentials and machine ID from old accounts. Trigger keywords: Claude anti‑ban, Claude Code blocked, instant block, Taiwan node, Windows, Clash, v2rayN, Camoufox, fingerprint browser, clear credentials, chain key, machine ID, /claude-code-guard.
