Configure Claude Code anti‑ban for the client’s machine: first detect the actual proxy (ladder) and operating system (macOS / Windows) running on the target computer, then, after the client selects a fixed node, apply the time zone, local proxy binding, Hook, fingerprint browser, and clean up leftover credentials and machineID from old accounts. Triggers: Claude anti‑ban, Claude Code being blocked, instant ban, Taiwan node, Windows, Clash, v2rayN, Camoufox, fingerprint browser, credential cleanup, chain key, machineID, /claude-code-guard.
