Configura l'anti-ban di Claude Code per la macchina del cliente: rileva prima il proxy effettivo (ladder) e il sistema operativo (macOS / Windows) in esecuzione sul computer di destinazione, quindi, dopo che il client ha selezionato un nodo fisso, applica il fuso orario, l'associazione del proxy locale, l'Hook, il browser delle impronte digitali e ripulisci le credenziali rimanenti e l'ID macchina dai vecchi account. Trigger: Claude anti-ban, codice Claude bloccato, ban istantaneo, nodo Taiwan, Windows, Clash, v2rayN, Camoufox, browser con impronte digitali, pulizia credenziali, chiave a catena, machineID, /claude-code-guard.
