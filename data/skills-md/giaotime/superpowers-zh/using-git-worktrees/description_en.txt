Use when you need to start feature development isolated from the current workspace or before executing an implementation plan — create an isolated git worktree with intelligent directory selection and secure validation.
