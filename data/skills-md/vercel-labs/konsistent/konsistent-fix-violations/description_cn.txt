运行一致的 CLI，检查所有报告的违规行为，并在代码库中修复它们。当用户想要“修复一致违规”、“解决一致错误”、“清理一致诊断”、“进行一致传递”、“解决结构约定违规”或要求运行一致并根据结果采取行动时使用。该技能是交互式的：当许多违规行为源于同一规则（规则与代码问题）时，它需要明确的用户决策，修改任何代码之前的明确用户确认，以及对重要违规行为的分类步骤。对于编辑“konsistent.json”，此技能遵循“konsistent-config”技能。
