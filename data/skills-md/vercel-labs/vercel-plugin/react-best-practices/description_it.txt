Revisore delle best practice di React per i file TSX. Si attiva dopo la modifica di più componenti TSX per eseguire un elenco di controllo della qualità condensato che copre la struttura dei componenti, l'utilizzo degli hook, l'accessibilità, le prestazioni e i pattern TypeScript.
