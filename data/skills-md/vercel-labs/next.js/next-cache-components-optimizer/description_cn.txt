通过在缓存组件/PPR 下、初始加载（硬导航）和客户端导航（软导航）上设置代理循环，将 Next.js 路由驱动到即时导航。将目标编码为失败的 @next/playwright instant() e2e 并将其设置为绿色，一次一条经过验证的路线；交付的测试可以防止回归。当被要求使路由的导航即时（其静态 shell 立即提交）、修复静态 shell 未预渲染/服务/预取的路由、增加路由的静态 shell 或修复其缓慢的第一次绘制、诊断哪个 Suspense 边界将路由保留在其静态 shell 之外或编写 instant() e2e 防护时使用。需要带有cacheComponents的Next.js 16.3+；如果较旧，则指示升级。
