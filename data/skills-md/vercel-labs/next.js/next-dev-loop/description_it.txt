Verifica il comportamento di runtime di Next.js dopo aver modificato il codice dell'app. Usa questa abilità per confermare che una modifica funziona effettivamente in un'app in esecuzione, non solo per la compilazione o il controllo del tipo. Combina /_next/mcp (vista di Next.js) con agent-browser (vista del browser). Richiede un `next dev` in esecuzione.
