مصادقة + البريد الإلكتروني للمعاملات + تخزين الأصول على مكدس Mongo، و Better Auth (رابط سحري، ورمز مميز مجزأ في حالة عدم النشاط) على Prisma + MongoDB، وAWS SES للتسليم، وAWS S3 + CloudFront للأصول، وبيئة الخادم التي تم التحقق من صحتها بواسطة zod، وعقد احتياطي واحد لـ IAM لعملاء AWS. استخدمه عند لمس تدفقات تسجيل الدخول/التسجيل، ومسار المصادقة الشامل، وتهيئة `auth`، واسترجاع الجلسة، ورسائل البريد الإلكتروني للارتباط السحري، ونماذج Prisma ``المستخدم`/`الجلسة`/`الحساب`/`التحقق`، وحقول المستخدم الإضافية (`الدور`، `اللغة المحلية المفضلة`، وما إلى ذلك)، أو مرسلي البريد الإلكتروني SES، أو تحميلات S3، أو عمليات إعادة التوجيه بعد المصادقة، أو التوسيع مخطط المصادقة.
