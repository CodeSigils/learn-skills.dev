数据库支持的内容 + i18n 堆栈、MongoDB 上的 `ContentPage` / `ContentString` Prisma 模型、`StringRole` 枚举、`Languages` 嵌入类型、在 TanStack 查询中永久缓存的公共 `getPageStrings` 服务器 fn、通过 `(fallback、role、section)` 解析的 `useTranslation(pageSlug)` 钩子、存储驱动的活动语言环境（Redux / Zustand / 等）、选择加入的语言环境切换器，作为事实来源的“种子翻译”脚本，以及用于实时补丁的门控管理编辑器。在页面上添加/编辑副本、将“useTranslation”连接到新路径、添加新内容页面、启用新语言、更改“StringRole”、触摸“语言”、保留每个用户区域设置首选项或处理翻译管理 UI 时使用。
