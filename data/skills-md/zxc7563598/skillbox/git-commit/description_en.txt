Standardize Git commit messages. Read the git diff to understand the intent of code changes, generate a commit message that conforms to the Conventional Commits specification (English type + Chinese description), display it to the user for confirmation, and then execute git commit. Use this when the user wants to commit code, generate a commit message, or standardize a commit message. Chinese trigger words: submit, submit code, commit, commit message, git commit, generate commit.
