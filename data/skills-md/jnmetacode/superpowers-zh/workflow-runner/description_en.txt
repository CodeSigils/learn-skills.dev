Run the agency-orchestrator YAML workflow directly in Claude Code / OpenClaw / Cursor—no API key needed; use the current session’s LLM as the execution engine. Triggered when the user supplies a .yaml workflow file or requests multi-agent collaboration to complete a task.
