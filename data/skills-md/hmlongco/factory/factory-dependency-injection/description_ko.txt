Swift와 SwiftUI용 컨테이너 기반 의존성 주입 시스템인 Factory에 대한 심층 가이드입니다. 사용자가 Factory, FactoryKit 또는 FactoryTesting을 사용하는 Swift 코드를 작성, 읽기, 리팩터링, 디버깅 또는 테스트할 때마다 이 기술을 활용하세요 — 여기에는 Container에 대한 등록, `@Injected`/`@LazyInjected`/`@WeakLazyInjected`/`@DynamicInjected`/`@InjectedObject`/`@InjectedObservable`/`@InjectedContainer`/`@InjectedType` 프로퍼티 래퍼, `Factory<T>` 및 `ParameterFactory<P, T>`, 스코프(`.unique`, `.cached`, `.shared`, `.singleton`, `.graph`), 컨텍스트 수정자(`.onTest`, `.onPreview`, `.onDebug`, `.onArg`, `.onSimulator`, `.onDevice`), 전역 `dependency(\.path)` 함수, `AutoRegistering`, 사용자 정의 컨테이너, `.container` Swift Testing 트레이트, 그리고 `promised()`를 이용한 모듈 간 와이어링이 포함됩니다. `Factory<`, `Container.shared`, `extension Container`, `@Injected(\.`, `import FactoryKit`를 보거나 사용자가 “Factory DI”, “FactoryKit”을 언급하거나 Resolver, Swinject, Needle와 비교할 때 트리거하세요.
