CreatiBI CLI shared foundation: application configuration initialization, authentication login (auth login), and identity viewing (auth whoami). Triggered when users need first-time configuration, use login authorization, encounter insufficient permissions, or use cbi-cli for the first time.
