Genera, ispeziona, esegui prove di prova e convalida staticamente il semplice `.gcode` FDM da file mesh 3D orchestrando reali CLI di slicer. Da utilizzare quando Codex deve suddividere `.stl`, `.obj`, `.3mf`, `.ply`, `.glb` o `.gltf` nel codice G con profilo della stampante, scoprire i backend dello slicer locale, verificare se una mesh è pronta per lo slice o convalidare il codice G generato prima di qualsiasi trasferimento specifico della stampante.
