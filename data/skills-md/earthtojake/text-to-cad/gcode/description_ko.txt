실제 슬라이서 CLI를 조정하여 3D 메시 파일에서 일반 FDM '.gcode'를 생성, 검사, 테스트 실행 및 정적으로 검증합니다. Codex가 `.stl`, `.obj`, 슬라이스되지 않은 `.3mf`, `.ply`, `.glb` 또는 `.gltf`를 프린터 프로파일링된 G 코드로 슬라이스하고, 로컬 슬라이서 백엔드를 검색하고, 메시가 슬라이스 준비되었는지 검사하거나, 프린터별 핸드오프 전에 생성된 G 코드를 검증해야 하는 경우에 사용합니다.
