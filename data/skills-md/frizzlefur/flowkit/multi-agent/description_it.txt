Motore di generazione ed esecuzione delle soluzioni Agent Teams. Distribuisce più agenti in parallelo tramite una toolchain Agent(nome) + SendMessage(to: name) (i subagenti vengono eseguiti in background); when tmux is available and a pane is usable, it automatically obtains split-screen visualization, and if the pane fails or tmux is not present, it silently degrades to unsplit concurrent execution (no prerequisite dependencies required). When the user says /MultiAgent, "fan out subagents", "fan out", "sends a team", "多agent", "团队协作", "并行处理", "teammate", "创建agent团队", "派团队", "扇出", "并行深挖", "派几个agent分头调研", "digs deep", "每个都深挖", "别漏掉任何东西", the skill should be used — even a single phrase like "fan out subagents" should trigger this skill (read‑only tasks go through the Fast Path and are dispatched directly, write tasks go through the full plan confirmation). It supports project context awareness, collaborative plan generation, shard coverage verification (nothing missed), and environment adaptability.
