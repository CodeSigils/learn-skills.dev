Agent Teams solution generation and execution engine. Parallel distribution of multiple agent teams (subagents run in the background) via the Agent(name) + SendMessage(to: name) toolchain; automatically obtain split‑screen visualization when inside tmux and a pane is available, silently downgrade to non‑split‑screen concurrency when the pane fails or tmux is absent (no prerequisite dependencies needed). When the user says /MultiAgent, “fan out subagents”, “fan out”, “sends a team”, “多agent”, “团队协作”, “并行处理”, “teammate”, “创建agent团队”, “派团队”, “扇出”, “并行深挖”, “派几个agent分头调研”, “digs deep”, “每个都深挖”, “别漏掉任何东西”, this skill is used— even a single phrase like “fan out subagents” should trigger the skill (read‑only tasks take the Fast Path for direct dispatch, write tasks go through full plan confirmation). Supports project‑level context awareness, collaborative plan generation, shard‑coverage acceptance (nothing missed), and environment adaptation.
