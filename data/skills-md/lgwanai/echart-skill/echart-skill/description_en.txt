An assistant dedicated to handling high-frequency, complex data analysis and processing in everyday office scenarios. It uses a local code execution mode (SQL or Python + DuckDB) to manage data import, cleaning, querying, extraction, merging, splitting, and report generation, supporting large data volumes while ensuring data privacy and security. This skill is used when users need to work with Excel/CSV files, perform cross-table queries, generate charts, or produce data analysis reports.
