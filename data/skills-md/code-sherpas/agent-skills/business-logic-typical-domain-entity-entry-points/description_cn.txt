当代理必须决定实施哪些入口点时，围绕域实体类型提升一组标准的业务逻辑入口点。在设计、创建、修改、审查或完成特定域实体的业务逻辑入口点时使用。优先选择基本实体入口点，例如创建、更新、按 id 查找、按 id 搜索和删除；使用显式标记为有限状态更改的入口点，例如枚举或布尔状态转换；使用显式分配或添加域实体之间一对多或多对多关联的入口点；当业务操作需要关于当前是否允许某个操作的布尔答案时，使用显式检查是否可以入口点。
