Obtenga la próxima propuesta de experimento de ML **leyendo el resumen de auditoría** en `scratch/audit/<stem>/audit.md` (producido por `audit-ml-pipeline` en § 4 record-outcome). Para cada fila en el `## Resumen de comprobaciones` del resumen cuya `gravedad` sea `problema` o `sugerencia`, siga la `documentation_url` de la fila para redactar una fila del Backlog cuyo `Elemento` sea la mitigación que recomiendan los documentos. El `## Resumen de métricas` proporciona contexto para el párrafo de resumen humano, pero no controla las filas del trabajo pendiente por sí solo. Devuelve las filas enriquecidas del Backlog + un resumen de un párrafo a `iterate-ml-experiment`, que escribe las filas en `JOURNAL.md` y representa el menú de abastecimiento para que el usuario pueda promocionar una fila `B<N>`. Se detiene en "Trabajo pendiente enriquecido, resumen devuelto"; nunca escribe una nota de diseño por experimento, nunca elige el hallazgo "ganador": el usuario elige mediante `B<N>`.
DISPARAR cuando: `iterate-ml-experiment` está eligiendo una estrategia de abastecimiento y el usuario elige `skore` del menú; el usuario dice "extraer el informe", "¿qué ve skore?", "completar el trabajo pendiente del diagnóstico"; el experimento anterior ha finalizado y el usuario quiere que el informe se convierta en elementos del trabajo pendiente procesables.
SALTAR cuando: el experimento anterior aún no se ha ejecutado (no hay un resumen de auditoría en el disco); el usuario tiene una idea de modelado concreta (use "iterar desde el usuario"); la tarea es la *mecánica* de ejecutar/abrir un informe - ruta a `evaluate-ml-pipeline`; el usuario desea una lectura narrativa de una sección específica del informe (ruta a "evaluate-ml-pipeline").
CÓMO UTILIZAR: lea el resumen `scratch/audit/<stem>/audit.md` existente como texto; NO vuelva a abrir el Proyecto skore, NO llame a los descriptores de acceso `report.*`. Para cada fila en la sección `## Resumen de comprobaciones` cuya gravedad sea `problema` o `consejo`, siga la `documentation_url` (a través de WebFetch) y redacte una fila del trabajo pendiente citando `audit:<stem>:checks.<code>`. Deduplicación de filas que ya están en el trabajo pendiente de `JOURNAL.md` por cita de fuente. Devuelve las filas candidatas + un resumen humano de un párrafo. La habilidad principal escribe las filas en `JOURNAL.md` y vuelve a mostrar el menú de abastecimiento.
