Trouvez la prochaine proposition d'expérience ML en **lisant le résumé d'audit** à l'adresse `scratch/audit/<stem>/audit.md` (produit par `audit-ml-pipeline` au § 4 record-outcome). Pour chaque ligne du « ## Checks summary » du résumé dont la « gravité » est « problème » ou « astuce », suivez l'« url_documentation » de la ligne pour rédiger une ligne de backlog dont « l'élément » est l'atténuation recommandée par la documentation. Le « Résumé des métriques ## » fournit un contexte pour le paragraphe de résumé humain, mais ne génère pas à lui seul les lignes du Backlog. Renvoie les lignes enrichies du Backlog + un résumé d'un paragraphe à `iterate-ml-experiment`, qui écrit les lignes dans `JOURNAL.md` et re-présente le menu de sourcing afin que l'utilisateur puisse promouvoir une ligne `B<N>`. S'arrête à « Backlog enrichi, synthèse renvoyée » ; n'écrit jamais de note de conception par expérience, ne sélectionne jamais le résultat « gagnant » — l'utilisateur choisit via « B<N> ».
DÉCLENCHEMENT lorsque : `iterate-ml-experiment` choisit une stratégie de sourcing et que l'utilisateur choisit `skore` dans le menu ; l'utilisateur dit « extraire le rapport », « que voit skore ? », « combler le retard du diagnostic » ; l'expérience précédente est terminée et l'utilisateur souhaite que le rapport soit converti en éléments de backlog exploitables.
SKIP quand : l'expérience précédente n'a pas encore été exécutée (pas de résumé d'audit sur le disque) ; l'utilisateur a une idée de modélisation concrète (utilisez « iterate-from-user » ); la tâche est la *mécanique* d'exécution/d'ouverture d'un rapport — route vers `evaluate-ml-pipeline` ; l'utilisateur souhaite une lecture narrative d'une section spécifique du rapport (route vers `evaluate-ml-pipeline`).
COMMENT UTILISER : lisez le résumé `scratch/audit/<stem>/audit.md` existant sous forme de texte — ne rouvrez PAS le projet skore, n'appelez PAS les accesseurs `report.*`. Pour chaque ligne de la section `## Checks summary` dont la gravité est `issue` ou `tip`, suivez l'`documentation_url` (via WebFetch) et rédigez une ligne du Backlog en citant `audit:<stem>:checks.<code>`. Déduplication des lignes déjà dans `JOURNAL.md` Backlog par citation de source. Renvoie les lignes candidates + un résumé humain d'un paragraphe. La compétence parent écrit les lignes dans « JOURNAL.md » et affiche à nouveau le menu de sourcing.
