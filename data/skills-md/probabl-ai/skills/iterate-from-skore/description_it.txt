Ottieni la prossima proposta di esperimento ML **leggendo l'audit digest** in `scratch/audit/<stem>/audit.md` (prodotto da `audit-ml-pipeline` al § 4 record-outcome). Per ogni riga nel "## Checks summary" del digest la cui "severity" è "issue" o "tip", seguire il "documentation_url" della riga per creare una riga Backlog il cui "Item" è la mitigazione consigliata dai documenti. Il "## riepilogo delle metriche" fornisce il contesto per il paragrafo di riepilogo umano ma non gestisce da solo le righe del backlog. Restituisce le righe del backlog arricchito + un riepilogo di un paragrafo a "iterate-ml-experiment", che scrive le righe in "JOURNAL.md" e ripresenta il menu di approvvigionamento in modo che l'utente possa promuovere una riga "B<N>". Si ferma a "Archivio arretrato arricchito, riepilogo restituito"; non scrive mai una nota di progettazione per esperimento, non sceglie mai il risultato "vincente": l'utente sceglie tramite "B<N>".
TRIGGER quando: `iterate-ml-experiment` sta scegliendo una strategia di sourcing e l'utente seleziona `skore` dal menu; l'utente dice "estrai il rapporto", "cosa vede skore?", "riempi il backlog dalla diagnostica"; l'esperimento precedente è terminato e l'utente desidera che il report venga convertito in elementi del backlog utilizzabili.
SKIP quando: l'esperimento precedente non è stato ancora eseguito (nessun audit digest su disco); l'utente ha un'idea concreta di modellazione (usa `iterate-from-user`); il compito è la *meccanica* di esecuzione/apertura di un report: instradamento a `evaluate-ml-pipeline`; l'utente desidera una lettura narrativa di una sezione specifica del report (percorso verso `evaluate-ml-pipeline`).
COME USARE: leggi il digest `scratch/audit/<stem>/audit.md` esistente come testo — NON riaprire il progetto skore, NON chiamare gli accessori `report.*`. Per ogni riga nella sezione "## Riepilogo controlli" la cui gravità è "problema" o "tip", seguire "documentation_url" (tramite WebFetch) e creare una riga del backlog citando "audit:<stem>:checks.<code>". Deduplica rispetto alle righe già presenti nel backlog "JOURNAL.md" in base alla citazione della fonte. Restituisci le righe candidate + un riepilogo umano di un paragrafo. La competenza principale scrive le righe in "JOURNAL.md" e mostra nuovamente il menu di approvvigionamento.
