モデルを設計する前にデータを理解します。共有インプロセス ランナーを介して `data/eda.py` (jupytext `# %%` スクリプト) を配置して実行し、ストリーミングされたダイジェストを読み取り、永続化された `data/eda.md` レポート (およびリンクされた `data/eda_<table>.html` スクラブ `TableReport` ページ) と `## Data Understanding (EDA)` セクションを書き込みます。 「ジャーナル/JOURNAL.md」。重要なのは、後の学習器/スプリッター/メトリクスの決定を正当化するデータセットの事実 (形状、dtype、欠損、カーディナリティ、ターゲット バランス/スキュー、日時/グループ構造、特徴の関連付け) を明らかにすることで、ユーザーがモデリングの選択が「なぜ」行われたのかを理解できるようにすることです。データフレームの概要と共有ランナーの `audit-ml-pipeline/scripts/run_cells.py` には `skrub.TableReport` を使用します。 「EDA が実行され、`data/eda.md` + HTML が書き込まれ、JOURNAL EDA セクションが更新されました。」で停止します。モデルを設計したり、`src/<pkg>/` を編集したり、ユーザーの生データ ファイルを変更したりすることはありません。
TRIGGER — 次のいずれか - `iterate-ml-experiment` § 0 ブートストラップ、ベースライン設計の前
  注 — ここで G-EDA ゲートが起動します (実行/スキップ)。
- ユーザーは、「データの探索」、「EDA の実行」、「プロファイリング」を要求します。
  データセット」、「データはどのようなものか」、「データを理解する」。
- 新しいデータ ソースまたは変更されたデータ ソースについては、事前に (再) 理解する必要があります。
  次の実験。

次の場合はスキップします: ワークスペースがまだスキャフォールド/ブートストラップされていない — `iterate-ml-experiment` § 0 がブートストラップ順序付けを所有し、G-EDA ステップでここにディスパッチされます。スキャフォールディングの前にスタンドアロンで実行しないでください (`iterate-ml-experiment` / `organize-ml-workspace` へのルート)。まだ調査するデータがありません。ユーザーは生のデータセット (`audit-ml-pipeline`) ではなく、終了した実行のスコア レポートを検査したいと考えています。ユーザーはデータの理解が終わっており、パイプライン/評価メカニズム (`build-ml-pipeline` / `evaluate-ml-pipeline`) を望んでいます。純粋なシンボル検索 (`python-api`); EDA はすでに記録されており (`data/eda.md` + JOURNAL EDA セクションが存在します)、ユーザーはそれを更新するように求めていません。
使用方法: 検出ステップを実行し (`data/eda.md` + JOURNAL EDA セクションはすでに存在しますか?)、プリフライト チェックリストを表示テキストとして出力し、停止条件を読み取り、次に `templates/eda.py` から `data/eda.py` を配置し、共有ランナー経由で実行し、ダイジェストを読み取り、`data/eda.md` + JOURNAL EDA セクションを作成します。 skrub/pandas/polars シンボルは、メモリからではなく、常に「python-api」経由で解決します。
