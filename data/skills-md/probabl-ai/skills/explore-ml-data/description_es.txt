Posee comprensión de los datos ANTES de diseñar cualquier modelo. Coloca y ejecuta `data/eda.py` (un script jupytext `# %%`) a través del ejecutor compartido en proceso, lee el resumen transmitido, luego escribe un informe `data/eda.md` persistente (más las páginas vinculadas `data/eda_<table>.html` skrub `TableReport`) y la sección `## Comprensión de datos (EDA)` de `journal/JOURNAL.md`. El objetivo es sacar a la luz los hechos del conjunto de datos (forma, tipos de datos, faltantes, cardinalidad, equilibrio/desvío objetivo, estructura de fecha y hora/grupo, asociaciones de características) que JUSTIFICAN las decisiones posteriores del alumno/divisor/métrica, para que el usuario comprenda *por qué* se toman las decisiones de modelado. Utiliza `skrub.TableReport` para descripciones generales de marcos de datos y el corredor compartido `audit-ml-pipeline/scripts/run_cells.py`. Se detiene en "EDA ejecutado, `data/eda.md` + HTML escrito, sección JOURNAL EDA actualizada". Nunca diseña el modelo, nunca edita `src/<pkg>/`, nunca modifica los archivos de datos sin procesar del usuario.
DISPARADOR - cualquiera de: - `iterate-ml-experiment` § 0 bootstrap, ANTES del diseño de referencia
  nota: la puerta G-EDA se activa aquí (ejecutar/saltar).
- El usuario solicita "explorar los datos", "hacer un EDA", "perfilar el
  conjunto de datos", "cómo se ven los datos", "comprender los datos".
- Una fuente de datos nueva o modificada necesita (re)comprensión antes de
  próximo experimento.

SALTAR cuando: el espacio de trabajo aún no está estructurado/arrancado: `iterate-ml-experiment` § 0 posee el orden de arranque y se enviará aquí en el paso G-EDA; no ejecutar de forma independiente antes del scaffolding (ruta a `iterate-ml-experiment`/`organize-ml-workspace`); aún no hay datos para explorar; el usuario desea inspeccionar el informe skore de una ejecución finalizada en lugar del conjunto de datos sin procesar (`audit-ml-pipeline`); el usuario ya no comprende los datos y desea mecanismos de canalización/evaluación (`build-ml-pipeline`/`evaluate-ml-pipeline`); una búsqueda de símbolos puros (`python-api`); EDA ya está registrado (`data/eda.md` + la sección JOURNAL EDA existe) y el usuario no solicita actualizarlo.
CÓMO USAR: ejecute el paso de Detección (¿ya existe `data/eda.md` + la sección JOURNAL EDA?), emita la lista de verificación previa al vuelo como texto visible, lea las condiciones de detención, luego coloque `data/eda.py` de `templates/eda.py`, ejecútelo a través del corredor compartido, lea el resumen y cree `data/eda.md` + la sección JOURNAL EDA. Resuelva siempre los símbolos skrub/pandas/polares a través de `python-api`, nunca desde la memoria.
