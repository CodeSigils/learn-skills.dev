Владеет пониманием данных ДО того, как будет разработана любая модель. Размещает и выполняет `data/eda.py` (сценарий jupytext `# %%`) через общий внутрипроцессный исполнитель, считывает потоковый дайджест, затем записывает сохраняемый отчет `data/eda.md` (плюс связанные страницы `data/eda_<table>.html` скраб `TableReport`) и раздел `## Понимание данных (EDA)` в `journal/JOURNAL.md`. Цель состоит в том, чтобы выявить факты набора данных — форму, dtypes, отсутствие, мощность, целевой баланс / перекос, структуру даты и времени / группы, ассоциации объектов — которые ОБОСНОВЫВАЮТ последующие решения учащегося / разделителя / метрики, чтобы пользователь понимал *почему* выбор моделирования сделан. Использует Skrub.TableReport для обзоров фреймов данных и общий бегун Audit-ml-pipeline/scripts/run_cells.py. Останавливается на «EDA выполнено, `data/eda.md` + HTML написан, раздел JOURNAL EDA обновлен». Никогда не разрабатывает модель, никогда не редактирует `src/<pkg>/`, никогда не изменяет файлы необработанных данных пользователя.
ТРИГГЕР — любой из: — `iterate-ml-experiment` § 0 начальной загрузки, ДО базового проекта
  обратите внимание — здесь срабатывают ворота G-EDA (бегите/пропускайте).
- Пользователь просит «изучить данные», «сделать EDA», «профилировать данные».
  набор данных», «как выглядят данные», «понимать данные».
- Новый или измененный источник данных требует (пере)осмысления, прежде чем
  следующий эксперимент.

ПРОПУСКАТЬ, когда: рабочее пространство еще не создано или не загружено — `iterate-ml-experiment` § 0 владеет порядком начальной загрузки и будет отправляться сюда на этапе G-EDA; не запускайте автономно перед созданием шаблонов (перейдите к `iterate-ml-experiment`/`organize-ml-workspace`); пока нет данных для изучения; пользователь хочет проверить отчет о результатах завершенного запуска, а не необработанный набор данных (`audit-ml-pipeline`); пользователь не понимает данных и хочет использовать механику конвейера/оценки («build-ml-pipeline» / «evaluate-ml-pipeline»); чистый поиск символов (`python-api`); EDA уже записан (`data/eda.md` + раздел JOURNAL EDA существует), и пользователь не запрашивает его обновление.
КАК ИСПОЛЬЗОВАТЬ: запустите этап обнаружения (существует ли `data/eda.md` + раздел JOURNAL EDA?), выведите предполетный контрольный список в виде видимого текста, прочитайте условия остановки, затем поместите `data/eda.py` из `templates/eda.py`, выполните его через общий исполнитель, прочитайте дайджест и создайте `data/eda.md` + раздел JOURNAL EDA. Всегда разрешайте символы skrub/pandas/polars через `python-api`, а не из памяти.
