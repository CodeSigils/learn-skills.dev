Possède la compréhension des données AVANT la conception de tout modèle. Place et exécute `data/eda.py` (un script jupytext `# %%`) via le coureur de processus partagé, lit le résumé en streaming, puis écrit un rapport `data/eda.md` persistant (plus les pages liées `data/eda_<table>.html` skrub `TableReport`) et la section `## Data Understanding (EDA)` de `journal/JOURNAL.md`. Le but est de faire apparaître les faits de l'ensemble de données - forme, types, absences, cardinalité, équilibre / biais cible, structure date/heure / groupe, associations de fonctionnalités - qui JUSTIFIENT les décisions ultérieures de l'apprenant / du séparateur / de la métrique, afin que l'utilisateur comprenne *pourquoi* les choix de modélisation sont faits. Utilise `skrub.TableReport` pour les aperçus des trames de données et le coureur partagé `audit-ml-pipeline/scripts/run_cells.py`. S'arrête à "EDA exécuté, `data/eda.md` + HTML écrit, section JOURNAL EDA mise à jour." Ne conçoit jamais le modèle, n'édite jamais `src/<pkg>/`, ne modifie jamais les fichiers de données brutes de l'utilisateur.
TRIGGER — l'un des : - `iterate-ml-experiment` § 0 bootstrap, AVANT la conception de base
  remarque — la porte G-EDA se déclenche ici (exécuter/sauter).
- L'utilisateur demande « d'explorer les données », de « faire une EDA », de « profiler les
  ensemble de données", "à quoi ressemblent les données", "comprendre les données".
- Une source de données nouvelle ou modifiée nécessite une (re)compréhension avant
  prochaine expérience.

SAUTER quand : l'espace de travail n'est pas encore échafaudé/amorcé — `iterate-ml-experiment` § 0 possède l'ordre d'amorçage et sera distribué ici à l'étape G-EDA ; ne pas exécuter de manière autonome avant l'échafaudage (route vers `iterate-ml-experiment` / `organize-ml-workspace`) ; il n'y a pas encore de données à explorer ; l'utilisateur souhaite inspecter le rapport skore d'une exécution terminée plutôt que l'ensemble de données brutes (`audit-ml-pipeline`) ; l'utilisateur a dépassé la compréhension des données et souhaite des mécanismes de pipeline/évaluation (`build-ml-pipeline` / `evaluate-ml-pipeline`) ; une recherche de symboles purs (`python-api`) ; EDA est déjà enregistré (`data/eda.md` + la section JOURNAL EDA existent) et l'utilisateur ne demande pas de l'actualiser.
COMMENT UTILISER : exécutez l'étape de détection (est-ce que `data/eda.md` + la section JOURNAL EDA existent déjà ?), émettez la liste de contrôle de pré-vol sous forme de texte visible, lisez les conditions d'arrêt, puis placez `data/eda.py` à partir de `templates/eda.py`, exécutez-le via le coureur partagé, lisez le résumé et créez `data/eda.md` + la section JOURNAL EDA. Résolvez toujours les symboles skrub / pandas / polaires via `python-api`, jamais de mémoire.
