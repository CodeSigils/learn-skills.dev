Possiede la comprensione dei dati PRIMA che qualsiasi modello sia progettato. Inserisce ed esegue `data/eda.py` (uno script jupytext `# %%`) tramite il runner in-process condiviso, legge il digest trasmesso in streaming, quindi scrive un report `data/eda.md` persistente (oltre alle pagine `data/eda_<table>.html` skrub `TableReport` collegate) e la sezione `## Data understanding (EDA)` di `journal/JOURNAL.md`. Il punto è far emergere i fatti del set di dati - forma, dtypes, mancanze, cardinalità, equilibrio / distorsione target, data/ora / struttura del gruppo, associazioni di caratteristiche - che GIUSTIFICANO le successive decisioni dello studente / splitter / metrica, in modo che l'utente capisca *perché* vengono fatte le scelte di modellazione. Utilizza `skrub.TableReport` per le panoramiche dei dataframe e il runner condiviso `audit-ml-pipeline/scripts/run_cells.py`. Si ferma a "EDA eseguito, `data/eda.md` + HTML scritto, sezione JOURNAL EDA aggiornata." Non progetta mai il modello, non modifica mai `src/<pkg>/`, non modifica mai i file di dati grezzi dell'utente.
TRIGGER: uno qualsiasi tra: - `iterate-ml-experiment` § 0 bootstrap, PRIMA del progetto di base
  nota: qui il gate G-EDA si attiva (esegui/salta).
- L'utente chiede di "esplorare i dati", "fare un EDA", "profilare il
  dataset", "che aspetto hanno i dati", "comprendere i dati".
- Una fonte di dati nuova o modificata necessita di (ri-)comprensione prima del
  prossimo esperimento.

SALTA quando: l'area di lavoro non è ancora stata sottoposta a scaffolding/bootstrap — `iterate-ml-experiment` § 0 possiede l'ordinamento bootstrap e verrà inviato qui al passaggio G-EDA; non eseguire in modalità autonoma prima dello scaffolding (percorso verso `iterate-ml-experiment` / `organize-ml-workspace`); non ci sono ancora dati da esplorare; l'utente desidera controllare il report del punteggio di una corsa terminata piuttosto che il set di dati grezzi (`audit-ml-pipeline`); l'utente non è in grado di comprendere i dati e desidera meccanismi di pipeline/valutazione (`build-ml-pipeline` / `evaluate-ml-pipeline`); una ricerca di simboli puri (`python-api`); EDA è già registrato (esiste `data/eda.md` + la sezione JOURNAL EDA) e l'utente non chiede di aggiornarlo.
COME USARE: esegui la fase di rilevamento (esiste già `data/eda.md` + la sezione JOURNAL EDA?), emetti la checklist pre-flight come testo visibile, leggi le condizioni di stop, quindi posiziona `data/eda.py` da `templates/eda.py`, eseguila tramite il runner condiviso, leggi il digest e crea `data/eda.md` + la sezione JOURNAL EDA. Risolvi sempre i simboli skrub/panda/polari tramite `python-api`, mai dalla memoria.
