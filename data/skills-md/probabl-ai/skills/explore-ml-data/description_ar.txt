يمتلك فهمًا للبيانات قبل تصميم أي نموذج. يضع وينفذ `data/eda.py` (نص jupytext `# %%` النصي) عبر المشغل المشترك أثناء العملية، ويقرأ الملخص المتدفق، ثم يكتب تقرير `data/eda.md` المستمر (بالإضافة إلى صفحات skrub `data/eda_<table>.html` المرتبطة `TableReport`) وقسم `## فهم البيانات (EDA)` من "مجلة/JOURNAL.md". الهدف هو إظهار حقائق مجموعة البيانات - الشكل، وأنواع d، والاختفاء، والأصل، والتوازن/الانحراف المستهدف، وبنية التاريخ/المجموعة، وارتباطات الميزات - التي تبرر قرارات المتعلم/المقسم/المقياس اللاحقة، بحيث يفهم المستخدم *لماذا* يتم اتخاذ خيارات النمذجة. يستخدم `skrub.TableReport` للحصول على نظرة عامة على إطار البيانات والمشغل المشترك `audit-ml-pipeline/scripts/run_cells.py`. يتوقف عند "تم تنفيذ EDA، وكتابة `data/eda.md` + HTML، وتحديث قسم JOURNAL EDA." لا يصمم النموذج مطلقًا، ولا يعدل أبدًا `src/<pkg>/`، ولا يعدل أبدًا ملفات البيانات الأولية للمستخدم.
TRIGGER — أي مما يلي: - `iterate-ml-experiment` § 0 bootstrap، قبل التصميم الأساسي
  ملاحظة - يتم إطلاق بوابة G-EDA هنا (تشغيل / تخطي).
- يطلب المستخدم "استكشاف البيانات"، و"إجراء EDA"، و"الملف التعريفي".
  مجموعة البيانات"، "كيف تبدو البيانات"، "فهم البيانات".
- يحتاج مصدر البيانات الجديد أو المتغير إلى (إعادة) الفهم قبل
  التجربة القادمة.

تخطي عندما: لم يتم دعم/تمهيد مساحة العمل بعد — `iterate-ml-experiment` § 0 يمتلك ترتيب التمهيد وسيتم إرساله هنا في خطوة G-EDA؛ لا تقم بتشغيل مستقل قبل السقالات (الطريق إلى `iterate-ml-experiment` / `organize-ml-workspace`)؛ ولا توجد بيانات لاستكشافها بعد؛ يريد المستخدم فحص تقرير نتيجة التشغيل النهائي بدلاً من مجموعة البيانات الأولية ('audit-ml-pipeline`)؛ لقد تجاوز المستخدم فهم البيانات ويريد ميكانيكا خط الأنابيب/التقييم (`build-ml-pipeline` / `evaluate-ml-pipeline`)؛ بحث خالص عن الرمز (`python-api`)؛ تم تسجيل EDA بالفعل (`data/eda.md` + قسم JOURNAL EDA موجود) ولا يطلب المستخدم تحديثه.
كيفية الاستخدام: قم بتشغيل خطوة الاكتشاف (هل "data/eda.md" + قسم JOURNAL EDA موجود بالفعل؟)، وأصدر قائمة التحقق قبل الرحلة كنص مرئي، واقرأ شروط الإيقاف، ثم ضع "data/eda.py" من "templates/eda.py"، وقم بتنفيذه عبر المشغل المشترك، واقرأ الملخص، وقم بتأليف "data/eda.md" + قسم JOURNAL EDA. قم دائمًا بحل رموز skrub/pandas/polars عبر `python-api`، وليس من الذاكرة أبدًا.
