Verfügt über Datenverständnis, BEVOR ein Modell entworfen wird. Platziert und führt „data/eda.py“ (ein Jupytext „# %%“-Skript) über den gemeinsam genutzten In-Process-Runner aus, liest den gestreamten Digest und schreibt dann einen persistenten „data/eda.md“-Bericht (plus verknüpfte „data/eda_<table>.html“-Skrub-„TableReport“-Seiten) und den Abschnitt „## Data Understanding (EDA)“ von `journal/JOURNAL.md`. Es geht darum, die Fakten des Datensatzes ans Licht zu bringen – Form, D-Typen, Fehlen, Kardinalität, Zielgleichgewicht/-abweichung, Datum/Uhrzeit/Gruppenstruktur, Merkmalszuordnungen –, die die späteren Entscheidungen des Lernenden/Splitters/Metrik rechtfertigen, damit der Benutzer versteht, *warum* die Modellierungsentscheidungen getroffen werden. Verwendet „skrub.TableReport“ für Datenrahmenübersichten und den gemeinsam genutzten Läufer „audit-ml-pipeline/scripts/run_cells.py“. Stoppt bei „EDA ausgeführt, „data/eda.md“ + HTML geschrieben, JOURNAL EDA-Abschnitt aktualisiert.“ Entwirft niemals das Modell, bearbeitet niemals „src/<pkg>/“ und ändert niemals die Rohdatendateien des Benutzers.
TRIGGER – einer von: - „iterate-ml-experiment“ § 0 Bootstrap, VOR dem Basisdesign
  Hinweis: Das G-EDA-Tor wird hier ausgelöst (Ausführen/Überspringen).
- Der Benutzer bittet darum, „die Daten zu untersuchen“, „eine EDA durchzuführen“, „ein Profil zu erstellen“.
  Datensatz“, „Wie sehen die Daten aus“, „Die Daten verstehen“.
- Eine neue oder geänderte Datenquelle muss vorher (neu) verstanden werden
  nächstes Experiment.

ÜBERSPRINGEN, wenn: der Arbeitsbereich noch nicht gerüstiert/gebootet ist – „iterate-ml-experiment“ § 0 besitzt die Bootstrap-Reihenfolge und wird hier beim G-EDA-Schritt versenden; Führen Sie nicht eigenständig vor dem Gerüst aus (Route zu „iterate-ml-experiment“ / „organize-ml-workspace“); es liegen noch keine Daten zur Untersuchung vor; Der Benutzer möchte den Skore-Bericht eines abgeschlossenen Laufs und nicht den Rohdatensatz („audit-ml-pipeline“) prüfen. Der Benutzer hat kein Datenverständnis mehr und möchte Pipeline-/Auswertungsmechanismen („build-ml-pipeline“ / „evaluate-ml-pipeline“); eine reine Symbolsuche („python-api“); EDA ist bereits aufgezeichnet (`data/eda.md` + der Abschnitt JOURNAL EDA vorhanden) und der Benutzer bittet nicht um eine Aktualisierung.
ANWENDUNG: Führen Sie den Erkennungsschritt aus (existieren „data/eda.md“ + der Abschnitt „JOURNAL EDA“ bereits?), geben Sie die Preflight-Checkliste als sichtbaren Text aus, lesen Sie die Stoppbedingungen, platzieren Sie dann „data/eda.py“ aus „templates/eda.py“, führen Sie ihn über den Shared Runner aus, lesen Sie den Digest und erstellen Sie „data/eda.md“ + den Abschnitt „JOURNAL EDA“. Lösen Sie Skrub-/Pandas-/Polarsymbole immer über „python-api“ auf, niemals aus dem Speicher.
