Metodologia per la valutazione di un singolo studente compatibile con sklearn (in particolare, lo `SkrubLearner` prodotto da `build-ml-pipeline`). Possiede: quale punto di ingresso chiamare (`skore.evaluate` prima, le classi di report esplicite quando necessario), quale validatore incrociato scegliere dal catalogo di scikit-learn, come consumare i metadati strutturali (`groups`, `times`, ...) allegati in fase di compilazione tramite `.skb.mark_as_X(split_kwargs=...)`. Si ferma a "cosa dice il rapporto". I valori predefiniti (metriche, grafici) provengono da skore; eseguire l'override solo su richiesta esplicita dell'utente.
TRIGGER quando: il codice chiama `cross_val_score`, `cross_validate`, `classification_report` o qualsiasi stampa metrica scritta a mano (`print(mean_squared_error(...))`); il codice chiama `.skb.cross_validate(...)` (percorso attraverso skore per un output più ricco); l'utente chiede come assegnare un punteggio, valutare o confrontare un singolo studente; l'utente chiede come scegliere un validatore incrociato; l'utente desidera vedere un report/metriche/grafici diagnostici per uno studente adatto.
SALTA quando: dichiari la pipeline (usa `build-ml-pipeline`); ricerca di iperparametri/modelli (competenza separata); adattare, persistere o servire il modello finale; monitorare o confrontare esperimenti su più esecuzioni nel tempo (abilità separata).
MODALITÀ D'USO: invocare prima di ogni chiamata di valutazione. **Per prima cosa, leggi il blocco "Condizioni di arresto" nella parte superiore del corpo ed emetti la lista di controllo pre-volo come testo visibile nella tua risposta: entrambi sono obbligatori prima che venga scritto qualsiasi codice di valutazione.** I fatti strutturali sui dati (chiavi di gruppo, ordinamento temporale) dovrebbero già essere codificati sul marcatore X tramite `split_kwargs` — se non lo sono e non puoi dirlo dai dati, torna a `build-ml-pipeline` e chiedi all'utente. Per le ricerche a livello di simbolo, fare riferimento a `python-api` (simboli skore) e `python-api` (splitter); non indovinare i nomi dalla memoria.
