Metodología para evaluar un único alumno compatible con sklearn (en particular, el "SkrubLearner" producido por "build-ml-pipeline"). Posee: qué punto de entrada llamar (`skore.evaluate` primero, las clases de informes explícitos cuando sea necesario), qué validador cruzado elegir del catálogo de scikit-learn, cómo consumir los metadatos estructurales (`grupos`, `veces`,…) adjuntos en el momento de la compilación a través de `.skb.mark_as_X(split_kwargs=...)`. Se detiene en "qué dice el informe". Los valores predeterminados (métricas, gráficos) provienen de skore; solo se anula a petición explícita del usuario.
DISPARAR cuando: el código llama a `cross_val_score`, `cross_validate`, `classification_report` o cualquier impresión métrica escrita a mano (`print(mean_squared_error(...))`); llamadas de código `.skb.cross_validate(...)` (ruta a través de skore para obtener resultados más ricos); el usuario pregunta cómo calificar, evaluar o comparar a un solo alumno; el usuario pregunta cómo elegir un validador cruzado; El usuario quiere ver un informe/métricas/gráficos de diagnóstico para un alumno adaptado.
SALTAR cuando: declare la canalización (use `build-ml-pipeline`); búsqueda de hiperparámetro/modelo (habilidad separada); adaptar, persistir o servir al modelo final; rastrear o comparar experimentos en múltiples ejecuciones a lo largo del tiempo (habilidad separada).
CÓMO UTILIZAR: invocar antes de cualquier llamada de evaluación. **Primero, lea el bloque "Condiciones de detención" en la parte superior del cuerpo y emita la lista de verificación previa al vuelo como texto visible en su respuesta; ambos son obligatorios antes de escribir cualquier código de evaluación.** Los datos estructurales sobre los datos (claves de grupo, ordenamiento de tiempo) ya deben estar codificados en el marcador X a través de `split_kwargs`; si no lo están y no puede distinguirlos de los datos, regrese a `build-ml-pipeline` y pregúntele al usuario. Para búsquedas a nivel de símbolo, consulte `python-api` (símbolos skore) y `python-api` (divisores); No adivines nombres de memoria.
