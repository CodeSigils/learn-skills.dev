단일 sklearn 호환 학습기(특히 `build-ml-pipeline`으로 생성된 `SkrubLearner`)를 평가하기 위한 방법론입니다. 소유: 호출할 진입점(먼저 `skore.evaluate`, 필요한 경우 명시적인 보고서 클래스), scikit-learn의 카탈로그에서 선택할 교차 검증기, `.skb.mark_as_X(split_kwargs=...)`를 통해 빌드 시 첨부된 구조적 메타데이터(`groups`, `times`, …)를 사용하는 방법. "보고서에 나오는 내용"에서 멈춥니다. 기본값(메트릭, 플롯)은 skore에서 가져옵니다. 명시적인 사용자 요청에만 재정의됩니다.
TRIGGER 언제: 코드가 `cross_val_score`, `cross_validate`, `classification_report` 또는 손으로 쓴 지표 인쇄(`print(mean_squared_error(...))`)를 호출하는 경우; 코드 호출 `.skb.cross_validate(...)`(더 풍부한 출력을 위해 skore를 통해 라우팅); 사용자는 단일 학습자의 점수를 매기고 평가하거나 비교하는 방법을 묻습니다. 사용자가 교차 검증기를 선택하는 방법을 묻습니다. 사용자는 적합한 학습자에 대한 보고서/지표/진단 플롯을 보고 싶어합니다.
다음의 경우 건너뛰기: 파이프라인 선언(`build-ml-pipeline` 사용) 하이퍼파라미터/모델 검색(별도의 기술); 최종 모델을 피팅, 유지 또는 제공합니다. 시간이 지남에 따라 여러 실행에 걸쳐 실험을 추적하거나 비교합니다(별도의 기술).
사용 방법: 평가 호출 전에 호출합니다. **먼저 본문 상단에 있는 "중지 조건" 블록을 읽고 응답에 표시 가능한 텍스트로 사전 체크리스트를 내보냅니다. 둘 다 평가 코드가 작성되기 전에 필수입니다.** 데이터에 대한 구조적 사실(그룹 키, 시간 순서)은 `split_kwargs`를 통해 X 마커에 이미 인코딩되어 있어야 합니다. 그렇지 않고 데이터에서 알 수 없는 경우 `build-ml-pipeline`으로 돌아가 사용자에게 문의하세요. 기호 수준 조회의 경우 `python-api`(skore 기호) 및 `python-api`(분할기)를 따르십시오. 기억에서 이름을 추측하지 마십시오.
