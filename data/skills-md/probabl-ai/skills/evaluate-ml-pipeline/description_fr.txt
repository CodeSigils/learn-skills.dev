Méthodologie d'évaluation d'un seul apprenant compatible sklearn (en particulier, le `SkrubLearner` produit par `build-ml-pipeline`). Possède : quel point d'entrée appeler (`skore.evaluate` en premier, les classes de rapport explicites si nécessaire), quel validateur croisé choisir dans le catalogue de scikit-learn, comment consommer les métadonnées structurelles (`groups`, `times`, …) attachées au moment de la construction via `.skb.mark_as_X(split_kwargs=...)`. S'arrête à "que dit le rapport". Les valeurs par défaut (métriques, tracés) proviennent de skore ; remplacer uniquement sur demande explicite de l'utilisateur.
DÉCLENCHEMENT lorsque : le code appelle `cross_val_score`, `cross_validate`, `classification_report` ou toute impression de métrique manuscrite (`print(mean_squared_error(...))`); le code appelle `.skb.cross_validate(...)` (route via skore pour une sortie plus riche) ; l'utilisateur demande comment noter, évaluer ou comparer un seul apprenant ; l'utilisateur demande comment choisir un validateur croisé ; l'utilisateur souhaite voir un rapport/des mesures/des tracés de diagnostic pour un apprenant équipé.
SKIP when : déclarer le pipeline (utilisez `build-ml-pipeline`) ; recherche d'hyperparamètres/modèles (compétence distincte) ; adapter, conserver ou servir le modèle final ; suivre ou comparer des expériences sur plusieurs exécutions au fil du temps (compétence distincte).
COMMENT UTILISER : invoquer avant tout appel d'évaluation. **Tout d'abord, lisez le bloc « Conditions d'arrêt » en haut du corps et émettez la liste de contrôle avant vol sous forme de texte visible dans votre réponse — les deux sont obligatoires avant qu'un code d'évaluation ne soit écrit.** Les faits structurels sur les données (clés de groupe, ordre temporel) doivent déjà être codés au marqueur X via `split_kwargs` — si ce n'est pas le cas et que vous ne pouvez pas le déterminer à partir des données, retournez à `build-ml-pipeline` et demandez à l'utilisateur. Pour les recherches au niveau des symboles, reportez-vous à « python-api » (symboles skore) et « python-api » (séparateurs) ; ne devinez pas les noms de mémoire.
