Methodik zur Bewertung eines einzelnen sklearn-kompatiblen Lernenden (insbesondere des von „build-ml-pipeline“ erstellten „SkrubLearner“). Besitzt: welcher Einstiegspunkt aufgerufen werden soll (`skore.evaluate` zuerst, die expliziten Berichtsklassen bei Bedarf), welcher Kreuzvalidator aus dem Katalog von scikit-learn ausgewählt werden soll, wie die strukturellen Metadaten (`Gruppen`, `Zeiten`, …) verwendet werden, die zur Build-Zeit über `.skb.mark_as_X(split_kwargs=...)` angehängt werden. Stoppt bei „Was sagt der Bericht?“. Vorgaben (Metriken, Diagramme) stammen von Skore; Nur auf ausdrückliche Benutzeranfrage überschreiben.
TRIGGER, wenn: Code „cross_val_score“, „cross_validate“, „classification_report“ oder einen handschriftlichen metrischen Ausdruck („print(mean_squared_error(...))“ aufruft; Code ruft „.skb.cross_validate(...)“ auf (Route durch Skore für eine reichhaltigere Ausgabe); Der Benutzer fragt, wie er einen einzelnen Lernenden bewerten, bewerten oder vergleichen kann. Benutzer fragt, wie man einen Kreuzvalidator auswählt; Der Benutzer möchte einen Bericht/Metriken/Diagnosediagramme für einen angepassten Lernenden sehen.
ÜBERSPRINGEN, wenn: die Pipeline deklariert wird (verwenden Sie „build-ml-pipeline“); Hyperparameter-/Modellsuche (separate Fähigkeit); das endgültige Modell anpassen, beibehalten oder ihm dienen; Verfolgen oder Vergleichen von Experimenten über mehrere Durchläufe hinweg im Laufe der Zeit (separate Fähigkeit).
ANWENDUNG: Vor jedem Evaluierungsaufruf aufrufen. **Lesen Sie zunächst den Block „Stoppbedingungen“ oben im Hauptteil und geben Sie die Preflight-Checkliste als sichtbaren Text in Ihrer Antwort aus – beides ist obligatorisch, bevor ein Auswertungscode geschrieben wird.** Die strukturellen Fakten zu den Daten (Gruppenschlüssel, Zeitreihenfolge) sollten bereits an der Für Suchvorgänge auf Symbolebene verwenden Sie „python-api“ (Skore-Symbole) und „python-api“ (Splitter); Erraten Sie keine Namen aus dem Gedächtnis.
