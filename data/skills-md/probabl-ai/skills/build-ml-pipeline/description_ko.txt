데이터 소스에서 예측기로의 파이프라인을 **skrub DataOps 그래프**(기본 `sklearn.Pipeline`이 아님)로 선언합니다. 모든 단계는 `.skb.apply_func`를 통해 연결된 순수 Python 함수(상태 비저장)이거나 `.skb.apply`를 통해 연결된 sklearn 호환 추정기(상태 저장)입니다. 선언된 개체에서 중지합니다. 맞춤, 분할, 조정, 지속성 또는 평가가 없습니다.
TRIGGER — 다음 중 하나: - 체인의 링크를 선언하는 코드 작성 또는 편집
  *데이터 소스 → 예측자*: 로더, 전처리, 인코더 /
  입력기/스케일러, 기능 단계, 구성 개체
  (`Pipeline`, `ColumnTransformer`, skrub `tabular_pipeline`,
  `nn.Module`) 또는 최종 추정기입니다.
- 순수 Python 데이터 처리 기능
  파이프라인 경로(정리/파생/형태 변경) - 래핑 여부
  `FunctionTransformer`, `skrub.@deferred` / `skrub.var`를 통해,
  맞춤 'BaseEstimator' 하위 클래스를 사용하거나
  추정기 이전의 훈련 경로.
- 단계 내에서 단계가 추가, 제거, 교환 또는 재정렬됩니다.
  기존 파이프라인 선언.
- 기본 `sklearn.Pipeline` / `make_pipeline`이 다음과 같이 사용되고 있습니다.
  최상위 수준 - Skrub DataOps 그래프로 리디렉션하기 위해 실행됩니다.
- 사용자가 파이프라인 구축/선언/설정/설정을 요청합니다.
  X에 대한 분류자/회귀자.

건너뛰기: `.fit(...)` 호출 / 훈련 루프 / `Trainer.fit` / 에포크 루프; 훈련/테스트 분할 또는 교차 검증 분할; 하이퍼파라미터 검색; 지속성(`joblib.dump`, 체크포인트); 평가/지표/점수; 사전 훈련된 모델에 대한 추론; 순수 EDA; 구체적인 선언이 없는 도서관 선택 질문.
사용 방법: 첫 번째 선언문 이전과 모든 구조적 편집(추가/교환된 단계, 변경된 입력 열, 변경된 추정기 제품군)에 대해 참조하세요. 미용적인 편집을 위해 다시 상담하지 마세요. **먼저 중지 조건을 읽고 코드 앞에 표시되는 텍스트로 사전 비행 체크리스트를 내보냅니다.** 입력하기 전에 항상 `python-api`를 호출하여 skrub/sklearn 기호 이름과 서명을 확인하세요. 기억으로 추측하지 마세요.
