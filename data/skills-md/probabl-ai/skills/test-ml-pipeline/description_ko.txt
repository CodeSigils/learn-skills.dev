ML 작업 영역의 'tests/' 폴더와 실험과 테스트 간의 연결 규칙을 소유합니다. 경량 라우터: 모든 테스트 카테고리에는 고유한 하위 기술이 있습니다(`smoke-test-ml-pipeline`은 v1의 유일한 기술입니다. `regression-test-ml-pipeline` / `distribution-test-ml-pipeline` 등은 작업 공간이 성장함에 따라 형제로 연결됩니다). 이 스킬은 빈 `tests/<category>/` 폴더를 배치하고, `tests/<category>/test_NN_<short_name>.py`와 `experiments/NN_<short_name>.py` 사이의 줄기 쌍 규칙을 시행하며, 사용자가 테스트를 요청할 때 일치하는 하위 기술로 전달합니다.
TRIGGER 언제: 새로운 디자인 노트가 'iterate-ml-experiment'에 의해 방금 승인되었으며 실험이 완료되었다고 표시되기 전에 일치 테스트 초안을 작성해야 합니다. 사용자가 "`02`에 대한 연기 테스트 작성", "회귀 테스트 추가", "X에 대한 테스트가 있습니까?"라고 묻습니다. 실험 스크립트가 편집되었으며 페어링된 테스트를 다시 방문해야 합니다. `pytest 테스트/`를 실행하려고 하는데 예상되는 쌍 테스트 중 하나가 누락되었습니다.
다음의 경우 건너뛰기: 설계 노트가 아직 존재하지 않습니다(먼저 `iterate-ml-experiment`로 경로 지정). 테스트는 패키지의 내부 도우미를 위한 것이며 특정 실험과 관련이 없습니다(정규 단위 테스트는 프로젝트의 `pytest` 구성이 이를 선택하는 모든 위치에서 실행됩니다. 여기서는 범위를 벗어납니다). 문제는 *테스트가 존재해야 하는가*(일치하는 하위 기술로 경로 지정)보다는 *테스트 결과가 무엇을 의미하는가*입니다.
사용방법 : 이 스킬은 라우터입니다. 디스패치 테이블을 읽고 사용자가 요청하는 테스트 카테고리를 소유한 하위 기술을 파악한 다음 전달합니다. 하위 스킬에 속하는 테스트 본문을 직접 작성하지 마십시오. 일치하는 스템과 pytest 스캐폴딩이 포함된 빈 테스트 파일을 배치한 다음 제어권을 넘겨주세요.
