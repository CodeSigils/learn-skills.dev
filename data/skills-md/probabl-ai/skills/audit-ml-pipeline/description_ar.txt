يمتلك مجلد "audit/": ملف Python واحد `# %%` (jupytext%) لكل تجربة، بمحاذاة 1:1 مع `experiments/NN_<short_name>.py` و`journal/NN_<short_name>.md`، والذي يقوم بتحميل تقرير درجة التجربة **للقراءة فقط** ويستخدم خلايا التعبير الأخير العارية التي يحمل `__repr__` إشارة التدقيق.   ينفذ الوكيل ملف التدقيق عبر برنامج التشغيل المُجمَّع أثناء العملية (`audit-ml-pipeline/scripts/run_cells.py` — IPython `InteractiveShell.run_cell`)، والذي يقوم بدفق ملخص تخفيض السعر لـ stdout لكل خلية + repr للتعبير الأخير إلى stdout (اختياريًا أيضًا إلى ملف). يغذي الملخص العمل السردي (حالة "JOURNAL.md" + تحديث التاريخ، وأسئلة المتابعة حول تجربة سابقة، والمقارنة بين التجارب). يتوقف عند "وضع Audit/NN_*.py وتنفيذه وإتاحة الملخص." لا تستدعي أبدًا `skore.evaluate(...)` أو `project.put(...)`.
TRIGGER — أي مما يلي: - `iterate-ml-experiment` § 4 سجل النتيجة - يتم إرسال التدقيق
  أولًا (يستبدل مجسات الخدش للاستخراج المتري).
- يسأل المستخدم "تجربة التدقيق 02"، "أرني كيف يبدو 03
  مثل "،" إعادة التدقيق 04 مقابل التقرير الجديد ".
- تم إعادة تشغيل التجربة (تمت الكتابة فوق نفس مفتاح `put()`) وتم حذف
  ملف التدقيق المطابق يحتاج إلى إعادة التنفيذ.
- يريد المستخدم سردًا يمكن قراءته بواسطة الإنسان لتجربة سابقة
  دون إطلاق التدفق الكامل "للتكرار من skore".

تخطي عندما: لم تتم الموافقة على ملاحظة التصميم بعد (التوجه إلى "iterate-ml-experiment")؛ لم يتم تشغيل التجربة (لا يوجد تقرير على القرص)؛ لم يتم تثبيت ميزة الوكيل (تفويض إلى `python-env-manager` § "ميزة الوكيل")؛ يقوم المستخدم بالتنقيب في التقرير كمصدر للتجربة *التالية* ("التكرار من skore`)؛ يريد المستخدم استكشاف **مجموعة البيانات الأولية** بدلاً من تقرير نتيجة التشغيل النهائي (`explore-ml-data` — يقرأ التدقيق التقرير، وليس البيانات).
كيفية الاستخدام: تأكد من وجود الاقتران الجذري رباعي الاتجاهات (تمت الموافقة على `journal/NN_*.md` + وجود `experiments/NN_*.py` + اجتياز اختبار الدخان + تقرير تحت هذا المفتاح في المشروع)، ثم ضع `audit/NN_<short_name>.py` من `templates/audit.py`، مع استبدال اسم الحزمة + كتلة init الخاصة بالمشروع المنسوخة من `التجارب/<stem>.py`.   نفذ عبر المشغل المجمّع: `pixi run -e agent python .agents/skills/audit-ml-pipeline/scripts/run_cells.py Audit/<stem>.py`. ** اقرأ شروط الإيقاف وأصدر قائمة التحقق قبل الرحلة قبل أي أمر كتابة أو أمر shell. ** قم دائمًا باستدعاء `python-api` لتوقيعات رموز skore - لا تكتبها من الذاكرة أبدًا.
