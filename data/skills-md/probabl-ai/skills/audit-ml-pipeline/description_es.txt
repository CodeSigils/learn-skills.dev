Posee la carpeta `audit/`: un archivo Python `# %%` (porcentaje de jupytext) por experimento, alineado 1:1 con `experiments/NN_<short_name>.py` y `journal/NN_<short_name>.md`, que carga el informe skore del experimento **de solo lectura** y utiliza celdas con la última expresión desnuda cuyo `__repr__` lleva la señal de la auditoría.   El agente ejecuta el archivo de auditoría a través del ejecutor en proceso incluido (`audit-ml-pipeline/scripts/run_cells.py` — IPython `InteractiveShell.run_cell`), que transmite un resumen de rebajas de la salida estándar + última expresión de cada celda a la salida estándar (opcionalmente también a un archivo). El resumen impulsa el trabajo narrativo (la actualización del estado + historial de `JOURNAL.md`, preguntas de seguimiento sobre un experimento anterior, comparación entre experimentos). Se detiene en "audit/NN_*.py se coloca, se ejecuta y el resumen está disponible". Nunca llama a `skore.evaluate(...)` o `project.put(...)`.
DISPARADOR - cualquiera de: - `iterate-ml-experiment` § 4 registro-resultado - se envía la auditoría
  PRIMERO (reemplaza sondas de rayado para extracción métrica).
- El usuario pregunta "experimento de auditoría 02", "muéstrame cómo se ve 03".
  "me gusta", "volver a auditar 04 con el nuevo informe".
- Se volvió a ejecutar un experimento (se sobrescribió la misma clave `put()`) y el
  El archivo de auditoría coincidente necesita volver a ejecutarse.
- El usuario quiere una narrativa legible por humanos de un experimento pasado.
  sin activar el flujo completo de "iteración desde skore".

SALTAR cuando: la nota de diseño aún no esté aprobada (ruta a `iterate-ml-experiment`); el experimento no se ha ejecutado (no hay informe en el disco); la función del agente no está instalada (delegar en `python-env-manager` § "Función del agente"); el usuario está extrayendo el informe para obtener el *siguiente* experimento (`iterate-from-skore`); el usuario quiere explorar el **conjunto de datos sin procesar** en lugar del informe skore de una ejecución finalizada ("explore-ml-data" — la auditoría lee un informe, no los datos).
CÓMO USAR: confirme que existe el emparejamiento de raíces de cuatro vías (`journal/NN_*.md` aprobado + `experimentos/NN_*.py` existe + prueba de humo aprobada + informe bajo esa clave en el Proyecto), luego coloque `audit/NN_<short_name>.py` de `templates/audit.py`, sustituyendo el nombre del paquete + el bloque de inicio literal del Proyecto copiado de `experiments/<stem>.py`.   Ejecute a través del corredor incluido: `pixi run -e agent python .agents/skills/audit-ml-pipeline/scripts/run_cells.py audit/<stem>.py`. **Lea las condiciones de detención y emita la lista de verificación previa al vuelo antes de cualquier comando de escritura o shell.** Invoque siempre `python-api` para las firmas de símbolos de skore; nunca las escriba desde la memoria.
