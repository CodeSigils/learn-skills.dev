Possiede la cartella `audit/`: un file Python `# %%` (percentuale jupytext) per esperimento, allineato 1:1 con `experiments/NN_<short_name>.py` e `journal/NN_<short_name>.md`, che carica il report del punteggio dell'esperimento **di sola lettura** e utilizza celle bare-last-expression il cui `__repr__` trasporta il segnale di audit.   L'agente esegue il file di audit tramite il runner in-process in bundle (`audit-ml-pipeline/scripts/run_cells.py` — IPython `InteractiveShell.run_cell`), che trasmette un digest markdown dello stdout + repr dell'ultima espressione di ciascuna cella allo stdout (facoltativamente anche a un file). Il digest alimenta il lavoro narrativo (l'aggiornamento di stato + cronologia di `JOURNAL.md`, domande di follow-up su un esperimento passato, confronto tra esperimenti). Si ferma a "audit/NN_*.py viene inserito, eseguito e il digest è disponibile." Non chiama mai `skore.evaluate(...)` o `project.put(...)`.
TRIGGER: uno qualsiasi tra: - "iterate-ml-experiment" § 4 record-outcome: l'audit viene inviato
  PRIMO (sostituisce le sonde antigraffio per l'estrazione metrica).
- L'utente chiede "esperimento di controllo 02", "mostrami che aspetto ha 03".
  like", "rieseguire l'audit 04 rispetto al nuovo rapporto".
- È stato eseguito nuovamente un esperimento (lo stesso tasto `put()` è stato sovrascritto) e il file
  il file di controllo corrispondente deve essere rieseguito.
- L'utente desidera una narrazione leggibile dall'uomo di un esperimento passato
  senza attivare l'intero flusso "iterazione da score".

SALTA quando: la nota di progettazione non è ancora stata approvata (indirizza a `iterate-ml-experiment`); l'esperimento non è stato eseguito (nessun report su disco); la funzionalità dell'agente non è installata (delegare a `python-env-manager` § "Funzionalità dell'agente"); l'utente sta estraendo il report per ottenere l'esperimento *successivo* ("iterate-from-skore"); l'utente desidera esplorare il **set di dati grezzi** piuttosto che il report sui punteggi di una corsa terminata ("explore-ml-data" — l'audit legge un report, non i dati).
COME USARE: conferma che esiste l'accoppiamento stem a quattro vie (`journal/NN_*.md` approvato + `experiments/NN_*.py` esiste + test del fumo superato + report sotto quella chiave nel progetto), quindi inserisci `audit/NN_<short_name>.py` da `templates/audit.py`, sostituendo il nome del pacchetto + il blocco init letterale del progetto copiato da `experiments/<stem>.py`.   Eseguire tramite il runner in bundle: `pixi run -e agent python .agents/skills/audit-ml-pipeline/scripts/run_cells.py audit/<stem>.py`. **Leggi le condizioni di arresto ed emetti la lista di controllo pre-volo prima di qualsiasi comando di scrittura o shell.** Richiama sempre `python-api` per le firme dei simboli skore: non scriverle mai dalla memoria.
