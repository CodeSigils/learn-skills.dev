Possède le dossier `audit/` : un fichier Python `# %%` (pourcentage jupytext) par expérience, aligné 1:1 avec `experiments/NN_<short_name>.py` et `journal/NN_<short_name>.md`, qui charge le rapport skore de l'expérience **en lecture seule** et utilise des cellules de dernière expression nue dont `__repr__` porte le signal de l'audit.   L'agent exécute le fichier d'audit via le programme d'exécution intégré au processus (`audit-ml-pipeline/scripts/run_cells.py` — IPython `InteractiveShell.run_cell`), qui diffuse un résumé de démarque de la sortie standard + la répétition de la dernière expression de chaque cellule vers la sortie standard (éventuellement également dans un fichier). Le résumé alimente le travail narratif (la mise à jour du statut et de l'historique de `JOURNAL.md`, des questions de suivi sur une expérience passée, une comparaison entre expériences). S'arrête à "audit/NN_*.py est placé, exécuté et le résumé est disponible." N'appelle jamais `skore.evaluate(...)` ou `project.put(...)`.
TRIGGER — l'un des : - `iterate-ml-experiment` § 4 record-outcome — l'audit est envoyé
  PREMIER (remplace les sondes à gratter pour l'extraction métrique).
- L'utilisateur demande "audit expérience 02", "montre-moi à quoi ressemble 03
  like", "ré-audit 04 par rapport au nouveau rapport".
- Une expérience a été réexécutée (même clé `put()` écrasée) et le
  le fichier d'audit correspondant doit être réexécuté.
- L'utilisateur souhaite un récit lisible par l'homme d'une expérience passée
  sans déclencher le flux complet « itérer à partir de skore ».

SAUTER quand : la note de conception n'est pas encore approuvée (route vers `iterate-ml-experiment`) ; l'expérience n'a pas été exécutée (pas de rapport sur le disque) ; la fonctionnalité d'agent n'est pas installée (déléguer à `python-env-manager` § "Fonctionnalité d'agent"); l'utilisateur exploite le rapport pour trouver la *prochaine* expérience (« iterate-from-skore ») ; l'utilisateur souhaite explorer l'**ensemble de données brutes** plutôt que le rapport skore d'une exécution terminée (`explore-ml-data` — l'audit lit un rapport, pas les données).
COMMENT UTILISER : confirmez que l'appariement de tiges à quatre voies existe (`journal/NN_*.md` approuvé + `experiments/NN_*.py` existe + test de fumée réussi + rapport sous cette clé dans le projet), puis placez `audit/NN_<short_name>.py` à partir de `templates/audit.py`, en remplaçant le nom du package + le bloc d'initialisation littéral du projet copié à partir de `experiments/<stem>.py`.   Exécutez via le programme d'exécution fourni : `pixi run -e agent python .agents/skills/audit-ml-pipeline/scripts/run_cells.py audit/<stem>.py`. **Lisez les conditions d'arrêt et émettez la liste de contrôle avant vol avant toute commande d'écriture ou shell.** Invoquez toujours « python-api » pour les signatures de symboles skore — ne les écrivez jamais depuis la mémoire.
