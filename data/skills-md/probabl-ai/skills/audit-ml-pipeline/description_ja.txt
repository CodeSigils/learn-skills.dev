`audit/` フォルダーを所有します。実験ごとに 1 つの `# %%` (jupytext パーセント) Python ファイルがあり、`experiments/NN_<short_name>.py` および `journal/NN_<short_name>.md` と 1:1 で配置され、実験のスコア レポートを **読み取り専用**でロードし、`__repr__` が監査を保持するベアラスト式セルを使用します。信号。   エージェントは、バンドルされているインプロセス ランナー (`audit-ml-pipeline/scripts/run_cells.py` — IPython `InteractiveShell.run_cell`) を介して監査ファイルを実行し、各セルの標準出力 + 最後の式表現のマークダウン ダイジェストを標準出力 (オプションでファイルにも) ストリーミングします。ダイジェストは、物語の作業を促進します (「JOURNAL.md」のステータスと履歴の更新、過去の実験に関するフォローアップの質問、実験間の比較)。 「audit/NN_*.py が配置され、実行され、ダイジェストが利用可能になります。」で停止します。 `skore.evaluate(...)` や `project.put(...)` を決して呼び出さないでください。
TRIGGER — 次のいずれか - `iterate-ml-experiment` § 4 レコード結果 — 監査がディスパッチされる
  FIRST (メトリック抽出用のスクラッチ プローブを置き換えます)。
- ユーザーは「監査実験 02」、「03 がどのようなものかを見せてください」と尋ねます。
  「新しい報告書に対して04を再監査する」など。
- 実験が再実行されました (同じ `put()` キーが上書きされました)。
  一致する監査ファイルを再実行する必要があります。
- ユーザーは、人間が読める過去の実験の物語を望んでいます。
  完全な「iterate-from-skore」フローを起動する必要はありません。

次の場合はスキップしてください: 設計ノートがまだ承認されていない (「iterate-ml-experiment」へのルート)。実験は実行されていません (ディスク上にレポートがありません)。エージェント機能がインストールされていません (`python-env-manager` § "エージェント機能" に委譲)。ユーザーは、*次の* 実験のソースとなるレポートをマイニングしています (`iterate-from-skore`)。ユーザーは、終了した実行のスコア レポートではなく、**生のデータセット**を探索したいと考えています (`explore-ml-data` — 監査はデータではなくレポートを読み取ります)。
使用方法: 4 方向のステム ペアが存在することを確認し (`journal/NN_*.md` 承認 + `experiments/NN_*.py` 存在 + スモーク テストに合格 + プロジェクト内のそのキーの下にレポート)、次に `templates/audit.py` から `audit/NN_<short_name>.py` を配置し、パッケージ名 + からコピーしたリテラルのプロジェクト初期化ブロックを置き換えます。 `experiments/<stem>.py`。   バンドルされたランナーを介して実行します: `pixi run -e Agent python .agents/skills/audit-ml-pipeline/scripts/run_cells.py Audit/<stem>.py`。 **書き込みまたはシェル コマンドの前に、停止条件を読み取り、プリフライト チェックリストを発行します。** スコア シンボルの署名については常に `python-api` を呼び出してください。決してメモリから書き込まないでください。
