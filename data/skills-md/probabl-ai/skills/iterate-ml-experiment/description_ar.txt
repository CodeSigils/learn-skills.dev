يمتلك حلقة التكرار أعلى مساحة عمل ML: فهرس `journal/JOURNAL.md` وملاحظات التصميم `journal/NN_short_name.md` لكل تجربة والتي يجب صياغتها والموافقة عليها من قبل المستخدم **قبل** إنشاء `experiments/NN_short_name.py`. يقود الاقتراح ← التكرار ← الموافقة ← التنفيذ ← حلقة التسجيل؛ يتم الإرسال إلى "iterate-from-skore" / "iterate-from-user" للمصادر.
TRIGGER - أي مما يلي: - يتم فتح الجلسة في مساحة عمل ML (سواء كانت `مجلة/` أم لا
  موجود حتى الآن — مفقود/عنصر نائب → وضع التمهيد).
- يقول المستخدم "ما هي الخطوة التالية"، "استئناف"، "أين كنا"، "دعونا".
  تكرار"، "اقتراح التالي"، "خط الأساس الأول".
- على وشك إنشاء `experiments/NN_*.py` جديد (ملف المطابقة
  يجب أن تكون `journal/NN_*.md` موجودة وأن تتم الموافقة عليها أولاً).
- يريد المستخدم تسجيل نتيجة من التشغيل النهائي. - يطلب المستخدم مقارنة التجارب السابقة أو مراجعة ما تم
  حاول ("قارن X و Y"، "أين نحن؟").

تخطي عندما: لا يوجد `مجلة/` حتى الآن ولا توجد سقالة لمساحة العمل (التوجه إلى `organize-ml-workspace`)؛ يكون العمل ميكانيكيًا داخل `pipeline.py` / `evaluate.py` / `data.py` بدون أي تأثير على مستوى المجلة (مملوك لـ `build-ml-pipeline` / `evaluate-ml-pipeline`)؛ يطلب المستخدم البحث عن رمز (`python-api`)؛ يقوم المستخدم بتشخيص تقرير درجة واحدة بدون إطار "ماذا بعد" ("تقييم-مل-خط الأنابيب").
كيفية الاستخدام: اقرأ "journal/JOURNAL.md" أولاً، وصنف المنعطف عبر **منتقي الوضع** (الجدول بالقرب من الجزء العلوي)، ثم اقرأ القسم المطابق فقط. يتم فتح مهارات الأخوة *في الوقت المناسب* عندما تتطلب إحدى الخطوات ذلك - لا تقرأها مسبقًا جميعها في بداية الجلسة. ملاحظات التصميم هي القطعة الأثرية الوحيدة التي تكتبها هذه المهارة؛ أوضاع القراءة والمقارنة والنظرة العامة لا تكتب.
