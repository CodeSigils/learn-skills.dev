ML 작업 영역 위에 반복 루프를 소유합니다. 'journal/JOURNAL.md' 인덱스와 'experiments/NN_short_name.py'가 생성되기 **전에** 사용자가 초안을 작성하고 승인해야 하는 실험별 'journal/NN_short_name.md' 디자인 노트입니다. 제안 → 반복 → 승인 → 구현 → 기록 루프를 구동합니다. 소싱을 위해 `iterate-from-skore` / `iterate-from-user`로 전달됩니다.
TRIGGER — 다음 중 하나: - ML 작업 영역에서 세션이 열립니다(`journal/` 여부에 관계 없음).
  아직 존재합니다 — 누락/자리 표시자 → 부트스트랩 모드).
- 사용자가 "다음 단계", "이력서", "우리는 어디에 있었나요?", "하자"라고 말합니다.
  반복", "다음 제안", "첫 번째 기준선".
- 새로운 `experiments/NN_*.py`를 생성하려고 합니다(일치하는
  `journal/NN_*.md`가 존재하고 먼저 승인을 받아야 합니다.)
- 사용자가 완료된 실행의 결과를 기록하려고 합니다. - 사용자가 과거 실험을 비교하거나 결과를 검토하도록 요청합니다.
  시도했습니다("X와 Y 비교", "우리는 어디에 있나요?").

다음의 경우 건너뛰기: 아직 `journal/`이 없고 작업 공간 스캐폴드도 없습니다(`organize-ml-workspace`로 경로 지정). 작업은 저널 수준의 의미 없이 `pipeline.py` / `evaluate.py` / `data.py` 내부에서 기계적입니다(`build-ml-pipeline` / `evaluate-ml-pipeline` 소유). 사용자가 기호 조회(`python-api`)를 요청합니다. 사용자는 "다음 단계" 프레임(`evaluate-ml-pipeline`) 없이 단일 점수 보고서를 진단하고 있습니다.
사용 방법: 먼저 `journal/JOURNAL.md`를 읽고 **모드 선택기**(상단 근처 표)를 통해 회전을 분류한 다음 일치하는 섹션만 읽으세요. 형제 기술은 단계에서 필요할 때 *적시에* 열립니다. 세션 시작 시 9개를 모두 미리 읽지 마십시오. 디자인 노트는 이 스킬이 작성하는 유일한 아티팩트입니다. 읽기, 비교 및 ​​개요 모드에서는 쓰지 않습니다.
