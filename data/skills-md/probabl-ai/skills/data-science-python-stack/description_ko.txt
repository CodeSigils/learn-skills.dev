데이터 과학/ML 작업을 위한 독창적인 Python 스택 — 작업당 라이브러리 1개, 계층(필수/사용자 선택/선택/전이)으로 구성됩니다. SKILL.md는 색인입니다. 라이브러리별 `references/<library>.md` 파일에는 범위, "이 경우 선택"/ "다른 경우 선택" 및 페어링이 포함됩니다.
(다음 중 하나) 다음의 경우 TRIGGER: (1) 이 스택의 도메인에서 **라이브러리 가져오기가 실패**합니다. 대답은 대체가 아니라 설치입니다(§ "종속성 누락" 참조). (2) **라이브러리를 선택해야 합니다** — 명시적으로(사용자가 "X에 대한 라이브러리는 무엇입니까?"라고 묻습니다) 또는 암시적으로(코드가 새로운 종속성을 도입하려고 하거나 프로젝트가 스캐폴딩 중이고 테이블 형식 라이브러리가 아직 선택되지 않았습니다); (3) 새로운 Python 데이터 과학/ML 프로젝트를 시작합니다. (4) 사용자 또는 현재 코드가 스택 외부의 대체 항목(xgboost, lightgbm, black, isort, flake8, poem,atch)에 도달하거나 `mlflow`에 도달하여 매개변수/메트릭을 기록하거나 `cross_val_score` + 필기 보고에 도달합니다. 리디렉션: 추적 → `skore` 프로젝트 API, 평가/보고 → `skore` 보고서 클래스, `mlflow`는 모델 제공/레지스트리에만 유지됩니다.
다음의 경우 건너뛰기: 프로젝트가 Python이 아닌 경우; 데이터사이언스와 관련없는 웹/백엔드/인프라 작업입니다. 라이브러리는 이미 선택되어 설치되었으며 작업은 새로운 종속성 없이 그 내부에서 구현됩니다(버그 수정, 기능 작업, 리팩터링).
사용 방법: **무언가를 권장하거나 설치하기 전에 이 SKILL.md를 끝까지 읽으십시오** — 단일 색인 항목에서 선택하면 계층(라이브러리가 필수인지, 사용자 선택인지, 선택인지 또는 이미 전이적으로 존재하는지 여부)과 쌍이 숨겨지며 둘 다 중요합니다. 그런 다음 선택한 라이브러리의 범위와 장단점에 대해 연결된 `references/<library>.md`를 읽어보세요. 한 라이브러리를 다른 라이브러리로 자동으로 대체하지 마십시오. 항목이 맞지 않으면 사용자에게 간격을 표시합니다.
