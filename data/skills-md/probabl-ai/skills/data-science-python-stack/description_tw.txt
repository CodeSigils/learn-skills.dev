用於資料科學/機器學習工作的固定 Python 堆疊－每個作業一個庫，組織成層（強制/使用者選擇/可選/傳遞）。 SKILL.md 是索引；每個庫的 `references/<library>.md` 檔案包含範圍、「何時選擇這個」/「何時選擇其他內容」以及配對。
當（其中任何一個）時觸發：（1）在此堆疊的域中**庫導入失敗** - 答案是安裝，而不是替換（請參閱§“缺少依賴項”）； (2) **必須做出庫選擇** — 顯式（用戶詢問“X 的哪個庫？”）或隱式（代碼即將引入新的依賴項，或者正在構建一個新的數據庫（M使用者或目前程式碼到達堆疊外部的替代品（xgboost、lightgbm、black、isort、flake8、poetry、hatch），或到達「mlflow」記錄參數/指標，或「cross_val_score」+手寫報告－重新導向：追蹤→「skore」項目API，評估/→→「skore」報告「
跳過以下情況：該專案是非 Python 的；該工作是與資料科學無關的網路/後端/基礎設施；該程式庫已經選擇並安裝，任務正在其中實現（錯誤修復、功能工作、重構），沒有新的依賴項在起作用。
如何使用：**在推薦或安裝任何內容之前從頭到尾閱讀此 SKILL.md** - 從單一索引條目中進行選擇會隱藏層（無論該庫是強制性的、用戶選擇的、可選的還是已經傳遞存在）和配對，兩者都很重要。然後閱讀連結的“references/<library>.md”以了解所選庫的範圍和權衡。不要默默地用一個庫替換另一個庫；如果沒有合適的入口，則向使用者顯示間隙。
