决定文件在 ML 实验项目中的位置：可重用代码在 `src/<pkg>/` 中，每个实验一个 `# %%` 脚本在 `experiments/` 中，设计说明 + 索引在 `journal/` 中，报告在 `reports/` 中，仅代理探针在 `scratch/` 中。拥有布局、文件创建规则（每个实验一个文件，编辑前询问）和 jupytext `# %%` 脚本约定。永远不要强加“data/”——用户拥有它。
触发 — 以下任意一项： - 启动新的 ML 项目/搭建工作空间。 - 将在项目中创建第一个实验文件。 - 即将创建 `src/<pkg>/data.py` / `features.py` /
  第一次`pipeline.py`/`evaluate.py`。
- 即将编写一个“.ipynb”进行实验 - 重定向到
  `experiments/` 下的 `# %%` 脚本。
- 用户询问某些东西应该存在于哪里，如何组织
  项目，或如何设置工作区。
- 即将添加新的实验迭代 - 决定新文件与
  编辑现有的（询问用户）。

跳过以下情况：该文件显然是已填充模块的一部分（例如，向现有的“features.py”添加函数）；在单个现有文件中进行纯重构；管道声明机制（`build-ml-pipeline`）；评估机制（`evaluate-ml-pipeline`）； skore 符号查找（`python-api`）。
如何使用：**首先运行下面的检测表** - 如果有任何信号匹配，请遵循现有约定（不要重命名或移动文件夹）。如果没有信号匹配，则搭建默认布局。 **将飞行前清单作为可见文本发出，并在创建或编辑任何文件之前阅读停止条件。** 在“templates/”中使用模板；复制和改编，不要从头开始重写。
