決定檔案在 ML 實驗項目中的位置：可重複使用程式碼在 `experiments/` 中，設計說明 + 索引在 `journal/` 中，報告在 `reports/` 中，僅代理探針在 `scrournal/` 中，報告在 `reports/` 中，僅代理探針在 `scrournal/` `scrscr。擁有版面配置、文件建立規則（每個實驗一個文件，編輯前詢問）和 jupytext `# %%` 腳本約定。永遠不要強加“data/”——使用者擁有它。
觸發 — 以下任一項： - 啟動新的 ML 專案/建置工作空間。 - 將在專案中建立第一個實驗文件。 - 即將建立 `src/<pkg>/data.py` / `features.py` /
  第一次`pipeline.py`/`evaluate.py`。
- 即將編寫一個“.ipynb”進行實驗 - 重定向到
  `experiments/` 下的 `# %%` 腳本。
- 使用者詢問某些東西應該存在於哪裡，如何組織
  項目，或如何設定工作區。
- 即將新增新的實驗迭代 - 決定新文件與
  編輯現有的（詢問使用者）。

跳過以下情況：該文件顯然是已填充模組的一部分（例如，向現有的“features.py”添加函數）；在單個現有文件中進行純重構；管道聲明機制（`build-ml-pipeline`）；評估機制（`evaluate-ml-pipeline`）；
使用方法：**首先運行下面的檢測表** - 如果有任何訊號匹配，請遵循現有約定（不要重命名或移動資料夾）。如果沒有訊號匹配，則建立預設佈局。 **將飛行前清單作為可見文字發出，並在建立或編輯任何文件之前閱讀停止條件。 ** 在「templates/」中使用範本；複製和改編，不要從頭開始重寫。
