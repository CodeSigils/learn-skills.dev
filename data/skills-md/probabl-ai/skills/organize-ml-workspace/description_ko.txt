ML 실험 프로젝트에서 파일이 어디에 있는지 결정합니다. `src/<pkg>/`에 재사용 가능한 코드, `experiments/`에 실험당 하나의 `# %%` 스크립트, `journal/`에 디자인 노트 + 색인, `reports/`에 보고서, `scratch/`에 에이전트 전용 프로브가 있습니다. 레이아웃, 파일 생성 규칙(실험당 하나의 파일, 편집 전에 확인) 및 jupytext `# %%` 스크립트 규칙을 소유합니다. 절대 `data/`를 부과하지 않습니다. 사용자가 이를 소유합니다.
TRIGGER — 다음 중 하나: - 새 ML 프로젝트 시작/작업 공간 스캐폴딩. - 프로젝트의 첫 번째 실험 파일을 생성하려고 합니다. - `src/<pkg>/data.py` / `features.py` /를 생성하려고 합니다.
  처음으로 `pipeline.py` / `evaluate.py`.
- 실험을 위해 `.ipynb`를 작성하려고 합니다.
  `experiments/` 아래의 `# %%` 스크립트.
- 사용자가 어디에 있어야 하는지, 어떻게 정리해야 하는지 묻습니다.
  프로젝트 또는 작업 공간을 설정하는 방법.
- 새로운 실험 반복을 추가하려고 합니다. 새 파일과 새 파일을 결정합니다.
  기존 내용을 편집합니다(사용자에게 문의).

다음의 경우 건너뛰기: 파일이 분명히 이미 채워진 모듈의 일부인 경우(예: 기존 `features.py`에 함수 추가) 단일 기존 파일 내의 순수 리팩터링; 파이프라인 선언 메커니즘(`build-ml-pipeline`); 평가 메커니즘(`evaluate-ml-pipeline`); 점수어 기호 조회(`python-api`).
사용 방법: **먼저 아래의 감지 테이블을 실행**합니다. 신호가 일치하면 기존 규칙을 따릅니다(폴더 이름을 바꾸거나 이동하지 마십시오). 일치하는 신호가 없으면 기본 레이아웃을 스캐폴드합니다. **파일이 생성되거나 편집되기 전에 비행 전 체크리스트를 눈에 보이는 텍스트로 내보내고 중지 조건을 읽으십시오.** `templates/`에서 템플릿을 사용하십시오. 복사하고 적용하세요. 처음부터 다시 작성하지 마세요.
