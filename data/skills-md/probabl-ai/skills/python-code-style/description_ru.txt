Владеет стилем кода Python для этого стека: ruff для lint + format, numpydoc для строк документации. Три обязанности: (1) разместить файл `ruff.toml` проекта из связанного шаблона, как только стек и рабочее пространство будут на месте, (2) запустить ruff для любых файлов Python, которые Клод только что сгенерировал или отредактировал, и (3) контекстуализировать комментарии каждого затронутого файла к проблеме обработки данных - переписать любую оставшуюся часть шаблона / рабочего процесса (имена навыков, шлюзы, бегун, дайджест, ограждения) в краткие, специфичные для проблемы документы, чтобы Зафиксированные пользователем файлы читаются так, как их написал коллега, а не как сгенерированный шаблон. Останавливается на «затронутые файлы проходят `грубую проверку` и документируют проблему, а не процесс».
ТРИГГЕР, когда (любой из этих случаев): (1) файл Python был только что создан или отредактирован с помощью Write/Edit/
    MultiEdit — активируйте этот навык, прежде чем объявить, что задача выполнена.
    запускается ruff И комментарии файла контекстуализируются по отношению к
    проблема;
(2) новое рабочее пространство ML было только что создано
    `organize-ml-workspace` и в проекте нет `ruff.toml`
    у него еще нет корня — скидываем комплектный шаблон;
(3) пользователь спрашивает о линте, формате, стиле строки документации или достижениях
    для `black`/`isort`/`flake8`/`pydocstyle` (перенаправление на
    ruff — канонический линтер стека, принадлежащий
    `data-science-python-stack` уровень 1).

ПРОПУСКАТЬ, когда: проект не Python; единственные правки на этом этапе касаются Markdown/TOML/JSON/YAML; файл находится в каталоге стороннего поставщика, которым пользователь не владеет.
КАК ИСПОЛЬЗОВАТЬ: запустите ruff вручную для файлов, которые вы только что коснулись — не настраивайте для этого перехватчик PostToolUse. **Прочитайте блок «Условия остановки» и отправьте предполетный контрольный список в виде видимого текста в своем ответе — оба обязательны перед запуском ruff.**
