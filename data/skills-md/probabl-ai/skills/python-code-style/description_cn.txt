拥有此堆栈的 Python 代码风格：ruff 用于 lint + 格式，numpydoc 用于文档字符串。三个职责 - (1) 一旦堆栈和工作区就位，就将项目的“ruff.toml”从捆绑模板中放置出来，(2) 对 Claude 刚刚生成或编辑的任何 Python 文件运行 ruff，以及 (3) 将每个涉及的文件的注释与数据科学问题联系起来 - 将任何剩余的模板/工作流程散文（技能名称、门、运行程序、摘要、护栏）重写为简洁的、针对特定问题的文档，以便用户提交的文件读起来像是同事写的，不像生成的脚手架。停在“所触及的文件通过‘ruff check’并记录问题，而不是过程。”
当（其中任何一个）触发：（1）刚刚通过 Write / Edit / 创建或编辑 Python 文件
    MultiEdit — 在声明任务完成之前调用此技能
    ruff 运行并且文件的注释与上下文相关
    问题；
(2) 一个新的 ML 工作区刚刚搭建完成
    `organize-ml-workspace` 并且该项目在以下位置没有 `ruff.toml`
    它的根源——删除捆绑的模板；
(3) 用户询问 lint、格式、文档字符串样式或到达信息
    对于 `black` / `isort` / `flake8` / `pydocstyle` （重定向到
    ruff — 堆栈的规范 linter，拥有者
    `数据科学-python-堆栈` 第 1 层）。

跳过以下情况：该项目是非 Python 的；本轮唯一的编辑是 Markdown / TOML / JSON / YAML；该文件位于用户不拥有的第三方供应商目录中。
如何使用：在您刚刚触及的文件上手动运行 ruff — 不要为此配置 PostToolUse 挂钩。 **阅读“停止条件”块并在响应中将飞行前检查表作为可见文本发出 - 两者在运行 ruff 之前都是强制性的。**
