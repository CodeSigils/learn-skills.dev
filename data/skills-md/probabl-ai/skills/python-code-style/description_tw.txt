擁有此堆疊的 Python 程式碼風格：ruff 用於 lint + 格式，numpydoc 用於文件字串。三個職責 - (1) 一旦堆疊和工作區就位，就將專案的「ruff.toml」從捆綁模板中放置出來，(2) 對 Claude 剛剛生成或編輯的任何 Python 文件運行 ruff，以及 (3) 將每個涉及的文件的註釋與數據科學問題聯繫起來 -將任何剩餘的模板/工作流程散文（技能名稱、門、運行程序、摘要、護欄）重寫為簡潔的、針對特定問題的文檔，以便用戶提交的文件讀起來像是同事寫的，不像生成的腳手架。停在“所觸及的文件通過‘ruff check’並記錄問題，而不是過程。”
當（其中任何一個）觸發：（1）剛剛透過 Write / Edit / 建立或編輯 Python 文件
    MultiEdit — 在宣告任務完成之前呼叫此技能
    ruff 運行並且文件的註釋與上下文相關
    問題；
(2) 一個新的 ML 工作區剛建置完成
    `organize-ml-workspace` 且該專案在以下位置沒有 `ruff.toml`
    它的根源－刪除捆綁的模板；
(3) 使用者詢問 lint、格式、文件字串樣式或到達訊息
    對於 `black` / `isort` / `flake8` / `pydocstyle` （重定向到
    ruff — 堆疊的規格 linter，擁有者
    `資料科學-python-堆疊` 第 1 層）。

跳過以下情況：該專案是非 Python 的；本輪唯一的編輯是 Markdown / TOML / JSON / YAML；該檔案位於使用者不擁有的第三方供應商目錄中。
使用方法：在您剛剛觸及的檔案上手動執行 ruff — 不要為此配置 PostToolUse 掛鉤。 **閱讀「停止條件」區塊並在回應中將飛行前檢查表作為可見文字發出 - 兩者在運行 ruff 之前都是強制性的。 **
