Explicit workflow for creating or regenerating the single-prompt audit loop under project/loops. Use only when the user explicitly invokes this skill or clearly asks to create or regenerate the coverage audit prompt.
