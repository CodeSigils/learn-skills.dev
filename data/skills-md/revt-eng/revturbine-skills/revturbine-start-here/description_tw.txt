整合 RevTurbine 的前門。在 `npm create revturbine` 搭建應用程式支架後立即使用，或每當有人說「新增 RevTurbine」、「設定貨幣化」、「啟動 RevTurbine 整合」或「下一步我要使用 RevTurbine 做什麼」時使用。按順序路由整個第一個會話 - 安裝和提供、運行本地 Playbook、控制一個付費操作，然後上線 - 透過將每個步驟交給重點技能。它排序；它本身不做這項工作。如果應用程式已經限制一項付費操作並且您只需要一個特定步驟（直接呼叫該步驟的技能），請跳過。
