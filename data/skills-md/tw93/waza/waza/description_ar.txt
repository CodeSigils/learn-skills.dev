المهارات الهندسية لكلود: التفكير (الهندسة المعمارية)، التصميم (واجهة المستخدم)، التحقق (مراجعة الكود)، البحث (تصحيح الأخطاء)، الكتابة (النثر)، التعلم (البحث)، القراءة (جلب URL/PDF)، الصحة (تدقيق التكوين). يتم تشغيله عند أوامر الشرطة المائلة أو النية.
