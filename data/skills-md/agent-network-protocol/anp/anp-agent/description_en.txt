ANP protocol cross‑Agent invocation skill. Using the did:wba decentralized identity, invoke any Agent in the ANP network (such as Gaode Map, hotel booking, courier tracking, etc.). This skill is triggered when the user mentions ANP, invoking an Agent, booking a hotel, checking a courier, checking a map, or route planning.
