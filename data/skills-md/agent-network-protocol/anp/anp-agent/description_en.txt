The ANP protocol enables cross-Agent skill invocation. Using the did:wba decentralized identity, it calls any Agent in the ANP network (such as Amap, hotel booking, express delivery inquiry, etc.). This skill is triggered when users mention ANP, invoking an Agent, booking a hotel, checking express delivery, checking maps, or route planning.
