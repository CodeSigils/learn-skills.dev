A practical handbook for Vue SFC in weapp-vite-based mini-program projects, covering `<script setup lang="ts">`, JSON macros, `definePageMeta`/layout, `defineModel`, `usingComponents`, template directive compatibility, `.weapp-vite` type support files, managed `prepare` workflow, and current SFC conventions aligned with the scaffold's `AGENTS.md` and local `dist/docs`.
