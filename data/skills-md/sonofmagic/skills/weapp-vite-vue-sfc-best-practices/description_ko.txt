`<script setup lang="ts">`, JSON 매크로, `definePageMeta`/layout, `defineModel`, `usingComponents`, 템플릿 지시문 호환성, `.weapp-vite` 유형 지원 파일, 관리되는 `prepare` 워크플로 및 스캐폴드의 `AGENTS.md` 및 로컬에 맞춰 조정된 현재 SFC 규칙을 다루는 weapp-vite 기반 미니 프로그램 프로젝트의 Vue SFC에 대한 실용적인 핸드북입니다. `dist/docs`.
