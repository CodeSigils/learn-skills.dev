An end-to-end workflow for WeChat DevTools runtime targeting weapp-vite monorepo layouts. Suitable for `e2e/ide/**`, `miniprogram-automator`, real runtime page assertions, shared automator startup, chaining via `miniProgram.reLaunch(...)`, conditional page maintenance in `project.private.config.json`, and forming a real runtime acceptance pipeline together with `weapp-vite screenshot/compare/ide logs`.
