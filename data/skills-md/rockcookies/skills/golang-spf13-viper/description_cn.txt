使用 spf13/viper 的 Golang 配置库 — 分层优先级（flag > env > file > KV > default）、BindPFlag/BindPFlags、SetEnvPrefix + SetEnvKeyReplacer + AutomaticEnv、ReadInConfig + ConfigFileNotFoundError、Unmarshal + mapstruct 结构标记、Sub 用于子树、WatchConfig + OnConfigChange 用于热重载、viper.New() 用于测试隔离和远程 KV 集成。在使用或采用 spf13/viper 或代码库导入 `github.com/spf13/viper` 时适用。有关 viper 的 CLI 命令结构，请参阅“golang-spf13-cobra”技能。对于一般 CLI 架构，请参阅“golang-cli”。
