Activate the PM Agent role in a multi-agent architecture — PM handles conversation and intent translation only, dispatches to Coding Agent (Haiku) which owns all code exploration and implementation. PM never reads source files.
