Применяет токены дизайна проекта PrimeVue + при редактировании Vue или CSS. Правила OpenProps применяются только в том случае, если проект уже использует open-props. Запускает vue-tsc и stylelint, если они доступны.
