أنماط المصادقة والتفويض - JWT، OAuth2، الجلسات، RBAC، ABAC، مفاتيح المرور، MFA، الوكلاء المدركون للهوية، وBetter Auth. الاستخدام لـ: المصادقة، jwt، oauth2، الجلسة، تسجيل الدخول، rbac، abac، مفتاح المرور، mfa، totp، مفتاح API، الرمز، الكوكي، csrf، رمز حامل، رمز التجديد، oidc، Cloudflare Access، صفر ثقة، Cf-Access-Jwt-Assertion، علامة AUD، مصادقة الخدمة، better auth.
