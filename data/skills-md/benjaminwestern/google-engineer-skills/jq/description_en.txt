MUST USE when parsing, filtering, querying, transforming, or validating JSON with jq. Covers extracting fields from API responses, reshaping JSON documents, filtering arrays and objects, building shell pipelines around JSON, and composing jq expressions. Do NOT use for YAML processing, SQL queries, or general text manipulation unless jq is explicitly part of the task.
