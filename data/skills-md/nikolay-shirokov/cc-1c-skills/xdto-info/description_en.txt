Analysis of the XDTO 1C package structure — types, properties, entry points. Use it as a preparatory step when writing code that creates and populates an XDTO object, parses incoming XML, and also to find out which packages are present in the configuration.
