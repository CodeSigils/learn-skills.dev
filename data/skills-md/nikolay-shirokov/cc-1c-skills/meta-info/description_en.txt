Analysis of the 1C metadata object structure from XML export—attributes, table sections, forms, movements, types. Use it to study object structure (instead of reading XML files directly) and as a preparatory step when writing queries and code that work with objects
