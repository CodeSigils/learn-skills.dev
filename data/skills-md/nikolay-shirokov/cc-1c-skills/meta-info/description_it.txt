Analisi della struttura di un oggetto di metadati 1C da un'esportazione XML: attributi, sezioni tabellari, moduli, movimenti, tipi. Usalo per studiare le strutture degli oggetti (invece di leggere direttamente i file XML) e come passaggio preparatorio quando scrivi query e codice che funzionano con gli oggetti
