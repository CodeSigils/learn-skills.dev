يُستخدم عند إنشاء محول OpenCLI جديد من البداية، أو إضافة دعم لموقع ويب أو نظام أساسي جديد، أو استكشاف نقاط نهاية API الخاصة بالموقع عبر DevTools للمتصفح، أو عندما يطلب المستخدم إنشاء واجهة سطر أوامر (CLI) لموقع ويب تلقائيًا (على سبيل المثال، "帮我生成 xxx.com 的 cli"). يغطي التوليد الآلي، وسير عمل اكتشاف واجهة برمجة التطبيقات (API)، واختيار استراتيجية المصادقة، وكتابة محول TS، والاختبار.
