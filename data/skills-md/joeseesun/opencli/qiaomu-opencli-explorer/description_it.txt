Da utilizzare quando si crea un nuovo adattatore OpenCLI da zero, si aggiunge il supporto per un nuovo sito Web o piattaforma, si esplorano gli endpoint API di un sito tramite DevTools del browser o quando un utente richiede di generare automaticamente una CLI per un sito Web (ad esempio "帮我生成 xxx.com 的 cli"). Copre la generazione automatizzata, il flusso di lavoro di rilevamento API, la selezione della strategia di autenticazione, la scrittura e il test dell'adattatore TS.
