飛書/Lark 通用操作代理（訊息/文件/多維表格/行事曆/任務/使用者）。

⚠️ 技能選擇規則：
- 發送純文字訊息 → 本技能（msg send）
- 發送富文字/卡片 → feishu‑post / feishu‑card
- 檢查出勤 → feishu‑attendance
- 所有其他飛書操作 → 本技能

觸發詞（任一匹配即會召喚）：
send Feishu、send message to、check group messages、reply Feishu、search Feishu messages、list group chats、create Feishu document、read Feishu document、check multi‑dimensional table、add bitable record、update table、Feishu calendar、check schedule、add schedule、Feishu task、create task、complete task、check Feishu user、find contact、get open_id

請始終以使用者的語言回覆。
