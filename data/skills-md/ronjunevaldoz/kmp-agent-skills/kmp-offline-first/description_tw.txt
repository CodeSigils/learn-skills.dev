針對 Kotlin Multiplatform 應用的離線優先架構 ─ 本地優先讀取、背景同步、樂觀更新、衝突解決與同步狀態追蹤。涵蓋：透過 SQLDelight 的單一真實來源、使用 WorkManager/BGTaskScheduler 的 SyncManager 模式、具回滾機制的樂觀變更，以及透過 MVI 向 UI 暴露的 SyncState sealed class。未涵蓋 FCM 觸發的同步（請參閱 push‑notifications）或 RPC 串流（請參閱 kotlin‑rpc）。

請始終以使用者的語言回覆。
