Sollevamento dello stato in Compose Multiplatform: il modello di spostamento dello stato fino all'antenato comune più basso che ne ha bisogno. Copre: componenti componibili con stato e senza stato, il modello di componente controllato (valore + onValueChange), la regola di sollevamento fino alla condivisione, distinzione tra stato dell'interfaccia utente e stato aziendale, quando interrompere il sollevamento e gli errori comuni di sollevamento eccessivo (tutto in ViewModel) e sollevamento insufficiente (stato sepolto che non può essere testato). Zero nuove dipendenze.
