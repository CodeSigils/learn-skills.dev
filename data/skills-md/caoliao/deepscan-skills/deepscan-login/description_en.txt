Handle the DeepScan login and authorization process. Supports QR-code scanning login or direct API key login. Triggered when the user needs to log in, provides an API key, authorization is required before entering the code-scanning page, the token has expired, or on first use.
