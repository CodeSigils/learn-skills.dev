Prisma ORM للوصول إلى قاعدة البيانات وتصميم المخطط والترحيل والاستعلامات في TypeScript/JavaScript. يُستخدم عندما يذكر المستخدم "prisma" أو "مخطط prisma" أو "ترحيل prisma" أو "عميل prisma" أو "prisma studio" أو "قاعدة بيانات ORM" أو "قاعدة بيانات النوع الآمن" أو "بذور prisma" أو أي مهمة متعلقة بـ Prisma.
