Команды менеджера пакетов для macOS (Homebrew) и Linux (apt, yum/dnf). Используйте, когда пользователь упоминает «brew», «homebrew», «apt», «apt-get», «yum», «dnf», «install package», «update packages», «manager package», «brew cask», «PPA», «системные зависимости», «обновить все» или управление системными пакетами.
