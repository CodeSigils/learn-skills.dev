Analyze the source code of the mini‑program project (including compressed/obfuscated code), identify the core business steps, extract network interfaces and JSAPI calls, generate skill subpackages that conform to the wx.modelContext specification (including atomic interfaces and atomic components), and complete the integration of the app.json / project.config.json configurations. Triggered in the following scenarios: transform mini‑program page capabilities into mini‑program AI atomic interfaces, generate the skills/ subpackage code, derive MCP tools from the source project, and generate development‑mode code for mini‑program AI. This task is limited to static generation; once generation is complete, the output must be handed over to wxa‑skills‑validate for validation.
