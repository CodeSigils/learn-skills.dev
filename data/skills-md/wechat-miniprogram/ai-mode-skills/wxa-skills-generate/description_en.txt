Analyze the source code of the mini‑program project (including compressed/obfuscated code), identify core business steps, extract network interfaces and JSAPI calls, generate skill sub‑packages that conform to the wx.modelContext specification (including atomic interfaces and atomic components), and complete the app.json / project.config.json configuration integration. Triggered in the following scenarios: transform mini‑program page capabilities into mini‑program AI atomic interfaces, generate skills/ sub‑package code, derive MCP tools from the source project, generate development‑mode code for mini‑program AI. It is only responsible for static generation; after generation is complete, it must hand over to wxa‑skills‑validate for validation.
