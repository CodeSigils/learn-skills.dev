Diagnose the local "math research stack" — Lean 4 + Mathlib, optional Wolfram Engine, and the MCP servers (`lean-lsp`, `lean-explore`, optional `wolfram`) that wire them into Claude Code. Runs an exhaustive checklist (toolchain versions, Mathlib build cache, MCP wiring, plugin installs) and reports status as a punch list with the smallest recommended next install step. Activate when the user asks "is my math setup ready", "check my Lean / Mathematica install", "verify the math stack", "/freshwater-doctor", or wants to bootstrap a fresh math research environment.
