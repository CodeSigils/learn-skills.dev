This skill is used to parse short video links, supporting the parsing of short videos and image‑text links from multiple mainstream platforms such as Douyin, Kuaishou, Bilibili, etc., and it can automatically extract audio content and transcribe it into text. It is suitable for scenarios that require batch retrieval of video metadata or conversion of video content into text; use this skill in those cases.
