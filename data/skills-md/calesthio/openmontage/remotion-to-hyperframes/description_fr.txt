Portez une composition Remotion (React) existante vers HyperFrames HTML. À utiliser UNIQUEMENT lorsque l'utilisateur demande explicitement de porter/convertir/migrer/traduire une source Remotion. N'utilisez PAS : (a) la création d'une nouvelle composition HyperFrames ; (b) Rémotion mentionnée au passage ; (c) Code de rémotion partagé à titre de référence uniquement ; (d) "même vidéo que ma Remotion" sans demande de migration explicite - traiter comme une nouvelle version. Doute → `/general-video`. Unidirectionnel, Remotion uniquement : pas d'exportation inversée (HyperFrames → Remotion ou tout autre framework), pas de source non-Remotion (After Effects, Framer Motion, plain React/CSS) → hors de portée, recréer via `/general-video`. Signale les modèles non pris en charge (useState, useEffect, async calculateMetadata, bibliothèques React tierces, `@remotion/lambda`) et recommande l'interopérabilité d'exécution plutôt que la traduction avec perte. Vous ne savez pas s'il faut porter ou construire à nouveau, ou seulement une mention passagère de Remotion ? → /hyperframes.
