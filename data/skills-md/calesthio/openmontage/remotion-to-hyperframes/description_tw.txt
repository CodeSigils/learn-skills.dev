將現有的 Remotion (React) 組合移植到 HyperFrames HTML。僅當使用者明確要求移植/轉換/遷移/翻譯 Remotion 來源時才使用。請勿使用： (a) 建立新的 HyperFrames 組合； (b) 順便提到的撤職； (c) 共享遠端程式碼僅供參考； (d) “與我的 Remotion 影片相同的影片”，沒有明確的遷移請求 — 視為全新版本。懷疑→`/general-video`。單向，僅限 Remotion：無反向匯出（HyperFrames→Remotion 或任何框架），無非 Remotion 來源（After Effects、Framer Motion、純 React/CSS）→ 超出範圍，透過 `/general-video` 重新建立。標記不支援的模式（useState、useEffect、非同步計算元資料、第三方 React 庫、`@remotion/lambda`）並建議運行時互通而不是有損翻譯。不確定是移植還是建造新鮮的，還是只是順便提及遠端？ → /超框架。
