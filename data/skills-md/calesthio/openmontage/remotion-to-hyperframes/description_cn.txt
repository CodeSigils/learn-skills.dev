将现有的 Remotion (React) 组合移植到 HyperFrames HTML。仅当用户明确要求移植/转换/迁移/翻译 Remotion 源时才使用。请勿使用： (a) 创作新的 HyperFrames 组合； (b) 顺便提到的撤职； (c) 共享远程代码仅供参考； (d) “与我的 Remotion 视频相同的视频”，没有明确的迁移请求 — 视为全新版本。怀疑→`/general-video`。单向，仅限 Remotion：无反向导出（HyperFrames→Remotion 或任何框架），无非 Remotion 源（After Effects、Framer Motion、纯 React/CSS）→ 超出范围，通过 `/general-video` 重新创建。标记不支持的模式（useState、useEffect、异步计算元数据、第三方 React 库、`@remotion/lambda`）并建议运行时互操作而不是有损翻译。不确定是移植还是构建新鲜的，还是只是顺便提及一下远程？ → /超框架。
