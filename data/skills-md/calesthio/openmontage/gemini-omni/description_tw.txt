使用 Google Gemini Omni Flash（「gemini-omni-flash-preview」）產生並以對話方式編輯短影片。在以下情況下使用：(1) 使用自然語言編輯迭代剪輯而不是重新生成（「使手機不可見，保持其他所有內容相同」），(2) 使用合成音訊、呈現的螢幕文字或時間編碼節拍產生 3-10 秒 720p 剪輯，(3) 使用 <FIRST_FRAME>/<IMAGE_REF_N> 標籤，(3) 使用 <FIRST_FRAME>/<IMAGE_REF_N> 標籤，將參考影像編號，將參考影像的使用專案的 GEMINI_API_KEY/GOOGLE_API_KEY 透過「gemini_omni_video」工具存取 — 與 Imagen 和 Google TTS 相同的金鑰。
