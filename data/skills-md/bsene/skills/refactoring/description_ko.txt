코드 냄새를 감지하고 *내부* 리팩토링 기술(메소드/클래스 추출, 조건 대체, 매개변수 객체 도입, 이름 바꾸기, 이동)을 적용하여 단일 함수, 클래스 또는 파일의 명확성, 테스트 용이성 및 유지 관리성을 향상시킵니다.
TRIGGER when: 사용자가 리팩토링 기회, 코드 냄새, 긴 메소드, 대규모 클래스, 기본 집착, 긴 매개변수 목록, 데이터 덩어리, 기능 부러움, 샷건 수술, 명령문 냄새 전환, 메소드 추출, 클래스 추출, 조건부를 다형성으로 대체, 매개변수 객체 도입, 단일 파일 단순화, 가독성 향상, 정리, 먼저 정리, "이 함수 정리", "이 클래스 리팩터링"을 언급합니다.
다음의 경우에는 사용하지 마십시오: 테스트가 존재하지 않는 경우(블라인드 리팩터링이 안전하지 않음 - 테스트를 먼저 추가하는 경우), 변경 사항이 구조 전용이 아닌 동작 변경인 경우(대신 `tcrdd` 사용), 사용자가 하나의 변경 사항이 여러 호출 사이트에 파급되는 *파일 간* 종속성 그래프를 안전하게 풀어야 하는 경우(대신 `mikado-method` 사용)
