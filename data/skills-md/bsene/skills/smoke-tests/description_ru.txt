Идентификация, написание и интеграция smoke‑тестов в CI для HTTP‑API — только с использованием Hurl или curl. ТРИГГЕР, когда: smoke test, smoke testing, critical path test, CI gate, gate before full suite, write smoke tests, add smoke test, smoke test template, which tests are smoke tests, hurl, .hurl file, curl smoke test. НЕ ИСПОЛЬЗОВАТЬ, когда: пользователю требуется полная стратегия или философия тестирования — используйте `testing` вместо этого.
