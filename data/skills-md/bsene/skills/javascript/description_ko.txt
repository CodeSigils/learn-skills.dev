JavaScript 모범 사례 및 규칙 적용 — 명명 규칙, 최신 구문 관용구, 'this' 처리, 모듈 구조, 배열/반복 성능 및 null 허용 여부. 일반 JavaScript에 적용되며 TypeScript 빌드의 기반입니다.
TRIGGER when: 언어(JavaScript, JS, .js, .mjs, .cjs, Node.js, 브라우저 JS, ES 모듈, ESM, CommonJS), 구문(const/let, 구조 분해, 템플릿 리터럴, 선택적 체이닝, nullish 병합, 화살표 함수), 패턴(이 바인딩, 콜백, 분리된 메서드, 순수 함수 대 메서드, 모듈 가져오기, 배럴 파일, index.js), 성능(배열 체인, filter().map(), 중간 배열, 핫 경로, GC 압력), null 허용 여부(null 대 정의되지 않음, 부재, sentinel 값, == null 확인), 이름 지정(camelCase, PascalCase, kebab-case, 부울 접두사), 질문(관용적인 JavaScript, JS 규칙, JS 모범 사례, JS 코드 검토). TypeScript에도 적용됩니다. 이는 TS 구축의 기반입니다. `.ts`/`.tsx`에서 `typescript`와 함께 로드하세요. 유형별 규칙(유형 어설션 `as T`, 인터페이스, 제네릭, `readonly` 수정자, `as const`, 반환 유형 주석)의 경우 `typescript` 기술이 위에 지침을 추가합니다.
유형 시스템 질문(차별화된 공용체, 제네릭, `as`, 인터페이스, 축소)에 대한 기본 기술로 사용하지 마십시오 → `typescript` / `typescript-type-system`으로 라우팅합니다. 이 기술은 이름 지정 및 JS 관용구에 여전히 적용됩니다.
