Apply an OpenSpec change by grouping pending tasks by scope, delegating implementation to subagents, and keeping the main agent focused on openspec-cn state, context reading, review, and progress reporting.
