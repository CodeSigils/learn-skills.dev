Struttura i documenti di benchmark e di valutazione utilizzando il quadro dei cinque pilastri (gap di ricerca, pipeline di costruzione, quadro di valutazione, risultati empirici, metodo di accompagnamento opzionale). Restituisce un controllo di completezza, una catena logica di introduzione in sei parti, uno scheletro della Sezione 2-7 e un elenco di controllo pre-invio. Da utilizzare quando si scrive un documento di benchmark, si struttura un documento di benchmark, si controlla se un'idea di benchmark è sostanziale, si redige un'introduzione di benchmark o si pianifica la pipeline o gli esperimenti di costruzione dei dati.
