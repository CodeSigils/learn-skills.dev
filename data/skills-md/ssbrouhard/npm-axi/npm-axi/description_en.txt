Use when you need npm registry data — search for packages, check a package's latest version, license, dependency count or download counts, or list recent versions. Returns token-efficient TOON output. Read-only, no auth required.
