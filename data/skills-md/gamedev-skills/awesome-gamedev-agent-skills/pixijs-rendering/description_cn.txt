构建 PixiJS v8 渲染层：创建异步应用程序、使用资源加载纹理、使用容器和 Sprite 组成场景图、驱动股票循环、连线指针事件以及使用渲染组进行组绘制。在构建或调试 PixiJS v8 时使用 - 当用户提到 PixiJS、Pixi、Application、app.stage、Container、Sprite、Assets.load、app.ticker 或 eventMode 时。固定 v8 async init() API。
