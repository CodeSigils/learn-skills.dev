Strumento di diagnostica e controllo dello stato del cluster ByteHouse per esaminare lo stato del cluster ByteHouse, diagnosticare problemi e anomalie del cluster, visualizzare lo stato del nodo del cluster e analizzare i parametri delle prestazioni del cluster. Utilizza questa competenza quando è necessario verificare l'integrità del cluster ByteHouse, diagnosticare problemi e anomalie del cluster, visualizzare lo stato del nodo del cluster o analizzare gli indicatori di prestazione del cluster.
