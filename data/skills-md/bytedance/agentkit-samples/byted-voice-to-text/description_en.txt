Voice-to-Text (ASR). Uses Volcano Engine BigModel ASR for speech recognition, including two modes: Fast Version (≤2h/100MB synchronous quick return) and Standard Version (≤5h asynchronous recognition). Supports Feishu voice messages, local audio files, and audio URLs. This skill is used when receiving voice messages or audio attachments (.ogg/.mp3/.wav).
