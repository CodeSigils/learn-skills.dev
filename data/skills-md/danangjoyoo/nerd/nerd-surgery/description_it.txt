Da utilizzare quando il codice, le integrazioni, l'automazione, l'infrastruttura o il comportamento di runtime sono interrotti, imprevisti, incoerenti o probabilmente implementati in modo errato e l'utente desidera diagnosi, analisi delle cause principali o riparazione.
