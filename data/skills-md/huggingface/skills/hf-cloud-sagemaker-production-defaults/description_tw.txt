创建默认启用自动扩展、CloudWatch 警报和标记的 SageMaker 终端节点（实时或异步）。每當要建立 SageMaker 終端節點、編寫呼叫「create_endpoint」的部署程式碼或在已知映像 URI 和 IAM 角色後完成部署時，請使用此技能。为实时端点提供deploy.py，为异步端点提供deploy_async.py（具有真正的缩放到零支持）。这是 SageMaker 部署工作流程的最后一步。如果沒有這些預設值，切勿產生裸露的「create_endpoint」呼叫 - 沒有自動縮放或警報的端點是演示，而不是部署。
