KI-Demos und GPU-Berechnungen mit Gradio Spaces und Hugging Face Spaces ZeroGPU. Wird verwendet, wenn Sie Code schreiben oder überprüfen, der „@spaces.GPU“ verwendet, „python_version“ oder „requirements.txt“ für einen ZeroGPU-Space konfigurieren oder ZeroGPU-spezifische Codeeinschränkungen handhaben – Pickle-basierte Prozessisolation, „gr.State“-Semantik über die Worker-Grenze hinweg, kein „torch.compile“ (verwenden Sie stattdessen AoTI), nur CUDA-Wheel-Builds (kein „nvcc“ beim Build oder Laufzeit), große vs. xlarge-Größen und aufrufbare Elemente mit dynamischer Dauer. Stellen Sie sicher, dass Sie diesen Skill immer dann verwenden, wenn der Benutzer ZeroGPU, „@spaces.GPU“ oder das Python-Paket „spaces“ erwähnt oder auf ZeroGPU-spezifische Codefehler wie „PicklingError“ über die Worker-Grenze hinaus, „illegale Dauer“ oder „flash-attn“-Wheel-Build-Fehler stößt – auch wenn der Benutzer nicht ausdrücklich nach ZeroGPU-Codierungsanleitungen fragt. Auslösen bei „Import Spaces“ oder „@spaces.GPU“ im Code.
