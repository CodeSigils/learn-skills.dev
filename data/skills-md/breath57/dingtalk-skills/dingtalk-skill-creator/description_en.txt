Create a new DingTalk Skill. Whenever the user mentions phrases like "create a new skill," "new skill," "develop DingTalk skill," "add DingTalk feature," "add DingTalk API support," "I need a DingTalk xxx skill," "DingTalk todo," "DingTalk check-in," "DingTalk attendance," "DingTalk approval," or "DingTalk schedule"—essentially whenever they want to wrap a DingTalk API domain into a reusable skill—this skill must be used. It covers the full creation workflow: SDK exploration → SDK Python test → pure HTTP Python test (both must pass) → write SKILL.md. Do not create the skill until all tests pass.
