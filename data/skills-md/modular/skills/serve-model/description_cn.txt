使用 MAX 的“maxserve”命令服务模型：设置环境（使用 max-nightly conda 通道/nightlywheel 索引的 pixi 或 uv），将服务器指向 Hugging Face 存储库或本地检查点，使用“--custom-architectures”定位自定义架构，并为模型选择正确的服务标志。每当用户想要在 MAX 上运行、启动、启动或托管模型、启动 OpenAI 兼容端点、服务自定义/移植架构、调试“最大服务”启动失败或找出给定模型需要哪些服务标志（设备、量化编码、最大长度、任务、信任远程代码）时，即可使用此功能，即使它们没有按名称说出“最大服务”。
