將 Mojo 程式碼從遺留參數閉包（`capturing[_]`、`@__parameter` / `@parameter`、`api[fn](args)`）遷移到取值統一閉包（`api(args, fn)` 和 `{imm}` / `{mut}` /`{varimm}` / `{mut}` 列表）。在刪除參數重載、修復「捕獲薄」轉換錯誤、重寫巢狀啟動/回調閉包或遷移任何採用 comptime 函數參數的 API 時使用。
