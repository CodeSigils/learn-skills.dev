当 MAX 模型加载、编译、服务和生成令牌但输出与参考实现不一致时，调试静默损坏。当奇偶校验调试在标量抽头上停止、模型返回乱码或错误的贪婪标记、logit 余弦较高但 argmax 不同或生成一致然后发散时使用 - 在架构端口、量化启动、多 GPU 转换期间或 MAX 升级之后。触发“奇偶校验失败”、“静默损坏”、“logits 匹配但令牌发散”、“top-1 不匹配”、“贪婪发散”和“模型提供服务但生成垃圾”。不适用于加载或保留脚手架时的崩溃（使用导入模型）。首先强制引用-vs-MAX 张量转储比较器，在重新编译之前验证数字修复，并在转储匹配但文本分歧时将服务与管道一分为二。
