Harness skill. Can initialize the main closed-loop scaffolding, and continuously decompose and execute tasks, defaulting to sharded task storage (index + buckets); feature_list is only compatible for legacy projects when they exist.
