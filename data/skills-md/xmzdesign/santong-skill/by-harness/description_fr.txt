Exploitez la compétence. Peut initialiser l'échafaudage principal en boucle fermée, et décomposer et exécuter des tâches en continu, avec un stockage de tâches fragmenté (index + compartiments) par défaut ; feature_list n'est compatible et conservé que lorsque des projets existants existent.
