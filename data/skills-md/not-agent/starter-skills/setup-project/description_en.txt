For each project, set the default values for the local document location that Starter Skills will read, the execution command, the access method, and the Starter Skills sections of AGENTS.md and CLAUDE.md. If you just enter `/setup-project`, it immediately uses the default settings, and if you enter `/setup-project <description>`, it incorporates the description.
