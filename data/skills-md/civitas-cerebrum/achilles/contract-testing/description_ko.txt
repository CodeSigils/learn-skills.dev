쓰기, 실행, 검토 또는 호출 등 테스트에서 백엔드 HTTP 엔드포인트가 사용될 때마다 사용하세요. 이 기술은 모든 API 상호 작용이 생성해야 하는 구조화된 형태(상태 코드 예상, 응답 스키마 어설션, 오류 봉투 유효성 검사, 헤더 적용 범위)를 소유합니다. 이는 명시적 의도 트리거(계약 테스트, 스키마 테스트, Pact, OpenAPI 적합성, API 잠금 계약, API 형태 확인, 주요 변경 감지, 소비자 중심 계약, 공급자 확인, API 호환성, 사양 적합성, API가 변경되지 않았는지 확인) 및 `steps.apiGet`, `steps.apiPost`, `steps.apiPut`, `steps.apiDelete`, `steps.apiPatch`를 호출하는 모든 테스트 또는 계획된 테스트에서 자동 활성화됩니다. `steps.apiHead`, `steps.verifyApiStatus` 또는 `steps.verifyApiHeader` — UI 흐름 사양 내에서도 마찬가지입니다. 이론적 근거: 모든 백엔드 호출에는 최소한 상태 코드 + 오류 봉투 어설션이 필요합니다. 이것이 없으면 엔드포인트가 회귀할 때 테스트가 자동으로 통과됩니다. 또한 "테스트에서 API 호출", "백엔드에 대해 테스트", "API 사용", "응답 확인", "JSON 형태 확인", "API 응답 유효성 검사", "백엔드 응답 잠금", "엔드포인트가 반환되어야 함" 및 사양 컨텍스트 내의 프로젝트 URL에 대한 HTTP 동사에 대한 언급과 같이 백엔드 상호 작용이 시작될 것임을 알리는 문구에서 활성화됩니다. 실제 경로: 요소 상호 작용의 동반 기술 테이블(여기에서 계약 의도를 라우팅), 직접적인 사용자 의도 및 테스트 작성기의 3단계 구현 규칙(계약 형태를 선택 사항으로 처리하지 않고 변형이 `steps.api*` 행을 내보낼 때마다 기술 도구를 통해 이 기술을 호출함). 대상이 아닌 경우: 순수 UI 흐름 어설션, 여정 범위의 적용 범위 확장, 페이지 수준의 버그 찾기 또는 테스트 실패 진단 — 해당 기술은 자체 기술로 전달됩니다.
