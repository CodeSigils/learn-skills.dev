每當測試中使用後端 HTTP 端點時使用 - 編寫、運行、審查或只是呼叫。此技能擁有每個 API 互動應產生的結構化形狀：狀態代碼期望、回應模式斷言、錯誤信封驗證、標頭覆蓋。它會在明確意圖觸發器（合約測試、模式測試、契約、OpenAPI 一致性、鎖定 API 合約、驗證 API 形狀、破壞性變更偵測、消費者驅動合約、提供者驗證、API 相容性、規格一致性、確保 API 未變更）以及呼叫「steps.apiGet」、「steps.apiPost」、「任何一個測試.apips.apips. `steps.apiHead`、`steps.verifyApiStatus` 或 `steps.verifyApiHeader` — 即使在 UI 串流規格中也是如此。基本原理：每個後端呼叫至少應該有一個狀態代碼+錯誤信封斷言；如果沒有這些，當端點回歸時，測試會默默地通過。也會在表示後端互動即將落地的短語上啟動：「在測試中呼叫 API」、「針對後端的回應」、「使用 API」、「驗證回應」、「斷言 JSON 形狀」、「驗證 API 回應」、「鎖定後端的回應」、「端點應該返回」以及針對規範上下文內的項目 URL 的任何 HTTP 動詞提及。真實路由：element-interactions 的伴隨技能表（在此處路由契約意圖）、直接用戶意圖和 test-composer 的 Step-3 實現規則，每當變體發出“steps.api*”行時，該規則就會通過技能工具調用此技能，而不是將契約形狀視為可選。不適合：純粹的 UI 流斷言、旅程範圍的覆蓋範圍擴展、頁面級別的錯誤搜尋或失敗測試診斷 - 這些都取決於他們自己的技能。
