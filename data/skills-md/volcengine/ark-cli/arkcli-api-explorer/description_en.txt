arkcli Raw API Explorer: invoke a registered Action as a fallback capability for product commands. Use it when the existing `arkcli <domain> <verb>` cannot meet the requirement, or when you need to validate the underlying Action contract.
