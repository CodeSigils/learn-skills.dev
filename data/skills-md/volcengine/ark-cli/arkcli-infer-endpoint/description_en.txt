arkcli inference endpoint **management** capabilities: handling **existing** Endpoints for retrieval, listing, starting, stopping, deleting, and updating (lifecycle management + start/stop + destroy). Prefer using the product command `arkcli infer endpoint ...` rather than calling the Raw API directly. **Anti‑trigger (important):** when a user says “create / new / create / deploy an endpoint / access point,” any intention to create a new endpoint must go through arkcli‑deploy (`+deploy`), not use this skill’s `infer endpoint create`; this skill’s `create` is only for scripted/CI scenarios that need unguarded, predictable raw CRUD actions (no reuse checks, no example rendering) and explicitly require bypassing the +deploy workflow guardrails. For TTS/ASR/voice models, even raw create should not be prompted—redirect to models search to indicate that arkcli does not support Endpoint creation.
