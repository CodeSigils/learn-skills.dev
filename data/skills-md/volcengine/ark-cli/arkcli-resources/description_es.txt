arkcli resources 实时控制面查询：按 perfil.tipo 派发，列出当前/指定 perfil 下可用的 punto final/plan 模型 ID 列表。solo lectura，不写 perfil.yaml。cambiar valor predeterminado 走`arkcli perfil set-default` y `+deploy --set-default`。
