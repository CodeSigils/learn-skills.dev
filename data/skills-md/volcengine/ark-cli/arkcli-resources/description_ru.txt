arkcli resources 实时控制面查询：按 Profile.type 派发，列出当前/指定 Profile 下可用的 конечная точка/план 模型 ID 列表。только для чтения，不写Profile.yaml。изменить значение по умолчанию 走 `arkcli Profile set-default` 或 `+deploy --set-default`。
