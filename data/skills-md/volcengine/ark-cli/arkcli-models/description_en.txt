arkcli model query capability: list, search, and retrieve details of Volcano public foundation models. Prefer using the product command `arkcli models ...` rather than calling the raw API directly. Note: queries/management of self‑trained or fine‑tuned custom models (`cm‑xxx`) under the account should use arkcli‑custommodel; this skill only covers the public foundation model catalog. Voice/TTS/ASR/podcast/timbre/real‑time voice interaction models only support plaza search and selection descriptions, and should not invoke +chat, +gen, +deploy, +code‑example, usage, pricing, onboard, or auth apikey.
