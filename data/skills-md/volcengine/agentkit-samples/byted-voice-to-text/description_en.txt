Speech-to-Text (ASR). Uses the Volcano Engine BigModel ASR to recognize speech, offering two modes: Express (≤2h/100MB, synchronous fast return) and Standard (≤5h, asynchronous recognition). Supports Feishu voice messages, local audio files, and audio URLs. Use this skill when receiving voice messages or audio attachments (.ogg/.mp3/.wav).
