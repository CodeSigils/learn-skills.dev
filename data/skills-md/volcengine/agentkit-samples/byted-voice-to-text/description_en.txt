Speech-to-text (ASR). Uses Volcano Engine BigModel ASR to recognize speech, offering two modes: Express (≤2h/100MB, synchronous fast return) and Standard (≤5h, asynchronous recognition). Supports Feishu voice messages, local audio files, and audio URLs. This skill is triggered when a voice message or audio attachment (.ogg/.mp3/.wav) is received.
