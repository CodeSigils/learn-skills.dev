Competenze sui container end-to-end che abbracciano immagini OCI, runtime e operazioni: progettazione di immagini, ottimizzazione Dockerfile/Containerfile, Docker Engine e Compose, Podman (rootless, pod, Quadlet), containerd e CRI, debug, rafforzamento della sicurezza e scansione CI. Da utilizzare durante la creazione o la revisione di immagini di contenitori, la scelta di Docker vs Podman, la risoluzione dei problemi di contenitori o pod, l'ottimizzazione di livelli e cache, l'utilizzo di nerdctl/ctr, runtime dei nodi Kubernetes, Containerfile, docker-compose, podman-compose, controlli di integrità o quando l'utente menziona contenitori, OCI, rootless, BuildKit o catena di fornitura di immagini.
