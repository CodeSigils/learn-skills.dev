Guides correct Zig 0.15.x programming, build system configuration, and standard library usage. Covers breaking API changes from prior versions (ArrayList, I/O rewrite, build.zig, Ed25519, JSON, HTTP client, @typeInfo enum casing, usingnamespace removal). Use when writing Zig code, debugging Zig compiler errors, configuring build.zig, or when the user mentions Zig, zig build, zig test, or Zig standard library.
