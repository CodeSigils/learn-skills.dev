Manage global tool permission configurations for Claude Code, automatically add MCP commands or other tools to allowedTools to avoid manual approval each time they are used. Workflow: confirm the command the user wants to add → confirm the addition level (default global ~/.claude.json) → execute the addition → verify and remind to restart.
