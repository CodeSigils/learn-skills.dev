Manage Claude Code’s global tool permission configuration, automatically add MCP commands or other tools to allowedTools to avoid manual approval each time they’re used. Workflow: confirm the command the user wants to add → confirm the addition level (default global ~/.claude.json) → execute the addition → verify and prompt for a restart.
