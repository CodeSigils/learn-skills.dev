System file cleaning tool. Scans and identifies large files and junk files (temporary files, caches, logs, backups, etc.), provides an interactive cleaning interface for users to select deletions. Use this capability when the user needs to free up disk space, organize system files, locate large files, delete junk files, or release storage space.
