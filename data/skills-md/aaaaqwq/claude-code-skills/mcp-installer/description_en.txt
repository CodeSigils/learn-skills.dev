Search GitHub and automatically install and configure the MCP (Model Context Protocol) server tool into the Claude configuration file. Trigger this skill when the user needs to install the MCP tool. Workflow: search for the MCP project on GitHub → extract npx configuration → add to ~/.claude.json → handle API keys (if any).
