Search GitHub and automatically install and configure the MCP (Model Context Protocol) server tool into the Claude configuration file. This skill is triggered when the user needs to install the MCP tool. Workflow: search for the MCP project on GitHub → extract the npx configuration → add it to ~/.claude.json → handle the API key (if any).
