Manage the global tool permission configuration for Claude Code, automatically add MCP commands or other tools to allowedTools, avoiding the need for manual approval each time they are used. Workflow: confirm the command the user needs to add → confirm the addition level (default global ~/.claude.json) → perform the addition → verify and prompt a restart.
