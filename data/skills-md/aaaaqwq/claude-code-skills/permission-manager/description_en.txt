Manage Claude Code's global tool permission configuration, automatically adding MCP commands or other tools to allowedTools to avoid manual approval every time they are used. Workflow: confirm the command the user needs to add -> confirm the addition level (default global ~/.claude.json) -> execute the addition -> verify and remind to restart.
