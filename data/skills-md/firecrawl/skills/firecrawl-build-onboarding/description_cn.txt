将 Firecrawl 凭据和 SDK 设置获取到项目中。当应用程序需要“FIRECRAWL_API_KEY”时，当代理应将 Firecrawl 添加到“.env”时，当用户想要对应用程序代码验证 Firecrawl 时，或者为新的 Firecrawl 集成选择第一个 SDK 和文档时，请使用。如果任务在当前会话期间是实时网络工作，则将其移交给“firecrawl/cli”。该技能包括其自己的浏览器身份验证流程，因此它不依赖于网站登录技能。
