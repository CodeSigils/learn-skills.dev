將 Firecrawl 憑證和 SDK 設定取得到專案中。當應用程式需要「FIRECRAWL_API_KEY」時，當代理程式應將 Firecrawl 新增至「.env」時，當使用者想要對應用程式程式碼驗證 Firecrawl 時，或為新的 Firecrawl 整合選擇第一個 SDK 和文件時，請使用。如果任務在目前會話期間是即時網路工作，則將其移交給「firecrawl/cli」。該技能包括其自己的瀏覽器身份驗證流程，因此它不依賴網站登入技能。
