Da utilizzare quando l'utente desidera generare un video promozionale renderizzato (non solo uno script) per una PR, una funzionalità o una modifica al prodotto: produce una composizione HTML HyperFrames, esegue l'iterazione con `npx hyperframes Preview` e genera mp4 + poster per X/LinkedIn/social.
