Integrate “externally delivered image service applications” — server‑side code not in this repo, delivered as Docker images (e.g., knowledge base, multi‑modal parsing, asynchronous task gateway, etc.). For any task that involves writing integration contracts for an external service or app.manifest.json, register‑app/provisioning registration wiring, /svc routing and health checks, the external service’s service accounts and scopes, one‑box local debugging, or reviewing an external team’s deliverables, be sure to use this skill first — even if the user only says “bring the XX service in.” Use when integrating an externally‑built docker‑delivered service app into the portal: manifest, register‑app, provisioning, /svc wiring, one‑box debugging, or reviewing an external team’s delivery.
