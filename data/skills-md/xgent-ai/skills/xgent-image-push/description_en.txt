Build the app image and push it to a self‑hosted private Harbor image registry (the address is provided by local configuration and is not written in this skill). When users mention releasing, deploying a new version, publishing an image, pushing an image, docker push, tagging and pushing to the repository, configuring CI image build and push steps, private image registry, Harbor, a robot account that can’t push, a push that hangs and can’t proceed, or the push stage before an ImagePullBackOff, they all use this skill— even if they only say “release a version” or “push this service’s image” without mentioning the repository name. Before pushing, it automatically pre‑checks four hard constraints that would cause the pipeline to fail silently (linkage, image architecture, immutable tag, retention policy); bypassing it and doing a direct docker push easily hits one of those constraints.
