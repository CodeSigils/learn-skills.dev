Build the app image and push it to a self‑hosted private Harbor image repository (the address is provided by local configuration and is not written in this skill). Whenever users mention releasing a version, deploying a new version, publishing an image, pushing an image, docker push, tagging and pushing to the repository, configuring CI image‑build‑push steps, a private image repository, Harbor, a robot account that can’t push, a push that gets stuck, or ImagePullBackOff before the push stage, they all use this skill — even if they only say “release a version” or “push this service’s image” without specifying a repository name. It automatically pre‑checks four hard constraints that would cause the pipeline to fail silently before pushing (linkage, image architecture, immutable tag, retention policy); bypassing it and doing a raw docker push easily triggers one of them.
