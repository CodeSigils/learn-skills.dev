Soluzione .NET e convenzioni sulla struttura del progetto. Copre il formato .slnx, Directory.Build.props, Directory.Packages.props per la gestione centralizzata dei pacchetti, gli usi globali e le convenzioni di denominazione. Carica questa competenza quando imposti una nuova soluzione, aggiungi progetti, configuri le proprietà di compilazione o quando l'utente menziona "struttura della soluzione", ".slnx", "Directory.Build.props", "gestione centrale dei pacchetti", "Directory.Packages.props", "utilizzazioni globali", ".editorconfig", "layout del progetto" o "convenzioni di denominazione".
