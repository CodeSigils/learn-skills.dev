Revisione del codice indipendente utilizzando una nuova sessione interattiva di Claude Code come revisore. DA USARE QUANDO l'utente dice "dev-review-claude", "/skill:dev-review-claude", "revisione Claude indipendente" o desidera una revisione della seconda opinione basata su Claude di un piano, build (diff non impegnato), percorso di file/directory o proposta. Accetta flag di modello facoltativi `--claude` / `--opus` / `--sonnet` e un argomento di destinazione (`plan`, `build`, `proposal` o un percorso).
