使用全新的互動式 Claude Code 會話作為審閱者進行獨立程式碼審查。當使用者說“dev-review-claude”、“/skill:dev-review-claude”、“獨立的 Claude 審查”，或者想要對計劃、構建（未提交的差異）、文件/目錄路徑或提案進行 Claude 支持的第二意見審查時使用。接受可選的 `--claude` / `--opus` / `--sonnet` 模型標誌和目標參數（`​​plan`、`build`、`proposal` 或路徑）。
