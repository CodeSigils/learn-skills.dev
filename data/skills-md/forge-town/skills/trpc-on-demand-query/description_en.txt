Without introducing GraphQL, add include (on-demand loading of related fields) and fields (response field trimming) parameters to the tRPC interface, enabling selective querying, reducing excessive data transfer and parsing costs, and lowering frontend type maintenance complexity.
