Use Zod Schema instead of traditional DTO, define table structure Schema and business view Schema, support type derivation, runtime validation, and front-end and back-end contract sharing. Suitable for data structure standardization across Service, Repository, and Controller layers.
