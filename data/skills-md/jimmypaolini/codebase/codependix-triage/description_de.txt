Reagiere auf einen fehlgeschlagenen Codependix‑Lauf oder einen, der Drift meldete — ein --check, der veraltete Exporte meldete, ein Projekt, dem der Export misslang, ein fehlender Ankerblock, ein NestJS‑Container, der nicht starten konnte, ein abgelehnter Befehlszeilenaufruf oder ein --write, das überhaupt keine Dateien erzeugte. Verwende dies, wenn Codependix mit einem Nicht‑Null‑Status beendet wird, wenn ein committeter Mermaid‑Block nicht mit einem frischen Lauf übereinstimmt, wenn ein Graph‑Export für ein Projekt fehlt, aber nicht für andere, oder bevor du ein exportiertes Diagramm von Hand bearbeitest, um einen Check zu bestehen.
