Agisci su un'esecuzione di codependix che è fallita o ha segnalato deriva — un --check che ha segnalato esportazioni obsolete, un progetto che non è riuscito a esportare, un blocco di ancoraggio mancante, un contenitore NestJS che non è riuscito ad avviarsi, una riga di comando rifiutata o un --write che non ha prodotto alcun file. Usalo quando codependix termina con valore di uscita diverso da zero, quando un blocco Mermaid committato non corrisponde a una nuova esecuzione, quando l'esportazione di un grafico è assente per un progetto ma non per gli altri, o prima di modificare manualmente un diagramma esportato per far superare un controllo.
