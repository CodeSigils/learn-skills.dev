运行 callidescope 并阅读它输出的内容——整个工作区的跟踪，或对一个或多个可调用对象执行深度和广度命令，每个对象以 file#qualified-name 形式标识。使用场景包括：运行 callidescope 或 npx callidescope 时；阅读调用栈时；模块展开行时；广度行时；或可能放错位置的行时；当深度以 “≥ n” 打印需要解释时；阅读已提交的 markdown 报告、mermaid 图或 JSON 报告时；以及在询问谁调用它、它调用什么、这个重命名会影响哪些内容、以及在重构开始前应如何拆分该可调用对象时。
