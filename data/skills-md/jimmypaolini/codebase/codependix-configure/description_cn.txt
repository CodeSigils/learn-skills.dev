编写或编辑 `codependix.config.ts` —— 指定运行哪些图类型、每个项目的覆盖设置、包含和排除的 glob 模式、整个工作区的图以及每个导出的位置。

在以下场景使用：工作区尚未拥有 codependix 配置时、运行未产生任何输出因为所有内容都解析为目标 none 时、添加 JSON 或 Markdown 目标时、在锚点块和独立文件之间进行选择时、因缺少目标而被拒绝的配置时，或者决定一个配置是否能够描述工作区中所有项目时。

请始终使用用户的语言作答。
