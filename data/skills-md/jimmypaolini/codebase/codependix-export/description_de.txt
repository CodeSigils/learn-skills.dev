Führen Sie einen Codependix‑Abhängigkeitsgraph‑Export aus, wählen Sie zwischen --check und --write, richten Sie einen Lauf auf das Arbeitsbereichs‑Wurzelverzeichnis oder eine Konfigurationsdatei aus oder lesen Sie einen von ihm erzeugten Mermaid‑Block bzw. JSON‑Graphen. Verwenden Sie ihn, wenn Sie codependix oder npx codependix ausführen, wenn ein Lauf mit Status 0 endet, ohne etwas geschrieben zu haben, wenn Sie nach einem Schalter suchen, der einen Graph‑Typ auswählt, wenn Sie codependix in einen CI‑Schritt einbinden oder wenn Sie einen exportierten Nx Neighborhood, NestJS‑Modul‑Graphen oder Datei‑Import‑Graphen lesen. Deckt den Kommandozeilen‑Host direkt ab, ohne einen Task‑Runner vorauszusetzen.
