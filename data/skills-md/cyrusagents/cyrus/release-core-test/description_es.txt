Invoque cuando el desarrollador pruebe un cambio de Cyrus que abarca CYPACK (edgeworker + CLI) y CYHOST (GUI alojada en Vercel) y la GUI alojada debe apuntar a un `cyrus-core` inédito de este repositorio. Publica `cyrus-core` (y `claude-runner` si es necesario) como una versión preliminar `-test.N` bajo la etiqueta dist npm `test` para que CYHOST pueda instalarlo a través de `cyrus-core@test` sin afectar la etiqueta dist `latest` ni enviar una versión real.
