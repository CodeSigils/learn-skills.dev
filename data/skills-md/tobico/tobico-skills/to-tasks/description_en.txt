Break a plan, spec, or PRD into sequential local task files under .tasks/, checked into the repo. Use when user wants to convert a plan into a local task backlog, break a feature into sequential chunks, or set up work without GitHub issues. Companion to /next-task.
