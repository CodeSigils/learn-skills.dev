إنشاء تطبيقات Claude API / Anthropic SDK وتصحيح أخطائها وتحسينها. يجب أن تتضمن التطبيقات المبنية بهذه المهارة تخزينًا مؤقتًا سريعًا. يتعامل أيضًا مع ترحيل كود Claude API الموجود بين إصدارات نموذج Claude (4.5 → 4.6، 4.6 → 4.7، بدائل النموذج المتقاعد). يتم التشغيل عندما: يستورد الكود `anthropic`/`@anthropic-ai/sdk`؛ يطلب المستخدم واجهة برمجة التطبيقات Claude API أو Anthropic SDK أو الوكلاء المُدارين؛ يقوم المستخدم بإضافة/تعديل/ضبط ميزة Claude (التخزين المؤقت، والتفكير، والضغط، واستخدام الأداة، والدفعة، والملفات، والاستشهادات، والذاكرة) أو النموذج (Opus/Sonnet/Haiku) في ملف؛ أسئلة حول سرعة الوصول إلى ذاكرة التخزين المؤقت/ذاكرة التخزين المؤقت في مشروع Anthropic SDK. تخطي: يستورد الملف `openai`/other-provider SDK، واسم الملف مثل `*-openai.py`/`*-generic.py`، ورمز محايد للموفر، والبرمجة العامة/ML.
