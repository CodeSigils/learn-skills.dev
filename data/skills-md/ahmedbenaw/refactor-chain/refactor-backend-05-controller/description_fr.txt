Utilisez cette compétence lors de l'organisation de la couche de contrôleur d'un service Web backend dans n'importe quelle pile détectée par le registre - en divisant les contrôleurs en un sous-package d'API externe et un sous-package d'API interne (noms configurables, par exemple `external/`/`internal/`), avec un placement décidé par des annotations de contrôleur actives et une chaîne de classification déterministe à trois niveaux, jamais par des noms de fichiers ou des conjectures. Déclencheurs sur des expressions telles que « vérification de la couche de contrôleur », « correction de la couche de contrôleur », « diviser les contrôleurs externes et internes », « organiser les contrôleurs par audience d'API ». Il s'agit de l'étape 5 sur 9 de la voie de gouvernance back-end en couches du bundle refactor-chain. Fonctionne sur n'importe quelle base de code backend dans n'importe quel langage détecté par le registre ; aucune règle verrouillée par le produit, l’organisation ou la langue.
