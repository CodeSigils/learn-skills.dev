当重构链需要知道“这个项目自己的规则是什么？”时使用此技能。 ——代码已经遵循的结构约定（需要依赖注入、命名约定、模块/包边界、分层规则，如控制器→服务→存储库、非循环依赖）。它将这些规则提取到规则清单中，然后在更改之前（基线）和之后（漂移检查）强制执行它们，因此现代化步骤永远不会悄悄地违反项目自己的架构。这是重构链管道的诊断/实操护栏。触发短语包括“提取项目规则”、“此存储库遵循哪些约定”、“确保重构尊重我们的架构”、“我们是否违反了层规则”、“强制 DI/命名/模块边界”、“检查分层违规”。与回购无关；提取时为只读，报告违规时为建议。
