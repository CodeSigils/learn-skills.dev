Optimise la qualité du code d'ingénierie Java sur les classes `@Service` et `@Repository`, en adaptant l'ensemble de règles à la gamme de produits détectée. Utilisez cette compétence lorsque l'utilisateur demande « optimiser le code du module », « corriger l'injection SQL », « améliorer la journalisation », « nettoyer le code », « exécuter l'étape 9 » ou « optimiser la révision du code » sur un service Java Maven. Il s'agit de l'étape 9 sur 9 de la voie d'architecture en couches Java dans le bundle de chaîne de refactorisation et sélectionne l'ensemble de règles de la ligne de produits dans `pom.xml` `<groupId>` (`grp.pt`, `grp.budget`, `gfmis.bgtex` / `com.ctjsoft.gfmis`).
