Utilizza questa competenza quando è necessario riordinare il livello di accesso ai dati di un servizio backend: le frasi di attivazione includono "pulizia DAO", "controllo del livello DAO", "correzione del pacchetto dao", "divisione del mapping e dell'entità", "consolidare le classi del modello" o "controllo/correzione del passaggio 3". Corregge la denominazione `imp`→`impl`, separa le interfacce DAO dalle implementazioni e i mappatori dalle entità utilizzando una tabella deterministica di nome file e posizione (il contenuto del file non viene mai letto per classificarlo) e consolida le classi del modello nel modulo del modello condiviso spostando fisicamente i file mantenendo le dichiarazioni dei pacchetti identiche ai byte: zero import churn. Passaggio 3 di 9 della corsia di governance del backend a più livelli nel bundle della catena di refactoring; funziona su qualsiasi progetto backend a più livelli (sistema di compilazione rilevato dal registro); supporta solo check, solo correzione o check-then-fix.
