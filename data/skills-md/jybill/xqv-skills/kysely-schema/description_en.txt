When the user provides MySQL DDL, CREATE TABLE statements, table-schema screenshots, or requests to fill in Kysely database types, DB interfaces, Insertable/Updateable types, bulk inserts, bulk updates, upserts, or query/modify methods, use this skill. The emphasis is on accurately converting the DDL into this repository’s Kysely types and corresponding service methods, avoiding errors with ToGenerated, null/default handling, and camelCase/snake_case mapping.
