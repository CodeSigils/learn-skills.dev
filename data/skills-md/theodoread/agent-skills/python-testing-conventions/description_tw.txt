在編寫或重構 Python 測試時使用 - 決定測試在停止可讀之前應進行多少重複，固定裝置應覆蓋哪些內容以及範圍，何時參數化而不是編寫另一個測試，依賴項是否應該加倍或真正運行，以及哪些內容屬於快速預設套件而不是較慢的標記層。也適用於 pytest 細節：固定裝置、conftest 放置、參數化 id、標記以及防止套件寫入真實的主目錄。給出每個問題的預設答案而不是評估，因此選擇在項目之間保持一致，而不是在會話之間漂移，並且每個條目都表明它是否覆蓋模型自身的本能或只是確認它。
