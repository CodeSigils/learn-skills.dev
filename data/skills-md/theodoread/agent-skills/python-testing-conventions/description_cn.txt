在编写或重构 Python 测试时使用 - 决定测试在停止可读之前应进行多少重复，固定装置应覆盖哪些内容以及范围，何时参数化而不是编写另一个测试，依赖项是否应该加倍或真正运行，以及哪些内容属于快速默认套件而不是较慢的标记层。也适用于 pytest 细节：固定装置、conftest 放置、参数化 id、标记以及防止套件写入真实的主目录。给出每个问题的默认答案而不是评估，因此选择在项目之间保持一致，而不是在会话之间漂移，并且每个条目都表明它是否覆盖模型自身的本能或只是确认它。
