当被要求审核、测量或重新检查代理会话如何使用 Bash 工具时使用 - 命令链（&&、;、|）、cd 到会话自己的存储库、头/尾截断、sed -n/cat/heredoc 而不是读取/编辑、git commit/push 在链内或在 git -C 之后 - 或者在决定是否是权限提示、白名单规则、~/AGENTS.md Bash 规则或权限模式时使用（acceptEdits 与 auto）需要改变，并且需要来自真实记录的证据而不是直觉。在 ~/.claude/projects/*.jsonl 上运行 stdlib 脚本，打印每个模型和每个会话的速率以及样本，并进行过时的研究，解释每个模式发生的原因以及修复所属的位置。也是记录新发现的 Bash 反模式的地方，以便下一次审计对其进行测量。
