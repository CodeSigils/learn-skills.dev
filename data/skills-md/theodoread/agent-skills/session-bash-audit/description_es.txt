Úselo cuando se le solicite auditar, medir o volver a verificar cómo las sesiones del agente usan la herramienta Bash: encadenamiento de comandos (&&, ;, |), cd en el propio repositorio de la sesión, truncamiento inicial/final, sed -n/cat/heredoc en lugar de Leer/Editar, git commit/push dentro de cadenas o detrás de git -C, o al decidir si se trata de una solicitud de permiso, una regla de lista de permitidos, una regla ~/AGENTS.md de Bash o el modo de permiso. (acceptEdits vs auto) necesita cambios y quiere evidencia de transcripciones reales en lugar de una corazonada. Ejecuta un script stdlib sobre ~/.claude/projects/*.jsonl, imprime tasas por modelo y por sesión, además de muestras, y lleva la investigación fechada que explica por qué ocurre cada patrón y dónde pertenece la solución. También es el lugar para registrar un antipatrón Bash recién detectado para que la próxima auditoría lo mida.
