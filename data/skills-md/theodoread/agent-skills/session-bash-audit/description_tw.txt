當被要求審核、測量或重新檢查代理會話如何使用 Bash 工具時使用 - 命令鏈（&&、;、|）、cd 到會話自己的存儲庫、頭/尾截斷、sed -n/cat/heredoc 而不是讀取/編輯、git commit/push 在鏈內或在 git -C 之後 - 或在決定是否為提示/權限 与 auto）需要改变，并且需要来自真实记录的证据而不是直觉。在 ~/.claude/projects/*.jsonl 上執行 stdlib 腳本，列印每個模型和每個會話的速率以及樣本，並進行過時的研究，解釋每個模式發生的原因以及修復所屬的位置。也是记录新发现的 Bash 反模式的地方，以便下一次审计对其进行测量。
