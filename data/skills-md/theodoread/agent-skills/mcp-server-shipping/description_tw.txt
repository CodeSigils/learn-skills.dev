在建置、安裝或註冊個人 MCP 伺服器時使用 - 新增其「[project.scripts]」入口點，從本機簽出或直接從 git 使用「uv tool install」安裝它，而不是發佈到 PyPI，使用「claude mcp add」註冊並選擇本機/專案/使用者範圍，在可編輯的開發安裝和已發佈的註冊之間dev-env.setup」、「inv」） Quality.precommit`，為什麼自動化需要`uv run inv`）來產生這些伺服器的儲存庫。
