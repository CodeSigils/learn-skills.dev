Tutto ciò che riguarda il livello dati: scegli uno dei sei modelli di backend pronti per l'esecuzione (TanStack Start + Drizzle + better-auth, Hono + Drizzle + better-auth, Hono + Prisma + better-auth, Hono + Drizzle + Auth.js/NextAuth, FastAPI + SQLAlchemy + JWT, Supabase) e collega il frontend ad esso attraverso i collegamenti Repository + AuthProvider; impalcatura di una risorsa CRUD (tabella Postgres + server fns + query hook + pagina DataTable + finestra di dialogo crea/modifica + voce della barra laterale); associare o scambiare l'origine dati di una risorsa (Drizzle/REST/GraphQL/in-memory); oppure reindirizzare i dati + l'autenticazione dell'intera app su un backend diverso. Da utilizzare quando si aggiunge un'entità di dati o si punta l'app a un backend diverso da Postgres + better-auth predefinito.
