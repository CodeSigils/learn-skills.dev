Publish a doc, a diagram, a code snippet, a table, a diff or a small file from the terminal, which answers with an unlisted URL. Use when the user asks to publish, post or put something online ("publiceer dit plan op dropoff.sh", "zet dit diagram online"), to update something published earlier, to upload a small image, to show a code change as a rich diff ("publiceer deze diff"), or wants a diagram of a process, flow, pipeline or architecture ("maak een diagram van het inlogproces", "teken deze pipeline").
