跨平台圖像壓縮工具，基於 Sharp 構建，可實現高效壓縮（節省 60–80% 檔案大小）。支援單次/批次壓縮、格式轉換（JPG/PNG/WebP/AVIF/HEIC）、品質調整、尺寸縮放。當使用者需要壓縮圖片大小、轉換格式、縮小尺寸、批次處理圖片或優化微信/電子郵件/網頁上傳的圖片時，可以使用此技能。觸發詞：「壓縮」、「縮小」、「轉換格式」、「轉換為 JPG/WebP」、「影像太大」、「批次處理影像」。不處理：旋轉、裁切、浮水印、EXIF 讀取或編輯操作；檔案管理（刪除、重新命名、移動）；內容閱讀（影像分析、元資料擷取）。
