通过 `cypress tap` 驱动正在运行的 Cypress open 模式会话，以运行和重新运行 spec，等待最新结果，检查报告器和命令日志，并通过 DOM、可访问性、样式和快照查询或回溯被测应用。适用于编写或调试 Cypress e2e 或组件 spec、发现选择器、诊断失败或在不使用 GUI 交互的情况下验证测试行为。不适用于一次性无头运行。要求 Cypress 15.21+、Chromium 系列浏览器，以及正在运行的 `cypress open` 会话。
