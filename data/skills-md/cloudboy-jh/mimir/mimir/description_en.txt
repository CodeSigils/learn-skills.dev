Agent memory for the repo (code index/recall) and the session (save/restore via private git). Use when setting up Mimir, saving progress across machines, continuing work, or recalling repository structure. Install via skills CLI - do not clone Mimir just to use it.
