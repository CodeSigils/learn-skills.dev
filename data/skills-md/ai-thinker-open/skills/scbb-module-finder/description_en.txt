Search and retrieve the required peripheral driver modules from the AiPi‑SCBB repository. Use it when users need to find sensor drivers, peripheral modules, or hardware driver code. Supports module lookup for protocols such as I2C, UART, SPI, PWM + DMA, etc.
