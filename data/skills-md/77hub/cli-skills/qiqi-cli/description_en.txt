Use the ready qiqi command to handle general ERP tasks for enterprise services. It is used for verifying identity and profile, discovering objects and fields, querying records or work views, handling pagination and large outputs, executing document or approval actions, deleting records, handling attachments, or when the result of a write is unclear.
