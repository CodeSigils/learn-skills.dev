Implements multiple planned user stories in parallel using subagents. Each story gets its own branch, commit, and PR. Use when asked to implement multiple issues, execute a batch of stories, or build several features at once.
