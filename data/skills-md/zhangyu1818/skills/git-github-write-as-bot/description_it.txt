DEVE essere utilizzato prima dei commit git, delle riscritture git commit, dei comandi gh write o delle azioni di scrittura MCP GitHub in modo che le scritture GitHub e git create dall'agente utilizzino l'identità del bot invece di quella predefinita umana o macchina.
