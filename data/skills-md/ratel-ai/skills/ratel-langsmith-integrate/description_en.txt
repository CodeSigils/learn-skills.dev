Inspect an agent codebase and write a markdown plan wiring LangSmith tracing/observability (run-tree boundaries, sub-agent handoffs, tool wrapping, run-naming/metadata vocabulary) plus a Ratel-value/agent-health dashboard build-spec. Use to instrument LangSmith, add @traceable/wrap_openai coverage, group runs into threads, design dashboards or Monitor charts, decide where LangSmith goes, onboard a just-signed partner, or `/ratel-langsmith-integrate`. Writes to <repo>/.ratel/; never edits code or calls the LangSmith API. The LangSmith branch of /ratel-observability-assessment; redirects if the vendor is Langfuse; routes to /ratel-langsmith-analyze once traces flow.
