Genera utilidades Tuple con seguridad de tipos (Tuple0–Tuple20) para proyectos Kotlin y Kotlin multiplataforma. Crea clases de datos Tuple, fábricas tupleOf, conversión toList, KSerializer para kotlinx.serialization, awaitAll con seguridad de tipos y allNotNullOrNull de una sola vez. Úselo cuando se solicite: "agregar tupla", "generar tupleOf", "tupla con seguridad de tipos", "awaitAll con seguridad de tipos", "agregar allNotNullOrNull", "esperar múltiples diferidos con seguridad de tipos", "verificar nulos múltiples valores que aceptan valores NULL a la vez". Para proyectos Kotlin/KMP donde Pair/Triple no es suficiente para la agrupación de múltiples elementos.
