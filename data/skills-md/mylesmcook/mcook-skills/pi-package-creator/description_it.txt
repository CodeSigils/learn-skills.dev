Crea, effettua il refactoring e convalida i pacchetti Pi. Da utilizzare quando un agente deve creare l'impalcatura di un nuovo pacchetto Pi, scegliere la superficie corretta del pacchetto Pi, collegare i manifest "package.json" "pi" o dimostrare che un pacchetto Pi funziona attraverso il percorso "pi install" reale.
