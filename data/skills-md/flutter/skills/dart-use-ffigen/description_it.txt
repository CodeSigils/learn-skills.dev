Guida gli agenti a utilizzare `package:ffigen` per generare automaticamente associazioni FFI invece di scriverle manualmente. Utilizza questa competenza quando un'attività prevede la scrittura di nuovi collegamenti FFI, l'estensione delle integrazioni C/Objective-C/Swift o la sostituzione di configurazioni `dart:ffi` realizzate manualmente.
