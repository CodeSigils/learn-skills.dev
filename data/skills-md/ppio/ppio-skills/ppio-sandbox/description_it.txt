Si attiva quando l'utente menziona "crea sandbox", "build template", "deploy agent", "list sandbox", "kill sandbox", "sandbox logs", "clone sandbox", "submit sandbox", "template build", "ppio-sandbox-cli", "ppio sandbox", "sandbox" o qualsiasi operazione correlata alla CLI di PPIO Sandbox.
