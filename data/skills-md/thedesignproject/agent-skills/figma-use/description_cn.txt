**强制性先决条件** - 您必须在每次调用“use_figma”工具之前调用此技能。切勿在未先加载此技能的情况下直接调用“use_figma”。跳过它会导致常见的、难以调试的故障。每当用户想要执行写入操作或需要在 Figma 文件上下文中执行 JavaScript 的独特读取操作时触发 - 例如创建/编辑/删除节点，设置变量或标记，构建组件和变体，修改自动布局或填充，将变量绑定到属性，或以编程方式检查文件结构。
