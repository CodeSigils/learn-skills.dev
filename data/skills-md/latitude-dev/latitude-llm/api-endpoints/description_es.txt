Agregar o cambiar rutas en `apps/api`. Una fuente de verdad (`defineApiEndpoint` + un esquema Zod) se convierte en un punto final HTTP, una operación OpenAPI, una herramienta MCP y un método TS SDK: las descripciones y los contratos deben escribirse teniendo en cuenta a los cuatro lectores.
