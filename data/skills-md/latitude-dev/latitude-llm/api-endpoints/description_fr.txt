Ajout ou modification d'itinéraires dans `apps/api`. Une source de vérité (`defineApiEndpoint` + un schéma Zod) devient un point de terminaison HTTP, une opération OpenAPI, un outil MCP et une méthode TS SDK — les descriptions et les contrats doivent être rédigés en pensant aux quatre lecteurs.
