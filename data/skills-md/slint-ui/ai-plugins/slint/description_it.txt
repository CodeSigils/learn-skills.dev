Da utilizzare durante la scrittura, la modifica, il debug o la revisione del codice dell'interfaccia utente `.slint` o durante il collegamento a Rust/C++/JS/Python: il linguaggio Slint, il layout, i trucchi in fase di compilazione, l'interoperabilità e il server MCP per i controlli di runtime.
