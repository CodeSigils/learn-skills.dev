Una habilidad que comprime por lotes y cambia el tamaño de varias imágenes adjuntas. Se utiliza para solicitudes como "comprimir la imagen", "aclarar esta captura de pantalla", "cambiar el tamaño", "reducir la imagen" y similares. Cambia el tamaño del lado más largo a 1920 px usando Pillow y guarda el resultado como JPEG o PNG en el directorio de salida.
