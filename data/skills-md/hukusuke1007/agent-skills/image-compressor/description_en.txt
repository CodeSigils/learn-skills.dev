A skill that batch compresses and resizes multiple attached images. It is used for requests such as “compress the image,” “lighten this screenshot,” “resize,” “make the image smaller,” and the like. It resizes the longer side to 1920 px using Pillow and saves the result as JPEG or PNG in the output directory.
