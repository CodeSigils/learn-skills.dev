Pianificazione Epic end-to-end: griglia i requisiti, scrivi un PRD, scomponi in problemi GitHub con dipendenze e crea un ramo di funzionalità per l'implementazione autonoma. Da utilizzare quando si avvia una nuova epica, si pianifica una funzione di grandi dimensioni o quando l'utente dice "flusso di lavoro epico", "pianifica questa funzione" o "avvia una nuova epica".
