Entry point for image creation and editing. Handles requests for images, posters, logos, portraits, scenes, illustrations, covers, reference images, and visual direction, and routes them to `art-direction`, `image-poster`, `image-logo`, `asset-reference`, or `image-prompt`. When using SeaArt for realistic generation, call the optional `seaart-execute` adapter after the prompt is completed.
