After the `image-create` routing, compile precise bilingual prompts for generating new images and editing source images that can be executed by the model. It must not be used as a top-level route or execution layer.
