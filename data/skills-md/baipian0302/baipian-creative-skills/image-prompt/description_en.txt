After the `image-create` route, generate bilingual prompts that are precise and executable by the model for new image generation and source image editing compilation. It must not be used as a top-level route or execution layer.
