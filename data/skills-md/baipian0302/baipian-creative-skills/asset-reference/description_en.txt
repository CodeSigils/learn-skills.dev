After passing through the `image-create` or `video-create` route, extract stable references for characters, products, props, scenes, sounds, and brands across images, clips, outputs, or sessions. Ordinary one‑off assets are not used.
