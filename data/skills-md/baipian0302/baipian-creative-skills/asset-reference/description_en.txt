After routing through `image-create` or `video-create`, extract characters, products, props, scenes, sounds, and brand references that remain consistent across images, clips, outputs, or sessions. Do not use ordinary one‑off assets.
