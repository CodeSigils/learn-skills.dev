Adattatore di esecuzione SeaArt opzionale per la generazione di immagini e video. Utilizzare solo dopo che `image-create` o `video-create` e `image-prompt` o `video-prompt` hanno preparato un prompt finale, l'utente chiede esplicitamente di generare con SeaArt ed è disponibile una connessione MCP SeaArt.
