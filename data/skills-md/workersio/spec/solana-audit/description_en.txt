TRIGGER when: user asks to audit, review, or security-check a Solana smart contract, OR codebase contains solana_program, anchor_lang, pinocchio, #[program], #[derive(Accounts)]. Also trigger for: "check for vulnerabilities", "find exploits", "security review" on Rust code with Solana dependencies.
