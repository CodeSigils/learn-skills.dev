ОБЯЗАТЕЛЬНО перед сохранением функции приложения Salla: сохраните оболочку шаблона — его первую И последнюю строку — со всем кодом внутри оболочки (без поднятого const/helper) и проверьте тип обработчика с помощью компилятора TypeScript на соответствие «типам» триггера (URL-адреса .d.ts) на панели редактора — нестрогий, игнорируя неиспользуемые (TS6133) — плюс проверьте любой документированный вызов Admin API на соответствие его схеме OpenAPI. (цикл salla-api-core). `salla_functions action=save` проверяет ТОЛЬКО эти строки-оболочки. Направлено из функций salla-app; выпустите с помощью salla-app-functions-release.
