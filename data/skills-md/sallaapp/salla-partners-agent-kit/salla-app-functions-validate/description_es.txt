REQUERIDO antes de guardar una función de aplicación Salla: mantenga el contenedor de la plantilla exactamente (su primera y última línea) con todo el código dentro del contenedor (sin constante/ayudante elevado) y verifique el tipo del controlador con el compilador TypeScript contra los "tipos" del activador (URL .d.ts) en la barra del editor (no estricto, ignorando los no utilizados (TS6133)), además de validar cualquier llamada de API de administración documentada contra su esquema OpenAPI. (bucle salla-api-core). `salla_functions action=save` valida SÓLO esas líneas contenedoras. Enrutado desde salla-app-functions; lanzamiento con salla-app-functions-release.
