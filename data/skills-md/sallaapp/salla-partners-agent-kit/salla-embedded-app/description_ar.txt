استخدمه عند إنشاء صفحة iframe داخل لوحة معلومات Salla Merchant (تطبيق مضمن). قم بتسجيله عبر salla_embedded_pages، ثم قم بتثبيت @salla.sa/embedded-sdk، وانتظر Embed.init() (جسر postMessage → التخطيط: السمة/اللغة المحلية/dir)، ثم قم بالمصادقة على Trust-but-Verify: يرسل FEembed.auth.getToken() رمزًا مميزًا قصير العمر إلى الواجهة الخلفية لديك، والذي يتحقق منه عبر POST /exchange-authority/v1/introspect (header) S-Source = معرف التطبيق الخاص بك) ويقوم بإصدار جلسته الخاصة؛ اتصل بـembedded.ready()‎ فقط بعد ذلك. استخدم وحدات الصفحة/التنقل/واجهة المستخدم الأصلية (قاعدة No-Chrome)، ومزامنة السمة/RTL. بيع الإضافات داخل التطبيق → salla-addon-purchase-embedded؛ نشر التدفق → salla-app-builder.
