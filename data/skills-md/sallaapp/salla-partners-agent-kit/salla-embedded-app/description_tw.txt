在 Salla Merchant Dashboard（嵌入式應用程式）內建立 iframe 頁面時使用。透過 salla_embedded_pages 註冊它，安裝 @salla.sa/embedded-sdk，awaitembedded.init()（postMessage 橋 → 佈局：theme/locale/dir），然後驗證 Trust-but-Verify：FEembedded.auth.getToken(POST /exchange-authority/v1/introspect（標頭 S-Source = 您的應用程式 ID）驗證它，並建立自己的會話；僅在此之後呼叫embedded.ready()。使用本機頁面/導覽/UI 模組（無 Chrome 規則），同步主題/RTL。在應用程式內銷售插件 → salla-addon-purchase-embedded；發布流程 → salla-app-builder。
