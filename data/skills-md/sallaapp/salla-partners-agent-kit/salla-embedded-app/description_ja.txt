Salla Merchant Dashboard (埋め込みアプリ) 内で iframe ページを構築するときに使用します。 salla_embedded_pages 経由で登録し、@salla.sa/embedded-sdk をインストールし、embedded.init() (postMessage ブリッジ → レイアウト: テーマ/ロケール/ディレクトリ) を待ってから、Trust-but-Verify を認証します。FEembedded.auth.getToken() は、有効期間の短いトークンをバックエンドに送信し、POST /exchange-authority/v1/introspect (ヘッダー S-Source) 経由で検証します。 = あなたのアプリ ID) を作成し、独自のセッションを作成します。その後でのみembedded.ready()を呼び出してください。ネイティブのページ/ナビゲーション/UI モジュール (Chrome なしルール) を使用し、テーマ/RTL を同期します。アプリ内でアドオンを販売 → salla-addon-purchase-embedded;パブリッシュフロー→salla-app-builder。
