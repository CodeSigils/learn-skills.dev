Passo 3 della creazione di una funzione dell'app Salla: scrivi il corpo del gestore dal `template` del trigger. Da utilizzare durante la codifica della logica: lettura del "contesto" digitato (carico utile/commerciante/impostazioni), restituzione del contratto di risposta tramite il builder "Resp" (o entità "Spedizione"), rimanendo all'interno del sandbox di isolamento V8 (API standard web, "fetch" per HTTP) e vincolando ogni "recupero" al timeout di sincronizzazione/asincrono. Instradato dalle funzioni salla-app; convalidare successivamente con salla-app-functions-validate. Gestione dei token → salla-app-auth; Dettagli API → salla-api-core.
