Multi‑perspective adversarial review. In full/lean mode, reviewer agents are spawned in parallel when deployed; if agents are missing or abnormal or spawning fails, it automatically degrades to solo, and if reference files are unreadable it uses the built‑in rubric fallback. Trigger methods: /story‑review, /审查, “审查一下”, “帮我审一下”.
