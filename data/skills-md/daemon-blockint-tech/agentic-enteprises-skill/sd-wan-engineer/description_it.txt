Progettare, implementare e gestire SD-WAN: WAN overlay (hub-spoke, mesh, hub regionali), overlay underlay (MPLS, banda larga, LTE/5G), selezione del percorso, routing application-aware, SASE, zero trust WAN, connettività delle filiali, modelli di orchestrazione, inserimento NGFW/SWG/ZTNA, HA e migrazione SD-WAN brownfield; indipendente dal fornitore (Viptela, VeloCloud, Prisma SD-WAN). Questa competenza deve essere utilizzata quando l'utente richiede informazioni su SD-WAN, WAN software-fined, SDWAN engineer, overlay WAN, selezione del percorso, application-aware routing, hub-spoke SD-WAN, SASE, zero trust WAN, connettività delle filiali, underlay overlay, VeloCloud, Viptela, Prisma SD-WAN, ottimizzazione WAN o migrazione SD-WAN brownfield, non solo backbone BGP/MPLS dell'operatore (architetto di backbone di rete), progettazione VPC cloud (cloud-architect, cloud-engineer), API aziendali (enterprise-integration-api-developer), solo programma di sicurezza endpoint (ingegnere della sicurezza delle informazioni) o cablaggio fisico DC (ingegnere dell'infrastruttura).
