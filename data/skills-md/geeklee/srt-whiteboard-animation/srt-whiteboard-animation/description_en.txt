Create a whiteboard hand‑drawn animation with a warm beige paper background from SRT subtitles: read subtitles → output illustration strategy → after confirmation generate line art in a uniform style → segment according to narrative‑semantic annotations → adjust in preview station → render MP4. The sequencing uses partition masks for reveal (annotation.json / sequence / startMs / protectedRegions), but the ink in each segment is replaced with a continuous stream of strokes (skeleton/grid ink → color). This is triggered when the user provides SRT subtitles and requests “make subtitles into a whiteboard hand‑drawn/streaming‑stroke video”, “SRT generates whiteboard animation”, or “draw hand‑drawn storyboard according to subtitles”.
