Elenca, carica, scarica ed elimina i pacchetti del registro dei pacchetti del progetto GitLab con glab. Da utilizzare durante l'ispezione dei pacchetti, il filtraggio delle voci del registro dei pacchetti GitLab in base al nome/tipo del pacchetto, l'impaginazione dei risultati del registro dei pacchetti, il caricamento o il download di file di pacchetto generici, l'eliminazione di pacchetti o l'utilizzo dei comandi dei pacchetti glab. Trigger su pacchetti GitLab, registro dei pacchetti, pacchetti glab, pacchetti npm in GitLab, pacchetti Maven in GitLab, caricamento di pacchetti generici, download di pacchetti generici, eliminazione di pacchetti.
