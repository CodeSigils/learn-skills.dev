إدارة مفاتيح SSH لحساب GitLab بما في ذلك عمليات الإضافة والقائمة والحذف. يُستخدم عند إعداد مصادقة SSH، أو إدارة مفاتيح SSH، أو تكوين Git عبر SSH. مشغلات على مفتاح SSH، إضافة مفتاح SSH، ومصادقة SSH، وGit SSH.
