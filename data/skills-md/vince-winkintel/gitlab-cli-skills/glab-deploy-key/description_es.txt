Administre claves de implementación SSH para proyectos de GitLab, incluidas operaciones de agregar, enumerar y eliminar. Úselo al configurar claves de implementación para CI/CD, administrar el acceso de solo lectura o configurar la autenticación de implementación. Se activa al activar la clave de implementación, la clave SSH, la clave de implementación y el acceso de solo lectura.
