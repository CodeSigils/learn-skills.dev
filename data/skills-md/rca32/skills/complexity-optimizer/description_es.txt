Analiza una base de código de software para la complejidad algorítmica y los puntos críticos de rendimiento, luego propone o implementa optimizaciones seguras sin romper el comportamiento. Utilízalo cuando a Codex se le pida escanear muchos archivos, encontrar bucles ineficientes, iteraciones anidadas, escaneos repetidos, renderizado/recomputación costosa, consultas N+1, operaciones evitables O(n^2) u O(n), o reducir la complejidad como O(n^2) a O(n log n) / O(n), mientras se preservan las pruebas, APIs, resultados y mantenibilidad. No lo uses para una revisión de código general de estándares/especificaciones o para diagnosticar una regresión de rendimiento inexplicada antes de que se aíslen sus síntomas y causas.  

Por favor, siempre responde en el idioma del usuario.
