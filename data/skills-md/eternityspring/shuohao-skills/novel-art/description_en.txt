Create an art design set (scenes + narrative props) for AI short drama: scene design intent, consistency anchors, lighting period variants, empty‑scene prompt terms; props’ dramatic function, state variants, scale references, white‑background no‑hand prompt terms. Output art.json + Markdown + a single‑page review report (including exported JSON). Designed for AI generation, not real photography—environments and props are all generated assets, delivering a consistency scheme that makes them appear uniform across sets; all 11 quality gates are deterministically checked by scripts (anchors 3–5, no person/no hand, white background removable, scale phrases, prompts in English, etc.). If a novel‑outline’s outline.json is present, use its seed to pre‑fill the scene list and appearance set; image generation uses Codex’s built‑in $imagegen (optional). Zero dependencies, zero API key, using the current session quota. Use when asked for scene settings, generate scenes, environment setting sets, scene consistency, scene bibles for AI short drama.
