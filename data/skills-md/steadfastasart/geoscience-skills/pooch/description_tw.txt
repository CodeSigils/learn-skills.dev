地球科學應用程式的資料檔案取得和快取。下載樣本
具有自動快取、校驗和驗證和多次下載的資料集
來源。當 Claude 需要執行以下操作時使用：(1) 從 URL 或 DOI 下載資料集，
(2)本機快取檔案並自動驗證，(3)驗證檔案完整性
使用 SHA256/MD5 雜湊值，(4) 擷取壓縮檔案（ZIP、TAR、GZIP），
(5) 為可重複的工作流程建立資料註冊表，(6) 從 Zenodo 取得
或其他儲存庫。
