Migrazione dell'SDK Dart/Flutter allo streaming di span Sentry (ciclo di vita della traccia span-first). Utilizzare quando viene richiesto di "abilitare lo streaming di span", "migrare allo streaming di span", "utilizzare il flusso traceLifecycle", "utilizzare Sentry.startSpan" o passare dalla distribuzione di span basata su transazioni a quella in streaming in un progetto Dart o Flutter.
