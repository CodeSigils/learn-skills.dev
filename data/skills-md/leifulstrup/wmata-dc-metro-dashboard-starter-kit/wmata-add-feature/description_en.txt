Add a new dashboard feature (chart, panel, metric, or whole tab) to app/dashboard.py while preserving the existing patterns for caching, color coding, and demo-mode fallback. Use when the user wants to extend the Streamlit UI.
