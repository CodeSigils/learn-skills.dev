Performs file copies, moves, and renames via shell ops (mv, cp, git mv) instead of read-then-write, and uses Grep + StrReplace for reference updates. Use when copying, moving, renaming, or relocating files or directories.
