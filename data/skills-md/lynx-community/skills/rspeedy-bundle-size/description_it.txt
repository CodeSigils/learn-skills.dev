Analizza e riduci la dimensione del bundle delle app rspeedy/Lynx (ReactLynx): la leva dimensione/`.lynx.bundle` (non prestazioni di runtime, tipi o CI). Da utilizzare quando l'utente desidera ottimizzare le dimensioni del bundle 包体积, trovare cosa rende grande un bundle rspeedy, decidere quale leva di dimensione utilizzare o ridurre `main-thread.js` eliminando il codice solo in background (jsb/telemetria/richieste/monitor SDK) dal percorso di rendering tramite il gating `__BACKGROUND__` / `'solo background''. Copre i livelli multimediali, sfondo JS e thread principale, oltre al rigonfiamento di mangle/eval-avvelenamento, duplicazione i18n-locale, deduplicazione di pacchetti duplicati, manopole extractStr/compile e suddivisione pigra/dinamica dei componenti (prima schermata rispetto alla dimensione totale). Misura prima e basato sull’evidenza: produce un rapporto con priorità; cambia il codice solo quando richiesto.
