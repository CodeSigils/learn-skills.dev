Analysez et réduisez la taille du bundle des applications rspeedy/Lynx (ReactLynx) — le levier taille / `.lynx.bundle` (pas les performances d'exécution, les types ou CI). À utiliser lorsque l'utilisateur souhaite optimiser la taille du package / bundle, trouver ce qui rend un bundle rspeedy volumineux, décider quel levier de taille tirer ou réduire `main-thread.js` en supprimant le code en arrière-plan uniquement (jsb/télémétrie/requête/moniteur SDK) du chemin de rendu via `__BACKGROUND__`/`'background only'` gating. Couvre les couches de médias, d'arrière-plan JS et de thread principal, ainsi que les ballonnements d'empoisonnement mangle/eval, la duplication de paramètres régionaux i18n, la déduplication de packages en double, les boutons extractStr/compile et le fractionnement paresseux/dynamique des composants (premier écran par rapport à la taille totale). Mesurer d'abord et s'appuyer sur des données probantes : produit un rapport hiérarchisé ; change le code uniquement lorsque cela est demandé.
