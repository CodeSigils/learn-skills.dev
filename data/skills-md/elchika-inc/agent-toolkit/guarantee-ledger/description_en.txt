Use when introducing a guarantee ledger into a project, adding guarantees, or wiring the ledger’s mechanical inspection. Declare the promise that must not be broken in a single document, link it to implementation and testing, and make the origin of that promise (the directive or decision event) mandatory. Triggers: “guarantee ledger,” “guarantee ledger,” “record non‑breakable behavior in the ledger,” “add guarantee,” “include ledger inspection.” The procedure for pin verification uses guarantee‑pin‑check.
