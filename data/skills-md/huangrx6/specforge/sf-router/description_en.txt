SpecForge workflow root entry. Used when the user simply says “sf”, asks which step they are at, presents a new requirement, says continue, wants automatic advancement, or does not know which SpecForge capability to use; this skill only scans the repository, determines the status, and routes to an sf-* subskill.
