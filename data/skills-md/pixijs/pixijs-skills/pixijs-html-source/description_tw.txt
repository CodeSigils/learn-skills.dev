透過實驗性 HTML-in-Canvas 瀏覽器 API 將即時 HTML/DOM 元素（或它們的凍結快照）渲染為 PixiJS v8 紋理時，請使用此技能。涵蓋 pixi.js/html-source 副作用導入、使用 canvas.requestPaint 進行功能檢測、用於即時重繪元素的 HTMLSource 在瀏覽器中保持互動（autoLayout/autoUpdate/autoRequestPaint、requestPaint、isReady、canvas 的直接子元素 + layoutsubtree 需求）、用於不可變的快照Im、canvas 的直接子元素 + layoutsubtree 需求）、用於不可變的快照Image ElementImageSource（autoClose、立即就緒），使用來源Sprite/Texture/Mesh，透過Texture.from優先權-10進行後備自動偵測，並銷毀/清理。觸發條件：HTMLSource、ElementImageSource、pixi.js/html-source、requestPaint、captureElementImage、ElementImage、layoutsubtree、autoRequestPaint、autoUpdate、autoClose、畫布中的 HTML、將 DOM 渲染到紋理、HTMLSourceOptions、ElementImageSource、HTMLImML。
