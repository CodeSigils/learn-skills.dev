創建和配置 PixiJS v8 應用程式時使用此技能。涵蓋新的 Application() + async app.init() 選項（寬度、高度、背景、抗鋸齒、解析度、autoDensity、首選項、resizeTo、autoStart、shareTicker、canvas、useBackBuffer、powerPreference、eventFeatures、accessib​​ility、​​nid​​iat​​k、web每個渲染器覆蓋）、app.stage/renderer/canvas/screen/domContainerRoot 訪問， ResizePlugin、TickerPlugin、CullerPlugin（cullable、cullArea）、透過 ExtensionType.Application 建立自訂 ApplicationPlugin、啟動/停止生命週期以及帶有 ease Glodestroydep.觸發條件：Application、app.init、app.stage、app.renderer、app.canvas、app.screen、app.domContainerRoot、ApplicationOptions、ApplicationPlugin、ExtensionType.Application、resizeTo、p reference、autoStart、sharedTicker、useBackBuffer、powerPreference、skipExtensionImports、preferWebGLVersion、preserveDrawingBuffer、cullable、CullerPlugin、app.start、app.stop、 app.destroy，釋放全域資源。
