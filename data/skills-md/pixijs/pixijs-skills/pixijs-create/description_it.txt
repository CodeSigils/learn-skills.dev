Utilizza questa competenza durante lo scaffolding di un nuovo progetto PixiJS v8 con la CLI create-pixi o quando aggiungi PixiJS a un progetto esistente. Copre comandi di creazione npm/yarn/pnpm/bun, flussi interattivi e non interattivi, categorie di modelli bundler e creazione, preimpostazioni di modello disponibili (bundler-vite, bundler-webpack, bundler-esbuild, bundler-import-map, Creation-web, framework-react, extension-default), requisiti di versione del nodo, `npm install pixi.js` per progetti esistenti, flusso di sviluppo post-scaffold e attesa di livello superiore di Vite gotcha di produzione-costruzione. Trigger su: create pixi.js, npm create, npm install pixi.js, scaffold, template, bundler-vite, bundler-webpack, Creation-Web, framework-react, nuovo progetto, progetto esistente, per iniziare, avvio rapido.
