À utiliser lorsque l'utilisateur a de nouveaux fichiers HTML du camp SGDF à importer dans le menu scout, ou souhaite réexécuter le pipeline d'extraction de menu (analyser le HTML → diviser les plats → générer des ingrédients → importer dans Supabase).
