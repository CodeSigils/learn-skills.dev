在交互式或手动更新后验证 JavaScript/TypeScript 依赖的升级。通过 git 确定真实的差异，检测 peer / engine / type 冲突，从发行说明中提取破坏性更改、迁移步骤和可采用的特性，评估被保留或看似未使用的包，并运行验证门。支持 bun、npm、pnpm 和 yarn。适用于刚升级的包需要验证、决定是否可以放行被保留的包、询问为何某依赖会出现，或检查发行说明中的破坏性更改和迁移步骤的场景。不用于自动挑选并应用升级、bun CLI（bun-cli）使用或非 JavaScript 包生态系统。
