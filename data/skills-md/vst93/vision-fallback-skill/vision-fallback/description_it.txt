Comprensione della visione/immagine per gli agenti il ​​cui modello non è in grado di leggere le immagini (restituisce "il modello non supporta le immagini", output vuoto/sconosciuto, bassa confidenza o errore segnalato dall'utente). Chiama un'API di visione compatibile con OpenAI (doubao o qualsiasi provider compatibile con OpenAI), restituisce JSON strutturato. Utilizzare ogni volta che un'immagine deve essere compresa. NON sostituire con l'OCR locale (tesseract): l'OCR estrae solo il testo, non il layout/la comprensione visiva.
