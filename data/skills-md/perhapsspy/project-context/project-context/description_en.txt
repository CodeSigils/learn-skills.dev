File-based working context for coding agents using a compact global working memory, durable reference notes, and dated task records for most write-bearing work. Use when a repository keeps, or should start keeping, agent context in `docs/memory.md`, `docs/reference/`, and `docs/tasks/...` so later sessions can resume work without rebuilding state from scratch.
