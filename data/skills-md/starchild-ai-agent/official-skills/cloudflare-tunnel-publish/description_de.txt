Veröffentlichen Sie einen lokalen Dienst über Cloudflare Tunnel in einer benutzerdefinierten Domäne des Benutzers.

Verwenden Sie diese Option, wenn der Benutzer eine eigene Domäne anstelle von community.iamstarchild.com möchte (z. B. app.mydomain.com, SSL 521 auf einer benutzerdefinierten Domäne korrigieren).
