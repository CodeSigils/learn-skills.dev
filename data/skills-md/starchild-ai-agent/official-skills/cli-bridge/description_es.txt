Administre paquetes de código corto que autoricen a la CLI de starchild local a comunicarse con este agente, incluido el canal de ejecución local del shell del agente.

Úselo al conectar o desconectar la CLI de starchild (por ejemplo, crear un código de puente de CLI, enumerar mis paquetes de CLI, revocar una sesión de CLI anterior o permitir que el agente ejecute comandos de shell en la propia máquina del usuario).
