Utilizza le macchine cmux Cloud tramite la CLI. Da utilizzare quando l'utente dice cmux cloud, cloud VM, cloud machine, cmux vm, area di lavoro di base, pannello macchine, esegui/esegui su una macchina cloud, agente cloud, snapshot/fork/ripristino VM, push/pull di file su una VM, apri una porta o un desktop su una macchina o invia una notifica dall'interno di una VM.
