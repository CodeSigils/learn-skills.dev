Update a Dart/Flutter package's CHANGELOG.md by computing changes between the previous released git tag and the current branch/HEAD. Use this whenever the user asks to “find changes since last release tag”, “generate/update release notes”, “update CHANGELOG.md”, or “what changed between versions” for a package folder in a git repo (especially Flutter/Dart packages).
