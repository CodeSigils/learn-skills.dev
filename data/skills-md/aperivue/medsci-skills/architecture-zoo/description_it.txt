Scegliere un'architettura modello per una domanda di ricerca sull'imaging medico prima dell'impalcatura. Mappa il compito (classificazione, segmentazione, rilevamento, trasferimento), modalità e dimensionalità, scala dei dati etichettati e squilibrio di classe su un elenco ristretto di architetture, ciascuna basata sul proprio documento di origine con un momento di utilizzo, un uso dell'imaging medico, un'implementazione di riferimento, la tipica configurazione di convalida e il modello modello-impalcatura corrispondente. Copre il curriculum di base (ResNet, DenseNet, EfficientNet, ViT, Swin; U-Net, 3-D U-Net, Attention/Residual U-Net, nnU-Net (+ ResEnc/MedNeXt/STU-Net), Mask R-CNN; SAM/MedSAM, nnInteractive/VISTA3D Interactive-3D, TotalSegmentator, BiomedCLIP, DINO/MAE/SimCLR; e reti neurali grafiche — GCN/GraphSAGE/GAT/GIN/BrainGNN — per i connettomi cerebrali). Insegna gli archetipi e la logica dal compito all'architettura (incluso l'avvertimento di rigore "scala la CNN, nuovo≠meglio"), non una classifica SOTA dal vivo.
