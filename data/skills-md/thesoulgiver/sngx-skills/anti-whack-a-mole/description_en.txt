Use when fixing bugs to prevent introducing new ones. Activates a 6-step structured workflow (Understand → Locate → Plan → Implement → Verify → Learn) that forces global context before touching code. Trigger on "fix bug", "修复", "报错", or any debugging request.
