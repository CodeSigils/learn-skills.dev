Analyzes the changed code, automatically generates a commit message that follows the commit convention, and executes the commit. Use it when you say "/auto-commit", "make a commit", "commit", "commit changes", or "commit work". It also works when you just say "commit". Use it when you want to automate commit message writing without manually writing it. Use this skill for git commit, conventional commit, and commit automation.
