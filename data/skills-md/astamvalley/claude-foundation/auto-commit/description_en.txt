It analyzes the changed code, automatically generates a commit message that follows the commit convention, and executes the commit. Use it when you say "/auto-commit", "make a commit", "commit please", "commit changes", or "commit work". It also works if you just say "commit". Use it when you want to automate committing without writing a commit message yourself. Use this skill for git commit, conventional commit, and commit automation.
