Crea problemi GitHub dai risultati della ricerca come risultati finali atomici. Orchestra l'agente dell'architettura del problema utilizzando il modello swarm completo (TeamCreate → TaskCreate → Agent(team_name) → SendMessage → TeamDelete). Utilizza questa abilità quando l'utente richiama /sigint:issues.
