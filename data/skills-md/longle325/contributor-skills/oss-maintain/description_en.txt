Rebase feature branches, resolve merge conflicts, update stale PRs, and keep open-source contributions up to date with upstream. Uses force-with-lease for safety. Writes maintenance-log.md artifact. Triggers on: 'rebase branch', 'resolve conflicts', 'update PR', 'sync with upstream', 'merge conflict', 'stale branch', 'rebase onto main', 'update fork', 'sync fork'. Use this skill whenever a PR has merge conflicts, is behind the base branch, or needs to be updated.
