Scrivi un test unitario per la funzione, il modulo o il comportamento in $ARGUMENTI. Da utilizzare per funzioni pure, query a livello dati o qualsiasi unità testabile che non richiede il rendering di React.
