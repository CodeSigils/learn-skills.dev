专门构建的步进重放引擎 + 用于 L1/L2/L3 策略的确定性填充模拟器。驱动每个股票条的迭代，只为每个策略提供过去的条 t，因此没有想法可以展望未来。 bt-1 覆盖重放循环； bt-2 增加了保守的柱内填充模拟器； bt-4 增加了每策略指标 + 买入并持有基准； bt-5 添加了规范 CLI（--strategy/--ticker/--interval + --from/--to 过滤器、--json AXI 信封、--forensic-drill 审计跟踪）。
