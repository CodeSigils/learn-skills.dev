USE WHEN: managing, creating, initializing, updating, moving, or validating Kanban board items using markdown cards. Target directory initialization (.configs, backlog, active, archive), frontmatter schema validation, card status directory migration, and link correction. EXCLUDES: general software development, non-Kanban documentation edits, or raw markdown formatting without Kanban context.
