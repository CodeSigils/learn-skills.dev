Interpret the name of Seoul’s administrative dong as a standard place_id using only the official name and permitted definitive alias notation, and request the weather risk forecast time window (weather_place_risk_window) for each location in Seoul as a single product via a hosted k-skill proxy in read‑only mode. The default path does not require a user API key or place_id input, and before registration you can explicitly perform a local‑direct verification.
