Interpret the official name of Seoul administrative districts, using only the primary or permitted definitive notation aliases, as a standard place_id and request the weather risk forecast time window (weather_place_risk_window) for each location in Seoul as a single product from a read‑only query on the hosted k‑skill proxy. The base path does not require a user API key or place_id input, and before registration you can explicitly perform a local‑direct verification.
