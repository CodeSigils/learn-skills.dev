Asigne un flujo de trabajo descrito al GRUPO de comandos ECC correcto con orden de ejecución y condición de parada, y explore todas las familias de recetas del grupo de comandos. Agrega una capa de agrupación familiar + orden de ejecución + cuándo detenerse en la parte superior del catálogo de comandos planos. Sólo asesoramiento. DISPARA cuando el usuario dice qué comandos para X, qué grupo de comandos ejecuta X, muestra recetas de ECC, enumera las canalizaciones de ECC o cómo ejecuto un flujo de trabajo con ECC. NO DISPARE cuando el usuario quiera que la tarea se ejecute directamente, quiera un documento profundo de un solo comando (use ecc-guide) o quiera reescribir un borrador del mensaje (use el optimizador de mensajes).
