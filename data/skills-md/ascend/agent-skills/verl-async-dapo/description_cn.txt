Verl 单异步 DAPO 训练配置生成器。触发场​​景：（1）启动单异步DAPO训练（2）生成训练脚本（3）配置特征参数（4）预训练检查。 **功能策略**：当用户不指定时，默认启用性能功能（flash_attn/dynamic_batch/remove_padding/gradient_checkpointing），而内存功能（offload/recompute）默认禁用。如果发生 OOM，会自动添加内存功能以进行重试。 **训练监控**：启动后，输出一个SwanLab链接供用户自行查看，仅在出现错误时通知用户。 **依赖技能**：SwanLab 配置是通过 swanlab 设置技能提供的。
