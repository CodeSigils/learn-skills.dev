ModelScope CLI model and dataset download tool. It is used when users need to download models or datasets from ModelScope, batch download models, verify file integrity, count model parameters, or perform network diagnostics.
