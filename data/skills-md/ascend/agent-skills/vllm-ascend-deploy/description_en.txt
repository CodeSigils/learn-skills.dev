Ascend NPU platform vLLM large‑model inference service one‑click deployment. Trigger: user says “deploy <model name>”, “NPU deploy model”, or “vllm serve”. Process: SSH check → NPU check → configuration discovery (must be validated) → user confirmation → deployment → cron monitoring → verification. Constraints: (1) configuration must be validated from the official documentation; guessing is prohibited. (2) background startup must be monitored with cron; manual polling is prohibited. Supports Qwen/Qwen 3.5, GLM, DeepSeek, Kimi.
