Ascend NPU platform vLLM large model inference service one-click deployment. Trigger: user says “deploy model_name”, “NPU deploy model”, “vllm serve”. Process: SSH check → NPU check → configuration discovery (must be verified) → user confirmation → deployment → cron monitoring → verification. Constraints: (1) configuration must be validated from official documentation, guessing is prohibited; (2) background launch must be monitored with cron, manual polling is prohibited. Supports Qwen/Qwen3.5, GLM, DeepSeek, Kimi.
