Initialize an AscendC operator project and generate a compilable operator skeleton.  
Trigger scenarios:  
(1) User requests to create a new operator;  
(2) Keywords: ascendc operator, new operator, operator directory, operator initialization;  
(3) Needs rapid implementation based on the ascend-kernel template.  

This skill not only creates directories but also outputs “ready-to-continue-development” standard files and a checklist.
