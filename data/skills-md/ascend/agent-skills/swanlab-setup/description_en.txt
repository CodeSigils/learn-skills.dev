SwanLab experiment tracking platform configuration and login management. Trigger scenarios: (1) Configure SwanLab login credentials (2) Install/login SwanLab inside a container (3) Configure SwanLab for a specific container (4) Check SwanLab connection status. Supports multiple ways to obtain configuration: environment variables, configuration files, interactive input. Can be invoked by other skills via `source scripts/functions.sh`.
