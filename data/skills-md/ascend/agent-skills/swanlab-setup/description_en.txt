SwanLab experiment tracking platform configuration and login management. Trigger scenarios: (1) Configure SwanLab login credentials (2) Install/login SwanLab within a container (3) Configure SwanLab for a specified container (4) Check SwanLab connection status. Supports multiple configuration acquisition methods: environment variables, configuration files, interactive input. Can be invoked by other skills via source scripts/functions.sh.
