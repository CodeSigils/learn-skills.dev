两个保存的角色在一个房间里录制播客 - 镜头切换到正在说话的人，每个演员在每次切换时都保持在相同的座位、桌子和麦克风位置。提供character_a和character_b（从list_characters保存的char_…id，或https图像URL）和A/B对话轮次的有序“脚本”（每个{发言者：“A”|“B”，行：“…”}）。该管道渲染一个共享的两个镜头，锁定每个演员的特写镜头，使用本地口型同步的 Seedance 声音为每个回合制作动画（每个演员在整个剧集中保持一致的外观和声音），并将回合硬剪切在一起作为 9:16 垂直视频。长镜头自动分割为 ≤15 秒的片段。字幕是选择加入的 — 首先询问用户，然后设置 subtitles:true。
