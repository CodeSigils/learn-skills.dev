공개 API(`POST /resources/applicants/{applicantId}/tags/add`(추가), `POST .../tags`(전체 세트 덮어쓰기), `DELETE .../tags`(제거), `GET .../one`을 통해 다시 읽기 등을 통해 Sumsub 신청자 프로필에서 태그를 추가, 교체 또는 제거합니다. 사용자가 "지원자 태그/태그 해제", "지원자 태그 추가/제거/삭제", "지원자 프로필 라벨 지정" 또는 "지원자의 태그 세트 교체"를 요청할 때 TRIGGER. 거래 태그, 점수 가중치가 있는 태그 정의 또는 신청자 위험 평가(`sumsub-create-kyt-rules` 사용), 확인 워크플로 내 자동 태그 지정(`sumsub-create-workflow` 사용) 및 태그 변경 웹후크 구독(`sumsub-manage-webhooks` 사용)의 경우 건너뛰세요.
