Ricetta end-to-end per l'integrazione di Sumsub Travel Rule: decidere quale dei tre flussi di scambio applicare, identificare la controparte VASP, inviare il trasferimento, leggere lo stato dello scambio, finalizzarlo o annullarlo, rispondere alle richieste inviate da altri VASP, gestire il percorso del portafoglio non ospitato quando non viene attribuito alcun VASP e trovare transazioni esistenti (deduplicazione prima degli scambi successivi al regolamento). TRIGGER quando l'utente chiede di "integrare/aggiungere/implementare Travel Rule", "inviare i dati di Travel Rule con un prelievo", "raccogliere i dati di Travel Rule per un deposito già arrivato", "rispondere a/rispondere alle richieste di Travel Rule in arrivo", chiede perché i loro scambi restano in `onHold` o `completed` e non raggiungono mai `finished`, perché le controparti non rispondono mai, come testare Travel Rule in Sandbox, riguardo a "verifica del portafoglio non ospitato / self-hosted / non-custodial", "dimostrare il l'utente possiede il portafoglio", "Test Satoshi", cosa succede su `counterpartyVaspNotFound` o `applicantKytTxnAwaitingUser`, oppure su "trova/cerca transazioni", "controlla se esiste già una transazione per questo hash". SKIP per creare il payload della transazione stessa (utilizzare `sumsub-create-transaction`), per scrivere le regole di punteggio che agiscono sul risultato (utilizzare `sumsub-create-kyt-rules`), per la registrazione collettiva degli indirizzi dei portafogli (utilizzare `sumsub-manage-wallet-address-book`) e per attività KYC/AML non correlate ai trasferimenti tra VASP.
