Analizza il sistema di progettazione di un'app mobile nativa (colori, caratteri, raggi degli angoli, margini, chiaro/scuro) e genera codice che applica temi all'SDK Sumsub Mobile per abbinarlo: Swift (sottoclasse SNSTheme) su iOS, Kotlin (SNSTheme DSL) su Android. Utilizzalo ogni volta che l'utente desidera che le schermate di verifica/KYC di Sumsub corrispondano all'aspetto della propria app: "tema/marca/stile dell'SDK Sumsub", "fai in modo che la schermata di verifica corrisponda alla mia app", "corrisponda ai colori e ai caratteri del mio flusso KYC", "personalizza l'aspetto di SNSMobileSDK" - anche quando non dice "tema" ma chiede di allineare i colori/caratteri dell'SDK con la propria app. Rileva la piattaforma dal progetto (iOS Xcode/Swift vs Android Gradle/Kotlin). Esegue la scansione dei token di progettazione (iOS — catalogo risorse `.colorset`, estensioni `Color`/`UIColor` e `Font`, accento/tinta; Android — `colors.xml` / `themes.xml` / `values-night`, Compose `ColorScheme` / `Typography`), ricava una tavolozza completa e scrive il file del tema. SALTA per creare temi per l'app host stessa (questo definisce solo l'SDK di Sumsub), i temi Web e l'integrazione/configurazione dell'SDK (usa `sumsub-integrate-msdk`).
