Analiza el sistema de diseño de una aplicación móvil nativa (colores, tipografías, radios de esquina, márgenes, modo claro/oscuro) y genera código que tematice el SDK móvil de Sumsub para que coincida — Swift (subclase SNSTheme) en iOS, Kotlin (DSL SNSTheme) en Android. Utiliza esto siempre que el usuario quiera que las pantallas de verificación / KYC de Sumsub coincidan con el aspecto de su app — “tematizar / marcar / estilizar el SDK de Sumsub”, “hacer que la pantalla de verificación coincida con mi app”, “igualar los colores y fuentes de mi flujo KYC”, “personalizar la apariencia de SNSMobileSDK” — incluso cuando no mencionen “tema” pero pidan alinear los colores / fuentes del SDK con su app. Detecta la plataforma a partir del proyecto (iOS Xcode/Swift vs Android Gradle/Kotlin). Busca tokens de diseño (iOS — asset‑catalog `.colorset`, extensiones `Color`/`UIColor` y `Font`, acento / tint; Android — `colors.xml` / `themes.xml` / `values-night`, Compose `ColorScheme` / `Typography`), deriva una paleta completa y escribe el archivo de tema. Omitir la tematización de la aplicación host (esto solo estiliza el SDK de Sumsub), la tematización web y la integración/configuración del SDK (usar `sumsub-integrate-msdk`). Por favor, siempre responde en el idioma del usuario.
