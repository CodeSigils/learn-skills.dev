保持多资产品牌集的视觉连贯性（徽标 + 应用程序图标 + 图标 + 插图）。构建 BrandBundle（调色板、版式、样式参考、请勿列出），强制每个模型使用调色板（Recraft `controls.colors`、Midjourney `--sref`、IP-Adapter、LoRA），验证与 CSD 和调色板 ΔE2000 的样式相似性，并将已接受的资产提升到参考集中，以便每个新一代都加紧锁定。
