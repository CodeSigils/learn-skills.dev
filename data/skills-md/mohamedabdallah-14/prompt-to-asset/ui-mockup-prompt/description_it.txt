Traduci un brief dell'interfaccia utente (una pagina, uno schermo, un singolo componente, una funzionalità) in un prompt pronto per l'incollaggio per Nano Banana Pro / gpt-image-2 / Ideogram / Flux 2 / Midjourney che produce un mockup di livello designer come ispirazione visiva - non un'interfaccia utente con specifiche pixel, non un'intelligenza artificiale. Utilizzare ogni volta che l'utente chiede "immagina la pagina X", "modella lo schermo Y", "dammi un messaggio per progettare nano banana/immagine gpt 2", "descrivi questa interfaccia utente per un modello di immagine", "crea una richiesta da cui il progettista possa trarre ispirazione" o ogni volta che l'agente deve produrre un messaggio di generazione di immagini dell'interfaccia utente per una superficie di prodotto reale (pagina dei prezzi, dashboard, impostazioni, onboarding, schermo mobile, eroe del marketing, singolo componente). Sii invadente - attiva anche quando l'utente dice "progetta" senza "richiesta" o "mostrami come potrebbe apparire X" - l'agente dovrebbe raggiungere questa abilità prima di lanciare manualmente un brief.
