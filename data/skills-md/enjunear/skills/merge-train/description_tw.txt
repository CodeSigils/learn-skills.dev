由 Claude 驅動的順序 GitLab 合併列車。對於每個 MR 依序：變基 → 等待 → 設定自動合併 → 輪詢直到合併。使用本機工作樹解決會話中的變基衝突。當使用者說“/merge-train”、“運行合併列車”、“按順序合併這些 MR”或傳遞 GitLab MR 列表/過濾器以按順序登陸時使用。路徑上需要 `glab`、`jq` 和 `git`；目前工作目錄必須是 GitLab 專案儲存庫。
