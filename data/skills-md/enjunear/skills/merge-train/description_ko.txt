Claude가 운전하는 순차적 GitLab 병합 열차. 순서대로 각 MR에 대해 리베이스 → 대기 → 자동 병합 설정 → 병합될 때까지 폴링합니다. 로컬 작업 트리를 사용하여 세션 중 리베이스 충돌을 해결합니다. 사용자가 "/merge-train", "병합 트레인 실행", "이 MR을 순서대로 병합"이라고 말하거나 GitLab MR 목록/필터를 전달하여 순차적으로 착륙할 때 사용합니다. PATH에 `glab`, `jq` 및 `git`이 필요합니다. 현재 작업 디렉터리는 GitLab 프로젝트 저장소여야 합니다.
