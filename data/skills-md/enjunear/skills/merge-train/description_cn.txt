由 Claude 驱动的顺序 GitLab 合并列车。对于每个 MR 按顺序：变基 → 等待 → 设置自动合并 → 轮询直到合并。使用本地工作树解决会话中的变基冲突。当用户说“/merge-train”、“运行合并列车”、“按顺序合并这些 MR”或传递 GitLab MR 列表/过滤器以按顺序登陆时使用。路径上需要 `glab`、`jq` 和 `git`；当前工作目录必须是 GitLab 项目存储库。
