Execute the next task of a learning project — small code steps with fill-in placeholders, predict-before-run checks, quizzes driven by the knowledge graph, and a graph update at the end. Use when the user says "next lesson", "let's continue the project", "next task", or invokes /next-lesson.
