HTTP를 통해 실제로 실행 중인 샌드박스 배포를 연습해야 할 때 사용합니다. 예를 들어 라이브 컨테이너에 대한 SDK 변경 사항을 검증하거나, 사용자가 보고한 문제를 재현하거나, `wrangler dev`를 가동하지 않고 API(FUSE 버킷 마운트 포함)를 실험할 수 있습니다. 호스트가 삽입할 때 `SANDBOX_WORKER_URL` + `SANDBOX_API_KEY`를 통해 연결할 수 있는 샌드박스 브리지 작업자를 문서화합니다.
