在目前穩定的 @cloudflare/sandbox 套件（預設 npm 標籤）上建置或更改 Cloudflare Sandbox 應用程式時使用 - 命令、會話、檔案、連接埠、隧道、終端、橋接器、生產或已棄用的 API 清理，同時保持穩定。不適用於@cloudflare/sandbox@next（使用sandbox-next）或移植到1.0（使用sandbox-migrate-to-next）。
