Da utilizzare durante la creazione o la modifica di app Cloudflare Sandbox su @cloudflare/sandbox@next (anteprima Sandbox SDK 1.0): esecuzione di codice, runner AI, interpreti, processi di tipo CI, terminali, file, montaggi, tunnel, URL di anteprima, ciclo di vita o errori. Non per il pacchetto stable predefinito (usa sandbox-stable) o per il porting di stable su @next (usa sandbox-migrate-to-next).
