在 @cloudflare/sandbox@next（Sandbox SDK 1.0 預覽版）上建置或更改 Cloudflare Sandbox 應用程式時使用 - 程式碼執行、AI 運行程式、解釋器、類似 CI 的作業、終端、檔案、安裝、隧道、預覽 URL、生命週期或錯誤。不适用于默认的稳定包（使用 sandbox-stable）或将稳定包移植到 @next（使用 sandbox-migrate-to-next）。
