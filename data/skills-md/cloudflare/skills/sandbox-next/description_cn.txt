在 @cloudflare/sandbox@next（Sandbox SDK 1.0 预览版）上构建或更改 Cloudflare Sandbox 应用程序时使用 - 代码执行、AI 运行程序、解释器、类似 CI 的作业、终端、文件、安装、隧道、预览 URL、生命周期或错误。不适用于默认的稳定包（使用 sandbox-stable）或将稳定包移植到 @next（使用 sandbox-migrate-to-next）。
