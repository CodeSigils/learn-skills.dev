Full-stack project development process management. Initialize the development team and follow the standard workflow of research requirements → review requirements → design solution → write code → test code → deploy and run. Automatically create a project development log directory, maintain a todolist, and store solution documents in a separate folder for each task.
