Leggi o risolvi la revisione Qodo di una richiesta pull con la CLI qodo: recupera lo stato strutturato, il commit SHA revisionato e i risultati per QUALSIASI PR come JSON, quindi facoltativamente risolvi i risultati aperti nel codice e registra ogni risultato, una volta o in un ciclo di controllo finché non viene pulito. Usa questo — mai `gh`/`curl` raschiando i commenti delle recensioni — per "la recensione è pulita in termini di PR
