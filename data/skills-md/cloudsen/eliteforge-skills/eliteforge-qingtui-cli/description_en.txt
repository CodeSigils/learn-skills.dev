Operate lightweight push and lightweight applications through the qingtui command. This package should be used when the user requests to operate lightweight push or lightweight applications, supporting user queries, user resolution, openid resolution, and text sending operations. list-users, resolve-users, send-text.
