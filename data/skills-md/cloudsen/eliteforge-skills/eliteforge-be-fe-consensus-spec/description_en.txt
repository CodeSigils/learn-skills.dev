Enforce the front‑end/back‑end interface consensus for the Radiant Workshop project. User‑designed interfaces, API contracts, interface documentation, Controller input and output parameters, front‑end/back‑end integration contracts, request/response structures, Param/VO, unified response body, service status codes, error codes, paginated responses, request methods, request URLs, Long field precision, enum codes, Accept‑Language internationalization, or when referring to “front‑end/back‑end consensus” or “response content” use.
