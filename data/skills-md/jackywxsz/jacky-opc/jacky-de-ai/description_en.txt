AI-Flavor Removal Rewriting Assistant. It is triggered when the user says “remove AI flavor,” “help me rewrite so it doesn’t look like AI,” “this part is too AI,” “humanize,” “reduce AI rate,” “rewrite it to look like a real person wrote it,” “remove machine flavor,” “the copy is too stiff,” or “reads like AI wrote it.” It also applies in scenarios where the user says “help me edit the manuscript,” “polish it a bit,” and the context suggests the text has AI‑generated traces. Scan the text sentence by sentence for AI writing traces, rewrite it into a human style using a six‑step process, and output a side‑by‑side report along with the full revised text.
