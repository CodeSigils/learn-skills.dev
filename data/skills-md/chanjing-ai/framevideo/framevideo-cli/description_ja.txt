FrameVideo CLI 開発ループ — スキャフォールディング (初期化)、検証 (lint、検査)、プレビュー、レンダリング、および環境のトラブルシューティング (ドクター、ブラウザー、情報、アップグレード) 用の「npx Framevideo」。これらのコマンドのいずれかを実行する場合、または FrameVideo のビルド/レンダリング環境のトラブルシューティングを行う場合に使用します。アセットの前処理コマンド (`tts`、`transcribe`、`remove-background`) の場合は、代わりに `framevideo-media` スキルを呼び出します。
