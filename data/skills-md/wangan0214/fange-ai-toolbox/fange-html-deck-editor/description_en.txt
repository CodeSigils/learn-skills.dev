Local HTML slide (deck) visual editor: edit text directly in the browser and save to disk + single‑session Playwright rendering + historical snapshot rollback + three‑layer editable PPT export + one‑click upload to Feishu cloud space. Works with any deck‑style HTML (multi‑page slides where each page is wrapped in `<section class="slide">`). It is a link in the delivery chain—‘guizang‑ppt‑skill (generate HTML) → this tool (refine text) → export PPTX / upload to Feishu’—integrating with the Feishu delivery path of feishu‑html‑slides.
