Solve Python project root directory module import issues. Trigger this skill when users encounter ModuleNotFoundError, need to configure editable install mode, or want to set up pip install -e .

Trigger scenarios:
- User reports ModuleNotFoundError or ImportError
- User asks "how to make project root directory importable"
- User wants to configure pip install -e . (editable install mode)
- User asks "how to configure packages in pyproject.toml"

Note: This skill focuses on import issues caused by project structure, not missing third-party packages.
