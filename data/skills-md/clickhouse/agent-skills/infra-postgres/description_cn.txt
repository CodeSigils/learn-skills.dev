使用 clickhousectl CLI 设置和管理 Postgres — 运行本地 Docker 支持的 Postgres 进行开发，并创建和操作托管 ClickHouse Cloud Postgres 服务（连接、TLS、运行时配置、只读副本、故障转移、时间点恢复）。当用户想要为其应用程序使用 Postgres 或 PostgreSQL 数据库、本地 Postgres 开发环境、psql 访问或 ClickHouse Cloud 中的托管/生产 Postgres 时使用，或者提到将本地 Postgres 移至生产环境。
