Lazy Cat AI Pod Application Development and Packaging Guidelines. Triggered when users need to build an AI application for deployment to the Pod, write the ai-pod-service docker-compose.yml, configure Traefik routing rules, package the AI browser plugin, or publish the AI application to the store.
