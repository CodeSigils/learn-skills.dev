Riparatore attivabile (il comando /claude-seo-ai:fix). Applica le correzioni sicure e deterministiche di ricerca SEO/AI derivanti da un audit al codice dell'utente: meta viewport/charset/lang, JSON-LD, direttive AI robots.txt, hreflang, mappe del sito, schede OG/Twitter, dimensioni dell'immagine, canonico, llms.txt. Anteprima di prova per impostazione predefinita; scrive solo dopo la conferma esplicita della modifica. Viene eseguito solo quando l'utente lo richiama, mai con attivazione automatica.
