审核数据库架构、ORM 模型或设计和性能迁移 — 生成两个独立的 0-100 分数（设计和完整性 + 性能和规模）以及优先排序的、有证据支持的报告。只读；从不写入或迁移。当用户要求审核、分析、审查、检查或评分数据库、模式、数据模型、迁移、索引、键、引用完整性、类型、约束、查询模式、分区或连接池时使用 - 适用于 Postgres、MySQL、Mongo、DynamoDB、Cassandra、pgvector、ClickHouse、Neo4j 等。
