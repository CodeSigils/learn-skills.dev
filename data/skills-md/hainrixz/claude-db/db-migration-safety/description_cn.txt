迁移安全 lint (M22) — 迁移安全审核员 (MSA) 和 db-migration-writer 在候选迁移运行之前对其强制实施的静态合约。检查可逆性（向下迁移当前/扩展契约）、锁定级别（是否在热表上采用 ACCESS EXCLUSIVE）、表重写（对类型/默认更改进行全表重写）、破坏性操作（DROP COLUMN/TABLE、TRUNCATE）、不安全的枚举突变、大表上没有默认值的 NOT NULL 以及声明性工件和迁移历史记录之间的架构漂移。提供性能和规模分数。
