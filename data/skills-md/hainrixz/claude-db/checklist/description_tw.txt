為資料庫呈現生產就緒的 GO/NO-GO 網格 - 執行唯讀審核，然後將結果對應到固定的啟動清單（PITR/備份、租用戶表上的 RLS、FK 索引、貨幣數字、timestamptz/UTC、連接池大小、遷移可逆性、不在架構中的機密、字元集/utf8mb4）。每行都是通過/警告/失敗/需要即時顯示的結果 ID，頂部是簡單語言的判決。只讀。當使用者詢問資料庫是否已做好生產準備、是否準備好啟動/交付、是否進行、預先啟動或準備檢查清單，或在上線之前需要修復什麼時使用。
