为数据库呈现生产就绪的 GO/NO-GO 网格 - 运行只读审核，然后将结果映射到固定的启动清单（PITR/备份、租户表上的 RLS、FK 索引、货币数字、timestamptz/UTC、连接池大小、迁移可逆性、不在架构中的机密、字符集/utf8mb4）。每行都是通过/警告/失败/需要实时显示的结果 ID，顶部是简单语言的判决。只读。当用户询问数据库是否已做好生产准备、是否准备好启动/交付、是否进行、预启动或准备检查清单，或者在上线之前需要修复什么时使用。
