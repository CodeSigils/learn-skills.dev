Avvia un sistema di guida agente completo per un repository: genera AGENTS.md (sempre attivo), .agents/{domain}.md (caricato su richiesta), CLAUDE.md (reindirizzamento) e .agents/design-decisions.md utilizzando la divulgazione progressiva e riferimenti canonici invece di codice incorporato obsoleto. Da utilizzare quando l'utente menziona la configurazione di AGENTS.md, l'avvio di una guida dell'agente, la configurazione di CLAUDE.md per un repository, la "configurazione del contesto dell'agente" o l'onboarding di un nuovo repository per gli agenti di codifica AI.
