Edit and design the user‑provided material (web page URL / PDF / DOCX / Markdown / plain text / screenshot / pasted material) into a beautiful, offline‑viewable and shareable **single‑file HTML web article**. Based on the reacticle component protocol: do not handwrite raw HTML/CSS, but use semantic components + a Raw free layer constrained by the theme; proceed through a small harness workflow of source → planning → double confirmation → generation → final review → fixing, defaulting to a long article that retains 100% of the information. Trigger scenarios: turn a URL/PDF/DOCX/article into a web article / long article / briefing / explanatory text / visual article / tutorial / review recap / solution analysis—“render this as a beautiful web article” / “turn this into a web article” / “generate a shareable HTML long article” / “reacticle article”. Only generate the article, not backend, forms, dashboard, product prototype, or generic web app.
