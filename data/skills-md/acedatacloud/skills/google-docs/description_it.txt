Leggi e modifica Google Documenti tramite l'API REST di Docs v1. Da utilizzare quando l'utente menziona un documento Google, legge il testo di un documento, crea un documento, inserisce o sostituisce testo o esporta il contenuto di un documento.
