搭建并从头开始构建一个完整的 Caffeine 应用程序 (caffeine.ai) — 项目布局、caffeine.toml 工作区 + 容器清单、单个 Motoko `backend` 容器的 mops.toml、React/Vite 前端和 caffeine CLI 构建循环（身份验证登录、医生、安装、检查 --fix、预览 --build）。每当用户想要创建、搭建、设置或构建 Caffeine 应用程序或 caffeine.ai 项目，询问 caffeine.toml 或 mops.toml 结构，或需要 caffeine CLI 工作流程时使用 - 即使他们没有说“从头开始”。始终将其与后端代码的“motoko”技能配对。不要将其用于 Motoko 语言语法或编译器错误（使用 `motoko` 技能），也不要将其用于通用 Internet 计算机/dfx / icp 部署（使用 `icp-cli` 技能）。
