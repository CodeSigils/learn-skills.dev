发现并回收 macOS 上浪费的 SSD 空间，然后以交互方式删除它，并说明后果。通过它是什么来查找膨胀，无论它位于何处 - 构建/dep目录（node_modules，target，.venv，DerivedData）分散在每个项目中，包/工具缓存，埋在应用程序支持下的Electron应用程序缓存，AI代理历史记录（Claude / Codex日志），膨胀的.git对象，合并的git工作树，.DS_Store群，每卷垃圾，Time Machine本地快照。按最大的顺序遍历发现的结果，只有在用户确认每个结果后，才将其直接删除（而不是删除到垃圾箱）——预先告诉用户哪些会重新生成，哪些会永远消失。记住每个删除/跳过选择，因此重新运行时仅询问新发现的项目。当用户磁盘/SSD 空间不足、想要释放空间、清理 Mac、清除缓存或删除构建/依赖项膨胀时使用。
