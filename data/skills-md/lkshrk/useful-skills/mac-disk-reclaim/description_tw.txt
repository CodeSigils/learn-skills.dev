發現並回收 macOS 上浪費的 SSD 空間，然後以互動方式刪除它，並說明後果。透過它是什麼來查找膨脹，無論它位於何處 - 構建/dep目錄（node_modules，target，.venv，DerivedData）分散在每個項目中，包/工具緩存，埋在應用程序支持下的Electron應用程序緩存，AI代理歷史記錄（Claude / Codex日誌），膨脹的.git節以最大的順序遍歷發現的結果，只有在用戶確認每個結果後，才將其直接刪除（而不是刪除到垃圾箱）——預先告訴用戶哪些會重新生成，哪些會永遠消失。記住每個刪除/跳過選擇，因此重新運行時僅詢問新發現的項目。當使用者磁碟/SSD 空間不足、想要釋放空間、清理 Mac、清除快取或刪除建置/依賴項膨脹時使用。
