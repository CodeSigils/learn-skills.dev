当用户想要通过 Xdrop 服务器从终端发送或获取文件、要求自动化加密的 Xdrop 共享链接工作流程、提供 Xdrop `/t/:transferId#k=...` 链接以在本地下载和解密，或者需要 Xdrop CLI 标志（例如 `--quiet`、`--json`、`--expires-in`、`--output` 或 `--api-url`，即使它们没有明确提及技能名称时，请使用此技能。
