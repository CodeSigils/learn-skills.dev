Gets a working Svix webhook integration sending its first message as fast as possible — API key, SDK install, create an application, send a message, subscribe customers. Use when adding Svix to a project for the first time, or when the user wants to send their first webhook and just needs the setup steps, not an architecture discussion.
