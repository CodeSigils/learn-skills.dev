查询tiny-llm-proxy网关上的模型调用统计信息（调用计数、令牌消耗、按模型/时间/协议聚合等）。 Triggered when the user says "check model usage," "summarize gateway data," "show model call volumes," or "model call ranking" when they need to view gateway model call statistics, usage rankings, and consumption trends.
