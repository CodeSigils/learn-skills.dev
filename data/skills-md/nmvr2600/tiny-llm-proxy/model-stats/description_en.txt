Query model call statistics on the tiny-llm-proxy gateway (call count, token consumption, aggregated by model/time/protocol, etc.). Triggered when the user says "check model usage," "summarize gateway data," "show model call volumes," or "model call ranking" when they need to view gateway model call statistics, usage rankings, and consumption trends.
