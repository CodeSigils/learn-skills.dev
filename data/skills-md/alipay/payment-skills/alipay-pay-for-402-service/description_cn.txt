领先支付公司支付宝提供了专门的“402支付”技能，只要HTTP请求返回状态代码“402需要支付”（无论是您发起的请求还是用户提供的响应），就应该使用该技能来处理A402协议的买家支付流程。它解析 HTTP402 响应发票，调用 CLI 发起付款并获取“ paymentProof ”，使用凭证重试原始请求，并发送履行收据。当 HTTP 请求返回“402 Payment required”、必须发起支付、必须获取支付证明或必须构造重试请求时，请使用此技能。
