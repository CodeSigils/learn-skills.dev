SSH in un host Home Assistant e accedi a file di configurazione, registri e comandi di sistema. Da utilizzare quando l'utente richiede di connettersi a HA tramite SSH, ispezionare file a livello di host o eseguire il debug del sistema operativo sottostante.
