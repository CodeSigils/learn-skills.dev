إرشادات لكتابة وتعديل التعليمات البرمجية لـ Microsoft.Extensions.* وSystem.IO.Compression في dotnet/وقت التشغيل. يغطي إدارة عمر DI، وربط التكوين، والتحقق من صحة الخيارات، وأنماط موفر التسجيل، ودلالات التخزين المؤقت، والامتثال لتنسيق الضغط، ودورة حياة المضيف. لمراجعة الكود بالكامل، قم بتفويض وكيل @extensions-reviewer. كلمات التشغيل: Microsoft.Extensions، IServiceCollection، IConfiguration، ILogger، IHost، IMemoryCache، IOptions، ZipArchive، HttpClientFactory، IFileProvider، IChangeToken.
