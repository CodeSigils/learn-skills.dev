Korrigieren Sie MSBuild-Ziele, die während des Builds Dateien generieren, diese Dateien jedoch bei der Kompilierung oder Ausgabe fehlen. Nur im MSBuild/.NET-Buildkontext aktivieren. VERWENDUNG FÜR: Generierte Quelldateien, die nicht kompiliert werden (CS0246 für einen Typ, der existieren sollte), benutzerdefinierte Build-Aufgaben, die Dateien erstellen, aber für nachfolgende Ziele unsichtbar sind, Globs, die Build-generierte Dateien nicht erfassen, weil sie zum Zeitpunkt der Auswertung erweitert werden, bevor sie durch die Ausführung erstellt werden, wodurch sichergestellt wird, dass generierte Dateien vom Clean-Ziel bereinigt werden. Deckt das korrekte BeforeTargets-Timing ab (CoreCompile, BeforeBuild, AssignTargetPaths), das Hinzufügen zu Compile/FileWrites-Elementgruppen unter Verwendung von $(IntermediateOutputPath) anstelle von hartcodierten obj/-Pfaden. NICHT VERWENDEN FÜR: C#-Quellgeneratoren, die bereits über die Roslyn-Pipeline funktionieren, T4-Entwurfszeitgenerierung, die in Visual Studio ausgeführt wird, Nicht-MSBuild-Buildsysteme. AUFRUF: keine Werkzeuge – reine Wissenskompetenz.
