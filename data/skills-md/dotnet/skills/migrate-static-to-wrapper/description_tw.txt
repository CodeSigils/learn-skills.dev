將靜態依賴項呼叫點機械地替換為跨有限範圍（檔案、專案或命名空間）的包裝器或內建抽象呼叫。執行 Codemod 樣式批次取代 DateTime.UtcNow 到 TimeProvider.GetUtcNow()、File.ReadAllText 到 IFileSystem 以及類似的轉​​換。新增建構函式註入參數並更新 DI 註冊。用途：以 TimeProvider 取代 DateTime.UtcNow、以 TimeProvider 取代 DateTime.Now、將靜態呼叫遷移到包裝器、以 IFileSystem 批次取代 File.*、將靜態程式碼修改為可注入、為時間提供者新增建構函式註入、靜態的機械遷移、將 DateTime 重構為TimeProvider、將靜態替換為注入的依賴項、將靜態呼叫轉換為使用抽象、替換類別中的靜態、將一個檔案遷移到 TimeProvider、範圍內遷移、更新呼叫站點。請勿用於：偵測靜態（使用偵測靜態相依性）、產生包裝器（使用產生可測試性包裝器）、在測試框架之間遷移。
