Reemplace mecánicamente los sitios de llamadas de dependencia estáticas con llamadas de abstracción integradas o envoltorios en un ámbito limitado (archivo, proyecto o espacio de nombres). Realiza un reemplazo masivo estilo codemod de DateTime.UtcNow a TimeProvider.GetUtcNow(), File.ReadAllText a IFileSystem y transformaciones similares. Agrega parámetros de inyección de constructor y actualiza el registro DI. USO PARA: reemplazar DateTime.UtcNow con TimeProvider, reemplazar DateTime.Now con TimeProvider, migrar llamadas estáticas al contenedor, reemplazar masivamente File.* con IFileSystem, codificar estático a inyectable, agregar inyección de constructor para el proveedor de tiempo, migración mecánica de estáticas, refactorizar DateTime a TimeProvider, intercambiar estática por dependencia inyectada, convertir llamadas estáticas para usar abstracción, reemplazar estática en una clase, migrar un archivo a TimeProvider, con alcance migración, actualización de sitios de llamadas. NO UTILIZAR PARA: detectar estáticas (use detect-static-dependencies), generar contenedores (use generate-testability-wrappers), migrar entre marcos de prueba.
