제한된 범위(파일, 프로젝트 또는 네임스페이스) 전체에서 정적 종속성 호출 사이트를 래퍼 또는 기본 제공 추상화 호출로 기계적으로 대체합니다. DateTime.UtcNow를 TimeProvider.GetUtcNow()로, File.ReadAllText를 IFileSystem으로, 유사한 변환을 codemod 스타일로 대량 교체합니다. 생성자 주입 매개변수를 추가하고 DI 등록을 업데이트합니다. 사용 대상: DateTime.UtcNow를 TimeProvider로 교체, DateTime.Now를 TimeProvider로 교체, 정적 호출을 래퍼로 마이그레이션, File.*를 IFileSystem으로 대량 대체, 정적을 주입 가능으로 코드 수정, 시간 공급자에 대한 생성자 주입 추가, 정적의 기계적 마이그레이션, DateTime을 TimeProvider로 리팩터링, 주입된 종속성을 위해 정적 교체, 추상화를 사용하도록 정적 호출 변환, 클래스의 정적 호출, TimeProvider로 한 파일 마이그레이션, 범위 지정 마이그레이션, 호출 사이트 업데이트. 사용하지 마십시오: 정적 감지(정적 종속성 사용), 래퍼 생성(테스트 가능성 래퍼 생성 사용), 테스트 프레임워크 간 마이그레이션.
