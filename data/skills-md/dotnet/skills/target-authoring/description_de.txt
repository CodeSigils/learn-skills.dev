Kanonische Muster zum Schreiben benutzerdefinierter MSBuild-Ziele. Nur im MSBuild/.NET-Buildkontext aktivieren. VERWENDUNG FÜR: Diagnose und Behebung von Anti-Patterns bei der Erstellung benutzerdefinierter Ziele, Überprüfung von MSBuild-Zieldefinitionen auf Richtigkeit, Diagnose defekter SDK-Zielketten über Dateien hinweg (z. B. Directory.Build.targets definiert SDK-Ziele stillschweigend neu), Behebung von Zielen, die CompileDependsOn ersetzen, anstatt es mit $(CompileDependsOn zu erweitern), Behebung von Abfragezielen, die aufgrund von Missbrauch von Outputs vs. Returns veraltete Ergebnisse zurückgeben, Behebung fehlender Eingaben/Ausgaben verursachen unnötige Neuerstellungen und beheben fehlende FileWrites-Registrierung. Behandelt DependsOnTargets vs. BeforeTargets vs. AfterTargets, das dreistufige Build→CoreBuild-Muster, die Einbindung in die Build-Pipeline und das Kettenerweiterungsmuster $(XxxDependsOn). NICHT VERWENDEN FÜR: inkrementelle Build-Optimierung (inkrementelles Build verwenden), Parallelisierung (Build-Parallelität verwenden), allgemeine Anti-Patterns (msbuild-antipatterns verwenden), Nicht-MSBuild-Build-Systeme.
