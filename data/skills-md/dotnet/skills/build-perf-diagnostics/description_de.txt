Diagnostizieren Sie Leistungsengpässe bei MSBuild-Builds mithilfe der binären Protokollanalyse. Nur im MSBuild/.NET-Buildkontext aktivieren. VERWENDUNG FÜR: Identifizieren, warum Builds langsam sind, durch Analysieren von Binlog-Leistungszusammenfassungen, Erkennen von ResolveAssemblyReference (RAR), das mehr als 5 Sekunden dauert, Roslyn-Analysatoren, die mehr als 30 % der Csc-Zeit verbrauchen, einzelne Ziele, die mehr als 50 % der Build-Zeit dominieren, Knotenauslastung unter 80 %, übermäßige Kopieraufgaben, NuGet-Wiederherstellung, die bei jedem Build ausgeführt wird. Behandelt die Zeitachsenanalyse, die Interpretation der Ziel-/Aufgabenleistungszusammenfassung und sieben gängige Engpasskategorien. Wird verwendet, nachdem build-perf-baseline Messungen erstellt hat. NICHT VERWENDEN FÜR: Einrichten erster Baselines (zuerst build-perf-baseline verwenden), Beheben von inkrementellen Buildproblemen (inkrementeller Build verwenden), Parallelitätsoptimierung (build-parallelism verwenden), Nicht-MSBuild-Buildsysteme. AUFRUF: Dotnet Msbuild Binlog-Wiedergabe mit Leistungsübersicht, Grep zur Analyse.
