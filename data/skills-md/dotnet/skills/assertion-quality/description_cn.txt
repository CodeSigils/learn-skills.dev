分析 .NET 测试套件中断言的多样性和深度。当用户要求评估断言质量、查找浅层测试、识别无断言测试（没有断言或只有像 Assert.IsNotNull 这样的琐碎断言）、标记自引用或同义反复断言（输出等于身份/往返操作的输入）、测量断言覆盖多样性或审核测试是否验证正确性的不同方面时使用。产生指标和可行的建议。适用于 MSTest、xUnit、NUnit、TUnit。请勿用于：编写新测试（使用writing-mstest-tests）、其他反模式，例如片状或重复（使用test-anti-patterns）或修复断言。
