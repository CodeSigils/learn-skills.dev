为 C# 中难以测试的静态依赖项生成包装器接口和 DI 注册。生成 IFileSystem、IEnvironmentProvider、IConsole、IProcessRunner 包装器，或指导 TimeProvider 和 IHttpClientFactory 的采用。用途：生成静态包装器，创建 IFileSystem 包装器，包装 DateTime.Now，使静态可测试，使类可测试，为 File.* 创建抽象，生成 DI 注册，TimeProvider 采用，IHttpClientFactory 设置，可测试性包装器，模拟友好的接口，测试中的模拟时间，创建正确的模拟抽象，如何模拟 DateTime，使用 File.ReadAllText 测试代码，环境的抽象，如何使静态可注入，采用System.IO.Abstractions，使文件调用可测试。请勿用于：检测静态（使用检测静态依赖项）、迁移调用站点（使用迁移静态到包装器）、与可测试性无关的通用界面设计。
