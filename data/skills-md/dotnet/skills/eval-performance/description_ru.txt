Руководство по диагностике и улучшению производительности оценки проектов MSBuild. Активируйте только в контексте сборки MSBuild/.NET. ИСПОЛЬЗОВАНИЕ ДЛЯ: медленная сборка перед началом компиляции, длительное время оценки при анализе бинлога, дорогостоящие шаблоны glob, проходящие по большим каталогам (node_modules, .git, bin/obj), глубокие цепочки импорта (> 20 уровней), предварительно обработанный вывод > 10 000 строк, указывающий на тяжелую оценку, функции свойств с файловым вводом-выводом ($([System.IO.File]::ReadAllText(...))), несколько оценок для каждого проекта. Охватывает 5 этапов оценки MSBuild, оптимизацию glob с помощью DefaultItemExcludes, анализ цепочки импорта с предварительной обработкой /pp. НЕ ИСПОЛЬЗУЙТЕ ДЛЯ: медленной компиляции (используйте диагностику производительности сборки), проблем с инкрементной сборкой (используйте инкрементную сборку), систем сборки, отличных от MSBuild. ВЫЗЫВАЕТ: dotnet msbuild -pp:full.xml для предварительной обработки, /clp:PerformanceSummary.
