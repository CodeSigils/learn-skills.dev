當新的 .NET 版本發佈時，更新 HAL+JSON 版本資訊圖表。使用圖形產生器工具重新產生跨版本層次結構（根 → 主要 → 補丁）、時間軸層次結構（時間軸 → 年 → 月）、llms.json 和下載的索引檔。用途：在圖表中新增新的補丁版本、新增新的主要版本、發布後更新時間軸條目、來源資料變更後刷新圖表。請勿用於：supported-os.json 更改（使用 update-supported-os 技能）、查詢圖表（在 release-index 分支上使用 dotnet-releases 技能）、手動編輯生成的圖表檔案（更新來源資料並重新生成）。
