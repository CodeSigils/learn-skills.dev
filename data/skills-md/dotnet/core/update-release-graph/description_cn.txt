当新的 .NET 版本发布时，更新 HAL+JSON 版本信息图。使用图形生成器工具重新生成跨版本层次结构（根 → 主要 → 补丁）、时间线层次结构（时间线 → 年 → 月）、llms.json 和下载的索引文件。用途：向图表中添加新的补丁版本、添加新的主要版本、发布后更新时间线条目、源数据更改后刷新图表。请勿用于：supported-os.json 更改（使用 update-supported-os 技能）、查询图表（在 release-index 分支上使用 dotnet-releases 技能）、手动编辑生成的图表文件（更新源数据并重新生成）。
