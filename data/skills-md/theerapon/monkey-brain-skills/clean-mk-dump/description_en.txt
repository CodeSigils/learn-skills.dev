Distills the latest monkey-brain dump into a clean structured summary, then writes it back to the dump file on approval. Use when user runs /clean-mk-dump or wants to clean and summarise their latest dump session.
