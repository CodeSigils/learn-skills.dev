在發布 Git 儲存庫版本時使用 - 標記、將發布分支合併到主分支、解決衝突或同步發布分支之間的變更。處理不明確的標籤命名（v-prefix vs plain）、未知的主分支（master/main/develop）、僅限 ff 的合併方法（強制選擇策略）、受保護的分支規則、跨版本哈希敏感同步和 MR/PR 額外文件清理。單一或多個儲存庫。觸發器 — “發版本”“打標籤”“發布版本”“版本發布”“發布流程”“git-release”“多倉庫發布”“發布同步到另一個發布”/發布一個版本，標記並合併，完成發布分支。
