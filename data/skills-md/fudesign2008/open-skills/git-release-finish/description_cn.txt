在发布 Git 存储库版本时使用 - 标记、将发布分支合并到主分支、解决冲突或同步发布分支之间的更改。处理不明确的标签命名（v-prefix vs plain）、未知的主分支（master/main/develop）、仅限 ff 的合并方法（强制选择策略）、受保护的分支规则、跨版本哈希敏感同步和 MR/PR 额外文件清理。单个或多个存储库。触发器 — 「发版本」「打标签」「发布版本」「版本发布」「发布流程」「git-release」「多仓库发布」「发布同步到另一个发布」/发布一个版本，标记并合并，完成发布分支。
