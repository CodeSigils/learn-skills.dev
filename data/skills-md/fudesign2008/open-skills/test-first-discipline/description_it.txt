Dura disciplina per il lavoro che modifica il comportamento: scrivere prima un test fallito, osservare il fallimento, quindi scrivere un codice di produzione minimo; eliminare il codice di produzione scritto prima che si osservasse un test fallito. Distinto da test-suite-ensure (copertura/impalcatura post-hoc). Referenziato dalle fasi di esecuzione PDCA. Trigger — 「测试先行」「先写失败测试」「红绿重构」「TDD 铁律」「test-first」 / disciplina test-first, prima il test fallito. NON utilizzare come alias di nome per tdd o test-driven-development.
