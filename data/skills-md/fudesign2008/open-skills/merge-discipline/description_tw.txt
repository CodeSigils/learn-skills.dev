合併到受保護分支之前的硬門：執行 A→B→C→R→D 部分（OpenSpec 存檔關聯、變基/衝突預檢查、覆蓋率首選項、透過 pr-review-gate 進行具有可選深度的 pr-code-review、tip-pin 合併）。当相关的 OpenSpec 变更仍处于活动状态时，请勿合并；不要跳过直接的“合并 MR”命令。觸發器 — “合併提示”“合併提示”“合併紀律”“推後合併”“歸檔合入”“合併前門控”“rebase 檢查”“衝突預檢”“合併前 rebase”“先歸檔再合併”“合併前代碼審查”/合併規則、合併前歸檔、rebase 預檢查、覆蓋門、合併”“合併前代碼審查”/合併規則、合併前歸檔、rebase 預檢查、覆蓋門、合併 pr 代碼合併。
