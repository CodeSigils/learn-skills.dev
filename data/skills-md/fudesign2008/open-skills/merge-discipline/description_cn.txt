合并到受保护分支之前的硬门：运行 A→B→C→R→D 部分（OpenSpec 存档关联、变基/冲突预检查、覆盖率首选项、通过 pr-review-gate 进行具有可选深度的 pr-code-review、tip-pin 合并）。当相关的 OpenSpec 变更仍处于活动状态时，请勿合并；不要跳过直接的“合并 MR”命令。触发器 — 「合并提示」「合并提示」「合并纪律」「推后合并」「归档合入」「合并前门控」「rebase 检查」「冲突预检」「合并前 rebase」「先归档再合并」「合并前代码审查」/合并规则、合并前归档、rebase 预检查、覆盖门、合并前 pr 代码审查。
