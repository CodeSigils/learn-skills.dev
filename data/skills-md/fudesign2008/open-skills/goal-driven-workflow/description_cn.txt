目标驱动的长期工作流程：自主运行代理数小时以实现可验证的目标。五个阶段​​ — ① 明确需求和输出合同 ② 分层验收标准 + 设计/目标条件（可测量的最终状态、规定的检查、约束、回合/时间上限） ③ 子代理划分和上下文管理（上下文腐烂缓解） ④ 启动长期运行（/目标、claude -p 非交互式或手动循环回退） ⑤ 完成报告和人工验收。构建在 Claude Code 的本机 /goal 工具之上，为没有 /goal 的环境提供通用回退。触发器 — 「目标长跑」「目标跑」「目标驱动」「目标驱动长跑」「一个目标下去跑」「长跑目标」「无人值守任务」「目标跑」/目标跑、目标驱动、长期目标、自主跑、跑到完成。
