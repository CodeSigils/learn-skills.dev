八阶段 PDCA 问题解决工作流程，将分析、提案、设计审查、计划、执行和验证保留到 OpenSpec 工件（openspec/changes/<name>/，存档到 openspec/specs/）中，而不是仅将它们留在聊天上下文中。用于需要长期行为合同可追溯性、团队审查或可审计性的功能工作、错误修复、重构和复杂工程任务。不要用于不需要可追溯性的快速一次性编辑 - 请改用解决工作流程。触发器：「opsx解决」「OpenSpec解决」「规范化解决」「创建OpenSpec变更」「创建opsx变更」「用OpenSpec分析」「用OpenSpec修复」「opsx自动解决」「OpenSpec自动解决」「opsx-solve」「opsx-solve-workflow」 / opsx解决，OpenSpec解决工作流程，创建OpenSpec变更。
