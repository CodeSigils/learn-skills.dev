Flusso di lavoro di risoluzione dei problemi PDCA in otto fasi che persiste analisi, proposta, revisione del progetto, pianificazione, esecuzione e verifica negli artefatti OpenSpec (openspec/changes/<name>/, archiviati in openspec/specs/) invece di lasciarli solo nel contesto della chat. Utilizzare per lavori su funzionalità, correzioni di bug, refactoring e attività di ingegneria complesse che richiedono tracciabilità dei contratti comportamentali a lungo termine, revisione del team o verificabilità. NON utilizzare per una modifica rapida una tantum senza necessità di tracciabilità: utilizzare invece Solve-Workflow. Trigger:「Opsx解决」「OpenSpec解决」「规范化解决」「创建OpenSpec变更」「创建opsx变更」「用OpenSpec分析」「用OpenSpec修复」「opsx自自解决」「OpenSpec自动解决」「opsx-solve」「opsx-solve-workflow」 / opsx risolvere, risolvere il flusso di lavoro OpenSpec, creare una modifica OpenSpec.
