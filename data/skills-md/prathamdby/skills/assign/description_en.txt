Delegate a task or plan to an external coding agent and monitor execution. Use when the user asks to run a task with an external agent, hand off work to OpenCode or a similar tool, or execute a plan non-interactively. Supports --agent <name> (default: opencode) and --model <provider/model>.
