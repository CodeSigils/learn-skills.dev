Create a pull request with plain-English titles by default. Use when the user asks to open a PR, create a pull request, submit a PR, or merge a branch. Supports --target <branch> (default: main), --ticket <id> (prefixes title with ticket number, off by default), and --conventional (conventional commit title format, off by default).
