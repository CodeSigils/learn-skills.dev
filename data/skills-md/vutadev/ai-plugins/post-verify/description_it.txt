Da utilizzare dopo aver completato un'attività, una funzionalità, una correzione, un refactoring, una fase di rilascio o un trasferimento quando l'agente deve verificare il reale comportamento di runtime prima di dichiarare che è stato eseguito. Si applica a backend, frontend, CLI, processi batch, integrazioni, elementi generati da documenti e lavoro fullstack. Utilizzare client curl/http per controlli HTTP, playwright-cli per controlli del browser, screenshot, sondaggi della riga di comando, log o prove manuali; avviare in modo proattivo i servizi locali quando è sicuro. Include una nuova modalità esplicita per la verifica dello sviluppo locale quando l'utente chiede di risolvere i conflitti di porte, cancellare i dati locali, avviare una sessione pulita o riavviare da zero.
