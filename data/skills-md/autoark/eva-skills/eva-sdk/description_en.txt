Launch the official EVA Demo, or skip running the Demo and directly select the released EVA SDK to integrate and customize existing applications. Users may request to view the current SDK, query the official public entry point, run the Demo, integrate the SDK, modify public capabilities, or use it for verification and troubleshooting of SDK consumer projects; route released SDKs through a local SDK catalog, and route runnable Demos through a release‑pinned examples catalog, without predefining a specific toolchain.
