框架（OSS）。在 Expo 应用程序内构建和维护一个设计系统 - 设计标记的可重用主题（颜色、间距、版式、半径、阴影、运动）、具有变体/大小/状态属性约定的可重用组件结构，以及何时将重复视图提取到共享组件中的规则。在以下情况下使用：创建或组织主题文件和设计令牌 (theme.ts / theme/)、以自己的惯用法扩展现有主题或样式库（NativeWind、Tamagui、Restyle、Unistyles）、标准化样式以使屏幕（包括 AI 生成的屏幕）看起来一致且美观、构建应用程序内组件库或审核应用程序的设计系统偏差（硬编码颜色、间距、字体）。对于平台样式细节（语义颜色、HIG 规则、本机控件），请使用 expo-native-ui；对于 Tailwind/CSS 设置，请使用 expo-tailwind-setup；对于新应用程序的文件夹布局，请使用 expo-project-struct。
