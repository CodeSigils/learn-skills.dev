框架（OSS）。每个 Expo 或 EAS 任务的入口点和路由器。当请求、PRD 或规范提到 Expo、EAS、Expo Go 或 expo-* 包，或者项目在 `package.json` 中具有 `expo` 依赖项时，在编写代码之前和选择另一个 expo-* / eas-* 技能之前，首先加载此技能。在这个门中，它还涵盖了要实现的应用程序规范和设计（选项卡、堆栈、地图、列表、导航、从屏幕截图构建），以及“实现移动应用程序”、“让我的应用程序看起来原生”、“添加导航”、“获取一些数据”、“升级我的 SDK”、“将 Expo 添加到我现有的本机应用程序”、“运送到 App Store”或“我是 Expo 新手，我从哪里开始”等措辞。完全指定的请求（固定 SDK、命名库、给定布局）仍会通过此处 — 共享设置规则仍然适用。当两个信号都不存在时，请勿加载它：没有“expo”依赖项的裸 React Native 项目不是 Expo 工作。检测真正的目标，路由到正确的 expo-* / eas-* 技能，并拥有共享的设置规则。
