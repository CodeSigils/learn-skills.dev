框架（OSS）。每個 Expo 或 EAS 任務的入口點和路由器。當請求、PRD 或規格提到 Expo、EAS、Expo Go 或 expo-* 套件，或專案在 `package.json` 中具有 `expo` 依賴項時，在編寫程式碼之前和選擇另一個 expo-* / eas-* 技能之前，先載入此技能。在這個門中，它還涵蓋了要實現的應用程式規範和設計（選項卡、堆疊、地圖、清單、導航、從螢幕截圖建置），以及「實現行動應用程式」、「讓我的應用程式看起來原生」、「新增導航」、「取得一些資料」、「升級我的 SDK」、「將 Expo 新增至我現有的本機應用程式。完全指定的請求（固定 SDK、命名庫、給定佈局）仍會通過此處 — 共享設定規則仍然適用。當兩個訊號都不存在時，請勿載入它：沒有「expo」依賴項的裸 React Native 專案不是 Expo 工作。偵測真正的目標，路由到正確的 expo-* / eas-* 技能，並擁有共享的設定規則。
