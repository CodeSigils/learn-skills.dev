Format the confirmed final WeChat public account article in Markdown and write it into the WeChat public account draft box. Used for scenarios where users say “publish to the public account, write to the draft box, public account formatting, change the formatting theme, send a Little Green Book image message,” etc. Automatically upload the cover and inline images, support multiple themes and custom CSS, multiple accounts, and server mode. Only create a draft without mass sending; credentials are provided only via environment variables, and when the adaptation tool is not installed, deliver a manual publishing package.
