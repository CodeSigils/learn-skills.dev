Avvia, esegui query, scrivi e gestisci database a grafo a colonne TuringDB utilizzando l'SDK Python e il dialetto Cypher. TRIGGER quando: il codice importa `turingdb` o `TuringDB`; l'utente menziona TuringDB, turing db o turing database; l'utente chiede informazioni sulle query Cypher TuringDB, sul controllo delle versioni dei grafici con modifiche/commit, sulla ricerca vettoriale in TuringDB o sulla CLI `turingdb`; i file contengono stringhe di connessione TuringDB (localhost:6666) o chiamate SDK TuringDB (`client.query`, `client.new_change`, `client.load_graph`). SALTA: domande generiche su grafici/Neo4j/Cypher senza menzione di TuringDB; lavoro generale del database non correlato a TuringDB.
