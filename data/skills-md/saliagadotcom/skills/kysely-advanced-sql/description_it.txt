Modelli di query molto avanzati: CTE (.with/.withRecursive), funzioni finestra (.over), espressioni CASE, aggregazione JSON (jsonArrayFrom/jsonObjectFrom), SQL non elaborato (tag sql), riferimenti dinamici di colonne/tabelle e streaming. Utilizzare per qualsiasi cosa oltre il semplice SELECT/WHERE/JOIN.
