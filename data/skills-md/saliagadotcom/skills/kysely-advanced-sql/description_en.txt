Kysely advanced query patterns: CTEs (.with/.withRecursive), window functions (.over), CASE expressions, JSON aggregation (jsonArrayFrom/jsonObjectFrom), raw SQL (sql tag), dynamic column/table refs, and streaming. Use for anything beyond basic SELECT/WHERE/JOIN.
