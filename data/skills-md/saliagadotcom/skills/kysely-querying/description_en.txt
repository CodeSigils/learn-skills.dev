Kysely SELECT queries: joins, WHERE filters, boolean expressions (eb.or/eb.and), aggregates (count/sum/avg), subqueries, ordering, pagination, $if conditionals. NOT for CTEs, window functions, CASE, JSON ops, or raw SQL — use kysely-advanced-sql.
