Guida alla ricerca vettoriale Redis che copre la scelta dell'algoritmo HNSW vs FLAT, la configurazione dell'indice vettoriale (dim, metrica della distanza, tipo di dati), ricerca ibrida filtrata che combina la somiglianza vettoriale con filtri TAG o NUMERICI e il modello di recupero RAG con RedisVL. Da utilizzare durante la definizione di un campo VECTOR in FT.CREATE, l'integrazione di incorporamenti (OpenAI, Cohere, trasformatori di frasi), l'ottimizzazione dei parametri HNSW (M, EF_CONSTRUCTION, EF_RUNTIME), la creazione di una pipeline di generazione aumentata con recupero o il filtraggio dei risultati vettoriali per attributo.
