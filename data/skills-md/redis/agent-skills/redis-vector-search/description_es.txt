Guía de búsqueda de vectores de Redis que cubre la elección del algoritmo HNSW vs FLAT, la configuración del índice de vectores (atenuaciones, métrica de distancia, tipo de datos), búsqueda híbrida filtrada que combina similitud de vectores con filtros TAG o NUMERIC y el patrón de recuperación RAG con RedisVL. Úselo al definir un campo VECTOR en FT.CREATE, integrar incrustaciones (OpenAI, Cohere, transformadores de oraciones), ajustar los parámetros HNSW (M, EF_CONSTRUCTION, EF_RUNTIME), crear un canal de generación de recuperación aumentada o filtrar resultados vectoriales por atributo.
