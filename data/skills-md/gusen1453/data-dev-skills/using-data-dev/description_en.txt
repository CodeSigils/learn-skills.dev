Route finance research and investment data engineering and governance requests to the corresponding skill based on responsibility. Use this when the user mentions “help me create a table / design database table structure / write DDL”, “build a data synchronization pipeline / incremental / backfill”, “design an interface to query XX data / MCP tool”, “quality inspection / acceptance / review a batch of data tables”, “data contracts / data governance / compliance / lineage / masking”, or is unsure which data‑dev skill to use. Routing: data contract/DDL → finance-ddl-design, pipeline development → finance-pipeline-dev, query interface → finance-query-contract, quality acceptance → finance-data-qc, data governance/compliance → executor not yet provided. It only handles routing and responsibility determination; it does not connect to data sources, execute queries, or generate business deliverables. When the primary responsibility is clear, it should go directly to the corresponding skill without lingering here. If a design method is needed first, you can use /data-grill to finalize requirements and /data-solid to review the data‑warehouse structure.
