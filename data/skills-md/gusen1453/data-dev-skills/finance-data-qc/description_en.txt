审查金融投研数据表的质量，产出质检报告（PASS/BLOCK/WATCH）与可定时重跑的监测脚本/SQL。当用户说 "这些数据对不对 / 帮我质检(验收)一下XX表 / 出个数据质量报告 / 这个市值怎么这么离谱 / 主键重复了 / 某天缺数据 / 财报怎么还没更新 / 和另一个库对不上 / 为什么捞出来一堆死票"时使用；也排查市值等 数值离谱、主键重复、字段缺失、行情/财务序列断档、公告财报更新滞后、跨表单位命名时区不一致、 注释口径含混、行业题材标签污染、查询接口返回截断或捞出失效标的。覆盖行情、财务、公告事件、 证券主数据、指数基金、宏观行业等投研库。只读探索，结论全部可追溯、可重跑。不负责建表（用 finance-ddl-design）、不负责管道（用 finance-pipeline-dev）、不负责查询接口设计（用 finance-query-contract）。
