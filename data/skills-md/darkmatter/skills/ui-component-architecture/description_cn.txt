保持 React 屏幕简洁，并将 monorepo 的共享 UI 包（packages/ui，导入为 @repo/ui）作为可复用组件的归宿。当在属于拥有共享 UI 包的 monorepo 的应用中编写或重新样式化页面、路由、屏幕或组件时触发——尤其是当文件逐渐堆满普通的 `<div className="...">` Tailwind 标记时。倾向于复用已有的 @repo/ui 基础组件，并将新的可复用单元从屏幕迁移到 @repo/ui，同时避免过早的过度抽取。不要在非 React 代码、没有共享 UI 包的单包应用，或仅编辑已有组件内部时触发。
