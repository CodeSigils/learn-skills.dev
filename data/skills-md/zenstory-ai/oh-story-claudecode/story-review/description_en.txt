Multi-perspective adversarial review. In full/lean mode, reviewer agents are spawned in parallel when deployed; if agents are missing, abnormal, or spawning fails, it automatically falls back to solo, and uses a built‑in rubric fallback when reference files are unreadable. Trigger methods: /story-review, /review, “review it”, “help me review”.
