使用 Dibbla CLI 搭建项目、在本地运行 dibbla-task.yaml 管道 (dibbla run)、安装项目模板 (dibbla 模板列表/安装)、将此技能安装到项目中以便其他 AI 编码代理也可以读取它 (dibbla 技能安装 dibbla)、部署应用程序、管理应用程序/数据库/机密以及在 Dibbla 平台上设计/迭代工作流程。当用户想要从 URL 运行本地任务文件或模板、安装入门模板、将 dibbla 技能安装到 Claude Code/Cursor/Gemini CLI/Opencode/Codex 的项目或主目录、登录（包括通过“dibbla login --browser”从非 TTY 上下文）、部署、管理应用程序/数据库/机密或设计和操作工作流程时使用 - 编写一个精简的 YAML 工作流程，将 LLM 代理连接到工具、验证使用 `dibbla wf validate` 进行部署，使用 `wf create`/`wf update` 进行部署，通过 `nodes add`/`edges add`/`inputs set`/`tools add` 进行迭代，使用 `revisions create` 进行快照，使用 `revisions Restore` 进行回滚，并通过 `wf execute` 或 `wf api-docs` 公开的 HTTP 端点执行。
