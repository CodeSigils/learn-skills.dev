使用 Dibbla CLI 建立專案、在本地運行 dibbla-task.yaml 管道 (dibbla run)、安裝專案範本 (dibbla 範本清單/安裝)、將此技能安裝到專案中以便其他 AI 編碼代理程式也可以讀取它 (didilala 技能安裝 dibbla)、部署應用程式、管理應用程式/資料庫/機密以及在工作流程上/機密當使用者想要從 URL 執行本機任務檔案或範本、安裝入門範本、將 dibbla 技能安裝到 Claude Code/Cursor/Gemini CLI/Opencode/Codex 的專案或主目錄、登入（包括透過「dibbla login --browser」從非作業進行 TTY 上下文）、部署流程、管理程式/資料庫簡單或作業流程 -browser」從非操作流程的 TTY 上下文）、部署流程、管理程式/資料庫簡單或作業代理連接到工具、驗證使用 `dibbla wf validate` 進行部署，使用 `wf create`/`wf update` 進行部署，透過 `nodes add`/`edges add`/`inputs set`/`tools add` 進行迭代，使用 `revisions createputs set`/`tools add` 進行迭代，使用 `revisions createputs set`/`tools 地址execute` 或 `wf api-docs` 公開的 HTTP 端點執行。
