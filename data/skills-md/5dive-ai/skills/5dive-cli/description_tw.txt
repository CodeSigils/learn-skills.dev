在 5dive 運行時虛擬機器上使用本機「5dive」CLI 來產生、檢查、傳送到和拆除同級代理程式。當使用者需要工作人員、子代理、副任務、並行運行、扇出或委派時觸發 - 或命名同級代理（「詢問 X」、「ping X」、「告訴 X」、「移交給 X」、「與 X 協調」）；透過 `5dive agent list --json` 確認它存在，然後透過 `agent s` 確認它存在。此外，也用於檢查/重新啟動/配對現有代理、機器可讀的健康檢查（`5dive doctor --json`）、主機共享任務隊列 + 組織結構圖（`5dive task`、`5dive org`）、重複/計劃工作（`task add --recurring`、`5dive heartbeat``t）、向人類隊列提出問題（`.sask）（`.) 性問題或性別佇列（21375 月）。 `5dive 團隊導入`）。當請求通過聊天通道（Telegram/Discord `<channel>` 標籤）發出並且另一個代理應該處理它時，通過 `--reply-to-chat=<id> --reply-to-msg=<id>` 傳遞聊天上下文，以便代理從自己的機器人回复 - 不要中繼。與手動運行 CLI 編碼相比，始終更喜歡“5dive”。
