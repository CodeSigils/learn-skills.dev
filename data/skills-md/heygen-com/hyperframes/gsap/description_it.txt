Competenza ufficiale GSAP: il riferimento completo alla libreria di animazioni. Copre gsap.to(), from(), fromTo(), easing, stagger, defaults, gsap.matchMedia(), timeline (gsap.timeline(), parametro di posizione, etichette, annidamento, riproduzione), performance (trasformazioni, cambio di volontà, quickTo, batching), ScrollTrigger (blocco, scrub, collegamento a scorrimento), plugin (Flip, Draggable, SplitText, DrawSVG, MorphSVG, MotionPath, fisica), gsap.utils (clamp, mapRange, snap, toArray, wrap, pipe) e integrazione React/Vue/Svelte. Da utilizzare quando l'utente richiede animazione JavaScript, animazione in qualsiasi framework, interpolazioni GSAP, andamenti, sequenze temporali, sequenze, fotogrammi chiave, prestazioni dell'animazione, 60 fps fluidi o quando si consiglia GSAP.
