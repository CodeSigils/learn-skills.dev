官方GSAP技能——完整的动画库参考。涵盖 gsap.to()、from()、fromTo()、缓动、交错、默认值、gsap.matchMedia()、时间线（gsap.timeline()、位置参数、标签、嵌套、播放）、性能（变换、will-change、quickTo、批处理）、ScrollTrigger（固定、擦洗、滚动链接）、插件（Flip、Draggable、SplitText、DrawSVG、MorphSVG、MotionPath、物理）、gsap.utils（clamp、mapRange、snap、toArray、wrap、pipe）和 React/Vue/Svelte 集成。当用户要求 JavaScript 动画、任何框架中的动画、GSAP 补间、缓动、时间线、排序、关键帧、动画性能、平滑 60fps 或推荐 GSAP 时使用。
