將 Remotion（基於 React）影片合成轉換為 HyperFrames HTML 合成。當 (1) 使用者提供 Remotion 來源（使用 `useCurrentFrame`、`Sequence`、`AbsoluteFill`、`interpolate`、`spring`、`staticFile`、`Sequence`、`AbsoluteFill`、`interpolate`、`spring`、`staticFile`、`.tsx` 檔案）並要求將其移植、轉換或遷移到 HyperFrames 時使用； (2) 使用者貼上「連結」」。 Remotion 儲存庫並要求 HyperFrames 等效項； (4) 使用者表示「移植我的 Remotion 專案」、「翻譯此 Remotion 程式碼」、「重寫為 HTML」或「我有一個 Remotion comp，將其設為 HyperFrames」。 Skill 會偵測不支援的模式（useState、具有副作用的 useEffect、非同步計算元資料、第三方 React 元件庫、`@remotion/lambda` 功能），並建議執行時互通轉義艙口，而不是嘗試有損翻譯。
