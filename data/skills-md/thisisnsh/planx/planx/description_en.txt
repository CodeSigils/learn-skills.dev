Plan a feature, refactor or migration as a reviewable, versioned artifact the user annotates line by line before any code is written, instead of a plan that scrolls away in chat. Also revises or executes a plan they already have, or answers a question about how PlanX itself works from its wiki. Use for /planx, when the user wants to plan work or review and approve a plan before building, and for anything mentioning planx.
