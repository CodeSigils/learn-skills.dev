直接从 URL 获取内容并将其作为 markdown、文本或原始 HTML 返回。当用户想要阅读网页、检查 URL、从站点提取文档、从链接提取页面内容或在不打开浏览器的情况下获取 Web 内容时使用。当正常的 HTTP 获取就足够时，优先使用此功能而不是浏览器自动化。支持为 Accept: text/markdown 返回 LLM 优化 markdown 的 GitHub blob URL 和站点。
