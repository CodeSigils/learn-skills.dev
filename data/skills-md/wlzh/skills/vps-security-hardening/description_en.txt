VPS Hardening Automation Skill. Log in to the VPS via SSH and apply a complete security-policy configuration.

Trigger phrases:
- "harden VPS security", "VPS security config", "new VPS init"
- "secure SSH", "change SSH port", "disable root password login"
- "VPS hardening", "server security settings", "hardening"

Features (7 security hardening moves):
1. Create sudo user, disable root password login
2. Change SSH port (supports different Ubuntu versions)
3. Automatic Fail2ban install & config
4. SSH key-based login support
5. SSH login notifications (optional)
6. UFW firewall setup
7. Docker security reminder

Author: github.com/wlzh
Version: 1.0.0
