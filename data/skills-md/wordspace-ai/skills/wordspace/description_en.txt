Used when generating or editing .html documents for Wordspace. Triggered when the user requests to write a Wordspace document, modify a Wordspace .html file, or mentions “Wordspace Schema” or “compliant HTML document”. The output must pass Wordspace’s deterministic schema validator.
