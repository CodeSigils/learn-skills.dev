Use this when you need to compile an embedded project via the PlatformIO command line, invoking the built-in script to parse the environment configuration, perform the build, and locate the firmware artifact.
