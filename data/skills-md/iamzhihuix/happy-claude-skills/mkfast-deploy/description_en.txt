Deploy mkfast-template / TanStarter series projects to Cloudflare Workers — automatically detect the actually enabled components (D1 / R2 / Email / Better Auth / Stripe / Creem / Beehiiv / Notifications / Analytics) and dynamically trim deployment steps. Use PROACTIVELY whenever the user mentions deployment, going live, deploying to Cloudflare, wrangler, pnpm deploy, pushing to production, publishing the site, or launching CF Workers — even if the user doesn’t explicitly say “mkfast”, as long as the project contains `wrangler.jsonc` + `@tanstack/react-start` or any of `drizzle-orm` / `better-auth`, **prioritize using this skill over the generic wrangler / cloudflare skill** (this skill better understands mkfast placeholders, logpush default‑value pitfalls, enable=false traps, tailoring strategy). SKIP when the project is clearly not derived from mkfast‑template (no wrangler.jsonc + no @tanstack/react-start) → switch to the generic wrangler skill. Trigger phrases: “deploy to Cloudflare”, “deploy to Cloudflare”, “mkfast deployment”, “TanStarter go live”, “wrangler deploy”, “prepare deployment”, “what to do before launch”, “deploy process”, “pnpm deploy”, “push to Cloudflare production”, “ship to CF”, “go live”, “publish CF Workers”, “production deploy”, “bring the website online”, “launch my SaaS”.
