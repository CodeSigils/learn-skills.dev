Analyze a real project, choose the correct mobile deployment path, install the right native dependencies, wire mobile release services like RevenueCat when needed, and ship with Expo/EAS, Fastlane, Capacitor, or native tooling without forcing the wrong stack.
