Provisiona e gestisci i pod GPU su RunPod per esperimenti di ricerca a lungo termine scelti esplicitamente. Usalo quando una replica di Feynman, un benchmark o un'esecuzione di ricerca con grandi dataset richiedono calcolo GPU persistente con accesso SSH.
