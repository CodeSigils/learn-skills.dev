Esegui il codice di ricerca all'interno di contenitori Docker isolati per una replica, esperimenti e benchmark sicuri. Usa quando l'utente seleziona Docker come ambiente di esecuzione o richiede di eseguire il codice in maniera sicura, in isolamento o in una sandbox.

Rispondi sempre nella lingua dell'utente.
