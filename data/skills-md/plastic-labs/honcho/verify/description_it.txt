Crea, avvia e gestisci uno stack Honcho locale per verificare una modifica nella sua superficie di runtime (l'API HTTP /v3 e la coda del derivatore). Da utilizzare quando si verifica una differenza o si conferma che una modifica funziona nell'app in esecuzione.
