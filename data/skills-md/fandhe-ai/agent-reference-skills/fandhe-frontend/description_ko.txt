Rust 기반 프런트엔드 프레임워크 **fandhe-frontend**에 대한 참조입니다. SSR, SPA, SSG 및 뷰 전환, 하이드레이션, 배포를 단일 실행 파일로 통합합니다. 기본 이스케이프 및 `forbid(unsafe_code)`를 사용한 안전한 설계입니다. 기본 상자: `core`, `app`, `interactive`, `headless-ui`(기본), `pre-styled-ui`(테마), `server`(SSG `generate_pages`/`generate_assets`) — 모두 `fandhe-frontend-` 접두사가 붙습니다. CLI 프레임워크(`new` / `gate` / `structure` / `impact`).
