基於 Rust 的前端框架 **fandhe-frontend** 的參考。將 SSR、SPA、SSG 和視圖轉換、水合作用和分發集成為單一可執行檔。具有預設轉義和“禁止（不安全代碼）”的安全設計。主套件：`core`、`app`、`interactive`、`headless-ui`（基元）、`pre-styled-ui`（主題）、`server`（SSG `generate_pages`/`generate_assets`）－皆以 `fandhe-frontend-` 為前綴。 CLI 框架（“新”/“門”/“結構”/“影響”）。
