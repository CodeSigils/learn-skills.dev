Riferimento Turborepo (sistema di compilazione monorepo ad alta velocità). turbo.json, turbo run, dipendenze delle attività (dependsOn), cache (locale/remota), spazi di lavoro (pnpm/npm/filato/bun), --filter, Remote Cache (Vercel), esecuzione parallela, turbo gen (generazione di codice), modalità orologio.
