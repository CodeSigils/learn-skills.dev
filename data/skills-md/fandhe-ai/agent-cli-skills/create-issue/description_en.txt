Create GitHub issues using a parent‑child (sub‑issue) structure. Generate parent and child issues with `gh issue create` and link them via `gh api …/sub_issues`. Titles should follow the Conventional Commits format (`feat:`, `fix:`, etc.). Used for commands like “create an issue” or “break down tasks into issues”.
