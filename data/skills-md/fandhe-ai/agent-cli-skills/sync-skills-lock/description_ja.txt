上流リポジトリの最新の状態と比較して、ルートレベルの `skills-lock.json` の `computedHash` を更新します。 `source` が `Fandhe-AI/` (安全弁) で始まらないエントリをスキップします (クローンを作成しないでください)。サブモジュール内の「skills-lock.json」ファイルを変更しないでください。これは、contribute-skill をマージした後、またはアップストリームと同期した後、「ハッシュ更新」や「スキルロック同期」などのタスクに使用されます。
