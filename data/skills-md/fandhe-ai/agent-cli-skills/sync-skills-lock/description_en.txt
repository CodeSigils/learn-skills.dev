Update the `computedHash` in the root-level `skills-lock.json` by comparing it with the latest state of the upstream repository. Entries whose `source` does not start with `Fandhe-AI/` are not cloned and are skipped (safety valve). Do not modify `skills-lock.json` under any submodule. This is used after merging `contribute-skill` or after syncing with upstream for tasks such as “hash update” or “skills‑lock sync”.
