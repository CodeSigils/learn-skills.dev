Create git commits in the Conventional Commits format. Infer the type/scope from staged diffs, and detect breaking changes and secret leaks (e.g., .env files). The pre‑commit hook must always be passed (using `--no-verify` is not allowed). Used with commands such as “commit”, “git commit”, “record changes”, and similar.
