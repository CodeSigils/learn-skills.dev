Create the implementation task plan in `_/local-plans/<plan-name>.md`.  
First investigate the code base with the Explore Agent, then write it at a granularity and in units that can be verified and executed in parallel, following a standard format that can be validated by the plan‑verifier Agent (background, current status, design, file‑structure tree, implementation steps, verification method). It is used for tasks such as “make a plan,” “design,” “break down tasks,” “consider implementation policy,” etc.  
plan, planning, design, implementation plan, task breakdown, create‑plan
