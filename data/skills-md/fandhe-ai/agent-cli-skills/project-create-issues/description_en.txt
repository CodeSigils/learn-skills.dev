Convert a project's draft item (DraftIssue) into a real GitHub Issue, preserving field values such as Status, Priority, and Size. You can specify an existing parent issue or create a new one and link it as a sub‑issue using `gh api …/sub_issues`. It’s used for actions like “turn a draft into an Issue,” “convert a DraftIssue,” or “make a project task an Issue.”
