Update the skill list and repository structure tree in CLAUDE.md based on the differences from the last update commit (tracked with `_/.last-update-docs`). When adding new skills or modifying `.claude/agents/` or `.claude/rules/`, use it with commands like “update documentation” or “update CLAUDE.md”.
