종속성 순서를 유지하면서 작업 트리를 사용하여 상위 이슈 아래 하위 이슈(손자 포함)를 병렬로 자동 구현하고 검토 전 푸시, PR 생성, CI 모니터링 및 스쿼시 병합까지 전체 프로세스를 자동화합니다. "이슈 트리의 병렬 구현", "그 아래의 모든 하위 이슈 구현", "전체 트리를 병렬로 구현" 및 "이슈 계층의 자동 개발"에 사용됩니다. 이슈별 인력 분담: 기획(Plan: opus) → 구현(Implement: sonnet). 검토 전 푸시(푸시 및 PR 생성은 검토가 통과된 후에만 발생하며 CI는 한 번만 트리거됩니다). 외부 검사 자동 결정(Cursor Bugbot이 없는 저장소에서도 불필요한 대기 없음) 병렬성(parallel)과 종속성(dependentsOn)으로 실행 순서를 제어합니다. 단일 문제 구현에 대해서는 구현 문제를 참조하세요. PR 검토는implement‑review‑pr을 참조하세요.
