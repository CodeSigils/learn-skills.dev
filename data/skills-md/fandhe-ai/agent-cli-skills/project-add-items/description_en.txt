Create project items in bulk from Markdown checklists, YAML task lists, requirement definition documents, and similar sources. Parse the title, body, Priority, and Size, then set the fields using `gh project item-create` plus `item-edit`. Used for actions such as “create items from requirement documents,” “add tasks to a project,” “import a roadmap,” and so on.
