Set up automatic synchronization of the Issue/PR status and project status. Mode A generates a `.github/workflows/project-sync.yml` using the `Fandhe-AI/actions/project-sync` Composite Action (PAT or GitHub App token). Mode B batch-corrects existing inconsistencies. Used for tasks such as “automatic sync setup,” “mark as In Review during PR review,” and “synchronize board and Issue status.”
