Runs an autonomous refactoring loop that picks files, applies behavior-preserving changes, runs the test suite, measures a quality metric (lint warnings, test coverage, type errors, complexity), and keeps improvements or reverts failures. Loops indefinitely. Triggers on "auto-refactor", "refactor loop", "clean up my codebase", "improve code quality", "reduce complexity", or requests to iteratively improve code health metrics.
