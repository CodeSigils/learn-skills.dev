Da utilizzare durante la migrazione del codice frontend dalle chiamate client API manuali (`api.get`, `api.create`, `api.surveys.get`, `api.dashboards.list`, `new ApiRequest()`) e dalle interfacce TypeScript scritte a mano alle funzioni e ai tipi API generati. Si attiva sull'importazione di file da `lib/api`, file con `api.get<`, `api.create<`, `api.<entity>.<method>`, definizioni di interfaccia manuale che duplicano serializzatori backend o qualsiasi file frontend che costruisce manualmente gli URL API. Copre l'intero flusso di lavoro di sostituzione: ricerca dell'equivalente generato, scambio di importazioni, adattamento dei siti di chiamata e rimozione di tipi manuali obsoleti.
