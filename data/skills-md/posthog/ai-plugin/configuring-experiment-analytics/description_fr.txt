Configure le côté analytique d'une expérience PostHog : critères d'exposition (par défaut `$feature_flag_call` et événements d'exposition personnalisés), métriques primaires et secondaires, types de métriques pris en charge (nombre, somme, ratio avec `math` et `math_property`, rétention avec `retention_window_start` et `start_handling`), gestion utilisateur multivariée ("Exclure" vs "Première variante vue") et comment lire les résultats une fois l'expérience terminée. vivre. À utiliser lorsque l'utilisateur ajoute ou modifie une métrique principale ou secondaire (par exemple, "ajouter une métrique secondaire de suivi 'fichier_téléchargé' par utilisateur"), configure une métrique de ratio (par exemple "revenus d'achat_terminés / pages vues"), configure une métrique de rétention (par exemple "$pageview → fichier_uploadé, fenêtre de 7 jours"), configure une exposition personnalisée (par exemple "ne compte que les utilisateurs qui ont cliqué sur /checkout"), modifie plusieurs variables. manipulation, ou demande « qui participe à l'analyse ? », « comment puis-je mesurer l'impact ? », « est-ce gagnant ? », « quel est le niveau de confiance ? » ou « dois-je expédier ? ».
