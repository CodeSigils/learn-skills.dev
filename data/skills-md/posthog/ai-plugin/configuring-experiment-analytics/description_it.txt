Configura il lato analitico di un esperimento PostHog: criteri di esposizione (predefinito `$feature_flag_tched` rispetto a eventi di esposizione personalizzati), metriche primarie e secondarie, i tipi di metriche supportate (conteggio, somma, rapporto con `math` e `math_property`, conservazione con `retention_window_start` e `start_handling`), gestione utente multivariata ("Escludi" rispetto a "Variante prima vista") e come leggere i risultati una volta che l'esperimento è attivo. Da utilizzare quando l'utente aggiunge o modifica una metrica primaria o secondaria (ad esempio "aggiungi una metrica secondaria che tiene traccia di 'file_scaricato' per utente"), imposta una metrica di rapporto (ad esempio "entrate da acquisto_completato/visualizzazioni di pagina"), imposta una metrica di fidelizzazione (ad esempio "$visualizzazione di pagina → file_caricato, finestra di 7 giorni"), configura l'esposizione personalizzata (ad esempio "conta solo gli utenti che hanno premuto /checkout"), modifica multivariata gestione o chiede "chi c'è nell'analisi?", "come misuro l'impatto?", "è vincente?", "qual è il livello di confidenza?" o "devo spedire?".
