At the start of each new session, check whether the user's first prompt includes the project name, project author, and project description; if any are missing, proactively ask to complete them. When the user requests to push a Docker image to the UIBP base, verify that the entity username, entity password, push path, and English project name are all provided; if any are missing, proactively ask to complete them. Before each push, forcibly increase the version number by 0.1 and use it as the Docker tag. After a successful push, ask the user whether to save the entity password.
