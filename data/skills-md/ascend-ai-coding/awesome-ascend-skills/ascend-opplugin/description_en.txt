Used to guide the integration of Ascend C (CANN / xpu_kernel) operator development into PyTorch after completion: interfacing with torch_npu, host‑side registration and building, Python exposure, and smoke testing. It covers lightweight integration within the operator project, referencing op‑plugin when necessary, rapid joint debugging with Pybind, reusing graph operator encapsulation, etc. Applicable to scenarios such as Ascend C, operator integration, torch_npu, NPU custom operators, op‑plugin, xpu_kernel, cpp_extension, custom operators, and so on.
