Ascend NPU single‑operator performance benchmark skill; the current version only checks the existing environment, identifies the CANN version, and runs the benchmark after user confirmation, without handling environment fixes or installations.
