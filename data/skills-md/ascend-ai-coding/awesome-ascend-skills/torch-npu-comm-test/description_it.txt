通过 PyTorch torch.distributed 接口测试昇腾 NPU 通信算子性能。支持指定任意 tensor shape、dtype，使用 torchrun Utilizzare per testare operatori di comunicazione collettivi (AllReduce, AllGather, ReduceScatter e così via) con forme tensoriali specifiche tramite torch.distributed su Ascend NPU.
