Set the user-specified higher-level model as the Advisor to consult at decision points of the current task. It is used for review before confirming an approach, verification before declaring completion, and diagnosing blockages. The advisor model must be explicitly specified by the user when called, without a default; it does not operate if unspecified. It runs only when the user invokes it via /advisor‑strategy.
