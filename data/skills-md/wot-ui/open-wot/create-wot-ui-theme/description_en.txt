Generate single-file theme SCSS for wot-ui, and append `@use` to App.vue when the user explicitly requests integration. Use this when the user needs brand theming, semantic variable implementation, or single-file theme integration.
