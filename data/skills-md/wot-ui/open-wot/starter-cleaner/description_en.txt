Simplify the wot-starter v2 template to the minimal developable state, removing documentation, demo subpackages, generated files, and monorepo configuration, and concurrently cleaning up related Vite and package.json settings. Used when users request “clean template,” “remove examples,” “generate minimal template,” or “streamline wot-starter v2.”
