Gestisci il rilascio, la compilazione, il bump dell'SDK Python, i metadati del pacchetto, il pin del client PyPI, uv.lock, il flusso di lavoro nox/build e pubblica le modifiche di verifica. Utilizzare per il lavoro del processo di rilascio di Python o i dossi dei pin di dipendenza; non utilizzare per l'implementazione ordinaria delle funzionalità Python.
