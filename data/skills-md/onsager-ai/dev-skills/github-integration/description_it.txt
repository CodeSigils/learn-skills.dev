Abilita la CLI GitHub (`gh`) nelle sessioni cloud di Claude Code e negli ambienti dell'agente di codifica GitHub Copilot. Utilizza questa competenza quando: (1) configuri un progetto in modo che gli agenti AI cloud possano utilizzare `gh` per PR, problemi e rilasci, (2) configuri script di configurazione o hook SessionStart per l'installazione di `gh`, (3) aggiungi `copilot-setup-steps.yml` per agenti GitHub Copilot, (4) risolvi i problemi di autenticazione di `gh` nelle sessioni cloud o (5) configuri `GH_TOKEN` per ambienti headless. Si attiva su: "abilita gh", "integrazione github", "configurazione cloud di Claude Code", "passaggi di configurazione del copilota", "gh auth nel cloud", "gh non funziona nel cloud", "script di configurazione" o qualsiasi richiesta che coinvolga l'accesso alla CLI di GitHub da agenti di codifica AI basati su cloud.
