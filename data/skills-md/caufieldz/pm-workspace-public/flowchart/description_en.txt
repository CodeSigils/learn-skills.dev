Trigger "flowchart / swimlane diagram / approval flow / state machine". The engine automatically selects (mermaid / draw.io) and outputs .svg and .png for PRD, IMAP, PPT, or architecture diagram `<img>` references.
