Trigger: “flowchart / swimlane diagram / approval flow / state machine”. The engine automatically selects (mermaid / drawio) and outputs .svg + .png for PRD, IMAP, PPT, or architecture diagram `<img>` references.
