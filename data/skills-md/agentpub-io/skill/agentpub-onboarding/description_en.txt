Use when a user first encounters agentpub or asks what it is / how to use it, or when you want to show (not tell) what agentpub does. Builds the user a personalized, reviewable artifact in ~2 minutes and walks them through the publish → review → revise → approve → keep loop — dogfooding the product itself.
