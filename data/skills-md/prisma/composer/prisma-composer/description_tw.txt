如何使用 Prisma Composer (`@prisma/composer`) 編寫、測試和部署應用程式：使用 `compute()` 和類型化依賴項聲明服務，定義 RPC 合約，編寫模組，聲明服務輸入（配置和機密作為一個模式，使用 `input()` 讀回），編寫現成的 cron/storage/`b 模組，使用一個現成的 cron/storage/``s 模組編寫一個現成的 cron/storage/``s 模組編寫一個現成的 cron/storage/``s 模組提供一個現成的 cron/storage/``s 模組提供一個現成的 cron/storage/3`s 模組，使用現成的 cron/storage/``s 模組提供一個現成的 cron/storage/3`7），編寫現成的 cron/storage/``s 模組提供一個現成的 cron/storage/3`s 模組，編寫現成的 cron/storage/``s 模組提供一個現成的 cron /p. 兼容的原始对象存储桶，查找扩展（名为 `prisma-composer-*` 的 npm 包），使用 `mockService`/`bootstrapService` 进行测试，使用 `prisma-composer dev` 在本地运行整个应用程序，并使用 `prisma-composer log` 跟踪其日志，并使用 `prisma-composer deploy` 进行部署（阶段，销毁）。在建立 Prisma 應用程式、連接服務依賴項、新增 Postgres 資料庫、新增排程作業/blob 儲存體/事件流/原始儲存桶、為組合服務編寫測試、在本機執行應用程式、讀取其日誌或部署/拆除環境時使用。在「prisma writer」、「@prisma/composer」、「prisma app」、「compute()」、「service.load()」、「module()」、「contract()」、「mockService」、「bootstrapService」、「prisma-composer dev」、「prisma-com deploy」、「--stage」、「--fresh」、「--tail」上觸發「prisma-composer 銷毀」、「prisma-composer-」、「bucket()」。
