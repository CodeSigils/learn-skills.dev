كيفية كتابة تطبيق واختباره ونشره باستخدام Prisma Composer (`@prisma/composer`): الإعلان عن الخدمات باستخدام `compute()` والتبعيات المكتوبة، وتحديد عقود RPC، وإنشاء الوحدات النمطية، والإعلان عن إدخال الخدمة (التكوين والأسرار كمخطط واحد، والقراءة مرة أخرى باستخدام `input()`)، وإنشاء وحدات cron/storage/streams الجاهزة، وتوفير مجموعة تخزين كائنات خام متوافقة مع S3 مع `bucket()`، وابحث عن الملحقات (حزم npm المسماة `prisma-composer-*`)، واختبرها باستخدام `mockService`/`bootstrapService`، وقم بتشغيل التطبيق بأكمله محليًا باستخدام `prisma-composer dev` وألحق سجلاته باستخدام `prisma-composer log`، وانشره باستخدام `prisma-composer Publish` (مراحل، تدمير). يُستخدم عند إنشاء تطبيق Prisma، أو توصيل تبعية الخدمة، أو إضافة قاعدة بيانات Postgres، أو إضافة وظائف مجدولة / تخزين كائن كبير الحجم / تدفقات الأحداث / مجموعة أولية، أو كتابة اختبارات للخدمات المكونة، أو تشغيل تطبيق محليًا، أو قراءة سجلاته، أو نشر/تدمير البيئة. المشغلات على "مؤلف prisma"، "@prisma/composer"، "تطبيق prisma"، "compute()"، "service.load()"، "module()"، "contract()"، "mockService"، "bootstrapService"، "prisma-composer dev"، "سجل prisma-composer"، "نشر ملحن prisma"، "--stage"، "--fresh"، "--tail"، "prisma-composer" تدمير"، "prisma-composer-"، "bucket()".
