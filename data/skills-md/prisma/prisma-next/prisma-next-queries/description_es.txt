Escriba consultas de Prisma Next: elija un carril (`db.orm.<Model>` para CRUD e incluye, `db.sql.<table>` constructor SQL para formas de generador de conjuntos que el ORM no expresa), filtro/proyecto/clasificación/paginación, relaciones de carga ansiosa con `.include(...)`, transacciones a través de `db.transaction(...)`, agregados a través de `.aggregate(...)`. Úselo para consultar, donde, seleccionar, ordenar por, tomar, omitir, incluir, cargar ansioso, primero, todo, contar, agregar, crear, actualizar, eliminar, insertar, devolver, transacción, db.transaction, estilo llovizna, estilo kysely, cliente prisma, db.close, script, el script no sale, se bloquea, cierra la conexión, db.end, pool.end, espera usar. También cubre el consumo de resultados (`.all()` es un Thenable, simplemente `espera`; no se necesita ayuda `collect()` / `toArray()`), semántica de consumo único (`RUNTIME.ITERATOR_CONSUMED`), nulidad agregada (`count` devuelve `number`, `sum/avg/min/max` devuelve `number | null` según la semántica SQL) y condiciones de rango (encadena cláusulas `.where()` o usa `and(...)` — no hay `.entre(...)`).
