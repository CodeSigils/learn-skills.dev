Prisma Next クエリを作成します — レーンを選択し (CRUD およびインクルードの場合は `db.orm.<Model>`、ORM が表現しないセットビルダー形状の場合は `db.sql.<table>` SQL ビルダー)、フィルター / プロジェクト / ソート / ページ付け、`.include(...)` による Eager-load リレーション、`db.transaction(...)` によるトランザクション、`.aggregate(...)` による集計。クエリ、where、select、orderBy、take、skip、include、eagerload、first、all、count、aggregate、create、update、delete、upsert、return、transaction、db.transaction、drizzle-style、kysely-style、prisma client、db.close、script、script が終了しない、ハング、接続を閉じる、db.end、pool.end、await に使用します。また、結果の消費 (`.all()` は Thenable です。単に「待つだけ」です。`collect()` / `toArray()` ヘルパーは必要ありません)、単一消費セマンティクス (`RUNTIME.ITERATOR_CONSUMED`)、集計 NULL 可能性 (`count` は SQL セマンティクスごとに `number` を返し、`sum/avg/min/max` は `number | null` を返します)、および範囲条件 (チェーン) についても取り上げます。 `.where()` 句を使用するか、`and(...)` を使用します。`.between(...)` はありません)。
