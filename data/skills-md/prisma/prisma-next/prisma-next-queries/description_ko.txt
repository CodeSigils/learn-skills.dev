Prisma Next 쿼리 작성 — CRUD용 레인(`db.orm.<Model>`을 선택하고 ORM이 표현하지 않는 set-builder 모양을 위한 `db.sql.<table>` SQL 빌더를 포함), 필터/프로젝트/정렬/페이지 매김, `.include(...)`를 사용한 관계 즉시 로드, `db.transaction(...)`을 통한 트랜잭션, `.aggregate(...)`를 통해 집계합니다. 쿼리에 사용합니다. where, select, orderBy, take, Skip, include,eager load, first, all, count, Aggregate, create, update, delete, upsert, return, transaction, db.transaction, drizzle-style, kysely-style, prisma client, db.close, script, script will not exit, hangs, close connect, db.end, pool.end, wait using. 또한 결과 소비(`.all()`는 Thenable입니다. 그냥 `기다립니다. `collect()` / `toArray()` 도우미가 필요하지 않음), 단일 소비 의미 체계(`RUNTIME.ITERATOR_CONSUMED`), Null 허용 여부 집계(`count`는 `number`를 반환하고, `sum/avg/min/max`는 SQL 의미 체계에 따라 `number | null`을 반환) 및 범위 조건(연쇄 `.where()` 절 또는 사용을 다룹니다. `and(...)` — `.between(...)`)이 없습니다.
