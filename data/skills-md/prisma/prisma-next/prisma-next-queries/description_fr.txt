Écrivez des requêtes Prisma Next — choisissez une voie (`db.orm.<Model>` pour CRUD et inclut, `db.sql.<table>` Générateur SQL pour les formes de constructeur d'ensembles que l'ORM n'exprime pas), filtrez/projetez/triez/paginez, chargez les relations avec `.include(...)`, transactions via `db.transaction(...)`, agrégations via `.aggregate(...)`. Utiliser pour la requête, où, sélectionner, commander par, prendre, ignorer, inclure, charger rapidement, d'abord, tout, compter, agréger, créer, mettre à jour, supprimer, insérer, renvoyer, transaction, db.transaction, style bruine, style kysely, client prisma, db.close, script, le script ne se terminera pas, se bloque, ferme la connexion, db.end, pool.end, attend l'utilisation. Couvre également la consommation des résultats (`.all()` est un Thenable — il suffit de l'attendre ; aucun assistant `collect()` / `toArray()` n'est nécessaire), la sémantique de consommation unique (`RUNTIME.ITERATOR_CONSUMED`), la nullité globale (`count` renvoie `number`, `sum/avg/min/max` renvoie `number | null` selon la sémantique SQL) et les conditions de plage (chaîne `.where()` clauses ou utilisation `et(...)` — il n'y a pas de `.between(...)`).
