Scrivi query Prisma Next: scegli una corsia (`db.orm.<Model>` per CRUD e include, `db.sql.<table>` generatore SQL per forme di set-builder che l'ORM non esprime), filtro/progetto/ordinamento/impaginazione, relazioni di caricamento desideroso con `.include(...)`, transazioni tramite `db.transaction(...)`, aggregazioni tramite `.aggregate(...)`. Utilizzare per query, dove, seleziona, ordina per, prendi, salta, includi, caricamento desideroso, primo, tutto, conta, aggrega, crea, aggiorna, elimina, upsert, restituisce, transazione, db.transaction, stile drizzle, stile kysely, client prisma, db.close, script, lo script non esce, si blocca, chiude connessione, db.end, pool.end, attende di utilizzare. Copre anche il consumo dei risultati (`.all()` è un Thenable — basta `await`; non è necessario l'helper `collect()` / `toArray()`), la semantica a consumo singolo (`RUNTIME.ITERATOR_CONSUMED`), la nullabilità aggregata (`count` restituisce `number`, `sum/avg/min/max` restituisce `number | null` per semantica SQL) e le condizioni dell'intervallo (clausole chain `.where()` o use `and(...)` — non c'è ".tra(...)").
