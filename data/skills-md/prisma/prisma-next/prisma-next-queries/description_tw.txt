編寫 Prisma Next 查詢 - 選擇一個通道（`db.orm.<Model>` 用於 CRUD 和包含，`db.sql.<table>` SQL 構建器用於設定構建器形狀 ORM 不表達），過濾/項目/排序/分頁，使用 `.include(...)` 加載關係，透過 `db.transaction(...) 聚合交易，通過 1.用來查詢、where、select、orderBy、take、skip、include、eager load、first、all、count、aggregate、create、update、delete、upsert、returning、transaction、db.transaction、drizzle-style、kysely-style、prisma cliclient、db.close、也涵蓋結果消耗（`.all()` 是一個 Thenable — 只需 `await` 即可；不需要 `collect()` / `toArray()` 幫助程式）、單次消耗語意 (`RUNTIME.ITERATOR_CONSUMED`)、聚合可空性（每個 SQL 語意學，`count` 傳回 `number/max / null`）與範圍條件（鍊式 `.where()` 子句或使用`and(...)` — 沒有 `. Between(...)`)。
