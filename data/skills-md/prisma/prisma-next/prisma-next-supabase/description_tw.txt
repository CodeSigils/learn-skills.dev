透過 `@prisma-next/extension-supabase` 將 Prisma Next 與 Supabase 專案結合 — 連線 `extensionPacks: [supabasePack]`，將跨空間 FK 宣告為 `supabase:auth.AuthUser`，編寫 RLS 原則（`policy_select /supabase:auth.AuthUser``poli `@@rls`、`auth.uid()` 謂詞），使用以下命令建構 `db.ts` `supabase()` 工廠，根據請求綁定角色 (`asUser(jwt)` / `asAnon()` / `asServiceRole()`)，透過 `db.`sup `. JWT（對於目前專案為 `jwksUrl` / 對於舊版 HS256 為 `jwtSecret`）。適用於supabase、RLS、行級安全性、策略、角色綁定、anon、已驗證、service_role、auth.users、auth.uid()、JWT、JWKS、SUPABASE_JWKS_URL、SUPABASE_JWT_SECRET、InvalidJwtEror、SupanvUsero @prisma-next/擴充-supabase。
