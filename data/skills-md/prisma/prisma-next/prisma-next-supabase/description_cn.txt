通过 `@prisma-next/extension-supabase` 将 Prisma Next 与 Supabase 项目结合使用 — 连接 `extensionPacks: [supabasePack]`，将跨空间 FK 声明为 `supabase:auth.AuthUser`，编写 RLS 策略（`policy_select` / `policy_update` / `@@rls`、`auth.uid()` 谓词），使用以下命令构建 `db.ts` `supabase()` 工厂，根据请求绑定角色 (`asUser(jwt)` / `asAnon()` / `asServiceRole()`)，通过 `db.supabase` 管理根查询 `auth.*` / `storage.*`，并验证 JWT（对于当前项目为 `jwksUrl` / 对于旧版 HS256 为 `jwtSecret`）。用于supabase、RLS、行级安全性、策略、角色绑定、anon、已验证、service_role、auth.users、auth.uid()、JWT、JWKS、SUPABASE_JWKS_URL、SUPABASE_JWT_SECRET、InvalidJwtError、SupabaseConfigError、RoleBoundDb、会话池、supabase:auth.AuthUser、 @prisma-next/扩展-supabase。
