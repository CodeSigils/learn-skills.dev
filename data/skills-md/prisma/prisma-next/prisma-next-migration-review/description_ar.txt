قم بمراجعة عمليات ترحيل Prisma Next التي سيتم تشغيلها عند الدمج أو النشر، وعرض الرسم البياني للترحيل، وحل التعارضات المتزامنة/التقارب الماسي، وتكوين مراجع البيئة لـ CI. استخدم لـ "ما سيتم تشغيله من عمليات الترحيل"، "ما يتم تشغيله عند النشر"، دمج الصراع، تقارب الماس، عمليات الترحيل المتزامنة، حالة الترحيل، إدارة المرجع، التدريج، الإنتاج، MIGRATION.DIVERGED، MIGRATION.NO_MARKER، MIGRATION.MARKER_NOT_IN_HISTORY، حالة ترحيل prisma، ترحيل prisma diff، حل ترحيل prisma.
