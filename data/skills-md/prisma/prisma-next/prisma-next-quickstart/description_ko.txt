Prisma Next를 새 프로젝트, 기존 데이터베이스에 채택하거나 부트스트랩 도구를 사용하여 비계에 넣은 후 첫 번째 이동으로 채택하세요. "Prisma Next로 무엇을 할 수 있나요?", "Prisma로 다음에 무엇을 할 수 있나요?", "어디에서 시작해야 할까요?", "먼저 무엇을 해야 할까요?", "createprisma를 실행했습니다.", "createprisma", "npx createprisma", "npx create-prisma", "첫 번째 단계", "첫 번째 쿼리", "지금 스캐폴드된 Prisma Next 프로젝트가 있습니다."에 사용하세요. `pnpm dlx prisma-next init` 그린필드 설정용; 기존 데이터베이스에 대한 'prisma-next contract infer' + 'db sign'의 경우. 또한 연결-쓰기-읽기 첫 번째 아크 방향, 일상적인 명령(`contract Emit`, `db init`, `db update`, `migration plan`, ` migration`, `dbschema`, `db verify`) 및 다음 이동을 위한 `prisma-next-contract` / `prisma-next-queries` / `prisma-next-runtime`으로의 라우팅을 다룹니다. 플래그: --target, --authoring, --schema-path, --probe-db, --output.
