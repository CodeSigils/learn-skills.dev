Adopte Prisma Next en un nuevo proyecto, en una base de datos existente o como primer paso después de que una herramienta de arranque lo dejó en un andamio. Úselo para "¿qué puedo hacer con Prisma Next", "qué puedo hacer a continuación con Prisma", "por dónde empiezo", "qué debo hacer primero", "acabo de ejecutar createprisma", "createprisma", "npx createprisma", "npx create-prisma", "primeros pasos", "primera consulta", "Tengo un proyecto Prisma Next con andamios y ahora qué"; para la configuración totalmente nueva de `pnpm dlx prisma-next init`; y para `prisma-next contract infer` + `db sign` contra una base de datos existente. También cubre la orientación del primer arco de conexión-escritura-lectura, los comandos del día a día ("contract emit", "db init", "db update", "migration plan", "migrate", "db Scheme", "db verificar") y el enrutamiento a "prisma-next-contract" / "prisma-next-queries" / "prisma-next-runtime" para el siguiente movimiento. Banderas: --target, --authoring, --schema-path, --probe-db, --output.
