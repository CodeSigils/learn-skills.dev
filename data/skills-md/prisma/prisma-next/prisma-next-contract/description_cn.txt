编辑 Prisma Next 数据合约 — 添加模型、字段、关系、索引、枚举、类型别名、多态类型 (`@@discriminator` / `@@base`)、使用扩展命名空间 (`pgvector.Vector(...)`、`cipherstash.EncryptedString(...)`)、将 `prisma-next.config.ts` 与 `@prisma-next/<target>/config` 中的 `defineConfig` 连接façade，然后运行“prisma-nextcontract emit”。用于架构、模型、字段、属性、软删除、偏执、范围、验证、回调、prisma 架构、PSL、contract.prisma、contract.ts、contract.json、contract.d.ts、外观导入、`@prisma-next/postgres/config`、`@prisma-next/postgres/contract-builder`、`@prisma-next/postgres/control`、 `@prisma-next/mongo/config`、`@prisma-next/mongo/contract-builder`、`扩展：`、`extensionPacks`、pgvector、cipherstash、postgis、paradedb、PN-CLI-4002、PN-CLI-4003、PN-CLI-4011。
