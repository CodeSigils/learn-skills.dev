Prisma Next 데이터 계약 편집 — 모델, 필드, 관계, 인덱스, 열거형, 유형 별칭, 다형성 유형(`@@discriminator` / `@@base`) 추가, 확장 네임스페이스(`pgVector.Vector(...)`, `cipherstash.EncryptedString(...)`) 사용, `@prisma-next/<target>/config`에서 `defineConfig`와 함께 `prisma-next.config.ts` 연결 façade를 실행하고 'prisma-next Contract Emit'을 실행합니다. 스키마, 모델, 필드, 속성, 일시 삭제, 편집증, 범위, 유효성 검사, 콜백, prisma 스키마, PSL, contract.prisma, contract.ts, contract.json, contract.d.ts, façade 가져오기, `@prisma-next/postgres/config`, `@prisma-next/postgres/contract-builder`, `@prisma-next/postgres/control`에 사용 `@prisma-next/mongo/config`, `@prisma-next/mongo/contract-builder`, `extensions:`, `extensionPacks`, pgVector, cipherstash, postgis, paradb, PN-CLI-4002, PN-CLI-4003, PN-CLI-4011.
