編輯 Prisma Next 資料合約 — 新增模型、欄位、關係、索引、枚舉、類型別名、多型別 (`@@discriminator` / `@@base`)、使用擴充命名空間 (`pgvector.Vector(...)`、`cipherstash.Encrypted (`pgvector.Vector(...)`、`cipherstash.Encrypted(...)( `@prisma-next/<target>/config` 中的 `defineConfig` 連接façade，然後執行「prisma-nextcontract emit」。用於架構、模型、欄位、屬性、軟體刪除、偏執、範圍、驗證、回呼、prisma 架構、PSL、contract.prisma、contract.ts、contract.json、contract.d.ts、外觀導入、`@prisma-next/postgres/config`、`@prisma-next/posts/contract/M. `@prisma-next/mongo/config`、`@prisma-next/mongo/contract-builder`、`擴充：`、`extensionPacks`、pgvector、cipherstash、postgis、paradedb、PN-CLI-4002、PN-CLI-4003、PN-CLILI。
