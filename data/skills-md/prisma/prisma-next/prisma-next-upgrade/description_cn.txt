在您的应用程序中升级 Prisma Next。将每个“@prisma-next/*”依赖项从锁定文件中固定的版本映射到请求的目标（或 npm“latest”），应用每次转换升级指令中的任何所需的代码翻译步骤，使用项目自己的类型检查+测试进行验证，并自行提交每个小步骤。当用户请求“升级 Prisma Next”、“升级 Prisma Next”、“移动到 Prisma Next X.Y”或要求代理处理其应用程序中的“@prisma-next/*”小升级时使用。
