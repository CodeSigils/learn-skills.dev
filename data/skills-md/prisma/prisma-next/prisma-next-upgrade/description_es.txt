Actualice Prisma Next en su aplicación. Cambia cada dependencia `@prisma-next/*` desde la versión fijada en el archivo de bloqueo hasta el destino solicitado (o npm `latest`), aplica los pasos de traducción de código requeridos de las instrucciones de actualización por transición, valida con las pruebas de verificación de tipos + propias del proyecto y confirma cada paso menor por sí solo. Úselo cuando el usuario solicita "actualizar Prisma Next", "actualizar Prisma Next", "mover a Prisma Next X.Y" o le pide a un agente que se ocupe de un problema menor `@prisma-next/*` en su aplicación.
