قم بتوصيل وقت تشغيل Prisma Next - إعداد `db.ts` باستخدام `postgres<Contract>(...)` من `@prisma-next/postgres/runtime`، وتكوين البرامج الوسيطة (القياس عن بعد من `@prisma-next/middleware-telemetry`؛ الوبر والميزانيات)، تكوين `DATABASE_URL`، التفرع لكل بيئة، التبديل بين واجهات بوستجرس ومونجو. يستخدم لـ db.ts، postgres()، mongo()، البرامج الوسيطة، القياس عن بعد، lints، الميزانيات، DATABASE_URL، .env، تجمع الاتصال، PoolOptions، dev vs prod config، المعاملات، db.transaction، قراءة النسخ المتماثلة، قاعدة بيانات متعددة، البرنامج النصي لن يخرج، توقف، اتصال وثيق، db.end، db. Close، Pool.end، [Symbol.asyncDispose]، في انتظار الاستخدام.
