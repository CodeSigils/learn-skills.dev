Prisma Next 런타임 연결 — `@prisma-next/postgres/runtime`의 `postgres<Contract>(...)`를 사용한 `db.ts` 설정, 미들웨어 구성(`@prisma-next/middleware-telemetry`의 원격 측정, Lints 및 예산), `DATABASE_URL` 구성, 환경별 분기, Postgres와 Mongo Façade 간 전환. db.ts, postgres(), mongo(), 미들웨어, 원격 측정, lints, 예산, DATABASE_URL, .env, 연결 풀, poolOptions, dev 대 prod 구성, 트랜잭션, db.transaction, 읽기 복제본, 다중 데이터베이스, 스크립트가 종료되지 않음, 정지, 연결 닫기, db.end, db.close, pool.end, [Symbol.asyncDispose], 사용 대기에 사용됩니다.
