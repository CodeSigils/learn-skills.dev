Câblez le runtime Prisma Next — configuration `db.ts` à l'aide de `postgres<Contract>(...)` de `@prisma-next/postgres/runtime`, composition middleware (télémétrie de `@prisma-next/middleware-telemetry`; peluches et budgets), configuration `DATABASE_URL`, branchement par environnement, commutation entre les façades Postgres et Mongo. À utiliser pour db.ts, postgres(), mongo(), middleware, télémétrie, lints, budgets, DATABASE_URL, .env, pool de connexions, poolOptions, dev vs prod config, transactions, db.transaction, lecture des répliques, multi-bases de données, le script ne se ferme pas, se bloque, ferme la connexion, db.end, db.close, pool.end, [Symbol.asyncDispose], attend l'utilisation.
