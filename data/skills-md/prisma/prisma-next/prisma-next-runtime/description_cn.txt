连接 Prisma Next 运行时 — 使用来自 `@prisma-next/postgres/runtime` 的 `postgres<Contract>(...)` 设置 `db.ts`、中间件组合（来自 `@prisma-next/middleware-telemetry` 的遥测；lints 和预算）、`DATABASE_URL` 配置、每个环境分支、Postgres 和 Mongo 外观之间的切换。用于 db.ts、postgres()、mongo()、中间件、遥测、lints、预算、DATABASE_URL、.env、连接池、poolOptions、开发与生产配置、事务、db.transaction、只读副本、多数据库、脚本不会退出、挂起、关闭连接、db.end、db.close、pool.end、[Symbol.asyncDispose]、等待使用。
