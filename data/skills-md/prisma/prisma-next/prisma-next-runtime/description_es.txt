Conecte el tiempo de ejecución de Prisma Next: configuración de `db.ts` usando `postgres<Contract>(...)` de `@prisma-next/postgres/runtime`, composición de middleware (telemetría de `@prisma-next/middleware-telemetry`; pelusas y presupuestos), configuración de `DATABASE_URL`, ramificación por entorno, cambio entre fachadas de Postgres y Mongo. Úselo para db.ts, postgres(), mongo(), middleware, telemetría, pelusas, presupuestos, DATABASE_URL, .env, grupo de conexiones, poolOptions, configuración dev vs prod, transacciones, db.transaction, lectura de réplicas, bases de datos múltiples, el script no sale, se bloquea, cierra la conexión, db.end, db.close, pool.end, [Symbol.asyncDispose], espera su uso.
