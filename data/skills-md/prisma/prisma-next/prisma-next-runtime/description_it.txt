Cabla il runtime di Prisma Next: configurazione di `db.ts` utilizzando `postgres<Contract>(...)` da `@prisma-next/postgres/runtime`, composizione del middleware (telemetria da `@prisma-next/middleware-telemetry`; lint e budget), configurazione di `DATABASE_URL`, ramificazione per ambiente, passaggio tra le facciate Postgres e Mongo. Utilizzare per db.ts, postgres(), mongo(), middleware, telemetria, lint, budget, DATABASE_URL, .env, pool di connessioni, poolOptions, configurazione dev vs prod, transazioni, db.transaction, lettura repliche, multi-database, lo script non esce, si blocca, chiude la connessione, db.end, db.close, pool.end, [Symbol.asyncDispose], attende l'uso.
