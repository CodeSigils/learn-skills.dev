連接 Prisma Next 運行時 — 使用來自 `@prisma-next/postgres/runtime` 的 `postgres<Contract>(...)` 設定 `db.ts`、中間件組合（來自 `@prisma-next/middleware-telemetry` 的遙測；用於 db.ts、postgres()、mongo()、中間件、遙測、lints、預算、DATABASE_URL、.env、連接池、poolOptions、開發與生產配置、交易、db.transaction、只讀副本、多資料庫、腳本不會退出、掛起、關閉連接、db.end、db.transaction、只讀副本、多資料庫、腳本不會退出、掛起、關閉連接、db.end、db.db.Iu.
