Подключите среду выполнения Prisma Next — настройка `db.ts` с использованием` postgres<Contract>(...)` из `@prisma-next/postgres/runtime`, композиция промежуточного программного обеспечения (телеметрия из `@prisma-next/middleware-telemetry`; lints и бюджеты), конфигурация `DATABASE_URL`, ветвление для каждой среды, переключение между фасадами Postgres и Mongo. Используется для db.ts, postgres(), mongo(), промежуточного программного обеспечения, телеметрии, lints, бюджетов, DATABASE_URL, .env, пула подключений, PoolOptions, конфигурации dev vs prod, транзакций, db.transaction, реплик чтения, нескольких баз данных, сценария не завершается, зависает, закрывает соединение, db.end, db.close,pool.end, [Symbol.asyncDispose], ожидает использования.
