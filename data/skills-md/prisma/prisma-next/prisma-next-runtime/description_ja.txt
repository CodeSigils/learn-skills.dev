Prisma Next ランタイムを接続します — `@prisma-next/postgres/runtime` の `postgres<Contract>(...)` を使用した `db.ts` セットアップ、ミドルウェア構成 (`@prisma-next/middleware-telemetry` からのテレメトリ、lint とバジェット)、`DATABASE_URL` 構成、環境ごとの分岐、Postgres と Mongo 間の切り替えファサード。 db.ts、postgres()、mongo()、ミドルウェア、テレメトリ、lints、予算、DATABASE_URL、.env、接続プール、poolOptions、dev vs prod config、トランザクション、db.transaction、リードレプリカ、マルチデータベース、スクリプトが終了しない、ハングする、接続を閉じる、db.end、db.close、pool.end、[Symbol.asyncDispose]、await に使用します。を使用しています。
