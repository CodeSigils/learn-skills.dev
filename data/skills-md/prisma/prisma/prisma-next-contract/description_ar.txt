تحرير عقد بيانات Prisma Next - إضافة نماذج، وحقول، وعلاقات، وفهارس، وتعدادات، وكائنات قيمة (أنواع مركبة)، وأسماء مستعارة للكتابة، ومساحات أسماء (مخططات Postgres)، ومفاتيح خارجية متقاطعة (FK متقاطعة)، وأنواع متعددة الأشكال (`@@discriminator` / `@@base`)، واستخدام مساحات الأسماء الملحقة (`pgvector.Vector(...)`، `cipherstash.EncryptedString(...)`)، قم بتوصيل prisma-next.config.ts` مع `defineConfig` من الواجهة `@internal/<target>/config`، وقم بتشغيل `prisma-next Contract emit`. يُستخدم للمخطط والنماذج والحقول والسمات والحذف الناعم والجنون العظمة والنطاقات وعمليات التحقق من الصحة وعمليات الاسترجاعات ومخطط المنشور وPSL وcontract.prisma وcontract.ts وcontract.json وcontract.d.ts و`@internal/postgres/config` و`@internal/postgres/contract-builder` و`@internal/postgres/control`، `@internal/mongo/config`، `@internal/mongo/contract-builder`، `extensions:`، pgvector، cipherstash، postgis، paradedb، supabase، `@internal/extension-supabase`، `@@control`، سياسة التحكم، مُدارة، مسموح بها، خارجية، تمت ملاحظتها.
