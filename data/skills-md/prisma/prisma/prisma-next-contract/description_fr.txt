Modifiez le contrat de données Prisma Next — ajoutez des modèles, des champs, des relations, des index, des énumérations, des objets de valeur (types composites), des alias de type, des espaces de noms (schémas Postgres), des clés étrangères de contrats croisés (FK inter-espace), des types polymorphes (`@@discriminator` / `@@base`), utilisez des espaces de noms d'extension (`pgvector.Vector(...)`, `cipherstash.EncryptedString(...)`), connectez `prisma-next.config.ts` avec `defineConfig` à partir de la façade `@internal/<target>/config` et exécutez `prisma-next contract submit`. À utiliser pour les schémas, modèles, champs, attributs, suppression logicielle, paranoïaque, portées, validations, rappels, schéma prisma, PSL, contract.prisma, contract.ts, contract.json, contract.d.ts, `@internal/postgres/config`, `@internal/postgres/contract-builder`, `@internal/postgres/control`, `@internal/mongo/config`, `@internal/mongo/contract-builder`, `extensions:`, pgvector, cipherstash, postgis, paradedb, supabase, `@internal/extension-supabase`, `@@control`, politique de contrôle, gérée, tolérée, externe, observée.
