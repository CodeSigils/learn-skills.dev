Prisma Next 데이터 계약 편집 - 모델, 필드, 관계, 인덱스, 열거형, 값 개체(복합 유형), 유형 별칭, 네임스페이스(Postgres 스키마), 상호 계약 외래 키(공간 간 FK), 다형성 유형(`@@discriminator` / `@@base`), 확장 네임스페이스 사용(`pgVector.Vector(...)`, `cipherstash.EncryptedString(...)`), 연결 추가 `@internal/<target>/config` façade의 `defineConfig`를 포함하는 `prisma-next.config.ts`를 실행하고 `prisma-next contract submit`을 실행합니다. 스키마, 모델, 필드, 속성, 일시 삭제, 편집증, 범위, 유효성 검사, 콜백, 프리즈마 스키마, PSL, contract.prisma, contract.ts, contract.json, contract.d.ts, `@internal/postgres/config`, `@internal/postgres/contract-builder`, `@internal/postgres/control`, `@internal/mongo/config`에 사용 `@internal/mongo/contract-builder`, `확장:`, pgVector, cipherstash, postgis, paradb, supabase, `@internal/extension-supabase`, `@@control`, 제어 정책, 관리됨, 허용됨, 외부, 관찰됨.
