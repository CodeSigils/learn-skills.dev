Adotta Prisma Next in un nuovo progetto, in un database esistente o come prima mossa dopo che uno strumento di bootstrap ti ha gettato su un'impalcatura. Utilizzare per "cosa posso fare con Prisma Next", "cosa posso fare dopo con Prisma", "da dove comincio", "cosa dovrei fare prima", "ho appena eseguito createprisma", "createprisma", "npx createprisma", "npx create-prisma", "primi passi", "prima query", "Ho un progetto Prisma Next impalcato e adesso"; per la configurazione greenfield di `pnpm dlx prisma-next init`; e per "prisma-next contract infer" + "db sign" rispetto a un database esistente. Copre anche l'orientamento del primo arco di connessione-scrittura-lettura, i comandi quotidiani (`contract emit`, `db init`, `db update`, `migration plan`, `migrate`, `db schema`, `db verify`) e l'instradamento a `prisma-next-contract` / `prisma-next-queries` / `prisma-next-runtime` per la mossa successiva. Flag: --target, --authoring, --schema-path, --probe-db, --output.
