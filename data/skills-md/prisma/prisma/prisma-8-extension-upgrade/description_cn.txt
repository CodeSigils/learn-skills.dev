在您的扩展中升级 Prisma Next。将每个“@internal/*”依赖项添加到请求的目标（或 npm“latest”），运行扩展 SPI 的每次转换升级指令（中间件生命周期、编解码器/迁移工具/框架组件搅动、种子迁移磁盘形状），通过“prisma-8-check-pins”验证引脚是否正确，运行扩展自己的类型检查和测试，并自行提交每个小步骤。当用户要求在扩展包中“升级 Prisma Next”，或将扩展的“@internal/*” deps 更新为新的次要版本时使用。
