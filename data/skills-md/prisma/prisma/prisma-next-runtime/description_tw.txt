連接 Prisma Next 執行時間 — 使用 `@internal/postgres/runtime` 中的 `postgres<Contract>(...)`、`@internal/sqlite/runtime` 中的 `sqlite<Contract>(...)` 或 `@internal/mongo/runtime` 中的 `sqlite<Contract>(...)` 或 `@internal/mongo/runtime` 中的 `mongo``o> `db.ts`；中介軟體組合（來自「@internal/middleware-telemetry」的遙測；lints 和預算）、「DATABASE_URL」配置、每個環境的分支、Postgres、SQLite 和 Mongo 外觀之間的切換。用於 db.ts、postgres()、sqlite()、mongo()、中間件、遙測、lints、預算、DATABASE_URL、.env、連接池、poolOptions、dev vs prod 配置、交易、db.transaction、只讀副本、多資料庫、腳本不會退出、掛起、最後連接、db.end、Di.close、bolsmend.close.，等待。
