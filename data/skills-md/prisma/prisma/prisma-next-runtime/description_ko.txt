Prisma Next 런타임 연결 — `@internal/postgres/runtime`의 `postgres<Contract>(...)`, `@internal/sqlite/runtime`의 `sqlite<Contract>(...)` 또는 `@internal/mongo/runtime`의 `mongo<Contract>(...)`를 사용하여 `db.ts` 설정; 미들웨어 구성(`@internal/middleware-telemetry`의 원격 측정, 린트 및 예산), `DATABASE_URL` 구성, 환경별 분기, Postgres, SQLite 및 Mongo 외관 간 전환. db.ts, postgres(), sqlite(), mongo(), 미들웨어, 원격 측정, lints, 예산, DATABASE_URL, .env, 연결 풀, poolOptions, dev 대 prod 구성, 트랜잭션, db.transaction, 읽기 전용 복제본, 다중 데이터베이스, 스크립트가 종료되지 않음, 중단, 연결 닫기, db.end, db.close, pool.end에 사용됩니다. [Symbol.asyncDispose], 사용을 기다립니다.
