Подключите среду выполнения Prisma Next — настройка `db.ts` с использованием `postgres<Contract>(...)` из `@internal/postgres/runtime`, `sqlite<Contract>(...)` из `@internal/sqlite/runtime` или `mongo<Contract>(...)` из `@internal/mongo/runtime`; состав промежуточного программного обеспечения (телеметрия из @internal/middleware-telemetry; тесты и бюджеты), конфигурация DATABASE_URL, ветвление по средам, переключение между фасадами Postgres, SQLite и Mongo. Используется для db.ts, postgres(), sqlite(), mongo(), промежуточного программного обеспечения, телеметрии, lints, бюджетов, DATABASE_URL, .env, пула соединений, PoolOptions, конфигурации dev vs prod, транзакций, db.transaction, реплик чтения, нескольких баз данных, скрипт не завершается, зависает, закрывает соединение, db.end, db.close,pool.end, [Symbol.asyncDispose], дождитесь использования.
