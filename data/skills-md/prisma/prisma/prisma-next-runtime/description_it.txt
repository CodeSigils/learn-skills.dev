Collega il runtime Prisma Next — la configurazione di `db.ts` utilizzando `postgres<Contract>(...)` da `@internal/postgres/runtime`, `sqlite<Contract>(...)` da `@internal/sqlite/runtime` o `mongo<Contract>(...)` da `@internal/mongo/runtime`; composizione del middleware (telemetria da `@internal/middleware-telemetry`; lint e budget), configurazione di `DATABASE_URL`, ramificazione per ambiente, passaggio tra le facciate Postgres, SQLite e Mongo. Utilizzare per db.ts, postgres(), sqlite(), mongo(), middleware, telemetria, lint, budget, DATABASE_URL, .env, pool di connessione, poolOptions, configurazione dev vs prod, transazioni, db.transaction, lettura repliche, multi-database, lo script non esce, si blocca, chiude la connessione, db.end, db.close, pool.end, [Symbol.asyncDispose], attende di essere utilizzato.
