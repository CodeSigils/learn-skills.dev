قم بتوصيل وقت التشغيل التالي لـ Prisma - إعداد `db.ts` باستخدام `postgres<Contract>(...)` من `@internal/postgres/runtime`، أو `sqlite<Contract>(...)` من `@internal/sqlite/runtime`، أو `mongo<Contract>(...)` من `@internal/mongo/runtime`؛ تكوين البرامج الوسيطة (القياس عن بعد من `@internal/middleware-telemetry`؛ الوبر والميزانيات)، وتكوين `DATABASE_URL`، والتفرع لكل بيئة، والتبديل بين واجهات Postgres وSQLite وMongo. يستخدم لـ db.ts، postgres()، sqlite()، mongo()، البرامج الوسيطة، القياس عن بعد، lints، الميزانيات، DATABASE_URL، .env، تجمع الاتصال، PoolOptions، dev vs prod config، المعاملات، db.transaction، قراءة النسخ المتماثلة، قاعدة بيانات متعددة، البرنامج النصي لن يخرج، توقف، اتصال وثيق، db.end، db. Close، Pool.end، [Symbol.asyncDispose]، في انتظار الاستخدام.
