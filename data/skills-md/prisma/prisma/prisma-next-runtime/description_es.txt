Conecte el tiempo de ejecución de Prisma Next: configuración de `db.ts` usando `postgres<Contract>(...)` de `@internal/postgres/runtime`, `sqlite<Contract>(...)` de `@internal/sqlite/runtime`, o `mongo<Contract>(...)` de `@internal/mongo/runtime`; composición de middleware (telemetría de `@internal/middleware-telemetry`; pelusas y presupuestos), configuración de `DATABASE_URL`, ramificación por entorno, cambio entre fachadas de Postgres, SQLite y Mongo. Úselo para db.ts, postgres(), sqlite(), mongo(), middleware, telemetría, pelusas, presupuestos, DATABASE_URL, .env, grupo de conexiones, opciones de grupo, configuración dev vs prod, transacciones, db.transaction, lectura de réplicas, bases de datos múltiples, el script no sale, se bloquea, cierra la conexión, db.end, db.close, pool.end, [Symbol.asyncDispose], espera su uso.
