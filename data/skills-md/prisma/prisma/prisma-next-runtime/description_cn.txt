连接 Prisma Next 运行时 — 使用 `@internal/postgres/runtime` 中的 `postgres<Contract>(...)`、`@internal/sqlite/runtime` 中的 `sqlite<Contract>(...)` 或 `@internal/mongo/runtime` 中的 `mongo<Contract>(...)` 设置 `db.ts`；中间件组合（来自“@internal/middleware-telemetry”的遥测；lints 和预算）、“DATABASE_URL”配置、每个环境的分支、Postgres、SQLite 和 Mongo 外观之间的切换。用于 db.ts、postgres()、sqlite()、mongo()、中间件、遥测、lints、预算、DATABASE_URL、.env、连接池、poolOptions、dev vs prod 配置、事务、db.transaction、只读副本、多数据库、脚本不会退出、挂起、关闭连接、db.end、db.close、pool.end、 [Symbol.asyncDispose]，等待使用。
