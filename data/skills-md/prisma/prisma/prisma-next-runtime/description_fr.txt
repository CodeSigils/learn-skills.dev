Câblez le runtime Prisma Next — configuration `db.ts` à l'aide de `postgres<Contract>(...)` de `@internal/postgres/runtime`, `sqlite<Contract>(...)` de `@internal/sqlite/runtime` ou `mongo<Contract>(...)` de `@internal/mongo/runtime` ; composition du middleware (télémétrie de `@internal/middleware-telemetry`; peluches et budgets), configuration `DATABASE_URL`, branchement par environnement, commutation entre les façades Postgres, SQLite et Mongo. Utiliser pour db.ts, postgres(), sqlite(), mongo(), middleware, télémétrie, lints, budgets, DATABASE_URL, .env, pool de connexions, poolOptions, dev vs prod config, transactions, db.transaction, réplicas en lecture, multi-base de données, le script ne se ferme pas, se bloque, ferme la connexion, db.end, db.close, pool.end, [Symbol.asyncDispose], wait en utilisant.
