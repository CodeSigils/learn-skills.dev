Prisma Next ランタイムを接続します — `@internal/postgres/runtime` の `postgres<Contract>(...)`、`@internal/sqlite/runtime` の `sqlite<Contract>(...)`、または `@internal/mongo/runtime` の `mongo<Contract>(...)` を使用して `db.ts` セットアップを接続します。ミドルウェア構成 (`@internal/middleware-telemetry` からのテレメトリ、lint とバジェット)、`DATABASE_URL` 構成、環境ごとの分岐、Postgres、SQLite、Mongo ファサード間の切り替え。 db.ts、postgres()、sqlite()、mongo()、ミドルウェア、テレメトリ、lints、予算、DATABASE_URL、.env、接続プール、poolOptions、dev vs prod config、トランザクション、db.transaction、リードレプリカ、マルチデータベース、スクリプトが終了しない、ハングする、接続を閉じる、db.end、db.close、pool.end、に使用します。 [Symbol.asyncDispose]、使用を待機します。
