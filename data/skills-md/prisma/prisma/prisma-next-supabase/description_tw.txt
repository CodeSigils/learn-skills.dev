透過 `@internal/extension-supabase` 將 Prisma Next 與 Supabase 專案結合使用 — 連線 `extensions: [supabasePack]`，將跨空間 FK 宣告為 `supabase:auth.AuthUser`，編寫 RLS 策略（`policy_select` /``cy_hepate`.謂詞），使用以下指令建置 `db.ts` `supabase()` 工廠，根據請求綁定角色 (`asUser(jwt)` / `asAnon()` / `asServiceRole()`)，透過 `db.asServiceRole().supabase` 管理根查詢根 `auth.*`/`jks 2/`3 的專案/``舊版 HS256 的 `jwtSecret`）。用於supabase、RLS、行級安全性、策略、角色綁定、anon、已驗證、service_role、auth.users、auth.uid()、JWT、JWKS、SUPABASE_JWKS_URL、SUP ABASE_JWT_SECRET、SUPABASE.JWT_INVALID、SUPABASE.CONFIG_INVALID、RoleBoundDb、會話池程式、supabase:auth.AuthUser、 @內部/擴展-supabase。
