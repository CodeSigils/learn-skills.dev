استخدم Prisma Next مع مشروع Supabase عبر `@internal/extension-supabase` - قم بتوصيل الامتدادات: [supabasePack]`، قم بتعريف FKs عبر الفضاء إلى `supabase:auth.AuthUser`، سياسات RLS للمؤلف (`policy_select` / `policy_update` / `@@rls`، `auth.uid()` المسندات)، البناء `db.ts` مع مصنع `supabase()`، وربط الأدوار لكل طلب (`asUser(jwt)` / `asAnon()` / `asServiceRole()`)، والاستعلام عن `auth.*` / `storage.*` عبر جذر المسؤول `db.asServiceRole().supabase`، والتحقق من صحة JWTs (`jwksUrl` للمشاريع الحالية / `jwtSecret` لـ HS256 القديم). يُستخدم لـ supabase وRLS وأمان مستوى الصف والسياسة وربط الأدوار وanon والمصادقة وservice_role وauth.users وauth.uid() وJWT وJWKS وSUPABASE_JWKS_URL وSUPABASE_JWT_SECRET وSUPABASE.JWT_INVALID وSUPABASE.CONFIG_INVALID وRoleBoundDb، مجمع الجلسة، supabase:auth.AuthUser، @internal/extension-supabase.
