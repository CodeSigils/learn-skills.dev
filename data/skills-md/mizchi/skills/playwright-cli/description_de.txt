Wird verwendet, wenn Playwright über die Terminal-CLI ausgeführt wird – „npx playwright test“ (Testläufer), „codegen“ (interaktive Aufzeichnung), „screenshot“ / „pdf“ (einmalige Aufnahmen) und CI-Sharding. NICHT für agentengesteuerte Echtzeit-Browsersteuerung (verwenden Sie dazu die MCP-Tools „claude-in-chrome“).
