استخدمه عند تشغيل Playwright عبر محطة CLI — `npx playwright test` (مُجري الاختبار)، و`codegen` (التسجيل التفاعلي)، و`screenshot` / `pdf` (لقطات لمرة واحدة)، وتقسيم CI. ليس للتحكم في المتصفح في الوقت الفعلي الذي يحركه الوكيل (استخدم أدوات MCP `clude-in-chrome` لذلك).
