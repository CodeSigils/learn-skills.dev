Da utilizzare quando si esegue Playwright tramite la CLI del terminale: `npx playwright test` (test runner), `codegen` (registrazione interattiva), `screenshot`/`pdf` (acquisizioni una tantum) e sharding CI. NON per il controllo del browser in tempo reale gestito da agenti (utilizzare gli strumenti MCP `claude-in-chrome` per questo).
