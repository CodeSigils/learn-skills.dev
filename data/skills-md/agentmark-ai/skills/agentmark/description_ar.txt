إنشاء مطالبات AgentMark ومجموعات البيانات والتجارب والتقييمات وتصحيحها وشحنها. TRIGGER: أي ملف `.prompt.mdx`، أو `agentmark.json`، أو `agentmark.client.ts`، أو `agentmark_client.py`، أو دليل `agentmark/` أو `.agentmark/`، أو واردات `@agentmark-ai/*`؛ `npm create agentmark` / `npx create-agentmark` أو أي أمر `agentmark` CLI (`dev`، `run-prompt`، `run-experiment`، `generate-types`، `generate-schema`، `link`، `pull-models`)؛ قيادة AgentMark Cloud برمجيًا (`agentmark-mcp`، وتوفير التطبيقات بدون رأس، وعمليات النشر المستندة إلى git)؛ أي ذكر لـ AgentMark، أو الإصدار الفوري، أو تجارب مجموعة البيانات، أو التقييمات السريعة، أو عمليات النشر، أو تتبع إمكانية المراقبة في مشروع AgentMark. يتم تشغيله حتى بالنسبة للطلبات من الذاكرة/عدم جلب المستندات. SKIP: رمز موجه محايد للموفر بدون علامات AgentMark؛ LangChain أو LlamaIndex أو كود OpenAI / Anthropic SDK الخام؛ الأسئلة العامة المتعلقة بالهندسة السريعة أو إمكانية ملاحظة LLM بدون سياق AgentMark؛ المنصات المنافسة (Langfuse، LangSmith، Phoenix، Braintrust، Traceloop).
