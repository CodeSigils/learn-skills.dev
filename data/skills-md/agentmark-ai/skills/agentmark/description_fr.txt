Créez, déboguez et expédiez des invites, des ensembles de données, des expériences et des évaluations AgentMark. DÉCLENCHEUR : tout fichier `.prompt.mdx`, `agentmark.json`, `agentmark.client.ts`, `agentmark_client.py`, un répertoire `agentmark/` ou `.agentmark/`, ou des importations `@agentmark-ai/*` ; `npm create agentmark` / `npx create-agentmark` ou toute commande CLI `agentmark` (`dev`, `run-prompt`, `run-experiment`, `generate-types`, `generate-schema`, `link`, `pull-models`) ; piloter AgentMark Cloud par programmation (`agentmark-mcp`, provisionnement d'applications sans tête, déploiements basés sur git) ; toute mention d'AgentMark, ou de versionnement d'invites, d'expériences d'ensembles de données, d'évaluations d'invites, de déploiements ou d'observabilité de trace dans un projet AgentMark. Se déclenche même pour les requêtes depuis la mémoire/sans récupération de documents. SKIP : code d'invite indépendant du fournisseur, sans marqueurs AgentMark ; LangChain, LlamaIndex ou code brut OpenAI / Anthropic SDK ; questions génériques d'ingénierie rapide ou d'observabilité LLM sans contexte AgentMark ; plateformes concurrentes (Langfuse, LangSmith, Phoenix, Braintrust, Traceloop).
