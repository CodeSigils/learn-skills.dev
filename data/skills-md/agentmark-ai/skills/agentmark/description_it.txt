Crea, esegui il debug e distribuisci prompt, set di dati, esperimenti e valutazioni di AgentMark. TRIGGER: qualsiasi file `.prompt.mdx`, `agentmark.json`, `agentmark.client.ts`, `agentmark_client.py`, una directory `agentmark/` o `.agentmark/` o `@agentmark-ai/*` importa; `npm create agentmark` / `npx create-agentmark` o qualsiasi comando CLI di `agentmark` (`dev`, `run-prompt`, `run-experiment`, `generate-types`, `generate-schema`, `link`, `pull-models`); gestione di AgentMark Cloud in modo programmatico (`agentmark-mcp`, provisioning di app headless, distribuzioni basate su git); qualsiasi menzione di AgentMark o controllo delle versioni dei prompt, esperimenti di set di dati, valutazioni dei prompt, distribuzioni o osservabilità delle tracce in un progetto AgentMark. Si attiva anche per richieste dalla memoria/nessun recupero di documenti. SKIP: codice prompt indipendente dal fornitore senza indicatori AgentMark; LangChain, LlamaIndex o codice SDK OpenAI/Anthropic non elaborato; domande generiche di prompt engineering o osservabilità LLM senza contesto AgentMark; piattaforme concorrenti (Langfuse, LangSmith, Phoenix, Braintrust, Traceloop).
