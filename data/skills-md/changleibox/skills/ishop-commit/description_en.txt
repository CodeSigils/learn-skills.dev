Before submitting the iShop project, a quality gate enforces code formatting and static analysis validation; once passed, a Conventional Commits-compliant message is generated. All actual Git operations (commit, push, conflict resolution, etc.) are delegated to the git-studio skill.
