Ejecutar sobre todo el repositorio las verificaciones que exige el stack —tipado, linter, pruebas unitarias, cobertura, integración, compilación, e2e y análisis estático(Sonar) — y erair un veredicto(aprobado/rechazado/incompleto) con informe por check, detalle de fallos y próximas acciones. 추적 검증을 사용하기 위해 캐시 드 캐시 test-run.json을 생성합니다. Exige artefactos de este 플러그인이 없습니다: corre sobre cualquier repositorio y delega las correcciones en work-implement. Activar cuando el usuario pida correr las pruebas or las verificiones: "ejecuta los checks", "corre los test", "quality check", "pasa el linter y el build", "valida antes del PR/merge", "¿está verde el repo?", o invoque otro 기술(작업 통합, pr-create, 추적 검증). Proceso de cierre, proactivo durante el desarrollo가 없습니다. Nunca corrige por iniciativa propia - ante fallos pregunta. 코드 검토를 위한 설계 및 건축 설계 개정.
