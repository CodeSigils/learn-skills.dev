Eseguire su tutto il repository le verifiche automatizzate che richiedono lo stack —tipado, linter, test unitari, copertura, integrazione, compilazione, e2e e analisi statistica (Sonar)— ed emettere un verdetto (aprobado/rechazado/incompleto) con informazioni di controllo, dettagli di errori e prossimi azioni. Produci la cache di corrispondenza test-run.json che consuma trace-validate. Non sono necessari artefatti da questo plugin: corri su qualsiasi repository e delega le correzioni nell'implementazione del lavoro. Attivarsi quando l'utente esegue le prove o le verifiche: "ejecuta los checks", "corre los tests", "quality check", "pasa el linter y el build", "valida antes del PR/merge", "¿está verde el repo?", o quando si invoca un'altra skill (work-integrate, pr-create, trace-validate). Proceso de cierre, no proactivo durante lo desarrollo. Nunca corrige por iniciativa propia — ante fallos pregunta. La revisione qualitativa del design e dell'architettura è una revisione del codice.
