Implementazione in codice di lavoro già specificato, di quattro tipi: (1) tareas técnicas (TK-XXX) sotto una storia di utilizzo; (2) tareas de mantenimiento (WI-XXX) sin historia — bug, refactor, deuda técnica, dependencias, operativas; (3) casos de prueba (TC-XXX) — automatizar las pruebas ya documentadas por test-define; (4) caratteristiche (FT-XXX) — automatizar los TC de sus criteri. Selezionare il tipo in base all'oggetto di riferimento. Implementa solo il lavoro nello stato: Ready; accettare inoltre una modalità di correzione registrata, delegata al controllo qualità, su qualsiasi artefatto del ram (US-XXX, WI-XXX, FT-XXX, TC-XXX en rama test/, o un artefatto esterno al plugin), per modificare un controllo o una prova in rosso. Attivare sempre che l'utente pida "implementar", "desarrollar", "ejecutar tareas", "codificar", "automatizar las pruebas", "implementa los test cases", "trabaja en el TK/WI/TC/FT" o varianti che implicano la scrittura del codice da una specifica già redatta.
