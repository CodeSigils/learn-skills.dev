Eseguire la batteria di verifica automatizzata di un progetto TypeScript/Node prima di accettare un'implementazione come apta per merge - `tsc --noEmit` (critica, fail-fast), `eslint` (qualità), suite di test (comportamento), `build` (integrazione) e `sonar-scanner` (analisi statistica). Utilizzare sempre che l'utente pida "revisión de código", "code review", "valida el código", "ejecuta los checks", "revisa antes de PR/merge", o debba terminare una cronologia o TK che tocca codice TS. Devolver un informe Markdown con estado por check, errores agrupados, veridicto (apto / no apto / incompleto) y próximas acciones priorizadas. Non correggere il codice o modificare la configurazione.
