CLI del database basato sull'agente per l'individuazione, l'esecuzione di query, l'introspezione e l'annotazione dei database. Da utilizzare quando l'utente desidera esplorare un database, eseguire SQL, ispezionare lo schema, gestire le connessioni o mantenere la conoscenza dei dati.
