Agent Mesh (Bridge Worker / CLI / Protocol) Code Development Guide.  
Used when you need to modify the code, adapters, Workers, or protocols of the agent-mesh sub-repository.  
Trigger words: mesh worker development, bridge worker development, CLI development, adapter development, agent adapter, bridge protocol, durable objects development, mesh protocol, modifying agent-mesh, mesh code, worker deployment, CLI release.
