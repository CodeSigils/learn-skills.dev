针对项目设置、等待断言、数据源、生命周期挂钩、并行执行、可扩展性和框架迁移的独立优先 TUnit 指南。当提示提及 TUnit、await Assert.That(...)、dotnet run on Microsoft.Testing.Platform、TUnit 属性（[Test]、[Before]、[After]、[Arguments]）或将 xUnit/NUnit/MSTest 测试迁移到 TUnit 时使用。
