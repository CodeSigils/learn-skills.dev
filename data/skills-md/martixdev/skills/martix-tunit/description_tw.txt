針對專案設定、等待斷言、資料來源、生命週期掛鉤、並行執行、可擴展性和框架遷移的獨立優先 TUnit 指南。當提示提及 TUnit、await Assert.That(...)、dotnet run on Microsoft.Testing.Platform、TUnit 屬性（[Test]、[Before]、[After]、[Arguments]）或將 xUnit/NUnit/MSTest 測試遷移到 TUnit 時使用。
