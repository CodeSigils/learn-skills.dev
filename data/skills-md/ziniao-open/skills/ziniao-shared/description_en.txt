Ziniao CLI shared foundation: application configuration initialization, unified apiKey authentication, error handling, output formatting, security rules. Triggered when the user needs to configure for the first time (`ziniao-cli config init`), encounters authentication/permission issues, or uses ziniao-cli for the first time.
