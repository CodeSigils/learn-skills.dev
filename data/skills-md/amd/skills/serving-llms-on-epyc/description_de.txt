Stellt ein Sprachmodell auf einem AMD EPYC-CPU-Host unter Verwendung von vLLM mit dem Zentorch-Backend in einem Container (Docker oder Podman) oder einer Conda-Umgebung bereit. Verwenden Sie diese Option immer dann, wenn der Benutzer ein LLM auf AMD EPYC, Zen-CPU, „vLLM auf CPU“, „Zentorch-Serving“ oder „ein Modell ohne GPU bereitstellen“ ausführen, bereitstellen, bereitstellen, starten, hosten oder starten möchte. Verwendung für „Qwen auf EPYC bereitstellen“, „einen CPU-vLLM-Endpunkt starten“, „einen OpenAI-Server auf meiner EPYC-Box ausführen“ oder ähnliches. Verarbeitet den gesamten Einzelinstanzfluss: Erkennen der CPU (einschließlich EPYC-Generierung), Validieren der Laufzeit/Umgebung, Überprüfen, ob vLLM das Modell unterstützt (über die vLLM-Registrierung, keine Modalitätsblockliste), Überprüfen, ob es zum Host-RAM passt, Größe der CPU-Threads/KV/NUMA von der Hardware, Bestätigen des Plans mit dem Benutzer, Starten und Abfragen, bis der Endpunkt reagiert. Einzelne Instanz, einzelner Socket (an einen Socket + seinen Speicher angeheftet; vLLM lässt sich schlecht über Sockets hinweg skalieren). Debuggt KEINE Fehler und führt KEINE erneuten Versuche durch – es wird eine Meldung erstellt und angehalten. Nicht für GPU/Instinct (verwenden Sie „serving-llms-on-instinct“) oder mehrere Knoten verwenden.
