컨테이너(Docker 또는 Podman) 또는 conda env에서 zentorch 백엔드와 함께 vLLM을 사용하여 AMD EPYC CPU 호스트에서 언어 모델을 제공합니다. 사용자가 AMD EPYC, Zen CPU, "CPU에서 vLLM", "zentorch 제공" 또는 "GPU 없이 모델 제공"에서 LLM을 실행, 제공, 배포, 시작, 호스팅 또는 시작하려고 할 때마다 사용합니다. "EPYC에서 Qwen 제공", "CPU vLLM 엔드포인트 시작", "내 EPYC 상자에서 OpenAI 서버 실행" 등에 사용합니다. 전체 단일 인스턴스 흐름을 처리합니다. CPU 감지(EPYC 생성 포함), 런타임/환경 유효성 검사, vLLM이 모델을 지원하는지 확인(양식 차단 목록이 아닌 vLLM의 레지스트리를 통해), 호스트 RAM에 맞는지 확인, 하드웨어에서 CPU 스레드/KV/NUMA 크기 조정, 사용자와 계획 확인, 시작 및 엔드포인트가 응답할 때까지 폴링합니다. 단일 인스턴스, 단일 소켓(하나의 소켓 + 해당 메모리에 고정됨, vLLM은 소켓 전체에서 제대로 확장되지 않음) 오류를 디버깅하지 않고 재시도하지 않습니다. 보고하고 중지합니다. GPU/Instinct(serving-llms-on-instinct 사용) 또는 다중 노드에는 사용하지 마세요.
