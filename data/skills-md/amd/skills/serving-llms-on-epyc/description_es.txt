Sirve un modelo de lenguaje en un host de CPU AMD EPYC usando vLLM con el backend de zentorch, en un contenedor (Docker o Podman) o en un entorno conda. Úselo siempre que el usuario quiera ejecutar, servir, implementar, iniciar, alojar o lanzar un LLM en AMD EPYC, CPU Zen, "vLLM en CPU", "servicio zentorch" o "servir un modelo sin GPU". Úselo para "servir Qwen en EPYC", "iniciar un punto final de CPU vLLM", "ejecutar un servidor OpenAI en mi caja EPYC" o similar. Maneja el flujo completo de instancia única: detecta la CPU (incluida la generación EPYC), valida el tiempo de ejecución/env, verifica que vLLM admite el modelo (a través del registro de vLLM, no una lista de bloqueo de modalidades), verifica que se ajuste a la RAM del host, dimensiona los subprocesos de CPU/KV/NUMA del hardware, confirma el plan con el usuario, inicia y sondea hasta que el punto final responda. Instancia única, socket único (fijado a un socket + su memoria; vLLM escala mal entre sockets). NO depura fallas y NO vuelve a intentarlo: informa y se detiene. No lo use para GPU/Instinct (use server-llms-on-instinct) o multinodo.
