Fornisce modelli IA su hardware GPU AMD Instinct utilizzando vLLM. Utilizza questa competenza ogni volta che l'utente desidera eseguire, servire, distribuire, avviare, ospitare o avviare un modello linguistico su una GPU AMD, AMD Instinct, MI300X, MI325X, MI350X o MI355X. Da utilizzare anche quando l'utente menziona vLLM su ROCm, vLLM su AMD, serve su HBM o chiede come far funzionare un modello sull'hardware del data center AMD. Da utilizzare quando l'utente chiede "esegui Qwen3", "serve DeepSeek", "avvia un endpoint vLLM", "ottieni un modello in esecuzione sulla mia macchina AMD" o qualsiasi frase simile. Gestisce l'intero flusso: rilevamento GPU, convalida dell'ambiente, configurazione vLLM, avvio e verifica dell'integrità. Non utilizzare per GPU NVIDIA, GPU AMD consumer (serie RX, Radeon), Ryzen AI, NPU, MI250X o MI100.
