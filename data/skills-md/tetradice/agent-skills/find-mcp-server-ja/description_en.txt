A Japanese-language skill that supports exploring, comparing, deploying public MCP (Model Context Protocol) servers and connecting via add‑mcp. Use it proactively when requests arise such as “Is there an MCP server for ___?”, “Looking for an MCP tool”, or “I want to add it with add‑mcp,” handling everything from candidate search to operation testing through databases, external APIs, file operations, GitHub integration, etc. If a separate “find‑mcp‑server” skill exists, ensure only “find‑mcp‑server‑ja” is loaded and never load both “find‑mcp‑server” and “find‑mcp‑server‑ja”.
