Wird verwendet, wenn ein Bridge-Befehl aufgerufen wird (make, fix, done, setup, drop, status) oder ein Figma-/design-system-/compiler-/Bridge-Workflow-Thema angesprochen wird. Legt Befehlsprioritäten und eiserne Gesetze fest (nur Compiler, nur semantische Token, Überprüfung vor dem Versand).
