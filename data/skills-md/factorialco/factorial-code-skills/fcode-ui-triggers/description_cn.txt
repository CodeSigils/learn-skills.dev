将 Factorial Code 流程显示为 Factorial 产品 UI 中的按钮 - 流程的metadata.json 中的“uiTrigger”块（位置 id、标签、图标、awaitResult）、流程在用户单击时收到的内容、同步触发器的“{ data }”/“{errors }”结果信封、表单支持的触发器、i18n 标签、图标白名单，以及 Factorial 的“FactorialCodeTrigger”组件如何呈现它们。当应用程序进程应在 Factorial 页面上显示为操作按钮时，或者在连接“从 Factorial UI 触发”触发器时使用。
