使用 Factorial Code CLI (fcode) 進行本機開發和雲端同步 — 拉取 → 新增 → 相依性：安裝 → 執行 → 推播流程、何時執行每個指令、--force safety、工作範例與工作區設定檔（使用 webhook/form/appRole/visibility 處理meta範例和工作區設定檔（使用 webhook/form/appRole/visibility 處理metadata.json、am.jdata.json）。在執行 fcode CLI 指令、在本機測試 Factorial Code 流程、將 Factorial Code (fcode) 資源部署/同步到雲端或啟動流程的 Webhook、表單或可見性設定時使用。
