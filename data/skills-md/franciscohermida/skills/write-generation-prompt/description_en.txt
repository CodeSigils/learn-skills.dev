Write a prompt for an image or video generation model, as a document that justifies every word in it. Use when the user asks for a generation prompt, or brings back a rendered result that came out wrong.
