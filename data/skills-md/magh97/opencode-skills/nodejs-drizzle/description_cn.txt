Drizzle ORM en Node.js（v1.0 RC，2026）。立方体架构声明、类似 SQL 的查询和关系、关系（一对一、一对多、多对多）、事务和保存点、drizzle-kit 的迁移、Zod 验证（createInsertSchema/createSelectSchema）、准备好的语句、连接池、SQL Server + PostgreSQL、Prisma 的混合策略。该项目使用 Drizzle 进行 ORM 操作，提供类型安全的模式、无服务器优化查询，或在 Drizzle 和 Prisma 中做出决定。
