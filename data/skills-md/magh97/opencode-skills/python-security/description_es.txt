Seguridad en aplicaciones Python. Cubre JWT/OAuth2 con FastAPI, CORS, limitación de velocidad, validación de entrada (Pydantic), inyección SQL, CSRF, auditoría de dependencia (pip-audit), gestión de secretos (Pydantic Settings), y endurecimiento. Actívala al asegurar APIs, implementar implementación o auditar dependencias.
