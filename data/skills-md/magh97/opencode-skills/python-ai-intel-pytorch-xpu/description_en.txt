PyTorch 2.7 con soporte nativo XPU para GPUs Intel (Arc, Flex, Max). Cubre tensores en XPU, torch.compile + TorchInductor + Triton, mixed precision (FP16/BF16), DataLoader con GPU affinity, migración desde IPEX, y entrenamiento distribuido. Actívala para training en GPUs Intel o migración desde CUDA.
