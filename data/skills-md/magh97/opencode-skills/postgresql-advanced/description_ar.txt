T-SQL متقدم وPostgreSQL. Cubre CTEs العودية (WITH RECURSIVE)، وظائف النافذة (ROW_NUMBER، RANK، LAG، LEAD)، JSON/JSONB (المشغلون، فهرس GIN، jsonb_path_query)، البحث عن النص الكامل (tsvector، tsquery)، المصفوفات، أنواع النطاق، السلطات والأنواع المخصصة، وامتدادات pgvector لـ AI/RAG. تنشيط تنفيذ الاستعلامات الكاملة، والتقارير التحليلية، والبحث المتقدم، أو خطوط التضمين.
