T-SQL은 PostgreSQL의 장점입니다. Cubere CTE 재귀(WITH RECURSIVE), 창 함수(ROW_NUMBER, RANK, LAG, LEAD), JSON/JSONB(연산자, indexación GIN, jsonb_path_query), 전체 텍스트 검색(tsVector, tsquery), 배열, 범위 유형, 도미니오 및 사용자 정의 팁, y 확장 pgVector para AI/RAG. 실행 중인 구현에서는 전체 쿼리, 분석 보고, 정보 전달, 임베딩 파이프라인을 보고합니다.
