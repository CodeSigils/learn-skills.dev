T-SQL avanzato in PostgreSQL. Cubre CTE ricorsivi (WITH RECURSIVE), funzioni finestra (ROW_NUMBER, RANK, LAG, LEAD), JSON/JSONB (operatori, indicizzazione GIN, jsonb_path_query), ricerca full-text (tsvettore, tsquery), matrici, tipi di intervallo, dominio e tipi personalizzati ed estensioni pgvettori per AI/RAG. Attiva l'implementazione di query complesse, report analitici, ricerche avanzate o pipeline di incorporamenti.
