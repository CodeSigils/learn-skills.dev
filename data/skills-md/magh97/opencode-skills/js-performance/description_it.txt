Rendimiento en JavaScript vanilla nel navigatore. Cubre DOM batching (lezioni di scrittura separate), debounce/throttle, IntersectionObserver per il caricamento lento, requestAnimationFrame, delega di eventi, perdite di memoria (ascoltatori huérfanos), e ottimizzazione del bundle (minimizzazione, scuotimento degli alberi con moduli ES). Attiva l'ottimizzazione delle pagine lente, riduci i rendering o migliora i Core Web Vitals.
