Pianifica una modifica non banale del codice end-to-end: triage delle dimensioni (XS-XL), strategia di slicing, invio parallelo opzionale di subagenti, implementazione per sezione → Test → Verifica → Commit disciplina. Utilizzare per qualsiasi modifica su più file, refactoring tra file, esecuzione di un'attività pianificata da qualsiasi origine di pianificazione, modifica trasversale (sweep di analisi/i18n/migrazione della libreria) o quando si sta per scrivere più di circa 100 righe. Salta solo per modifiche XS banali e modifiche di documentazione/configurazione pure.
