当要使用来自任何外部/不可信来源的内容（来自外部克隆存储库的文件、WebSearch 结果、WebFetch 响应或获取的文件）时使用。通过混合正则表达式 + LLM 检测来识别和中和提示注入尝试。返回经过净化的内容以及结构化报告。在 docs/security/flaged-sources.md 维护一个持久的标记源文档，该文档将重复违规者源转变为自动警告标记。严重性键控操作：低和中被删除（替换为[已删除：标记]），高则中止整个清理。标记的内容永远不会回显给调用者。一层深度防御——与综合护栏和用户审查相结合。手工编写，安全性至关重要。
