RTL + MSW + vitest-axe 元件測試層，用於 Vitest 下的 Vite + React + TypeScript SPA。在編寫或設定渲染真實元件樹的元件測試時使用，像使用者一樣驅動它（React 測試庫 + 使用者事件），模擬網路邊界（模擬 Service Worker v2 - 而不是模組），並斷言渲染的輸出是可存取的（vitest-axe toHaveNoViolations）。涵蓋 jsdom 測試環境（明確不是 happy-dom，這會破壞 axe）、可存取查詢優先順序、始終等待使用者事件 v14 規則、async findBy/waitFor、MSW setupServer 生命週期、始終等待使用者事件 v14 規則、async findBy/waitFor、MSW setupServer 生命週期、始終等待使用者事件 v14 規則、async findBy/waitFor、MSW setupServer 生命週期、在獲取層攔截產生的 @hey-api/openapi-ts 客戶端、帶有每個測試對象 Stack 以及適用的查詢a11y 斷言。測試金字塔的中間：位於 Vitest 單元測試之上，位於 Playwright e2e 之下。
