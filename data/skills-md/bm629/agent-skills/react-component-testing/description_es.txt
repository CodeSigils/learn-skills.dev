RTL + MSW + capa de prueba de componente vitest-axe para un Vite + React + TypeScript SPA en Vitest. Úselo al escribir o configurar pruebas de componentes que representen un árbol de componentes real, condúzcalo como lo hace un usuario (React Testing Library + evento de usuario), simule el límite de la RED (Mock Service Worker v2, no el módulo) y afirme que la salida representada es accesible (vitest-axe toHaveNoViolations). Cubre el entorno de prueba jsdom (explícitamente no happy-dom, que rompe el hacha), prioridad de consulta accesible, la regla v14 de evento de usuario siempre en espera, async findBy/waitFor, el ciclo de vida del MSW setupServer, interceptando un cliente @hey-api/openapi-ts generado en la capa de recuperación, un TanStack Query QueryClientProvider por prueba con retry:false y form + request-body + todas las afirmaciones. La mitad de la pirámide de pruebas: encima de las pruebas unitarias de Vitest, debajo de Dramaturgo e2e.
