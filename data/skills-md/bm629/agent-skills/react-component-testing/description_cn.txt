RTL + MSW + vitest-axe 组件测试层，用于 Vitest 下的 Vite + React + TypeScript SPA。在编写或设置渲染真实组件树的组件测试时使用，像用户一样驱动它（React 测试库 + 用户事件），模拟网络边界（模拟 Service Worker v2 - 而不是模块），并断言渲染的输出是可访问的（vitest-axe toHaveNoViolations）。涵盖 jsdom 测试环境（明确不是 happy-dom，这会破坏 axe）、可访问查询优先级、始终等待用户事件 v14 规则、async findBy/waitFor、MSW setupServer 生命周期、在获取层拦截生成的 @hey-api/openapi-ts 客户端、带有 retry:false 的每个测试 TanStack 查询 QueryClientProvider 以及 form + request-body + a11y 断言。测试金字塔的中间：位于 Vitest 单元测试之上，位于 Playwright e2e 之下。
