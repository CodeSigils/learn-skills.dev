在审查/判断已完成的用户流程文档时使用，以确定下游线框图通道是否可以枚举其中的每个屏幕 - 接受门，而不是创作。用户流程文档是导航/交互图：用户完成每个目标所采用的路径，包括入口点、决策分支、错误/恢复路径和所遍历的屏幕。根据完整性+可步行性条来判断：每个 PRD 目标/角色都映射到一个流（无孤儿），每个流都有定义的入口 + 出口，每个分支都会解决，每个错误状态都有恢复（没有死胡同），步骤是可步行的，美人鱼图和编号叙述保持同步，屏幕索引是可枚举的。准确地发出“VERDICT：批准|修订”以及可操作的结果（失败条件+修复）。批准符合标准的文档（无错误修订），仅针对指定的差距进行修订。不适用于创作用户流程文档，不适用于屏幕布局/线框（使用线框审核技能），不适用于上游 PRD（使用 PRD 审核技能）。
