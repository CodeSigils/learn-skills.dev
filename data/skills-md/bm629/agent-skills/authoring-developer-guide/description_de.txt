Verwenden Sie dies beim Verfassen oder Anpassen von Entwickler‑Tool‑Dokumentation — die Adoptions‑ und Integrations‑Erzählung für ein SDK, eine Bibliothek, ein CLI, ein Framework oder eine API‑Plattform: der Leitfaden, den ein Entwickler liest, um das Tool zu installieren, zu integrieren und zu betreiben. Leitet die METHODE, nicht die Gliederung: Entwickler‑ZIELE, nicht Endpunkte; ein deutlich gekennzeichneter „Hier starten“ + ein schneller, nachprüfbarer erster Erfolg; Konzepte vor Anleitungen; nach Zielen benannte, ausführbare Anleitungen; ein End‑zu‑End‑Tutorial; fundierte Best‑Practices; ein Fehlersuch‑Pfad; archetyp‑bewusste Betonung (API/SDK/CLI/Framework); VERWEISE auf — niemals eine Kopie von — der API‑Referenz; und das Anpassen eines Leitfadens als versioniertes Delta, das die Änderungen des Tools für veraltete Beispiele berücksichtigt. Vollständige Diataxis korrekt typisiert, jedes Beispiel ausführbar + exakt zum Tool, nichts erfunden. Zusammengesetzt mit einem Vorlagen‑Tool + tiefgehender Recherche. Nutzt das übergebene Feature‑Spec + API‑Referenz + PRD — niemals eine leere Seite. Nicht das API‑Referenz‑Katalog, das End‑Benutzer‑Handbuch, Engineering‑Dokumente oder deren Überprüfung.
