当任何人（用户或代理）即将编写任何结构化内容（RFC、ADR、运行手册、规范、计划、PRD、自述文件、回顾、博客文章、PR 描述、问题正文、票证、多段落提交消息、公告或任何其他形状）时使用，无论目的地如何（本地文件、GitHub、Jira、Linear、Slack、电子邮件、Confluence、Notion、Google Drive 等）。这项技能是每个内容创作操作都要经过的大门。它 (1) 通过研究从意图中识别内容类型 + 变体，(2) 检查 docs/templates/<content-type>/<variant>/ 是否存在现有模板，(3) 如果存在，则返回它以及您必须遵循的硬拒绝指令，(4) 如果丢失，则通过研究伪造一个新指令，然后在同一指令下返回它，(5) 在“建议：”意图上，帮助放置不适合的内容（映射到部分或提出新的内容）变体）。模式：使用或创建（默认）、强制重新生成、强制新变体。独立：如果有的话，更喜欢具有研究能力的技能，否则内置 WebSearch + WebFetch。
