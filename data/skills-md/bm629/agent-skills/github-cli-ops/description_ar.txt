يُستخدم عند تنفيذ عملية GitHub برمجيًا - إنشاء مشكلة أو طلب سحب، أو إدارة عمليات إعادة الشراء، أو الإصدارات، أو التصنيفات، أو تشغيل الإجراءات، أو المشاريع، أو الأسرار، أو أي مهمة على موقع github.com - مع تفضيل `gh` CLI والعودة إلى `gh api` (REST) ​​أو `gh api graphql` فقط في حالة عدم وجود أمر. تتم مصادقة كل مكالمة لكل استدعاء باستخدام `GH_TOKEN`؛ لا يقوم مطلقًا بتشغيل "gh auth Switch" ولا يطبع الرمز المميز أبدًا. يستخدم أوامر `gh` عالية المستوى (والتي تقوم أيضًا بتشفير الأسرار من جانب العميل) للتغطية وبيئة العمل؛ بالنسبة للذيل الطويل REST، فإنه ينشئ استدعاء `gh api` من مواصفات OpenAPI المجمعة عبر فهرس نقطة النهاية + محلل `$ref`. github.com السحابة؛ يستهلك بيانات الاعتماد المحقونة بواسطة المتصل (المضيف + رمز مميز يتم حله بواسطة اسم متغير) - ولا يوفرها أو يحلها.
