SQLAlchemy 2.x에서 DB 지원 작업 대기열/작업 스케줄러를 구축할 때 사용합니다. 해당 준비 상태는 종속성 중심이고 작업은 장기 실행 및 상태 저장이며 유일한 내구성 있는 기반은 관계형 DB입니다(단일 상자, 내장 가능, 브로커 없음, 다중 언어(SQLite/PostgreSQL/MySQL)). 일반 작업/job_deps 모델, 준비된 쿼리, 언어별 원자 임대(PostgreSQL + MySQL 8의 업데이트 SKIP LOCKED 및 SQLite의 BEGIN IMMEDIATE, 방언 감지 포함), 하트비트를 통한 충돌 재개 + 임대 만료 + 오래된 임대 회수, 정지되었지만 살아 있는 디스패치별 시간 초과, group_key에 대한 가중 공정 공유, 스캔 → 순위 → 임대 → 디스패치 → 지속 → 틱 루프 회수, 폴링 대 LISTEN/NOTIFY 깨우기 및 최소 한 번 멱등성 계약. 키워드: 작업 대기열, 작업 스케줄러, 준비 설정, SKIP LOCKED, 임대, 충돌 재개, 다중 언어, SQLAlchemy, 백그라운드 작업. 동기화 우선. SQLAlchemy 데이터 계층이나 잠금 기본 요소(sqlalchemy)가 아니고 마이그레이션(allembic)도 아닙니다.
