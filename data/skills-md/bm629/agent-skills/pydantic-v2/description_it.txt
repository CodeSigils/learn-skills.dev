Da utilizzare durante la definizione, la convalida, la serializzazione o la configurazione dei dati con Pydantic v2 in Python — BaseModel, vincoli Field, validatori di campo/modello, serializzazione model_dump, impostazioni pydantic, TypeAdapter, enumerazioni, letterali e unioni discriminate — o durante la correzione del codice che utilizza ancora idiomi dell'era v1 (class Config, .dict()/.json(), @validator). Produce codice Pydantic v2 corretto, idiomatico e attuale. Parole chiave: pydantic, BaseModel, ConfigDict, model_validate, validatore, impostazioni.
