將 Python 儲存庫架構或分割為多包 uv-workspace monorepo（一個共享內部庫加上一個或多個依賴它的應用程式或 CLI 成員）時使用，並且您需要正確的跨包邊界：哪些程式碼成為共享庫與應用程式成員、非循環依賴向內依賴方向（應用程式依賴於每個包邊界API、跨成員測試佈局以及安全性從現有套件中提取共享程式碼。涵蓋 uv 工作區連接（成員、tool.uv.sources、單一鎖定檔案、--package）並使用 uv 和 python 專案結構技能進行組合。關鍵字：monorepo、uv 工作區、多套件、共享庫、套件邊界、依賴方向、循環導入、導入 linter。
