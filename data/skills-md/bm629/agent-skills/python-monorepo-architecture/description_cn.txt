将 Python 存储库架构或拆分为多包 uv-workspace monorepo（一个共享内部库加上一个或多个依赖于它的应用程序或 CLI 成员）时使用，并且您需要正确的跨包边界：哪些代码成为共享库与应用程序成员、非循环依赖向内依赖方向（应用程序依赖于核心库，从不相互依赖）、导入隔离规则 uv 无法强制执行、包边界处每个成员的公共 API、跨成员测试布局以及安全性从现有包中提取共享代码。涵盖 uv 工作区连接（成员、tool.uv.sources、单个锁定文件、--package）并使用 uv 和 python 项目结构技能进行组合。关键词：monorepo、uv 工作区、多包、共享库、包边界、依赖方向、循环导入、导入 linter。
