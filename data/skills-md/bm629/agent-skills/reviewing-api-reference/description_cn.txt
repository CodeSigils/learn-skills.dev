在审查/判断集成开发人员调用 API 时已发布的、面向消费者的 API 参考时使用 - 决定他们是否可以从中集成。一个门，而不是创作。根据单一来源的 11 个条件可用性 + 合同一致性标准进行判断：入门 + 验证到达第一次调用；每个 api 规范操作（包括事件/webhooks）都记录有目的 + 类型化参数 + 工作示例 + 错误；错误是一流的；适用时记录速率限制+分页；版本控制 + 弃用/日落声明；参考与提交的 api 规范一致（无漂移，无制造端点 - 承载检查）；样本可运行；修订重新同步到更改后的合同。准确发出“VERDICT：批准|修订”以及可操作的调查结果；批准按比例大小的参考，修改指定的差距。不是创作它，不是最终用户用户指南，不是开发人员采用指南，不是工程 api 规范（审查 api 规范）。
