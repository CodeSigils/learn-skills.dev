Úselo al revisar/juzgar una referencia de API publicada orientada al consumidor desde la que un desarrollador de integración llama a una API, para decidir si pueden integrarse desde ella. Una puerta, no una autoría. Jueces en contra de una usabilidad de 11 condiciones de fuente única + barra de coherencia del contrato: introducción + autenticación llegar a una primera llamada; cada operación de especificación de API (incluidos eventos/webhooks) está documentada con un propósito + parámetros escritos + un ejemplo resuelto + errores; errores de primera clase; límites de tarifas + paginación documentada cuando corresponda; control de versiones + obsolescencia/extinción declarada; la referencia es CONSISTENTE CON LA especificación API ENTREGADA (sin deriva, sin punto final fabricado: la verificación de carga); muestras ejecutables; una enmienda se vuelve a sincronizar con el contrato modificado. Emite exactamente `VEREDICTO: aprobar|revisar` más hallazgos procesables; aprueba una referencia de tamaño proporcional, revisa una brecha nombrada. No es su autor, ni la guía del usuario final, ni la guía de adopción del desarrollador, ni la especificación de API de ingeniería (revisión de especificaciones de API).
