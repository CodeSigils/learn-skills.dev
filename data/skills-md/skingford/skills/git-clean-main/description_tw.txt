將 AI 工具檔案（.claude、.codex、.cursor、.windsurf、.augment、.kiro、.cline、.roo、.gemini 等）保留在 dev 分支上，但將它們從 main/master 中排除。在管理分支、建立 PR 到主分支、合併到主分支或設定儲存庫的分支策略以進行 AI 輔助開發時使用。觸發針對 main 或 master 的 git merge/PR 操作。
