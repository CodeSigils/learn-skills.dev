Habilite la inteligencia de código (ir a la definición, buscar referencias, pasar el cursor, escribir información) para cualquier lenguaje de programación instalando y configurando un servidor LSP para Copilot CLI. Detecta el sistema operativo, instala el servidor correcto y genera la configuración JSON (nivel de usuario o nivel de repositorio). Úselo cuando necesite una comprensión más profunda del código y no haya ningún servidor LSP configurado, o cuando el usuario solicite configurar, instalar o configurar un servidor LSP.
