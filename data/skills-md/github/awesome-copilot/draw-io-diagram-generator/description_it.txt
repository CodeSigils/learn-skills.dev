Utilizzare durante la creazione, la modifica o la generazione di file di diagrammi draw.io (.drawio, .drawio.svg, .drawio.png). Copre la creazione XML di mxGraph, le librerie di forme, le stringhe di stile, i diagrammi di flusso, l'architettura di sistema, i diagrammi di sequenza, i diagrammi ER, i diagrammi delle classi UML, la topologia di rete, la strategia di layout, l'estensione VS Code hediet.vscode-drawio e il flusso di lavoro completo dell'agente dalla richiesta a un file pronto per l'apertura.
