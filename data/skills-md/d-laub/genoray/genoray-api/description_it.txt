Da utilizzare durante la scrittura o la modifica del codice Python che importa `genoray` per leggere genotipi/dosaggi da file VCF, PGEN o SparseVar (`.svar`). Copre la superficie dell'API pubblica, le costanti di modalità, le query di intervallo, la suddivisione in blocchi, il filtraggio e il flusso di lavoro SparseVar. Salta per lavori di bioinformatica non correlati.
