Tracking and KPI Design Plan Generator. Starting from product requirements or core user flows, it produces a complete tracking plan document (events, fields, trigger timing, definition specifications, QA validation checklist).

Trigger condition: the user mentions keywords such as “埋点” (tracking), “tracking”, “event design”, “data collection”, “reporting plan”, “tracking plan”, “event specification”, “field design”, “KPI definition”, “data acceptance”, “QA validation”, etc.

Also applicable when: the user provides a product PRD/requirements document and asks for a tracking plan; the user provides a core user flow and needs events broken down; the user requests standardized event naming or field definitions; the user requests a data acceptance plan.

Typical input: event naming conventions + field dictionary + core flow description/flowchart.

Not applicable to: pure data analysis (using pm‑analytics), pure BI dashboard building, pure SQL query writing.
