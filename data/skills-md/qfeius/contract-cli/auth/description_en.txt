contract-cli login and identity switching skill: initialize a dev profile, perform user OAuth login, input the bot’s app_id/app_secret and immediately exchange a tenant_access_token, view status, switch the default identity, and troubleshoot local config/secrets persistence issues. Triggered when the user needs `contract-cli config add`, `contract-cli auth login --as user|bot`, `contract-cli auth status/logout/use` or is troubleshooting login anomalies.
