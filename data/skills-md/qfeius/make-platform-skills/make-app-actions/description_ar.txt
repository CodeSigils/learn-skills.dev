استخدم عند إنشاء، دمج، إعادة هيكلة، مراجعة أو تصحيح أخطاء أزرار عمليات قائمة سجلات Make CanvasTable وإجراءات الاختيار؛ هذا هو السلوك الافتراضي لقوائم سجلات Make القابلة للكتابة ما لم يتم إلغاءه صراحة. يتم تفعيله بواسطة صندوق الاختيار، عمليات الصف، شريط عمليات الاختيار، تعديل، حذف، تعديل جماعي، لا توجد عمليات متاحة، تحديد الكل، اختيار باستخدام Shift، حد 200 عنصر عند الضغط على Shift، نية الاختيار، إعادة بناء CanvasTable، تغير totalCount، فحص ما قبل كتابة مستوى الصف، noPermissionRecordIds، تلوين الصفوف غير المصرح بها باللون الأحمر، record‑write‑permission، records/bulk، @qfei-design/make-app-actions، أو اختبارات الإجراءات. يغطي تكامل الحزمة، أذونات التحديث/الحذف/التحديث الجماعي المستقلة، نية اختيار Canvas، لقطات غير قابلة للتغيير، مرشحات الكتابة الفعّالة، الفحص المسبق للخدمة/عقود الدفعات، توافق محول الواجهة UI، رد فعل دقيق للصفوف المرفوضة، أمان البيانات القديمة، والاختبارات. لا يملك داخلية CanvasTable، سياسة IAM الرئيسية، طبقة الخدمة العامة، غلاف الصفحة، داخلية محرر الحقول، المصادقة، حزم runtime، DSL، Make CLI، أو دلالات الفلترة/الترتيب/التجميع.
