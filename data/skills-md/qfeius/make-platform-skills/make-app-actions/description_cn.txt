在生成、集成、重构、审查或调试时使用 Make CanvasTable record-list 操作按钮和选择操作；这是可写 Make 记录列表的默认行为，除非明确选择退出。由顶层、行操作、选择操作栏、编辑、删除、大规模编辑、暂时无可用的操作、selectAll、Shift Selection、Shift 200条上限、selectionIntent、CanvasTable重建、totalCount变化、行级写权限预检、noPermissionRecordIds、权限无行爆红、record-write-permission、records/bulk、@qfei-design/make-app-actions或动作测试触发。涵盖包集成、独立更新/删除/批量更新权限、Canvas 选择意图、不可变快照、有效写入过滤器、服务预检查/批量合同、UI 适配器兼容性、精确拒绝行反馈、陈旧安全性和测试。不拥有 CanvasTable 内部结构、主要 IAM 策略、一般服务分层、页面 shell、字段编辑器内部结构、身份验证、运行时打包、DSL、Make CLI 或过滤器/排序/组语义。
