在設計或產生 Make 平台 DSL YAML 時使用 - 定義應用程式、實體、欄位、關係、檢視或記錄模式。也可以由「建模」、「建表」、「加字段」、「定義關聯」或「產生 DSL」等請求觸發。不部署資源（使用 makecli）、建立前端 UI（makeui）、實作服務/API 程式碼（make-app-service）、處理驗證（make-app-auth）、執行時間打包（make-app-runtime）、OCR 整合（make-integration）或畫布行為。
