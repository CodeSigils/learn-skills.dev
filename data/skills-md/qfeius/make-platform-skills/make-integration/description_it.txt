Utilizzare quando l'utente richiede di riconoscere fatture, fatture, ricevute, biglietti o altri file voucher con i servizi Make Integration. Attivato anche da richieste come "发票内容", "识别发票", "票据 OCR", "ocr 这张图片" o "验真发票". Non gestisce le risorse Make (utilizza makecli), progetta schemi DSL (makedsl), crea l'interfaccia utente (makeui), implementa l'autenticazione (make-app-auth), il codice servizio/API (make-app-service), il pacchetto runtime (make-app-runtime) né esegue l'OCR non Make al di fuori dei comandi di integrazione makecli.
