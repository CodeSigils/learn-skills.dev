Utilícelo cuando el usuario solicite reconocer facturas, recibos, tickets u otros archivos de comprobantes con los servicios de Make Integration. También se activa mediante solicitudes como "发票内容", "识别发票", "票据 OCR", "ocr 这张图片" o "验真发票". No administra recursos Make (use makecli), diseña esquemas DSL (makedsl), crea UI (makeui), implementa autenticación (make-app-auth), código de servicio/API (make-app-service), empaqueta en tiempo de ejecución (make-app-runtime) ni realiza comandos de integración de makecli fuera de OCR que no son de Make.
