读取characters.json和backgrounds.json，然后使用kie.ai nano-banana-pro API为每个角色和背景生成参考图像。角色图像（1:1 宽高比）保存到 ./characters/，背景图像 (16:9) 保存到 ./backgrounds/，并按其 ID 命名（char_001.png、bg_001.png 等）。然后，每个图像都会上传到 imgbb 进行永久公共托管，并且 imgbb URL 会作为 image_url 字段写回两个 JSON 文件中 — 这些 URL 是generate_videos.py 所需要的。故事到动画流程的一部分（第 3 步，共 5 步）。运行scripts/generate_images.py来执行。当两个 JSON 提示文件都存在并且用户想要生成实际的参考图像时使用。生成所有图像后，始终显示输出并等待明确的用户批准。不要自动触发下一个管道步骤。
