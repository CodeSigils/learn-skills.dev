معايير ترميز أودين استخدم عند كتابة كود أودين. يغطي معالجة الأخطاء الاصطلاحية باستخدام or_else وor_return، وأنماط الذاكرة/المخصص، والسمات، واصطلاحات التسمية وواجهة برمجة التطبيقات (API)، والاختبار باستخدام Core:testing.
