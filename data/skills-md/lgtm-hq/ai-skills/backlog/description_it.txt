Dispatcher interattivo per il lavoro arretrato. Pone una domanda di instradamento (guidare le PR a unire o implementare i problemi in uno stato di PR aperte), quindi follow-up in base alla risposta e passaggio all'implementazione dei problemi / babysitter delle PR con la politica permanente applicata. Da utilizzare quando viene richiesto di lavorare sul backlog o di eseguire /backlog.
