Úselo al agregar dependencias de C/C++ de terceros a un proyecto Xmake, a través de `add_requires`/`add_packages`, configurando opciones de paquetes, fijando versiones o usando `xrepo` desde la línea de comando.
