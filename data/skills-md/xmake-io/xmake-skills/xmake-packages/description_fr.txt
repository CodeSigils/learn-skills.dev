À utiliser lors de l'ajout de dépendances C/C++ tierces à un projet Xmake — via `add_requires` / `add_packages`, en configurant les options du package, en épinglant les versions ou en utilisant `xrepo` à partir de la ligne de commande.
