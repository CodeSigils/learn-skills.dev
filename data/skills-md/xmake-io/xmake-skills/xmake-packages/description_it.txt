Da utilizzare quando si aggiungono dipendenze C/C++ di terze parti a un progetto Xmake — tramite `add_requires` / `add_packages`, configurando le opzioni del pacchetto, bloccando versioni o utilizzando "xrepo" dalla riga di comando.
