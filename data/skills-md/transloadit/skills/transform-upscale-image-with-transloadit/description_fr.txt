Mise à l'échelle unique d'une image IA (image locale -> image mise à l'échelle) à l'aide de Transloadit via la CLI « transloadit ». Préférez l'intention `image upscale` et utilisez `--output` pour un chemin déterministe.
