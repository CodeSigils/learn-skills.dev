Collage de fotos en mosaico justificado único (N imágenes locales -> imagen única) utilizando la CLI oficial `@transloadit/node`. Utiliza el efecto `mosaico` del robot `/image/merge` para crear un diseño en mosaico que mantiene cada foto completamente visible y descarga el resultado a una ruta de salida explícita a través de `--output`.
