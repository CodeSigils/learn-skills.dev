Collage di foto a mosaico giustificato una tantum (N immagini locali -> immagine singola) utilizzando la CLI ufficiale `@transloadit/node`. Utilizza l'effetto "mosaico" del robot `/image/merge` per creare un layout piastrellato che mantiene ogni foto completamente visibile e scarica il risultato in un percorso di output esplicito tramite `--output`.
