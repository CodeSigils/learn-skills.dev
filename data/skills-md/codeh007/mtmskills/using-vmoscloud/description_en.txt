Use when you need to connect to cloud phones via VMOSCloud OpenAPI, SSH tunnel, and ADB, or troubleshoot cases where the signature is correct but interfaces such as `/padApi/adb` / `infos` return “System is busy”.
