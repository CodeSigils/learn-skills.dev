اختبار تحليل التأثير لrepos fullstack. شوكة tdad v0.2.0 مع دعم Vue SFC وتحسينات CLI. يُستخدم عندما تحتاج إلى العثور على الاختبارات التي تتأثر بتغييرات التعليمات البرمجية في مشروع متعدد اللغات - على سبيل المثال. قمت بتغيير أداة Python + مكون Vue، وتريد معرفة ملفات pytest + vitest + jest التي سيتم إعادة تشغيلها. قم بالتنشيط عندما ترى دليل `.tdad/` في مشروع يحتوي على كل من ملفات `pyproject.toml` (أو `package.json`) وملفات `.vue`، أو عند العمل على monorepo متعدد اللغات. يستبدل `git diff | xargs jest --findRelatedTests سير العمل مع رسم بياني واحد.
