Edit images on RunComfy — this skill is a smart router that matches the user's intent to the right edit model in the RunComfy catalog.选择 Nano Banana Edit（批量最多 20 个，默认保留身份）、OpenAI GPT Image 2 Edit（多语言图像内文本重写、多参考合成、布局精度）、Flux Kontext Pro（单参考高保真本地编辑）或 Z-Image Turbo Inpaint（掩模驱动的精确区域编辑）。 Bundles each model's documented prompting patterns so the skill gets sharper edits without burning iterations on the wrong model. Calls `runcomfy run <vendor>/<model>/edit` through the local RunComfy CLI.在“图像编辑”、“编辑图像”、“图像到图像”、“i2i”、“交换背景”、“删除对象”、“重写标题”或任何编辑单个或一批图像的明确要求时触发。
