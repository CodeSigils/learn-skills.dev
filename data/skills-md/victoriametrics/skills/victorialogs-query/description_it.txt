Interroga VictoriaLogs tramite curl. Da utilizzare durante la ricerca di log con LogsQL, l'esecuzione di query sulle statistiche dei log, la scoperta di campi/flussi di log, l'analisi dei modelli di riscontro dei log o l'esplorazione delle sfaccettature dei log. Si attiva su: query di log, LogsQL, ricerca di log, statistiche di log, rilevamento di campi, rilevamento di flussi, sfaccettature di log, riscontri di log, valori di campi di log.
