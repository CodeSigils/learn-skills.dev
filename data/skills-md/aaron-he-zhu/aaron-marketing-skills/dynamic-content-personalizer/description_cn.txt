当用户要求“个性化电子邮件”、“添加合并标签/动态内容”、“为每个分段设置条件块”或“使名字和产品推荐字段安全回退”时使用；生成带有每个标签后备的合并标签地图、带有每个段变化的条件块规则、后备安全审核以及可能呈现的 PII 防护，通知 SEND E（参与/个性化）维度。不适用于构建段——使用 list-segment-builder；不用于编写基本副本 - 使用 email-creative-builder；不是为了对 EQS 进行评分或否决——使用 email-quality-auditor。 个性邮件化/合并标签/条件内容块/兜底默认值
