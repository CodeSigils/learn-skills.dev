Laravel/PHP backend skill that enforces the two-layer state machine + workflow engine pattern for bulletproof business logic. Entities own transitions, domain events are facts, workflows orchestrate side effects, and async operations are first-class signals. Use when modeling entity lifecycles, multi-step processes, async operations, or when business rules are scattered across services and controllers in any Laravel or PHP project.
