Deploy the current project's static build output to Show for a temporary public preview. Use when the user says "deploy to show", "deploy this", "preview this site", or wants a live URL for a frontend build.
