Passa in rassegna una modifica nell’app in esecuzione in un browser, attraverso tutte le persona, e segnala cosa fa realmente. Usalo quando l’utente chiede di verificare, fare QA o un smoke test di una modifica nell’app in esecuzione, quando una revisione del codice è stata approvata e la modifica non è stata ancora vista in esecuzione, o quando il comportamento di una specifica deve essere osservato anziché letto.

Per favore rispondi sempre nella lingua dell’utente.
