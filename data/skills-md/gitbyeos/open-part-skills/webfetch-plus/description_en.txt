Use the Browser to fetch the content of web pages that ordinary WebFetch fails to retrieve, and output the main text suitable for large models to read. Use this when the user explicitly mentions webfetch-plus or when WebFetch fails.
