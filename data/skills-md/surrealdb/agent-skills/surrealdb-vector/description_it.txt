Ricerca vettoriale con SurrealDB utilizzando indici HNSW, query KNN e punteggio di somiglianza. Use when creating vector indexes, querying vectors with KNN distance operators, building semantic search or RAG pipelines, tuning HNSW parameters (EFC, M, M0, distance function, type), or implementing recommendation systems with SurrealDB. Trigger: HNSW, vettore, incorporamento, KNN, coseno, euclideo, ricerca semantica, RAG, vettore::distanza.
