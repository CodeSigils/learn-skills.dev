Ricerca vettoriale con SurrealDB utilizzando indici HNSW, query KNN e punteggio di somiglianza. Da utilizzare durante la creazione di indici vettoriali, l'interrogazione di vettori con operatori di distanza KNN, la creazione di ricerche semantiche o pipeline RAG, l'ottimizzazione di parametri HNSW (EFC, M, M0, funzione di distanza, tipo) o l'implementazione di sistemi di consigli con SurrealDB. Trigger: HNSW, vettore, incorporamento, KNN, coseno, euclideo, ricerca semantica, RAG, vettore::distanza.
