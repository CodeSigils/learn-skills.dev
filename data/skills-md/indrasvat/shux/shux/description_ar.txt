يمكنك قيادة الجلسات الطرفية والأجزاء وواجهات TUI من الوكيل - نشر الأصداف وإرسال ضغطات المفاتيح والتقاط صور PNG مثالية للبكسل لأي جزء وتوسيع shux نفسه باستخدام مكونات JSON-RPC الإضافية المحددة بأسطر بأي لغة. استخدمه عندما تحتاج إلى عمل طرفي متعدد، أو تشغيل TUI الذي يمكنك التحكم فيه بطريقة أخرى باستخدام tmux / screen / iTerm2 / Accept / pexpect / asciinema / vhs / termshot، أو تشغيل تفاعلات CLI/REPL المكتوبة، أو إجراء انحدار مرئي بدون رأس على واجهة مستخدم طرفية، أو كتابة مكون إضافي للعملية يشترك في ناقل أحداث shux ويعاود الاتصال من خلال `window.rename`، `pane.send_keys`، "state.apply"، وما إلى ذلك. تتضمن عبارات التشغيل "drive Terminal"، و"spawn pty session"، و"إرسال المفاتيح إلى TUI"، و"screenshot a tui"، و"snapshot pane"، و"replace tmux"، و"iTerm2 Automation"، و"expect script"، و"headless Terminal test"، و"agent multiplexer"، و"asciinema Record"، و"write a shux plugin"، و"extend shux"، و"shux plugin install".
