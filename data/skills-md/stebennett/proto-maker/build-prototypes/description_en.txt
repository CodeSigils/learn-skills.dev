Stage 4. Generates N (default 3) wireframe alternatives in parallel via the Designer subagent, then dispatches Critic + User Advocate + Engineer per alternative for parallel critique, then asks the Designer to evolve each. Writes 03-prototypes/alt-*/ + index.html landing page.
