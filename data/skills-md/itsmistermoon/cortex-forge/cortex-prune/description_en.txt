Health check the vault — detects dead links, orphan pages, missing provenance, and unprocessed sources. Invoke when the user asks "is everything organized?", "check if something is broken", "are there orphan pages?", "run a vault health check", or "prune the vault".
