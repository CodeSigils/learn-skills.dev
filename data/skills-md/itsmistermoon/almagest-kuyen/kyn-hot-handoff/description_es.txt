Compacte la conversación actual en un documento de transferencia en `.hot/` en el directorio de trabajo actual, tomando una instantánea del estado de la sesión para que otro agente la retome. Se activa cuando el usuario cierra o finaliza la sesión (por ejemplo, "guardar contexto", "Ya terminé por ahora").
