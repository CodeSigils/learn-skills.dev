Refresh the AI's understanding of the code. Use this skill when the user mentions "refresh," "re-understand," "clear cache," or "reload." It compares all modified files, re-reads and comprehends any code that humans may have altered, ensuring the AI's context stays in sync with the latest code state.
