Use this skill only when the user explicitly writes `kimippt`, `/kimippt`, `帮我生成kimippt`, `kimi ppt`, `kimi slides`, or asks to generate a PPT via Kimi / kimi.com/slides. Drives https://www.kimi.com/slides in the user's EXISTING Chrome via `agent-browser` (CDP port 9222). Uploads the user's document, picks 智能布局 + 自由风格 (unless the user names a different style), waits patiently for generation, downloads the .pptx, and delivers it. Do NOT trigger for generic PPT requests — use the `pptx` or `minimaxppt` skills for those.
