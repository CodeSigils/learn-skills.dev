Connect to a local AI chart workbench (http://localhost:3001) to CREATE chart templates, APPLY real data to render charts, EXPORT images, and BUILD illustrated analysis reports / periodic digests — all over HTTP. Use when the user wants to “create a chart/dashboard/visualization/bar chart/line chart/pie chart/Sankey diagram”, “generate chart template”, “replicate a chart”, “draw data into a chart / generate a chart from data / create an analysis chart”, “batch chart generation”, “export chart/SVG/PNG”, “generate analysis report / business report / data report”, “create weekly report / monthly report / periodic report”. You generate template files yourself and write them through the API; you do NOT call the workbench's built-in AI.

Please always respond in user's language.
