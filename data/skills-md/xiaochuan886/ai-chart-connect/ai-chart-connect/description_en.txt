Connect to a local AI chart workbench (http://localhost:3001) to CREATE chart templates, APPLY real data to render charts, EXPORT images, and BUILD illustrated analysis reports / periodic digests — all over HTTP. Use when the user wants to “create a chart/dashboard/visualization/bar chart/line chart/pie chart/Sankey diagram”, “generate a chart template”, “replicate a chart”, “draw data into a chart / render charts from data / generate analytical charts”, “bulk chart generation”, “export charts/SVG/PNG”, “generate analysis report / business report / data report”, “create weekly/monthly/periodic reports”. You generate template files yourself and write them through the API; you do NOT call the workbench’s built‑in AI. Please always respond in the user’s language.
