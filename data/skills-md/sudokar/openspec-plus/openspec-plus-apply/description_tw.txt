每當 OpenSpec 應用階段開始時就會啟動的強制技能。觸發器： /opsx-apply 運行， openspec-apply-change vanilla Skill 被引用或激活，「openspec instructions apply」被調用，或使用者要求實施、應用、執行或建構 OpenSpec 變更（「實施變更」、「應用任務」、「執行變更」、「建置變更」）。僅接管普通步驟 6（實現循環）並模擬步驟 7 的輸出（最終狀態）。
