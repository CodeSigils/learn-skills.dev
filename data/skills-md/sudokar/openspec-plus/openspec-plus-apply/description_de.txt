OBLIGATORISCHE Fertigkeit, die immer dann aktiviert wird, wenn die OpenSpec-Anwendungsphase beginnt. Auslöser: /opsx-apply wird ausgeführt, der OpenSpec-Apply-Change-Vanilla-Skill wird referenziert oder ist aktiv, „OpenSpec-Anweisungen anwenden“ wird aufgerufen oder der Benutzer bittet um die Implementierung, Anwendung, Ausführung oder den Aufbau einer OpenSpec-Änderung („Änderung implementieren“, „Aufgaben anwenden“, „Änderung ausführen“, „Änderung erstellen“). Übernimmt nur Vanilla-Schritt 6 (Implementierungsschleife) und emuliert die Ausgabe von Schritt 7 (Endstatus).
