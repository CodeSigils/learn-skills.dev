Use when building or iterating on frontend UI code, especially when mirroring an HTML/JS prototype, to drive a real browser with the Playwright plugin and verify every button, input, and interaction as you develop — not after.
