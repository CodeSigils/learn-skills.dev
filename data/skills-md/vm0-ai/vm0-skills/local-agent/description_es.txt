CLI de agente local VM0 para delegar trabajo a un host Codex o Claude local ya vinculado. Úselo cuando un agente debe enumerar los hosts del agente local o ejecutar tareas a través de `npx -p @vm0/cli zero local-agent`.
