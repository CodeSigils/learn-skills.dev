CLI dell'agente locale VM0 per delegare il lavoro a un host Codex o Claude locale già collegato. Da utilizzare quando un agente deve elencare gli host dell'agente locale o eseguire attività tramite `npx -p @vm0/cli zero local-agent`.
