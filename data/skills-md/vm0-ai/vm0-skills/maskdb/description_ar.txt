بوابة استعلام Postgres المقنعة للقراءة فقط من Maskdb. يُستخدم عندما يذكر المستخدم "maskdb" أو "قاعدة البيانات المقنعة" أو "الاستعلام المقنع" أو الاستعلام عن قاعدة بيانات Postgres من خلال واجهة برمجة التطبيقات (API) المقنعة والمنظمة.
