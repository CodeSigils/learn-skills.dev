Inizializzare un repository per il flusso di lavoro di cablaggio. Da utilizzare quando si configura un repository per la prima volta, quando manca CONTEXT.md o CONTEXT-MAP.md, quando non è stato scelto il modello subagente predefinito o quando non è stata scelta la configurazione del tracker.
