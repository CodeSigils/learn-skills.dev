Use this skill when the user says "tdd commit", "commit tdd", "finish tdd session", "TDD 커밋", "세션 커밋", or wants to commit after a TDD session. Reads the active .tdd-sessions/ file, summarizes completed tasks, proposes a Conventional Commits message, executes the commit, and cleans up the session file.
