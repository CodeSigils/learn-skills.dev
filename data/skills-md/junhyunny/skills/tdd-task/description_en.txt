Use this skill when the user says "tdd task", "next tdd task", "run tdd", "TDD 태스크", "다음 태스크", or wants to run a TDD RED-GREEN-REFACTOR cycle. Reads the active .tdd-sessions/ file, finds the current pending task, and runs the full ping-pong TDD cycle with the developer.
