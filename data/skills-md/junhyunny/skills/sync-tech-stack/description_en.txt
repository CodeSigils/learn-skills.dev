Use this skill when the user says "sync tech stack", "detect stack", "update stack", "refresh stack", "스택 감지", "기술 스택 업데이트", or when another skill needs a shared project stack file. Detects project tech stacks and frameworks, writes `.agents/tech-stack.md`, and updates it when the stack changes.
