Run benchmark validation for the current iteration candidate. Use during the benchmark phase to validate changes against the baseline engine. Triggers on: benchmark, validate performance, run match, evolution benchmark.
