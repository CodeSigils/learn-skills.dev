Persistenza JSON, scritture atomiche, blocco dei file, ripristino da arresto anomalo del sistema e modelli di controllo delle versioni dello stato. Da utilizzare quando si implementano librerie con stato o funzionalità che richiedono uno stato persistente. TRIGGER quando: persistenza dello stato, scrittura atomica, blocco dei file, ripristino da crash, checkpoint. NON SI ATTIVA quando: utilità stateless, funzioni pure, letture di configurazione, documentazione.
