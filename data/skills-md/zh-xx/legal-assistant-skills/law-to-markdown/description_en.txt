Convert legal provisions or specification documents (.txt/.docx/.pdf) to Markdown. Applicable to user requests such as “legal provisions to markdown” or “pdf/docx to markdown”. When handling .pdf/.docx files, first check whether the mineru-ocr skill is installed; if it is not installed, guide the user to install it, and once installed, prioritize using mineru-ocr. Only use a local fallback solution if the user explicitly consents.
