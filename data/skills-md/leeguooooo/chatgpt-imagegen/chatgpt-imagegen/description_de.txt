Generieren Sie Rasterbilder (PNG/JPEG/WebP) mit dem ChatGPT-Abonnement des Benutzers über eine lokale Ein-Datei-Python-CLI – kein OPENAI_API_KEY, kein Gateway, kein Daemon. Verwenden Sie diese Option, wenn ein Agent ein brandneues Bitmap-Asset für das aktuelle Projekt erstellen muss (Fotos, Illustrationen, Symbole, Heldenbanner, Modelle, Sprites, Konzeptzeichnungen) und die Ausgabe eine im Arbeitsbereich gespeicherte Bitmap-Datei sein soll. Nicht verwenden, wenn die Aufgabe besser gelöst werden kann, indem vorhandene SVG-/Vektor-Assets bearbeitet, Code-native Grafiken (HTML/CSS/Canvas) geschrieben oder ein etabliertes Repo-Symbolsystem erweitert werden.
