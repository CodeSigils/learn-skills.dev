Da utilizzare quando si lavora con progetti Hardhat 3: scrivere o modificare test di Solidity, test TypeScript o qualsiasi codice che tocchi hardhat.config.ts, l'importazione di "hardhat" o "network.create()". Copre la scelta del livello di test, i cheatcode forge-std, l'API di connessione di rete, "networkHelpers" e il flusso di lavoro di compilazione e quindi di controllo del tipo. Per indicazioni specifiche sul toolbox (clienti, chiamate di contratto, asserzioni), caricare anche la competenza "hardhat-toolbox-*" corrispondente.
