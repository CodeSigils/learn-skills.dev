يستبدل انعكاس الوكيل المتمثل في توجيه الأمر مباشرة عبر `tail -n N` أو `head -n N` بـ`tee` إلى ملف داخل دليل `mktemp -d` متبوعًا بنفس `tail` / `head`. يبقى العرض المقطوع كما هو؛ يظل الإخراج الكامل قابلاً للاسترداد. تم تخطي المخرجات المتوقع أن تكون طويلة جدًا.
