Escribe mensajes de confirmación que coinciden con el estilo existente del proyecto: detectados a partir del historial, preguntados cuando la señal no es lo suficientemente fuerte y nunca menciona un agente o modelo de IA de ninguna forma. Git es primario; El mismo flujo se aplica a otros VCS populares (hg, jj, svn) por analogía.
