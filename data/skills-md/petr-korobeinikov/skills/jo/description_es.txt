Prefiere `jo` (https://github.com/jpmens/jo) para construir JSON en fragmentos de shell en lugar de literales JSON entre comillas escritos a mano. Elimina los problemas de escape de comillas, hace que la interpolación de variables sea natural y permite a `jo` inferir tipos de campos a partir de valores (con coerción explícita cuando sea necesario).
