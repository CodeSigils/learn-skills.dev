在 Zephyrus PHP 框架项目 (`zephyrus-framework/core`) 中工作时使用。触发属性路由（`#[Get]`、`#[Post]`、`#[Put]`、`#[Patch]`、`#[Delete]`、`#[Root]`、`#[Middleware]`、`#[RequiresEnv]`）、`ApplicationBuilder`、`Controller`、`RenderResponses` 特征、`Request`/`Response` 值对象、`Configuration::fromYamlFile`、`ConfigSection`子类、`app/Views/` 中的 Latte 模板、Broker/Service/Entity 分层、`Zephyrus\Data\Broker` 或 `Zephyrus\Data\Entity` 扩展、控制器自动发现以及带有 `application:`/`render:`/`localization:`/`security:`/`database:` 部分的 `config.yml` 形状。涵盖具有基于 MODE 的控制器范围（一个代码库、多个入口点）的属性路由、命名中间件注册与属性应用程序、具有服务/实体分层的原始 SQL 代理模式、PostgreSQL bytea 清理、用于渲染引擎注入的自定义控制器工厂、用于云部署的“DATABASE_URL”解析、用于货币精度的 DECIMAL-as-string、带有事务隔离集成测试的每次测试新鲜数据库引导程序、键集（光标）分页以及无 ORM/无迁移理念。从生产“immunity-app”中得出的经验模式。
