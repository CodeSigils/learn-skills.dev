Úselo cuando trabaje en un proyecto de marco PHP Zephyrus (`zephyrus-framework/core`). Activadores en enrutamiento de atributos (`#[Get]`, `#[Post]`, `#[Put]`, `#[Patch]`, `#[Delete]`, `#[Root]`, `#[Middleware]`, `#[RequiresEnv]`), `ApplicationBuilder`, `Controller`, rasgo `RenderResponses`, objetos de valor `Solicitud`/`Response`, `Configuration::fromYamlFile`, subclases `ConfigSection`, plantillas Latte en `app/Views/`, capas Broker/Service/Entity, extensiones `Zephyrus\Data\Broker` o `Zephyrus\Data\Entity`, descubrimiento automático del controlador y la forma `config.yml` con `aplicación:`/`render:`/`localización:`/`seguridad:`/`base de datos:` secciones. Cubre enrutamiento de atributos con alcance de controlador basado en MODE (una base de código, múltiples puntos de entrada), registro de middleware con nombre frente a aplicación de atributos, patrón de Broker SQL sin formato con capas de servicio/entidad, desinfección de bytes de PostgreSQL, fábrica de controladores personalizados para inyección de motor de renderizado, análisis `DATABASE_URL` para implementaciones en la nube, DECIMAL como cadena para precisión monetaria, arranque de base de datos nueva por prueba con pruebas de integración aisladas de transacciones, paginación de conjunto de claves (cursor) y Filosofía de no ORM/no migración. Patrones empíricos extraídos de la producción "immunity-app".
