在 Zephyrus PHP 框架專案 (`zephyrus-framework/core`) 中工作時使用。觸發屬性路由（`#[Get]`、`#[Post]`、`#[Put]`、`#[Patch]`、`#[Delete]`、`#[Root]`、`#[Middleware]`、`#[RequiresEnv]`)、`ApplicationBuilder``、Controller`、`Renders``Reses/Repons`.值物件、`Configuration::fromYamlFile`、`ConfigSection`子類別、`app/Views/` 中的 Latte 模板、Broker/Service/Entity 分層、`Zephyrus\Data\Broker` 或 `Zephyrus\Data\Entity` 擴充、控制器自動發現以及帶有 `applii/``/ft/`hoation:``25):``205/``i​​ft部分的 `config.yml` 形狀。涵蓋具有基於 MODE 的控制器範圍（一個程式碼庫、多個入口點）的屬性路由、命名中間件註冊與屬性應用程式、具有服務/實體分層的原始 SQL 代理模式、PostgreSQL bytea 清理、用於渲染引擎注入的自定義控制器工廠、用於雲部署的“DATABASE_URL”解析、用於貨幣精度的 DECIMAL-as-string） ORM/無遷移理念。從生產“immunity-app”中得出的經驗模式。
