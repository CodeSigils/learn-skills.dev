Scrivi, spiega ed esegui il debug del codice GPU Metal inclusi shader (vertice, frammento, calcolo), pipeline di rendering, pipeline di calcolo, gestione buffer/texture e API Metal 4. Utilizza questa competenza ogni volta che l'utente menziona Metal, programmazione GPU, shader, MSL (Metal Shading Language), passaggi di rendering, kernel di calcolo, MTLDevice, MTLCommandBuffer, MTLRenderPipelineState o qualsiasi argomento di programmazione GPU/grafica Apple. Si attiva anche quando l'utente desidera eseguire calcoli paralleli su dispositivi Apple, scrivere codice accelerato dalla GPU o lavorare con Metal Performance Shader, MetalFX, MetalKit o Compositor Services. Copre iOS, macOS, iPadOS, tvOS e visionOS.
