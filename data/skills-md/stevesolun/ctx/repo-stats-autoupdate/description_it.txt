Mantiene il badge README e i conteggi in linea sincronizzati con il numero reale di competenze, agenti, nodi/bordi del grafico, comunità, pipeline convertite e test pytest. Viene eseguito automaticamente su ogni commit tramite un hook pre-commit git. Da utilizzare quando il README si discosta dalla realtà o prima di pubblicare un comunicato.
