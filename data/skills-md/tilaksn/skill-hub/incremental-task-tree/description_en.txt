Breaks down large tasks into a hierarchical task tree (level-by-level), limits work scope to small directories/packages, verifies feasibility upfront, and executes work in user-reviewed milestone chunks. Use when the user requests "incremental review", "work in small steps", "limited directory scope", "step-by-step milestones", or explicit tree-based task breakdown.
