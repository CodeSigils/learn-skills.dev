The frogoe block registry — how to find, evaluate, install, theme, and bind HUD recipe blocks into a game folder, and how to author a NEW block that meets the bar. Use when choosing HUD parts (score, hearts, fuel, game-over), when a block needs retheming beyond --block-* variables, when adding a block to the registry, or when validating a block dir.
