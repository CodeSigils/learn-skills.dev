在使用 Vunor（Vue 3 + UnoCSS 设计系统和组件库）时使用此技能。涵盖presetVunor、vunorShortcuts、defineShortcuts、mergeVunorShortcuts、VunorVueResolver、Nuxt模块和Vu前缀组件（VuButton、VuInput、VuSelect、VuCombobox、VuCheckbox、VuRadioGroup、VuSlider、VuDatePicker、VuCard、VuDialog、VuTabs、 VuMenu、VuAppLayout、VuIcon）。加上 Vunor UnoCSS 规则和语义类 - 范围（主要、次要、良好、警告、错误、灰色、中性）、layer-0..4 深度背景、surface-0..900 彩色块、current-* 颜色系统、c8 可点击样式（填充、平面、轮廓、浅色、镀铬）、i8 输入样式（平面、填充、圆形）、卡片/卡片密集、指尖触摸目标、$xxs..$xxl 间距标记， text-h1..text-caption 排版，text-mt/mb-* 边距补偿。加上调色板调整（最亮、最暗、图层深度、平坦度、生动、饱和度、主调色板、图层调色板、表面）和快捷方式覆盖模式。导入：vunor、vunor/主题、vunor/utils、vunor/vite、vunor/nuxt。
