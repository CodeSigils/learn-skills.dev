在使用 Vunor（Vue 3 + UnoCSS 設計系統和元件庫）時使用此技能。涵蓋presetVunor、vunorShortcuts、defineShortcuts、mergeVunorShortcuts、VunorVueResolver、Nuxt模組和Vu前綴元件（VuButton、VuInput、VuSelect、VuCombobox、VuCheckboxVuRadioGroup、VuRadioGroup、VuDli、VuCheck VuMenu、VuAppLayout、VuIcon）。加上 Vunor UnoCSS 規則和語義類 - 範圍（主要、次要、良好、警告、錯誤、灰色、中性）、layer-0..4 深度背景、surface-0..900 彩色塊、current-* 顏色系統、c8 可點擊樣式（填充、平面、輪廓、淺顏色、鍍鉻）、i8 鍍鉻）、i8 片面文件、觸摸text-h1..text-caption 排版，text-mt/mb-* 邊距補償。加上調色板調整（最亮、最暗、圖層深度、平坦度、生動、飽和度、主調色板、圖層調色板、表面）和快捷方式覆蓋模式。導入：vunor、vunor/主題、vunor/utils、vunor/vite、vunor/nuxt。
