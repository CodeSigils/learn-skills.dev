Wire the agent-skills toolkit into CI — install the GitHub Actions workflow that runs validate/lint/audit/deps on every PR, and install the pre-commit hooks for local-loop parity. Use when the user asks to set up CI for skills, install pre-commit hooks, configure GitHub Actions for SKILL.md validation, or wire the toolkit into a new repo.
