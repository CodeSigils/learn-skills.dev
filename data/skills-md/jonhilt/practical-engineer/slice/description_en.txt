Break one spec into a sequence of vertical slices — each slice a thin TDD-sized chunk of modules, responsibilities, and behaviours. The bridge between /spec (or /spike) and /tdd. Use after /spec (or /spike) and before /tdd.
