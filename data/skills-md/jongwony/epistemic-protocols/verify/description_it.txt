Questa funzionalità dovrebbe essere utilizzata quando l'utente richiede di “verificare i protocolli”, “controllare la coerenza prima di eseguire il commit”, “validare le definizioni”, “eseguire i controlli pre‑commit”, “verificare la solidità” o desidera garantire la qualità epistemica del protocollo. Invocare esplicitamente con /verify per la validazione pre‑commit.

Si prega di rispondere sempre nella lingua dell'utente.
