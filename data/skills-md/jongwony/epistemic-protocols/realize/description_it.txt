Questa funzionalità dovrebbe essere usata quando l'utente richiede di “eseguire la valutazione”, “verificare se il protocollo funziona effettivamente a runtime”, “controllare la realizzazione del tipo”, “misurare l'adempimento del protocollo”, “eseguire la suite di realizzazione del tipo”, “il cancello si ferma davvero” o desidera una prova a runtime che il contratto di transizione formale di un protocollo sia realizzato, piuttosto che una valutazione della qualità dell'output a valle. Invocare esplicitamente con /realize e un argomento di skill.

Rispondi sempre nella lingua dell'utente.
