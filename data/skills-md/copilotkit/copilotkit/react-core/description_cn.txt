@copilotkit/react-core — 在 Next.js 应用程序路由器 / React Router v7 / TanStack Start / SPA 应用程序中安装 CopilotKitProvider，放入 CopilotChat/CopilotPopup/CopilotSidebar（v2 聊天组件来自 React-core/v2 — 而不是 React-ui，v2 中仅包含 CSS），使用 useAgent / useAgentContext / useCapability 访问和订阅代理，在多个代理之间切换，管理持久的智能线程使用 useThreads，通过 useFrontendTool 注册浏览器端工具，使用 useRenderTool / useComponent / useDefaultRenderTool 渲染工具调用，使用 useHumanInTheLoop 进行门执行，使用 useAttachments 连接文件附件，配置建议丸，以及注册活动和自定义消息渲染器。 publicLicenseKey 是规范的（publicApiKey 是已弃用的别名）。加载与您的任务匹配的引用/下的参考。
