@copilotkit/react-core — monta CopilotKitProvider in un'app Next.js App Router / React Router v7 / TanStack Start / SPA, inserisci CopilotChat/CopilotPopup/CopilotSidebar (i componenti della chat v2 vengono forniti da react-core/v2 — NON react-ui, che è solo CSS nella v2), accedi e iscriviti agli agenti con useAgent / useAgentContext / useCapabilities, passa da un agente all'altro, gestisci intelligence durevole thread con useThreads, registrare strumenti lato browser tramite useFrontendTool, eseguire chiamate allo strumento di rendering con useRenderTool / useComponent / useDefaultRenderTool, eseguire il gate con useHumanInTheLoop, collegare file allegati con useAttachments, configurare pillole di suggerimenti e registrare renderer di attività e messaggi personalizzati. publicLicenseKey è canonico (publicApiKey è un alias deprecato). Carica il riferimento in riferimenti/ che corrisponde alla tua attività.
