@copilotkit/react-core — 在 Next.js 應用程式路由器 / React Router v7 / TanStack Start / SPA 應用程式中安裝 CopilotKitProvider，放入 CopilotChat/CopilotPopup/CopilotSidebar（v2 聊天元件來自 React-core/v2 — 而不是 React-CopilotSidebar（v2 聊天元件來自 React-core/v2 — useCapability 存取權和訂閱代理，在多個代理之間切換，管理持久的智慧執行緒使用 useThreads，透過 useFrontendTool 註冊瀏覽器端工具，使用 useRenderTool / useComponent / useDefaultRenderTool 渲染工具調用，使用 useHumanInTheLoop 進行門執行，使用 7ttments publicLicenseKey 是規範的（publicApiKey 是已棄用的別名）。載入與您的任務相符的引用/下的參考。
