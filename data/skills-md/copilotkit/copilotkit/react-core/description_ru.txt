@copilotkit/react-core — смонтируйте CopilotKitProvider в приложении Next.js App Router / React Router v7 / TanStack Start / SPA, вставьте CopilotChat/CopilotPopup/CopilotSidebar (компоненты чата v2 поставляются из реакции-core/v2 — НЕ реагируйте-ui, который в версии 2 предназначен только для CSS), получите доступ и подпишитесь на агентов с помощью useAgent / useAgentContext / useCapabilities, переключитесь между несколькими агентами, управлять устойчивыми потоками интеллекта с помощью useThreads, регистрировать инструменты на стороне браузера с помощью useFrontendTool, отображать вызовы инструментов с помощью useRenderTool / useComponent / useDefaultRenderTool, выполнять шлюзы с помощью useHumanInTheLoop, связывать вложения файлов с помощью useAttachments, настраивать таблетки предложений и регистрировать средства визуализации действий и пользовательских сообщений. publicLicenseKey является каноническим (publicApiKey — устаревший псевдоним). Загрузите ссылку в раздел references/, соответствующую вашей задаче.
