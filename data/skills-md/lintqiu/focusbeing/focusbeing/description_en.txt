Digital human generation CLI tool, calls backend API with accessKeyId/accessKeySecret, supports submitting tasks (MP3/MP4/text→video) and querying task results (orderNo); does not persist business data in this repository; credentials are used only in-process for request headers; data is sent to the backend configured by the user.
