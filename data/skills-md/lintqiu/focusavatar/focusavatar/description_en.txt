Digital human generation CLI tool, uses accessKeyId/accessKeySecret to call backend API, supports submitting tasks (MP3/MP4/text→video) and querying task results (orderNo); does not store user data, credentials sent only via request headers.
