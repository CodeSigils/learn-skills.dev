Riferimento completo in lingua russa per Open Terminal: terminale API REST self-hosted per agenti AI. Utilizza questa competenza per qualsiasi domanda su Open Terminal: endpoint API, distribuzione (Docker, bare-metal), configurazione, integrazione con Open WebUI, architettura, debug, scrittura del codice client per interagire con Open Terminal. Da utilizzare anche quando si lavora con la codebase open-terminal: struttura del progetto, runner.py, main.py, server MCP, notebook Jupyter. Trigger: terminale aperto, terminale aperto, API terminale remoto, /execute, /files, API sandbox, sessioni terminale, terminale Open WebUI.
