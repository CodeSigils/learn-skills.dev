مرجع كامل باللغة الروسية لـ Podkop على OpenWrt. استخدم هذه المهارة لأية مهام تتعلق بـ `itdoginfo/podkop`: التثبيت والتحديثات وترحيل `/etc/config/podkop` وتكوين التوجيه الانتقائي/المجال عبر `sing-box` والعمل مع `dnsmasq` و`nft/tproxy` وLuCI UI وسلاسل عناوين URL للوكيل (vless وtrojan وss وhysteria2 وsocks) وقوائم المجتمع/المستخدمين، محدد/urltest، الاستبعاد، لوحة معلومات Clash API/YACD، مراقبة BadWAN، FakeIP، تشخيص الخدمة وتطوير `podkop`، `luci-app-podkop`، `fe-app-podkop`. المشغلات: podkop، وluci-app-podkop، وsing-box على OpenWrt، وfakeip، وproxy_string، وقوائم المجتمع، والتوجيه الانتقائي، وclash api، وyacd، وurltest، والمحدد، والاستبعاد.
