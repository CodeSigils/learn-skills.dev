Guía completa para usar `looker-cli`. Cubre la configuración y los valores predeterminados del perfil, usando el indicador --profile, inicios de sesión persistentes (`session login`), descubrimiento de comandos (`meta tree` y `meta search`) y solicitud de inspección del cuerpo (`--describe-body` & `--template`).
