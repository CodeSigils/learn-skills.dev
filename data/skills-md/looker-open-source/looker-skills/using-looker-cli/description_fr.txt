Guide complet pour l'utilisation de `looker-cli`. Couvre la configuration du profil et les valeurs par défaut, en utilisant l'indicateur --profile, les connexions persistantes (`session login`), la découverte de commandes (`meta tree` & `meta search`) et l'inspection du corps de la demande (`--describe-body` & `--template`).
