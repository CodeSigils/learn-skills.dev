Guida completa per l'utilizzo di `looker-cli`. Copre la configurazione e le impostazioni predefinite del profilo, utilizzando il flag --profile, accessi persistenti (`session login`), rilevamento dei comandi (`meta tree` e `meta search`) e ispezione del corpo delle richieste (`--describe-body` e `--template`).
