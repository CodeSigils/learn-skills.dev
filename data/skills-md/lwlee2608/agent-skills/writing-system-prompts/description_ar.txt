يُستخدم عند كتابة أو تحرير موجه نظام لأي LLM API أو SDK (أي كود يمرر معلمة دور `system=` / `system`، أو ملف `.txt`/`.md` يحمل مثل هذه المطالبة). يطبق أفضل ممارسات الهندسة السريعة والتخزين المؤقت السريع.
