Da utilizzare durante la scrittura o la modifica di un prompt di sistema per qualsiasi API o SDK LLM (qualsiasi codice che passa un parametro di ruolo `system=` / `system` o un file `.txt`/`.md` che contiene tale prompt). Applica le migliori pratiche di ingegneria dei prompt e di memorizzazione nella cache dei prompt.
