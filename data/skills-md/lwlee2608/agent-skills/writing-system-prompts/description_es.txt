Úselo al escribir o editar un mensaje del sistema para cualquier API o SDK de LLM (cualquier código que pase un parámetro de función `system=`/`system`, o un archivo `.txt`/`.md` que contenga dicho mensaje). Aplica las mejores prácticas de ingeniería y almacenamiento en caché de avisos.
