Use when implementing a feature plan file that is split into phases with task checkboxes. Builds one phase per branch, opens a PR against a feature-wide integration branch, reviews it twice in a subagent, fixes only what is worth fixing, then merges before starting the next phase. After the last phase it demos the whole feature the way the user asks, and leaves the final merge to main to them.
