Verifica dell'intera storia: deduce ciò che l'utente sta costruendo, quindi verifica il flusso completo end-to-end: browser → API → dati → risposta. Trigger all'avvio del server di sviluppo e segnali "perché non funziona".
