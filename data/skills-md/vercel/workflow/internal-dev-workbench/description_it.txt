Avvia una sessione di sviluppo portless + tmux per Workflow SDK che fornisce a ciascun worktree git isolato URL `<branch>.<name>.localhost` per il workbench Next.js e l'interfaccia utente di osservabilità, oltre a una statusline Claude che mostra tali URL. Utilizzare solo quando l'utente richiede una "sessione di sviluppo portless", un "layout di sviluppo tmux per il flusso di lavoro", "URL di sviluppo isolati dall'albero di lavoro" o desidera collegare gli URL di sviluppo del flusso di lavoro nella riga di stato di Claude. Non attivare per l'attività generica "avvia il server di sviluppo"/"esegui pnpm dev".
