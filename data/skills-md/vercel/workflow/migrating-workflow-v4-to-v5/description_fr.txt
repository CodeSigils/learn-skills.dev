Met à niveau une application de Workflow SDK 4.x vers 5.0. À utiliser lors du remplacement des dépendances `workflow` / `@workflow/*` vers la v5, ou lorsque vous appuyez sur les API v4 supprimées - `runStep`, `stepEntrypoint`, `workflow/internal/private`, `@workflow/core/private`, `writeToStream` / `closeStream` / `readFromStream` sur un monde, `world.steps.get` sans runId, `hook.getConflict()` renvoyant `{ runId }`, `experimental_setAttributes`, `createLocalWorld` / `createVercelWorld`, `NestLocalBuilder` importé de `@workflow/nest` ou une transformation SWC invoquée avec `mode : 'client'`.
