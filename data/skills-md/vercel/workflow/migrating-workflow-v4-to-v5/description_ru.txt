Обновляет приложение с Workflow SDK 4.x до 5.0. Используйте при переносе зависимостей `workflow` / `@workflow/*` в v5 или при удалении API v4 — `runStep`, `stepEntrypoint`, `workflow/internal/private`, `@workflow/core/private`, `writeToStream`/` closeStream` / ``readFromStream` в мире, `world.steps.get` без runId, `hook.getConflict()`, возвращающий `{ runId }`, `experimental_setAttributes`, `createLocalWorld` / `createVercelWorld`, `NestLocalBuilder`, импортированные из `@workflow/nest`, или SWC-преобразование, вызванное с `mode: 'client'`.
