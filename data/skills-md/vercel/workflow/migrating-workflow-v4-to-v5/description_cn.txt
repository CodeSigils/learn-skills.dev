将应用程序从 Workflow SDK 4.x 升级到 5.0。当将 `workflow` / `@workflow/*` 依赖升级到 v5，或遇到已移除的 v4 API 时使用——`runStep`、`stepEntrypoint`、`workflow/internal/private`、`@workflow/core/private`、在 World 上的 `writeToStream` / `closeStream` / `readFromStream`、没有 runId 的 `world.steps.get`、`hook.getConflict()` 返回 `{ runId }`、`experimental_setAttributes`、`createLocalWorld` / `createVercelWorld`、从 `@workflow/nest` 导入的 `NestLocalBuilder`，或使用 `mode: 'client'` 调用的 SWC 转换。
