Workflow SDK 4.x에서 5.0으로 앱을 업그레이드합니다. `workflow` / `@workflow/*` 의존성을 v5로 올리거나 v4 API가 제거된 경우에 사용합니다 — `runStep`, `stepEntrypoint`, `workflow/internal/private`, `@workflow/core/private`, World에서 `writeToStream` / `closeStream` / `readFromStream`, `world.steps.get` (runId 없이), `hook.getConflict()` 가 `{ runId }` 를 반환하는 경우, `experimental_setAttributes`, `createLocalWorld` / `createVercelWorld`, `@workflow/nest` 에서 가져온 `NestLocalBuilder`, 또는 `mode: 'client'` 로 호출되는 SWC 트랜스폼 등.
