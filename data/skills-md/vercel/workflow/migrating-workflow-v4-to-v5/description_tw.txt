將應用程式從 Workflow SDK 4.x 升級到 5.0。將 `workflow` / `@workflow/*` 依賴項升級到 v5 時，或點擊已刪除的 v4 API 時使用 - `runStep`、`stepEntrypoint`、`workflow/internal/private`、`@workflow/core/private`、World 上的 `wStreamStreamStream /run/`cl 是/`` / ``Stream `Fs ``Stream `F / `` /Frun ``Stream `Fle ``Stream `F / ``/ FStream `F 的 `pStream 看到/Frun. `world.steps.get`、回傳 `{ runId }` 的 `hook.getConflict()`、從「@workflow/nest」匯入的「experimental_setAttributes」、「createLocalWorld」/「createVercelWorld」、「NestLocalBuilder」，或使用「mode: '「」轉換」的 SWCclient'。
