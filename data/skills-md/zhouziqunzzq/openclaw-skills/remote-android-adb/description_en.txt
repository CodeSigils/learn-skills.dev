Run Android ADB commands against a phone connected to a remote physical machine over Tailscale + SSH. Use when OpenClaw runs on a VPS but ADB runs on a home/office Linux box with USB-attached Android devices.
