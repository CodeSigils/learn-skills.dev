Use when the user explicitly requests to “compress/streamline/simplify a certain Agent Skill’s Markdown documentation” or “reduce skill context overhead without changing functionality.” First understand the target skill’s actual capabilities and safety boundaries, then, while ignoring `tests/`, `plans/` and the target skill’s `README.md` and `CHANGELOG.md`, compress work‑oriented Markdown such as `SKILL.md`, `references/*.md`, and isolate the intermediate products into `.compact-bensz-skills/`. ⚠️ Not applicable: when the user mainly wants to add features, fix script logic, batch modify code, or only wants to compress non‑skill documents.
