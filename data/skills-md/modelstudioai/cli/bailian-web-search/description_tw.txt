阿里雲百磊線上搜尋（網頁搜尋）入口點：為線上搜尋/網頁搜尋/取得最新資訊提供路由分發。首先辨識目前連線身分：Token Plan（設定檔`token-plan`或base_url主機為`token-plan.<region>.maas.aliyuncs.com`）→模型內建搜尋（`bl text chat --api messages --tool '{"type":"web_search"}'``)；當身份驗證失敗、未啟用或傳輸失敗時，才會回退到模型的內建搜尋。 Token Plan網路搜尋失敗或兩條路徑混合認證的故障排查也經過這個技巧。反觸發：主機可以處理的任務——正常問答/編程/寫作→無觸發；知識庫 RAG → bailian-cli (`bl Knowledge`);原始圖像/原始視頻/音頻→ bailian-gen;微調/訓練 → bailian-finetune; Agents.yaml → bailian-託管-託管-託管-代理。共享協定（同意/版本預檢查/身份驗證/錯誤報告）位於 bailian-protocol 中；官方安裝`bl技能初始化`。
