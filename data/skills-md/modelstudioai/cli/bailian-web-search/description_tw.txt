阿里雲百聯互聯網搜索（網頁搜索）入口點：用於在線搜索/網頁搜索/獲取最新信息，具有路徑分佈。首先，識別目前連線身分：Token Plan（設定檔`token-plan`或base_url主機為`token-plan.<region>.maas.aliyuncs.com`）→模型內建搜尋（`bl text chat --api messages --tool '{"type":"web_search"}'`）；當身份驗證失敗、未啟用或傳輸失敗時，才會回退到模型的內建搜尋。 Token Plan網路搜尋失敗以及跨兩條路徑混合認證的排查也是經過這個技巧的。反觸發：普通問答/編程/寫作等主機可以處理的任務→不觸發；知識庫 RAG → bailian-cli (`bl Knowledge`);原始圖像/原始視頻/音頻 → bailian-gen;微調/訓練 → bailian-finetune; Agents.yaml → bailian-託管-託管-託管代理。共用協定（同意/版本預檢查/驗證/錯誤報告）位於 bailian-protocol 中；使用「bl Skill init」進行正式安裝。
