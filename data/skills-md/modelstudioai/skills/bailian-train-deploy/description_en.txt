Use the Bailei CLI (`bl`) to complete the full loop of “data → fine‑tuning training → export → deployment → invocation,” or skip training and directly deploy the base model. It covers dataset validation/upload, creating SFT/DPO/CPT fine‑tuning tasks, waiting for training, exporting the best checkpoint, creating inference deployment, waiting for readiness, and providing invocation examples. When a user mentions “training a model,” “fine‑tuning,” “fine‑tune,” “finetune,” “deploying a model,” “model launch,” “running/calling a fine‑tuned model,” “training an inference model,” “continuing pre‑training,” “LoRA/SFT/DPO training,” etc., on Bailei / DashScope / Alibaba Cloud Model Studio, this skill should be activated— even if the user does not explicitly say “use bl”; as long as the intent is training or deployment on the Bailei platform, use this skill and do not piece together commands yourself.
