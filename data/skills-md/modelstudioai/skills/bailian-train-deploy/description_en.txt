Use the Baijian CLI (`bl`) to complete the full loop of “data → fine‑tuning training → export → deployment → invocation,” or skip training and directly deploy the base model. This covers dataset validation/upload, creating SFT/DPO/CPT fine‑tuning tasks, waiting for training, exporting the best checkpoint, creating an inference deployment, waiting for it to become ready, and providing invocation examples. When a user mentions training a model, fine‑tuning, “fine‑tune,” “finetune,” deploying a model, launching a model, running/calling a fine‑tuned model, training an inference model, continuing pre‑training, LoRA/SFT/DPO training, etc., on Baijian / DashScope / Alibaba Cloud Model Studio, this skill should be activated— even if the user doesn’t explicitly say “use bl.” As long as the intent is training or deploying on the Baijian platform, use this skill and do not piece together commands yourself.
