An ultra‑lightweight channel for the feature process—skip writing a design or checklist and jump straight in, but first guide the AI to query the CodeStable knowledge base before starting work. Trigger: the user says “quick mode”, “fastforward”, “don’t have so many steps”, or “just start”, and the request is small enough that going through a design process isn’t warranted.
