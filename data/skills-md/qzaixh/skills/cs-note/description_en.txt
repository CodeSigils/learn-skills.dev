Append the piece of project knowledge that’s too brief to merit its own document but that the AI must know every time CodeStable is launched into a fixed section of `.codestable/attention.md`—for example, special compilation flags, services that must be started before running, path pitfalls, command aliases, environment variable conventions. Trigger: the user says “make a note,” “add to attention.md,” “the project requires X to compile,” “from now on it always needs Y,” or just encounters a project‑specific setting that can be explained in one sentence.
