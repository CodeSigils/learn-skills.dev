Smart Git commit assistant. Use it only when manually triggered by the user (enter /commit or explicitly request "提交代码", "commit"). Automatically stage relevant changed files, verify whether the current branch name matches the commit type, and if it does not, automatically create a new branch, generating a Chinese commit message that conforms to the Conventional Commits specification. Do not proactively invoke this skill unless the user explicitly requests a commit.
