Generate reference documentation entry by entry for the library’s public surface (components / functions / commands), with list tracking, supporting both single entries and batch processing. The information source is the source code itself (different from the task‑oriented guidedoc). Trigger: the user says “write API documentation”, “component documentation”, “libdoc”, or after acceptance discovers newly added public interfaces.
