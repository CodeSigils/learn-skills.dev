CodeStable is the main entry point of the workflow, introducing the overall system architecture and routing requests to the corresponding cs-* sub-skills. Trigger: the user only inputs `cs`, says "Introduce CodeStable," "Which skill should I use," "I don't know which one to use," or the request is still open and not yet converged. This skill only performs routing and does not execute tasks.
