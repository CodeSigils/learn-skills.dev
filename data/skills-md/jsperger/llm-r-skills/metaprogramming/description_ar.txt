يُستخدم عند كتابة كود R الذي يعالج التعبيرات، أو يبني الكود برمجيًا، أو يحتاج إلى فهم آليات نزع فتيل/حقن rlang. الأغلفة: التعطيل باستخدام expr()/enquo()/enquos()، وتتبع بيئة quosure، والحقن باستخدام !!/!!!/{{، وبناء الرمز باستخدامsym()/syms(). لا يغطي: أنماط برمجة قناع البيانات (التقييم المرتب)، ومعالجة الأخطاء (شروط اللغة)، وتصميم الوظائف (التصميم المرتب للوظائف).
