Analyze Git staging area changes, first read the commitlint config in the repo and generate the commit message according to the project rules; if no such config exists, fall back to the built-in commitlint (Angular) convention. Triggered when the user says "help me commit", "generate commit", "write a commit message", "confirm commit", "commit this message", "commit: <message>"; if a single candidate message has already been shown in the last turn, any short reply that clearly indicates agreement to commit should also proceed.
