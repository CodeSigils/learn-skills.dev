Gestisce la CLI git-agent: commit, init, configurazione e configurazione del provider tramite ~/.config/git-agent/config.yml o git config. Le sostituzioni della CLI del provider appartengono solo ai flussi di eccezioni. Da utilizzare ogni volta che l'utente menziona git-agent, desidera eseguire il commit/init o deve configurare un provider.
