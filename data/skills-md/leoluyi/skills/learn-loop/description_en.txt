Leo's structured learning loop (teach first, then test + source verification) refines a new concept into a knowledge crystal within an Obsidian vault. It is only activated when Leo explicitly invokes it (Claude Code `/learn-loop`, Codex `$learn-loop`, or explicitly says “run learn-loop process to learn X”); it should not be automatically triggered just because the conversation mentions wanting to understand something — this is a deliberate six‑step interactive process that occupies an entire segment of the dialogue.
