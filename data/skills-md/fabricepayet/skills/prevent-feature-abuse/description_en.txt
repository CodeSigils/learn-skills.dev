Use when auditing or implementing a feature exposed to excessive requests, uploads, retries, concurrency, queues, external API or AI spend, tenant quota exhaustion, replay, or offline resubmission, especially before rollout or after adding rate limiting.
