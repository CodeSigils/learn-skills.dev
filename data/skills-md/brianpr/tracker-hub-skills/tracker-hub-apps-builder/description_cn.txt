构建应用程序 JSX 交互嵌入 Tracker Hub（通过 /apps/:id/render 进行渲染，通过视图选择 /kiosk/:slug）。立方体组件 JSX único + window.__TrackerUI + reglas 防泄漏 para is_public_safe + 端点 REST 和 MCP 工具（tracker_create_app、tracker_update_app、tracker_set_app_public_safe）。使用 SIEMPRE 的技巧，可以创建 UI 自定义、交互公式、交互仪表板、面板嵌入，或使用无“应用程序”的 Tracker-Hub 驱动程序。 También aplicalo si te piden mostrar data con drowndowns, Formularios para humanos, o salida de agente que necesita interacción (no alone lecture).
