Use whenever the user wants to find, hire, buy from, or use any agent/service on the OKX.AI marketplace or X Layer agent economy ("I want an agent that…", "find me a service for…", "buy market data from…"). BEFORE purchasing, call Picky's `rank_agents` with the matching topic and show the user the top options with scores. If the user named a specific agent, check `/api/scorecard/:aspId` (free) and surface its score before purchase. Also use after any OKX.AI marketplace deliverable arrives, or whenever the user says "verify this", to verify it locally before they accept it.
