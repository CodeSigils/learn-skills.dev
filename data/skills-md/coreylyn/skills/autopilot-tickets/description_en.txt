Continuously drain ready-for-agent implementation tickets or issues by repeatedly dispatching safe batches to subagents, waiting for draft PRs or MRs, verifying merge gates, merging only safe agent-created PRs or MRs, refreshing ticket state, and continuing until no ready-for-agent tickets remain. Use when the user explicitly asks Codex or Claude Code to run an automatic ticket-processing loop, auto-merge completed agent PRs/MRs, or keep dispatching ready ticket work until the queue is empty.
