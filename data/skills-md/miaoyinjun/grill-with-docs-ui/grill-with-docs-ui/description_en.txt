grill-with-docs: This skill performs grilling with documentation (executes grilling + domain modeling); when frontier ≥ 2, use a local form, and after submission the subsequent round proceeds to an empty frontier. “Submitted” / “Answered” → resume (fallback).
