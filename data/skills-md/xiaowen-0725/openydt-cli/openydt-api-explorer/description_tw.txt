通用 API 回退和介面探索：當某個介面缺少專用的一等命令時，使用 openydt api <cmd> --body 直接發送任意可調用接口，並從 Catalog.json 中查找 cmd/參數，以區分可主動調用的可調用接口和只能被動接收的 Webhook。當cmd在領域技能中找不到專用子命令時、需要調用非一級接口（如城市運營優惠券、第三方停車場、社區門禁、月票預訂、授權訪客、文檔規則、車輛標籤等）時、或者詢問“如何調用該cmd/平台會回調我”時使用此命令。
