通用 API 回退和接口探索：当特定接口缺少专用主命令时，使用 openydt api <cmd> --body 直接调用任意可调用接口，命令和参数参考catalog.json，区分可主动调用的 Callable 和只能被动接收的 Webhooks。当命令在领域技能中找不到专用子命令、需要调用非一级接口（例如城市运营凭证、第三方停车场、社区门禁、月票预订、授权访客、凭证规则、车辆标签等）或询问“如何调用该命令/平台是否会回调我”时应用此命令。
