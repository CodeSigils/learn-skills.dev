以一系列编号“帧”的形式生成向量和矩阵运算的逐步计算演练，显示每个步骤的显式状态。 3Blue1Brown 动画的文本等效项 — 每个帧都会显示发生了什么变化以及原因，因此学习者可以手动重新跟踪操作。当学习者需要“查看”计算展开（特征值计算、3 个标记的注意力、梯度下降步骤、2×2 上的 SVD、3 向量上的层范数、小输入的 softmax）时，当给出解释但学习者需要将其扎根于工作示例时，或者当引入符号形式令人生畏但在铅笔和纸形式中微不足道的操作时使用。
