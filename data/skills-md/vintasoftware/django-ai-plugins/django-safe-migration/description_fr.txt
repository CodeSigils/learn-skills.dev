Écrivez, révisez et réécrivez les migrations Django pour PostgreSQL avec des garanties sans temps d'arrêt. Utilisez cette compétence chaque fois que l'utilisateur mentionne des migrations, des modifications du schéma Django ou la sécurité du déploiement, même s'il ne dit pas explicitement « zéro temps d'arrêt ». Déclenchez sur : "examiner cette migration", "cette migration est-elle sûre ?", "écrire une migration pour...", "réécrire cette migration", "comment ajouter une colonne NOT NULL / supprimer une colonne / ajouter un index / renommer une colonne / ajouter un FK sans temps d'arrêt", "cette migration provoquera-t-elle des verrous ?", "cette migration bloque la production". Couvre : les divisions en deux fichiers SeparateDatabaseAndState, AddIndexConcurrently, FK NOT VALID + VALIDATE, db_default pour les colonnes NOT NULL, lock_timeout et les règles de sécurité RunPython.
