Drive kobe — a local TUI that runs many parallel AI coding sessions — from your shell via `kobe api`. Use to spawn/fan-out parallel attempts, AND to manage the full task lifecycle (create, rename, re-branch, archive, delete, pin, switch focus) without leaving the terminal. Each task is its own git worktree + agent session. Run `kobe api schema` to discover the whole surface.
