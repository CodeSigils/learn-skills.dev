Guida kobe, una TUI locale che esegue molte sessioni di codifica AI parallele, dalla tua shell tramite "kobe api". Utilizzare per generare/distribuire tentativi paralleli E per gestire l'intero ciclo di vita dell'attività (creare, rinominare, ramificare nuovamente, archiviare, eliminare, bloccare, cambiare focus) senza lasciare il terminale. Ogni attività è la propria sessione git worktree + agente. Esegui "kobe api schema" per scoprire l'intera superficie.
