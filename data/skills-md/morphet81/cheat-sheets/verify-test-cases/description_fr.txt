Vérifiez les cas de test dans tous les fichiers de test modifiés depuis la branche de base. Vérifie que les cas de test ont du sens, ne comportent pas de duplications et fournissent une couverture significative. Génère des agents parallèles pour l’analyse multi-fichiers. Une fois que l'utilisateur a confirmé les modifications du scénario de test, exécute la couverture (npm test:coverage ou Jest/Vitest fallback) et corrige les tests jusqu'à ce que la couverture soit réussie.
