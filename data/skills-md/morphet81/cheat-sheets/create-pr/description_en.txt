Push the current branch and create a pull request on GitHub. When a JIRA ID is in the branch name, derives title and description from the ticket; otherwise still pushes and offers PR title options. Draft by default, use --no-draft for a ready PR.
