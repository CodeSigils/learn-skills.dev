Use this skill when the user wants to download videos. Two ways to get video URLs: 1) The agent uses browser automation tools (headless mode recommended) to extract URLs. 2) The user provides URLs directly. Then download and convert to PPT-compatible formats.
