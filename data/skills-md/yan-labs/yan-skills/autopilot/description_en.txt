Receive a vague instruction, automatically investigate, classify, and break it down into an XML stage plan, select skills, and set completion criteria, then execute it fully in an unattended mode via loop/goal + agent-mode— including automatic deployment, automatic end‑to‑end testing, and automatic code review, without skipping any stage. When the user throws a broad task such as “fix the bug”, “add tests”, “optimize performance”, “finish this feature”, “scan the code”, or “investigate why XX”, use it proactively. It also triggers when the user says “autopilot”, “auto”, “help me plan”, “handle it yourself”, “run directly”, “you break it down”, “don’t ask me how to do it”. Even if the user does not use these keywords, as long as the input is clearly an unparsed broad intent, it should be activated proactively. Invoking autopilot = authorizing fully automated unattended execution, with no mid‑process confirmation needed.
