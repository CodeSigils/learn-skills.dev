GANG entry skill. The user types /gang to upgrade the current pane into a GANG orchestrator and start the gang loop. Skill content = run `hive gang init`, move the current pane to a new window, set up board + skeptic, and automatically dispatch /gang-orch to take over duty.
