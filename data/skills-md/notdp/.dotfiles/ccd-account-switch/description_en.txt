Restore the Claude Code desktop app’s session list and scheduled tasks (routines) after switching Claude accounts. Use this when a user reports that after changing accounts the sessions are missing, the conversation list is empty, historical sessions have disappeared, routines are gone, or scheduled tasks are no longer running.
