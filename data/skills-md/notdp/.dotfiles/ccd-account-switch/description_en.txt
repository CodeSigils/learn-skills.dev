Restore the session list and scheduled tasks (routine) in the Claude Code desktop app after switching Claude accounts. Use this when a user reports that after changing accounts the sessions are missing, the conversation list is empty, historical sessions are gone, the routine has disappeared, or scheduled tasks are no longer running.
