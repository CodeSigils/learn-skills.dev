Feishu Markdown: view, create, upload, edit, and compare Markdown files. Used when a user needs to create or edit Markdown files, read, modify, apply partial patches, or compare differences. It does not handle importing Markdown into Feishu online documents, nor does it manage file search, permissions, comments, moving, deleting, or other cloud‑space management operations.
