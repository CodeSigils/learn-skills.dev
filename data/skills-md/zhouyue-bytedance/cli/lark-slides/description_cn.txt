飞书幻灯片：创建和编辑幻灯片。创建演示文稿、阅读幻灯片内容、管理幻灯片页面（创建、删除、阅读、部分替换）。当用户需要创建或编辑幻灯片，或者阅读或修改单个页面时，请使用此选项。当用户提供了doubao.com /slides/ URL/token时，直接使用该技能；不要因为域名不是飞书而退回到WebFetch。路由基于 URL 路径模式和令牌，而不是域。不负责：编辑云文档内容（由lark-doc处理）、云文档内的独立白板对象（由lark-whiteboard处理；注意幻灯片中嵌入的流程图/架构图仍然属于该技能）、上传或下载常规文件（由lark-drive处理）。
