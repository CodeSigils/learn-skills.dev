Translate design drafts, annotated files, or visual specs into production-ready application code while maintaining high-fidelity visual parity. Use when the user needs to "implement the design," "build from the draft," "replicate the UI/components/page," provides design links/screenshots/spec docs, or asks to build the interface to spec. If connected to Tencent Design Ardot MCP, pull context and screenshots per "Appendix: Ardot MCP" below; if the user's goal is only to revise within design tools rather than deliver repo code, see skill boundaries.
