Créez la page de destination SaaS dans `sites/<slug>/` une fois la marque approuvée. S'active automatiquement lorsque `approval.json.approved === true` et que l'utilisateur demande une page de destination, un site Web, un site marketing ou exécute `/build-landing`. Génère une application Next.js + Tailwind + Framer Motion, connecte le mouvement du héros généré par Higgsfield + les micro-animations de fonctionnalités, écrit une copie SaaS à partir de `brand.json` + `saas-context.json` et place TOUJOURS une section `<Waitlist />` en bas.
