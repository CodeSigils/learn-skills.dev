Crea la pagina di destinazione SaaS in `sites/<slug>/` una volta approvato il marchio. Si attiva automaticamente quando "approval.json.approved === true" e l'utente richiede una pagina di destinazione, un sito Web, un sito di marketing o esegue "/build-landing". Genera un'app Next.js + Tailwind + Framer Motion, collega il movimento dell'eroe generato da Higgsfield + presenta microanimazioni, scrive una copia SaaS da `brand.json` + `saas-context.json` e inserisce SEMPRE una sezione `<Waitlist />` in fondo.
