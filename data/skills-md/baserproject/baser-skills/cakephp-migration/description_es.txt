Una colección de patrones de cambios importantes y obsoletos y recetas de corrección para las actualizaciones de la versión CakePHP de baserCMS (5.0 → 5.1 → 5.2…). Cubre temas como "alias de asociación ya establecido", "ya existe una regla de validación con el nombre", "_Token no encontrado", "encabezados ya enviados", "el complemento llamado X ya está cargado", "errores de ResultSet/PaginatedResultSet", "order()/group() en desuso", "valores de retorno del detector de eventos en desuso", "find('all', $options) en desuso", "Tabla::obtener opciones de matriz en desuso/argumentos con nombre", “excepciones en valores nulos en condiciones de matriz”, “configuración de caché _cake_core_”, “pluginBootstrap donde el archivo settings.php de los complementos posteriores no se lee y Configure es nulo (iteración en vivo)”, “_checkFilePath provoca que... la plantilla arroje MissingPluginException/500”, “TreeBehavior con root parent_id = null corrompiendo lft/rght”, “Entity::set Bulk‑array en desuso”. “AbstractMigration/AbstractSeed está en desuso → BaseMigration/BaseSeed (cakephp/migrations4.5+)”, “config/routes.php fallbacks() causando que solo las pruebas devuelvan 404”, “métodos reales auxiliares que se delegan a métodos mágicos haciendo que método_existe sea falso (por ejemplo, TextHelper::truncate → is_callable check)”, etc. Consulte esto cuando investigue y solucione problemas que se originan en el núcleo de CakePHP y sus paquetes relacionados. Cuando admita una nueva versión de CakePHP, agregue entradas específicas de la versión a este documento. Los problemas causados ​​por el propio PHP deben consultarse a través de la habilidad de migración de php.
