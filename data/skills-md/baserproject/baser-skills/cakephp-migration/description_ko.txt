baserCMS의 CakePHP 버전(5.0 → 5.1 → 5.2 …) 업데이트를 위한 더 이상 사용되지 않는 주요 변경 패턴 및 수정 레시피 모음입니다. "연결 별칭이 이미 설정됨", "해당 이름의 유효성 검사 규칙이 이미 존재함", "_Token을 찾을 수 없음", "헤더가 이미 전송됨", "X라는 이름의 플러그인이 이미 로드됨", "ResultSet/PaginatedResultSet 오류", "order()/group() 지원 중단됨", "이벤트 리스너 반환 값 지원 중단됨", "find('all', $options) 지원 중단됨", "테이블::get 배열 옵션"과 같은 문제가 포함됩니다. 더 이상 사용되지 않음/명명된 인수", "배열 조건의 null 값에 대한 예외", "_cake_core_ 캐시 설정", "pluginBootstrap이 후속 플러그인의 settings.php를 로드하지 않아 구성이 null이 됨(실시간 반복)", "_checkFilePath로 인해 .. template MissingPluginException/500", "parent_id=null이 lft/rght를 손상시키는 TreeBehavior 루트", "Entity::set 대량 배열 더 이상 사용되지 않음”, “AbstractMigration/AbstractSeed 더 이상 사용되지 않음 → BaseMigration/BaseSeed(cakephp/migrations 4.5+)”, “config/routes.php fallbacks()는 테스트에서만 404를 생성합니다”, “도우미 실제 메서드가 매직 메서드 위임으로 바뀌어 method_exists를 false로 만듭니다(예: TextHelper::truncate → is_callable check)” 등 에. CakePHP 코어 또는 관련 패키지에서 발생하는 문제를 조사하고 수정할 때 참조됩니다. 새로운 CakePHP 버전을 지원할 때 이 가이드에 버전별 참고 사항을 추가하세요. PHP 자체에서 발생하는 문제의 경우 PHP 마이그레이션 기술을 참조하세요.
