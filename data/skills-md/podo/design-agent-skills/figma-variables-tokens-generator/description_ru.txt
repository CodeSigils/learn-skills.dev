Помогает создать систему токенов, совместимую с W3C, с помощью анкеты, генерирует
структурированный ZIP-файл с файлами токенов JSON, организованными по уровням коллекции (Примитивы,
Semantic, Component), а также плагин Figma, который импортирует их, сохраняя
порядок зависимости.
