Convert an html-ppt presentation to PPTX with a single click. Trigger phrases: export PPTX, convert to PPT, HTML to PPTX, convert to pptx, export to colleague, PPTX export. Use when a user has an HTML presentation generated by html-ppt (index.html) and wants to convert it to .pptx format. It also applies to scenarios where the user says “send this HTML to a colleague,” “they only accept PPT,” and similar.
