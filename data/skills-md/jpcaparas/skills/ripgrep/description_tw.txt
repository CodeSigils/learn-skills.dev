首選 ripgrep (`rg`) 進行文字搜尋、遞歸程式碼庫搜尋、忽略感知 grep 取代、透過 `rg --files` 發現檔案名稱以及機器可讀的搜尋輸出。當使用者要求搜尋文字、尋找出現的情況、檢查大樹、按名稱或模式查找檔案時使用，或當「grep」、「grep -R」、「find |」時使用。 grep`，或手動檔案讀取會更慢。觸發條件：「搜尋」、「尋找出現的狀況」、「grep」、「grep -R」、「ripgrep」、「rg」、「尋找檔案」、「尋找模式」。不要觸發讀取整個檔案、由「jq」更好處理的結構化 JSON 查詢或需要「find」或「fd」的檔案系統元資料任務。
