IndyKite ContX IQ (CIQ) ポリシーと、IndyKite Graph (IKG) 内の既存のノードに 1 つ以上のプロパティを設定するナレッジ クエリを作成し、「POST /contx-iq/v1/execute」経由で実行します。新しいプロパティを追加する場合、既存のプロパティを上書きする場合、またはプロパティ メタデータを添付する場合に使用します。ノードの作成、関係の書き込み、削除は行われません。
