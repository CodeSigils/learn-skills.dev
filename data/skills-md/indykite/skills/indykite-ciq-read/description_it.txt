Crea una policy IndyKite ContX IQ (CIQ) di sola lettura più la sua query di conoscenza, quindi eseguila tramite `POST /contx-iq/v1/execute`. Da utilizzare quando si espongono nodi, relazioni o valori aggregati IKG come query di lettura parametrizzata: senza aggiornamenti o eliminazioni.
