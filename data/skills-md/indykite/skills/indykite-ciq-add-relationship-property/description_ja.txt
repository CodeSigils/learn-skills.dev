IndyKite ContX IQ (CIQ) ポリシーと、IndyKite Graph (IKG) 内の既存の関係に 1 つ以上のプロパティを設定するナレッジ クエリを作成し、「POST /contx-iq/v1/execute」経由で実行します。新しいプロパティを追加する場合、既存のプロパティを上書きする場合、または IKG に既に存在するリレーションシップにプロパティ メタデータを添付する場合に使用します。リレーションシップの作成、ノードの書き込み、削除は行われません。
