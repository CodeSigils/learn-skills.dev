Crea una policy IndyKite ContX IQ (CIQ) più la sua query di conoscenza che crea un nodo nuovo di zecca E lo collega a uno o più nodi esistenti tramite nuove relazioni in una singola chiamata "POST /contx-iq/v1/execute". Da utilizzare quando si inserisce una nuova entità che deve essere collegata atomicamente all'IKG: combina la creazione del nodo e la creazione della relazione in un'unica operazione.
