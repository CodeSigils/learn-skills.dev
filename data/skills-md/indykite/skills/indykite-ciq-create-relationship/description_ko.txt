IndyKite ContX IQ(CIQ) 정책과 IndyKite Graph(IKG)의 두 기존 노드 사이에 새로운 관계를 생성하는 Knowledge Query를 작성한 다음 `POST /contx-iq/v1/execute`를 통해 실행합니다. CIQ를 통해 두 개의 기존 엔터티를 함께 연결할 때 사용합니다. 새 노드 없음, 관계 업데이트 없음, 삭제 없음.
