Feature process stage 2 — advance according to the paradigm‑dimension steps defined in the design section of {slug}-checklist.yaml; which files to modify at each step are decided autonomously by the implementer, and after completing the work, report using a unified format. Trigger: the user says “the plan is confirmed, start implementation”, “write code according to the plan”, or “start work”. Prerequisite: the design has been approved and a checklist exists. If a situation outside the plan arises, return to discuss the plan rather than forcing ahead.
