CodeStable is the root entry of the workflow, introducing the entire system and routing requests to the appropriate cs-* sub‑skill. Trigger: the user only inputs `cs`, says “introduce CodeStable,” “which skill should I use,” “I don’t know which one to use,” or the request is still very open and not yet narrowed down. This skill merely performs routing and does not carry out any actions.
