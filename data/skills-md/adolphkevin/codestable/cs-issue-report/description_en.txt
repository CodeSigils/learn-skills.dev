Issue process stage 1 — through a conversation turn the problem into a reproducible, traceable {slug}-report.md and decide whether to follow the standard path or the fast lane. Only ask about the symptoms, do not guess the root cause. Trigger: the user says “create an issue”, “record this bug”, “I found a problem”. This is the starting point of the issue workflow.
