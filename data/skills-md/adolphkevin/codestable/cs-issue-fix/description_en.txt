Issue process stage 3 – Perform targeted fixes and verification according to the confirmed root cause and plan, and write {slug}-fix-note.md to record the changes. There are two entry points: the standard path comes from analyze, and the fast lane comes directly from report. It is triggered when the user says “start fixing the bug”, “fix according to analysis”, or “let’s edit the code”. Only modify the files specified in the plan; do not make opportunistic optimizations.
