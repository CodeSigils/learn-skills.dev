The sub‑process entry for new feature development, taking “add X capability” from idea to acceptance loop. Trigger: the user says “develop a new feature,” “add X,” or “implement XX.” It only performs routing, deciding whether to go through brainstorm, design, fast‑forward, implement, or acceptance based on existing artifacts. It does not handle bugs.
