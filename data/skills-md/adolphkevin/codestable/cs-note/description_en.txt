Add the project fragment knowledge that is “too short to warrant its own document, yet AI must know it every time it launches the CodeStable skill” to a fixed section of `.codestable/attention.md`—for example, special compilation flags, services that need to be started before running, path traps, command aliases, environment variable conventions. Trigger: when the user says “make a note,” “add to attention.md,” “the project needs X to compile,” “from now on it must Y,” or just encounters a project‑specific setting that can be explained in a single sentence.
