Maintain the `.codestable/architecture/` system map that records only the current state, using three modes: update, check, backfill. Triggers: a user says “refresh architecture,” “perform an architecture check,” “add the architecture documentation for this module,” “does the plan match the code,” or an architecture action is needed during the feature stage. Do not write any future plans (follow the cs‑roadmap).
