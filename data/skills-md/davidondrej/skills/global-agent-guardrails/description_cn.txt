一个共享的灾难性 shell 命令拒绝名单（rm -rf on / 或 ~、dd/mkfs、sudo rm、fork 炸弹、curl|sh、git push --force、gh repo delete）作为 PreToolUse/pre-exec 防护在机器上的每个 AI 编码代理上强制执行 - Cursor、Claude Code、Codex、OpenCode、Pi、Hermes、Grok、Droid、Devin。在添加或调整阻止命令模式、将防护连接到新代理或新机器、调试命令被（或未被）阻止的原因或当用户提到命令防护、护栏、危险命令挂钩或 PreToolUse 安全时使用。
