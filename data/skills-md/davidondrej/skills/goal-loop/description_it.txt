Spiega e scrivi istruzioni efficaci per la funzione `/goal`: il ciclo persistente dell'agente di autocontrollo (pianificazione → azione → test → revisione → iterazione), disponibile in agenti come Codex, Claude Code e Hermes Agent. Da utilizzare quando l'utente menziona `/goal`, "goal loop", "Ralph loop", desidera avviare un'esecuzione di un agente autonomo di lunga durata, chiede come scrivere una richiesta di obiettivo o desidera la stesura di un'istruzione sull'obiettivo di un paragrafo.
