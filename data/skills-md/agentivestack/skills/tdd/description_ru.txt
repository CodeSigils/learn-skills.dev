Реализуйте функцию или исправьте ее, используя прагматичную разработку через тестирование. Подход «трассировщик» — один тест, одна реализация, повтор. Тесты проверяют поведение через общедоступные интерфейсы, а не через детали реализации. Используйте при реализации среза из /slice или любой работы, требующей тестов.
