Run a timeboxed exploration to answer a technical question or validate an approach. Use when the user says 'spike', 'explore whether', 'can we use', 'what if we tried', 'is it feasible', 'prototype this', or needs to investigate before committing to a direction. Produces insight, not production code.
