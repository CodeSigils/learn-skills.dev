创作并验证渲染基础架构的 render.yaml 蓝图。当用户需要编写或编辑 render.yaml、将服务与 fromDatabase/fromService/fromGroup 连接在一起、为多服务应用程序设置项目和环境、配置预览环境、根据架构进行验证或修复不可变字段错误时使用。触发术语：render.yaml、Blueprint、IaC、fromDatabase、fromService、envVarGroups、预览、项目、环境。
