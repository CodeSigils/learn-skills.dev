Erstellt und stellt Docker-Container auf Render bereit – Docker-Dateien, mehrstufige Builds, Blueprint-Docker-Felder, private Registrierungen, Layer-Caching und Plattformeinschränkungen. Verwenden Sie diese Option, wenn der Benutzer Docker, Dockerfile, Container-Images, mehrstufige Builds, Container-Registrierung, GHCR, ECR, BuildKit, DockerContext, Laufzeit-Docker oder -Image oder die Optimierung von Docker-Builds auf Render erwähnt.
