Generate a standardized commit message for the uncommitted changes in the current Git project (including a time estimate for each item), or create a work report (work report) with time estimates based on existing commit records from a specified branch or time period. When the user mentions keywords such as “generate commit message,” “commit info,” “time estimate,” “time report,” “work report,” “generate work report,” “tally the time,” “how long did these changes take,” “help me write commit info,” etc., this capability must be triggered. Even if the user only says “generate commit message” without explicitly mentioning time, the skill should still be activated, because time estimation is the core added value. If the workspace contains multiple Git projects and the user has not specified one, ask the user to clarify the target project.
