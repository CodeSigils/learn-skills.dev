部署/監控/關閉 NAMED Senpi 交易策略。當使用者指定要執行的策略時使用 - 「安裝 Spider」、「部署 Polar」、「設定 Kodiak」、「執行 Spider 策略」、「我的策略是否有效？」、「我正在運行什麼」、「列出我的策略」（→ status.py）、「我的頭部是否受保護？/他們有止損 (DSL) 嗎？以及用於拆卸，例如「關閉所有策略」、「將資金返還至主帳戶」、「撕毀所有內容」向下」（→ close.py --all）。總是透過 close.py 來拆除，而不是原始的strategy_close（這會耽誤運行時）。策略是一個包（strategy.ya + 每個進程中沒有監督執行三個可恢復步驟（建立→執行時→驗證）；close.py 拆除（停止運行時+strategy_close→平倉，返還資金）。close.py 拆除（停止運行時+strategy_close→平倉，返還資金）。
