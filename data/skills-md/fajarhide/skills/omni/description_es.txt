Úselo al instalar, verificar o configurar OMNI, cuando la salida del comando lleva un marcador `[OMNI: ...]` o cuando la salida parece más corta de lo esperado. OMNI es un gancho local que acorta la salida del shell antes de que el modelo lo lea y devuelva un identificador para lo que cortó.
