在 OAuth 登录成功后将 IDCS 声明映射到组织成员身份时使用。涵盖 mapProfileToUser、session.create.before、session.create.after 挂钩、MERGE INTO upserts、tenant-org 映射和first-admin bootstrap。关键词：IDCS 组、org_members、配置、会话挂钩、租户映射、MERGE INTO。
