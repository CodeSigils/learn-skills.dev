Resolve merge, rebase, cherry-pick, or revert conflicts by composing both sides against routed product direction, architectural constraints, executable contracts, and accepted exceptions. Writes only when explicitly authorized.
