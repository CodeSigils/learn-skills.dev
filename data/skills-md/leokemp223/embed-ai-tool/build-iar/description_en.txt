Used when you need to compile an embedded project via the IAR Embedded Workbench command line, invoking the built-in script to parse the project file, perform the build, and locate the firmware artifacts.
