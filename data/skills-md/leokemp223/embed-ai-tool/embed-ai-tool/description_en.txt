The central control entry point for the embedded development skill set. It handles two types of tasks: (1) When a user requests to install the skill from this repository, guide them to choose either a full installation or a selective installation; (2) When a user issues a vague command (such as “flash,” “compile,” “debug”) and it cannot automatically determine which specific skill to use, list candidate skills within the same category for the user to choose.
