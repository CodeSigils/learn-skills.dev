Utilizzato quando è necessario avviare o allegare una sessione GDB tramite OpenOCD, richiamando gli script in bundle per eseguire il download del firmware, il debug in tempo reale o l'analisi degli arresti anomali.
