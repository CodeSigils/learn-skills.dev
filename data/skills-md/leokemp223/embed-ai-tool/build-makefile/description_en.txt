Use this when you need to compile an embedded firmware project based on a bare Makefile (without CMake/IDE), invoking the built-in script to parse the Makefile, run make, and locate the firmware artifacts.
