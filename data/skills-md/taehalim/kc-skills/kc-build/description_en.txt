It implements the confirmed requirements of the current project as actual functions, screens, and behavior, and performs basic testing appropriate to the implementation scope. It is used for requests to create or modify code, such as “make a service,” “keep developing,” “implement the next feature,” or “complete it according to this material.” It is not used when the request is only for defining product direction, generating sample data, connecting permanent data, fixing existing issues, verification, or deployment.
