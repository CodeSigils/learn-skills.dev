Effectively prepares the Git repository, AGENTS.md, CLAUDE.md, and the initial commit for a new project in an empty folder. It is used for requests such as “set up the project,” “create a new project,” “prepare a new project workspace,” or “initialize this empty folder.” It should not be used in folders that already contain code or project settings.
