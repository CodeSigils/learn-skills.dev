Consulta la documentazione dell'API/SDK Wix per confermare un endpoint esatto, un metodo HTTP, una forma di richiesta/risposta, un campo, un'enumerazione o un errore prima di scrivere il codice Wix: non indovinare mai un'API Wix dalla memoria. Una ricerca è un flusso breve: trova la pagina giusta, quindi leggila. Due modi: (1) semplice `curl` (zero dipendenze): trova una pagina tramite la **ricerca semantica** (`POST /mcp-docs-search/v1/docs/search`, `{ search_term, document_type }` in linguaggio naturale) **o sfogliando** l'albero dei documenti come menu dalla radice `llms.txt` (aggiungi `.md` a qualsiasi percorso dei documenti), quindi leggi la pagina aggiungendo `.md` a il suo URL; e (2) gli strumenti di documentazione Wix MCP quando il tuo agente li ha. Trigger: cerca un'API Wix, trova l'endpoint/metodo Wix, conferma il corpo o il campo di una richiesta Wix, verifica una forma API Wix, esplora i documenti Wix, quale API Wix chiamo, leggi uno schema di un metodo Wix.
