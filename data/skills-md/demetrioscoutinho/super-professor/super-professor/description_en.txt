Unified entry point for the full lesson creation pipeline. Accepts a prompt or requirements file. Shows interactive plan before executing. Calls each skill in sequence with QA checkpoints. Saves state so pipeline can be resumed. Never generates lesson content directly.
