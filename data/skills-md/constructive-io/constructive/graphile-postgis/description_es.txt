Cómo exponer consultas PostGIS entre tablas a la capa ORM/GraphQL sin enviar GeoJSON al cliente. Cubre la etiqueta inteligente @spatialRelation (8 operadores, distancia paramétrica), el nodo del plano RelationSpatial y la forma ORM `donde:` que consume el cliente generado.
