So stellen Sie tabellenübergreifende PostGIS-Abfragen der ORM/GraphQL-Ebene zur Verfügung, ohne GeoJSON an den Client zu senden. Deckt das Smart-Tag @spatialRelation (8 Operatoren, parametrischer Abstand), den RelationSpatial-Blueprint-Knoten und die ORM-Form „where:“ ab, die der generierte Client nutzt.
