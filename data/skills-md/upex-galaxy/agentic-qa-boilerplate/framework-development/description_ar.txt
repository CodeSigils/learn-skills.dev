وضع تطور الإطار - يطور نموذج ضمان الجودة نفسه (KATA، Installations، cli/، scripts/، api/schemas/ Pipeline، package.json deps). خطة قائمة بذاتها → الكود → التحقق → خط أنابيب الأرشيف؛ يتم تشغيله ضمن تثبيت "gentle-ai - الحد الأدنى المحدد مسبقًا" (لا يتطلب الأمر مهارات SDD-*). يُستخدم عند إضافة واجهات برمجة التطبيقات الثابتة الجديدة، أو إعادة هيكلة فئات KATA الأساسية، أو تطوير برنامج التثبيت، أو تعديل مسار مزامنة OpenAPI، أو أي تغيير في البنية التحتية لإطار العمل الذي لا يمثل كتابة اختبار لكل تذكرة أو ضمان الجودة اليدوي. المشغلات على: /framework-development، و"evolve Framework"، و"framework refactor"، و"new Installation API"، و"modify KATA base"، و"refactor cli"، و"التطور النموذجي". لا تستخدم من أجل: كتابة اختبارات التذكرة (الاستخدام / أتمتة الاختبار)، وضمان الجودة اليدوي لكل تذكرة (الاستخدام / اختبار الركض)، وتوثيق حالات الاختبار (الاستخدام / توثيق الاختبار)، وتشغيل مجموعات الانحدار (الاستخدام / اختبار الانحدار).
