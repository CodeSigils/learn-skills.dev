框架演化模式－演化 QA 樣板本身（KATA、fixtures、cli/、scripts/、api/schemas/ pipeline、package.json deps）。自包含計畫→程式碼→驗證→歸檔管道；在「gentle-ai install --presetminimal」安裝下運作（不需要 SDD-* 技能）。在新增的固定裝置 API、重構 KATA 基底類別、改進安裝程式、修改 OpenAPI 同步管道或對框架基礎架構進行任何變更（非以單一測試編寫或手動 QA）時使用。觸發：/framework-development、「進化框架」、「框架重建」、「新夾具 API」、「修改 KATA 基礎」、「重建 cli」、「樣板進化」。請勿用於：為工單編寫測試（使用 /test-automation）、針對每個工單進行手動 QA（使用 /sprint-testing）、記錄測試案例（使用 /test-documentation）、執行迴歸套件（使用 /regression-testing）。
