Use when a user requests a read-only audit of existing or external spec/plan artifacts, or when implementation intake finds artifacts unchecked or stale; detect ambiguity, coverage gaps, contract mismatches, missing verification, and quality-gate violations.
