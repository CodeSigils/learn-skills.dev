監視 AG2 代理程式的流 - 記錄事件、偵測重複的工具呼叫、追蹤令牌支出、建構觸發器驅動的觀察者、將觀察者警報路由到模型，並在致命情況下停止。涵蓋`@observer(...)`（無狀態）、`BaseObserver`（有狀態）、內建函式（`TokenMonitor`、`LoopDetector`）、`Watch`原語（`EventWatch`、`CadenceWatch`、`DelayWatch`、`IntervalWatch`、`CronWatch`、`Cron``、Ofjert``fserver、`Cron````Cronf`fserver、OfFron``o）、`Cron````Cronf`fserver(OfFron``fserver、`Cron````Cronf`fserver、OfFron``o. (`Severity.INFO/WARNING/CRITICAL/FATAL`)、`AlertPolicy` 和 `HaltEvent`。當使用者需要可觀察性、運行時安全防護、警報或基於批次/時間的反應邏輯時使用。
