AG2 에이전트의 스트림을 모니터링합니다. 이벤트를 기록하고, 반복되는 도구 호출을 감지하고, 토큰 지출을 추적하고, 트리거 기반 관찰자를 구축하고, 관찰자 ​​경고를 모델로 라우팅하고, 치명적인 조건에서 중지합니다. `@observer(...)`(상태 비저장), `BaseObserver`(상태 저장), 내장(`TokenMonitor`, `LoopDetector`), `Watch` 프리미티브(`EventWatch`, `CadenceWatch`, `DelayWatch`, `IntervalWatch`, `CronWatch`, `AllOf`, `AnyOf`, `Sequence`), `ObserverAlert`를 포함합니다. (`Severity.INFO/WARNING/CRITICAL/FATAL`), `AlertPolicy` 및 `HaltEvent`. 사용자가 관찰 가능성, 런타임 안전 가드, 경고 또는 배치/시간 기반 반응 논리를 원할 때 사용합니다.
