管理 AG2 多代理网络 — 身份（“Passport”、“Resume”）、每个代理“Rule”，具有两个顶级块“access”（“AccessBlock”）和“limits”（“LimitsBlock”，嵌套“RateBlock”+“InboxBlock”）、可交换的“HubArbiter”/“RuleBasedArbiter”访问和路由接缝、“AuthAdapter”/“AuthRegistry”注册、带有 `audit` / `notify_channel` / `auto_close` 违规处理程序的通道级 `Expectation`、集线器的仅追加审核日志和 `AUDIT_KIND_*` 常量、实时 `HubListener` / `BaseHubListener` 可观察性以及 `Hub` `on_*` 挂钩和 `register_sweeper`，以及通过 `agent.task(...)` + `TaskMirror` 进行任务观察（更新“Resume.observed”用于同行排名）。当用户需要速率限制、访问策略、SLA、合规性跟踪、实时指标/警报、功能驱动的对等排名或检查网络上实际发生的情况时使用。在 `ag2-network-quickstart` 之后加载它。对于代理端表面（自定义处理程序、视图、LLM 工具、“HumanClient”），请参阅“ag2-network-tools-and-views”。
