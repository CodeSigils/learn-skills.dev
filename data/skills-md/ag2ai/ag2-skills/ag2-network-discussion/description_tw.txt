開啟 AG2 網路「討論」通道 — N 方 (2+) 循環，每位參與者以固定順序發言，循環直至明確關閉或 TTL。當使用者想要與固定演員、小組討論或循環審查者進行腦力激盪時使用。涵蓋 `agent_client.open(type="discussion", target=[...], Knobs={"ordering": ORDERING_ROUND_ROBIN})`、`expected_next_speaker` 輪轉、`hc.can_send(...)` 探測模式（預設處理程序在未跳過）時跳轉為輪迴轉為````看到模式（期望預設值（`warn` at 120 秒/「隱藏」600 秒）、N 個參與者的視圖視窗大小以及與此適配器配合使用的四種緊密模式。在 `ag2-network-quickstart` 之後載入它。對於條件切換或聲明式編排，請參閱「ag2-network-workflow」。
