다중 에이전트 AG2 네트워크를 구축하십시오. 둘 이상의 '에이전트'가 상호 작용해야 할 때마다 이를 로드하십시오. 네트워크는 'ag2'의 표준 다중 에이전트 패턴입니다. `Hub.open`, `LocalLink`, `HubClient.register`, `Passport`, `Resume`, 채널 수명 주기(PENDING → ACTIVE → CLOSING → CLOSED), 2개의 2자 채널 어댑터(엄격한 1Q1R의 경우 `consulting`, 자유 형식의 경우 `conversation`), `Envelope` 통신 형식, 시청자 라우팅, `wait_for_channel_event`, WAL을 다룹니다. `hub.read_wal`을 통해 재생하고 5개의 채널 폐쇄 경로를 사용합니다. 진입점 — 보다 심층적인 요구 사항을 위해 `ag2-network-discussion`(N-party 라운드 로빈), `ag2-network-workflow`(선언적 오케스트레이션/GroupChat 마이그레이션), `ag2-network-governance`(규칙/예상/감사) 또는 `ag2-network-tools-and-views`(사용자 정의 처리기/피어 검색)로 라우팅합니다.
