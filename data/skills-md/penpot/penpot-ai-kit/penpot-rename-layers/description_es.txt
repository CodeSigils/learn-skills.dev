Cambie semánticamente el nombre de las capas de Penpot a nombres de elementos HTML (nav, encabezado, principal, sección, artículo, botón, entrada, etiqueta, h1-h6, p, ul, li, img) o nombres de funciones de kebab-case, reemplazando nombres generados automáticamente como 'Rectángulo 12'. Úselo para limpiar los nombres de capas, preparar un archivo para la transferencia o como condición previa para auditorías de accesibilidad (jerarquía de encabezados) y revisión de diseño a código (mapeo semántico). Activadores: 'cambiar el nombre de las capas', 'nombres de las capas semánticas', 'cambiar el nombre a elementos HTML', 'limpiar los nombres de las capas', 'arreglar los nombres de las capas', 'agregar nombres semánticos a las capas', 'preparar las capas para la transferencia'.
