Displays the real token usage and estimated reduction amount for the current session. Reads directly from the Claude Code session log — no AI estimation. Launched with `/genshijin-stats`. The output is injected by a mode‑tracker hook, and the model itself does not perform numeric calculations.
