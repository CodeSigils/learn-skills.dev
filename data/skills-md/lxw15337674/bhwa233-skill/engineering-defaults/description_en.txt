Use this before adding dependencies, hand‑crafting generic components or utility functions, and before adding, modifying, or deleting any tests—including tests you personally want to add. The default answer is to reuse rather than create, and to avoid adding tests rather than add them. It includes a five‑level decision ladder that prioritizes reuse, four situations where writing tests is worthwhile, thirteen test patterns that must be avoided, and what counts as verification when you don’t write tests. Applicable to any repository.
