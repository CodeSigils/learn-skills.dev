Genera un servizio Angular che chiama gli endpoint CRUD up-api per una determinata entità (getAll, getById, save, update, delete). Da utilizzare quando l'utente richiede il passaggio 3 del Giorno 2 dell'esercitazione CRUD o chiede di creare il servizio Angular che comunica con up-api. Richiede la stessa entità utilizzata il giorno 1.
