2D character skill distiller. Input a character name + work title, locally retrieve core data → cross-validate → behavior distillation → generate a runnable, traceable, updatable character skill. Trigger phrases: "generate XX's skill", "distill XX", "make a XX character", "turn XX into skill", "create a XX", "/csp". Vague requests also trigger: "want to talk about a tsundere character", "any yandere recommendations", "help me make a character from XX work". Depends on Python; the core site prefers local scripts, while web search skill / MCP / WebFetch is used only as reinforcement and fallback.
