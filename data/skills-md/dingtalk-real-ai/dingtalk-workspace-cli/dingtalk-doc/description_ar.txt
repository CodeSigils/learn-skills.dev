DingTalk Docs (المستندات السحابية). يُستخدم عندما يقول المستخدم كتابة مستند / قراءة المستند / إنشاء مستند / تحرير مستند / بحث في مستند / كتلة مستند / تحرير كتلة / إدراج تخفيض السعر / تحميل ملف إلى مستند. يختلف عن dingtalk-drive (تخزين ملفات DingTalk)، وdingtalk-aitable (جداول البيانات)، وdingtalk-wiki (مساحة قاعدة المعرفة). بادئة الأمر: dws doc.
