Autonomous software factory for a single Linear ticket — plan with a smart model, blind-critique and refine the plan, comment it on the ticket, implement via sub-agent, open atomic stacked PRs, babysit CI/review until mergeable, then merge on user green-light. Use when the user wants /factory, factory, or to fully drive one Linear ticket from plan → PRs → merge.
