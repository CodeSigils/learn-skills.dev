通过单个命令端到端修复跟踪器问题（默认为 GitHub 问题）。以交互方式驱动自动修复链 - 证明问题仍然需要工作 (om-verify-in-repo)，定位错误 (om-root-cause)，通过回归测试实现最小修复 (om-fix)，打开带标签的草稿 PR (om-open-pr)，然后在自动修复模式下循环 om-auto-review-pr 直至干净。在隔离的工作树中运行，遵守正在进行的索赔协议，并在问题已解决或已索赔时干净地停止。
