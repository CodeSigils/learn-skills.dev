最近マージされた PR が正式に修正されたトラッカーの問題を (`fixes`/`closes`/`resolves` キーワードまたは `closedIssuesReferences` を介して) 閉じ、PR がマージされずに閉じられたか、非ベース ブランチにマージされた問題に関する情報コメントを投稿します。マージ後のハウスキーピングとリリースの準備に使用します。クレーム ロックを尊重し、裸の `#N` メンションには決して作用しません。
