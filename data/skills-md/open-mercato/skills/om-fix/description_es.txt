Implementa el cambio mínimo de código identificado por el paso om-root-cause, agrega pruebas de regresión y ejecuta la puerta de validación configurada. Reclama el problema del rastreador al inicio (cesionario + etiqueta en progreso + comentario de reclamo), por lo que la automatización simultánea retrocede. No confirma, empuja ni abre un PR: ese es el trabajo del paso om-open-pr.
