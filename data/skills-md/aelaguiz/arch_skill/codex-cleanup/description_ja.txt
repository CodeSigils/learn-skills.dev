~/.codex で 1 週間以上古い古い状態をパージして、並列コーデックス CLI/TUI セッションが肥大化したログ SQLite、ローテーションされていない 4 GB 以上の TUI ログ、および数か月にわたる日付の付いたセッション JSONL でロックアップするのを防ぎます。 ~/.codex が複数 GB の場合、12 個以上の並列 codex インスタンスが「データベースがロックされている」で停止する場合、logs_2.sqlite の WAL が 100 MB を超えた場合、または毎週の定期的なパージが必要な場合に使用します。ライブ構成 (auth.json、config.toml、version.json、installation_id、.codex-global-state.json) の編集用ではなく、コーデックス Git リポジトリ用ではなく、~/.codex の外部にある無関係なキャッシュのクリーニング用でもありません。
