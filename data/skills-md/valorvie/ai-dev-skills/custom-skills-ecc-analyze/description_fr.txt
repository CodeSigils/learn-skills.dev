Examinez la liste de distribution ECC (everything‑claude‑code), qui comprend deux modes :  
(A) Réduction de la liste existante : analysez le chevauchement et la qualité entre `distribution.yaml.enabled` et les `ai-dev-skills` de première partie, puis produisez des recommandations `enabled_remove` ;  
(B) Évaluation de l’introduction de nouveaux éléments : à partir de `ecc-catalog.yaml` et de la portée de la dernière mise à jour, présentez les compétences ECC en amont nouvellement ajoutées, analysez les scénarios d’application et recommandez s’il faut les ajouter à `enabled`.  

Utilisation : (1) évaluer si la liste de distribution ECC doit être allégée, (2) décider d’adopter ou non une compétence ECC en amont récemment ajoutée, (3) créer un fichier de démarrage `ecc-profile.yaml` pour les utilisateurs finaux, (4) réaliser une revue périodique trimestrielle ou semestrielle.  

Déclencheurs : « analyze ECC skill », « ECC overlap », « ECC list slimming », « ECC new items », « ECC upstream addition », « whether to adopt », « ecc analyze », « ecc new items review », « ecc profile starter », « ECC quality review ».  

Veuillez toujours répondre dans la langue de l’utilisateur.
