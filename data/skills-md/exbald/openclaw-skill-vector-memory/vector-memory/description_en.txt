Supplementary memory layer — semantic vector search over ALL workspace files and chat sessions using OpenAI embeddings. Expands coverage beyond built-in memory_search (which only indexes MEMORY.md + memory/*.md). Use for recalling past conversations, finding context in tasks/CRM/projects, and searching across the full workspace.
