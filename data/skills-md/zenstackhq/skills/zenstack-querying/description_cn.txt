使用 ZenStack V3 客户端查询和更改数据。在创建 ZenStackClient（SQLite/Postgres/MySQL 方言）、使用 Prisma 兼容的 ORM API（findMany/create/update/etc.）、关系查询（select/include）、计算字段、多态模型、强类型 JSON、写入数据验证（@email/@length/@@validate）、事务、Kysely 查询生成器逃生舱口（$qb / $expr）、自定义过程、原始 SQL、日志记录或处理时使用ORM 错误。
