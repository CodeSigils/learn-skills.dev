قم بإعداد وتكوين وإدارة مشروع ZenStack V3. يُستخدم عند تثبيت ZenStack، أو دعم مشروع جديد، أو تشغيل `zen` CLI (إنشاء). (بالنسبة لعمليات ترحيل مخطط قاعدة البيانات، استخدم zenstack-db-migration؛ لترحيل مشروع Prisma، استخدم zenstack-migrate-from-prisma؛ للترقية من ZenStack V2، استخدم zenstack-migrate-from-v2.)
