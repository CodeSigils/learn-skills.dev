Prisma ORM patterns in a TypeScript backend — pattern design, query optimization, transactions, pagination, and key pitfalls such as updateMany returning a count instead of records, $transaction timeouts, migrate dev resetting the database, @updatedAt being skipped in bulk writes, and serverless connection exhaustion.
