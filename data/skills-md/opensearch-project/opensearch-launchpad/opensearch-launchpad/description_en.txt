Build search applications with OpenSearch. Guides you through setting up semantic search, vector search, hybrid search, neural search, BM25, dense vector, sparse vector, agentic search, RAG, retrieval, embeddings, and KNN. Sets up OpenSearch locally via Docker, plans search architecture, creates indices, ML models, ingest pipelines, launches a search UI, and optionally deploys to AWS OpenSearch Service or Serverless. Use when the user mentions OpenSearch, search app, index setup, search architecture, document search, search relevance, or any related search topic.
