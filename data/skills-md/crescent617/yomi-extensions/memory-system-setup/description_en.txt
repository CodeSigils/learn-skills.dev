Bootstrap a persistent memory system for a new workspace in a single step (memory/ directory + recall retrieval + the two permanently running crons dream/janitor). To trigger manually, say “install memory system” when you want to set it up.
