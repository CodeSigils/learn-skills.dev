Use the persistent markdown files (plan.md / findings.md / progress.md) under <CWD>/.agents/tasks/ for planning and tracking the progress of complex tasks, preventing goal drift and context loss in long-running tasks. Apply this when a task involves multiple steps or extensive tool calls, when the user requests a “plan/outline/decompose the task,” or when a conversation is interrupted and progress needs to be restored.
