Utilizza questa competenza quando un agente AI deve ispezionare, verificare, eseguire il debug o profilare un'app Vite live eseguendo snippet temporanei all'interno della pagina del browser e leggendo i registri del browser o gli artefatti acquisiti. Da utilizzare per lo stato del client dopo interazioni, moduli di app importati, stato DOM, input di tipo umano, stato canvas/WebGL/Three.js, screenshot, video, analisi CPU/rete/prestazioni/heap, WebXR/Three.js XR con IWER e comportamento solo runtime senza modificare i file dell'app.
