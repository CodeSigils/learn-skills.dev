Main workflow for video generation driven by SRT subtitles. Convert an SRT subtitle file into a Remotion video project, automatically generate storyboard scripts, create scene components, and compose the final video. Used when a user needs to generate a Remotion video from an SRT subtitle file.
