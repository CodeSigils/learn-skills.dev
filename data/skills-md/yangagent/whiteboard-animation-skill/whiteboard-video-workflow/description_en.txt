An end-to-end workflow that automatically generates a complete whiteboard animation video from an SRT subtitle file. It proceeds sequentially through three stages: storyboard parsing, image generation, and video generation. It is triggered when the user provides an SRT file and requests the creation of a whiteboard animation video, or says "generate whiteboard video from subtitles" or "whiteboard video workflow".
