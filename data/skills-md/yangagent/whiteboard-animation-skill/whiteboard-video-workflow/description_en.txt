An end-to-end workflow that automatically generates a complete whiteboard animation video from an SRT subtitle file. It sequentially completes the three stages of storyboard parsing, image generation, and video generation. Triggered when the user provides an SRT file and requests to generate a whiteboard animation video, or says "generate whiteboard video from subtitles" or "whiteboard video workflow".
