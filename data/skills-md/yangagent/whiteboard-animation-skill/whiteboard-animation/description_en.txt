Generate whiteboard hand-drawn animation videos from images. Convert any color image into an animation that includes a line-art drawing phase and a coloring phase, with a hand-overlay effect, and output as H.264 MP4 video. Supports both single and batch modes. Triggered when the user says "turn image into whiteboard animation", "whiteboard animation", or "batch whiteboard animation".
