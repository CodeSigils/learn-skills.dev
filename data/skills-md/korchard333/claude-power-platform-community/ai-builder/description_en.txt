AI Builder for Power Platform. Use when: document processing, prediction models, GPT prompt actions in flows, prebuilt models (text recognition, sentiment, entity extraction), custom models, AI Builder in Canvas Apps and Power Automate, licensing.
