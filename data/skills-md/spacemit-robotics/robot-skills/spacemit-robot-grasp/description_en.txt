The build, functional testing, and performance testing entry for components/control/grasp; by default, dummy driver tests are run first, and hardware verification is performed manually via smoke testing.
