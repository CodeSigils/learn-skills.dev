SpacemiT Robot SDK's initial acquisition and initialization entry; used for preparing the SDK on the board side for local/remote scenarios, or only preparing the PC local SDK in hybrid mode, handling repo base dependencies, workspace, repo init/sync, and SROBOTIS_ROOT verification, without taking on routine builds or testing.
