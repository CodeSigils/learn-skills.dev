Úselo cuando los operadores ternarios están anidados, expresiones condicionales complejas como `a && !b || c` se usan sin nombre, o el código que no se ejecuta simultáneamente se mezcla en un solo componente
