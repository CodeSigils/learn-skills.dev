Da utilizzare quando gli operatori ternari sono espressioni condizionali nidificate e complesse come `a && !b || c` vengono utilizzati senza nome oppure il codice che non viene eseguito contemporaneamente viene mescolato in un unico componente
