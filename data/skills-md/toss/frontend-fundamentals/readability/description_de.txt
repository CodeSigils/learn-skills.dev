Wird verwendet, wenn ternäre Operatoren verschachtelte, komplexe bedingte Ausdrücke wie „a && !b ||“ sind c` werden ohne Namen verwendet oder Code, der nicht gleichzeitig ausgeführt wird, wird in einer einzigen Komponente gemischt
