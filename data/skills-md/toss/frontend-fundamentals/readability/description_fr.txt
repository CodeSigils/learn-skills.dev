À utiliser lorsque les opérateurs ternaires sont des expressions conditionnelles complexes imbriquées telles que `a && !b || c` sont utilisés sans nom, ou le code qui ne s'exécute pas simultanément est mélangé dans un seul composant
