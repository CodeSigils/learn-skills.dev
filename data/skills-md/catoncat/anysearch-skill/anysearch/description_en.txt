Probe the web with AnySearch when current facts, source discovery, URL extraction, multi-angle comparison, or live vertical schemas are needed. Use a compact probe first, extract selected URLs only when evidence is needed, and recover through the key pool on auth/quota failures.
