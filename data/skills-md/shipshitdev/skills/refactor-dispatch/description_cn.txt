改进现有代码的单一前门 - 按模式路由到正确的引擎：deslop（剥离 AI slop、代码 + 产品）、代码（安全行为保留重构）、债务（技术债务寄存器）、perf（前端 + 后端优化）、结构（只读结构审查）或堆栈（依赖项 + 框架模式现代化）。支持 /refactor 命令。当被要求重构、清理、deslop、偿还技术债务、优化或现代化时使用，并且必须从“deslop”、“debt”、“perf”或“stack”等参数中选择模式。
