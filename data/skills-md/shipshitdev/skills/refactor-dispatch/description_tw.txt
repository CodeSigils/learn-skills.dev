改進現有程式碼的單一前門 - 按模式路由到正確的引擎：deslop（剝離 AI slop、程式碼 + 產品）、程式碼（安全行為保留重構）、債務（技術債務暫存器）、perf（前端 + 後端最佳化）、結構（唯讀結構審查）或堆疊（依賴 + 框架模式現代化）。支援 /refactor 命令。當被要求重構、清理、deslop、償還技術債、最佳化或現代化時使用，並且必須從「deslop」、「debt」、「perf」或「stack」等參數中選擇模式。
