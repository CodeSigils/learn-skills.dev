Turborepo monorepo 构建系统指南。触发：“turbo.json”、任务管道、
`dependsOn`、缓存、远程缓存、`turbo` CLI、`--filter`、`--affected`、CI 优化、
环境变量、内部包、monorepo 结构和包边界。

当用户配置任务或工作流程、创建包、设置
monorepo，在应用程序之间共享代码，运行更改的包，调试缓存行为，
或者在“apps/”加上“packages/”工作区中工作。
