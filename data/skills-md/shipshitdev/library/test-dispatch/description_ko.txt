테스트를 위한 단일 현관문. 하위 명령(run, qa, tdd, e2e, Coverage, init 또는 regression)을 구문 분석하고 올바른 테스트 엔진으로 라우팅합니다: test-runner(테스트 실행, 실패 수정), qa-reviewer(완료된 작업에 대한 구조화된 검증 통과), tdd(red-green-refactor 워크플로), playwright-e2e-init(프론트엔드 프로젝트에 대한 스캐폴드 E2E 테스트), husky-test-coverage(커버리지 적용) git 후크를 통한 임계값), test-cicd-init(Vitest + GitHub Actions CI 설정) 또는 ai-regression-testing(AI 사각지대를 대상으로 하는 회귀 테스트 설계). /test 명령을 지원합니다. 테스트 실행, 테스트 작성, 테스트 설정, 적용 범위 확인 또는 TDD 시작을 요청할 때 사용하고 작업은 "run", "qa", "tdd", "e2e", "coverage", "init" 또는 "regression"과 같은 인수에서 선택해야 합니다.
