Uncle Hua's "md/html bidirectional pipeline" skill offers three capabilities in one package: (1) Use Microsoft Markitdown to convert any file (PDF/DOCX/PPTX/XLSX/HTML/image/audio/YouTube/EPub/ZIP) into clean markdown; (2) Use Pandoc plus four carefully chosen templates to transform markdown into impressive HTML (articles/reports/reading mode/interactive exploration), inheriting huashu-design's anti‑AI slop aesthetic; (3) Use html-to-markdown plus trafilatura to convert HTML or URLs back into markdown losslessly. This implements Uncle Hua's "markdown production, HTML consumption" methodology. Trigger words: md to html, html to md, pdf to md, docx to md, pptx to md, xlsx to md, file to md, URL to md, document to md, convert to markdown, make html, generate html, web page to md, import document, import md, export html, md to html, html to md, any to md, markitdown, pandoc. Even if the user only says "turn this PDF into md", "make this md into a web page", or "save this web page", it should still trigger.
