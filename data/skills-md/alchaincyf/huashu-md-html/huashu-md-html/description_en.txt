Uncle Hua's "md/html bidirectional pipeline" skill offers three capabilities in one stop: (1) Use Microsoft Markitdown to convert any file (PDF/DOCX/PPTX/XLSX/HTML/image/audio/YouTube/EPub/ZIP) into clean markdown; (2) Use Pandoc + four carefully selected templates to transform markdown into outstanding HTML (articles, reports, reading mode, interactive exploration), inheriting huashu‑design's anti‑AI sloppy aesthetic; (3) Use html‑to‑markdown + trafilatura to losslessly convert HTML or URLs back to markdown. This implements Uncle Hua's "markdown production, HTML consumption" methodology. Trigger words: md to html, html to md, pdf to md, docx to md, pptx to md, xlsx to md, file to md, URL to md, document to md, convert to markdown, make html, generate html, webpage to md, import document, import md, export html, md to html, html to md, any to md, markitdown, pandoc. Even if a user simply says "convert this PDF to md", "make this md into a webpage", or "save this webpage", it should trigger.
