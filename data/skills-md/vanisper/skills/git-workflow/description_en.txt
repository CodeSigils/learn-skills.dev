Reusable Git workflow skill. Used to handle operations such as git status, staging, commit, push, Pull Request, branch merging, rollback, remote repository, and tags. The commit header must follow the Conventional Commits and Vanisper commitlint style: English type, optional English scope, subject following the project's language preferences, header length no more than 72 characters, emojis can be placed before the type or at the beginning of the subject.
