A reusable Git workflow skill. It handles git status, staging, commit, push, Pull Request, branch merging, rollback, remote repository, and tag operations. The commit header must follow Conventional Commits and Vanisper commitlint style: English type, optional English scope, subject follows the project's language preference, header length no more than 72 characters, and emoji can be placed before the type or at the beginning of the subject.
