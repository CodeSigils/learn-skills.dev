(Heropy) Used when designing and writing global state (store) with Zustand (JuiceStand) in a React project. When the user mentions “zustand,” “JuiceStand,” “global state,” “store separation,” “store creation,” “make a store for me,” “OO store creation,” “state management library,” “create function,” “useShallow,” “persist/immer/devtools middleware,” or tries to resolve prop drilling between React components, this skill must be followed. It includes store definition patterns (action separation, initialization, deletion), selector usage rules, middleware composition order, TypeScript conventions, and the procedure to automatically generate store files from trigger input.
