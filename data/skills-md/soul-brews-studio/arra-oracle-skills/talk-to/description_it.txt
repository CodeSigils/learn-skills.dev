Parla con un altro agente Oracle tramite thread. Da utilizzare quando l'utente dice "parla con", "messaggio", "chatta con" o desidera comunicare con un altro agente (ad esempio "parla con pulsare", "messaggio neo"). NON eseguire l'attivazione per il feed social OracleNet (utilizzare /oraclenet), la gestione delle competenze (utilizzare /oracle) o il registro familiare (utilizzare /oracle-family-scan).
