Find every test suite touched or impacted by the branch's changes, run them, and emit a Markdown pass/fail table for the PR. Use when a test report, test table, "which tests are impacted", or PR test coverage is wanted, or at the end of /implement.
