在选择哪个 VTEX IO 身份验证令牌应支持来自后端应用程序的请求时应用。涵盖 `ctx.authToken`、`ctx.storeUserAuthToken`、`ctx.adminUserAuthToken`、`authMethod` 以及请求者上下文应如何确定 VTEX 客户端使用的身份。用于决定在店面支持的请求、管理操作或应避免硬编码的 VTEX 凭据的应用程序级集成中与 VTEX 端点进行通信的身份。
