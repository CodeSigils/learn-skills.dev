`@atscript/*` 패키지 또는 `.as` 파일로 작업할 때 사용합니다. `.as` = 유형, 메타데이터 및 유효성 검사 제약 조건에 대한 단일 정보 소스입니다. `.as` 구문, `@meta.*` / `@expect.*` / 사용자 정의 주석, 기본 요소, `asc` CLI + `atscript.config.*`, 생성된 `.as.d.ts` / `.as.js` / `atscript.d.ts`, 런타임 헬퍼(`Validator`, JSON 스키마, 직렬화), `unplugin-atscript`, `@atscript/moost-validator`, VSCode를 다룹니다. LSP 및 플러그인 저작.
