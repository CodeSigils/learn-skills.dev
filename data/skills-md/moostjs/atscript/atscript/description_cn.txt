使用“@atscript/*”包或“.as”文件时使用。 `.as` = 类型、元数据和验证约束的单一事实来源。涵盖 `.as` 语法、`@meta.*` / `@expect.*` / 自定义注释、原语、`asc` CLI + `atscript.config.*`、生成的 `.as.d.ts` / `.as.js` / `atscript.d.ts`、运行时助手（`Validator`、JSON Schema、序列化）、`unplugin-atscript`、`@atscript/moost-validator`、VSCode LSP 和插件创作。
