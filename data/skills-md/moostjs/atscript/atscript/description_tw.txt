使用“@atscript/*”套件或“.as”檔案時使用。 `.as` = 類型、元資料和驗證約束的單一事實來源。涵蓋 `.as` 語法、`@meta.*` / `@expect.*` / 自訂註解、原語、`asc` CLI + `atscript.config.*`、產生的 `.as.d.ts` / `.as.js` / `atscript.d.ts`、執行時間、JValidator Schema、序列化）、`unplugin-atscript`、`@atscript/moost-validator`、VSCode LSP 和插件創作。
