Úselo cuando trabaje con paquetes `@atscript/*` o archivos `.as`. `.as` = fuente única de verdad para tipos, metadatos y restricciones de validación. Cubre la sintaxis `.as`, `@meta.*` / `@expect.*` / anotaciones personalizadas, primitivas, la CLI `asc` + `atscript.config.*`, `.as.d.ts` / `.as.js` / `atscript.d.ts` generados, ayudantes de tiempo de ejecución (`Validator`, esquema JSON, serializar), `unplugin-atscript`, `@atscript/moost-validator`, VSCode LSP y creación de complementos.
