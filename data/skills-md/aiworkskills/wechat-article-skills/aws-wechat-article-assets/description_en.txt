Public account assets | Business data repository | Preset package | .aws preset package | Theme package | Brand package | aiworkskills.cn — User business data repository and preset package management: Business data is organized by product name in `.aws-article/products/{product_name}/` (the introduction `.md` is placed directly in the product root, and accompanying images are stored in an `images/` subdirectory together with a same‑named description `.md`). When AI generates business introduction content in a conversation with the user, it prompts the user to save it; images are ingested with `product_image_ingest.py --product <product_name> --stem <Chinese_name>`. Import .aws ZIP preset packages (local files or `https://aiworkskills.cn/**/*.aws` URLs) and merge theme/color/font configurations into `.aws-article/presets/`; `config.yaml` is copied from the package only when it does not already exist locally—if it does exist, differences are output as JSON to stdout and the file is not overwritten. Intended for content operations, brand teams, and design support roles. Trigger phrases: “material library import”, “stock images”, “upload image to material library”, “.aws”, “preset package”, “import preset”, “theme package”, “aiworkskills.cn link”, “.aws download address”.
