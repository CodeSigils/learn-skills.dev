Basic data for customer editing scenarios (dropdown options). Used when a user needs to retrieve the selectable ID lists for customer status, tags, region, and industry to modify customer information; via `lfy-cli base get_options`, the property includes only these four categories; the `cli` parameter can be omitted on the API (default is false); the Agent example still recommends setting `cli=true`.
