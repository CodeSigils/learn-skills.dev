Da utilizzare quando si aggiunge, si modifica, si testa o si rivede la persistenza di Postgres nei servizi Rust che utilizzano sqlx, in particolare durante la progettazione di migrazioni, query! o query_as! chiamate, iniezione PgPool, transazioni, SQL controllato in fase di compilazione, modalità offline SQLx, limiti del repository o test di integrazione supportati da database.
