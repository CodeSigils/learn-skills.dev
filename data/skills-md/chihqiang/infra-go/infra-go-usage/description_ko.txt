infra-go Go 인프라 라이브러리를 사용하여 비즈니스 프로젝트에서 서비스를 구축하세요. conf(구성 로딩), logger(구조적 로깅), orm(MySQL/PostgreSQL/SQLite), redisx(Redis 및 분산 잠금), 캐시(통합 캐시: 메모리/Redis), httpx(HTTP 서비스, 매개변수 바인딩, 통합 Response[T] 응답, 미들웨어: 회로 차단/시간 초과/속도 제한/암호화/로드 차단), jwt(인증 미들웨어), ratelimit를 다룹니다. (메모리/Redis 속도 제한), 차단기(회로 차단기), 재시도(재시도), taskq(비동기 작업 대기열), 저장소(OSS/COS/KODO 개체 저장소), websocket(실시간 통신), 추적(추적 링크), 해시(암호/다이제스트/암호화/HMAC 서명), Cast(유형 변환), stringx(문자열), syncx(동시성 기본 형식), 서비스(ServiceGroup 서비스 오케스트레이션). 사용 시기: Go에서 비즈니스 서비스를 작성할 때 인프라 모듈을 선택/초기화/조립해야 하거나, conf+logger+orm+redisx+httpx+jwt를 결합해야 하거나, 통합 응답, 미들웨어, 정상적인 종료 및 기타 인프라가 필요합니다.
