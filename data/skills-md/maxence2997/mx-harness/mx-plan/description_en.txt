Decompose an approved design spec into a concrete, ordered task list. Reads ~/.mx/<project>/<name>/spec.md and produces ~/.mx/<project>/<name>/plan.md. Each task maps to exactly one mx-commit type and specifies the expected test and outcome. No vague placeholders allowed. Hard gate: no implementation begins until the user approves the task list. Use after mx-brainstorm approves the design spec.
