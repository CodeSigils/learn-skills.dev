使用 wooksjs monorepo 或任何 @wooksjs 包时使用此技能。涵盖 @wooksjs/event-core (EventContext、key/cached/cachedBy 插槽、defineWook 可组合项、defineEventKind、AsyncLocalStorage 传播、useRouteParams、useEventId、useLogger)、@wooksjs/event-http (createHttpApp、HTTP 路由、useRequest、useHeaders、useCookies、useUrlParams、useAuthorization、useAccept、useResponse、HttpResponse、HttpError、 prepareTestHttpContext），@wooksjs/event-cli（createCliApp，命令路由，useCliOptions，useCliOption，useCliHelp，useAutoHelp），@wooksjs/event-ws（WooksWs，onMessage，onConnect，onDisconnect，useWsConnection，useWsMessage，useWsRooms，useWsServer，WsRoomManager，WsBroadcastTransport，WsError）， @wooksjs/event-wf（createWfApp、工作流程步骤和流程、useWfState、暂停/恢复、条件、循环、字符串处理程序、StepRetriableError、出口）、@wooksjs/ws-client（createWsClient、WsClient、发送、调用、订阅、开启、重新连接、RPC、推送侦听器、WsClientError）。
