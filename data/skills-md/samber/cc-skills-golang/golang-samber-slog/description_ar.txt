ملحقات التسجيل المنظمة لـ Golang باستخدام حزم samber/slog-**** - خطوط أنابيب متعددة المعالجات (slog-multi)، أخذ عينات من السجل (slog-sampling)، تنسيق السمات (slog-formatter)، البرامج الوسيطة HTTP (slog-fiber، slog-gin، slog-chi، slog-echo)، وتوجيه الواجهة الخلفية (slog-datadog، slog-sentry، slog-loki، slog-syslog، slog-logstash، slog-graylog...). يتم تطبيقه عند استخدام أو اعتماد slog، أو عندما تستورد قاعدة التعليمات البرمجية بالفعل أي حزمة github.com/samber/slog-*.
