Golang 重构——用于重构现有 Go 代码的安全、大规模流程：覆盖范围自适应安全网、工具驱动的行为保留转换（gopls Rename/Inline/Extract、`gofmt -r`、`eg`、`gopatch`、`go/analysis` 修复程序）、映射到 Go 的 Fowler 目录、打破导入周期、跨包移动类型，以及在重构分支。当代码难以维护、函数/类型变得太大、代码异味需要修复、添加功能被当前结构阻止或用户要求清理、重构或改进 Go 代码时适用——也适用于大规模重命名、提取函数/接口、在包之间移动代码、拆分包或计划多步骤重构。其他地方拥有的目标样式 → 请参阅“samber/cc-skills-golang@golang-naming”（重命名）、“@golang-project-layout”（拆分）、“@golang-modernize”（习语）、“@golang-code-style”（控制流）、“@golang-design-patterns”（模式/DI）。
