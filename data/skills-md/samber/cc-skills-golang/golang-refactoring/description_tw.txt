Golang 重構－用於重構現有 Go 程式碼的安全性、大規模流程：覆蓋範圍自適應安全網、工具驅動的行為保留轉換（gopls Rename/Inline/Extract、`gofmt -r`、`eg`、`gopatch`、`go/analysis` 修復程式）、映射到 Go 的 Fowler 目錄、導入週期、`go/analysis` 修復程式）、映射到 Go 的 Fowler 目錄、導入週期、在跨包之間。當程式碼難以維護、函數/類型變得太大、程式碼異味需要修復、添加功能被當前結構阻止或用戶要求清理、重構或改進 Go 程式碼時適用——也適用於大規模重命名、提取函數/介面、在套件之間移動程式碼、分割包或規劃多步驟重構。其他地方擁有的目標樣式 → 請參閱“samber/cc-skills-golang@golang-naming”（重新命名）、“@golang-project-layout”（拆分）、“@golang-modernize”（習語）、“@golang-code-style”（控制流）、“@golang-design-patterns”（“@golang-code-style”（控制流）、“@golang-design-patterns”（1/DI）。
