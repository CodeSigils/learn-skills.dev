Golang 中的内存缓存使用 samber/hot — 逐出算法（LRU、LFU、TinyLFU、W-TinyLFU、S3FIFO、ARC、TwoQueue、SIEVE、FIFO）、TTL、缓存加载器、分片、重新验证时失效、缺少密钥缓存和 Prometheus 指标。适用于使用或采用samber/hot、代码库导入github.com/samber/hot时、或项目高频度重复加载相同的中低基数资源且需要降低延迟或后端压力时。
