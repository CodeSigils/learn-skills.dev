Almacenamiento en caché en memoria en Golang usando samber/hot: algoritmos de desalojo (LRU, LFU, TinyLFU, W-TinyLFU, S3FIFO, ARC, TwoQueue, SIEVE, FIFO), TTL, cargadores de caché, fragmentación, estado obsoleto mientras se revalida, almacenamiento en caché de claves faltantes y métricas de Prometheus. Aplicar cuando se usa o se adopta samber/hot, cuando el código base importa github.com/samber/hot, o cuando el proyecto carga repetidamente los mismos recursos de cardinalidad media a baja con alta frecuencia y necesita reducir la latencia o la presión del backend.
