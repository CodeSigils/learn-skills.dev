使用 spf13/viper 的 Golang 設定函式庫 — 分層優先權（flag > env > file > KV > default）、BindPFlag/BindPFlags、SetEnvPrefix + SetEnvKeyReplacer + AutomaticEnv、ReadInConfig + ConfigFileNotFoundError、nomarr + AutomaticEnv、ReadInConfig + ConfigFileNotFoundError、Mmarr n OnConfigChange 用於熱重載、viper.New() 用於測試隔離和遠端 KV 整合。使用或採用 spf13/viper 或程式碼庫匯入 `github.com/spf13/viper` 時適用。有關 viper 的 CLI 指令結構，請參閱「samber/cc-skills-golang@golang-spf13-cobra」技能。對於一般 CLI 架構，請參閱“samber/cc-skills-golang@golang-cli”。
