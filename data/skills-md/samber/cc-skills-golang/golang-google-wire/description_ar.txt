تجميع حقن التبعية في Golang باستخدام google/wire — wire.NewSet، wire.Build، wire.Bind (interface→concrete)، wire.Struct، wire.Value، wire.InterfaceValue، wire.FieldsOf، وظائف التنظيف، //go:build wireinject injector files، وإنشاء wire_gen.go. يتم تطبيقه عند استخدام google/wire أو اعتماده، أو عندما تستورد قاعدة التعليمات البرمجية `github.com/google/wire`، أو عند توصيل رسم بياني للتطبيق في وقت الترجمة عبر `wire.Build`. للتعرف على DI في وقت التشغيل مع الانعكاس، راجع مهارة `samber/cc-skills-golang@golang-uber-dig`.
