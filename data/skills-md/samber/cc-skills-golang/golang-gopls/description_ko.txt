공식 Go 언어 서버인 `gopls`를 통한 Golang 의미 코드 인텔리전스 - 정의로 이동, 참조 찾기, 호출/구현 계층 구조, 작업 공간 기호 검색, 패키지 API 검색, 진단, 안전한 이름 바꾸기, 리팩터링(추출/인라인/채우기/코드 작업 재작성), 형식 지정 및 생성된 테스트. gopls의 자체 MCP 서버(`go_*` 도구), Claude Code의 기본 `LSP` 도구 또는 `gopls` CLI를 통해 에이전트에 연결합니다. Go 코드 탐색 또는 리팩토링 시 사용합니다. 정의로 이동, 이름 바꾸기 전 호출 사이트 찾기, 파일 또는 패키지의 종속성 이해, 편집 후 진단 실행, 추출/인라인화/이름 바꾸기 등이 가능합니다. 게시된 생태계에는 해당되지 않습니다. `go.mod`에 없는 패키지, 버전, 라이선스, 가져오기 도구 - → `samber/cc-skills-golang@golang-pkg-go-dev` 스킬(`godig`)을 참조하세요. 전체 트리 취약점 감사가 아닌 경우 → `samber/cc-skills-golang@golang-security` 스킬(`govulncheck`)을 참조하세요.
