Riferimento alle best practice per ScraperAPI Python SDK (pacchetto scraperapi-sdk). Consulta ogni volta che l'utente scrive, esegue il debug o rivede il codice Python che chiama ScraperAPI. Da utilizzare quando l'utente chiede: "scrape a website with Python and ScraperAPI", "how do I use ScraperAPIClient", "ScraperAPI Python render example", "Python ScraperAPI async", "batch scraping Python ScraperAPI", "ScraperAPI premium Python", "come gestire gli errori ScraperAPI in Python", "ScraperAPI dati strutturati Python". Copre la configurazione del client, tutti i parametri della richiesta, la scala di escalation (standard → rendering → premium → ultra_premium), lavori asincroni per batch, endpoint di dati strutturati, gestione degli errori e budget del credito.
