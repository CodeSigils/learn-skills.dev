Notify the user through Notifai when work finishes, blocks, or needs their attention. Use for sending native notifications from agents via the notifai CLI, setting Notifai up when it is missing or not ready, and configuring when/how a user wants to be notified (per project or globally).
