Before /clear, generate a minimal recoverable snapshot to tmp/eo/handoff/<topic>.md so that the next session can load that single file and continue from the current node. Prioritize recording task status, key metrics, the next fork, and actively discard the exploration process. Triggers: handoff / save progress / I want clear / /eo-handoff.  
NOT FOR: mechanically compressing the dialogue flow (use the built‑in /compact).
