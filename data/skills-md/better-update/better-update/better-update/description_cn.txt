驱动 better-update CLI（@better-update/cli，“better-update”命令）来发布 OTA JS/资产更新，运行本地原生 iOS/Android 构建，管理端到端加密的签名凭证库，设置服务器端环境变量，并通过分阶段推出和即时回滚的通道/分支路由版本 - 对于 Expo、React Native、Kotlin 多平台和本机应用程序（通过自定义构建命令的任何项目类型）。每当您工作的项目使用 better-update（`@better-update/cli` 依赖项、`better-update …` 命令、app.json 中的 `expo.extra.betterUpdate.projectId` 或 `updates.jmango360.dev` 清单 URL）时，或者当用户要求发布/发布 OTA 更新、分阶段部署或回滚版本、删除候选版本、在渠道之间推广更新、构建本地 IPA/APK/AAB、管理密钥库/分发证书/配置文件/APN 推送密钥，或配置每个环境的机密 — 即使他们从未说过“更好更新”这个词。涵盖每个命令、每个标志以及发布 → 分支 → 通道 → 设备路由模型。
