驅動 better-update CLI（@better-update/cli，“better-update”命令）來發布 OTA JS/資產更新，運行本地原生 iOS/Android 構建，管理端對端加密的簽名憑證庫，設置服務器端環境變量，並通過分階段推出和即時回滾的通道/分支路由版本 - 跨版本路由版本 - 跨版本平台的版本。每當您工作的專案使用 better-update（`@better-update/cli` 依賴項、`better-update …` 指令、app.json 中的 `expo.extra.betterUpdate.projectId` 或 `updates.jmango360.dev` 使用者清單時，或發佈更新、分階段部署或回溯版本、刪除候選版本、在頻道之間推廣更新、建置本機 IPA/APK/AAB、管理金鑰庫/分發憑證/設定檔/APN 推播金鑰，或設定每個環境的機密 — 即使他們從未說過「更好更新」這個字。涵蓋每個指令、每個標誌以及發布 → 分支 → 通道 → 設備路由模型。
