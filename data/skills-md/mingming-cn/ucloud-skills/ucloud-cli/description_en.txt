Use it when the user needs to perform real UCloud resource operations via the official UCloud CLI, including viewing, creating, updating, or deleting cloud hosts, networks, EIPs, load balancers, and databases. Also use this skill when the user wants to deploy a web application on UCloud or launch a service, even if the user describes a deployment task rather than explicitly specifying a cloud product.
