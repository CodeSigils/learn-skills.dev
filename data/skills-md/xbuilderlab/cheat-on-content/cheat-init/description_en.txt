cheat-on-content's first onboarding and scaffold generator. Unified process— all users go through the same five-stage closed loop; the only difference is that users who have posted videos take an extra step during init: fetch existing videos to build historical context (used for subsequent cheat‑seed to provide more relevant topics and a more accurate baseline). Trigger words: “初始化”, “init”, “首次使用”, “我是新用户”, “setup cheat-on-content”. **Must be executed in the user's first session; other sub‑skills automatically route here when .cheat-state.json does not exist.**
