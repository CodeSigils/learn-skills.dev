To all content creators who want to turn “feeling” into calibratable predictions. **Methodology is universal** — Scoring → Blind prediction → T+3d review → Evolving the rubric loop applies to any content that can be quantified (plays / reads / listens / clicks). **The rubric is the content of the loop, not the loop itself** — Currently includes a built‑in viewpoint video rubric (based on a blogger’s 25+ video fit); other formats can start with this set and bump/adjust weights. **Strongly recommend importing benchmark accounts** as an initial signal source (/cheat‑learn‑from). Trigger words: “initialize”/“score this article”/“start prediction”/“published”/“review”/“upgrade rubric”/“recommend topics”/“capture trends”/“status”/“find benchmark”/“learn from”. **The first use must run /cheat‑init first.**
