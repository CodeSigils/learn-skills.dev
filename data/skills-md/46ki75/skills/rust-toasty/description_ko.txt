Toasty Rust ORM에 대한 전문가 지침: `#[derive(toasty::Model)]` 및 `#[key]`, `#[auto]`, `#[unique]`, `#[index]`, `#[has_many]`, `#[belongs_to]`, `#[has_one]` 속성을 사용한 모델 정의 관계, 연관 사전 로드, `create!`, `update!`, `find_by_*` 및 `filter_*` 쿼리 매크로 및 빌더 배치 작업, 트랜잭션, 임베디드 유형, 지연된 필드, 스칼라 'Vec' 배열 필드, SQLite, PostgreSQL, MySQL 및 DynamoDB에 대한 드라이버별 동작. 또한 기여자를 위한 Toasty 내부 내용인 앱/db 스키마 계층 및 매핑, 쿼리 엔진 컴파일 파이프라인(AST → Simplify → Lower → Plan → Execute) 및 드라이버 특성을 다룹니다. Toasty, `toasty` 상자, `toasty::Model`, `toasty::Db`, `toasty::HasMany`, `toasty::BelongsTo`, `toasty::HasOne`, `toasty::create!` 또는 `toasty::update!` 매크로, `submodules/toasty/` 아래의 코드 또는 Rust를 언급하는 모든 질문에 대해 항상 이 기술을 호출하세요. `toasty`를 임포트하는 코드입니다.
