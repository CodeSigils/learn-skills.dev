Guida esperta per l'ORM Toasty Rust: definizione del modello con `#[derive(toasty::Model)]` e gli attributi `#[key]`, `#[auto]`, `#[unique]`, `#[index]`, `#[has_many]`, `#[belongs_to]`, `#[has_one]`; relazioni, precaricamento delle associazioni, macro di query e builder `create!`, `update!`, `find_by_*` e `filter_*`; operazioni batch, transazioni, tipi incorporati, campi differiti, campi di array scalari "Vec" e comportamento specifico del driver per SQLite, PostgreSQL, MySQL e DynamoDB. Copre anche gli aspetti interni di Toasty per i contributori: i livelli e la mappatura dello schema app/db, la pipeline di compilazione del motore di query (AST → Simplify → Lower → Plan → Execute) e le caratteristiche del driver. Richiama sempre questa abilità per qualsiasi domanda che menziona Toasty, il crate `toasty`, `toasty::Model`, `toasty::Db`, `toasty::HasMany`, `toasty::BelongsTo`, `toasty::HasOne`, le macro `toasty::create!` o `toasty::update!`, il codice in `submodules/toasty/`, o qualsiasi codice Rust che importi "toasty".
