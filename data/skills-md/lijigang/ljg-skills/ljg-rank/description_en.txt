Given a domain, find the few independent forces that truly underpin it. Reduce a dozen phenomena to the minimal set of generators—only when you can regenerate every phenomenon from them does it count.
