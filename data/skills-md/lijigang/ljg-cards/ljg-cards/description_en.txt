Generates reading cards from text input. Default mode renders a single long PNG (1080px wide, height auto). Use -m flag for multi-card mode (1080x1440 per card). Output saved to ~/Downloads/. Use when user says "做成卡片", "生成阅读卡", "reading card", "卡片", or provides text and asks for visual cards.
