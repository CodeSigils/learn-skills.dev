Use when the user asks what work is ready or blocked, why a project is blocked, what a project unlocks, how tracked projects depend on each other, requests a dependency path or graph audit/export, or wants to add/remove a typed project relationship. Builds a deterministic project graph over the existing todo loop; never guesses dependencies from prose.
