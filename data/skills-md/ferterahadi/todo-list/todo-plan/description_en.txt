Use when the user invokes /todo-plan, says "plan this project", or names a project and wants a plan created. Runs discovery, verifies the target repo exists locally, then writes plan.md and tasks.md.
