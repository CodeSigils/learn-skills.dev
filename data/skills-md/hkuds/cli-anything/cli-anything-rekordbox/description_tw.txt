Pioneer Rekordbox 6/7 的命令列介面 - 透過受保護的 SQLCipher master.db 存取 (pyrekordbox) + 虛擬 MIDI 映射進行 DJ 庫和現場甲板控制。提供庫檢查、播放清單建立/新增/清除以及現場混音（播放/同步/交叉淡入淡出/EQ）。 Pioneer 不提供播放 REST API；該工具將僅有的兩個真實表面（加密的 DB + MIDI）組合到一個代理本機 CLI 中。
