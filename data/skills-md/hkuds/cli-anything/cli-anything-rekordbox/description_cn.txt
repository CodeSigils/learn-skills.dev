Pioneer Rekordbox 6/7 的命令行界面 - 通过受保护的 SQLCipher master.db 访问 (pyrekordbox) + 虚拟 MIDI 映射进行 DJ 库和现场甲板控制。提供库检查、播放列表创建/添加/清除以及现场混音（播放/同步/交叉淡入淡出/EQ）。 Pioneer 不提供播放 REST API；该工具将仅有的两个真实表面（加密的 DB + MIDI）组合到一个代理本机 CLI 中。
