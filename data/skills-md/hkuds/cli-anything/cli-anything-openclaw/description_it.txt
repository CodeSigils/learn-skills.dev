Da utilizzare quando l'agente desidera definire, elencare, ispezionare o eseguire macro della GUI tramite la CLI di OpenClaw Macro System. Le macro sono flussi di lavoro parametrizzati e richiamabili dalla CLI: l'agente richiama `macro run <name>` e il sistema gestisce il routing del backend (plugin, trasformazione dei file, accessibilità, riproduzione della GUI compilata).
