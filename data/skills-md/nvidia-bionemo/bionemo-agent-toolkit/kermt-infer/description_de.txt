Führen Sie Vorhersagen mit einem fein abgestimmten KERMT-Kontrollpunkt auf einer reinen SMILES-CSV-Datei aus. Der Skill validiert, dass der Eingabe-Cckpt Task-FFN-Köpfe hat (verweigert Pretrain-Cckpts mit einer Weiterleitung zu Kermt-Finetune), validiert die CSV, bereitet die Daten vor (Clean + rdkit_2d-Funktionen) und startet dann main.py Predict im Kermt-Container (Blockierung, Minutenskala).
