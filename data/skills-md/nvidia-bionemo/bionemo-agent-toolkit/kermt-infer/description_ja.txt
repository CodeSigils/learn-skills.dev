SMILES 専用 CSV 上で微調整された KERMT チェックポイントを使用して予測を実行します。このスキルは、入力 ckpt にタスク FFN ヘッドがあることを検証し (kermt-finetune へのリダイレクトで事前トレーニング ckpt を拒否します)、CSV を検証し、データを準備して (クリーン + rdkit_2d 機能)、kermt コンテナー内で main.py 予測を起動します (ブロッキング、分スケール)。
