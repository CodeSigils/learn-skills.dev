Exécutez des prédictions avec un point de contrôle KERMT affiné sur un CSV SMILES uniquement. La compétence valide que le ckpt d'entrée a des têtes de tâche FFN (refuse les ckpts de pré-entraînement avec une redirection vers kermt-finetune), valide le CSV, prépare les données (fonctionnalités clean + rdkit_2d), puis lance main.py predict à l'intérieur du conteneur kermt (blocage, échelle des minutes).
