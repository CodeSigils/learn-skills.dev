Continua il pre-allenamento da un checkpoint KERMT esistente. La competenza convalida il checkpoint dell'utente e il CSV di pretraining, prepara i dati nel modulo shard/vocab/features, quindi avvia pretrain_ddp.py all'interno del contenitore kermt (separato per lunghe tirature). Invia automaticamente `--pretrain_mode` in base al tipo di checkpoint (grover_base vocab-only, cmim o ibrido).
