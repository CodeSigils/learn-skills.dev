Configurazione iniziale, configurazione dell'ambiente e installazione del peso del modello per Proteina-Complexa. Ottieni questa abilità ogni volta che l'utente dice "imposta complexa", "installa complexa", "configura il mio .env", "prima configurazione", "quali modelli ho installato", "cosa c'è nel mio .env", "scarica i pesi del modello", "scarica Complexa / AF2 / RF3 / ProteinMPNN / LigandMPNN / ESM2 / ESMFold checkpoint", "preflight della mia GPU", "verifica ambiente", "complexa init", "complexa download", "complexa download --status", "complexa validate env" o ogni volta che è necessario rendere eseguibile un nuovo checkout. Questa è la prima abilità da eseguire su un nuovo clone: ​​gestisce `complexa init`, `complexa download` e `complexa validate env` end-to-end, modifica le chiavi `.env` richieste, sceglie il runtime corretto (UV vs Docker) ed emette un artefatto di configurazione rigiocabile.
