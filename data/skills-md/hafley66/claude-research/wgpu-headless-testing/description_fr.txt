Exécution de wgpu/egui dans CI sans GPU à l'aide du backend du logiciel Mesa llvmpipe, affichage virtuel xvfb, rendu sans tête en texture, régression visuelle avec instantanés insta. Déclencheur sur GPU sans tête, moteur de rendu logiciel, tests GPU CI, rouille de régression visuelle, wgpu CI, egui headless, llvmpipe.
