This skill is used to write unit tests for EGG applications. It covers HTTP interface testing, Service/DI object testing, mock data simulation, BackgroundTask and EventBus testing. It uses APIs such as @eggjs/mock, app.httpRequest(), app.getEggObject(), mm(), etc.
