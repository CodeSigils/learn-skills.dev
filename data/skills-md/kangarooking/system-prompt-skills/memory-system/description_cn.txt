当你需要为AI设计记忆存储、检索、应用和更新机制时，请调用此技能。典型场景包括：设计持久性内存架构（用户偏好、历史背景、项目知识）、定义内存的创建/读取/更新/删除生命周期、实现静默内存使用（不在回复中透露内存内容）以及管理敏感内存的边界。  

不适用于：定义工具接口（工具规范）、定义安全规则（安全护栏）、定义个性风格（个性系统）。  

关键触发信号：人工智能需要记住跨会话的用户信息，记忆内容可能很敏感，记忆需要在回复中隐式应用而不是显式引用，用户请求“记住这一点”。
