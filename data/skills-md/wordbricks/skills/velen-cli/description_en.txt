Use when the user wants to inspect company or customer data that lives behind Velen, run ad hoc read-only SQL against a Velen-connected source, verify or extend a known Velen insight by public ID, or manage org-scoped Knowledge Graph memory through Velen. Do not use for local databases, direct credentials, or SQL work that bypasses Velen access controls.
