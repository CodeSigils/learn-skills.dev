Transcribe voz a texto utilizando la API de voz a texto de OpenRouter. Úselo cuando el usuario solicita transcribir audio, convertir voz a texto, extraer una transcripción de una grabación o reunión, subtitular el audio de un video o menciona STT, voz a texto, ASR o transcripción.
