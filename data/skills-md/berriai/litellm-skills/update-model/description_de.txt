Aktualisieren Sie ein vorhandenes Modell auf einem Live-LiteLLM-Proxy. Fragen Sie nach der model_id und was geändert werden soll (API-Schlüssel, Basis-URL usw.) und rufen Sie dann POST /model/update auf.
