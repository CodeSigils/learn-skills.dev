Реализует утвержденную спецификацию. Проверяет, что состояние означает «Утверждено» (на любом языке), создает ветку git с именем в честь спецификации, переключается на нее и запускает реализацию шаг за шагом с паузами для просмотра различий.
