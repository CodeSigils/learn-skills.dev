الاستعلام عن تقدم تنفيذ CawPlan QA Insights A3 TestRail وتلخيصه لإصدار أو تشغيل: معدل التنفيذ، وجودة التمرير، وقائمة انتظار الفشل/الحظر المكررة، وجداول التقدم لكل تذكرة.
يُستخدم عندما: تقدم تنفيذ A3، ملخص تنفيذ الاختبار، تقدم تشغيل TestRail، ملخص التنفيذ، قائمة الفشل، معدل النجاح، عدد لم يتم اختباره، عدد الحالات، الحالة المخصصة، إصدار A3، إصدار TestRail تشغيل، تشغيل، تشغيل، تشغيل، تشغيل، تشغيل، تشغيل، تشغيل الحالة.
ليس من أجل: استيراد حالات الاختبار، أو إنشاء خطط/عمليات TestRail، أو تسجيل العيوب (استخدم `cawplan-defect-ticket`)، أو إصدار تقييم المخاطر.
