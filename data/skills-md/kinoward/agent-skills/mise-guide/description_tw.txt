必須用於與 mise、開發工具版本或開發環境設定相關的任何內容。觸發器：(1) 使用者提及 mise、mise.toml、.tool-versions 或 mise 指令，例如「mise use」、「mise install」、「mise run」。 (2) 使用者想要在專案或全域層級安裝、切換、固定、升級或檢查開發工具的版本（node、python、go、ruby、java、rust 等），即使不提及 mise（例如「設定節點 22」、「python 版本是什麼」、「升級 go」、「檢查過時的工具」、「設定開發環境」）。 (3) 使用者希望透過設定檔管理每個項目的環境變數（例如「新增 DATABASE_URL env var」、「為不同環境設定 env var」）。 (4) 使用者想要透過 mise 定義或執行專案任務（例如「建立建置任務」、「使用 mise 執行測試」）。請勿觸發：Dockerfile、package.json 腳本、Makefile、nvm/pyenv/rbenv 指令、pip/npm 套件安裝、git 標籤、CI/CD 設定或部署。
