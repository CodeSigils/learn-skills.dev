Task analysis that writes findings under a scoped workspace, supports explicit promotion to a /goal, and evaluates named code-review Fix-now blockers before remediation. No ticket write by default. Use for a task, idea, or review backlog; /write-ticket and /code-review may call it.
