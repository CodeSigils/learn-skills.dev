Standalone only — never in a flow, never autonomous. Writes durable behavior-lock tests for complex architectural parts (hooks, domain/business logic, facades, stateful classes). Locks deeply thought-through behavior so outside edits cannot silently break it. Use only when the user runs /create-test (only after /code-review or /pr-review recommends a lock). Not for coverage theater, trivial units, or /goal.
