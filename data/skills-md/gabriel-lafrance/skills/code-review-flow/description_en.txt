Goal-scoped three-axis review (Standards + Spec + Routes) of work produced under .agents/temp/goals/<goal-id>/ (plans, GOAL Done when, file lane). Looked up by /goal after validate-flow. Not for auto-invocation — use /code-review for branch/main diffs the user asked for.
