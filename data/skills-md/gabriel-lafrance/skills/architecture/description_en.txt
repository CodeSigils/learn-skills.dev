Shape scalable code: domain services with public APIs, strong primitives inside deep modules, simple entry points, clear folders, write-path data design. Dual skill: agent picks standalone vs flow via variants.md. Use when adding features, splitting logic, designing data/reads, or about to dump files / duplicate domain logic. User must invoke.
