Écrivez un nouveau test Playwright E2E à partir des étapes de test fournies par l'utilisateur lorsque le code source de l'application n'est PAS accessible. Utilise Playwright CLI (`@playwright/cli`) pour inspecter une URL en direct via des instantanés et `generate-locator`, puis écrit le test, l'exécute et le débogue si nécessaire.
