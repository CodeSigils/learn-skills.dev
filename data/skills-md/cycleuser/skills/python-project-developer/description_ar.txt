استكمال مواصفات تطوير مشاريع Python المتعددة لأدوات CLI/GUI.

يتم التشغيل عند: إنشاء مشروع Python جديد بدعم CLI وGUI، أو إعداد pyproject.toml مع نشر README وPyPI، أو تنفيذ واجهة برمجة التطبيقات الموحدة مع نمط ToolResult، أو إضافة تكامل أدوات استدعاء وظائف OpenAI، أو كتابة اختبارات ووثائق موحدة.

القدرات: توجيه هيكل المشروع من ملف واحد إلى ترحيل الحزمة، وعلامات CLI الموحدة (-V، -v، -o، --json، -q)، واجهة برمجة تطبيقات Python مع نمط فئة البيانات ToolResult، استدعاء الوظائف باستخدام TOOLS + نمط الإرسال، وثائق README ثنائية اللغة مع لقطات شاشة تلقائية، واختبار pytest باستخدام بنية اختبار موحدة.
