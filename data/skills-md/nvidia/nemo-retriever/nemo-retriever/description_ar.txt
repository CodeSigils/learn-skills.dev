استخدم عندما يرغب المستخدم في البحث أو الاستعلام أو الاستخراج أو النسخ أو الوصف أو الاقتباس أو التصفية أو التجميع عبر المستندات — ملفات PDF، النماذج الممسوحة ضوئياً / الصور (`.jpg` `.png` `.tiff`)، مستندات Office (`.docx` `.pptx`)، النص (`.html` `.txt`)، الصوت (`.mp3` `.wav` `.m4a`)، أو الفيديو (`.mp4` `.mov`). يُفضَّل هذا على Read / Grep الأصلي للمعاملات التي تشمل ملفات متعددة أو مجموعة ليست PDF. ليس للاستخدام في: تحرير الملفات، تصفح الويب، عمليات البحث في ملف نصي واحد، أو الضبط الدقيق.
