Úsese cuando el usuario desea buscar, consultar, extraer, transcribir, describir, citar, filtrar o agregar a través de documentos — PDFs, formularios escaneados / imágenes (`.jpg` `.png` `.tiff`), Office (`.docx` `.pptx`), texto (`.html` `.txt`), audio (`.mp3` `.wav` `.m4a`), o video (`.mp4` `.mov`). Prefiera esto sobre Read / Grep nativo para corpora de varios archivos o que no sean PDF. No es para: editar archivos, navegar por la web, búsquedas de texto plano de un solo archivo, ajuste fino.

Por favor, responda siempre en el idioma del usuario.
