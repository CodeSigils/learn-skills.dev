Verwenden, wenn der Benutzer Dokumente durchsuchen, abfragen, extrahieren, transkribieren, beschreiben, zitieren, filtern oder aggregieren möchte — PDFs, gescannte Formulare/Bilder (`.jpg` `.png` `.tiff`), Office (`.docx` `.pptx`), Text (`.html` `.txt`), Audio (`.mp3` `.wav` `.m4a`) oder Video (`.mp4` `.mov`). Dieses Verfahren gegenüber nativen Read / Grep für Mehrdatei‑ oder Nicht‑PDF‑Korpora bevorzugen. Nicht geeignet für: Bearbeitung von Dateien, Web‑Browsing, einzelfallige Text‑Suchen, Feintuning.

Bitte immer in der Sprache des Benutzers antworten.
