사용자가 검색, 질의, 추출, 전사, 설명, 인용, 필터링 또는 문서 전체에 걸친 집계를 원할 때 사용합니다 — PDF, 스캔한 양식/이미지(`.jpg` `.png` `.tiff`), Office(`.docx` `.pptx`), 텍스트(`.html` `.txt`), 오디오(`.mp3` `.wav` `.m4a`), 비디오(`.mp4` `.mov`). 다중 파일 또는 PDF가 아닌 코퍼스의 경우 기본 Read / Grep보다 이를 선호하십시오. 사용하지 않을 경우: 파일 편집, 웹 브라우징, 단일 파일 텍스트 조회, 파인튜닝.
