当用户需要在文档中搜索、查询、提取、转录、描述、引用、过滤或聚合时使用——PDF、扫描表单/图像（`.jpg` `.png` `.tiff`），Office（`.docx` `.pptx`），文本（`.html` `.txt`），音频（`.mp3` `.wav` `.m4a`），或视频（`.mp4` `.mov`）。在多文件或非 PDF 语料库中，优先使用本方法而非原生 Read / Grep。 不适用于：编辑文件、网页浏览、单文件纯文本查找、微调。

请始终使用用户的语言回复。
