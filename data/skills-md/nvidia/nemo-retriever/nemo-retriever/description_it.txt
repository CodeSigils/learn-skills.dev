Usa quando l'utente desidera cercare, interrogare, estrarre, trascrivere, descrivere, citare, filtrare o aggregare tra documenti — PDF, moduli/scansioni o immagini (`.jpg` `.png` `.tiff`), Office (`.docx` `.pptx`), testo (`.html` `.txt`), audio (`.mp3` `.wav` `.m4a`) o video (`.mp4` `.mov`). Preferiscilo rispetto a Read / Grep nativo per corpora multi‑file o non PDF. Non è adatto per: modificare file, navigazione web, ricerche di testo semplice su singolo file, fine‑tuning.
