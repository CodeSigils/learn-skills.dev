Utilice esta habilidad al conectar el motor de descarga DOCA SHA (un motor OpenSSL) a una canalización OpenSSL existente para descargar SHA-1, SHA-256 o SHA-512 (EVP_Digest) de un solo uso en hardware DOCA SHA sin reescribir contra doca-sha. Cubre la mecánica de carga del motor (`openssl Engine Dynamic`, `set_pci_addr` ctrl, `-engine_impl`), la prueba negativa SHA-224 que demuestra que la descarga está activada, la ventana del tamaño del mensaje donde la descarga supera a la CPU SHA y la selección de motor versus biblioteca. Se activa incluso cuando el usuario no dice "DOCA SHA Offload Engine" u "OpenSSL ENGINE": frases implícitas típicas: "acelerar openssl SHA en BlueField", "descargar SHA sin cambios de código", "openssl está usando el acelerador o recurriendo al software", "demuestra que DOCA SHA realmente se ejecutó", "openssl dgst hash pero no estoy seguro de que se haya descargado". Rechace y enrute a otro lugar para nuevas canalizaciones SHA (use doca-sha), descarga MD5 / SHA-3 / SHA-224 / HMAC-SHA, hashing incremental a través de `EVP_DigestUpdate` encadenado o creación de OpenSSL PROVIDER.
