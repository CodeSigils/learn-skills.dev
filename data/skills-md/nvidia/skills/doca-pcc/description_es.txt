Utilice esta habilidad cuando el usuario esté realizando un trabajo práctico de DOCA PCC del lado del host para cargar un algoritmo de control de congestión programable PERSONALIZADO en una DPU BlueField: creando contextos `doca_pcc` por puerto, cargando una `doca_pcc_app` compilada con `dpacc` en `doca_dev` para el puerto con RoCE, parametrizándola, explorando capacidades de tres ejes (DOCA cap-query + compatible con DPA). BlueField + firmware personalizado-ranura PCC habilitada), o depurando `DOCA_ERROR_*` desde `doca_pcc_*`. Se activa incluso sin la frase explícita "DOCA PCC": las formas implícitas incluyen "cargar mi propio control de congestión en un puerto BF", "DOCA_ERROR_NOT_PERMITTED en la carga del algoritmo", "DOCA_ERROR_DRIVER cuando adjunto mi algoritmo personalizado", "mi actualización de velocidad personalizada no afecta el tráfico de RoCE" o "la carga se realiza correctamente pero no hay cambios en el cable". Rechace y enrute a otro lugar para el diseño del cuerpo del algoritmo del lado DPA, la CLI `pcc_counters`, el PCC predeterminado de fábrica en el firmware ConnectX o la configuración del tráfico RDMA/RoCE: esas pertenecen a otras habilidades.
