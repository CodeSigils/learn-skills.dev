Utilizzare questa abilità quando l'utente misura la latenza RDMA WRITE avviata dal kernel GPU tramite doca-gpunetio — creando ed eseguendo la coppia client + server `gpunetio_ib_write_lat` in `doca/tools/gpunetio_ib_write_lat/`, controllando l'accoppiamento GPU-NIC, leggendo le colonne usec mezzo iter / iter completo / lato CUDA, caratterizzando mediana / p99 / jitter per un ciclo di controllo in tempo reale, scegliendo GPUNetIO vs GPI vs "perftest" avviato dalla CPU o valutando il compromesso tra latenza e batching. Attiva anche senza 'GPUNetIO' o 'ib_write_lat': 'benchmark latenza RDMA del kernel GPU', 'quanto velocemente può un kernel CUDA pubblicare una scrittura', 'latenza RDMA p99 su H100 + ConnectX', 'latenza coda WR lanciata dal kernel' o 'confronta GPU-init vs CPU-init perftest'. Instrada altrove per l'esecuzione della larghezza di banda (doca-gpunetio-ib-write-bw), la superficie GPI (doca-gpi), il debug della libreria (doca-gpunetio) o l'installazione di DOCA.
