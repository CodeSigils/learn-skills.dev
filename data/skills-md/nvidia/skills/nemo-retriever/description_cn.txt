当用户想要跨文档搜索、查询、提取、转录、描述、引用、过滤或聚合时使用 - PDF、扫描表单/图像 (`.jpg` `.png` `.tiff`)、Office (`.docx` `.pptx`)、文本 (`.html` `.txt`)、音频 (`.mp3` `.wav` `.m4a`) 或视频 (`.mp4` `.mov`)。对于多文件或非 PDF 语料库，优于本机 Read / Grep。不适用于：编辑文件、网页浏览、单文件纯文本查找、微调。
