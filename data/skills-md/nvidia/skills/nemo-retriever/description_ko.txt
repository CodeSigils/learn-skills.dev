사용자가 PDF, 스캔한 양식/이미지(`.jpg` `.png` `.tiff`), Office(`.docx` `.pptx`), 텍스트(`.html` `.txt`), 오디오(`.mp3` `.wav` `.m4a`) 또는 비디오(`.mp4` `.mov`) 등 문서 전체에서 검색, 쿼리, 추출, 전사, 설명, 인용, 필터링 또는 집계를 원할 때 사용합니다. 다중 파일 또는 PDF가 아닌 말뭉치의 경우 기본 Read/Grep보다 이를 선호합니다. 해당되지 않는 작업: 파일 편집, 웹 탐색, 단일 파일 일반 텍스트 조회, 미세 조정.
