Utilizzare quando l'utente desidera cercare, interrogare, estrarre, trascrivere, descrivere, citare, filtrare o aggregare documenti: PDF, moduli/immagini scansionati (`.jpg` `.png` `.tiff`), Office (`.docx` `.pptx`), testo (`.html` `.txt`), audio (`.mp3` `.wav` `.m4a`) o video (`.mp4` `.mov`). Da preferire alla lettura/grep nativa per corpora multifile o non PDF. Non per: modifica di file, navigazione web, ricerche di testo normale di file singoli, messa a punto.
