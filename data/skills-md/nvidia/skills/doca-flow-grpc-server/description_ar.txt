PLAINTEXT-ONLY: يستخدم خادم `doca_flow_grpc` الذي تم شحنه `grpc::InsecureServerCredentials()` مع عدم وجود مقبض TLS / mTLS / token-auth على الثنائي - يجب أن يأتي أمان النقل من بنية تحتية خارجية (على سبيل المثال، وكيل mTLS / عربة جانبية) على مقطع موثوق به. استخدم هذه المهارة عند إظهار أو تكوين أو تقوية أو تصحيح أخطاء `doca_flow_grpc` - سطح التحكم عن بعد لـ gRPC المزوّد بـ DOCA أمام `doca-flow` الذي يتيح للعملاء غير C++ (Python وGo وRust وJava) برمجة أنابيب التدفق والإدخالات عبر RPC بدلاً من ربط `libdoca_flow.so` مباشرةً. يتم التشغيل حتى عندما لا يقول المستخدم "doca-flow-grpc-server" أو "gRPC" - على سبيل المثال. "قواعد تدفق البرنامج من Python على مضيف آخر"، "تكوين الأنابيب عن بعد على BlueField"، "انتهت مهلة العميل للاتصال بخادم التدفق"، "أين هو .proto للتدفق"، "غير مصادق عليه / FAILED_PRECONDITION على Flow RPC". قم بالتوجيه إلى مكان آخر لواجهة برمجة تطبيقات doca-flow الأساسية أو أدوات gRPC العامة (البروتوكول أو روابط اللغة) أو تثبيت DOCA/إظهار BFB.
