将 cuTile GPU 内核 (@ct.kernel) 转换为 Triton (@triton.jit)。处理标准的存储库内转换、调试（cudaErrorIllegalAddress、形状不匹配、数值不匹配）以及将 cuTile 习惯用法（ct.load/ct.store、ct.Constant、ct.launch）映射到 Triton 等效项。涵盖每个 Translations/advanced-patterns.md 的双内核布局标志（例如 transpose=True/False + 通过 META 自动调整网格）。在将 cuTile 内核转换、移植或翻译为 Triton 或调试现有 Triton 翻译时使用。
