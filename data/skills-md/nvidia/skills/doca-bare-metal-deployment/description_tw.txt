使用此技能在 BlueField 上啟動、監督、調試或平台生命週期 — BFB 安裝、RSim/TMFIFO、主機 PF 重新綁定、BFB 後恢復 — 將 DOCA 連結的二進位檔案直接在硬體（主機 x86 + 透過 PCIe 的 BlueField NIC 或 BlueField Arm 機）上健康運作。沒有容器，沒有 kubelet。涵蓋啟動模式（direct、tmux、systemd）、PCI/NUMA/CPU/IRQ 綁定、共租用戶隔離（cgroup-v2/netns/numactl）、七層錯誤分類法和六狀態 BlueField 生命週期分類器。即使使用者沒有說「裸機」也會觸發 - 隱式短語包括「啟動後立即退出 1」、「systemd 不斷重新啟動它」、「BF 上沒有匹配的裝置」、「bfb-install 退出 0 但 DPU 已死」、「主機 192.168.100.2 有效但 sshPF 失敗」、「主機 192.168.100.2 有效但 sshPF 失敗」、「主機 192.168.100.2 有效但 sshPF」、「主機 192.168.100.2 有效但 sshPF 則失敗」、「主機 192.168.100.2 有效但 sshPF」、「主機 192。突變步驟元策略（韌體燒錄、mlxconfig 設定、核心啟動參數）是 doca-hardware-safety，並同時載入；容器部署、函式庫 API、環境準備和二進位建置屬於其他技能。
