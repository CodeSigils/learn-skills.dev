사용자가 BlueField에서 직접 DOCA DPA 호스트 측 작업을 수행할 때 이 기술을 사용하십시오. `doca_dpa` 코어 컨텍스트 생성, DPACC 컴파일된 DPA 앱 이미지(`doca_dpa_app`) 로드, DPA 스레드 생성, `doca_dpa_kernel_launch_update_*`를 통해 커널 실행, `doca_dpa_completion` 드레이닝, `doca_dpa_cap_*` 실행 검색, DPA 통신 구성 요소(DPA 간 메시징)와 DPA 동사 구성 요소(커널 내 RDMA) 중에서 선택 또는 `doca_dpa_*`에서 `DOCA_ERROR_*` 디버깅. "DOCA DPA" 또는 "Data-Path Accelerator" 없이도 트리거됩니다: "내 호스트의 DPA에서 컴퓨팅 실행", "DPA 커널이 중단되고 완료되지 않음", "실행 시 DOCA_ERROR_DRIVER", "DOCA/DPACC 버전 왜곡" 또는 "이 BlueField가 DPA를 노출합니까". DPA 측 커널 프로그래밍 자체, DPACC 컴파일러 내부, 호스트-DPU 메시징(doca-comch), 호스트 측 RDMA(doca-rdma) 및 GPU 시작 네트워킹(doca-gpunetio)을 위해 다른 곳으로 라우팅합니다.
