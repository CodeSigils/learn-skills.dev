Da utilizzare quando si aggiunge, si modifica, si ottimizza o si esegue il debug del codice di ottimizzazione automatica CuTile. Segnali di attivazione: `exhaustive_search` / `replace_hints` / `hints_fn` / `cuda.tile.tune` nel codice, `autotune` nei nomi dei file o problemi di correttezza/prestazioni nei kernel CuTile ottimizzati automaticamente. Copre: modello di ottimizzazione/cache/lancio, configurazioni per architettura (sm80–sm120), progettazione dello spazio dei parametri (dimensioni dei riquadri, occupazione, num_ctas) e 7 insidie ​​​​comuni con soluzioni.
