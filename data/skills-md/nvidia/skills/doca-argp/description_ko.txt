배송된 샘플 또는 새로운 DOCA 사용 앱에 대한 실습 DOCA Arg Parser CLI 작업에 이 기술을 사용하십시오. 플래그 추가/제거/이름 바꾸기; `doca_argp_init` → 등록 매개변수 → `doca_argp_start` → `doca_argp_destroy`를 순서대로 연결합니다. 전체 공개 열거형에서 매개변수 유형 선택(`DOCA_ARGP_TYPE_STRING`, `_INT`, `_BOOLEAN`, `_DEVICE`, `_DEVICE_REP`, `_DOUBLE` — 3개가 아닌 6개의 값) 표준 `--device` / `--representor` / `--json`(`-j`, 실제 플래그는 `--json-config`가 아니라 `--json`입니다.) / `--sdk-log-level` 표면을 유지합니다. 또는 `doca_argp_*`에서 `DOCA_ERROR_BAD_STATE` / `INVALID_VALUE` / `NOT_SUPPORTED` / `NOT_FOUND` / `IO_FAILED`를 디버깅합니다. 암시적 문구에 대해 트리거: "DOCA 샘플에 사용자 정의 플래그 추가", "여기서 getopt를 사용해야 하나요?", "새 매개변수 등록 중 BAD_STATE", "내 JSON 구성 키가 거부되었습니다." 또는 "내 샘플의 --json이 무시됩니다." variadic-flag / 하위 명령 / 쉘 완성 기능, DOCA 핵심 컨텍스트 또는 DOCA 로그 내부를 거부하고 다른 곳으로 라우팅하세요.
