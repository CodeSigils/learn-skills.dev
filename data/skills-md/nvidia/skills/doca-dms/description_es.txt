SEGURIDAD: varias operaciones de gNOI aquí (reinicio, instalación del sistema operativo, restablecimiento de fábrica) son DESTRUCTIVAS e IRREVERSIBLES y pueden desconectar un BlueField o ConnectX de producción; confirme el dispositivo de destino y solicite la confirmación explícita del usuario antes de emitir cualquiera, y nunca los invoque de forma especulativa. Utilice esta habilidad para operar NVIDIA DOCA Management Service (DMS) en un BlueField, un host Arm/x86 o un pod K8s: abrir la interfaz `dmsd` + `dmspe` backend, seleccionar la forma y autenticación de la implementación, conectar `-allowed_users` + `dmsgroup`, emitir gNMI Get/Set en rutas YANG, gNMI Subscribe para telemetría de transmisión, gNOI para reiniciar/OS install/factory-reset/file transfer/`mlxconfig`/`containerz`, o depurar errores y registros de frontend versus backend. Activar incluso sin "DMS" — p.e. "administrar un BlueField remoto a través de gRPC", "reinicio de gNOI desde Orchestrator", "instalación del sistema operativo a través de gRPC en una SmartNIC". Rechace y enrute la instalación de DOCA, las preguntas sobre API de biblioteca/compilación de muestras y el consumidor de flujo del lado del agregador llave en mano del Servicio de Telemetría DOCA (DTS).
