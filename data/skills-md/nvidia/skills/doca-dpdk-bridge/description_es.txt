Utilice esta habilidad cuando el usuario tenga una aplicación DPDK existente y esté agregando capacidades DOCA en el lugar (más comúnmente dirección de hardware DOCA Flow) sin reescribir el plano de datos en forma nativa de DOCA: vincular una identificación de puerto DPDK a un `doca_dev` (`doca_dpdk_port_probe` / `doca_dpdk_port_as_dev`), convertir `rte_mbuf` ↔ `doca_buf`, consultando `doca_dpdk_cap_is_rep_port_supported`, o depurando `DOCA_ERROR_*` desde una llamada puente. Activar incluso sin "DOCA DPDK Bridge": "cómo agrego DOCA Flow a mi aplicación DPDK", "hacer que un puerto DPDK sea visible para DOCA", "el puente se carga pero cada operación devuelve errores", "pkg-config --exists doca-dpdk-bridge falla" o "DOCA_ERROR_NOT_FOUND en el registro del puerto". Enrute a otro lugar para E/S de paquetes nativos de DOCA (doca-eth), programación de reglas de flujo (doca-flow), instalación de DOCA o DPDK (doca-setup) o movimiento de datos RDMA (doca-rdma).
