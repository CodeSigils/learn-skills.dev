Use this when the user requests to commit code, organize commits, prepare a commit, split a commit, push, or specify commit and push guidelines. By default, use Chinese commit messages and split large, disparate changes into multiple commits; before pushing, first perform fetch, stash, rebase, stash pop, then push.
