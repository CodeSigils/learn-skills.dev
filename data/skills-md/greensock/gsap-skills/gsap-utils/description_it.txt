Competenza GSAP ufficiale per gsap.utils: clamp, mapRange, normalize, interpolate, random, snap, toArray, wrap, pipe. Da utilizzare quando l'utente richiede informazioni sulle utilità gsap.utils, clamp, mapRange, random, snap, toArray, wrap o helper in GSAP.
