像素級 750px 影像到程式碼和高清切片工作流程。 Use this when Codex needs to convert the currently selected UI image, screenshot, app/web design mockup, Figma exported image, or design long image into code and output separate transparent PNG slice assets. It is suitable for strict 1:1 proportional restoration, artboard width exactly 750 px, high‑resolution enlarged slicing, anti‑blur and background removal, using image2/imagegen to extract transparent PNG when needed, editable text, simple graphics as native vectors/code, with the final code required to be responsive/adaptive, or when the finished artwork must be exported/imported to Figma as editable layers.
