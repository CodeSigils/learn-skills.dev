Pixel‑level 750px image‑to‑code and high‑resolution slicing workflow. Use this when Codex needs to convert the currently selected UI image, screenshot, App/Web design mockup, Figma export, or design long image into code and output independent transparent PNG slice assets. It is suitable for strict 1:1 proportional restoration, artboard width precisely 750px, high‑resolution upscaled slicing, anti‑blur and background removal, using image2/imagegen to extract transparent PNG when necessary, editable text, simple graphics as native vectors/code, with the final code needing responsive adaptation, or for tasks that require exporting/importing the finished work to Figma as editable layers.
