Scan all changes in the Git working directory, intelligently group them, commit in order, and display the plan for confirmation. Suitable when the user says "commit", "submit changes", or "help commit". Supports fast mode (non-lock-file changes combined into a single commit). Not suitable for scenarios requiring manual staging or push.
