Scan all changes in the git workspace, intelligently group them, commit sequentially, and display the plan for confirmation. Applicable when the user says "commit", "submit changes", or "help me commit". Supports fast mode (non-lock file changes combined into a single commit). Not applicable for scenarios requiring manual staging control or push execution.
