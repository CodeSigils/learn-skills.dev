Extract a vertical-slice task list from the PRD, generate executable tasks with acceptance criteria and size labels after deep-diving the codebase, iterate to confirm, then save locally as Markdown or push as GitHub sub-issues. Use after the PRD is done and tasks need splitting or converting to GitHub issues. Not for writing PRDs, directly implementing features, or editing/closing existing GitHub issues.
