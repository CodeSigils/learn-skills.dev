مرجع الرسوم المتحركة GSAP للإطارات الفائقة. يغطي gsap.to()، from()، fromTo()، التخفيف، الترنح، الإعدادات الافتراضية، المخططات الزمنية (gsap.timeline()، معلمة الموضع، التسميات، التداخل، التشغيل)، والأداء (التحويلات، will-change، QuickTo). يُستخدم عند كتابة رسوم GSAP المتحركة في تركيبات HyperFrames.
