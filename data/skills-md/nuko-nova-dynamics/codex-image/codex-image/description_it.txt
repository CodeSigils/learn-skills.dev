Genera o modifica immagini con gpt-image-2, addebitato sull'abbonamento ChatGPT dell'utente tramite Codex CLI OAuth: non è necessaria OPENAI_API_KEY. Da utilizzare ogni volta che l'utente desidera generare, creare, disegnare, modificare o modificare un'immagine, un'immagine, una foto, un logo, un'icona, un'illustrazione, una ripresa del prodotto, un modello dell'interfaccia utente, uno sfondo o un'infografica, inclusi PNG con sfondo trasparente/ritaglio. Attiva frasi come "fammi un'immagine di X", "disegna un logo per Y", "genera un'immagine di Z", "ora rendila più scura", "rimuovi lo sfondo" o un'esplicita invocazione di /codex-image.
