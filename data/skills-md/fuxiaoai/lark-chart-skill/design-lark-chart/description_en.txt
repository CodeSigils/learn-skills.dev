When a user requests any type such as “architecture diagram / flowchart / swimlane diagram / sequence diagram / organizational chart / state machine / funnel / Gantt chart / milestone / matrix quadrant / link architecture / hand‑drawn style architecture / complex business swimlane diagram / ER diagram / mind map” and wants it implemented in Feishu, or provides a Feishu document URL and asks to draw within it, or says “sync to Feishu / write to Feishu whiteboard,” this skill must be used. In addition to the 17 built‑in templates above, this skill also offers a “freeform drawing mode”: when a user asks to draw oil paintings / hand‑drawn illustrations / mobile app product prototypes / maps / game screens / Sankey diagrams / any reference image recreation that the templates don’t cover, or explicitly says “free drawing / free to create / don’t use templates,” the skill must likewise be used (the output remains an editable Feishu whiteboard). The generation pipeline strictly follows eight steps: Normalize → Select → Plan → Layout → Render → StaticCheck → VQA → Deliver, prohibiting hard‑coded templates, hard‑coded coordinates, and fabricating business content the user didn’t mention. All styles are read from assets/style‑tokens/ (except for code‑native diagrams and freeform drawing mode). Before delivery, it must pass the quality gate (executed according to routing) and a dual‑reviewer visual review (any rating below 9 or any blocker results in failure).
