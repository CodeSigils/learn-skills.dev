An integrated team skill that handles E2E environment setup based on Spec and ui-proto + automatic unit scenario splitting + integrated suite creation + execution + compliance validation all at once. Triggered by keywords: "e2e verify", "integration validation", "full flow test", "team e2e", "check if it works per spec", "compare with ui-proto", "final validation", "pre-release check". Shares patterns across three skills—peach-e2e-setup, peach-e2e-scenario, and peach-e2e-suite—and externalizes validation criteria to ui-proto screens + Spec business rules. Forms a single development-validation delivery flow with peach-team-dev, while keeping implementation and validation contexts separate. Runs in team mode if Claude Code team features are available, or generic mode in Codex/skills.sh environments. Key differentiator: beyond simple code behavior checks, it automatically validates alignment with product intent.
