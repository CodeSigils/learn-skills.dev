An integrated team skill that handles E2E environment setup based on Spec and ui-proto + automatic unit scenario splitting + integrated suite creation + execution + compliance validation all at once. Triggered by keywords: "e2e verify", "integration verify", "full flow test", "team e2e", "check if it works per spec", "check against ui-proto", "final validation", "pre-release verify". Shares the patterns of three skills—peach-e2e-setup + peach-e2e-scenario + peach-e2e-suite—and externalizes validation criteria as ui-proto screens + Spec business rules. Forms a single dev-validation delivery flow with peach-team-dev, but keeps implementation context and validation context separate. Runs in team mode if Claude Code team features are available, or generic mode in Codex/skills.sh environments. The key differentiator is automatic validation not just of code behavior, but of alignment with design intent.
