Leggere un file di storia e implementarlo. Carica il contesto completo (storia, requisiti GDD, linee guida ADR, manifest di controllo), indirizza all'agente programmatore corretto per il sistema e il motore, implementa il codice e il test e conferma ogni criterio di accettazione. La capacità di implementazione principale: eseguire dopo /story-readiness, prima di /code-review e /story-done.
