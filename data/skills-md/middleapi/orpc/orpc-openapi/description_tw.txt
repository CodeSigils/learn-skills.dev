將 oRPC 路由器公開為符合規範的 OpenAPI HTTP API。當專案依賴 @orpc/openapi 時使用，或在 oRPC 過程上定義 REST 風格的路由（openapi() 元資料或具有方法、路徑、successStatus 的 .route），使用 OpenAPIHandler 與 RPCHandler 一起提供服務，使用智慧強制強制查詢和路徑字串，使用 OpenAPILink 號碼 OpenAPI 資料，使用 OpenAPI 型號，使用智能強制要求 3.插件提供 Scalar 或 Swagger 文件。偏向從 oRPC 文件中檢索而不是預先訓練的知識。對於契約優先的工作（使用 oc 定義契約，使用 Implement 實現它們，或從現有 OpenAPI 規範生成契約），請使用 orpc-contract 技能；對於沒有 REST 暴露的普通 RPC 服務、核心構建器、中間件或客戶端工作，請改用 orpc 技能。
