将现有代码库迁移到当前的 oRPC，涵盖 tRPC 到 oRPC（通过 @orpc/trpc 集成进行增量包装或使用概念映射进行完全重写）和 oRPC v1 到 v2（包重命名、重大更改和安全操作顺序）。当要求从 tRPC 迁移到 oRPC、转换或包装 tRPC 路由器、将 oRPC v1 升级到 v2、修复 oRPC v2 重大更改或将“@trpc/*”包交换为“@orpc/*”等效项时使用。偏向于从 oRPC 文档中检索而不是预先训练的知识。不适用于新的 oRPC 工作或已迁移代码库中的新功能：使用 orpc 技能来完成这些工作。
