Rules and patterns for per-side padding settings — linked/unlinked toggle, 2×2 grid for individual controls, migration from a legacy single-value field, and s()-scaled CSS output. Use when adding padding controls to a component.
