為專案引導一個 nao 代理 - 在一輪中收集倉庫 + 範圍 + 額外上下文信息，從 nao 文件中查找特定於倉庫的配置，編寫 nao_config.yaml，運行 nao init + naosync，設置 LLM 密鑰，並生成第一個 RULES.md。當用戶剛決定在新項目上使用 nao 時使用。僅適用於首次設定；若要編輯規則、產生測試或查看現有上下文，請使用 write-context-rules / create-context-tests /audit-context。
