hyperresearch V8 管道的第 10 步。编排器为每个草稿预先挑选 20‑50 个特定角度的源 ID，然后并行生成 3 个 hyperresearch‑draft‑orchestrator 子代理——每个子代理读取其挑选的列表（不进行金库调研，不获取源），并写入一个特定角度的草稿。所有 3 份草稿写完并通过验证后，此步骤结束。第 11 步（合成器）负责合成写入，生成最终报告。对于轻量级方案：直接将单个草稿写入 final_report.md，并跳到第 15 步（润色）。通过 Skill 工具调用。
