Cypher および Neo4j スタイルのクエリを HelixDB Rust DSL クエリに変換します。入力に ​​Cypher、Neo4j、MATCH、OPTIONAL MATCH、WHERE、RETURN、ORDER BY、LIMIT、DISTINCT、MERGE、CASE、UNWIND、FOREACH、DETACH DELETE、IS NULL、または可変長パス パターンが含まれており、目的が同等の Helix Rust クエリを生成する場合に使用します。
