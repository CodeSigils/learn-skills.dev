驅動 HelixDB `helix` CLI 來執行、查詢和部署 Helix 實例。當任務是建置專案（helix init / Chef / add）、管理本機 Docker/Podman 實例（helix 啟動、停止、重新啟動、狀態、日誌、修剪）、向正在執行的實例傳送動態查詢（使用 --file / --json / -e TypeScript DSL / --ts-file，針對 POST / Helv1/query 的操作或在操作）查詢，針對 POST / Helv auth、push、sync、workspace、project、cluster）時使用。涵蓋 helix.toml、本地與雲端工作流程和 v3 心理模型（無螺旋編譯/螺旋檢查/.hx 檔案）。對於編寫查詢體本身，請遵循 helix-query-* 技能。有關完整命令目錄，請參閱 REFERENCE.md；有關端對端演練，請參閱 Examples.md。
