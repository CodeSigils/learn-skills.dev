Genera un sistema di brand design completo e accessibile da un brief — primitivo → semantico → token DTCG componenti (colore, tipo, spaziatura, raggio, ombra, movimento), chiaro + scuro, più un singolo tema.css — verificato per WCAG. Da utilizzare quando l'utente desidera una base di brand/design creata da zero, una nuova tavolozza + un sistema di tipi o un kit di token con tema per un prodotto.
