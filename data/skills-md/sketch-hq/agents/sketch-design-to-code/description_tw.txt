使用 Sketch MCP 从 Sketch 设计生成具有 1:1 视觉保真度的代码（`get_document_info`、`get_layer_tree_summary`、`get_screenshot`、`get_symbol_overrides`、`get_design_assets`、目标 `run_code`）。首先检查现有项目代码和约定，将符号和覆盖映射到可重用组件，导出资产，并根据 Sketch 屏幕截图验证实现（Web：运行应用程序与“get_screenshot”）。在 React、Tailwind、SwiftUI、Kotlin 或 Sketch 文件、頁面、框架或層的其他技術中實作或更新 UI 時使用。
