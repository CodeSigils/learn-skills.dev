使用 Sketch MCP 從 Sketch 設計產生具有 1:1 視覺保真度的程式碼（`get_document_info`、`get_layer_tree_summary`、`get_screenshot`、`get_symbol_overrides`、`get_design_assets`、目標 `run_code`）。首先檢查現有項目代碼和約定，將符號和覆蓋映射到可重複使用元件，匯出資產，並根據 Sketch 螢幕截圖驗證實作（Web：運行應用程式與「get_screenshot」）。在 React、Tailwind、SwiftUI、Kotlin 或 Sketch 文件、頁面、框架或層的其他技術中實作或更新 UI 時使用。
