Generate design tokens (theme colors, spacing, typography, components) from any input — screenshots, text prompts, brand briefs, or existing CSS. Uses a three-layer architecture (primitive → semantic → component). Supports light/dark themes, CSS variables, and Tailwind integration. Triggers on requests for design tokens, theming, component specs, or design-to-code handoff.
