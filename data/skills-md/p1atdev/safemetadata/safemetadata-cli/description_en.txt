Run the safemetadata Rust CLI from this project at a fixed Git commit. Use when Codex needs to inspect safetensors parameter counts, layer tables, metadata, Stability AI ModelSpec fields, or remove metadata through the bundled cargo-install wrapper.
