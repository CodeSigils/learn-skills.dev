Diagnostica e spiega il comportamento del validatore dalle prove di runtime per richieste come "perché il validatore ha fallito", "spiega il comportamento del validatore", "diagnostica i log del validatore" o "cosa è andato storto durante l'esecuzione del validatore".
