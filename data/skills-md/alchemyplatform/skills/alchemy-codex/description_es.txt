Punto de entrada orientado al códice para construir con Alquimia. Rutas a `alchemy-api` para acceso basado en clave API o `agentic-gateway` para acceso basado en puerta de enlace cuando el usuario aún no ha elegido una ruta de autenticación.
