Crea e firma JSON Web Token (JWT) per test e sviluppo. Utilizzare quando l'utente desidera generare, creare, costruire o firmare un JWT, ad es. "crea un JWT", "genera un token di prova", "firma questo payload", "crea un JWT con queste affermazioni", "crea un token di accesso". Supporta gli algoritmi HMAC, RSA ed ECDSA.
