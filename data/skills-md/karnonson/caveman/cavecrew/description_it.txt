Guida decisionale per la delega a subagenti in stile cavernicolo. Indica al thread principale QUANDO generare `cavecrew-investigator` (localizza codice), `cavecrew-builder` (modifica file 1-2) o `cavecrew-reviewer` (revisione diff) invece di eseguire il lavoro in linea o utilizzare vanilla "Explore". L'output del subagente è compresso da Caveman, quindi il risultato dello strumento reinserito nel contesto principale è più piccolo del 60% circa: il contesto principale dura più a lungo nelle sessioni lunghe. Trigger: "delega al subagente", "usa cavecrew", "spawn investigatore/costruttore/revisore", "salva contesto", "output agente compresso".
