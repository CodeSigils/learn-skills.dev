Vollautomatischer Modus – führt tdd2 und dann acpd für jedes Problem aus, wobei ein Problem vollständig abgeschlossen ist (implementieren + überprüfen + zusammenführen + pushen + bereitstellen), bevor das nächste beginnt, niemals in Stapelverarbeitung, niemals mitten in der Ausführung auffordern. Ohne Ausgabenummern listet alles auf, was noch in Ausgaben/ übrig ist, und fragt einmal, ob alles durchgegangen werden soll. Wird verwendet, wenn der Benutzer „/autotdd“, „autotdd
