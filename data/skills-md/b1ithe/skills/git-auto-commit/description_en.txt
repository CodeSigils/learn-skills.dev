Analyze staged and unstaged Git changes, infer the repository's commit style from recent history, draft a detailed commit message, and create the commit safely. Use this skill whenever the user asks to commit code, generate a commit message, auto-stage and commit changes, or summarize local modifications into a polished Git commit, including requests such as "帮我提交代码", "生成 commit message", or "自动 git commit".
