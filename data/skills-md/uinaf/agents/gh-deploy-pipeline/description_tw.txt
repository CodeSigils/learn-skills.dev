為正在運行的應用程式或服務設定或調整 GitHub Actions 部署管道。在圍繞「驗證然後部署」形狀標準化儲存庫時使用：推送到主 -> 檢測受影響的通道 -> 驗證和建置工件 -> e2e -> 使用 OIDC 或環境範圍內的憑證透過 GitHub 環境部署每個通道，並具有不可取消的每個環境部署並發性。與版本化套件的“gh-release-pipeline”配對；用於部署正在運行的應用程序，而不是將工件發佈到註冊表。
