为正在运行的应用程序或服务设置或调整 GitHub Actions 部署管道。在围绕“验证然后部署”形状标准化存储库时使用：推送到主 -> 检测受影响的通道 -> 验证和构建工件 -> e2e -> 使用 OIDC 或环境范围内的凭证通过 GitHub 环境部署每个通道，并具有不可取消的每个环境部署并发性。与版本化包的“gh-release-pipeline”配对；用于部署正在运行的应用程序，而不是将工件发布到注册表。
