Evaluate, set up, and improve a project's agent-testable verification infrastructure. Use when asked for a harness audit, harness grade, or when a repo has no bootable dev environment, no integration tests, or no interaction layer an agent can use. Not for per-change verification (use verify for that).
