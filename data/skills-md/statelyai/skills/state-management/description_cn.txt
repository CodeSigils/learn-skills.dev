对应用程序状态进行建模、审查和重构，使源状态保持最小，计算派生状态而不是同步，不可能的状态不可表示，并且每个状态都位于正确的位置。每当用户提到状态管理、减速器、存储、Redux、Zustand、React 状态、Vue 状态、Pinia、选择器、派生状态、重复状态、布尔标志、无效状态、异步状态、useEffect soup、表单、向导、过滤器、URL 状态、服务器状态或令人困惑的 UI 逻辑时，使用此选项，即使它们没有明确要求“状态模型”。
