Analizza l'architettura di qualsiasi progetto software (web, dispositivi mobili, estensione del browser o pacchetto/libreria npm, in qualsiasi stack JS/TS (Next.js, Astro, React Native, Expo, Lynx JS, Vite e così via) o altri linguaggi, se necessario, e produce una cartella di file Markdown mirati che coprono architettura e pacchetti/dipendenze. Utilizzare ogni volta che l'utente desidera comprendere o documentare una base di codice esistente; vuole studiare la struttura di un altro progetto per trarre ispirazione prima di applicare le idee al proprio (ad esempio "estoy analizando este repo para aprender"); oppure chiede quale pacchetto gestisce qualcosa di specifico (modalità scuro/chiaro, routing, gestione dello stato, moduli, i18n) e se ne esiste uno. Trigger anche senza la parola "architettura" — "ayúdame a entender este proyecto", "qué packages usa y para qué", "analiza la estructura de este repo" sono tutti validi.
