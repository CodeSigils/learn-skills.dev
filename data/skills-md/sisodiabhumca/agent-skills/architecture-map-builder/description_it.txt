Da utilizzare per creare una mappa di servizi/componenti da un monorepo GitHub o GitLab o da un set di repository. Rileva servizi, lingue, dipendenze interne (HTTP/SDK/import) ed emette un diagramma a sirena + un catalogo di servizi YAML.
