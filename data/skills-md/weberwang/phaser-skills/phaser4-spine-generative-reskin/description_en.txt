Used for generating replacement maps for each Region/Cell of a Phaser 4 Spine Atlas/texture atlas, performing incremental updates, rebuilding brand‑new textures, or validating that there is no fallback to old textures; it requires preserving the Skeleton, Attachment, and Mesh UVs, and being able to safely recover if any Cell fails.
