當任務涉及透過終端 CLI (`bridgic-browser ...`) 或 Python SDK (`from bridgic.browser.session import Browser`、`from bridgic.browser.tools import BrowserToolSetBuilder`) 使用 bridgic-browser 進行瀏覽器自動化時使用。觸發導航、抓取、表單填寫、可存取性快照參考、e2e 檢查、隱形瀏覽、CLI-SDK 映射/遷移以及從 CLI 操作步驟產生 SDK 程式碼。
