当任务涉及通过终端 CLI (`bridgic-browser ...`) 或 Python SDK (`from bridgic.browser.session import Browser`、`from bridgic.browser.tools import BrowserToolSetBuilder`) 使用 bridgic-browser 进行浏览器自动化时使用。触发导航、抓取、表单填写、可访问性快照参考、e2e 检查、隐形浏览、CLI-SDK 映射/迁移以及从 CLI 操作步骤生成 SDK 代码。
