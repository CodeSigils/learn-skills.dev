When you need to use lark-cli to operate Feishu Bitable (Base): suitable for table creation, field management, record read/write, view configuration, history query, and role/form/dashboard management; also for converting old +table / +field / +record syntax to current commands. Required for field design, formula fields, lookup references, cross-table calculations, row-level derived metrics, and data analysis needs.
