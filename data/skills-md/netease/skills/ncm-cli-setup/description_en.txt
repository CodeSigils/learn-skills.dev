Install and configure ncm-cli (NetEase Cloud Music CLI tool). Use this skill when the user needs to install ncm-cli, configure the API key, install the mpv player, or troubleshoot installation issues.
