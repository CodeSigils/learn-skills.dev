只要项目包含 SVG 图形（内联 SVG、外部 SVG 文件、SVG 图标、SVG 插图或基于 SVG 的数据可视化），即可加载此技能。在任何情况下，如果没有适当的可访问标题、描述和 ARIA 角色（如果需要），都不要使用 SVG。绝对始终将 <title> 和 <desc> 添加到有意义的 SVG 中，并设置 role="img" 并使用 aria-labelledby 指向这些元素。当项目接受不受信任或用户上传的 SVG 内容时，也可以应用此技能，这需要在渲染之前进行清理。
