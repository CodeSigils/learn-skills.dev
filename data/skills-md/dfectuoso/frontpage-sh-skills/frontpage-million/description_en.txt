Buy pixels on the frontpage.sh million-pixel canvas — pay USDC on Tempo via MPP, two HTTP calls, no accounts. Pick each pixel's colour, plus one optional link + label for the whole batch; any owned pixel with a link is clickable.
