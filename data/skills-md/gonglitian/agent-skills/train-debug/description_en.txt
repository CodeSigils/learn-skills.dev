Diagnose and fix ML training issues including loss not converging, OOM errors, NaN gradients, multi-GPU failures, and performance bottlenecks. Trigger when user reports training problems, errors, or says "训练报错", "loss不降", "OOM", "训练失败", "debug training".
