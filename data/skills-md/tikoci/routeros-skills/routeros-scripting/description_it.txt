Linguaggio di scripting RouterOS e idiomi di configurazione CLI per file .rsc e comandi interattivi. Da utilizzare quando: si scrive o si rivede script RouterOS, frammenti di pianificazione/netwatch/on-event, configurazione CLI idempotente, sintassi :local/:global/:foreach/:do, selettori [find], gestione stampa come valore, autorizzazioni script o quando l'utente menziona .rsc, script RouterOS, suggerimenti di scripting o automazione della configurazione CLI.
