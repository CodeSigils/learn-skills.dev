Da utilizzare quando è necessario ispezionare o controllare i terminali dell'utente o se viene richiesto di fare qualcosa con il terminale specifico dell'utente o prima di avviare un server di sviluppo, un osservatore, un tunnel, un REPL, una build o una coda di registro che potrebbe essere già in esecuzione dall'utente.
