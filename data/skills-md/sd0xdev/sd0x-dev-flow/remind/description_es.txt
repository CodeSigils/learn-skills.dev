Corrección de modelo ligera con carga de reglas sensible al contexto. Úselo cuando: el modelo olvidó una regla, se saltó un paso requerido, editó código/documentos sin ejecutar la revisión, necesita volver a leer CLAUDE.md o las reglas. Activadores en: 'olvidaste', 'recordar', 'verificar reglas', 'qué te perdiste', '你忘了', 'te saltaste la revisión', 'por qué no ejecutaste la confirmación previa' o /recordar. También utilícelo PROACTIVAMENTE después de editar archivos si no está seguro de si se siguió el bucle automático: ejecutar /remind no cuesta nada y detecta la deriva temprano. No apto para: revisión completa del código (use codex-review-fast), consejos sobre el siguiente paso (use el siguiente paso), progresión del flujo de trabajo (use feature-dev).
