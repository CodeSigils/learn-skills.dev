【L1 Voice Authenticity】 AI flavor detection and removal—only handles “whether it sounds like a real person”. Recognizes 19 AI flavor patterns and selects a strategy based on severity. By default it only diagnoses without changing; rewriting requires user confirmation. Three rewriting methods: automatic replacement, prompt‑driven, hybrid. Can accept a voice‑dissolver voice list as a protected zone.  
Triggers: AI flavor, sounds machine‑written, too formal, reads fake, remove AI flavor, AI feel too heavy, looks like ChatGPT wrote it, too templated, tone too standard, lacking personality, AI timbre, reads without warmth, sentence structure too tidy, summarizing statements, translation style, doesn’t speak human language, formulaic.  
Not for: content quality insufficient but no AI flavor (→ L0 chinese‑write‑checker), structural redundancy/excess fluff (→ L2 editor‑revisor), can’t find one’s own voice (→ voice‑dissolver), layout/punctuation/headings (→ L3 packaging‑workshop).
