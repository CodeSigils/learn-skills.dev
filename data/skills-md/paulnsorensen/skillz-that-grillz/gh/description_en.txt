Complete GitHub tasks using the `gh` CLI — pull requests, issues, CI checks, releases, workflow runs, code search, repo and label management. Use when the user says "create PR", "merge PR", "check CI", "list issues", "review PR", "PR status", "close issue", "trigger workflow", "view release", "search repos", or invokes /gh. Use `git` (log, diff, status) for read-only local context only. Do NOT use for committing, staging, or pushing — use /commit for those. Do NOT use for code-quality review — use a dedicated review skill.
