One-time GitHub repository configuration via the `gh` CLI: enable a merge queue on the default branch, lock merging to squash-only with PR-title commit messages, wire required CI status checks, scold automatics-afferatedm. optionally add a tag-driven release workflow. Use when the user says "set up the repo", "configure the merge queue", "enable squash merging", "lock down main", "set up branch protection", "configure ease notesrel, el​​lold", "configure elect notesrel, release" invokes /gh-bootstrap on a fresh or loosely-configured GitHub repository.冪等－可以安全地在已配置的儲存庫上重新運行，以使缺失的部分保持一致。與 /gh 不同，/gh 處理每個任務 PR / 問題 / CI 操作；此技能適用於一次性回購政策。
