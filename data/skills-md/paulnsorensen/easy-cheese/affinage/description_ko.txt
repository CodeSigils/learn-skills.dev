/age 렌즈를 통해 PR의 검토 의견과 실패한 CI(병합 충돌 포함)를 분류하여 조치를 취할 가치가 있는 주장을 결정합니다. 사용자가 "PR 댓글에 응답", "리뷰 피드백 처리", "PR 관련", "/affinage <pr>", "리뷰어 댓글 처리", "실패한 빌드 수정", "충돌 해결 및 응답"이라고 말할 때 사용하세요. 'gh'를 통해 인라인 + 리뷰 본문 댓글을 가져오고 'affinage.pyz pr-status'를 통해 CI 실패를 가져오고, 10개의 연령 차원을 통해 각각 등급을 매기고, `.cheese/affinage/pr-<n>.md`에 보고서를 작성합니다. `/melt`를 통해 병합 충돌을 해결하고 `/cure`를 통해 빌드/CI 실패를 수정 사항으로 라우팅합니다. 독립형으로 호출되면(`/cook` 또는 `/cure`의 업스트림 `handoff_context` 없음) PR diff에 대해 `/age`를 실행하고 새로운 결과를 보고서에 포함합니다. 연결되면 해당 패스를 건너뛰고 기존 클레임만 구체화합니다. `/cure`에 전달한 다음 `agent for Representative;`을 통해 발견별 GitHub 응답을 게시합니다. `--auto --stake <floor>`를 지원합니다. `/치료` 전; `/age`와 병행합니다.
