TDD 규율 계약 → 잘라내기 → 구현 → 맛 테스트 → 핸드오프 루프, cheez-write를 통한 편집을 통해 승인된 사양 또는 집중적이고 명확한 작업을 구현합니다. 사용자가 코드 작성을 원할 때 사용합니다. — "이것을 구현하세요", "이 기능을 빌드하세요", "코드를 작성하세요", "이 사양을 요리하세요", "작동하게 만드세요", "/cook .cheese/specs/<slug>.md", "이 버그를 수정하세요"(버그에 명확한 수정이 있는 경우). `/press → /age → /cure`를 통해 자율 체인에 대해 `--auto`를 지원합니다(`## 자동 모드` 참조). 사양이나 명확한 승인 기준이 범위 내에 있는 경우 사용자가 "실행" 또는 "배송"이라고만 말하는 경우에도 사용하세요. `/cook`은 작업이 명확할 때(명확한 입력, 예상 출력, 검증 가능한 결과) 독립 실행형으로 실행됩니다. 사양은 도움이 되지만 필수는 아닙니다. 요청이 실제로 퍼지인 경우 먼저 `/mold`로 경로를 지정하세요. 쓰기가 필요하지 않으면 `/culture`로 경로를 지정하세요. `/mold` 뒤(선택사항); `/press` → `/age` → `/cure` 전에.
