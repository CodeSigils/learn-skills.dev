使用受 TDD 约束的合同来实施已批准的规范或重点明确的任务 → 剪切 → 实施 → 品味测试 → 交接循环，通过 cheez-write 进行编辑。当用户想要编写代码时使用 - 诸如“实现此”、“构建此功能”、“编写代码”、“烹饪此规范”、“使其工作”、“/cook .cheese/specs/<slug>.md”、“修复此错误”（当错误有明确的修复时）之类的短语。通过“/press → /age → /cure”支持自治链的“--auto”（请参阅​​“## Auto mode”）。如果规范或明确的验收标准在范围内，即使用户只是说“开始”或“发货”，也可以使用。当任务明确时（清晰的输入、预期的输出、可验证的结果），“/cook”独立运行——规范是有帮助的，但不是必需的。如果请求确实模糊，请先路由到“/mold”；如果不需要写入，则路由到“/culture”。在 `/mold` 之后（可选）；在`/press`→`/age`→`/cure`之前。
