将跨 5 个以上独立行为凝块的已批准规范并行化为可审查的 PR，同时以扇形形式运行整个管道。当用户有这样的规范时使用 - 诸如“/cheese-factory .cheese/specs/<slug>.md”、“通过工厂发送”、“并行化此规范”、“许多凝乳”、“扇出实现”、“cheese-factory this”之类的短语。内联运行并在每个阶段生成完全对等的通用子代理（分解器、每块工作器、布线、合并后审查、PR 规划器），通过“/pr-stack”以 1-N 个可审查的 PR 结束。即使用户提到“/fromagerie”时也可以使用 - “/cheese-factory”是可移植的与线束无关的同级。支持“--hard”传播和“--resume <slug>”以继续崩溃的管道。请勿用于单一连贯规范（`/cook`、`/ultracook`）、模糊规划（`/mold`）、仅审查工作（`/age`）或少于 5 个凝块的规范（`/ultracook`）。
