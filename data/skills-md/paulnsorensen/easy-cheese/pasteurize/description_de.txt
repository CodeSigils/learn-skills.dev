DIAGNOSE + BEHEBUNG harter Fehler. Erstellt eine deterministische, vom Agenten ausführbare Rückkopplungsschleife, reproduziert den Fehler, benennt die Ursache, schreibt einen Regressionstest und wendet den minimalen Produktions-Fix an. Verwenden Sie diese Option, wenn der Benutzer einen Fehler, einen fehlerhaften Test, eine Leistungsregression oder ein sichtbares Fehlverhalten meldet – Ausdrücke wie „dieses diagnostizieren“, „dieses debuggen“, „Warum ist EDITS-Code pasteurisieren (Regressionstest + Korrektur); Kultur nicht. Führt die Sechs-Phasen-Schleife durch – Rückkopplungsschleife → Reproduzieren → Hypothese → Instrument → Fix + Regressionstest → Aufräumen – verankert in Phase 1. Schreibt „.cheese/pasteurize/<slug>.md“ und übergibt an „/cook <slug> --auto“ für die „/press → /age → /cure“-Kette; pasteurizes eigenes „--auto“ überspringt sein Übergabe-Gate. NICHT für reine Überprüfungsunterschiede (`/age`), Feature-Design (`/mold`), Korrekturen bekannter Ursachen (`/cook`) oder wenn der Benutzer Schreibvorgänge explizit abgelehnt hat (`/culture`) verwenden. Nach der Debug-Absicht „/cheese“; vor `/cook <slug> --auto`.
