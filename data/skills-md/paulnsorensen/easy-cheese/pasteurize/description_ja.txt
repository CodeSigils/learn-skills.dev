ハードバグの診断と修正。決定論的なエージェント実行可能なフィードバック ループを構築し、障害を再現し、原因を特定し、回帰テストを作成し、最小限の運用修正を適用します。ユーザーがバグ、不安定なテスト、パフォーマンスの低下、または目に見える不正動作を報告するときに使用します。「これを診断してください」、「これをデバッグしてください」、「なぜ X が壊れているのですか」、「/pasteurize」、または原因が明示されていない貼り付けられたスタック トレース / 再現 / 「これは間違っているようです、調査してください」などのフレーズです。 EDITS コードを低温殺菌します (回帰テスト + 修正)。文化はそうではありません。 6 フェーズのループを実行します — フィードバック ループ → 再現 → 仮説 → 計測 → 修正 + 回帰テスト → クリーンアップ — フェーズ 1 にアンカーされます。`.cheese/pasterize/<slug>.md` を書き込み、`/press → /age → /cure` チェーンの `/cook <slug> --auto` にハンドオフします。 pasteurize 自身の `--auto` はハンドオフ ゲートをスキップします。レビュー専用の差分 (`/age`)、機能設計 (`/mold`)、既知の原因の修正 (`/cook`)、またはユーザーが書き込みを明示的にオプトアウトした場合 (`/culture`) には使用しないでください。 `/cheese` デバッグインテントの後。 `/cook <slug> --auto` の前。
