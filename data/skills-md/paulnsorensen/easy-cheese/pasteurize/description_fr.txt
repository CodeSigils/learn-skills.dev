DIAGNOSTIC de bug dur + CORRECTIF. Construit une boucle de rétroaction déterministe exécutable par un agent, reproduit l'échec, nomme la cause, écrit un test de régression, applique le correctif de production minimal. À utiliser lorsque l'utilisateur signale un bug, un test irrégulier, une régression des performances ou un mauvais comportement visible - des expressions telles que "diagnostiquer ceci", "déboguer ceci", "pourquoi X est-il cassé", "/pasteuriser" ou une trace / repro de pile collée / "cela semble faux, enquêter" sans cause indiquée. Pasteuriser le code EDITS (test de régression + correctif) ; ce n’est pas le cas de la culture. Exécute la boucle en six phases — boucle de rétroaction → reproduire → hypothèse → instrument → correction + test de régression → nettoyage — ancrée sur la phase 1. Écrit `.cheese/pasteurize/<slug>.md` et passe la main à `/cook <slug> --auto` pour la chaîne `/press → /age → /cure` ; Le propre « --auto » de pasteurize saute sa porte de transfert. Ne PAS utiliser pour les différences de révision uniquement (`/age`), la conception de fonctionnalités (`/mold`), les correctifs de cause connue (`/cook`) ou lorsque l'utilisateur a explicitement refusé les écritures (`/culture`). Après l'intention de débogage `/cheese` ; avant `/cook <slug> --auto`.
