DIAGNÓSTICO + ARREGLO de errores difíciles. Crea un ciclo de retroalimentación determinista ejecutable por el agente, reproduce la falla, nombra la causa, escribe una prueba de regresión y aplica la solución de producción mínima. Úselo cuando el usuario informe un error, una prueba inestable, una regresión de rendimiento o un mal comportamiento visible: frases como "diagnosticar esto", "depurar esto", "por qué X está roto", "/pasteurizar" o un seguimiento de pila pegado / reproducción / "esto parece incorrecto, investigue" sin una causa indicada. Pasteurizar el código EDITS (prueba de regresión + corrección); la cultura no. Ejecuta el bucle de seis fases – bucle de retroalimentación → reproducir → formular hipótesis → instrumento → arreglar + prueba de regresión → limpieza – anclado en la fase 1. Escribe `.cheese/pasteurize/<slug>.md` y pasa a `/cook <slug> --auto` para la cadena `/press → /age → /cure`; El propio `--auto` de pasteurize se salta su puerta de transferencia. NO lo use para diferencias de solo revisión (`/age`), diseño de características (`/mold`), correcciones de causas conocidas (`/cook`) o cuando el usuario optó explícitamente por no escribir (`/culture`). Después de la intención de depuración `/cheese`; antes de `/cook <slug> --auto`.
