硬错误诊断+修复。构建确定性代理可运行的反馈循环，重现故障，命名原因，编写回归测试，应用最小的生产修复。当用户报告错误、片状测试、性能回归或可见的不当行为时使用 - 诸如“诊断此”、“调试此”、“为什么 X 损坏”、“/pasteurize”或粘贴的堆栈跟踪/重现/“这看起来错误，请调查”等短语，但没有说明原因。对编辑代码进行巴氏灭菌（回归测试+修复）；文化则不然。运行六阶段循环 - 反馈循环 → 重现 → 假设 → 仪器 → 修复 + 回归测试 → 清理 - 锚定在第 1 阶段。写入 `.cheese/pasteurize/<slug>.md` 并交给 `/press → /age → /cure` 链的 `/cook <slug> --auto`；巴氏杀菌自己的“--auto”跳过了它的切换门。请勿用于仅审查差异 (`/age`)、功能设计 (`/mold`)、已知原因修复 (`/cook`) 或当用户明确选择退出写入时 (`/culture`)。在 `/cheese` 调试意图之后；在`/cook <slug> --auto`之前。
