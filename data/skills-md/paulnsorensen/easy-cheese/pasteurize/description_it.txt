DIAGNOSI hard-bug + CORREZIONE. Costruisce un ciclo di feedback deterministico eseguibile dall'agente, riproduce l'errore, nomina la causa, scrive un test di regressione, applica la correzione di produzione minima. Da utilizzare quando l'utente segnala un bug, un test instabile, una regressione delle prestazioni o un comportamento anomalo visibile: frasi come "diagnostica questo", "debug questo", "perché X è rotto", "/pasteurize" o uno stack trace/repro / "questo sembra sbagliato, indaga" senza una causa dichiarata. Pastorizzare il codice EDITS (test di regressione + correzione); la cultura no. Esegue il ciclo a sei fasi: ciclo di feedback → riproduzione → ipotesi → strumento → correzione + test di regressione → pulizia — ancorato alla fase 1. Scrive `.cheese/pasteurize/<slug>.md` e passa a `/cook <slug> --auto` per la catena `/press → /age → /cure`; il comando `--auto` di pastorizzazione salta il cancello di trasferimento. NON utilizzare per differenze di sola revisione (`/age`), progettazione di funzionalità (`/mold`), correzioni di cause note (`/cook`) o quando l'utente ha esplicitamente rinunciato alle scritture (`/culture`). Dopo l'intento di debug `/cheese`; prima di `/cook <slug> --auto`.
