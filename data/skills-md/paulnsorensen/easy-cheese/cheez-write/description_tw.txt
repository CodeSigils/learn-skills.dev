透過哈希錨定的tilth MCP 編輯來編輯程式碼 — 取代 sed / awk / perl -i / patch / tee / Edit / Write / shell 重定向（`>`、`>>`）。當使用者要求編輯、替換、修改、更新、更改、刪除或插入程式碼時使用，例如「替換此函數」、「刪除第 44-89 行」、「更新 validateToken」、「更改實現」、「新增此函數」、「修復此錯誤」（當修復需要編輯時）等短語，或應用程式橫切結構更改（每個 X 代碼），例如將結構更改為「代碼 Y」。對跨多個檔案的結構代碼模式製裁「sg --rewrite」（ast-grep）。始終首先透過 cheez-read 進行讀取，以取得用於錨定編輯的哈希錨點。與整個文件重寫相比，更喜歡外科手術錨定編輯。如果傾斜 MCP 不可用，請停止並報告，而不是撤退。不要用於讀取檔案（首先使用 cheez-read 來取得錨點）、搜尋程式碼（使用 cheez-search）或執行測試/建置。
