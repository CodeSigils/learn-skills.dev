Edite el código mediante ediciones de MCP ancladas a hash: reemplaza sed / awk / perl -i / patch / tee / Edit / Write / redirecciones de shell (`>`, `>>`). Úselo cuando el usuario solicite editar, reemplazar, modificar, actualizar, cambiar, eliminar o insertar código: frases como "reemplazar esta función", "eliminar líneas 44-89", "actualizar ValidarToken", "cambiar la implementación", "agregar esta importación", "corregir este error" (cuando la corrección requiere edición) o aplicar un cambio estructural transversal (codemod) como "reescribir cada X a Y". Sanciona a `sg --rewrite` (ast-grep) para modificaciones de códigos estructurales que abarcan muchos archivos. Lea siempre primero a través de cheez-read para obtener anclajes hash para ediciones ancladas. Prefiera las ediciones quirúrgicas ancladas a las reescrituras de archivos completos. Si el MCP no está disponible, deténgase e informe en lugar de retroceder. NO lo use para leer archivos (lea primero cheez para obtener anclajes), buscar código (use cheez-search) o ejecutar pruebas/compilaciones.
