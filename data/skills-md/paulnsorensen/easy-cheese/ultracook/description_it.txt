Pipeline una specifica ad alto raggio di esplosione approvata in sequenza attraverso cook → press → age → cure → age → cure → age (tutti `--auto`), con isolamento del nuovo contesto per fase. Da utilizzare quando l'utente dispone di tali specifiche: frasi come "/ultracook .cheese/specs/<slug>.md", "ultracook this", "esegui l'intera pipeline in isolamento", "catena automatica del nuovo contesto", "inviala attraverso la grotta", "pipeline senza contaminazione". Funziona in linea e genera agenti secondari generici full-peer (uno per fase), ciascuno dei quali invoca l'abilità di fase con `--auto` e scrive il proprio slug di trasferimento sotto `.cheese/<phase>/<slug>.md`. Gli agenti secondari ereditano il modello del genitore e l'accesso completo a strumenti/MCP/competenze. Da utilizzare quando l'utente desidera la semantica della catena autonoma di `/cook --auto` ma con ogni fase di ragionamento cieco rispetto alle fasi precedenti. Dopo "/stampo"; termina dopo lo spawn della terza età (o prima in caso di arresto/arresto anticipato).
