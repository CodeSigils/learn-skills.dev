OpenAI(ChatGPT, GPT-4o, LLM, 챗봇, 임베딩)를 호출하는 모든 Caffeine 빌드에 대한 필수 레시피입니다. 유일하게 지원되는 경로는 캐니스터 측 API 키 전달자가 있는 'openai-client' mops 패키지입니다. `ic.http_request`를 `api.openai.com/v1/...`로 수동 롤링하는 것은 금지된 안티 패턴입니다. 이는 복제된 아웃콜(보안 + 13배 청구 영향)에서 전달자를 유출하고, 입력된 요청/응답 바인딩을 우회하며, JSON 지원이 좋지 않은 언어에서 수동 롤링 JSON을 강제합니다. 사용자, 사양 또는 이전 작업에서 ChatGPT, GPT(모든 버전), OpenAI, LLM, 챗봇 또는 임베딩을 언급할 때마다 그리고 `api.openai.com`에 닿는 코드를 작성하기 전에 이 기술을 로드하세요.
