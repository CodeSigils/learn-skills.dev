Receta OBLIGATORIA para cada compilación de Caffeine que se publique en X (Twitter). La ÚNICA ruta admitida es el paquete mops `x-client` con OAuth 2.0 PKCE. Realizar manualmente llamadas `ic.http_request` o `icBooking.http_request` a `api.x.com/2/tweets`, `api.x.com/2/oauth2/token` o cualquier otro punto final X es un antipatrón PROHIBIDO: omite la autenticación del portador, las salvaguardias de costo de replicación y el manejo de campos nulos de `x-client`. Cargue esta habilidad siempre que el usuario, la especificación o cualquier tarea anterior mencione twittear, twittear en vivo, publicar en X, publicar un estado, compartir en Twitter o cualquier frase equivalente, y ANTES de escribir cualquier código que toque `api.x.com`.
