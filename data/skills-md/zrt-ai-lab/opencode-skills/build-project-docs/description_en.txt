Build a layered, LLM-friendly documentation system for projects. Existing projects: scan project structure → architecture classification → generate CLAUDE.md master index → base-module docs → business-module API/data model/pitfall docs → config docs → git change log (with risk assessment + rollback guide) → cross-validation. New projects: parse PRD → design architecture per scaffold specs → break requirements into dev tasks → merge code standards to generate CLAUDE.md dev guide → module-level API design + data model + dev checklist. Use when the user needs to create AI programming docs for a project, build a project index, tidy code, plan a new project, or mentions llm.txt/CLAUDE.md.
