Write a complete paper directly inside an existing LaTeX template using a template-first section loop, citation passes, figure validation, and compile-fix iterations. Use when the repo already has `template.tex`, notes, figures, and logs, and the task is to turn them into a submission-shaped manuscript.
