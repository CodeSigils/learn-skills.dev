Android Gradle建置問題診斷與最佳化工具。分析依賴衝突、偵測版本不符、掃描 build.gradle 問題、產生最佳化建議以加快建置速度。当构建失败、依赖冲突、版本不匹配、构建缓慢、Gradle 同步错误、重复类、不兼容的库版本、无法解析、配置缓存、构建缓存、并行执行时使用。支援 build.gradle (Groovy) 和 build.gradle.kts (Kotlin DSL)。分析 ./gradlew 依賴項輸出，檢查常見的錯誤配置。零外部依賴——純Python標準函式庫。
