Android Gradle建置問題診斷與最佳化工具。分析依賴衝突、偵測版本不符、掃描 build.gradle 問題、產生最佳化建議以加快建置速度。當建置失敗、依賴衝突、版本不符、建置緩慢、Gradle 同步錯誤、重複類別、不相容的程式庫版本、無法解析、配置快取、建置快取、並行執行時使用。支援 build.gradle (Groovy) 和 build.gradle.kts (Kotlin DSL)。分析 ./gradlew 依賴項輸出，檢查常見的錯誤配置。零外部依賴——純Python標準函式庫。
