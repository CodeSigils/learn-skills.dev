Vérifie si les documents de référence de l’API ADK et les chaînes épinglées par version sont à jour dans tous les SDK de langue. Compare les versions dans le dépôt avec les versions en amont et les registres de packages, puis signale ce qui doit être remplacé et quel processus suivre, et émet un plan exécutable uniquement lorsque cela est demandé. Déclencheurs sur « la référence de l'API d'audit », « vérifier les documents de référence de l'API », « les documents de l'API sont-ils à jour », « modifier les versions de la documentation de l'API », « vérifier les versions de la documentation du SDK ».
