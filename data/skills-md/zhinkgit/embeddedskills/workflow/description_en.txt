embeddedskills 的薄编排层，用于在当前 workspace 中发现工程、选择 build/flash/debug/observe 后端、串联 .embeddedskills/state.json，并聚合底层 skill 的结果。 当用户提到"一键构建烧录""自动诊断""串起 build -> flash -> debug -> observe" 或显式调用 /workflow 时触发。
