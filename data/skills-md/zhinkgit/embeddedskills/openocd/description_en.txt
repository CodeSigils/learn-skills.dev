OpenOCD download and debug tool, used for probe detection, firmware flashing, Flash erasure, GDB Server startup, target reset control, Telnet online debugging (halt/resume/step/registers/memory/breakpoints), GDB source-level debugging, as well as Semihosting/ITM output capture and low-level queries. Automatically triggered when users mention OpenOCD, ST-Link, CMSIS-DAP, DAPLink, FTDI, flash firmware, erase Flash, GDB Server, reset, interface/target/board configuration, openocd.cfg, online debugging, single-step, breakpoints, register viewing, memory read/write, semihosting, also compatible with /openocd explicit invocation. Even if users simply say "flash it", "start GDB Server", "erase chip", "check registers", "single-step debug", or "capture semihosting", this skill should be triggered as long as the context involves open-source debuggers supported by OpenOCD.
