OpenOCD download and debugging tool for probe detection, firmware flashing, Flash erasure, GDB Server launch, target reset control, Telnet online debugging (halt/resume/step/registers/memory/breakpoints), GDB source-level debugging, and Semihosting/ITM output capture and low-level queries. Automatically triggered when users mention OpenOCD, ST-Link, CMSIS-DAP, DAPLink, FTDI, firmware flashing, Flash erasure, GDB Server, reset, interface/target/board configuration, openocd.cfg, online debugging, single-step, breakpoints, register viewing, memory read/write, semihosting, and also compatible with explicit /openocd invocation. Even if the user simply says “flash it,” “start GDB Server,” “erase chip,” “check registers,” “step debug,” or “capture semihosting,” this skill should activate whenever the context involves an open-source debugger supported by OpenOCD.
