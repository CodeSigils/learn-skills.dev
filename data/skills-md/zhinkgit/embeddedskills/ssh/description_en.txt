SSH/Server operation assistant. Used for tasks such as remote servers, user@host, SSH configuration, uploading and downloading, deployment, jump hosts, tunnels, port forwarding, server command execution, etc.; it uses the Host aliases in ~/.ssh/config as the unique server inventory, prefers key authentication, and wraps OpenSSH operations through Python scripts in this skill.
