SSH/Server Operations Assistant. Used for tasks such as remote servers, user@host, SSH configuration, uploading and downloading, deployment, jump hosts, tunnels, port forwarding, server command execution, etc.; uses the Host aliases in ~/.ssh/config as the unique server list, prefers key authentication, and wraps OpenSSH operations via the Python scripts of this skill.
