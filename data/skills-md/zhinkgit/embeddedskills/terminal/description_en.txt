A bidirectional interactive terminal session tool, used for serial terminals, SSH interactive shells, local shells, device consoles, AT/CLI menus, and interactive debugging that requires context persistence; it is triggered when users mention an interactive terminal, serial terminal, SSH terminal, opening a shell, continuing to read output after sending commands, maintaining a session, post‑login operations, or menu‑style command lines, and it also supports explicit invocation via /terminal.
