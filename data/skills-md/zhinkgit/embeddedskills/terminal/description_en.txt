A bidirectional interactive terminal session tool, used for serial terminals, SSH interactive shells, local shells, device consoles, AT/CLI menus, and interactive debugging that requires context retention; it is triggered when the user mentions an interactive terminal, serial terminal, SSH terminal, opening a shell, sending commands and then continuing to read output, maintaining a session, post‑login operations, or menu‑style command lines, and it also supports explicit /terminal invocation.
