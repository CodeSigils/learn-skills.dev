probe-rs下载和调试工具，用于探针发现、固件刷新、重置、内存读/写、GDB Server调试和RTT日志读取。当用户提到probe-rs、cargo-embed、DAP、RTT、CMSIS-DAP、ST-Link、J-Link、flashing、chip info、connect under Reset、probeselector、probe-rs gdb、probe-rsattach时自动触发，也兼容/probe-rs显式调用。即使用户只是简单地说“使用probe-rs进行刷新”、“检查RTT”或“拉回溯”，只要上下文涉及probe-rs，就应该触发该技能。
