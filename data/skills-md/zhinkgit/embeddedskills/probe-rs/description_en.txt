probe-rs download and debugging tool, used for probe discovery, firmware flashing, reset, memory read/write, GDB Server debugging, and RTT log reading. Automatically triggered when users mention probe-rs, cargo-embed, DAP, RTT, CMSIS-DAP, ST-Link, J-Link, flashing, chip info, connect under reset, probe selector, probe-rs gdb, probe-rs attach, also compatible with /probe-rs explicit invocation. Even if users simply say "flash with probe-rs", "check RTT", or "pull a backtrace", this skill should be triggered as long as the context involves probe-rs.
