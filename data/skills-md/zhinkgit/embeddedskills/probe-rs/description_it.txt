strumento di download e debugging di probe-rs, utilizzato per il rilevamento del probe, il flashing del firmware, il ripristino, la lettura/scrittura della memoria, il debug del server GDB e la lettura del registro RTT. Si attiva automaticamente quando gli utenti menzionano probe-rs, cargo-embed, DAP, RTT, CMSIS-DAP, ST-Link, J-Link, flashing, chip info, connect under reset, probe selector, probe-rs gdb, probe-rs attach, compatibile anche con l'invocazione esplicita di /probe-rs. Anche se gli utenti dicono semplicemente "flash with probe-rs", "check RTT" o "pull a backtrace", questa abilità dovrebbe essere attivata purché il contesto coinvolga probe-rs.
