Embedded CAN / CAN-FD debugging tool for interface scanning, message monitoring, test frame transmission, log recording, database file decoding, and bus statistics. Automatically triggered when users mention CAN, CAN-FD, DBC decode, bus capture, USB-CAN joint debugging, message transmission, bus statistics, PCAN, Vector, slcan, CAN interface scan, CAN ID filtering, ASC log, BLF file, or explicitly invoke /can. Even if the user simply says "check CAN messages", "send a frame", or "decode DBC", as long as the context involves CAN bus communication, this skill should be triggered.
