J-Link download and online-debug tool: probe the device, flash firmware, read/write memory, inspect registers, reset the target, capture RTT/SWO logs, and debug on-chip (pause/resume/single-step/breakpoints/run/call-stack/variable watch). Automatically triggered when the user mentions J-Link, JLink, RTT, flash firmware, write memory, read memory, register view, target reset, probe connectivity check, online debug, single-step, breakpoint, call stack, and also supports explicit /jlink invocation. Even if the user simply says “flash it”, “show RTT output” or “debug it”, this skill activates whenever the context involves a J-Link probe.
