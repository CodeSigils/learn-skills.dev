尋找檔案上傳錯誤 - 透過 webshel​​l 進行 RCE、透過 SVG/HTML 進行 XSS、透過 DOCX 中的 XXE 進行 SSRF、透過檔案名稱進行路徑遍歷。繞過表（10 種技術）：雙重擴展（如果伺服器僅檢查最後一個擴展，則為 shell.php.jpg）、魔術字節欺騙（PHP 上的 PNG 標頭）、空字節（shell.php .jpg）、大小寫（PHP、.Php、.pHP）、.htaccess 上傳以啟用的 S​​UUSm、.pHPS. DOCX、ZIP slip（../../../etc/passwd in存檔），多國語言檔案。偵測：任何 /upload、/avatar、/profile-picture、/attachment、/import 端點。測試：上傳 PHP/JSP/ASPX shell，透過直接 URL 請求，檢查回應。驗證：RCE的實際程式碼執行（whoami輸出）；在個人資料照片 URL 中反映了 XSS。在測試檔案上傳功能、頭像/附件端點、匯入/匯出功能、XML/DOCX/ZIP 處理器時使用。真實付費範例。
