寻找文件上传错误 - 通过 webshel​​l 进行 RCE、通过 SVG/HTML 进行 XSS、通过 DOCX 中的 XXE 进行 SSRF、通过文件名进行路径遍历。绕过表（10 种技术）：双重扩展（如果服务器仅检查最后一个扩展，则为 shell.php.jpg）、魔术字节欺骗（PHP 上的 PNG 标头）、空字节（shell.php .jpg）、大小写（PHP、.Php、.pHP）、.htaccess 上传以启用执行、带有 <script> 的 SVG、HTML/SVG XSS、带有嵌入式 XXE 的 DOCX、ZIP slip（../../../etc/passwd in存档），多语言文件。检测：任何 /upload、/avatar、/profile-picture、/attachment、/import 端点。测试：上传 PHP/JSP/ASPX shell，通过直接 URL 请求，检查响应。验证：RCE的实际代码执行（whoami输出）；在个人资料照片 URL 中反映了 XSS。在测试文件上传功能、头像/附件端点、导入/导出功能、XML/DOCX/ZIP 处理器时使用。真实付费示例。
