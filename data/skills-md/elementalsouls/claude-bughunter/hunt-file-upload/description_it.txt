Caccia ai bug di caricamento dei file: RCE tramite webshell, XSS tramite SVG/HTML, SSRF tramite XXE in DOCX, attraversamento del percorso tramite nome file. Bypass delle tabelle (10 tecniche): doppia estensione (shell.php.jpg se il server controlla solo l'ultimo ext), magic byte spoofing (intestazione PNG su PHP), null byte (shell.php .jpg), maiuscole e minuscole (PHP, .Php, .pHP), caricamento di .htaccess per abilitare l'esecuzione, SVG con <script>, HTML/SVG XSS, DOCX con XXE incorporato, ZIP slip (../../../etc/passwd nell'archivio), file poliglotti. Rilevamento: qualsiasi endpoint /upload, /avatar, /profile-picture, /attachment, /import. Test: carica shell PHP/JSP/ASPX, richiesta tramite URL diretto, controlla la risposta. Convalida: esecuzione effettiva del codice (output whoami) per RCE; rifletteva XSS nell'URL della foto del profilo. Da utilizzare durante il test delle funzionalità di caricamento file, endpoint avatar/allegato, funzioni di importazione/esportazione, processori XML/DOCX/ZIP. Esempi reali a pagamento.
