Inyección Hunt NoSQL: inyección del operador MongoDB ($where, $regex, $gt, $ne), CouchDB, inyección de comando Redis, omisión de autenticación a través de NoSQLi, volcado de datos. Úselo cuando el destino use MongoDB/Mongoose, CouchDB, Redis o muestre mensajes de error NoSQL.
