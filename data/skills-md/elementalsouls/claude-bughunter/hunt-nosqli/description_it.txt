Hunt NoSQL Injection: iniezione di operatori MongoDB ($where, $regex, $gt, $ne), CouchDB, iniezione di comandi Redis, bypass di autenticazione tramite NoSQLi, dump dei dati. Da utilizzare quando la destinazione utilizza MongoDB/Mongoose, CouchDB, Redis o mostra messaggi di errore NoSQL.
