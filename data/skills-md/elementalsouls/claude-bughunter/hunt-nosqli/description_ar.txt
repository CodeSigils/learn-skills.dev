Hunt NoSQL حقن - حقن مشغل MongoDB ($where، $regex، $gt، $ne)، CouchDB، حقن أوامر Redis، تجاوز المصادقة عبر NoSQLi، تفريغ البيانات. يُستخدم عندما يستخدم الهدف MongoDB/Mongoose، أو CouchDB، أو Redis، أو يظهر رسائل خطأ NoSQL.
