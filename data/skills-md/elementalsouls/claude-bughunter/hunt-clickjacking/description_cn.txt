狩猎点击劫持 - 缺少 X-Frame-Options / CSP 框架祖先让攻击者可以将目标页面嵌入到不可见的 iframe 中，并诱骗受害者单击他们看不到的按钮（UI 更正）。目标：登录流程、转账、帐户设置、OAuth 确认页面。通过获取页​​面进行确认，然后在真实的浏览器中证明它的框架，并且敏感的状态更改操作可以在跨站点上下文中幸存下来（SameSite cookies/framebusting JS 可以击败它）——仅标头缺失并不是一个发现。
