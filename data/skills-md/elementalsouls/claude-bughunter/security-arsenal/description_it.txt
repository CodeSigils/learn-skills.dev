Payload di sicurezza, tabelle di bypass, elenchi di parole, nomi di modelli gf, elenco di bug sempre rifiutati e tabella condizionatamente valida con catena. Da utilizzare quando sono necessari payload specifici per XSS/SSRF/SQLi/XXE/NoSQLi/command injection/SSTI/IDOR/path-traversal/HTTP contrabbando/WebSocket/MFA bypass, tecniche di bypass o per verificare se un risultato è inviabile. Utilizzare anche quando viene chiesto cosa NON inviare.
