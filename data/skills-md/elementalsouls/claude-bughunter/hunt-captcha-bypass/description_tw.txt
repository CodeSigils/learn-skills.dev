Hunt CAPTCHA 繞過 — 6 種不同的模式：(1) 請求中簡單地省略 CAPTCHA 欄位（缺少伺服器端驗證），(2) 從已解決的質詢中重播 CAPTCHA 令牌（無一次性強制執行），(3) 在與解決問題不同的端點上接受 CAPTCHA 回應（不綁定到操作/會話），可預測的 CAPTCHA或靜態或靜態或靜態或靜態或靜態值（例如「0」、「null」、空字串），(5)音訊/可訪問性驗證碼可以透過程式設計輕鬆解決，（6）驗證碼僅在 N 次失敗後強制執行（前 N 個請求繞過它）。檢測：攔截成功的表單提交，完全刪除驗證碼字段，重播 - 如果仍然成功，則伺服器端驗證不存在。中度嚴重程度獨立；當它刪除保護登入、註冊或付款端點的唯一速率限制門時為高。
