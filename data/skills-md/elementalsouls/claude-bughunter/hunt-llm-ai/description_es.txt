Busque errores en las funciones de LLM/AI: inyección rápida, inyección indirecta, exfiltración mediante el uso de herramientas, contrabando de ASCII, marco de seguridad de IA agente (ASI01-ASI10). Patrones: inyección directa de mensajes en la entrada del usuario (evite el mensaje del sistema con 'ignorar instrucciones anteriores'), inyección indirecta a través de documentos/páginas web que lee el modelo, contrabando de ASCII (bloque de etiquetas Unicode U+E0000-U+E007F invisible para los humanos, visible para el modelo), exfiltración de uso de herramientas (el modelo tiene la herramienta fetch_url, el atacante inyecta URL, el modelo exfila el historial de chat), extracción de mensajes del sistema (manipular el modelo para revelar instrucciones ocultas), extracción de datos de entrenamiento, IDOR-via-AI (el modelo lee los datos de otros usuarios a través de la confusión del sistema). Herramientas: chatbots, endpoints RAG, resumen, copilotos agentes. Detección: cualquier punto final respaldado por LLM, carga de documentos que activa el procesamiento de IA, agente autónomo con herramientas. Validar: fuga de datos entre usuarios, aviso del sistema revelado, demostración del uso de herramientas. Úselo cuando busque funciones de IA, chatbots, RAG y sistemas agentes.
