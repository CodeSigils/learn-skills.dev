Chụp 屏幕截图 màn hình vừa 构建 qua MCP và hỏi 用户 xác nhận ngắn gọn trước khi commit，thay vì tự động chấm điểm UI bằng 视觉模型（tốn token không） cần thiết cho quy mô alone dev）。该技能可用于 mcp-loop 构建并构建 git-checkpoint。
