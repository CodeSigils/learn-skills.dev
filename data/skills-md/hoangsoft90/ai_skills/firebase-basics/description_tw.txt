提供基礎 Firebase CLI 設定、CLI 安裝、版本檢查（`firebase-tools@latest --version`）、CLI 登入（包括 --no-localhost）、專案建立、專案選擇（`firebase use`）和應用程式設定檔下載（`google-services.json`、`GoogleService-Info.plist`）。僅用於 CLI 登入、專案建立/切換或下載應用程式設定檔。請勿用於 Firebase 託管部署、Firestore、驗證、應用程式託管、資料連線、Crashlytics 或遠端設定。
