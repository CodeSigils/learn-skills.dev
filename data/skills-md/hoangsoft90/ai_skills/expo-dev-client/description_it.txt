Quadro (OSS). Crea e distribuisci client di sviluppo Expo localmente o tramite TestFlight per test interni. Per le versioni di TestFlight in produzione e l'invio allo store, utilizza la competenza eas-app-stores.
