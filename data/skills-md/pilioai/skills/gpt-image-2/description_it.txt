Crea o modifica immagini con Pilio GPT Image 2 tramite l'API unificata per sviluppatori Pilio. Utilizzare quando l'utente desidera la generazione di testo in immagine, la modifica di immagini basata su prompt, il restyling, la trasformazione di foto di prodotto o la composizione da una o più immagini di riferimento locali.
