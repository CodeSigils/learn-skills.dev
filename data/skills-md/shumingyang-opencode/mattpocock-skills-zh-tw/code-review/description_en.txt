Review changes since a fixed point (commit, branch, tag, or merge base) along two axes—the standards axis (does the code conform to the coding standards recorded in this repo?) and the specifications axis (does the code meet the requirements of the original issue/specification?). Execute the two reviews in parallel sub‑agents and display them side by side. Used when the user wants to review a branch, PR, ongoing changes, or says “review since X”.
