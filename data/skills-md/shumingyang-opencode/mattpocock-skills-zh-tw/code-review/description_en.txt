Review changes along two axes since a fixed point (commit, branch, tag, or merge base)—the standard axis (does the code adhere to the coding standards recorded in this repo?) and the specification axis (does the code meet the original issue/specification requirements?). Run both reviews in parallel sub-agents and display them side by side. Use this when a user wants to review a branch, PR, ongoing changes, or says “review since X.”
