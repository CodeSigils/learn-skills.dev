deslop 管道的一次性每個儲存庫設置，通常由 /deslop 作為步驟 0 調用（如果 STACK.md 已存在，則跳過）。訪問使用者有關他們的堆疊（身份驗證、付款、後端樣式、資料庫、秘密、操作）；首先檢測它可以從倉庫中獲取什麼；並產生審計技能讀取的 STACK.md 內部覆蓋層。首次在儲存庫中使用時由 /deslop 自動觸發；當使用者的堆疊發生變更（切換身分驗證提供者、支付處理器、框架、資料庫）且 STACK.md 需要重新產生時直接呼叫。
