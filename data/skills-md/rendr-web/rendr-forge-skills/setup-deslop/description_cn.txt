deslop 管道的一次性每个存储库设置，通常由 /deslop 作为步骤 0 调用（如果 STACK.md 已存在，则跳过）。采访用户有关他们的堆栈（身份验证、支付、后端样式、数据库、秘密、操作）；首先检测它可以从仓库中获取什么；并生成审计技能读取的 STACK.md 内部覆盖层。首次在存储库中使用时由 /deslop 自动触发；当用户的堆栈发生更改（切换身份验证提供程序、支付处理器、框架、数据库）并且 STACK.md 需要重新生成时直接调用。
