Costruisci, ristruttura o revisiona le app SwiftUI utilizzando un’architettura MV native‑first con stato locale della vista, logica di business di proprietà del servizio, un DataStore basato su Observation per lo stato globale dell’app, codice riutilizzabile del DesignSystem, Risorse e Utilità. Usalo quando lavori sulla struttura del progetto SwiftUI, sostituendo i ViewModel predefiniti, organizzando Views/Models/Enums/Services/DataStore/DesignSystem/Resources/Utils, iniettando i servizi tramite Environment, creando uno stato dell’app @Observable o verificando i confini dell’architettura.
