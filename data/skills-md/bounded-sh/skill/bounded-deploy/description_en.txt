Ship and configure a Bounded app: the bounded CLI (init, verify, deploy, share, dashboard), multi-environment policy files, local live-edit deploys, custom domains and vanity slugs, and account/project config (bounded.json, account profiles, credentials, key safety). Use when deploying, releasing, sharing access, or configuring the project and its accounts. Part of the Bounded skill family; policy authoring lives in bounded-backend, client work in bounded-frontend.
