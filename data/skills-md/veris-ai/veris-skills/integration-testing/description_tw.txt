針對 Veris 依賴項沙箱（而不是真正的供應商）執行儲存庫的整合測試，並使用 veris-proxy 在核心層級重新路由程式碼的出站 HTTP(S)。驗證先決條件（API 金鑰、Veris MCP 伺服器、veris-proxy 二進位檔案、docker），然後使用一個命令在代理旁邊的容器中運行測試，該命令部署每次運行的沙箱，並證明沙箱在信任任何綠色之前確實收到了流量。当与外部服务交互的代码需要在更改调用完成之前验证其集成行为时使用。
