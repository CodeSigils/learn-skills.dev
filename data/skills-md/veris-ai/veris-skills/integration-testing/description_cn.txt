针对 Veris 依赖项沙箱（而不是真正的供应商）运行存储库的集成测试，并使用 veris-proxy 在内核级别重新路由代码的出站 HTTP(S)。验证先决条件（API 密钥、Veris MCP 服务器、veris-proxy 二进制文件、docker），然后使用一个命令在代理旁边的容器中运行测试，该命令部署每次运行的沙箱，并证明沙箱在信任任何绿色之前确实收到了流量。当与外部服务交互的代码需要在更改调用完成之前验证其集成行为时使用。
