将原始客户代理存储库与 Veris 端到端集成。安裝或驗證 veris-cli、登入、建立或重複使用 Veris 環境、分析儲存庫、產生或更新 `.veris/veris.yaml`、`.veris/Dockerfile.sandbox`、`.veris/.dockerignore`、設定執行階段環境變數，並可使用 `veris env Push` 完成。当存储库尚未设置 Veris 时，或者现有的 `.veris/` 集成已过时且需要刷新时，请使用。
