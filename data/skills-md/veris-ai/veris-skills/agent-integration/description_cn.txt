将原始客户代理存储库与 Veris 端到端集成。安装或验证 veris-cli、登录、创建或重用 Veris 环境、分析存储库、生成或更新 `.veris/veris.yaml`、`.veris/Dockerfile.sandbox`、`.veris/.dockerignore`、配置运行时环境变量，并可以使用 `veris env Push` 完成。当存储库尚未设置 Veris 时，或者现有的 `.veris/` 集成已过时且需要刷新时，请使用。
