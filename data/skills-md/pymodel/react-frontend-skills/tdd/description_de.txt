Test-Driven Development-Methodik und Red‑Green‑Refactor‑Arbeitsablauf (früher test‑tdd). Diese Fähigkeit sollte verwendet werden, wenn TDD praktiziert wird, Tests zuerst geschrieben werden, Tests vor der Implementierung entworfen werden oder test‑first‑Ansätze überprüft werden. Auslöser: „write tests first“, „test before code“, „red green refactor“, „test driven development“. Diese Fähigkeit deckt KEINE spezifischen Details des Vitest‑Frameworks ab (verwende die Vitest‑Fähigkeit) oder das Mocken von APIs mit MSW (verwende die MSW‑Fähigkeit). Bitte immer in der Sprache des Benutzers antworten.
