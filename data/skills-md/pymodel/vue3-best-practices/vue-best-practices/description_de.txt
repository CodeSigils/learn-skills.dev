MUSS für Vue.js‑Aufgaben verwendet werden. Empfiehlt dringend die Composition API mit `<script setup>` und TypeScript als Standardansatz. Deckt Vue 3, SSR, Volar, vue‑tsc ab. Laden für jedes Vue‑Projekt, .vue‑Dateien, Vue Router, Pinia oder Vite mit Vue‑Arbeiten. VERWENDE IMMER die Composition API, es sei denn, das Projekt erfordert ausdrücklich die Options API.

Bitte immer in der Sprache des Benutzers antworten.
