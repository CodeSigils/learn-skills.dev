Initialize or refactor a project with the modern Python toolchain (uv, ruff, ty): generate/adjust pyproject.toml, local check commands, pre-commit and CI templates; read the What's New for each version from 3.12 onward, stacking up to the project's minimum version (default >=3.12, preferably the latest stable micro), to exploit new features. It should be loaded when the user mentions "new Python project", "write a standalone script that stays maintainable", "unify lint/format/type checking", or talks about uv/ruff/ty/Python engineering; aligned with tech-preferences' Python baseline, this skill handles the concrete steps and file contents.
