Initialize or refactor projects with the modern Python toolchain (uv, ruff, ty): generate/adjust pyproject.toml, local check commands, pre-commit and CI templates; starting from 3.12, read each version’s What’s New in order to use new features according to the project’s minimum version (default ≥3.12, preferably the latest stable minor). Should be loaded when “creating a new Python project”, “writing maintainable standalone scripts”, “unifying lint/format/type checks”, or the user mentions uv/ruff/ty/Python engineering; consistent with the Python baseline in tech-preferences, this skill is responsible for the implementation steps and file contents.
