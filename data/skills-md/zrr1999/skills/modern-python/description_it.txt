Inizializza o modernizza i progetti con la moderna toolchain Python (uv, ruff, ty): genera/regola pyproject.toml, comandi di controllo locale, pre-commit e modelli CI; a partire dalla 3.12, leggere le Novità di ciascuna versione in modo da sfruttare le nuove funzionalità in base alla versione minima del progetto (default >=3.12, preferibilmente l'ultima micro stabile). Dovrebbe essere caricato quando l'utente dice "nuovo progetto Python", "scrivi uno script autonomo gestibile", "unifica controllo lint/formato/tipo" o menziona uv/ruff/ty/Python engineering; si allinea con la linea di base Python delle preferenze tecniche, questa abilità gestisce i passaggi di implementazione e il contenuto dei file.
