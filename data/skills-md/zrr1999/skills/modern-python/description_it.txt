Inizializza o rifattorizza i progetti con la moderna toolchain Python (uv, ruff, ty): genera/regola pyproject.toml, comandi di controllo locale, pre-commit e modelli CI; a partire dalla 3.12, leggere le Novità di ogni versione per utilizzare le nuove funzionalità in base alla versione minima del progetto (default ≥ 3.12, preferibilmente l'ultima minore stabile). Dovrebbe essere caricato quando si "crea un nuovo progetto Python", "si scrivono script autonomi manutenibili", "si unificano i controlli lint/formato/tipo" o l'utente menziona uv/ruff/ty/Python engineering; coerente con la linea di base di Python nelle preferenze tecniche, questa abilità è responsabile delle fasi di implementazione e del contenuto dei file.
