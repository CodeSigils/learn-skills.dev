La superficie dell'API REST HTTP agentmemory, il protocollo primario per comunicare con il server di memoria. Da utilizzare quando si chiama agentmemory su HTTP, quando MCP non è disponibile ed è necessario un fallback o quando si integra un host che non parla MCP.
