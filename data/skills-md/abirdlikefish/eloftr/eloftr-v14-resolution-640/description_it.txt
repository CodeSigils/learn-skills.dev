EfficientLoFTR v14 = v13 多维优化版 (训练分辨率 832->640 + 多项工程加速),
cfg 继承 v13 ddp + 7 项 override, sh 改 7 处. v14 vs v13 是干净的训练
分辨率 ablazione——训练 impronta digitale 仅由 IMG_RESIZE 主导, 其他都是"语义
等效" (反向缩放保 TRUE_LR/passo WARMUP effettivo 不变, pianificazione 等比例放大保
sample-pass 总量不变) 或"工程加速" (val 协议跟 eval bat 一致, TB 数据
精简 80%).

v14 12 项改动 / 5 组逻辑动机 (bug NPE 已删除, 详见 v14 cfg "bug NPE
post-mortem"; 历史上 v14 初版误加 NPE [832,832,640,640] 让 RoPE 跨分辨率
rapporto di allungamento=1,3, 实测 ep0 auc@10=0,214 < outdoor.ckpt linea di base 0,30+,
根因 stretch 是 estrapolazione 用的, v14 lato_lungo 640 < 832 是 interpolazione
不该 allungare; 修复后 v14 不显式设 NPE di train.py:130 fallback
[832,832,832,832] 跟 v0-v13 一致):
1. 核心 ablazione 变量 (1 项): MGDPT_IMG_RESIZE 832 -> 640 (in data cfg
   mega Depth_syn_pose_640.py, NON cfg principale, 因 train.py:127 merge 顺序
   data_cfg AFTER main_cfg, main 设 IMG_RESIZE (会被覆盖)
2. bs + LR/WARMUP 反向缩放 (3 项): bs 2->4, CANONICAL_LR 1e-3->5e-4,
   WARMUP_STEP 225->450 (TRUE_LR=1.25e-4 / passo WARMUP effettivo=1800 ·
   v13/v10/v9 完全一致)
3. val 加速 (2 项): EVAL_TIMES 5->1 (跟 eval_metu_vistir_finetuned.bat
   --ransac_times 1 协议完全一致, best-ckpt 选择 trend 不受影响),
   --limit_val_batches 0.5->0.2 (val 实跑 4493*0.2=899 coppia, 跟 LoFTR
   mega Depth_val_1500 业界 1500 paia 量级一致)
4. N + programma 等比例放大 (4 项): N_SAMPLES_PER_SUBSET 200->100 (LoFTR
   carta predefinita), max_ep 12->18 (N 砍半 -> ep 翻倍补偿 + ES 余地),
   MSLR_MILESTONES [3,5,7]->[6,10,14] (pass campione 角度等效),
   EARLY_STOPPING_PATIENCE 3->5 (ep 等比例略压: 3/12=25% vs 5/18=28%);
   总 sample-pass v14 ~156K vedi v13 ES 早停后实际 ~158K 等效
5. TB 精简 (2 项): ENABLE_PLOTTING True->False (lightning_loftr.py:562
   figura del treno 关掉, figura della val 不受影响, eventi 1,1 GB->220 MB),
   --log_every_n_steps 50->500 (scalare 10x 稀疏 + step 加速 3-5%)

显存预算修正 (基于 v13 nave 实测 832+bs=2 = 20,2 GB / unità, +4 GB rispetto al piano):
  Area di lavoro benchmark cuDNN + buffer bucket DDP NCCL 实测吃 +4 GB.
  v14 640+bs=4: sim_matrix inferiore al 29% (0,93->0,66 GB), inferiore a bs, inferiore a 4 GB
  spazio di lavoro, capacità ~ 17 GB / tempo (rispetto a 24 GB), meno 7 GB, capacità.

5 个新建文件 (零 src/ 改动):
(1) configs/data/mega Depth_syn_pose_640.py (~80 anni, da mega Depth_syn_pose_
    trainval.py copia 仅改 MGDPT_IMG_RESIZE 832->640 + docstring)
(2) configs/loftr/eloftr_full_v14_pose_msyn_ddp.py (~115 行, 继承 v13 ddp
    + 8 项 cfg overrides, docstring 含 v13 实测曲线 + v14 schedule 推算)
(3) configs/loftr/eloftr_full_v14_pose_msyn_singlecard.py (~23 anni, 继承
    v14 ddp + 单卡反向缩放 CANONICAL_LR=2e-3 / WARMUP_STEP=1800)
(4) MyScripts/run_msyn_v14_pose_ddp.sh (~110 anni, versione v13 ddp.sh a 7 anni:
    data_cfg / main_cfg / nome_exp / dimensione_batch 2->4 /
    limit_val_batches 0,5->0,2 / max_epochs 12->18 /
    log_ogni_n_passi 50->500)
(5) MyScripts/run_msyn_v14_pose_singlecard.sh (~56 anni, versione v13 singlecard
    sh改4处)

Spedizione 状态 (14-05-2026): NON ANCORA spedita, solo piano.
cfg + sh 已 commit/未 commit 见 git status (路径全部 ?? untracked 至本
abilità 创建时刻). 等 v13 nave 完成 (剩 1-2h ES 即将触发) + 跑 METU eval
拿数字, 再决定 v14 是否上.

v13 实测对照基础 (作为 cancello di accettazione v14 起点; 见
.cursor/skills/eloftr-v13-pose/SKILL.md):
  v13 migliore ep=6, auc@10=0,4096, auc@5=0,2508, auc@20=0,5730
  v13 orologio da parete 9 ep x 167 min = 25h
  eventi v13.tfevents 1,1 GB

Orologio da parete v14 推算:
  sim_matrix 单 picco batch: 832@bs=2 0,93 GB -> 640@bs=4 0,66 GB (-29%)
  tempo passo singolo: v13 ~1,0 s/passo (bs=2 832) -> v14 ~0,83 s/passo
    (bs=4 640 + 关 cifra treno -5% + log 频率砍 10x -3%)
  treno per ep: 167 min @v13 -> ~32 min @v14 (-81%, N 砍半 + bs 翻倍)
  val per ep: 23 min @v13 -> ~1,4 min @v14 (-94%, 640+1x RANSAC+0,2 limite)
  ep totali: 167 minuti @v13 -> ~33 minuti @v14
  v14 估 17 ep x 33 min ~= 9h (~2.8x 加速 vs v13 25h)
  events.tfevents: 1,1 GB @v13 -> ~220 MB @v14 (-80%)

pianificazione v14 推算 (sample-pass 等效 v13 ES 早停后):
  v13 miglior ep=6 (passaggio campione 6 * passo 4400 * bs=4 = 105.000 / rango)
    -> v14 miglior ep ~12 (passa campione 12 * 2300 * bs=4 = 110K / rango, 等效)
  v13 ES 触发 ep~9 -> v14 ES 触发 ep~15-17, max_ep 18 给 ES 留余地

理论预期 (METU all auc@20 vs v13):
  消除 832->640 mancata corrispondenza treno/valutazione (NPE 频率自动校准 + attenzione AGG
    modello spaziale + fine_window 物理覆盖率): +5~15% relativo
  640 anni di attività 832 giorni: -5% relativo
  N=100 passaggi campione 略减: -2% relativo
  净 -2 ~ +8% auc relativo@20 (大概率持平或微升)

Cancello di accettazione (val auc@10 monitor + METU all auc@20):
  Forte: val auc@10 >= 0,415 (v13 0,4096 + 0,005 ass / +1,2% rel);
           METU all auc@20 >= v13 + 0.5pp -> 论文 "分辨率 ablazione 成功"
  Medio: val auc@10 in [0,405, 0,415); METU +- 0,5pp ->
           832/640
  Debole: val auc@10 < 0,405; METU < v13 - 1.0pp -> 回退 v13 832 percorso
  Crash: NaN / METU auc 远 < outdoor.ckpt -> NPE 设错 / cfg merge
           顺序错; 查 train.py:129-130

风险点:
- Bug NPE 已修复 (post-mortem): v14 初版加了 cfg.LOFTR.COARSE.NPE =
  [832,832,640,640] 想跨分辨率校准, rapporto di allungamento = 1,3 让 RoPE
  fase di posizione 错位 50%, ep0 auc@10=0,214 < linea di base outdoor.ckpt 0,30+.
  Testo: position_encoding.py:21-22 的 train_res / test_res stretch Testo
  estrapolazione 用的 (input > train, es. LoFTR paper MegaDepth1500 1152
  ingresso vs treno 832); v14 lato_lungo 640 < 832 是 interpolazione, intero
  posizione (1..20) 是 outdoor.ckpt 学过 (1..26) 的子集, attenzione 兼容
  良好, 不该 allungamento. Nome: cfg.LOFTR.COARSE.NPE formato, nome
  train.py:130 fallback [832.832.832.832] (rapporto=1,0). · v14 / 衍生
  版本前再次检查 cfg 是否误加 NPE override.
- unione cfg dati 顺序: v14 ddp main cfg 不要也设 MGDPT_IMG_RESIZE=640
  (会被 data cfg 覆盖, audit 时困惑; 在 main cfg 里设 IMG_RESIZE 是 dead
  codice).
- outdoor.ckpt 加载兼容: outdoor.ckpt 是 832 treno, buffer RoPE sin/cos
  按 max_shape=(256,256) + npe=[832,832,832,832] 算. NPE di riserva v14
  跟 outdoor.ckpt 完全一致 (都是 [832,832,832,832] 不 stretch),
  Register_buffer(persistent=False) 不入 ckpt (position_encoding.py:37-38),
  buffer NPE di fallback v14, 完全兼容.
- bs=4 avanzamento: 832+bs=4 OOM, 640+bs=4 avanzamento -29% sim_matrix picco +
  4 GB cuDNN/NCCL 实测预期 ~17 GB / giorno, o 7 GB 余量, 安全. 首次跑前
  nvidia-smi 看稳态值.
- Velocità GPU: versione v13 GPU 0/2/3: 81-87 C (GPU 2 velocità 88 C termica
  throttling 阈值), GPU 1: 66 C 散热好. 当 GPU 2 触发 acceleratore, 整组
  NCCL all_reduce 等最慢 rango 拖慢 5-10%. v14 跑前可选 sudo nvidia-smi
  -i 2 -pl 280 限 Potenza GPU 2 87->78 C, tempo di passo -5% 但避免 acceleratore
  净持平.

Da utilizzare durante l'esecuzione/il debug/l'estensione di v14 / 解释 ablazione v14 vs v13 /
832 vs 640 / NPE 跨分辨率校准机制 / 显存预算修正 / sim_matrix
bs=4 在 640 percorso 安全 / TB 数据精简 ENABLE_PLOTTING / N_SAMPLES pianificazione
等比例放大 / 写论文分辨率 ablazione 章节 / v14 ckpt 选择 / METU eval v14
ckpt 决定派生.

Trigger: v14 / v14_pose / v14_length_640 / msyn_v14_pose_ddp /
IMG_RESIZE 832 -> 640 / Bug NPE post-mortem / Interpolazione stretch NPE
vs estrapolazione/fallback NPE [832.832.832.832]/position_encoding.py
train_res test_res / mega Depth_syn_pose_640.py / sim_matrix 砍 29% /
bs 2 -> 4 反向缩放 / CANONICAL_LR 1e-3 -> 5e-4 / WARMUP_STEP 225 -> 450 /
TRUE_LR 1.25e-4 不变 / EVAL_TIMES 5 -> 1 / limit_val_batches 0.5 -> 0.2 /
N_SAMPLES_PER_SUBSET 200 -> 100 (predefinito carta LoFTR) / max_ep 12 -> 18 /
MSLR [3,5,7] -> [6,10,14] / ES pazienza 3 -> 5 / ENABLE_PLOTTING Falso /
log_every_n_steps 50 -> 500 / eventi 1.1GB -> 220MB / orologio da parete 25h ->
9 ore / Throttling termico GPU 2 / 12 GB / 5 GB / memoria 17 GB /
卡 / METU 640 协议 treno/partita di valutazione / LWIR termico nativo 640 假高频问题,
Inglese 'risoluzione ablazione da 832 a 640', 'NPE RoPE a risoluzione incrociata
calibrazione', 'sim_matrix 4-esimo ridimensionamento della risoluzione di potenza', 'scala inversa
LR/WARMUP mantiene TRUE_LR invariante', 'N_SAMPLES pianificazione equiproporzionale
scale-up', 'ENABLE_PLOTTING True to False salva l'80% degli eventi',
"13 modifiche / 6 gruppi logici", "spedizione solo piano in sospeso".

v14 = percorso J 衍生 ablazione (训练分辨率维度), 不破坏 v13 percorso J 的核心
posa intermodale 监督定位 (训练 impronta digitale 仅由 IMG_RESIZE 主导).
Grafico dell'ereditarietà:
  eloftr_full.py (base di riferimento v0)
    -> v13 (posa B-pura 监督, 832, bs=2, eloftr-v13-pose)
      -> v14 (QUESTA abilità, 640 + 工程加速, bs=4)

Compagno:
  eloftr-v13-pose (cancello di accettazione genitore / v14 起点 0.4096)
  eloftr-mega Depth-syn-data (SOP set di dati Mega Depth_Syn, v14 复用)
  eloftr-cross-modal-experiments (catena 路线总览; v14 是 v13 衍生)
  eloftr-server-multigpu (runtime DDP + 5 trap)
  eloftr-eval-pipeline (eval_metu_vistir_finetuned.bat 协议; v14 ckpt
    待跑该 pipistrello)
  eloftr-results (跨版本数字总表; v14 数字待 backfill)
  eloftr-tb-summary (训练侧 KPI 总表; v14 wall-clock / best_ep / 待
    riempimento)
  eloftr-v15-pose-contrast (v14 的进一步 ablazione 衍生, v14 + v1
    perdita contrastiva, 1 项改动)
  eloftr-v17-llvip (v14 的 finetune 派生, v14 best ckpt + LLVIP 像素对齐
    数据集 + aggressivo single-side H aug + Schedule V2; Non è una messa a punto precisa
    dalla v14 而不是 cold start outdoor.ckpt 的 v* 实验)
