(NS) 在一个会话中运行完整的线束初始化后 AI 引导程序 — 架构规则、线束同步、棕地地图、业务反向规范和项目 AGENTS.md。每当用户说harness 准备、安装后设置、在harness 初始化后引导此项目、板载brownfield 存储库、运行完整的项目准备或希望按顺序使用所有brownfield 技能而无需手动调用每个斜线命令时，即可使用 - 即使它们将 ns-harness-architecture-rules、ns-harness-bootstrap-brownfield、ns-harness-codebase-reverse-spec 和 ns-harness-agents-md 列为单独的后续步骤。请勿用于尚无应用程序代码的绿地存储库、单技能运行（直接调用工作人员技能）或仅限 CLI 的基线 AGENTS.md（使用harnessagents-md）。
