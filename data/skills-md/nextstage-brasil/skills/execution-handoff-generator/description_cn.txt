(NS) 为计划的 SDD 版本生成并更新execution-handoff.md — 操作提示、每个任务状态表以及以秒为单位的时间跟踪。在所有task-NNN-*.md 文件存在后的规划结束时、任务存在但缺少切换时、或者在版本实施期间更新任务状态时使用。不要用于没有版本生命周期、版本分区 (version-partitioner) 或仅 GitLab 问题执行 (execute-gitlab-issue) 的临时编码。
