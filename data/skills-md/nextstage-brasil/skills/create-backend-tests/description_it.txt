(NS) Crea e mantiene test unitari e di integrazione PHPUnit nel pacchetto backend (test bootstrap/quando mancanti). Esecuzione obbligatoria solo su Docker con phpunit --testdox --stop-on-failure --stop-on-error e timeout di 120 secondi. Da utilizzare durante la scrittura o il refactoring di test PHPUnit, l'implementazione di attività di test unitari o lo scaffolding di test di backend su progetti Laravel/PHP greenfield, non quando si pianifica il markdown delle attività di test (utilizzare unit-test-task-generator). Leggere le regole dei test backend del cablaggio quando presenti.
