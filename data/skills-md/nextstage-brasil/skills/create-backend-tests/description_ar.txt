(NS) إنشاء وصيانة وحدة PHPUnit واختبارات التكامل في حزمة الواجهة الخلفية (اختبارات التمهيد/ في حالة فقدانها). التنفيذ الإلزامي لـ Docker فقط باستخدام phpunit --testdox --stop-on-failure --stop-on-error ومهلة 120 ثانية. يُستخدم عند كتابة اختبارات PHPUnit أو إعادة هيكلتها، أو تنفيذ مهام اختبار الوحدة، أو اختبارات الواجهة الخلفية على مشاريع Laravel/PHP ذات الحقول الخضراء - وليس عند التخطيط لتخفيض مهمة الاختبار (استخدم مولد مهمة اختبار الوحدة). اقرأ قواعد اختبارات الواجهة الخلفية عند وجودها.
