(NS) Esegui un problema GitLab end-to-end: alberi di lavoro, implementazione, commit atomici, MR, sincronizzazione dello stato MCP, gate di revisione del codice obbligatorio. Da utilizzare quando l'utente fornisce un GitLab ISSUE_URL o chiede di implementare direttamente un problema GitLab, non per la codifica ad hoc solo locale (usa il codificatore). Richiede mcp-gitlab-usage per MCP e code-reviewer per Phase 5 gate.
