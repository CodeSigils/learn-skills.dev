(NS) Execute a GitLab issue end-to-end — worktrees, implementation, atomic commits, MRs, MCP status sync, mandatory code review gate. Use when the user provides a GitLab ISSUE_URL or asks to implement a GitLab issue directly — not for local-only ad-hoc coding (use coder). Requires mcp-gitlab-usage for MCP and code-reviewer for Phase 5 gate.
