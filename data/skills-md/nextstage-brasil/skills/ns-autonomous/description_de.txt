(NS) Autonome Ausführungs-Engine – plant Tiefe, löst Zweifel aus Dokumenten/Spezifikationen, Multi-Agent-Arbeit in einem isolierten Arbeitsbaum. Für einen lokalen/eingefügten Plan verwenden oder diesen ohne GitLab „autonom ausführen“. Auch Phase 2 unter ns-execution-gitlab-issue (dann nicht eigenständig ausführen). Nicht für Einzelschrittbearbeitungen (ns-coder) oder GitLab-Probleme direkt (ns-execution-gitlab-issue).
