(NS) Diagnosi della causa principale da errori, log, analisi dello stack, test non riusciti o CI: priorità di ingresso 4 quando l'utente desidera la diagnosi SENZA implementare ("perché X si rompe?"). Utilizzare in modo proattivo in caso di errori di test/CI non chiari se non è stato ancora chiesto di risolverli. NON utilizzare per l'implementazione/correzione (ns-coder), l'ambito della versione (ns-spec-driven), l'esecuzione di GitLab ISSUE_URL o la revisione pre-unione (ns-reviewer).
