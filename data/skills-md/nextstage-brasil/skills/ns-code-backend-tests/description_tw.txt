(NS) 在後端套件中建立和維護 PHPUnit 單元和整合測試（引導測試/缺失時）。使用 phpunit --testdox --stop-on-failure --stop-on-error 和 120 秒逾時強制僅執行 Docker。在編寫或重構 PHPUnit 測試、實作單元測試任務或在新建 Laravel/PHP 專案上建置後端測試時使用 - 而不是在規劃測試任務 markdown 時使用（使用 ns-pm-unit-test-task-generator）。閱讀線束後端測試規則（如果存在）。
