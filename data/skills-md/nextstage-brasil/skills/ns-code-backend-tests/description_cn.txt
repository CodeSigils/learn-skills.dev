(NS) 在后端包中创建和维护 PHPUnit 单元和集成测试（引导测试/缺失时）。使用 phpunit --testdox --stop-on-failure --stop-on-error 和 120 秒超时强制仅执行 Docker。在编写或重构 PHPUnit 测试、实现单元测试任务或在新建 Laravel/PHP 项目上搭建后端测试时使用 - 而不是在计划测试任务 markdown 时使用（使用 ns-pm-unit-test-task-generator）。阅读线束后端测试规则（如果存在）。
