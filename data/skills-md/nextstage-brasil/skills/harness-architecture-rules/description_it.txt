(NS) Scansiona una base di codice e genera o aggiorna una costituzione snella architecture-rules.md per gli agenti AI: stack, layout, modelli, vincoli e puntatori a regole più profonde. Da utilizzare ogni volta che l'utente richiede di generare regole di architettura, creare .nextstage-harness/rules/architecture-rules.md, contesto dell'agente bootstrap, codificare convenzioni di progetto, stack di documenti e layout del modulo per gli agenti o impostare regole di progetto sempre attive, anche se dicono "documenta come funziona questo repository per l'intelligenza artificiale" o "crea regole che l'agente legge ogni sessione". NON utilizzare per specifiche aziendali/funzionali (harness-codebase-reverse-spec), mappe brownfield SDD (harness-bootstrap-brownfield) o regole cursore ad argomento singolo ad hoc senza un pass architettura completo.
