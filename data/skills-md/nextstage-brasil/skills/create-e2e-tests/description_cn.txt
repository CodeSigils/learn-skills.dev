(NS) Create and maintain Cypress E2E tests with TypeScript using DRY device-aware command architecture (shared/pages/device).在编写或重构 Cypress 规范、自定义命令或实现 E2E 任务时使用，而不是在规划 E2E 任务降价时使用（使用 e2e-test-generator）。如果存在，请阅读线束 e2e 规则。在编写规范之前必须进行发现。
