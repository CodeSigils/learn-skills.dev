(NS) Generate E2E test task markdown for Cypress/TypeScript during SDD planning after frontend tasks exist. Use when planning Cypress coverage per feature — not for writing specs (use create-e2e-tests). Requires data-testid contract from frontend task. Device-aware desktop/tablet/mobile structure.
