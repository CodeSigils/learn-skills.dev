Ottieni i record ripetuti da una pagina Web (griglie di prodotti, risultati di ricerca, elenchi di lavori, feed di notizie, tabelle) come JSON, senza scrivere selettori CSS e senza spendere una chiamata modello per leggere l'HTML. Funziona su siti senza API, compresi quelli protetti da login o protezione bot. Funziona localmente, un binario, nessuna chiave API. Da utilizzare quando l'utente dice "scrape", "estrai l'elenco", ottieni tutti i prodotti/risultati/post da, trasforma questa pagina in JSON o richiede dati da un sito che non dispone di API.
