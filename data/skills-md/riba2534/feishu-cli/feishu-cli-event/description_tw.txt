飛書即時事件訂閱（WebSocket）。 `event list`顯示支援的EventKeys； `event schema` 顯示事件負載/範圍； `event Consumer` 啟動一個長期存在的連線訂閱，將事件流以 NDJSON 的形式寫入標準輸出（阻塞，一個程序訂閱一個 EventKey）； “事件狀態”顯示本地機器上活動的消費進程；`event stop`` `` s```ID-- Key 1` 消費進程；`event stop`` 1` 」表 ``ID ``ID(支援22+事件鍵（IM訊息接收/讀取/召回/反應、群組成員變更、聯絡人員工變更、行事曆變更、雲端磁碟標題/協作者、審核實例和任務、VC會議開始/結束）。支援斷線重連，支援多個EventKey並行訂閱。當使用者要求「監聽飛書事件」、「即時接收訊息事件」、「訂閱核准回呼」、「事件流」、「WebSocket長連線監聽」、「事件消耗」、「事件清單/模式/狀態/停止」或「AI Agent即時回應」時使用。注意：此功能僅處理訂閱；處理事件 webhook 業務邏輯（推送到飛書訊息/寫入 Bitable）應該使用 `feishu-cli-msg` / `feishu-cli-bitable` 來完成。
