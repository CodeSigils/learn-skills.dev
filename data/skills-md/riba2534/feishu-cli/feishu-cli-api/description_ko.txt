Feishu OpenAPI 원시 호출. `api GET/POST/PUT/DELETE/PATCH <path>`는 feishu‑cli가 아직 래핑하지 않은 인터페이스를 포함하는 모든 Feishu OpenAPI 엔드포인트를 직접 호출합니다(lark‑cli의 API 기능에 맞춰 조정됨). `--params`(쿼리), `--data`(JSON 본문), `--data-file`(파일에서 본문 읽기), `--as auto|user|bot`(ID), `--dry-run`(미리 보기), `-o`(바이너리 다운로드), `--format` 및 `--jq`를 지원합니다. 사용자가 "X API 호출", "Feishu 엔드포인트 원시 호출", "feishu‑cli로 래핑되지 않은 인터페이스 호출 방법", "원시 API", "api 명령으로 요청 보내기" 또는 "Feishu 미디어/파일 바이너리 다운로드"를 요청할 때 사용합니다. 해당 없음: 호출하지 않고 스키마만 확인합니다(`feishu-cli 스키마` 사용). 이미 전용 명령이 있는 고주파 시나리오(해당 `feishu-cli <모듈>` 사용)
