Feishu Bitable (Bitable/Base) Full-Feature Operations: create bases, manage tables, manage fields, add/edit/delete/view records (single & batch), manage views, search/filter/sort. Use when the user requests "create base", "operate table", "add record", "query record", "manage fields", "bitable", "base", "table". Also applies to: scenarios where users need to create structured databases, batch-import data, manage table fields and views. Note: for spreadsheets (Sheets) use feishu-cli-toolkit; they are different products.
